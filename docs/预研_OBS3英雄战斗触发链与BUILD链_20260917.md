# 预研: OBS-3 英雄战斗触发链 + BUILD 动作链 (09-17 只读分析, 训练期完成)

方法: WSL vcmi-native 源码只读 grep (子代理×2) + 引擎代码亲自闭环验证 + 历史日志对照。零修改零重编。

## §1 英雄 vs 英雄战斗触发链 (批次B 判据②③可达性)

### 1.1 引擎层: 敌对英雄同格相遇 = 必然 FIGHT (好消息, 原版逻辑无 fork 风险)

| 环节 | 位置 (vcmi-native) | 结论 |
|---|---|---|
| moveHero 入口 | server/CGameHandler.cpp:856 | 目标格 visitableObjects 非空 → objectToVisit=敌英雄 |
| 障碍判定 | 同上 :916 | `movingOntoObstacle = t.blocked() && !t.visitable()` — **敌英雄格 blocked+visitable → false, 不拒绝** ("destination tile is blocked" :950 只给岩石等真障碍; #143 实测被拒应是动作层/旧版行为, 引擎层无此闸门) |
| blockingVisit | 同上 :1020-1036, 调用点 :1062/:1100 | 敌英雄 isBlockedVisitable()=true (CGHeroInstance.cpp:114 恒真) && !passableFor → **doMove(BLOCKING_VISIT)** |
| 访问分发 | 同上 :3869→:3608-3629 | onHeroVisit; 英雄在城里则转攻城战 (:3613) |
| 相遇决策 | lib/mapObjects/CGHeroInstance.cpp:551-575 | ENEMIES → **startBattle 无条件, 无战力评估**; 非敌对→heroExchange。JOIN/FLEE 的 takenAction 只存在于 CGCreature (:355-410), 英雄路径完全没有 |
| 先后手 | startBattle :4530 | 先动方=ATTACKER, 被访方=DEFENDER (享城防) |

### 1.2 真正的闸门: 观测 passable 通道 (这就是"真实击杀=0"的最终机械根因)

- obs[3211:3219] passable 通道把**敌英雄格标 0** (知识库"贴脸"口径) → 三重封锁:
  1. 模型方向动作: ep_runner L478-481 `logits[:8][~passable]=-inf` → 踏向敌格的方向永不被采样
  2. MOVE_TO/BFS: passable=0 不入 BFS → 引导只到邻格
  3. 蓝方 AI: AAI.cpp:109-111 nearestInteractable **显式跳过 HERO** 且不选敌占对象 → 蓝英雄不会主动进攻; 唯一战斗通道 = 蓝 AI 目标格恰被红英雄占据的"间接撞上" (概率极低且不可控)
- 结论: **模型/引导/对手三方全部结构性无法踏入敌英雄格 → 真实战斗 (含攻城战, 城格同样 pas=0) 在当前观测下永不可触发** → WIN-1 "真实击杀=0"、TOWN_CAPTURE=0、批次B 判据②③ 恒 0, 全部由此闭环解释
- 战斗内层再核过: BAI 主动投降评估 `#ifndef ENABLE_ML` 被编译排除 (BAI.cpp:380-389), mlbot 需 GRAIL 不触发; 唯一逃走通道 = v13 模型 RETREAT 动作 (野战默认可 flee) — 不影响"是否开战"

### 1.3 对批次B 的影响与预案方向

- **contact_d=2 贴脸奖价值不变** (击杀链塑形第一环: 先学会贴脸); 但"贴脸→战斗"最后一跳被 passable 闸门挡住
- **预案 (C++ 最小面)**: `ML/strategic_state.cpp` passable 填充处对"敌英雄格/敌城格"特判——**这不是改 obs 语义, 是修正观测错误** (引擎里该格实际可走且触发战斗, passable=0 才是与引擎不符的值); 维度零变更, 但属引擎侧改动 (重编 strategic_state/lib) → 触铁律, 单独立项, 等批次B 窗收口后拍板
- 备选: 专用攻击动作通道 (动作空间侵入, 更重, 不推荐先做)

### 1.4 立项材料: passable 闸门 C++ 预案 (09-17 补, 待拍板)

- **改动点定位**: `ML/strategic_state.cpp` 两处填充 — L698-700 (`state->passable[d] = tt->isClear(hposTile)`) 与 L955-974 (同口径第二副本); 敌英雄/敌城格 isClear=false → passable=0。L83 注释记录 #143 "moveHero 全被拒"真因 = 当年 passable 围绕蓝英雄计算的坐标系 bug (已修), 与本预案无冲突
- **方案甲 (口径对齐, 推荐)**: 判定改 `tt->blocked() && !tt->visitable() ? 0 : 1` — 与 GHandler `movingOntoObstacle` (CGameHandler.cpp:916) **逐字对齐**, 敌英雄格/敌城格/怪格全部开放。影响面: ①模型获得"踏上敌格/怪格"新动作面 (contact+15/守卫+100/kill+40 上行驱动 vs death_penalty -50 下行, 激励闭环) ②顺带修复 #103 "模型绕开守卫" (怪格开放) ③怪格开放 = 模型可能冲进打不过的怪 (战力评估缺位 → death 罚 + 探索成本, 中期靠 P-H3/守卫奖塑形)
- **方案乙 (最小特判)**: isClear 口径不动, 仅对"敌方英雄格/敌方城格"例外置 1。影响面最小 (只开击杀链), 但怪格 #103 依旧、口径与引擎仍有偏差
- **验证方案**: 开窗后观察 ① 首个真实战斗事件 (引擎 battleStarted/战斗结果日志) ② BHERO_KILL 非零 ③ death_penalty 触发频率 (防送死通胀, 判据 = 死亡局占比 <20%) ④ avg_r 无塌
- **回退**: .so 备份恢复 (改树前 cp 流程, 项目惯例) 或代码 revert; 参数层无开关 (C++ 改动), 回退必须重启窗
- **排期建议**: 与 BUILD P1 (T05/T06 生成器预置 fort+L1 改图) 合并同一次停启窗 — 两者都需重启, 一次窗两轴? 否 — passable 是击杀链轴, BUILD P1 是经济轴, 按一次一轴应分窗; 但改图与 .so 重编可同窗准备、分两次启停分别开 (或用户拍板合并)

## §2 BUILD 动作链 (act19/20/21 零效果之谜)

### 2.1 链路与断点

- 链路: economy_force 绕过 logits 强制采样 (ep_runner L645-652) → g_rl_action → AAI::yourTurn (AAI.cpp:655,760) → case 19/20/21 (AAI.cpp:411-433): nearestOwnTown → canBuildStructure==ALLOWED 才 buildBuilding, **失败完全静默** (对比 RECRUIT 有全套 [RL-DIAG] 日志)
- **核心断点 (高置信)**: T05/T06 红城 = dungeon 族空城 (vmap 无 buildings), dungeon.json `dwellingLvl1 requires ["fort"]` → act20 (巢穴链) 在 fort 建成前恒 **PREREQUIRES** 静默拒绝; 加引擎**每日一建** (CANT_BUILD_TODAY) 约束, 从零起 fort→hall→L1 需多游戏日, 250 步局内难走完
- **观测盲区**: recruit_mask 只盯巢穴位; act19 大厅/act21 fort 建成**零训练侧信号**; build_mask (obs[672:704], 引擎每拍已填) 只在 T04 分支读 → BUILD 效果对训练不可见
- [BUILD_NEW] 3 条全为 T06 夺来的 conflux 城 (已有 fort+L1, 接建 L2/L3) 佐证: **BUILD 链路本身通, 断在 dungeon 起始条件**
- 日志口径勘误: [ECON]/[RECRUITED]/[BUILD_NEW] 实际在主日志可见 (子代理称只进 hermes_ep 不确); 本窗 877 RECRUITED 主要是夺城后 conflux 现成巢穴招兵, 非红方 dungeon 空城

### 2.2 落点 (备料, 一次一轴排队)

| 优先 | 改动 | 成本 |
|---|---|---|
| P0 | ep_runner: T05/T06 也读 obs[672:704] build_mask 打 [BUILD_MASK] 日志 (观测盲区先补) | 零重编, runner 改动 |
| P1 | T05/T06 生成器给红城预置 fort+dwellingLvl1 (与 T04 起点对齐, act20 直接可用) | 改图 (走 sync_maps 流程) |
| P2 | AAI case 19-21 加 canBuildStructure 诊断日志 (每状态可观测) | 重编 libMMAI.so |
| P3 | [BUILD_NEW] 改 build_mask+recruit_mask 双口径 | 随 P0 |

## §3 纪律与排期

- 两项结论均**只读预研**, 未动任何运行文件; C++ 预案 (§1.3) 与 BUILD P1/P2 触铁律/改图, 全部等批次B 窗收口后按一次一轴拍板
- 批次B 当前窗判据预期修正: ① 接战率 (contact_d=2) 照常可验; ② 单杀 / ③ TOWN_CAPTURE **在 passable 闸门修复前恒 0** — 不是批次B 参数问题, 判读时勿误归因
