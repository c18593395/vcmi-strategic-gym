# 已完成任务（对应编号详细归档）

> 追加型文档：记录已闭环任务的详细过程，按总任务 T 编号归档。新完成任务时在此追加（新条目置顶），保留日期与结论。
>
> **排版约定（2026-09-14 整理）**：条目按完成日期倒序；标题统一 `### 主题 ✅ (日期, 关联编号)`；正文用 **背景/根因/改动/验证/落盘/指针** 小节（按需）。跨文档引用以编号（T.n / 踩坑 #n）为准。

---

---

### P1 引擎窗口三项收口：298 对照定谳 + #215 守卫重写 + connector 重编 ✅ (2026-09-26, P0-2/P1-3/P1-4 销项)

- **背景**：服务器不通窗口，用户拍板做 P1 引擎侧存量欠账（纯 WSL 本机）。P0-2（三套 298 对照实验）/ P1-3（#215 headless 守卫正式 commit）/ P1-4（开源周 C++ 路径环境化重编生效）一次收口。

- **P1-2 / P0-2 三套 298 A/B 对照（N≥4 + 基线，#298 纪律）**：
  - 实验设计：同 4 图（good_to_go / judgement_day / elbow_room / a_viking）× 4 轮 30 步。A 臂 with-patches = 09-24 .so 原态（含三套 298 + [ML-time] + force game_over，零重编）；B 臂 baseline = `py/revert_298.py` 手术式逆向三套 298 → 重编 libvcmi/libmlclient（保留 09-24 独立提交）。
  - 基线工具关键决策：`.so.bak_stk298/.bak_netfix298/.bak_298_0923` 全是 09-23 快照，直接 rollback 会误伤 09-24 的 [ML-time]/force 提交 + 源码树不对齐 → 新建 `py/revert_298.py`（import 三套 patch 脚本的 EDITS 常量做 `new→anchor` 精确逆向，`apply` 可重放恢复）。预检 19 项 18 项可逆（BR 那项 09-19 上游同步后锚点缺失，本就没打上，跳过正确）。踩坑 #321。
  - 结果：A 臂 **16/16 全 rc=0 零冻结**；B 臂 **4/16 rc=0 + 12×rc=124 超时冻结**（judgement_day 4/4、a_viking 4/4、elbow 3/4、g2g 2/4）。复现率口径定谳 = **三套全留**（栈打点哨兵 / 方案1 / upgrade 熔断 cap 8）。09-23「去留倾向」三选三全留实锤。
  - 边界（重要）：**压制非根治**。A 臂 16 局零冻结是采样证据；冻结死锁根因（AI EndTurn realize 死锁 / Mode B 架构级 read-lock 饥饿）仍在，根治方向另排（知识库 09-23 章三档：① 服务器查询栈看门狗 ② MapObjectVisitQuery 根因 ③ endTurn waitTillRealize=false）。踩坑 #322。

- **P1-3 #215 headless 守卫重写 + vcmiclient 链接修复**：
  - 根因：09-16 首次修复 commit 65515ef24（14 处 `if(ENGINE)` 守卫）被 09-19/09-23 上游 1160 文件全量重同步（`375fba919e`）冲掉；原 commit 对象随 09-18 WSL rootfs 事故（#269）永久丢失——WSL 仓 / D 盘全部镜像 / 09-19 rootfs 备份 tar 的 .git 三处 `cat-file -t 65515ef24` 全灭，原始 diff 无法捞回。踩坑 #320。
  - 改动：按 fact_store #215 记录语义重写为 P8 终局热路径**最小 6 处**（`py/patch_215_engine_guard.py`）：CServerHandler.cpp 5 处（sendRestartGame / sendStartGame CLoadingScreen 双分支 + showHighScoresAndEndGameplay + endGameplay discord().setStatus + showServerError）+ Client.cpp 1 处（removeGUI 二次崩点 `ENGINE->windows().clear()`）。上游自带 9 处 `if(ENGINE)` 不动。
  - 同 commit 修 vcmiclient 链接失败（7/26 起欠账）：上游重同步冲掉 mlclient 链接行，`clientapp/CMakeLists.txt` 补 `if(ENABLE_ML) target_link_libraries(vcmiclient PRIVATE mlclient)`。根因 = NetPacksClient.cpp visitPlayerEndsGame 硬引用 `strategic_state_force_game_over` + `g_adventure_allied_ai`。
  - 环境件：`data/config/settings.json` 写 `server.ML.statsMode=disabled` + `rel/bin/-`（sqlite）建 stats/stats_md 表 + seed 行 (side=0, n_pools=1, pool_size=2)。踩坑 #323（stats db 三连环）。
  - 验证：P8 p8c_query_reply **VERDICT: PASS**（171KB LobbyStartGame 广播不崩、zero 197 fishy、MY TURN #1 轮转、`grep -c "not allowed|fishy"` = 0）；冻结复验 g2g 重跑 4/4 rc=0（初跑 1×rc=124 定性 flake，N≥4 口径）。

- **P1-4 connector 重编（开源周 C++ 路径环境化首次生效）**：
  - vcmi_gym/connectors 3 个 .so（v13/14/15）+ 3 exporter 重编：`VCMI_DIR` CACHE PATH（`-DVCMI_DIR` 可覆盖）+ threadconnector XDG_DATA_HOME 取 `$HOME/.local/share`（已设不覆盖，根治 root 侧跑静默拿 /root 地形全零）。此前 09-23 开源周改完一直未重编，C++ 改动没进 .so。
  - 验证：cmake 全目标绿 + dlopen OK + trainer 冒烟链路正常。

- **落盘**：
  - vcmi-native 仓（mmai-ml-wsl 分支）`11be8ba34d`：client/CServerHandler.cpp + client/Client.cpp + clientapp/CMakeLists.txt + data/config/settings.json（4 文件，本地仓不推）
  - vcmi_gym 仓（master）`87df8cc`：connectors/CMakeLists.txt + connectors/v13/threadconnector.cpp（2 文件，本地仓不推）
  - 工具入库主仓：`py/revert_298.py` + `py/patch_215_engine_guard.py`（待本 commit 一并入）
  - 证据目录：`/home/administrator/_298_p1_20260926/`（rootfs 持久：arm_a.txt / arm_b.txt + 32 份逐局 log + build log + P8 log）

- **指针**：知识库「09-26 P1 引擎窗口三项收口」章 + 踩坑 #320-#325 + 当前任务清单 P0-2/P1-3/P1-4 销项 + T13 §六 遗留①销项。

---

### T13 外挂 AI 参考源 = VCMI 官方网络协议栈 + VCMI GitHub 生态调研 ✅ (2026-09-24, 外部资源核实)

- **背景**：总任务 T13 问"参考的是 VCMI 哪个项目"。
- **结论（逐条核实）**：
  - T13 外挂 AI 架构参考的是 **VCMI 官方引擎本体 `vcmi/vcmi` 的网络协议栈**（`lib/network/` + `lib/networkPacks/` + `server/CVCMIServer.cpp`），**非第三方项目**。VCMI 是 authoritative server 架构，Server 只认协议不区分 client 来源 → 「不改 VCMI 一行代码、外部 AI 接入」成立【来源：vcmi.eu/developers/Networking；DeepWiki vcmi/vcmi 2.1/8】。
  - **VCMI 内置 AI（VCAI/Nullkiller/StupidAI）是进程内 `cb` 接口，非独立网络 client**；GitHub 生态无现成"纯 TCP 网络 AI client"可抄，T13 需自研，参照 `client/` 目录的序列化/收包逻辑【来源：vcmi.eu/developers/AI】。
  - **VCMI GitHub 生态对本项目可用性（分档）**：`vcmi/vcmi`（底层引擎，必跟踪）/ `smanolloff/vcmi-gym`（本项目 upstream，做战斗 AI，方向不同、基础设施可借鉴）/ `vcmi-mods/chinese-maps-collection` + `custom-templates`（扩充训练地图池）/ `vcmi-mods/vcmi-extras`（运行必需文件）/ `potmdehex/homm3tools`（H3M 预处理校验，不碰 VMAP）/ HotA·WoG（慎引）【来源：GitHub 检索 2026-09-24】。
- **认知纠正**：
  - **homm3tools 不碰 VMAP**：`h3mconvert` 只做 H3M 版本间转换；翻译机 H3M↔VMAP 只能是 VCMI（你已用 VCMI 转换入池）。
  - **h3msnake 链接 404**：`potmdehex/h3msnake` 不存在，勿 clone。
- **交叉引用**：知识库 §7.1/§7.2/§7.3 + 踩坑点 1.10/3.5/3.6；总任务 T13 条目已补「参考源」；当前清单 §六 T13 已补同注。

---

### 172.16.2.40 checkpoint 自动推送链路 + WIN RATE 结果中枢 ✅ (2026-09-24, 服务器三件套闭环 + 每轮训练自动出新模型 WIN RATE)

- **背景**：训练每 50 step 存 `checkpoints/wsl2_ckpt_<step>.pt`，需打通 WSL 训练存档 → scp 服务器 → `/models/load` → 评测 → 结果回传，让每轮自动产出新模型 WIN RATE 并集中一处可查。
- **架构结论（关键）**：服务器 ARM64 旧 v13 架构（256 维/11 动作）**跑不了**新模型（3464 维 + 地形 CNN + 25 动作）→ WIN RATE 评测**必须在 WSL2 算**，结果回传服务器 `:8088` 集中存放（见 `服务器踩坑点.md` 2.1）。
- **新增组件**：
  - 结果中枢 `results_hub.py`（服务器 `/DATA/hero3/results_hub.py`，systemd `vcmi-results`，`:8088`）：接收/查询历史 WIN RATE，落盘 `winrate_history.jsonl`，重启从 jsonl 恢复；API：`GET /health` / `POST /results` / `GET /results/latest` / `GET /results/{model}` / `GET /models`。
  - WSL2 三件套：`py/winrate_eval.py`（本地评测，复用 `StrategicEnv`+`Net`）/ `py/ckpt_push_hook.py`（导出 ONNX→scp→`/models/load`+activate→本地评测→结果 POST `:8088`+scp 落盘）/ `py/ckpt_watcher.sh`（cron 每 15min 去重触发，或训练尾部 hook）。
- **修复**：`ckpt_push_hook.py` 的 `global` 声明顺序 bug（`SyntaxError: name 'SERVER' is used prior to global declaration`）已修 + 显式 `import urllib.error`。
- **git**：commit `37b4d32b`（7 文件，未 push）。
- **任务体系关联**：`总任务.md`（两段式设计 + 七、资产索引）/ `当前任务清单.md` §三-B（S-1~S-4，本链路 S-4）/ `服务器知识库.md` §五（服务器三件套 + 推送链路/结果中枢）+ `服务器踩坑点.md` 2.1/2.2/1.9 / `docs/checkpoint推送链路与WINRATE自动评测.md`。
- **待用户侧**：WSL2 内端到端实跑一次验证（先 `--games 4` 小样本）；尚未在 WSL 内跑通（本机 sandbox 禁用 wsl.exe）。

### passable 闸门（方案甲）立项部署：观测口径对齐 GHandler，重编 libmlclient ✅ (2026-09-18, WIN-1 击杀链②③ + duel 改善共同前提)

**背景**：批次B+A3 中期结算（09-17，42 局）判据①④达标、A3 全绿，但②单杀/③TOWN_CAPTURE 恒 0。09-17 预研定谳（`docs/预研_OBS3英雄战斗触发链与BUILD链_20260917.md` §1.2-§1.4）：引擎英雄相遇链 `moveHero → blockingVisit → onHeroVisit → startBattle` 无条件触发战斗（无 aggression 开关，JOIN/FLEE 仅怪物有）；真闸门 = 观测 passable 通道——旧 `isClear` 口径（`entrableTerrain && !blocked`，CMap.cpp:152）把敌英雄格/敌城格/怪物格全标 passable=0 → 模型方向掩码 -inf、MOVE_TO/BFS 排除、蓝方 AAI `nearestInteractable` 显式跳过 HERO，三方结构性不可达。用户 09-18 拍板按方案甲（口径对齐）立项执行。

**改动**（引擎侧，触铁律流程）：
| 文件 | 改动 |
|------|------|
| `ML/strategic_state.cpp`（vcmi-native，WSL 树） | 两处 passable 填充（原 L698-700 / L955-974）：`isClear(hposTile)` → `getTerrain()->isPassable() && !(tt->blocked() && !tt->visitable())`，逐字对齐 GHandler `movingOntoObstacle`（CGameHandler.cpp:916）；附口径注释 |
| 备份 | `strategic_state.cpp.bak_passable_0917` + `rel/bin/libmlclient.so.bak_passable_0917`（双备份，BACKUP_OK） |
| 补丁脚本 | `py/patch_passable_0917.py`（两处精确替换 + `src.count(old)==1` 断言，失败即退出不改文件；执行成功） |
| 重编 | `cmake --build /home/administrator/vcmi-native/rel --target mlclient -j8` RC=0（首次误用 `build/` 目录无 CMakeCache，踩坑 #256；rel 原位即部署，无需 cp 同步） |

**部署**：`systemctl stop homm3-train-v5`（优雅保存）→ `bash py/restart_train_v5_win1_batchB.sh start`（8 Environment 不变，checkpoint resume）。

**验证 4 判据（窗内观察，待执行）**：① passable 新口径不炸（无 moveHero 全被拒/#143 复发）② 首个真实战斗事件（引擎 battleStarted/战斗结果日志）③ BHERO_KILL 非零 ④ 死亡局占比 <20% + avg_r 无塌。回退 = .so 备份恢复 + 重启窗。

**落盘**：知识库「09-18 passable 闸门（方案甲）立项部署」章 + 踩坑 #256（构建目录 rel vs build）/#257（口径定性）；任务清单 WIN-1 段改部署态 + OBS-3 改述。

**指针**：预研 §1.2-§1.4（四层证据链+方案甲/乙）/ 踩坑 #143（坐标系 bug 历史层）/#250（P-H2 同格不可达，d≤2 判据已修）/ `py/patch_passable_0917.py`。

### 当前任务清单收敛：过程项合并归档 ✅ (2026-09-17)

2026-09-17 当前任务清单.md 全量收敛，以下过程项从"当前清单"移出（各自详细历史见 git 版本库与同日各独立条目，本条仅留索引）：

- **P10-B h3m2vmap 正向转换器 B1-B4 全部收官（09-16）**：B1 独立 C++ 工具（--selftest/--check-h3m 引擎级读回，#240-#245）；B2 `--save` Knee Deep 直通转换（413 对象 ROUNDTRIP OK，#246）；B3 R1-R7 改写层 + 9 case 开关对账全过（#247/#248）；B4 R2 玩家槽位重配（red=human/blue=ai/余禁用+游离 hero 兜底，序列化免写 teams），正式产出 `B4_adventure_knee_deep.vmap` ROUNDTRIP OK + ep_runner 冒烟 exit=0 steps=6 r=+11.55。下一步 C/D/E（产出入训练池）属远期，WIN 窗外。
- **OBS-5 108_02_duel 无标记短终局定性关闭（09-15）**：2 局（34/41 步 r≈16-20）= 开局 8 向全堵 zombie 段自然终局（动作退化为纯 END_TURN、无 BattleStart、err=no），非脏局非崩溃；2/36=5.5% 低频不修，复发阈值 >20% 再查起始格 passable。`py/obs5_short_term_analysis.sh` 可复用。
- **WIN-1 09-16 白天三次窗口核查（25/39/103 局）作废归档**：当晚已由「击杀激励重设计归因定谳」取代——三层证据链证明 proxy 双帧确认无 bug、修复后真实击杀基线=0（"没杀到"非"杀到没认"），三次核查中的中间结论（#238 结构性不可达等）亦被 P10-target-2b 证伪。权威口径以 `docs/方案_WIN1_击杀激励重设计_20260916.md` §0 为准。
- **D3 熵 bonus + C2 L0 合批部署、P10-target/P10-target-2/2b、C1 B4 规格+人工拍板晋级、T13.10 P8-A~E**：均已有独立条目（见 09-15/09-16 各条），本次仅从工作台表格移出。

### P10-target-2b duel 图 move_to_force 分区分段策略 ✅ (2026-09-16, WIN-3②副作用修复)

**背景**：WIN-3② 250 步方案（`STEPS_PER_EP=250` + T06 全覆盖 `move_to_force=250`）部署后，duel 图（72X72_02_duel / 72X72_01_duel / 108X108_02_duel）250 步截断局 r 从旧 200 步的 -1280~-1289 恶化至 **-1665~-1671**，恶化幅度 ~1245。根因：`move_to_force=250` 与 `act_loop_from_step=60` 解耦后，step 60~249 共 190 步 act=2 循环惩罚 -1.0/步 全生效，叠加 NK2 威胁漂移 + 横跳/往返惩罚累积至 r≈-1665。

**改动**：

| 文件 | 行号 | 旧 | 新 |
|------|------|----|----|
| `ep_runner_one.py` | L153 | `args.move_to_force = 250`（T06 全覆盖）| duel 图 `move_to_force=60`；非 duel T06 维持 `250` |
| `train_wsl2_ppo_v2.py` | L184 | `# T06 move_to_force=200=max_turns → act_loop 门控永假` | `# T06 move_to_force: duel=60, 非duel=250`（注释同步）|

`ep_runner_one.py` L153 修改后代码：
```python
if args.mapname.startswith('T06'):
    if 'duel' in args.mapname:
        args.move_to_force = 60    # duel: P10 引导前 60 步走 24，避免 190 步循环惩罚
    else:
        args.move_to_force = 250   # 非 duel 1v3: 蓝城全程引导
    args.guard_done_steps = 0
```

**验证**（重启后 5 局 duel）：

| 图 | steps | r | 对比 |
|----|-------|---|------|
| 72X72_02_duel | 74 | -183 | -1665→-183，改善 89% |
| 72X72_02_duel | 158 | -123 | 改善 93% |
| 108X108_02_duel | 139 | -137 | -1671→-137，改善 92% |
| 72X72_01_duel | 62 | -174 | 改善 89% |
| 72X72_02_duel | 111 | -165 | 改善 90% |

- steps 不再卡 250 截断（74/62/158/139/111），act 序列从全 `2` 死循环变为 `3,4,5,6,7,18,19,20,21` 混合探索
- P10 SCORE 行正常驱动蓝英雄目标，`pick=(66,66) type=blue_hero` 持续命中

**#238 定性更新**：108_02_duel 蓝英雄 plen=201 < 250 已可达，"结构性不可达"定性作废；duel 图 r 极差问题彻底解除。

**落盘**：踩坑点 #239 + 知识库「P10-target-2b duel 图 move_to_force 分区分段策略」已归档。

**背景**：D3 熵 bonus 4 hunk 已实装到 `train_wsl2_ppo_v2.py`（L3/L17-18/L476/L487-489/L534），C2 L0 崩溃插桩已实装（ep_runner_one.py + train_health_monitor.ps1），均等自然重启窗生效。T7.5 S2 因 #235 BHERO_KILL=0 前置断链回退延后，本批仅 D3+C2 L0 两项。

**部署**：`systemctl stop homm3-train-v5`（优雅保存 step=696906）→ 清 `__pycache__` → `systemctl start homm3-train-v5`（PID 10651，09-15 05:34 起）。

**验证**：
- D3 首条 entc 行：ep=14 `step698978`，`entc=0.0853 ent=1.560`，与公式 `0.05×(1+exp(-698978/2M))=0.0853` 完全一致 ✓
- C2 L0：零 SIGSEGV/SIGABRT/rc=139/rc=134 命中（零崩溃静默，符合设计）
- P10 `[SCORE]` 灰度延续：own_town 128 / blue_hero 79，无异常
- avg_r 小样本扰动（重启后 25 局 mean=-0.26 vs 重启前 6 局 +0.7），最近 5 局回升至 -0.02，非退化
- time 从 527s/ep 降至 ~300s/ep（S2 回退后奖励数值变小 + episode 更早结束）

**四项备料**（零部署，复发/触发时取用）：
- `py/c2_crash_gdb.sh`：L1 core/gdb 取证（bt 30 + 全线程 + so mappings）
- `crashlog/`：L0 崩溃局 ep_log 归档目录
- `py/t05_02_data_pull.py`：T05 36X36_02 J1-J5 判据数据拉取
- `py/win3_move_town_108_02_duel.py`：WIN-3 蓝城 (105,105)→(98,98) 挪城（执行后必跑 sync_maps_to_runtime.py --strict）

**git 4 连 commit（09-15）**：
| commit | 内容 |
|--------|------|
| `62ec87a` | P10 target_scorer 实施 + 灰度启用 + B4 落地 C1 + B1-B4 收口 (#232/#233/#234) |
| `e4d8487` | D3 熵 bonus 实施 + C2 L0 就绪 + S2 回退延后（用户拍板）+ 知识库沉淀 |
| `fe1f7dd` | S2 回退(前置断链) + D3/C2L0 部署清单 + 四项备料脚本 (#235) |
| `6d558d9` | D3+C2 L0 部署落地 + 任务清单状态同步 (PID 10651) |

**踩坑沉淀**：
- #235：合批判断必须双维（技术前置 + 业务触发前置），S2 BHERO_KILL=0 前置断链 → 回退
- D3 L534 行延迟 ~55min 才出现（BATCH=2048 样本需 ~10-14 局），部署后前 30min 看不到 entc 属正常现象

**文档**：`docs/总任务.md` 里程碑 + 09-15 部署后观察记录 + 训练参数快照；`docs/当前任务清单.md` C2/D3/P5 行更新为已部署；`docs/方案_C2_崩溃插桩_20260915.md` §6 备料 SOP；`docs/方案_T05_36X36_02重评估_20260910.md` §4.4 自动化判据脚本；`docs/WSL知识库.md` S2 决策章 + #235；`docs/WSL踩坑点.md` #235 方法论坑。

**状态**：✅ 部署完成 + 验证通过，T6.4 训练 09-15 关机暂停（重启需 `systemctl start homm3-train-v5` + Windows keepalive）

---

### P10-target target_list 加权排序 Python 旁路打分器实施 ✅ (2026-09-15, P10 收口 + WIN-1⑤ 联动)

**背景**：P10 遗留课题 = target_list 排序纯曼哈顿 top-8 无价值/威胁加权，大图最近 8 目标易被强守卫堵死（与 09-09 mine_taken 死信同源），且 D4 蓝英雄完全不在目标池 → 72_02 卡点（58% 截断，蓝城 stall 后无目标承接 → 失败局东向撞墙 ~90 步）。09-15 WIN-1⑤ 拍板方向：干预转 P10 target 加权排序，零部署待灰度。方案 `docs/方案_P10_target加权排序_20260915.md` §4 实现形态 5 步。

**改动**：
- **新文件 `py/target_scorer.py`**（P10 核心产物）：
  - 候选池 5 类 = C++ target_list（矿/资源/篝火/宝箱/宝物）+ obs 蓝英雄段 + obs 蓝城段 + obs 己方取兵城段 + vmap 静态守卫注入
  - 统一打分公式：`score = w_type*V + w_win*Δcap*V + w_pow*F*(V+20) - w_dist*g + w_stick*stick - p_phase - budget_pen`
  - 硬约束打分侧过滤：BFS 不可达剔除 / 守卫/蓝英雄贴脸不走 BFS / man>30 跨图剔除 / 取兵 man≤25 / own_town_limit 超限整类剔除 / guard_blacklist / dyn_blocked
  - `pick_from_scored(scored)` 选头名，返回 `(pick, runner_up)`，pick 含 pos/ttype/tl_idx/is_blue_hero/is_blue_town/is_guard/is_own_town/meta
  - 默认权重 `DEFAULT_W = dict(w_type=1.0, w_win=1.5, w_pow=1.0, w_dist=0.5, w_stick=2.0, margin=20.0, temp=30.0)`
  - 类型基础价值：蓝英雄=100 / 蓝城=80 / 守卫=45 / 己城=35 / 矿=30 / 宝物=25 / 宝箱=20 / 篝火=12 / 资源=10
- **`ep_runner_one.py` 动作 24 段 scorer 分支**（L675-717 新增，legacy 段 L718-874 整体缩进 +1）：
  - `importlib` 加到 import 行 + `_target_scorer = None` lazy import 标志
  - argparse 7 参数：`--target_chain {legacy,scorer}` + `--ts_w_type/--ts_w_win/--ts_w_pow/--ts_w_dist/--ts_w_stick/--ts_margin/--ts_temp`
  - scorer 分支：lazy import → `power_self = int(obs[base+10])` → 构造 `_w` dict → `score_candidates(...)` → `pick_from_scored(...)` → 赋值 `move_target/move_stall/next_dir_idx/move_guard_target/move_town_target/move_town_bfs` → `[SCORE]` 诊断日志
  - 默认 `--target_chain legacy` 零行为变化
- **新文件 `py/test_target_scorer.py`**（离线静态验证，7 项测试）：
  - 1 候选完整性（5 类全部入池）✅
  - 2 打分单调性（蓝英雄>蓝城>守卫>矿>资源堆）✅
  - 3 72_02 卡点（蓝英雄入池且打分最高，legacy 完全不在池）✅
  - 4 硬约束过滤（BFS 不可达/黑名单/取兵限次整类剔除）✅
  - 5 步预算惩罚（远候选加罚）✅
  - 6 粘滞 bonus（当前目标未 stall → 强化保持）✅
  - 7 阶段调制（economy 罚蓝英雄/蓝城，capture 拉满）✅

**验证**：
- `py_compile` 全量通过（`target_scorer.py` + `ep_runner_one.py` + `test_target_scorer.py`）
- 离线 7 项测试全过：`/home/administrator/vcmi-workspace/venv/bin/python py/test_target_scorer.py`
- 清 `__pycache__`（训练链路依赖清缓存，方案 §4.5）

**B1-B4 补全核实**（本轮同步完成）：
- B1 被 T05 全面化覆盖（09-04），P3 已标 ❌ 过时可废弃
- B2 经济实装（09-03 R6 后 0→54 次/40ep），ep_runner L99 `--economy_force` + L384-386 RECRUIT/BUILD_2 奖励段实装到位
- B3=D3 熵补丁（09-15 就绪），`py/d3_entropy_bonus.patch` 存在，🟡 等自然重启窗应用
- B4 规格已落地 C1（09-15），`py/eval_promo.py` 五指标 grep + 双条件 PROMO_HINT 已补
- **B 区全部收口，无遗留待办**

**指针**：方案 `docs/方案_P10_target加权排序_20260915.md` / 打分器 `py/target_scorer.py` / 测试 `py/test_target_scorer.py` / ep_runner scorer 分支 L675-717 / B1-B4 方案 `docs/方案_B1-B4_20260829.md` / C1 工具 `py/eval_promo.py` / D3 补丁 `py/d3_entropy_bonus.patch`


---

---

### WIN-1 capture proxy 空拍误报翻案与修复 ✅ (2026-09-15, 踩坑 #231)

**背景**：09-15 日志分析复核 WIN-1 五判据（check_win1_watch.py），⑤200 步截断 46-50% 亮红，凌晨 analyze_trunc200.py 专项归因"72_02 地形堵点 (40,5)/蓝城 (69,2) 贴东缘"，用户拍板执行方案 A 改图。动图前按事实核查纪律解包 vmap 实证，归因被证伪并挖出真 P0。

**实证链**：① T06_72X72_02.vmap 地形 = gr24_ ×5184 全草地，蓝城 BFS=64 可达、(40,5) 草地——无墙可改；② 4 个截断局 200 动作逐位相同 = 同一确定性策略局（矿 step42/守卫 (23,20) step51 后东缘空转 ~90 步），"成功局"前 75 拍动作与其完全一致、触发 capture 时位置 (34,18) 与蓝英雄（x≥66）零接触；③ battle_quality_events.log 严格配对（map+step）：**全历史 218 次 TOWN_CAPTURE 100% 紧跟同拍 HEROSEG_EMPTY（8 槽全 id=0 未填充帧），现池真实 BHERO_KILL=0**。根因 = ep_runner proxy 块在空拍（`_bnow=空集`）时照算 `prev-空=全灭` 必发 +100；#209 只排 duel（49），非 duel 169 次假奖污染价值学习。

**改动**：
- `ep_runner_one.py` L416 增 `_kill_pending` 挂账；L952-979 proxy 判定重写：空拍帧整体跳过（条件加 `_bnow`）+ 双拍确认（下一非空拍仍缺席才 +100，日志带 `confirmed 2 frames`），空拍冻结/回来撤账/滚动再挂，duel 排除保持。
- `docs/当前任务清单.md` WIN-1 区翻案（判据①修复后重新攒窗、⑤归因改"策略东缘空转"、撤销改图）、WIN-3②、09-15 决策记录订正。

**验证**：七序列状态机桩测 ALL_PASS（误报形态 0 奖/真杀延迟确认/瞬态恢复撤账/duel 保持排除/双杀一次/滚动确认）；py_compile + 清 `__pycache__`；695099 优雅停启窗（state 已存零回退），resume 后首批局（含旧必误报的 King 112 步局）零 Traceback、零 TOWN_CAPTURE。

**后续**：WIN-1 判据①重开 40 局窗（重新非零且带 confirmed 才是真信号）；⑤只数 C 类退化（当前 0）；东缘空转观察假奖噪声消失后是否自愈，否则走 P10 target 加权排序（零部署待灰度）。**地图与 runtime 配置全程未动**。
**指针**：踩坑 #231 / `battle_quality_events.log` / `py/check_win1_watch.py` / `py/analyze_trunc200.py`（分类仍可用，地形归因段已废）。

### 工作区大扫除：临时脚本/.bak/崩溃转储清理 4.4GB ✅ (2026-09-15, commit 1a37398)

**背景**：工作区积累 4 个 GUI 卡死 dmp（3.87GB）、backups/ 整目录（425MB，含 35 个 git tracked 历史源码/.so 快照）、根目录 7 组 wsl2_model*.bak、checkpoints.bak_kl015/、55 个 py/ `_` 前缀侦查脚本、8 月 train.log.bak×6、0 字节怪文件 `]"`。

**执行**：四组清单逐组用户授权后删除（DeleteFile + WSL inode 法处理怪文件）；tracked 65 项白名单 `git add` 精确暂存（排除 4 个接手前历史遗留 D：hexdump_h3m.py/run_check_h3m*.sh），commit `1a37398`（65 files, 31176 deletions），零夹带。保留：checkpoints/ 正式存档、__pycache__、maps、运行时软链、tracked restart 脚本。补扫出 py/gui5、gui8 两个同批漏网 dmp（2GB）一并删除。
**指针**：经验 2241074（tracked 删除单独确认）/ 4 个历史遗留 D 仍未暂存待用户定夺。

### B4 晋级判据规格落地 C1 eval_promo.py ✅ (2026-09-15, B1-B4 补全收口)

**背景**：B1-B4 方案（08-29，`docs/方案_B1-B4_20260829.md`）4 项中 B1/B2/B3 已分别被 T05 全面化覆盖（09-04）/ 经济实装（09-03 R6 后 0→54 次/40ep）/ D3 熵补丁（09-15 就绪）消化，唯一遗留 = B4 晋级判据规格（5 指标 grep + 双条件 PROMO_HINT）未落地到 C1 `py/eval_promo.py`。B4 规格原文（方案 L83-105）：5 指标 = GUARD 首胜率≥30% / 正局率≥70% / avg_r≥+15 连续 3 窗 / 大负率保持 0 / vloss<0.75 连续下行 3 窗；双条件 AND 触发 PROMO_HINT；T03 用 GUARD 首胜作 proxy，T04 起改用真对局胜。

**改动**（`py/eval_promo.py`，B4 常量 + 5 指标计算 + 双条件判定 + 历史窗查）：
- 新增 B4 常量段：`B4_GUARD_PRIME_RATE=0.30` / `B4_POS_RATE_MIN=0.70` / `B4_AVG_R_MIN=15.0` / `B4_AVG_R_WINDOWS=3` / `B4_BIG_NEG_MAX=0.0` / `B4_BIG_NEG_TH=-100.0`
- 汇总段新增 3 指标：`pos_rate`（正局率 r>0 占比）/ `big_neg`（大负率 r<-100 占比）/ `guard_prime_rate`（GUARD 首胜 proxy = guard>0 占比；1v3 课程无独立 GUARD 首胜标记，capture/守卫首胜双终局，沿用 B4 L99 注"GUARD 首胜率作 proxy"口径）
- legacy 分支判定段补 B4 线③正局率 / 线④大负率（vloss 离线 N/A 跳过）
- 1v3 分支判定段追加 B4 双条件 PROMO_HINT 块：
  - 双条件 = `guard_ok (≥30%) AND avg_r_ok (≥+15) AND windows_ok (连续 3 窗)`
  - `windows_ok` 查 `eval_history.jsonl` 本 profile 同 ckpt 最近 3 条 avg_r，全 ≥+15 才算达成；历史不足 3 条（首跑仅 1 条）不触发，需累积
  - n<10 样本不足不出结论；达标打 `[PROMO_HINT]` 提示需人工确认
- 历史 record 补写 `b4_pos_rate` / `b4_big_neg` / `b4_guard_prime_rate` 字段，供后续趋势对比

**验证**（py_compile + 干跑四场景，不跑对局纯逻辑推演）：
- 场景 A n=3 → 样本不足(n=3<10) ✓
- 场景 B n=10 guard 7/10 avg_r=+20 历史 3 窗全 ≥+15 → PROMO_HINT GUARD 70% avg_r 20.0 ×3窗 ✓
- 场景 C n=10 avg_r=+10 → B4 未达标: avg_r 10.0<+15.0 ✓
- 场景 D n=10 历史仅 1 条 → B4 未达标: avg_r 连续 3 窗未达 ✓

**B1-B4 补全收口现状**（当前任务清单 B 区同步更新）：B1 被 T05 全面化覆盖 / B2 经济实装 / B3=D3 熵补丁 → 均已消化；**B4 规格已落地 C1**，B1-B4 补全任务闭环。

**指针**：B4 规格原文 `docs/方案_B1-B4_20260829.md` L83-105 / C1 工具 `py/eval_promo.py` / 历史落盘 `py/eval_history.jsonl` / 当前任务清单 B 区 + C1 行 / GUARD 首胜代码 `ep_runner_one.py` L908-918（`guard_first_win` 标记 +100）。

---

### P8-E 人机混局验证（2 客户端拓扑 PASS）✅ (2026-09-15)

**背景**：T13.10 目标场景"人类 GUI + 1 外挂 AI 同局"实跑验证。P8-B/C/D 已全 ✅，真正缺口 = 从未验证"人类 GUI 客户端 + 外挂 AI 客户端"可在同一 VCMI_server 同局并正常开局。

**拓扑**：单机端口 3030（fork build `D:\vcmi-fork-build\bin`，隔离训练），2 客户端：
- Python host (cid=1, VCMITCPConnection，先连当 host，发 `LobbyClientConnected` + `ChangeHost(2)` 让位)
- 外挂AI (cid=2, headless, `VCMI_TESTMAP_ONLYAI=1`，guest，自动接管蓝方回合)
- 人类 GUI（仅同机共存验证，不 join lobby 不占 Twins 2 玩家 slot）

**关键踩坑（#202 衍生）**：Twins.h3m 仅 2 玩家 slot，第 3 个客户端 join lobby 会触发 server NEW_GAME 初始化 "Picking random factions for players" → "Disaster happened" 崩溃（09-15 实测：3 客户端连 Twins，`14LobbyStartGame` 广播后 server 在 NEW_GAME 崩）。故人类 GUI 若真实 join lobby（第 2 个 ClientConnected），AI（第 3 个）超出 2 槽 → 必崩。收敛为 2 客户端：人类 GUI 不连 3030，仅验证进程存活 + SDL 窗口。

**改动**：`py/p8e_human_mix_probe.py`
- 人类 GUI 不带 `--testmap`/`--serverport`（避免 `EntryPoint.cpp` L379 默认 `onlyai=true` 导致人类 GUI 也 join lobby）
- AI 等 server log 第 2 个 `ClientConnected` 后 `send_change_host(2)` 让位
- 判定从 `srv_log_cc()>=3`（3 客户端）收敛为 `>=2`（2 客户端）

**验证**（P8-E PASS）：
- server 存活 ✅
- Python host 连入 ✅
- 人类 GUI 进程存活（同机共存，不 join slot）✅
- 外挂 AI 连入（server log 第 2 个 ClientConnected）✅
- 对局开始（server log 含 "Received CPack of type 14LobbyStartGame"）✅
- server 无崩溃（tail 30 行无 "Disaster happened"/"Crash info"）✅
- 全进程存活（server / human / ai）✅
- 含 BattleStart(132) 多回合闭环（战斗实际发生，AI 正常接管蓝方回合）

**指针**：任务清单 T13.10 P8-E 行 / 知识库 P8-E 人机混局章 / 踩坑 #202 (ChangeHost 时序) + Twins 2 slot 崩溃 / `py/p8e_human_mix_probe.py`。

---

### P8-D 单机双实例部署验证（13/13 PASS）✅ (2026-09-14)

**背景**：T13.10 双机部署路径验证。按既定口径**不再用第二台真实机器**，统一用「单机双实例」完成双机路径实机验证——同一台 Windows 起 2 套独立 VCMI 实例（真实 VCMI_server 双实例 + 真实 ModelAI client 跨实例 + 端口隔离 3030/3031，共享 `D:\vcmi-fork-build\bin`——Windows 多进程可共载同一 dll，无需 2GB 副本），已比 09-14 WSL 双节点 sim 更贴近真实部署拓扑，`--remote <host>` 路径保留但不再作为待办。

**改动**：
1. 新增 `py/p8d_two_instance.py`——同机 2 实例探针，13 项检查：
   - 实例 A（node1/host）：真实 `VCMI_server.exe --port=3030` 存活 + TCP 探活
   - 实例 B（node2/guest）：真实 `VCMI_server.exe --port=3031` 存活 + TCP 探活（同机 2 server 并存端口隔离）
   - HSK 预交换：生成 32B → node1 侧落盘 → `shutil.copyfile` 跨实例交换到 node2 侧（模拟 U 盘/SCP）
   - 真实 ModelAI client 连 3030（host 侧）与 3031（guest 侧，跨实例），`VCMI_TESTMAP_ONLYAI=1`
   - 协议层认证：`AuthProxy(3031)` 收 `AuthToken` 验签 → `0x01` ACK（正例）
   - 负例：错误 HSK → `signature mismatch` → `0x00` → `AuthFailedError`
   - `StatusFileWriter` 双节点（node1@3030, node2@3031）状态文件
2. **检查顺序 bug 修正**：首跑 12/14——"同机 2 server 并存"判定放在 c2（起 srv_b 时 t=+7s，srv_a 尚未被 client 连入，VCMI server 无客户端会提前退出 → `srv_a.poll()` 非 None 误判）→ 判定延后到 c8（client 连入后 2 server 稳定存活）；`except` 分支 `True if True else None` 笔误一并修正。

**验证**（重跑，rc=0）：**13/13 PASS**——同机 2 真实 VCMI_server 并存（端口隔离）+ 2 真实 ModelAI client 跨实例连入 + HSK 跨实例预交换 + HMAC 正负例闭环 + 双节点状态文件。`ACK 日志: ['ok→1', 'signature mismatch→0']`。

**边界说明**：真实 VCMI_server 不识别我们的 AuthToken 帧（T13.10 已知边界，server 端未实装认证逻辑），故认证走协议层验证（`AuthProxy` 补认证端点，照 `p8d_dual_node_sim.py` 范式），游戏帧走真实 VCMI。将来若 server 端实装 `AuthProxy` 同逻辑（收帧 → `TokenVerifier.verify` → 回 1B ACK），即可无缝切换（当前口径下以单机双实例为准，无第二台机器）。

**指针**：探针 `py/p8d_two_instance.py`；报告 `C:\...\Temp\p8d2_report.json`（13/13 PASS）；server/client 日志 `p8d2_srv_{a,b}.log` / `p8d2_cli_{a,b}.log`；认证范式参考 `py/p8d_dual_node_sim.py` + `py/vcmi_protocol/auth.py`。

---

### P8-B 混人长局浸泡 30min 探针（新建 + 修正地图路径）✅ (2026-09-14)

**背景**：T13.10 备料型工作③——混人长对局浸泡。需 30min+ 真实对局监控 server 崩溃 / client 断线 / 回合流转中断，无现成脚本。

**改动**：
1. 新增 `py/p8/p8b_soak_30min.py`——30min 浸泡探针：fork server + 真实 ModelAI client（蓝方）+ Python host（红方），方案 F ChangeHost 让位；每 5min checkpoint（turn/pkts/srv_alive/cli_alive），1min 一次 server 日志 tail 崩溃关键字检测，崩溃即 break。
2. **地图路径修正**：首跑开局失败 `getFullFileURI MISS name=[MAPS\TWINS]` → client crash（`Maps\Twins.h3m` 反斜杠被 `--testmap` 解析转义）→ 改正斜杠 `Maps/Twins.h3m`（与 `p8be_host_start.py` 一致），重跑开局成功。

**验证**（30min 浸泡跑完，rc=0）：**PASS（稳定性）**——无崩溃 / 0 断线 / 0 重连 / 743 包 / 26 回合；6 个 checkpoint 全程 srv_alive=True cli_alive=True。
⚠ **但回合在前 ~1min 冻结**（`turn` 停在 26，后 25min 零新包）：包统计 `BattleStart(132)=1 + BattleResult(135)=1` 实锤对局打了一场战斗后进入战斗终态。根因同源 P8-C 数据源墙——**无回合计时器时 server 不打断当前玩家回合，对局稳定停在战斗终态（非卡死：进程健康不崩不断，但回合不再推进）**。

**两层结论**：
- 稳定性 ✅：30min 无崩溃 / 0 断线 / 0 重连（浸泡目标达成）
- 回合推进 ✗：无 timer 场景回合冻结；要长局持续回合推进需启用 turn timer（有计时器场景）

**指针**：探针 `py/p8/p8b_soak_30min.py`；报告 `p8b_soak_report.json`（turns=26 / packets_total=743 / disconnects=0 / reconnects=0 / server_crash=false / client_crash=false / verdict=PASS）；server/client 日志 `p8b_soak_{srv,cli}.log`；开局范式参考 `py/p8/p8be_host_start.py`；回合冻结根因见「P8-C QueryReply qid≠-1 样本积累」条（同源数据源墙）。

---

### P8-C QueryReply qid≠-1 样本积累（重跑确认数据源墙仍成立）✅ (2026-09-14)

**背景**：T13.10 备料型工作②——积累 QueryReply 真实 qid≠-1 样本。09-11/09-12 记录「数据源墙」：无回合计时器场景下 server 不下发需回复的 Query（PlayerStartsTurn 全 qid=-1），属 server 行为非协议缺陷。

**验证**（重跑 `py/p8/p8c_query_probe_real.py`，rc=0）：**PASS(qid=-1 only)**——全程 22 条 `PlayerStartsTurn` 全 qid=-1，0 条真实 QueryReply，server 健康（无 fishy/crash），回合正常流转（turns_act=11, turn_ends=21）。确认数据源墙仍成立；要拿 qid≠-1 样本需有计时器场景（启用 turn timer）或真实多玩家长局触发 Query 派生包。

**指针**：探针 `py/p8/p8c_query_probe_real.py`；报告 `C:\...\Temp\p8c5_report.json`（query_received 22 条全 qid=-1 / query_reply_sent 0 条）；设计见 09-12 P8-C 阶段5 条目。

---

### P8-D 双机实机验证（WSL 双实例 9/9 PASS）✅ (2026-09-14)

**背景**：T13.10 唯一剩余实机项——P8-D 跨机器部署。09-12 设计骨架 + 本机 13/13 已过，双机路径（HSK 预交换 + 跨节点 TCP + HMAC 认证握手 + 远端状态文件）缺实机验证。按既定口径**不用第二台真实机器**，本条目用 WSL 内双独立节点模拟双机拓扑（node1=server 端点，node2=client 部署目标，跨 WSL 网络命名空间非 127.0.0.1），双机路径另以单机双实例（`py/p8d_two_instance.py` 13/13 PASS）实跑完成。

**改动**：
1. 新增 `py/p8d_dual_node_sim.py`——WSL 双节点模拟探针，覆盖 6 项检查（HSK 生成+预交换+0o600 / VCMI 部署目录就绪 / 跨节点 TCP 可达 / 正例 HMAC 认证握手 / 负例错误 HSK 拒绝 / 双节点状态文件）。
2. **P8-D 遗留 bug 修复**（`py/vcmi_protocol/remote_connection.py`）：`_auth_handshake` 认证失败路径调 `self.close()` 但类中无此方法（AttributeError）→ 改为 `self.disconnect()`。

**验证**（WSL 内 `venv/bin/python py/p8d_dual_node_sim.py`，9/9 PASS rc=0）：
- ACK 日志正例 `ok→1`，负例 `signature mismatch→0`（HMAC 验签闭环真实生效，非模拟 ACK）
- 跨 WSL 网络（`172.23.41.125:40311`）连接非 localhost
- HSK 权限 0o600 确认
- 双节点状态文件读写 OK

**指针**：探针 `py/p8d_dual_node_sim.py`；本机 13/13 路径 `py/p8d_deploy_probe.py --local`；双机部署已用单机双实例实跑完成（`py/p8d_two_instance.py` 13/13 PASS，`--remote <host>` 路径保留但不再作为待办）；设计骨架 09-12 条目；知识库 P8-D 章。

---

### H3 standardDefeat 败北信号断链根修（server 结算 + ML 终局通道 + Python 配套）✅ (2026-09-14, #228)

**背景**：T7.4 判据 1（死亡局样本）在全草地大图池长期 0 命中。红败定向探针实锤：蓝 NK2 击杀红英雄后占红城被引擎永久拒绝（`engaged in battle and simultaneous turns are still active`），探针 200 步 C 类截断、双方存活——引擎层游戏本身卡死；且真败北后 Python 300s 超时返回全零 obs / go=0 / r=0，**-200 败北信号丢失 + 每局白烧 300s**。

**根因（双层，均代码实锤）**：
- 根因 C（server 战斗结算断链，更上游）：08-17 ML 补丁把 `endBattleConfirm()` 的 `popIfTop` 改为 `QueriesProcessor::removeQuery()`，但体内 `removalDone` 守卫使 `CBattleQuery::onRemoval` 每查询只调一次。PvP 中该 query 被红蓝双方共享（计数=2，PvE 中立战=1）→ 计数 2→1 永久挂住 → `battleFinalize` 不发 BattleEnded（currentBattles 残留 → simturns 占城全拒）、不跑 `checkVictoryLossConditions`（双方都不判 LOSER）。PvE 因计数=1 不受影响，解释了 88015 行日志普通打野战全正常、唯独红蓝英雄交战即挂。
- 根因 B（ML 终局通道缺失）：红方真 vanquished（无将无城）后 yourTurn 不再回调，`adventure_wait_for_turn()` 只等 turn generation 无终局唤醒通道；旁证 `g_ml_player_cb` 全局量定义后从未赋值。

**⚠ 编译树纠错（本任务最大坑）**：生产 vcmiserver 编自 `/home/administrator/vcmi-native/rel`（strings 内嵌 `vcmi-native/server`×168 + CMAKE_HOME_DIRECTORY + 产物 mtime 三证）；`vcmi-native-build` 是 08-02 陈旧树、不参与生产，在其上看到的「popIfTop 未接线 / removeQuery 游离」全是假象。生产树 08-17 补丁本完整自洽，唯一缺陷即 removalDone 守卫。

**改动（生产树 vcmi-native，不碰 libvcmi.so）**：
1. `server/queries/QueriesProcessor.cpp` `removeQuery()`：删 `removalDone` 守卫，每个持有 query 的玩家各调一次 `onRemoval`（二次安全由 `battleFinalize` 的 `finishingBattles.count(battleID)==0 return` 兜底，gdb 实机二次 onRemoval 无段错误）。
2. `ML/strategic_state.cpp`：`adventure_process_turn` 入口持久化 `g_ml_player_cb=userData`；`adventure_wait_for_turn` spin 每 25 拍（250ms）`shared_lock(CGameState::mutex)` 轮询 `gs.players`（跳过 NEUTRAL），存活≤1 → `fill_strategic_state` 刷终局快照 → 返回新码 **-2**。
3. `vcmi_gym/envs/v13/strategic_env.py`：`_adventure_wait` 识别 -2 静默 return → step 走 `_read_state(go=2)→reward→terminated`。
4. **第二处 Python 配套（修复后首验才暴露）**：NK2 shaping 分支在 `_calc_reward` 提前 `return clip(r,-10,300)`，到不了非 NK2 分支 ±200；终局 state_value 退化为有界值（nk2_val=10/delta -26），-200 不可达、-26 被剪成 -10。在该分支 return 前补显式 `game_over==1 +reward_win / ==2 -reward_win`，clip 下限放宽到 -300。
5. `py/probe_t06_gameover.py` 收尾改 `os._exit(0)`：真终局后 env.close() 与 NK2 后台 makingTurn 线程析构竞态 SIGSEGV（rc=139）；生产 ep_runner_one.py L1261 本就 os._exit，仅探针受影响。

**验证（新二进制，全 rc=0 / 零 SIGSEGV / 零 assert）**：红败三图（RedLossProbe 20X20 `--idle` / 72X72 1v3 / 72X72 duel 带 ckpt689498）wait **0.3-0.8s 秒回**（原 300s/卡死）、go=2、alive obs[19]=0/[34]=1、**r=-226/-232（含 -200）**、terminated；PvE monster 战斗链 `CBattleQuery→battleFinished RETURN` 正常（含恢复训练后 108X108_02 首局，错误仅 MMAI 非法移动 fishy/音频缺失良性噪音）；A Viking We Shall Go.h3m（官方 144×144）headless 8 回合 10.3s 存活 go=0 截断，覆盖官方图加载与存活路径。红胜 go=1 未自然触发（模型对 NK2 三阵皆败），降级生产首批异步观察（go==1 与 go==2 共用同一代码路径、+200 与成熟分支逐字一致）。

**部署与恢复**：构建 `cmake --build /home/administrator/vcmi-native/rel --target vcmiserver mlclient -j8`（09-14 11:45/11:46）；vcmiserver 仅 rel/bin 一份，libmlclient.so 双副本（rel/bin + build/bin，md5 一致）；备份后缀 `.bak.H3.20260914`；清 `vcmi_gym/**/__pycache__`；训练 stop→start 干净恢复，resume step=689938 / maps=10，首批生产局 err=no 无 300s 长尾。

**指针**：完整根因/验证矩阵见踩坑 **#228（✅已修复）**；双编译树判定/构建部署拓扑见知识库「H3 双编译树判定 + 构建部署拓扑」章；补丁脚本 `py/patch_h3_server_0914.py`、`py/patch_h3_souser_0914.py`（`patch_h3_wire_0914.py` 仅陈旧 build 树，勿对生产树重跑）；探针 `py/probe_t06_gameover.py`、红败图生成 `py/gen_redloss_probe_0914.py`。

---

### 假局事故三道拦截 + T06 _02 四图/King 全部修复回池 + 改图同步固化 ✅ (2026-09-14, #221/#224-#227)

**背景**：09-13 header.players 批量修补后 King 仍产出 603s 脏局，T06 _02 系四图 max_turns 故障；且残留 traj 假局曾污染训练。本窗一次性治理并把干净 10 图池回池。

**改动**：
1. **残留 traj 假局三道拦截**：run 开头删 traj + 记录 ep_rc + rc≠0/身份不符 `[FILTER]`（train_wsl2_ppo_v2.py L134-137/L196/L229-235），脏局 obs_nz==0 不进 PPO buffer。
2. **T06 _02 四图故障根因双层**：①header 仅 5 字段缺 8 字段（`py/patch_t06_02_header_0914.py`，#225）；②补丁只改 maps/training 未部署——运行时 data/Maps 是两棵 VCMI 树软链到 `vcmi/data/Maps` 的同一真实目录（#224 部署拓扑）。max_turns=1 + 200 步完整局 4/4 rc0 后回池。
3. **King 剥三层**：core:dragon 非法 id（`py/patch_king_dragon_0914.py` + 转换器治本）→ town_1 orange 玩家槽 SIGSEGV → #222 旧补丁误把 red 城/将送 blue 致 red 无城无英雄（`py/patch_king_rebuild_0914.py` 按 72X72_01 模板 1v3 重建，#226）；另修 ep_runner L1196 `passable` 无模型探针 NameError（#227）。King 完整局 120 步/240s/r=183.1 rc=0 err=no。
4. **改图同步固化**：`py/sync_maps_to_runtime.py`（MAPS AST 清单 + 软链去重 + 源侧 header.players/owner/identifier 预检 + 原子写 + 写后校验，`--check/--strict/--dry-run/--purge`，负向回归 `py/_test_sync_negative_0914.sh`）；写入项目硬约束「改图必跑 --strict，rc≠0 禁训」。

**验证**：修复后干净 10 图池 resume 685402 首批 36 局——零 SIGSEGV / 零 err=yes / 零挂死；King 1 脏局被 FILTER 拦截后首个生产有效局 149 步/259s/r=157.2（GUARD_DONE 早停）；三张原致命图（72_02 header 挂死、108_02 SIGSEGV、King 三层连环）全部产出有效局；TOWN_CAPTURE=4 全在 1v3 图（三蓝英雄全灭 +100）。

**指针**：踩坑 #221（King reset 竞态假设证伪）/#224（运行时部署拓扑）/#225（header 字段）/#226（King 重建）/#227（passable NameError）；知识库「VMAP 运行时部署拓扑 + sync 同步固化」「生产回池首批验证」章。

---

### VMAP header.players=[] 系统性缺陷批量修补 + 地图轴二次扩展 MAPS=10 ✅ (2026-09-13, #215/#222/#223)

**① header.players 空数组批量修补**：King of Pain 3 局全脏（no_own_town）初判 reset 竞态（#221），逐层排查实锤真实根因 = VMAP `header.players=[]` 空数组（地图初始化异常）。`py/_scan_players.py` 扫 60 张 VMAP 共 5 张同病（King + 4 张 T06 `_02(_duel)`，其中 72X72_02_duel 就在训练 MAPS）。`py/patch_king_players.py` v2 + `py/patch_t06_02_players.py` 注入 blue/red + mods=[]→{}（King 另改 town_1+hero_0 owner→blue），回归扫描 players=[] 归零（#222 King 单图 / #223 T06 批量）。长期治本：h3m2vmap / vcmi_full_to_slim / fix_t06_maps 生成管线导出时注入默认 players，新图入池前必扫 `_scan_players.py`。
**② 地图轴二次扩展（用户拍板）**：T7.4 判据 1 不走 T04 死路密集轴（T04_30X30_01 200 步局 7 方向可走非 ZOMBIE，结构性无效），移除 T04×2，加入 T06 三张（72X72_02 + 108X108_02_duel + 108X108_02），MAPS=9→10；`py/_check_108_ids.py` 验证 108 identifier（hero core:alchemist / town core:conflux+dungeon）。King 入池语法截断 bug 修复（`"King_of_Pain_h3` 未闭合 → 正确字符串，#215，此前 King 从未真正入池）。停训 + 清 pycache + 重启 resume step=680723，`maps=10` 确认。

**指针**：踩坑 #215（语法截断丢图）/#222/#223；工具 `py/_scan_players.py`、`py/patch_king_players.py`、`py/patch_t06_02_players.py`、`py/_check_108_ids.py`。

---

### P11 VMAP → H3M 反向转换 + King of Pain roundtrip 首次闭环 ✅ (2026-09-13)

**需求**：把训练用精简 VMAP 反向转成官方 H3M 二进制，用于 (a) 真实 HoMM3/原版客户端人工验证关卡设计；(b) H3M→VMAP→H3M 对象级保真 roundtrip；(c) 地图编辑工具链对偶对账。定位：备料型，纯 Windows/WSL Python，不碰训练/GPU/.so。

**实现**：`py/vmap2h3m.py`（SOD 格式，5 类对象 town/hero/monster/mine/resource）；关键坑 = `FACTION_CODE`/`MINE_CODE` 双表需覆盖 VCMI 完整 vmap 族系/资源名变体（补 orePit/sulfurDune/crystalCavern，abandoned 走跳过）；donor 模板库 = 官方图库 raw (id,subid) 匹配复制（不重造 H3M 原始编号），库容 485 条来自 56 张官方图。

**King roundtrip 首闭环**：King of Pain 精简 vmap → `/tmp/kop_rt.h3m`（SOD 72×72）：输入 173 对象（hero=1/town=5/mine=38/resource=68/monster=61）→ 输出 168 对象 + report 跳过 5（1 abandoned mine + 4 dragon creature 引擎 index 未收录）；h3m_tool `objects` 对账 `skipped=0`、`scan` version=28 name="King of Pain" 2 players terrain_end=36709、strict 读回 168 对象全 OK。
**King 入池修补**：`py/fix_kop_town_subtype.py` 从 `options.availableFactions[0]` 就地重写 3 副本×5 镇 subtype `core:object→core:dungeon`；加固 `py/vcmi_full_to_slim.py` L146-165（`object` 占位符 → availableFactions[0] → dungeon 三级回退）。
**边界**：仅 SOD/AB 写入（ROE/CHR 回源 h3mtxt 或 P10 主链）；donor 库无 dragon(creature 139)，4 张 dragon 图需补 Dragon Orb.h3m 类含 dragon 官方图；abandoned mine 静默跳过。验收口径：`h3m_tool objects <h3m> | tail -1` 见 `skipped=0`。

**指针**：正向 P10 见 09-01 h3m2vmap 条目与 `docs/P10-B-h3m2vmap转换器设计稿.md` §8；h3mtxt 管线踩坑 #199；工具 `py/vmap2h3m.py`、`py/fix_kop_town_subtype.py`。

---

### 09-13 上午 T06 duel C 方案 hero-kill proxy +100 误报修复 ✅ (2026-09-13, #213)

**核查结论**: duel 图 `[TOWN_CAPTURE] blue_hero_killed=[1]` 49 次触发全部为误报，真实击杀 0 次。

**三重证据链**:
- 证据 A: `BHERO_KILL=0`（obs heroes 段正常填充时蓝英雄消失 0 次）
- 证据 B: `HEROSEG_EMPTY=0`（obs heroes 段全空拍 0 次；`_bnow` 非空但少了 id=1 的"部分少报"形态更隐蔽）
- 证据 C: TOWN_CAPTURE step 95 触发后 ep 持续到 step 96 才 end（err=no），蓝英雄并未真死，游戏继续

**根因（两条路径守卫不对称）**:
- C 方案 L948: `if (bhero_ids_prev is not None and not _t06_hero_kill_capture)` — 无 `_bnow` 非空守卫
- `BHERO_KILL` L963: `if _bnow:` — 有 `_bnow` 非空守卫，全空/部分少报时跳过
- obs 战斗瞬态期间 `_bnow` 部分少报（有蓝英雄其他 id 但少了 id=1） → C 方案差集 `bhero_ids_prev - _bnow` 含 id=1 → 误判击杀 +100；`BHERO_KILL` 因 `if _bnow:` 有内容而跳过 → 0 条

**修复**: `ep_runner_one.py` L948 加 duel 排除 — `and not args.mapname.endswith('_duel.vmap')`。duel 1v1 语义下蓝英雄死 = game_over = ep 终止，游戏引擎已处理，无需额外 reward proxy。

**影响评估**:
- 4900 虚假 reward 已注入当前 checkpoint（49×+100），可能让 PPO 价值网络学到"进战斗 = +100"错误关联
- 修复需 stop/restart 才生效；4900 污染量级相对正常 avg_r≈150 的 episode 尚在小数点级别，短期可接受
- 非 duel 图（T01-T05）C 方案是否也存在同类误报，需后续监控 `HEROSEG_EMPTY` 与 `blue_hero_killed` 计数对照

**落盘**: `ep_runner_one.py` L948；`docs/WSL知识库.md`「T06 duel C 方案 hero-kill proxy +100 全误报修复 (09-13, #213)」；`docs/WSL踩坑点.md` #213；后续空拍观察在当前任务清单 OBS-2。

**关联**: 踩坑 #213 / C 方案 hero-kill proxy (#195) / `TOWN_CAPTURE` obs 战斗瞬态部分少报 vs 全空拍（更隐蔽误报形态）/ 09-12 深夜 P8-C 协议栈四层修复。

---

### P8-D 跨机器部署脚手架（本机 13/13 PASS）+ TOWNSTALL 平台段误判修复 ✅ (2026-09-12, #209)

**① P8-D 设计 + 骨架 + 本机实机**（T13.10 跨机器阶段，不碰训练）：新增 `py/vcmi_protocol/auth.py`（HMAC-SHA256 Token：签发/验证/防重放/轮转）+ `remote_connection.py`（远程 TCP + 认证握手 + 指数退避重试）+ `deployment.py`（多节点拓扑/进程管理/状态文件/密钥分发）+ `py/p8/p8d_scaffold_design.py`（主入口，3 种拓扑 A/B/C + 离线自测 20/20）。本机实机 `py/p8d_deploy_probe.py --local` 13/13 PASS（HSK 生成/落盘/轮转/权限 + VCMI_server 启动 + TCP 探活 + 连接/状态 + 状态文件 + 双节点命令构造 + 密钥轮转闭环）；修复 2 处：`remote_connection._do_connect` 改用 `VCMITCPConnection.connect()`（原调不存在的 `_socket_connect`）、`get_stats` 去掉对 VCMITCPConnection 不存在方法的依赖。E2E 162/162 全绿。双机脚本已备（`--remote <host> --port 3030`）；按既定口径后续以单机双实例（`py/p8d_two_instance.py` 13/13 PASS）完成双机路径实机验证，不再依赖第二台机器。
**② TOWNSTALL 平台段误判根修（#209）**：`ep_runner_one.py` L877 `cur_dist >= move_stall_prev` 把 BFS plen 持平（横移/绕岩平台段）也计停滞，攒满 6 步误触发 TOWN_BLOCKED 提前禁用城镇引导，L887 `dyn_blocked.add` 又把可通行格拉黑放大误判（pas 全 1 仍判 stall，全 log TOWNSTALL 3817/TOWN_BLOCKED 460）。修复 = L877 `>=` 改 `>`，重启后 66 局 TOWN_BLOCKED=0、TOWNSTALL 41 全为 move_stall==1 瞬态诊断。

**指针**：P8-B/C 见下方 09-11/09-12 T13 条目；踩坑 #209；TOWNSTALL 后续观察转当前清单 OBS-4。

---

### 09-12 深夜 T13.10 P8-C 协议栈四层修复 + MoveHero/Query 双探针闭环 ✅ (2026-09-12)

**协议栈四层隐性缺陷一次性修复 (py/vcmi_protocol/)**:
- **层 1 — QueryManager 派回复错包**: `protocol.py` 用 `type_id` 代替真实 `query_id` 派 `QueryReply` → 全部回错 qid。修法 = `handle_query` 前从包首字段（`Query : CPackForClient { QueryID queryID; }`）提取真 qid, qid==-1 时跳过回复 (NetPacksBase.h L47-50: "非实际 query, 不应回复")。
- **层 2 — 三个 Query 派生类字段序错位**: HeroLevelUp / BlockingDialog / GarrisonDialog 在 packs.py 原字段序与 `PacksForClient.h` 不匹配。逐个按 wire 顺序覆写 `serialize/deserialize`。
- **层 3 — parse_client_pack 扁平 vs 嵌套不一致**: 单测手写 dict 掩盖了 wire 层 bug；生产返回 `{type_id, class_name, data:{...}, raw}` 嵌套结构，测试却直接读顶层字段 → 假绿。修法 = 测试改读 `.data`，且新增 7 项 TryMoveHero wire 断言。
- **层 4 — TryMoveHero 字段序**: 从占位 2 字段 (`source/destination/reason`) 改真 wire 序 `oid + result(0=FAILED 1=SUCCESS 2=TELEPORT 3=BLOCKING_VISIT 4=EMBARK 5=DISEMBARK) + start(int3) + end(int3) + movePoints + fowRevealed(vector<int3>) + attackedFrom(int3)`。

**双探针四用例矩阵**:
| 探针 | 载体 | 结果 | 关键指标 |
|---|---|---|---|
| 离线 Query (`py/p8c_query_probe_offline.py`) | 127.0.0.1:0 mock | 6/6 PASS | HeroLevelUp/BlockingDialog/GarrisonDialog/qid=-1 skip |
| 离线 MoveHero (`py/p8c_movehero_offline_probe.py`) | 127.0.0.1:0 mock | 6/6 PASS | default_success/multi_point/fow_entries/rejected/endturn/req_id_echo |
| 在线 Query (`py/p8/p8c_query_probe_real.py`) | 实机 vcmiserver 3030 | PASS(qid=-1 only) | turns_act=14 turn_ends=27 |
| 在线 MoveHero (`py/p8c_movehero_probe_real.py`) | 实机 vcmiserver 3030 | PASS | turns_act=4 move_accepted=9 move_failed=0 try_moves_total=12 |

**实机首解证据**: `oid=350 result=1 start=(1,8,0) end=(2,7,0) mp=1760 fow=14` — TryMoveHero 全字段实机首次完整解出。E2E 167/167 保持全绿。

**Query 分支控制流教训 (#208)**:
- 现象: 只收到 1 条 `PlayerStartsTurn qid=-1 (INVALID, 跳过回复)` 后 server 断开
- 根因: `if tid in QueryManager.QUERY_TYPES: ... continue` 分支吞 tid=88 → 后续 `elif tid==88: act_turn()` 兜底永不触发 → EndTurn 永不下发 → server 等回合结束超时踢连接
- 修复: Query 分支内对 tid=88 特殊处理，无论 qid 是否 -1 都触发 act_turn
- 教训: ①Query 分支不能只写"回复/跳过"两义；②实机是终极仲裁（离线全绿 ≠ 实机跑通）；③qid=-1 语义 ≠ 整包可忽略

**落盘**: `py/vcmi_protocol/packs.py` (TryMoveHero 真字段序 + 三 Query 派生类覆写) / `py/vcmi_protocol/protocol.py` (parse_client_pack 嵌套 + QueryManager 真 qid + qid=-1 skip) / `py/vcmi_protocol/tests/test_e2e.py` (TryMoveHero 7 项 wire 断言) / `py/p8c_movehero_offline_probe.py` (新建) / `py/p8c_movehero_probe_real.py` (新建) / `py/p8/p8c_query_probe_real.py` (Query 分支特判 tid=88)。

**剩余**: P8-C 混人回合 → P8-D 跨机器（P8-D 已独立推进，见 09-12 条目）。

**关联**: 踩坑 #207（协议栈四层隐性缺陷）+ #208（Query 分支吞 PlayerStartsTurn）/ #205（恶意包炸服）/ #206（SRV-DIAG 破数据源墙）/ 任务清单 T13.10 专区 / 知识库「09-12 T13.10 P8-C 协议栈+双探针闭环」章。

---

### C4 NK2 寻路加速 merge（#7632）+ 新栈首窗观察 ✅ (2026-09-11, #178-#184)

**内容**：停训窗 merge 官方 NK2 寻路加速 PR #7632（native `1c580aee3`+`0a44db3b9` / workspace `825fa0b8e7`），训练 resume step=621725。首窗 20 局全 err=no 零崩溃，ZOMBIE 均为正常 hero 死亡事件。
**吞吐判定（拍板依据）**：**本负载无 +40% 收益**——T06 duel 中性（4.96/7.35s/步，旧区间 3.67-6.23）；T05_52_mir 持续 +11% 慢（104-112s vs 旧 97s×6，#184 假设坐实）；T05_36X36 中性；reward 全程不劣化。
**拍板（09-11 用户）**：保留新栈不回滚——本负载中性无害，课程向 72/108 更大图推进时 NK2 可能兑现收益，且再开停训窗回滚风险 > 收益。回滚安全网保留（~/so_backup_0910_7632 + git revert 三笔），未来晋级大图轴若吞吐仍无收益再议。
**指针**：知识库「C4 #7632 执行记录」章；踩坑 #178（ASSERT namespace 陷阱）/#179（<ranges> 显式 include）/#180（ResetInfo API 映射）/#181（副本三目录不在链路）/#184（T05 小图 +15% 变慢预警）。

---

### 09-11 深夜 T13.10 P8-C 决策接入 ✅ (2026-09-11, 双图实机 PASS)

**MoveHero 真实决策 (首步, Twins 图)**:
- 数据源墙 (#203) 第四路线破解 = SRV-DIAG server 侧注入 (#206): `CGameHandler::start` (!resume) 逐英雄 dump `[SRV-DIAG] HERO OI=.. owner=.. pos=(锚点)`, Python 外挂 tail server 日志拿运行时 OI, 免解析 171KB StartGame
- 我方回合发真实 `MoveHero(182)` (path=锚点坐标 8 邻域逐步 / layer=0 / hid=OI) → server applied + `TryMoveHero(109) result=SUCCESS` 回流 → EndTurn → Turn2/3 轮转 → zero fishy
- 实锤: red OI=350 (1,8,0)→(2,7,0) 实移 (后续同格 = day1 MP 尽, 非拒绝); blue ModelAI OI=732 自主移动
- 字段序修正 (#206): TryMoveHero = id+result+start+end+movePoints+fow(vector)+attackedFrom (非 id+start+end+result; 离线两侧同错自洽, 实机对拍抓出, 与 #199 同型)

**城镇决策链 (A Warm 图, red 自带城 OI=11 + 贴脸英雄)**:
- `BuildStructure(185, bid=30 DWELL_LVL_1, LVarInt)` applied=True → 新增 `[SRV-DIAG] TOWNAVAIL OI=.. creatures=core:imp` (build 成功后 dump town->creatures jsonKey — 112 广播的 CreatureID 带跨包去重负 ref, 外挂侧不可还原, 必须走 diag)
- `RecruitCreatures(187, crid="core:imp" x15 → hero 730)` applied=True — 招兵入城全链闭环
- 类型序列化实锤: BuildingID=StaticIdentifierWithEnum (LVarInt 数字, 非 string); HeroTypeID/CreatureID=EntityIdentifier (string jsonKey)

**漏洞 #205 (本链发现并已修, 联网安全)**: 外部客户端发非法 entity 字符串 (假 CreatureID) → `IdentifierResolutionException` 在 `retrievePack` 未捕获 → **整服 Disaster 崩溃**。修复 = `CVCMIServer::onPacketReceived` try/catch 丢包并 log。任何 wire 带 entity-string 字段的包 (CreatureID/HeroTypeID/SpellID) 均可触发, 人机联网前必修。

**落盘**: vcmi bc3e124fa2 (SRV-DIAG hero) + 1c3be8d030 (TOWNAVAIL + #205 修复); 主仓 7967511 (movehero probe) + 6020e10 (town chain probe + packs.py 修正) + 3a60a2d (docs); 脚本 `py/p8/p8c_movehero_probe.py` / `py/p8/p8c2_town_chain_probe.py`; 离线 test_e2e 145/145 PASS。

**剩余**: QueryReply(197) 实战 query 样本 (server 日志已见 MapObjectVisitQuery qid=4 真查询) → P8-C 混人回合 → P8-D 跨机器。

**关联**: 踩坑 #203/#204/#205/#206 / 任务清单 T13.10 专区 / 技能 vcmi-network-protocol references/p8c-decision-hook.md。

---

### 09-11 晚 T7.4 死亡惩罚实装 ✅ (2026-09-11, checkpoint step=637994 续训, 观察期进行中)

**实装 (设计稿 `docs/方案_T74_死亡惩罚_20260910.md` 落地)**:
- ep_runner_one.py: ① `--death_penalty` argparse (L127, 默认 -50, 0=关闭) ② zombie 确认点 (zombie_streak>=2 块, L1213-1222) 追加 `r += args.death_penalty; traj["rewards"][-1] += args.death_penalty; traj["done"][-1]=True` ③ 日志改 `[HERO_DEATH] penalty -50 [ZOMBIE] hero dead (all-blocked xN), end ep at step N` 格式
- train_wsl2_ppo_v2.py: L194 主日志高亮白名单补 `[HERO_DEATH]` — 死亡事件进主日志可 grep
- 语义: 红英雄死 (zombie) → -50; 蓝英雄死 (capture proxy, C 方案 #195) → +100; 两事件不同帧不同事件, 无同帧双罚 (方案_T74 §6 复核: 红英雄全灭后蓝英雄已无 target, 不会再触发 capture)
- 全局生效不按图分支 (T04/T05/T06 统一, 无死亡局零扰动)
- 参数定档: -50 试探 (守卫 +100/死亡 -50: 打赢净 +50 仍正, 不劝退接战) / -100 加深 / 0 回退; 回退线 = 接战率塌 >30% 或 avg_r 跌 >20%

**部署**:
1. `systemctl stop homm3-train-v5` → 优雅存盘 `Saved STATE_PATH (step=637994) and MODEL_PATH`
2. 两处 patch 语法验证通过
3. restart: `Loaded train state (model+optimizer, step=637994)` 干净续训, KL adaptive ON (target=0.5 coef=0.3 kl_ref frozen from BC); 重启后 batch=2048 maps=8 (C4 #7632 新栈参数沿用), C 方案标志 grep=3 在位

**首窗观察 (09-11 晚, 11 局 T05 52X52)**:
- `[HERO_DEATH]` 触发 0 次 — 11 局全守卫胜局 (ep_steps=71~73, GUARD won +100, r=153~166), 该负载段不产英雄死亡事件
- 判据 1 (死亡局 r 转负): ⏸️ 暂不可验 (0 样本), 挂起待死亡局出现
- 判据 2 ([GUARD] 接战率不塌 ±20%): ✅ 暂通过 (ep=10 guard won step 48 / ep=11 guard won step 54)
- 判据 3 (avg_r 跌幅 <20%): ✅ 通过 (2.20 vs 重启前基线 2.34, 跌 6%)
- 验证方法论沉淀 = 踩坑 #204 (0 命中三义性: 未生效/生效但0触发/窗口错; 切窗行号法见 #170; 无样本判据挂起不反推)

**后续**: 攒 ~100 局 (1-2 窗) 复核三判据; 达标 → 加深 -100 或维持; 回退线触发 → 降档 -25 或 0。引擎侧 standardDefeat 判负根修 = 紧接的 C++ 课题 (落地时复核 done 双路同帧双罚, 方案_T74 §6)。

**关联**: 踩坑 #195 (C 方案 capture proxy, 正交) / #170 (切窗行号法) / #204 (本条方法论) / 知识库 T7.4 章 / 任务清单「活跃任务 3」。

---

### 09-11 T13.10 P8-B 阶段2 完整对局实机 PASS ✅ (2026-09-11, commit 407e8e5)

**方案F: Python host + LobbyChangeHost 让位 + guest 装图 + 完整对局 EndTurn 轮转**

- 脚本: `py/p8/p8be_host_start.py` (6112B), `py/p8/p8b2_guest_join.py` (备用方案B)
- 流程实锤:
  1. `VCMI_server.exe --port=3030` 独立启动 (~9s 加载)
  2. Python 先连当 host → 发 LobbyClientConnected(216) → server 分配 cid=1
  3. 真实 client1 `--testmap Maps/Twins.h3m --donotstartserver --serverport 3030 --headless` 连入 (VCMI_TESTMAP_ONLYAI=1) → server 分配 cid=2
  4. **关键**: Python(host) 发 LobbyChangeHost(225) — 手工帧 `bytes([0, 0, 0xe1, 0x01, 0x02])` (isNull=0 + pid=0 + tid=225 + newHost=2)
  5. client1 成为 host → 发 LobbySetMap(229, 1486B 完整 CMapInfo) 被接受
  6. server 广播: LobbyPrepareStartGame(223) → LobbyLoadProgress(221)×2 → LobbyStartGame(224, 171KB 全状态)
  7. GAMEPLAY: NewTurn(116) → PlayerStartsTurn(88, p0) → Python 发 EndTurn(180, player=0) → server "successfully applied" → PlayerEndsTurn(102) → PlayerStartsTurn(88, p1) → [SKIP] 蓝方不抢 → ModelAI 自主 TryMoveHero(109, 40B) → Turn 2 NewTurn → server zero fishy/not-allowed
- 根因发现: SetMap 是 host-only (visitForLobby 默认 isClientHost)。guest 发 SetMap 被静默拒绝 → mi 恒 NULL → client1 mi_loop 10s 超时。解法 = host 先 ChangeHost 让位给 client1。
- EndTurn(180) 帧格式: `isNull(0) + pid(LVarInt 0) + tid(LVarInt 180) + player(LVarInt) + requestID(LVarInt)`。player=0 (red)。
- 连续两回合 EndTurn 被 server 接受 ("successfully applied")，ModelAI 对手真实移动 (TryMoveHero 40B)，Turn 2 正常轮转，server 日志 zero fishy/not-allowed。
- **关键修复 (commit 9cc08b9)**: PlayerStartsTurn(88) 包体 = `queryID(LVarInt, -1→0x41) + player(LVarInt)`，非旧 Python 定义的 `player + time_limit`。旧脚本每个 88 包盲目发 EndTurn 导致蓝方回合 "not allowed/fishy" 拒绝；修复后解析 player 字段只在自己回合发，对方回合 SKIP。实机字节: 88帧 = `00 00 d800 41 00`。
- 剩余: 阶段3 = obs/决策接入 (MoveHero/Recruit 替换 EndTurn) → P8-C 混人回合 → P8-D 跨机器。

---

### 09-11 T13 外挂 AI 协议栈主体完成 + T13.10 Lobby/P8 前置 ✅ (2026-09-11)

**① T13.1-T13.9 实现与离线验证闭环**:
- 协议规格: `docs/序列化协议规格.md` 覆盖 TCP 帧、LVarInt、容器、字符串全局限去重、指针、optional/variant/bitset、类型 ID 注册表。
- 代码栈: `py/vcmi_protocol/` — `serialization.py` / `types.py` / `packs.py` / `connection.py` / `protocol.py` / `model_bridge.py` / `tests/test_e2e.py`。
- 包实现: 战略包 12 类、客户端包 18 类、战斗包 `MakeAction/BattleAction`、Lobby 基础包 7 类。
- Query: `QueryManager` 自动识别 Query 并发送 `QueryReply`，支持自定义 handler。
- 模型桥: `ModelBridge` 将 obs(3464) 转 action(0-24)，再映射为 CPackForServer 发送。
- 验证: `python py/vcmi_protocol/tests/test_e2e.py` → `144 passed, 0 failed / 144 total`。

**② T13.10 P8 前置落地**:
- 新增 Lobby 包: `LobbyClientConnected(216)` / `LobbyClientDisconnected(217)` / `LobbyChatMessage(218)` / `LobbyUpdateState(226)` / `LobbyQueryState(265)` / `LobbyModsCheck(266)` / `LobbySetMap(229)`。
- 新增 P8-A 实机入口: `python py/vcmi_protocol/tests/test_e2e.py --p8-1v7 --map Maps/Twins.h3m`。
- 判据定义: TCP 连 server → 收到 Lobby 状态 → AI 收 `PlayerStartsTurn` → 发 `EndTurn` 收 `PackageApplied` → 人+AI 同局完成 1 局无崩溃。
- 提交: 代码 `bdce29a`，文档 `81c67e6`。

**③ 边界/遗留**:
- 当前完成的是 P8-A/协议前置，不是真实多人对战闭环；离线解析通过不能证明 VCMI server 接受。
- 剩余 P8-B: 实机 server/client 启动并让 AI 加入/退出；P8-C: 人+AI 混合完整局；P8-D: 跨机器部署。
- 已沉淀踩坑 #185: T13.10 前置完成但实机多人局未完成。

---

### 09-10 三重修复上线 + 观察窗快照 + GUI 两层根因闭环 + 第三崩溃修复 ✅ (2026-09-10)

**① [GUARD]/[MINE] 位置重合判据系统性失真发现 + visitable 口径修复 (用户拍板 B+C)**:
- 假糖时序实锤 (ep_418 T06 逐行解剖): [GUARD] +100 打印在 battleStarted 之前 = "hero 路过静态位"假糖
- 机理链 (CGObjectInstance.cpp 源码实锤): vmap objects.json x,y = setAnchorPos 锚点语义; monster/hero 的 visitable 格 = pos − getVisitableOffset() (mask 'A' 相对位置, hero 3x2 offset=(1,0) 历史实锤) → hero 路过 anchor 格 = 贴脸不 visit = 判据假命中; hero 真战胜后站 visitable 格 = 判据漏奖
- 双实证: ep_1386 (T05 36X36_01) hero step42 站 (10,9) 拿 +100 全程 0 引擎战斗 (诊断局同图 hero=(3,3) 零偏移 = 图文件无问题); ep_418 [GUARD] step32 早于 battleStarted
- 影响面: [GUARD] 1442 条全 T05 时期假糖为主; [MINE] (==判据)/[TOWN] (dist<=1) 同失真风险 ([TOWN] owner 标注因两侧同 anchor 语义巧合自洽); T04 [MINE] 92% 口径存疑; T05/T06 高分局 +100 全为路过假糖, 剧本解读修正; capture 检测 (obs owner 口径) 不受影响 ✓
- 修复实现: ep_runner_one.py 新增 `_anchor_to_visitable(o)` — 解析 template.mask 复刻 C++ `calculateVisitableOffset` (y 外 x 内扫第一个 'A'/'T' 格, ObjectTemplate.cpp L245-249), get_guards/get_objectives mines 全换算; towns 保持 anchor 口径 ([TOWN] dist<=1 两侧同语义 241 次匹配自洽, 蓝城引导去 anchor 邻格=门格语义正确); 下游消费点 ([GUARD] 判据/守卫目标池/接近梯度/黑名单/v2 fail-count 贴脸计数) 全部经 get_guards 源头自动生效
- 验证: py_compile + mask 算法独立单测 3 组全过 + 真图对照 (T05 (10,9)→(9,8) = ep_1386 假阳性位正确化; T06 (5,7)→(4,6) = 0909 振荡局 hero 曾站位实锤)
- 诊断基建: py/diag_obj_dump.py (引擎 hero/towns 实位 dump, 单图 2s) + py/stat_t06_guard_battle.sh 入库

**② 观察窗快照复核 (29 局, py/snapshot_observe.py 可复用)**:
- visitable 修复核心验证 PASS: [GUARD] 26/26 全命中 visitable 真位 miss=0; [MINE] 26 与 [GUARD] 1:1 = 清守卫即占矿战斗-经济闭环真实化; ZOMBIE/ENDTURN_FUSE/异常局/HEROSEG_EMPTY 全 0; 正局率 28/29
- r 新基线不降反稳: avg_r 141.3 中位 154.7, 36X36_01_mir 160.2 / 52X52_01_mir 164.7 / 72X72_01 avg 171.1 (单局 188.6 新高) — 旧口径假发≈漏奖总量相抵, 净效应 = 时序/位置正确化
- capture 路径推进: 72X72_01 局长 73→156 步翻倍 (move_to_force=200 生效), [TOWN_CAPTURE] 仍 0, [TOWNSTALL] 18 次引导可达性残留
- 慢局 7/29 (24%) 被 [EP_TIME] 抓住 (52X52_01 10.6s/步 / 52X52_02_mir 8.9s/步, T06 长局 3.6-4.3s/步 大图天然慢需区分), 慢速样本积累启动
- 特征带全独立 (215/223-224/263/301) 无混图

**③ T06 蓝城引导卡点深挖 + BFS blocked 统一修复**:
- 引导状态机实锤 (ep_runner L747-805): 优先级 = 守卫 > 矿 > 回城取兵 > 蓝城 > 资源堆
- ep_2908 duel 实锤蓝城引导已激活推进正常 (hero (5,5)→(30,15), tgt=(69,69) plen=54, TOWNSTALL 时 pas 8 方向全 1)
- 卡点根因闭环: block=(31,16) = monster_2 vis 真位 — hero 贴脸怪 → guard_best 选中 → fail-count 3 步拉黑 → 落蓝城引导, 但蓝城 BFS blocked=dyn_blocked 不含守卫黑名单 → 路径仍穿已拉黑怪格 → 引擎拒 → move_stall 6 步 → TOWNSTALL, 守卫引导与蓝城引导撞同格两头堵死
- 修复 (零停训 ep_runner 层): 5 处 bfs_full_dir 调用 blocked 统一 `dyn_blocked | guard_blacklist - {hero当前格}` (排除 hero 格防自堵: 战斗胜后该格入黑名单, 起点 blocked 会致 BFS None 引导死); py_compile OK
- 生效判据: TOWNSTALL 后绕行推进 / 200 步 truncation 减少 / [TOWN] visited / [TOWN_CAPTURE] 首触发

**④ GUI 死锁两层根因闭环 + 重编修复 + 战斗链路验证 (插桩实锤)**:
- 根因① interfaceMutex 泄漏: onPacketReceived (CServerHandler.cpp:1061) scoped_lock 只覆盖 DISCONNECTING 检查 → CPlayerInterface::waitWhileDialog 的 makeUnlockGuard "解锁→等→析构重锁" 无人配对 → 每次回合切换泄漏一次锁 → 下一个获取者永久冻结
- 根因② SPECTATOR 空指针: testmap-onlyai 观众 playerID=-4 无 PlayerState → AdventureMapShortcuts::optionCanViewQuests L647 getPlayerState->quests.empty() 空指针读 0x6d8
- 修复: optional<unique_lock> 持锁覆盖整个 pack->visit (恢复"包处理持锁"不变量) + optionCanViewQuests 判空
- 验证 (gui12): --testmap Maps/Twins.h3m 全 AI 局 (ModelAI×2) 跑至 day=31, 62 次 INFER, 7 battleStarted / 1 battleFinished(winner=1) 闭合, 零冻结零崩溃; heroMoved→endTurn 多轮闭环 = 09-09 ModelAI 修复在设计路径上工作正常
- 诊断突破: [THREAD]×16 插桩 (7ebadd80ea 落地) + GameEngine::LoggingMutex ([MUTEX] LOCKED/UNLOCK tid+ra 打点, ra=__builtin_return_address→pdata 定位锁点; winpthreads Normal mutex 不记录 owner, dump 无法定位持锁者必须运行时打点); winpthreads pthread_mutex_t=指针+内部结构, 0x1618 是 event 句柄非 TID
- IME 根治: EntryPoint.cpp main() 开头 ImmDisableIME(0) (动态加载 imm32.dll), 搜狗不再注入 (踩坑 #151 闭环)
- 重编坑: libFacade $<TARGET_PROPERTY> regen 泄漏 $<LINK_ONLY> 进 build.ninja (sed 修补) / serverapp 行尾重复 vcmi 修 MinGW 链接序 / facade 补 VCMI_DLL=1+server/strategic_state.cpp (ENABLE_ML=OFF 缺 adventure_capture_turn) / NK2 三处 getDate→getCalendar().getCurrentDay() / BattleInterface windows.h 需 NOMINMAX+NOGDI+NOUSER+#undef IGNORE (winbase.h IGNORE 撞 Canvas.h 枚举)
- 三产物 09-10 版重编落地 (备份 bin_backup_0910); 复测口径变更: --testmap 直开全 AI 局 (bypass lobby SelectionTab 空指针, 该崩溃未修但已无关); 残留: [MUTEX] 差值 -2 无害待观察 / "actGot false" = ModelAI 出招偶发被拒非致命; 插桩保留作复发雷达

**⑤ 第三崩溃立项 (观众 SPECTATOR 战斗结算后 UI 空指针) — 根因闭环 + 修复重编生效**:
- 崩溃现场 (crashinfo.dmp 18:13 版, gui12 浸泡 day=31 后): 主线程 0xC0000005 读 0x10, RIP=exe+0x20c09f, rdx=0xfffffffc=-4=SPECTATOR
- 反汇编定位 (py/disasm_gui12_crash3.py: .pdata 函数边界 + 崩点对齐扫描 + 字符串四重锚定 "Nothing is selected"/"showMovePath"/"gameTweaks"/"simpleObjectSelection") → 崩点 = AdventureMapInterface::onTileLeftClicked L548: GAME->interface()->localState → getCurrentArmy() 返回 null → `->ID != Obj::HERO(34)` 解引用 null
- 根因链: 观众永不轮到回合 → onPlayerTurnStarted 的 setSelection 初始化链 (getCurrentHero→getOwnedTowns→getWanderingHero 三级 fallback, AdventureMapInterface.cpp L446-456) 永不触发 → PlayerLocalState::currentSelection 恒 null → L548 无判空直接解引用; battleFinished 结算后观众 UI 恢复冒险地图, 偶发输入触发收雷
- 与第二崩溃 (optionCanViewQuests) 同族 = 观众接口缺 SPECTATOR 防护系列第三例; 官方 onTileHovered L629 自己判空 ("fake move before full initialization"), onTileLeftClicked 漏判 = 官方缺陷
- 同族审计 (全客户端 getCurrentArmy()-> 无判空 5 处): onTileHovered 有早退防护安全 ✓ / ClientCommandManager 三处 = 手动调试命令低风险不动
- 修复 (Windows vcmi/ 树 L548 前判空 return, 双覆盖: 观战 + 玩家英雄全灭 removeWanderingHero 后同雷)
- 重编 (09-10 19:24, 停训窗核实后): ninja 增量 6 步全绿, VCMI_client.exe 19:24:16 版 (1.8.0.ec87c2826a); headless 冒烟 PASS (5 分钟主循环流转 + BattleEnded 闭合 + stderr 零字节)
- ⚠ 新踩 PATH 坑: 非 msys2 PATH 下 cc1plus DLL_NOT_FOUND 静默失败 (EXIT=1 零输出, exit code 3221225781), 编译前必须注入 C:\msys64\mingw64\bin
- 唯一遗留: GUI 浸泡复测 (需用户在场) → 当日晚 soak_gui15 PASS，见本条目⑨

**⑥ a1ea3f4d2d 重编待办结案 (停训窗核实)**:
- libMMAI.so 无需重编二次实锤 (训练 --blue_ai MMAI_RANDOM, MMAI/ 目录 race 代码 grep 0 命中, 运行时无 receivedAnswerConfirmation 符号 = NK2 域专属) — 误登记再现 (踩坑 #171)
- 同窗发现 NK2 源码树↔构建树双向分叉: 构建树独有 TBB 防 OOM fix (C8.5 max_allowed_parallelism=4), 源码树独有 a1ea3f4d2d + [ML-*] 打点 + battleEnded/ring6 fix, 直接任一侧重编会丢对方修复 → 已合并 (TBB fix 摘回源码树 + cp 构建树 diff IDENTICAL; Windows 树精确插入 TBB 3 行)
- Windows 树另有引擎 API 级分叉 (getCalendar/showGarrisonDialog MetaString 新签名 = 09-09 NK2 编译修复产物), 与 WSL 旧 API 各自匹配各自引擎, 不可镜像同步只做 fix 级摘取
- 三树双标记验证全绿 (a1ea=2/tbb=1), 备份 /tmp/AIGateway.cpp.bak_{src,build}_0910
- NK2 运行时 so 未重编 (libNullkiller2.so 仍 09-02 产物, 但 NK2 全链路无消费者: 训练 MMAI_RANDOM / eval StupidAI / GUI ModelAI), 回用 NK2 时 (P6 对手池) 直接重编即生效
- ⚠ removeQuery 语义变化需记住: 锁已挪至 receivedAnswerConfirmation 函数头, 未来新调用点必须自带外层锁

**⑦ 慢速画像基建 [EP_TIME]**: 历史慢局样本确认丢失 (ep 日志轮换覆盖 + ep 行无 time 字段) → ep_runner 加局耗时打点 [EP_TIME] map= steps= secs= r= err= (L326 t0 / L1221 打印) + 主日志白名单补录 (主进程改动优雅重启 step=599094 零损失); 基线首样本 52X52_02_mir 73 步 94s = 1.29s/步正常档

**⑧ 36X36_02 重评估结论** (docs/方案_T05_36X36_02重评估_20260910.md):
- 地形压制假设排除 (两图 0 岩石 0 水体纯草地布局同构); 真实差异 = 守卫减半 + red 半场近资源 (18,5)/(5,18) 移远 (18,30)/(30,18) → red 短目标真空长局空转大负分自洽
- 51 局全负数据采集于判据失真时代归因失效 → 建议第一步原样回池新口径重评估 (判据: 与 01 局均 r 差异<20%), 第二步差异化重设计备料 (图 03 = 01 骨架 + gold mine 加 swordsman 6 守门 = 矿-战斗耦合课程); 图池变更与 T7.4/capture 错窗纪律

**⑨ GUI 五崩修复 + soak_gui15 浸泡复测 PASS (09-10 晚, 活跃任务收尾)**:
- 五崩全部修复并重编生效: 第三崩 ①interfaceMutex 泄漏死锁 + ②SPECTATOR 空指针判空 (见④⑤); 第四崩 = showOverview 观众模式 spectator 守卫; 第五崩 = runServer + runNetwork 线程 catch-all 兜底。
- 用户全流程浸泡 (战斗结算/层切换/系统菜单/KingdomOverview 直接点击) 确认无问题; stderr 93617 行零 THREW / 零 Disaster。
- 非阻塞遗留: 第五崩原始 throw 点 (已埋点, 复发即报 e.what()) / [MUTEX] 差值 / 良性刷屏两源。
- 指针: 踩坑 #164/#165 + 知识库「GUI 观众模式崩溃系列闭环」章; 工具 py/parse_crash_dmp.py 留用。

---

### 09-09 T06 capture 引导四连修 + 守卫振荡 v2 + GUI 死锁根因闭环 ✅ (2026-09-09)

**① capture 激励首窗复盘 + 引导断链修复 (用户授权 AI 分析拍板)**:
- 首窗数据: capture 上线后 1v3 新增 24 局 + duel 27 局占城 0 触发; 1v3 尾窗 avg 77.6 (较结案 81.1 回落 4.3%, 比值 0.95 达标) / 自发 100% / 200 步局 0% / ZOMBIE 0, 全局 112 局; BHERO_KILL 空拍防护生效 0 新误报
- 根因链闭合 (T06 近 30 局画像): [START_HOME]×3/局 + [TOWN_VISIT]×2-3/局 + [GUARD]×1/局 + [GUARD_DONE]×1/局 (100% 守卫胜收局) + [MINE]=0 / [TOWN]=0 → 因果链 = 72X72 大图 target_list top-8 距离排序被近目标挤占 (P10 已知瓶颈) → 矿引导 obj_best 恒 None → mine_taken 永 False → 蓝城引导激活条件永假 → capture +100 死信 (守卫奖能被发现是因守卫挡路)
- 修复 (零停训 ep_runner 层): ep_runner_one.py L655-656 T06 图绕过 mine_taken 门槛 (`mine_taken or _t06_direct`), T04/T05 不变; town 引导分支基础设施全现成 (BFS 可达过滤 / move_stall 6 步放弃 / town_blocked 本局禁用 / countdown 重置 L735)

**② T06 双死锁修复 (args 覆盖式, 用户拍板先停训再修)**:
- 机理实锤: [GUARD_DONE] 打点 guard_done_steps=15 (守卫胜后 15 步收局, 引导只活 15 步) + move_to_force=60 强制窗外模型不采 24; 后续再修正: MOVE_TO 引导是动作替换制 (move_target 非 None 即接管, 24 只是中间语义被翻译成方向, 全历史 0 命中) — move_to_force 窗是伪死锁, guard_done=15 才是真死锁 (蓝城 ~95 格 15 步走不到)
- 修复: ep_runner_one.py L126-132 args Namespace 属性覆盖 (T06 图分支: move_to_force=200 全程 + guard_done_steps=0 禁收局, 200 truncation 兜底); parse_args 后立即覆盖, 下游引用点 (L494 强制窗/L851 countdown 启动/L1132 GUARD_DONE) 自动生效; guard_done=0 时 countdown 恒 None, 所有重置点 `is not None` 守卫自动跳过
- 部署: 优雅停 step=596306 零损失 → py_compile + 清 pycache → restart_train_v5.sh → resume 零损失验证

**③ 取兵震荡修复**: 修复后首局 T06 duel 200 步 truncation r=28.68 (guard_done=0 生效, 不再 73 步收局), 解剖出 [TOWN_VISIT]×4 (step 67/105/145/187, garrison 每周期回满 → own_town 引导复活) 与蓝城引导反复打架, 200 步耗在往返; 亮点 = 守卫胜 +100 ✓ + act 大量朝蓝城方向 ✓ → 修复: ep_runner L684-685 T06 取兵引导限次 2 (`own_town_guide_count >= 2` 禁用, 复用 L367 计数器), T04/T05 不限

**④ 守卫振荡陷阱定位 + 黑名单修复 v1→v2**:
- 定位 (traj_ep.json hero 位置序列): T06 duel 局 hero (1,2)→(5,7) 守卫格后 (4,6)↔(5,7) 回跳振荡 26/60 步 (唯一位置仅 9 个, 两局 act 逐字相同); 战斗未触发且排除逻辑只在 hero 恰在格上生效, 离格后 guard_best 又选中 → 往复; cycle_detect=5 八步窗抓不到 2 周期振荡 (回位 4 次<5), 位置级罚分只扣分不强改
- v1 失败: 站上格条件没接住 (hero 邻格被引擎拒从未站上, 验证局 act 逐字复现振荡)
- v2 生效: fail-count 贴脸计数 (guard_best 选中且 dist<=2 每步 +1, 3 步未胜黑名单) + 接近梯度块同步排除黑名单守卫 (防梯度拉回成新振荡源); 不依赖站上, 邻格停滞即计数
- v2 验证实锤: T06 局 r 序列 28.68 → 30.03 → 41.80 (v1) → 93.95 (v2, 46 步守卫胜快速闭环) — 振荡解除
- 知识库新章 + 踩坑 #144-#147 已沉淀 (MOVE_TO 翻译层 / 振荡陷阱 / BHERO_KILL 空拍 / 间歇性慢速)
- 引擎侧遗留: T06 守卫对象战斗未触发疑与 fix_t06_maps.py 补丁 (aggression=guard 重打) 相关, 待 capture 打通后专项

**⑤ 0909 晨二次复盘两项发现**:
- T06 引导修复未生效 (双重死锁实锤): 修复部署后 T06 52 局 capture 仍 0, 全部 73 步守卫胜收局 → 促成②修复
- 16% 局单步卡 300s (fuse 兜底特征, R7 复发中等置信): 199 局 32 局超长 311-398s (平均 136s vs 正常 99s), 全程均匀分布跨图 (T05×27/T06×7), 步数/r 正常; ep=200 query 链打点: qid37/38 popIfTop FAIL (top=null) + MapObjectVisitQuery notify-done ✓ + CHeroMovementQuery qid37 onExposure ENTER 后无 visit-done = 链路中断 (与 R7 三候选兼容, battleFinished RETURN 0 命中), 但 38 回收完成与完整卡死判据不完全吻合 → 处置: fuse 兜底非阻塞 (局不死仅慢 300s), 登记观察积累样本, 恶化再专项

**⑥ Windows GUI 死锁根因闭环 + ModelAI 回调修复**:
- 根因 (minidump 锁 owner 实锤): ModelAI heroMoved 回调 (网络线程) 启动 detached 延迟线程调 endTurn → 线程生命周期失控, 持锁路径随退出失效 → ENGINE->interfaceMutex 永久失锁 → 主线程+runNetwork 双死等
- 修复 (D:\vcmi_model_ai\model_ai.cpp, 旧版备份 .bak_0908_deadlock): 移除 detached 线程 → heroMoved (无战斗) / battleEnded (有战斗) 回调内同步 endTurn (waitTillRealize=false 非阻塞) + battle_active 原子变量分流 + in_my_turn 重置防误触发; NK2 编译错误三连修 (turnCounter 声明 / getCalendar / showGarrisonDialog 参数)
- 验证: gui3 复测 endTurn after heroMoved 正常流转; minidump 工具链固化 (py/ 五件套: take_dump / walk_stuck_dump / identify_all_threads / stuck4_cfbb / stuck4_heap)
- [THREAD] 打点 ×5 处落 vcmi/ 源码树 (VCMI_client.exe 未重编前不生效, 后 09-10 重编带上)

**⑦ GUI 复测弹窗崩溃定性 = 系统资源耗尽, VCMI 无责**: 事件日志实锤 — 07:14 Resource-Exhaustion 2004 (3×python.exe 共 36GB commit, 各 11.5-12.5GB) → 07:18-19 pwsh/GDEPService/dwm.exe 四进程连锁崩 + LiveKernelEvent 141; VCMI 无 WER APPCRASH 无新 rpt/dmp; 排查方法论: 多进程齐崩 = 资源耗尽指纹, 先查 WER (Application 1000/1001 + System 2004) 再怀疑代码 (踩坑 #149); 干净环境复测 checklist 入知识库

---

### 09-08 1v3 结案 + capture 激励上线 + battle-only/GUI 战斗链闭环 ✅ (2026-09-08)

**① 1v3 课程正式结案 (用户拍板)**: 全局累计 88 局超结案线 (30), 尾窗四判据全达标 — avg_r 81.1 (比值 0.97≥0.8) / 自发经济 100% / 200 步局 0% / 守卫胜闭环 100% vs duel 基线 96.9% / ZOMBIE 0; 回退线全程未接近 (avg 反超 duel 基线 +7%, 挂死 0%)。口径修正: ①蓝英雄击杀 0 = 真实观测 (BHERO_KILL 埋点上线验证, T05 图 6 起, 1v3 图 blue 3 英雄未与红方交战) ②完整胜利路径 (消灭蓝英雄+占蓝城) 无局达成 → 遗留课题。结案后训练继续跑积累 1v3 经验不停训

**② 下一难度轴拍板 + 完整胜利路径引导执行 (用户授权 AI 拍板)**: 四候选对比选 ④ 1v3 完整胜利路径引导 (激励轴) — ③等数据是伪选项 (剧本锁定 r 67.7~88.0 每局同构守卫胜收局, 激励不变数据只重复); ①② 地图轴留下一窗; 08-31 拍板已预授权 capture 大额方案。实施 (全部零停训, ep_runner 子进程每局重启生效): ①TOWN_CAPTURE 接 +100 奖励 (ep_runner_one.py L943-957, 仅 T06 双图生效, 复用守卫一次性事件模式每城一次封顶, countdown 重置防占城后 truncation, 检测滞后 1 步) ②旁路事件文件 battle_quality_events.log (主日志白名单无 TOWN_CAPTURE, 行带 map= 自含归属) ③check_duel_watch.py 升级 (per-map 占城列 + 结案面板旁路口径)
- ⚠ 附带发现 BHERO_KILL 空拍误报: 上线后 9 起 (T05×6+T06×3) 全 live_slots=0 = heroes 段整段空拍; 空拍防护已加 (跳过差集与 prev 更新); 根因 (0910 实锤) = strategic_state.cpp L644 先清 8 槽 id=-1 后填充循环 0 行, 时机特征 (T06 三起同 step 56 = 守卫战斗触发点) → 主候选战斗瞬态竞态 (obs 填充跑 yourTurn 线程恰落 server 战斗初始化/结算窗口); 诊断埋点 [HEROSEG_EMPTY] 已部署 (ep_runner L907-916, 复发一击定位: 全槽 -1=竞态 / 全槽 0=共享内存未填充 / red 在 blue 空=owner 异常 / red 也空=整段视角异常); 空拍只损失击杀观测不影响训练正确性

**③ battle-only 场景 ModelAI 战斗 AI 选择 — 静态+运行时双验证**: 链路实锤 (fork 1.8 源码): battle-only 全 AI 对局玩家 AI = red 走 g_adventure_allied_ai 注入或 settings["ai"]["adventureAlliedAI"], 非红走 aiNameForPlayer() → adventureEnemyAI → CDynLibHandler::getNewAI → AI/<name>.dll — 走 adventure AI 链而非 getNewBattleAI; aiNameForPlayer 有 DLL 存在性检查。四要素全绿: ①ModelAI.dll 导出 GetNewAI (CGlobalAI 工厂 model_ai.cpp L569, 战斗回调由 CGlobalAI→CBattleGameInterface 继承承接) ②schema enum allow "ModelAI" (660ea59c28) ③运行时 settings 双配 ④DLL+onnxruntime.dll+bc_model_v3464b.onnx 部署在位。运行时冒烟: DLL 污染修复 (踩坑 #172: 24 种 GCC→统一 msys64 GCC 16.x) → ModelAI 加载成功 (blue+green lead by ModelAI); 崩在 AI 首轮 (后 09-08 下午闭环见⑤)

**④ v13 战斗 onnx 数据缺口补齐 + 推理验证 PASS**: 缺口定性 — bc_model_v3464b.onnx 实为战略模型 (metadata 无 version 键, MMAI v13 factory readVersion 拒绝), defender-ipnkyfqb-best3.onnx 是 38B 断链占位文本 → 全项目无有效 v13 战斗 onnx (数据缺口非代码缺口); 本地重导出不可行 (战斗 checkpoint 缺失) → 官方 vcmi-gym 成品 defender-fqcbvmti-best7.onnx (v1.4 4 输入静态版 22.4MB) 部署 ~/mmai-battle-test + mmai-settings.json 四键。GUI 全链路两次受挫 (run1 red 避战 3 轮不触发加载 / run2 NK2/GUI 层 ~5min 随机 core dump) → 改 onnxruntime 直推验证 (py/verify_v13_inference.py 按 C++ V13::NNModel 4 输入接口) → VERIFY PASS (6 输出 shape 全精确匹配 + act0 softmax 和=1.0) → 数据缺口正式闭环。遗留: GUI 全链路待客户端稳定后复验; side=1 defender 跑 attacker 键已知妥协 (报错则补 v2.0 attacker-pdpyqkrb)

**⑤ Windows battle-only AI 首轮崩根因闭环**: 根因 = AI DLL 缺失 — client/Client.cpp:253-257 全 AI 玩家无条件走 adventureEnemyAI (默认 "Nullkiller2") 而 bin/AI/ 仅 ModelAI.dll + 2016 官方旧 BattleAI.dll, build.ninja 只有 NK2/StupidAI 的 .obj 规则无链接目标 (fork Windows 构建从未产出任何 adventure AI DLL) → LoadLibraryW error 126 → throw → "Disaster happened"。排查方法: Windows 事件日志三类签名 → py/analyze_vcmi_dump.py (minidump 解析, 手动 CONTEXT x64, dump 无栈内存) → IFEO PageHeap (GlobalFlag=0x02000000+PageHeapFlags=3 无需 Debugging Tools) + headless testmap 复现一击命中。修复 = settings.json (My Games/vcmi/config) 恢复 ai.adventureAlliedAI/adventureEnemyAI = "ModelAI" (此前 StupidAI 复测后被清空)。验证 PASS: headless testmap (Twins.h3m) 跑至 day=31, ModelAI 战斗真实发生 query 链闭合 ([BTL-AI] battleEnded + CGCreature::battleFinished winner=1)。附带发现: ①Windows ModelAI.dll = fork 自研版 (model_ai.cpp/model_infer.cpp, MODELAI_MODEL 环境变量, 默认 rl_model_v3464t_0829.onnx) — 与 WSL MMAI v13 factory 两套体系, fqcbvmti v13 不直接适用 ②"堆损坏 0xc0000374" = throw/abort 次生表象 (PageHeap 全程无真崩) ③两树 diff 实锤 R5: ServerPlugin.cpp (HeroPool 放宽未同步 Windows) / CGameHandler.cpp / CServerHandler.cpp (ENGINE 保护 Windows 已含) / Client.cpp 四文件分叉 ④官方 VCMI 1.7.5 (MSVC) + ModelAI.dll (GCC) ABI 不兼容 (踩坑 #173) 永远不能加载。PageHeap 注册表已清理。battle-only 链路打通 = T8 战斗集成前置条件就绪

**⑥ router bug 修复版 .so 停机窗口同步**: MMAI router.cpp 资源路径 bug 修复版 libMMAI.so (77840da2) 同步 vcmi-native/rel/bin/AI/ + vtest/bin/AI/ 两处旧版 (3a28f494), 四副本一致 (构建树/训练目录/vtest/mmai-battle-test), 旧版备份 ~/backup-so-sync-0908/; 铁律流: 优雅停 step=580111 → cp → systemd-run 重启 (踩坑 #168 二次复现: stop 后 systemctl start not found, is-active 对已消失 unit 输出 inactive 有迷惑性) → resume 无缝; 训练走 baggage 路径不触发此 bug 行为零影响

---

### 09-07 1v3 数据面达标 + 训练全面后台化 + a1ea3f4d2d 纠偏 ✅ (2026-09-07)

**① 1v3 首周判据数据面达标 + 训练全面后台化**: 观察窗爬坡 — 首夜窗 23 有效局 avg≈68.9 (含 r=4.0 战败) → 午后窗口 15 局 avg≈80.4 (75.1~86.11) 超 duel 基线 75.5, 前 1/3 80.0 → 后 1/3 81.8 上行, 早期战败局消失; 自发 100% / 200 步局 0% / ZOMBIE 0 / obs_nz=301 带稳定; 回退线从未接近。多敌图 reward 反超 duel 解读: 3 蓝城+5 金矿经济事件目标更多 + 蓝方 roaming 未形成压制, 非战斗变强。运维: 16:09 发现 unit+keepalive 双 inactive (前台终端实例死亡风险兑现) → #169 恢复序第二次实战 (keepalive PID 389 → restart_train_v5.sh → resume 555165, 损失 ~1245 步); 定论: 训练一律后台 transient unit, 禁前台终端跑法 (无存档兜底)。统计基建: 多 resume 日志窗口取法固化 (踩坑 #170 awk 陷阱 + 行号法), check_duel_watch.py 尾窗口径与行号窗口互补

**② a1ea3f4d2d 部署纠偏结案 + 开机零损失恢复**: 纠偏 — 原登记"重编 libMMAI.so"有误: 改动 AI/Nullkiller2/AIGateway.cpp 属 libNullkiller2.so 域, 训练栈加载 libMMAI.so (--blue_ai MMAI_RANDOM, NK2 已弃用), MMAI/ 无同构 race 代码 (grep 0 命中) → 无需重编, race 不在训练链路; 改动保留 NK2 源码树, 回用 NK2 时重编即生效 (踩坑 #171: 登记待重编任务前双确认 target 归属 + 运行时加载链); cmake 零编译行 = 依赖未变正确信号。开机恢复 (#169 恢复序第三次): 关机前优雅 stop 存档 564079 → 开机 keepalive + restart_train_v5.sh → resume 564079 零损失 — 实证"优雅停=零损失 vs 被动死=回滚", 关机前主动 stop 入标准流程。battle-only GUI 冒烟四要素静态全绿 (运行时冒烟后续 09-08 完成)

---

### 09-06 T06 课程启动: duel→1v3 + 双根因修复 ✅ (2026-09-06, 含 09-05 扩图)

**① T05 扩图 3→7 张 (09-05)**: ①3 张可用图镜像变体 (gen_t05_expand.py, 180° 旋转守卫同步旋转难度不变) ②36X36_02 守卫减半回归 (swordsman 8→4/archer 10→5/peasant 15→8 可赢难度), 双路径同步 (Maps/training + rel/bin/data/Maps, 踩坑 #143 事实 2)。验证: 52X52_01_mir 首次真实加载 obs_nz=215 独立特征带 (区别原版 223) + 原版 r=150.44 新高 — map= 标签真实性检验法 (obs_nz 特征带) 首次实战应用成功

**② 330ep overnight 复查 + 36X36_02 二次移除**: 战斗剧本大爆发 — [GUARD] 252 场/330ep + [MINE] 250 + [RECRUITED] 5962 + _mir 镜像图成最优图集 (52X52_01_mir 多局 147-167 / 52X52_02_mir 147-168 / 36X36_01_mir 134-148, 全场新高 168.48) + avg_r 纯 T05 口径 1.8~3.4 + 200 步局降至 8% + ZOMBIE/RouterEx 0。36X36_02 调守卫失败二次移除: 减半后 51 局全负 (-50→-181 恶化趋势) — 非守卫兵量问题, 疑守卫位置/地形结构压制, 回归需重设计。当前 MAPS 6 图全绿, step=524897 resume; T05 全面化判据 4/4 全达标

**③ T06 前置评估 + 方案 A 执行 (用户拍板)**: 评估结论 — 4 张 T06 (72X72_01/02 + 108X108_01/02) 结构 = 1v3 (1 red vs 3 blue 英雄+3 蓝城) + 5 金矿 + 15 资源 + 20 野怪, 一次叠加 3 个难度轴违反一次一轴纪律; 三类非法 identifier 与 T05 同套路 (hero core:inham / resource core:resourceGold 带 amount 缺失 / monster 集含空格 bug); obs 兼容 ✓ (MAX_HEROES=8, 英雄段 8 槽×26 字段无盲区; 地形走 terrain_grid.bin 旁路不破 3464 冻结)。方案 A (只加大图轴): ①fix_t06_maps.py 修 4 图全部 identifier ②生成 T06_adventure_72X72_01_duel.vmap (去 hero_2/3 + town_2/3 → 1v1 对角) ③inspect 5 图全绿 + 双路径同步。单集冒烟 73 步守卫胜 r=+75.31 零卡死; 混入 MAPS 7 图, duel 首集 ep_steps=73 r=+84.74 obs_nz=263 time=97s — T05 战斗剧本无缝迁移 72X72 大图。运维固化: transient unit 停止即消失 (--collect), 重启必须 systemd-run 重建 → py/restart_train_v5.sh

**④ duel 判据草案 + 变体矩阵备料**: duel 站稳判据 4 条 (局数≥30 无恶化 / 自发≥80% / 200 步局≤20% / 守卫胜≥70%) 触发上 1v3; 1v3 上线方案 (混 1 张原版, 回退线 avg -30% 或挂死>40%, 纪律: 与其他轴不同窗)。duel 变体矩阵备料 ✅: gen_t06_duel.py 生成 72X72_02_duel + 108X108_01/02_duel (inspect 全绿未入 MAPS 即插即用; ⚠ 108 对角 ~145 格 200 步上限可完成度先观察); 数据源 py/check_duel_watch.py

**⑤ 1v3 上线 + 双根因修复 (09-06 晚)**: duel 判据 4/4 终判达标 (32 局, 后 1/3 avg 83.0 vs 前 1/3 65.1 比值 1.27 / 自发 100% / 0% 挂死 / 守卫胜 96.9%) → MAPS 8 图混入 72X72_01 原版。首两局暴露双根因: ①core:alchemist 是职业名 (hero subtype 字段) 非英雄名, 引擎报 Couldn't resolve hero identifier 拒启动 → 4 原图 hero_3 alchemist→core:christian (castle.json 真实名; 教训: identifier 校验集必须实测验证, 不能混入 subtype/模板类字段值; fix_t06_hero3.py, inspect/fix 工具 VALID_HERO 同步修正) ②fork 训练栈 server/ML/ServerPlugin.cpp HeroPool 对称性校验 (2 owner 时双方 pool 英雄数必须相等, 1≠3 throw) → 校验放宽: pool 大小不等降级 stdout 警告 (pool 名不同仍 throw); libmlserverplugin.so 独立库不触 libvcmi.so 铁律 (单文件重编, 备份 .bak_pool_0906, vtest+hero3_vcmi 副本同步)。1v3 真实首局 ep_steps=73 r=82.38 与 duel 基线同水平。隔夜运维 (09-07 晨): keepalive 丢失 → VM idle shutdown 非优雅中断 (踩坑 #169) → checkpoint 回滚 546379; 四坑 #166~#169 入踩坑点, T06 双根因 + a1ea3f4d2d/battle-only 两章入知识库

---

### 09-03 R6 战斗修复 + T05 混入 + 加图 + 经济塌方拍板 ✅ (2026-09-03~09-04)

**① 观察窗复查 + 加图执行 (触发条件①满足)**: 96ep/5.8h 复查 — Router fallback 0 / ZOMBIE 0 / FUSE 0 全程健康; avg_r 0.2~7.3 无上台阶; 自发经济仍 0 (16-18 共 122 次=强制窗供给); 固化依旧 (ep 结局同构逐字反复, 短局占比升高) → 加图。gen_level3.py 与实际不符 (含守卫怪, 解包 objects.json 实锤 T04 实际 0 守卫: 2 城+2 英雄+1 金矿+5 资源堆) → 改 180° 旋转变体方案 (py/gen_t04_mirror.py: terrain 行列反转+对象坐标变换, 保留 0 守卫/城/矿/资源/英雄/胜利条件 = 同级同特征零难度轴), 生成 6 张 _mir (20X20/30X30/36X36 各 2), MAPS 12 图, 镜像图加载与泛化正常。运行时地图路径实锤: 训练 CWD=项目根, VCMI ResourceHandler 读 Maps/training/ (relative), 无需额外副本同步

**② T05 混入轮换 (用户拍板: 战斗密度提升打破固化剧本)**: 动作重复专项定量 — 6 ep act 序列逐字节相同 + 相邻重复 41% + hermes 'Cannot move hero' 56 次/局 (选→拒→盲重试); 固化三层机制 = 策略熵塌缩 (根本) + 引擎拒绝盲重试 (放大) + start_home/经济窗固定段落; 12 张同构图探索糖吃完 + 战斗密度 ≈0 → T05 混入 (自带 4 守卫挡矿路 = 战斗信号密度, 不动经济奖励参数): MAPS 16 图。T05 首 ep 即兑现: [GUARD] guard fought & won +100 首落袋 (R6 修复后战斗奖励首次经策略模型打通) + GUARD_DONE 终局机制苏醒 + r=131

**③ 200 步挂死局诊断修复 → 09-04 复查回滚**: r=-126 局解剖 — start_home 取兵正常 → econ_force 瓜分 → move_to_force 60 步窗被瓜分殆尽 → 60 步后引导撤走, 模型面对陌生 T05 图熵塌缩 → END_TURN 连刷 28 次 + 城边徘徊 140 步 → 200 步空转耗尽。修复尝试 move_to_force 60→200 全程引导 → 09-04 复查判定过度修正回滚 60 (200 版 13/32 挂死暴增 + r=-429/-280/-191 + END_TURN 1089 次成第一大动作 34% + TOWNSTALL 10.4/ep — 全程引导把英雄拖着撞墙; 60 版 avg_r 1.8 有 +131 局明显更优)。遗留: 200 步局根因 = obj_best 引导目标可达性 (TOWNSTALL 实锤引导失效), 引导目标选择机制优化待立项

**④ R6 战斗 AI 恒 fallback StupidAI 修复**: 根因 — MMAI/CMakeLists L60 `add_definitions(-DUSING_ONNX=0)` 硬编码 + 无 onnxruntime 依赖 → CreateBAI return 空指针 → router 断言 → 红方战斗恒 StupidAI (修复前全部训练成果均在此条件下取得, 08-31 定位)。修复: ①onnxruntime 1.19.2 linux-x64 部署 /opt/onnxruntime + ldconfig (WSL 断网 Windows curl 中转 /mnt/d, 踩坑 #141) ②CMakeLists USING_ONNX=1 + target_link, 双树同步 ③停训窗重编 libMMAI 一次通过 (21.5MB, ldd OK, vtest 副本同步) ④resume 验证: 战斗实测发生 (ep_11638 三层 query 栈 movement→visit→battle 与 R7 机理链一致) + Router fallback 零出现 (修复前每战必有) + V13::BAI 接管。⚠ 战斗强度变化 = 非平稳 (课程节点一次付, 战略层 act 序列不变已验证)。定向冒烟 (T03 守卫图 守卫 3→8 均势): 战斗触发 + 守卫消灭 + winner 确定性结算 + query 链闭合 = VCMI 战斗循环完整执行 (战斗在 server 内部同步毫秒级完成)。附带修复: AAI.cpp:396-401 if 作用域缺陷 (P1b 引入: fprintf 单语句抢走 if 作用域 → recruitCreatures 无条件对所有 tier 执行, 空 tier second.front() = UB → 冒烟 segfault libMMAI+0x49948; 训练 96ep UB 潜伏未爆) — 加花括号+break。新发现: ①core:footman 当前 fork 缺失 (T05 全部 4 图加载失败实锤, gen_level3 含 footman 守卫同样不可用 — T04 实际 0 守卫的历史成因) ②VCMI moveHero 到敌英雄格被 "destination blocked" 拒 (英雄战不由此触发, T04 win"消灭对方英雄"达成机制待查) ③冒烟图运行时路径 = rel/bin/data/Maps/

**⑤ R7 battle query 卡顶立项排查 (五件套)**: 机理链闭合 — 移动踩守卫/野怪 → 栈叠三层 movement→visit→battle (ep_4040 实锤); moveHero/visitObject 末尾 popIfTop FAIL×2 = 原版正常; 战斗结束 endBattleConfirm → removeQuery(battle) → visit.onExposure (ENTER 实锤到达) → notifyObjectAboutRemoval 内部中断 (BattleQueries.cpp:38-41) → 残留 query → RED 全部动作被拒 → 回合系统死等 → 冻结。已排除: restartBattle/L86 result 清空 (RED isHuman=false 不可达) / heroGotLevel queryID=-1 (升级 fix AI 自动选技能无害)。三候选待埋点区分: result 空跳过 / visitedObject 非 CGCreature / battleFinished DEFENDER 分支死循环或 hero 悬空指针。埋点增强部署 (notifyObjectAboutRemoval hasResult+objType / battleFinished RETURN winner / onExposure 补 qid / popIfTop OK) + fuse 900→300s (strategic_env.py:861) + 双树同步 mlclient/MMAI 重编 resume (step=442907) 验证通过。Router dynamic cast 失败 (router.cpp:442) = 并存独立异常, 已随 R6 修复解决。阶段性结案: 埋点部署完成无复发, 转长期观察

**⑥ 经济塌方定量实锤 + 拍板 (用户)**: 最近 12 ep 515 步动作分布 = 移动交互 494 + 自发经济动作 0 (16-18 全部来自 start_home economy_force 强制窗, 19-21 建设类 0); S1 判据"招兵/建设频率不塌"已塌 → S2 暂停。定性候选: ①糖减半后自发招兵期望收益<探索糖 ②start_home 白送兵掩盖自发需求 ③R6 前兵力无战斗力兑现信号弱 ④长局 act 高度重复探索糖主导。拍板: 经济塌方不动等 R6 战斗修复 (兵力无战斗力兑现是结构性弱信号, 回撤糖价/降频 start_home 都只动症状; R6 修复后兵力价值可兑现, 经济动作预期自然回归); start_home 强制窗保留 (招兵链路验证资产)。(事后: R6 当日修复, 09-04 自发经济 0→54 次/40ep 兑现回归预判 ✓)

**⑦ T04 同级加图拍板 (用户)**: R6 = 大非平稳节点 (StupidAI→BAI+Python), 观察窗 (50+ ep 或 1 训练日) 内不加图避免归因混合; 触发条件任一满足即加图: ①观察窗后经济动作仍未回归+行为固化依旧 → 加同级图 ②R6 带来明显收益 → 跳过加图直接评估 T05。(事后: 触发①满足, 当日加图已执行)

**⑧ T04 态势 (09-02 更新存档)**: avg_r ≈ 0 (43 集窗口, S1 撤梯子后与基线持平) / [MINE] 占矿 92% (73/79) / [TOWNSTALL] 2.8 次/集良性收敛 (回合末剩余机动点<141 对角价但 ≥100 直行价, 黑名单改道正交兜底) / [TOWN_BLOCKED] 均为真围堵 / ZOMBIE ~32% (自 75% 高点回落) / ⚠ 主日志是选择性转储, [ECON]/[RECRUITED] 必须查 /tmp/hermes_ep_*.log (踩坑 #132)

---

### T7.6 取兵链路修复 (P1+P1b+P1c+P1d+P2+P3+start_home+P1e) ✅ 完成+闭环验证通过 (2026-09-02~09-03)

- **根因 (三层, 79 集 13 取兵窗 0 效果)**: ①招兵 dst=getUpperArmy() 落 garrison 黑洞 (城格 pas=0 visit 永不发生 + VCMI 1.8 getUpperArmy 不含 visiting hero, 踩坑 #133) ②garrison obs 恒 0 双盲 ③兵力检测器布局错位 (11-19 vs 真实 15-21 纯 count)
- **修复链**: P1 = case16-18 招兵前 visit 块 + 邻接守卫 (distSq>2 快速放弃); P1b = dst 显式 getVisitingHero(); P1c = 邻接守卫改 visitablePos() Chebyshev≤1 口径 + 双路径 moveHero (锚点不同走 standPos / 已在锚点走对象格触发 visit) — 修 P1 锚点坐标系误杀 71/71 全弃 (踩坑 #137); P2 = 检测器改 15-21 纯 count 权重 [10,40,120,350,900,1600,2500]; P3 = garrison obs 填充 (T7.5 硬前置提前完成, strategic_state.cpp getUpperArmy() 7 槽 count → obs 城槽 field 6-12, 布局不变不违冻结)
- **start_home 出发前招兵 (用户设计)**: ep_runner_one.py 每局开局 start_home=True — a=24 回城引导 move_town_bfs 到首个己方城邻格, 已邻接退出交引擎 P1 visit+招兵接管; 无己方城/超 80 步放弃; 冷却保持防 spam; 确定性前置取兵不依赖模型自发
- **09-02 深夜真因链闭环**: obs 城段 owner 语义证无罪 (owner=0=red 与地图数据吻合); 真因四层: ①bfs_full_dir 城格目标结构性不可达 → 贪心裸奔徘徊 fuse (已修: 城格目标降级 8 邻可站代理目标) ②P1c visitableOffset 口径死锁 ③P1d-v1 两缺陷: x 向候选格恒被挡单轴退化只走 y + 回合末移动力<100 moveHero 静默失败 (void 无反馈) → hero 原地打转 8 连弃 ④P1d-v2: 抛弃手工单步逼近, `cb->moveHero(cur, tp, false)` 引擎寻路直走门格 (tp=town->visitablePos()=真门格)
- **P1e→r2→r3 (approach 分支重写, 闭环验证通过)**: P1e = approach 分支 PathfinderCache 门格 8 邻选中间格 + CGPath 逐点推进 + 尾补 standGoal 兜底; r2 部署后实锤端到端全通但同时实锤 CGPathNode 格子坐标直喂 moveHero = 幽灵 x-1 偏移 (plen=4 只推进 2 格; 根因 CGameHandler.cpp L874 convertToVisitablePos(dst) — moveHero dst 是锚点坐标, CGPathNode 是格子坐标, offset=(1,0)); r3 = path 节点入队前 convertFromVisitablePos (格子→锚点) + 起点跳过改 convertToVisitablePos(cur->pos) — plen 4→3 单拍直达门格 heroPos=(1,3) visited=1 同拍招兵, 取兵四拍链压缩为 approach→招兵两拍; .so md5 5872b453 (备份链 .bak_p1e_r2), 镜像 tools/patches_20260902/ 同步
- **坐标三实锤**: hero appearance offset=(1,0) 非 (0,0) (双数据点交叉确认); town offset=(2,0) (城锚点 (2,3) → 真门格 (0,3)); P1c 主路径 standPos 语义在真 offset 下自洽, 缺陷仅限 approach 分支
- **runner 三态诊断增强**: start_home 块 begin step=0 首拍 8 城段全转储 (id/owner/x,y) + abort(no_own_town|timeout>80) 带原因 + train 主日志白名单加 [START_HOME]/[RECRUITED] (防 hermes_ep 逐局覆盖丢失, 踩坑 #132)
- **部署**: libMMAI + libmlclient 重编 (-j4), 4 副本同步 (rel/bin 运行时 + workspace + vtest + build); 备份链 .bak_taketroops_0902_0433 → AAI.cpp .bak_p1d_0902/.bak_p1dv1_0902 → .so .bak_p1dv1_0902; C++ 补丁镜像 tools/patches_20260902/ (含 P1c+DIAG7+P1d-v2); 当夜 WSL 曾被 -j8 编译与训练并发压死一次, `wsl --shutdown` 恢复 (踩坑 #131)
- **附带发现 (TOWNSTALL 规律)**: 被拒步 100% 为对角步 (回合末机动点 <141 对角价但 ≥100 直行价), 黑名单改道正交后良性收敛; 后于 09-12 发现另有 BFS plen 平台段误判, 已修 (#209)
- **闭环验证 (四拍判据链全通)**: ①[START_HOME] adjacent → ②DIAG7 approach2 续走至 afterMove visited=1 (⚠ 重启须显式清空 rl_recruit_diag.log — 未清混入旧 .so 残留判别法: 有 enter 无 guard=P1c / 有 approach to=P1d-v1 / 有 approach2=P1d-v2) → ③dstIsHero=1 → ④[RECRUITED]; 一键 py/monitor_recruit.sh; 确认后删 AAI.cpp 临时诊断
- **遗留观察**: 死亡局正收益问题 (ZOMBIE 局 r=+50~+93 vs 超时局 r=-18~-107, 死亡无惩罚 → "拿完 +30 送死止损"梯度) → T7.4 关联; [RECRUITED] 首触发后已随自发经济回归闭环 (09-03 起)

---

### 09-01 P10 h3m2vmap 转换器 B1 + vmap2h3m 旁路 ✅ (2026-09-01)

- **B1 转换器完成**: 设计稿 docs/P10-B-h3m2vmap转换器设计稿.md (08-31 接口梳理 + 改写规则 R1-R7 + 待验证 V1-V6); 工具源码 vcmi-native/tools/h3m2vmap/, Windows 副本 tools/h3m2vmap/
- **vmap2h3m 反向转换旁路**: py/vmap2h3m.py (SOD, 5 类对象特征集), h3m_tool strict 对账 skipped=0 + 引擎 h3m2vmap --check-h3m 实测 ENGINE LOAD OK; V1 (cb=nullptr) 实测定论可用, modName 须传 "map"; 详见设计稿 §8

---

### 08-31 城镇动线三轴修复: TOWN 复活 + 回城取兵链路 + 双根因定位 ✅ (2026-08-31)

**① TOWN 轴复活 (T7.2 配套修复)**:
- 判定修复 (ep_runner_one L605-611): 精确站上城格 → **dist≤1** (8邻+自身)
- **根因定性 (结构性, 非概率性)**: 城格不可站 → `英雄坐标==城坐标` 永假 — vmap template 实锤: town mask=["VVVVV","VVAVV","VVVVV"] 中心 'A'=actionable+**blocking** (逐格移动阻挡), visitableFrom 四周 '+' = 引擎语义为英雄走到**邻格**触发访问且停在邻格, 英雄坐标结构性不可能等于城坐标; 佐证: 1850 局 × 24% 动步占比若城格可站早该偶然命中, 0 次只可能是永假
- 我方城过滤: get_objectives 读 vmap options.owner, 过滤 owner=="red" — 己方城 step0 即在身旁, 计入会使 +30 变免费糖
- blue 城引导: obj_best 矿占完后 (mine_taken) 注入最近 blue 城, 优先级 = 守卫 > 矿 > blue城 > 资源堆; 不设距离上限 (blue 城全在对角远端 20X20 ~22 格), 卡死由 move_stall(6步) + town_blocked 本局禁用兜底 (每局学费上限 6 步 vs first +30)
- **判定与引导缺一不可**: 判定修复让"路过城边的糖能发出来" (必要), blue城引导让"模型会走到城边" (充分) — 只改判定不做引导触发率大概率仍 0
- **效果**: 触发率 0 → 26% → **36% 局** (纪元内 15/42), 轴完全激活

**② 回城取兵链路 (新能力, 兵力增量奖励管道打通)**:
- 机制发现: C++ RECRUIT 执行链 `dst = town->getUpperArmy()` = visiting hero 优先 (CGTownInstance.cpp L879) → **英雄 visit 己方城时发 RECRUIT, 新招兵直接进英雄部队** → 兵力增量 0.01×dp 立即生效 (此前招兵进 garrison, 该奖励管道对 RECRUIT 是断的)
- 实现: visit 检测 (英雄 dist≤1 己方城 obs[336+ti*18] owner==0 + recruit_mask 非零 → 4 步 RECRUIT 窗 + 30 步冷却防 spam) + own_town_best 引导分支 (优先级插在取兵位: 守卫 > 矿 > **回城取兵** > blue城 > 资源堆; dist≤25)
- 触发信号演进: 首版用 garrison (obs 恒 0 废弃, 见④) → recruit_mask (fill_v3_fields 有真实填充, 城有巢穴可招)
- 状态: 代码就位下局生效, 待 [OWN_TOWN_GUIDE]→[TOWN_VISIT] 实战验证 (判读表: 无 GUIDE=引导没启动查 garrison 数据管道 / 有 GUIDE 无 VISIT=城格到达问题 / 连续出现=链路通)

**③ Router 断言根因定位 (R6, 89 局 ZOMBIE 100% 共现)**:
- 数据: 近 300 局逐局切分, ZOMBIE 局与 "dynamic cast to V13::BAI failed" **100% 共现** → 因果链 = 断言 → 红方战斗 AI 回退 StupidAI → 接战必败 → hero dead
- 排除项: visibility hidden 假设不成立 (新旧 .so 符号表结构相同; 初判 "typeinfo=0" 是 grep 模式错误 — typeinfo 是 mangled 名 _ZTI* 不含字面); version≠13 throw 路径日志 0 次
- **真根因**: MMAI/CMakeLists L60 `add_definitions(-DUSING_ONNX=0)` 硬编码 + 无 onnxruntime 依赖 → factory.cpp CreateBAI `#if USING_ONNX` 块编译掉 → **return 空 shared_ptr** → 空指针 cast → 断言
- 战略认知: 红方战斗 AI **从训练第一天起恒为 StupidAI** — T03 毕业/T04 突破/96% 正局率全部在此条件下取得, **非回归是既定环境**; 比例性 (rout 15-55%) = 接战场次占比, zomb% ⊆ rout%
- 修复 (R6 登记绑定 T8/T5 前置): 部署 onnxruntime + USING_ONNX=0→1 + 重编 libMMAI + 三副本; ⚠ 启用后战斗强度变化 = 非平稳, 课程节点一次付

**④ towns.garrison obs 恒 0 发现 (T7.5 撤梯子硬前置)**:
- strategic_state.cpp L724 towns 全知填充段 `memset(t.garrison, 0, ...)` — H.3 schema 定义了 garrison[7] 但从未实现填充
- 影响: 模型世界观里所有城都是空城 → 撤梯子后内生回城取兵策略信息上不可学; T5 攻城评估/守城决策/兵力分布感知全依赖
- 修复: C++ 补 ~5 行 (布局不变不违冻结), **绑定撤梯子③同批进换图节点** (恒 0→非零 = 分布突变)

**⑤ libMMAI 全量重编 + 部署 (功能等价)**:
- 过程: visibility default 移出测试条件块 (AI/MMAI/CMakeLists L81) → 全量重编 (RelWithDebInfo; 途中误传 Release 触发 strip 产 953KB 空壳, 已纠) → 产物 18.7MB fe48be90 部署两树
- 经验: set_target_properties 改动需重新 configure 才生成新 flags (make 直接跑用老 flag); rm MMAI.dir 必须在 cmake 之前 (build.make 是生成物)
- 备份: backups/so_bak_20260831/ (libMMAI 13c0f041 + libmlclient 5d2b9e9d + MMAI_CMakeLists.txt.bak)

**当日整体态势**: avg_r -0.2 接近转正 / 正局率 96% / 80+ 局 14-15/100 / KL 压制期回撤自然消化; 训练重启两次 (step=380986 停编重启 + 389659 根因排查后恢复); 训练期纪律全程未破 (ep_runner 层改动下局生效, C++ 重编在停训窗口)

---

### T7.3 开经济 + B 方案自主通道修复 ✅ (2026-08-29 深夜)

**开经济三件套生效**: explore 0.3 / economy_force 24 / nk2 0.45 (train 注释切换); T04 掩码自动开 + 经济成型奖励自动激活。

**三连调优** (观察-诊断-修复循环):
1. economy_force 50→24: 50 步原地空转学费 -25 步罚 (坑 #121), r 均值 -93→-4.5; 自主 MOVE_TO 涌现
2. [ECON] 观察盲区: 事件不在主日志转储词表 (坑 #122), ep log grep 实证 RECRUIT×3/BUILD_2 正常触发
3. 经济奖励上调: RECRUIT +5→+12 / BUILD_2 +8→+15 (自主经济卡 0.6/局平台); 城镇引导降级移除 (RECRUIT/BUILD 玩家级远程无需到城, 贪心必卡死)

**关键事实核查** (坑 #123): 动作 16-21 真实语义 = 三种招兵策略 (低级×1/高级×1/全招) + 三条建筑链 (大厅/兵种/防御), 非资源档位; 执行链 = adventure_send_action → AAI::yourTurn → executeAdvancedAction → nearestOwnTown + recruitCreatures/buildBuilding (玩家级远程, 与英雄位置无关, [ECON] step0-4 触发实证)。

**B 方案根因发现 (坑 #124)**: first 奖励被 economy_force 强制期截胡 — 自主 16-21 是零即时信号动作, 90+ 局自主率卡 0.6/局的真因 (周期提醒只解决体验不解决回报)。两件套: ① "每日提醒"周期化 (前 24 步连续后每 40 步插 4 步经济窗, ~10% 占比) ② 每次执行小额奖励 RECRUIT +2/BUILD_2 +3 (first 保留, 保险丝每档/局 5 次) + 兵力增量 0.001→0.01。

**效果 (148 ep)**: avg_r 段高 +0.8 / 80+ 分局 3/15 常态化 (+91.8/+86.6/+84.8) / 历史新高 +105.9 (153 步) / 快速闭环标杆 +97.2 (59 步) / 自主 16-21 0.6→2.0/局 / 深负 1/15 / 战死率 35%→20%。

**B1-B4 四份设计方案出稿** (`docs/方案_B1-B4_20260829.md`, 纯文档零训练影响): B1 解封 (守卫梯度缩放代码已存在 `--guard_grad_scale_by_map`, MAPS 渐进扩容 2→3→4→6, vmap 移近备选) / B2 经济上线参数 (economy_force=24 步轮换, T04 6 图先开 2) / B3 熵 bonus (ent_coef 前期×2 指数衰减 200 万步, 不动 ep_runner 无采样污染) / B4 晋级判据 (双条件 = GUARD 首胜率 ≥30% 且 avg_r ≥+15 连续 3 窗, 5 指标 grep 规格已定实装随 C1)。实装触发链: B1→D2 / B2→D1 / B3→D3 / B4→C1。

---

### 训练期并行任务批量闭环: P1/P2/P4/P9 + A 档四项 ✅ (2026-08-29)

**P1 训练空转根因 (断言雷) 修复**: 根因链 = 战斗结束攻方 queryID=-1 触发 ASSERT 崩引擎 → ep_steps=1 空转。修复: libMMAI.so 换 13c0f041 (08-27 构建树版, 内含 queryID=-1 守卫, 单文件重编产物), libmlclient 保持 5d2b9e9d; 冒烟 2 战斗 0 断言, 续训 ep 156/200 步满局 r=43/19.75。详见踩坑 #115。

**P2 晋级判据自动化 eval** ([py/eval_promo.py](../py/eval_promo.py)): 子进程包 ep_runner_one (训练同款参数/口径), CPU-only (CUDA 屏蔽+线程=2) 单局 237s 不抢训练资源; 首胜/晋级线与 analyze_ab 同口径, n<10 不出达标结论; 历史落 py/eval_history.jsonl + 趋势对比; 晋级重启保留人工确认。端到端验证过 (ep1: 200 步 r=7.15)。

**P4 ZOMBIE/GUARD 死锁聚合分析** ([py/analyze_deadlock.py](../py/analyze_deadlock.py), 报告 docs/分析报告_zombie_guard_20260829.md, commit 076aa58): 按"续训横幅"切纪元统计 677 局。核心结论: ① zombie 三层防御生效 — v4 段 60 大负局 (avg_r -73.2) → 防御后大负清零; ② ZOMBIE 全程 18 次 (2.7%), 死亡步位集中 30-50 步 = 英雄打守卫战死是主死锁模式; ③ ENDTURN_FUSE 全程 0 次 (30 连喷从未触发, 熔断仅兜底); ④ 首胜率趋势: v5 段 6.3% → 191687 段 15%, 全程 12/677; ⑤ 空转 61 局全集中 .so 断言雷事故段, 修复后清零。⚠ 转储词表补 [GUARD]+Assertion 修复 (train_wsl2_ppo_v2.py 启动时加载, 下次重启自动生效)。

**P9 done 信号核实 (误报排除)**: "AAI::gameOver 空壳"无关紧要 — 本架构走轮询链: libmlclient strategic_state.cpp 每步填 state 时查 `players[p].alive`, `alive_count<=1 → game_over=last_alive+1` (L734-739) → env `_check_done` (strategic_env.py L1131) → ep_runner `break` (L619)。实战证据: 12 个首胜局全部 steps<60 即终, 若链路断必跑满 200 步。gameOver 事件回调是另一条可选通道, 本架构不需要。

**A1 S5 健康复核**: v5 重启后 4 局全 200 满步, ZOMBIE/FUSE/ASSERT 全 0 (08-28 双保险 + 断言守卫生效); 服务 active。

**A2 A Viking 战斗验证 (forktest67)**: 14 场战斗 (20BattleResultsApplied) 全部结算, 0 Disaster, 战后回合持续轮转 (gives turn 90+, 战斗后 heroMoved 43) → **RL 模型下战斗链闭环确认**。fishy 39 = 战斗内 WALK_AND_ATTACK 偶发被拒 (P46 已知残留, 不阻塞)。

**A3 1.7.5 MSVC 重编**: ModelAI_175.dll (249KB) 含 rl_model 双输入 + MODELAI_MODEL + OBS_DUMP; 部署 1.7.5 GUI (AI/ModelAI.dll + 根目录 onnx + AI/onnxruntime.dll)。坑: cmd //c 从 git-bash 弹交互壳 (踩坑 #120), 改 powershell cmd /c 成功。

**A4 MOVE_TO(act24) DLL 执行器**: moveToOneStep() = target_list[0] + reserved[0] BFS 首步方向, 单步 moveHero; fork + 1.7.5 双端编译部署 (forktest68: 0 Disaster, 链路健康; RL 模型当前少输出 24 属预期, 经济动作上线后价值显现)。git: vcmi_model_ai 4470d6b。

---

### Phase T6.2/T6.4 Level 2 (T03) 毕业 + Level 3 晋级执行 ✅ (2026-08-29 夜)

**T03 段最终战绩** (2026-08-29):
- eval_promo 10/10 全胜 avg_r=99.0 (全部 29 步收局, ckpt=203594; 对比上午 ckpt=194821 时 0% 胜率 7.15 — 守卫自动终局上线后单日反转)
- analyze_ab: 20X20_01 胜率 51.5% / 20X20_02 53.3% / 30X30_01 59.1%; 30X30_01 A/B 44 局 0% 大负率 (判定线 ≤15%, 提前终止条件远未触发) — "30X30+远资源大负率"假设证伪 (真因 = zombie 段 + 振荡烧分, 已根治)
- 近 100 局首胜率 64%, avg_r 双 50 局窗口均为正

**晋级切换** (step=205345 resume):
- MAPS = T04×6 (跳过 30X30_02 — 与 30X30_01 同构边际低, T04 六图自带 30X30 档)
- Level 3 超参: BATCH=2048 / EPOCHS=6 / EXTREME_ADV_CLIP=6.0
- 经济三件套保持注释 (economy_force / explore 0.3 / nk2 0.45) — 一次一轴

**晋级前置修复 — 守卫击杀自动终局** (`--guard_done_steps 15`):
- 根因: 20X20_01 全 0 胜 = 杀守卫 +100 后英雄存活无终局信号 → 振荡烧分 final r 跌破 80
- 实现: ep_runner_one 守卫胜利启动倒计时, 15 步无新目标 (守卫/矿/资源) 即置 done; train/eval 同口径
- 验证: [GUARD]/[GUARD_DONE] 15:15 全触发, ep_steps=41 r=89.65

**T04 冷启动诊断 + 引导三件套** (19 局 r=-80~-110 → 修复, 详见 WSL知识库.md 二十四):
- 诊断链: T04 0 守卫 (gen_curriculum_all 设计) / standardDefeat 胜利条件 / reward clip 下限剪掉失败 -200 (坑 #116) / 资源堆近目标吸住 + 强制期不够长 (坑 #117)
- 修复: clip±300 对称 + `--objective_reward 30` ([MINE]/[TOWN] 事件) + obj_best 目标优先层 (矿/城恒优先, town_blocked 防卡死) + T04 force 60
- 关键事实核查: target_list 矿/资源堆 C++ 已填城镇不填; next_dir 远目标有效; 引擎加载 rel/bin/data/Maps 部署副本 (坑 #119)

**T03 遗产处置**: 守卫 +100 / [GUARD_DONE] 终局 / 守卫恒优先注入在 T04 自动休眠 (get_guards 返回空), 代码保留供回退

---

### T5.4 敌方可见性验证 ✅ (2026-08-14)

**根因确认**: `CGameInfoCallback::getHeroes()` 第 686 行有 `isVisibleFor(hero, *getPlayerID())` 过滤 — AI 侧 callback 带 red 视角, 视野外的敌方英雄不返回。obs 结构有 8 槽位但 fill 只拿到己方 → "战略感知受限"真相。

**修复**: fill_strategic_state 的 players/heroes/towns 三段改从 `gicb->gameState()` 全知直读 (遍历 gs.players), 绕过 callback 可见性过滤。训练期全知视角, 部署期用 isVisibleFor 掩码 (见部署映射设计.md)。

**验证** (collect_bc.py, For Sale.h3m 3玩家):
- players: 3 玩家全可见 (red gold=4050 / blue gold=11000 / tan gold=10900, alive 全 1)
- heroes: 3 玩家英雄全可见 (敌方 owner=1 id=222 pos=(31,23) army=[20]; owner=2 id=223 pos=(3,32) army=[20,7])
- towns: 敌方城镇可见 (owner=2 id=448 pos=(4,31))
- 8 pairs 采集稳定无崩溃

**踩坑** (本次最大教训):
1. **临时验证脚本 (h4_dump_visible.py/h4_verify_v3.py) 会崩, 生产工具 collect_bc.py 一直正常** — 排查 2 小时以为是 .so 问题, 实际是脚本与环境的兼容问题。验证一律用 collect_bc.py
2. **strategic_state.h 改动会连带重编 MLClient.cpp/AIGateway.cpp** (都 include 该头) — MLClient.cpp 里有 `new StrategicState()` + `sizeof(StrategicState)`, 不重编会按旧结构分配 → 堆越界写 → 崩。改头文件后必须强制重编所有依赖 (`rm -f *.o` + rebuild)
3. **8-2 fill_strategic_state 里根本没有 towns 段** (towns 只在 strategic_state_update) — 采集路径 towns 一直是空, 本次补上全知填充
4. g_rl_action / adventure_cb_trampoline / adventure_get_action / register_adventure_delegate 曾丢失 (重写文件时) → 必须保留
5. fill 主体加 shared_lock 后, 内部 fill_exploration/fill_mines/fill_v3_fields 不能再各自加锁 (shared_mutex 非递归, 同线程重复加锁死锁)

### T5.5 obs v3 实现 (2689→3464) ✅ (2026-08-15 全部完成)

**OBS schema v3 冻结** (见动作空间设计文档 §2, OBS_DIM=3464, _version=3):
- players 8×15 (增 total_power/weekly_income/relation_to_me)
- heroes 8×26 (增 total_power/is_garrisoned/has_commander)
- towns 8×18 (增 recruit_mask_lo/hi/build_mask_lo/hi)
- local 15×15×3 (增对象类型+守卫战力通道)
- nav 32 / target_list 64 / enemy_threat 7 / battle_pred 4 / events 4 / reserved 134

**实现状态 (2026-08-15 全绿)**:
- C 头 + ctypes 双端一致 (7952 字节, `_schema_check.py` 全过) ✅
- fill 全知直读 (players/heroes/towns) ✅ 已部署验证
- fill_v3_fields (战力/驻守/招募/建造/威胁/预测) ✅ 本次完成
- local 通道 1/2 (对象类型/守卫战力) ✅ 本次完成

**fill_v3_fields 实现细节** (strategic_state.cpp, collect_bc.py 验证 Dungeon Keeper.h3m 10 pairs):
- heroes.total_power = Slots() 遍历 getCreature()->getAIValue() × getCount() 求和
- heroes.is_garrisoned = town->getUpperArmy() dynamic_cast CGHeroInstance 命中 (garrisonHero 是 private!)
- heroes.has_commander = hero->getCommander() != nullptr (commander 是 private, 必须走 getter)
- players.total_power = 该玩家所有英雄战力和; weekly_income = Σ town->dailyIncome()[GOLD] × 7
- relation_to_me: PlayerRelations 枚举 (ENEMIES=0/ALLIES=1/SAME_PLAYER=2) → 映射 0=中立 1=敌对 2=结盟
- towns.recruit_mask = bit=level×2+upgrade, dwelling 建筑 hasBuilt (BuildingID::getDwellingFromLevel)
- towns.build_mask = 只算己方城镇 (owner==0) — canBuildStructure 对敌城打 ERROR 刷屏!
 置位: ALLOWED | CANT_BUILD_TODAY | NO_RESOURCES (排除已建/前置未满足)
- enemy_threat[7] = 排除自己后各玩家 total_power
- battle_pred = active hero total_power / 所在格守卫战力 / win_prob=my×100/(my+target)
- local 通道1 = tile.visitableObjects.front() → getObjInstance → ID (Obj 枚举, int8 保护 <128)
- local 通道2 = guardingCreatures(pos) 守卫战力和, log2 压缩防 int8 溢出

**验证结果** (Dungeon Keeper.h3m, 10 pairs):
- players.total_power 20/80 非零, weekly_income 20, relation 10
- heroes.total_power 35 非零, is_garrisoned 3 (有驻守英雄), has_commander 0 (无指挥官图)
- towns.recruit_mask 20, build_mask 10 (己方城镇 11 个可建 bits)
- enemy_threat 每帧 1 个非零, battle_pred my=2540 win=100
- local ch1 17-21 格/帧, ch2 3-12 格/帧 (log2 压缩后 max=12)
- 24 步采集无崩溃无死锁

**本次新踩坑**:
1. canBuildStructure 对非己方城镇打 ERROR "Town is not owned!" 刷屏拖慢 → build_mask 只算 owner==0
2. CGHeroInstance::commander / CGTownInstance::garrisonHero 都是 private → 必须 getCommander()/getUpperArmy()+dynamic_cast
3. vcmi_loglevel_global="error" 时 logGlobal->warn 被过滤 → 诊断用 fprintf(stderr) 或直接看数据
4. For Sale.h3m 开局建筑已建满/今天已建过 → build_mask=0 是正确行为, 验证换 Dungeon Keeper.h3m

### T5.6 C++ 动作 11-24 实现 ✅ (2026-08-15, reasonix CLI 代写 + Hermes 验证)

**范围** (见动作空间设计文档 §1.2, 码表冻结):
- 11-13 SPLIT×3: `splitStack` / `bulkMoveArmy` (目标=最近友方英雄, v1 简化)
- 14 MERGE_FROM: `mergeStacks`/`bulkMoveArmy`
- 15 SWAP_ARMY: `swapCreatures` 逐槽交换
- 16-18 RECRUIT×3: `recruitCreatures` (目标=最近己方城镇)
- 19-21 BUILD×3: `buildBuilding` (大厅链/兵种链/防御链)
- 22 GARRISON: `swapGarrisonHero`
- 23 RECRUIT_HERO: `getAvailableHeroes[0]` + `recruitHero`
- 24 MOVE_TO: `getAllVisitableObjs` 最近对象 + `moveHero`

**实现** (AAI.cpp, reasonix 代写 270 行):
- 全部走 asyncTasks->run() + lambda 内 shared_lock + try/catch 静默 (battleEnd 同款模式)
- 辅助: nearestAllyHero/nearestOwnTown/nearestInteractable (欧氏距离含 z, 跳过敌方)
- MOVE_TO 锁外等 2s 到位 (T4.6 同款)
- 一次编译通过 (API 全部正确), collect_bc.py 回归 SAVED 10 pairs 无崩溃

**关键踩坑/决策**:
- `bulkSplitStack` 引擎实现是军队内部平铺 (CGameHandler.cpp:1705, srcArmy==dstArmy), 不能跨英雄 → SPLIT_ALL 改用 bulkMoveArmy (有 isAllowedExchange 检查, 失败静默)
- strategic_env.py N_ACTIONS 11→25 (Discrete 保持)

**reasonix 使用经验 (CLI 方式, 勿用 mcp__reasonix)**:
- MCP 版 (mcp__reasonix__delegate_task) 三轮只调研不写码, 浪费 token — 弃用
- CLI 版: `"D:/Bigdata/Reasonix/reasonix-cli.exe" run --permission-mode bypassPermissions --preset delivery --max-steps 150 --events-jsonl --dir "D:/Bigdata/hero3_fresh" "任务" > .reasonix-h6.events.jsonl 2>&1`
- 必须后台跑 (reasonix 单任务远超 600s 前台上限); 派单后 30s 查事件流行数确认在跑
- git-bash 传 --dir 路径必须用正斜杠 "D:/Bigdata/hero3_fresh" (反斜杠被吞)
- reasonix 写的代码仍需 Hermes 验证 (编译 + collect_bc.py 回归) 后提交

### T5.7 冒烟测试 16 项 ✅ (2026-08-15 全过, 含 24 局稳定性)

**scripts/smoke_h7.py (MMAI 模式, Dungeon Keeper.h3m) → PASS:16 FAIL:0 (exit 0)**

| # | 测试 | 结果 |
|---|------|------|
| 5-7 | SPLIT/MERGE/SWAP | ✅ 单英雄图目标缺失静默无崩溃 |
| 8 | RECRUIT ×3 | ✅ 无崩溃 (兵营空不扣钱合法) |
| 9 | BUILD ×3 | ✅ 无崩溃 |
| 10 | GARRISON | ✅ is_g=1 驻守成功 |
| 11 | RECRUIT_HERO | ✅ hero_count 1→2 |
| 12 | MOVE_TO | ✅ 无崩溃 (逐格逼近) |
| 13 | 组合链路 | ✅ 无 segfault |
| 14 | 敌方信息 dump | ✅ T5.4 已验 (obs 全知) |
| 16 | 双端 schema | ✅ _schema_check.py 全过 |
| 15 | 24 局稳定 | ✅ 完成 (2026-08-15, bc_collect_v3464.log) |

**24 局稳定性结果** (collect_bc.py, NK2 对手, 4 图轮换 Dungeon Keeper/Good Witch Bad Witch/Fort Noxis/Key to Victory):
- TOTAL 463 pairs across 24 episodes, 无崩溃无死锁
- ep16 最多 57 pairs, ep14/18/22 (Good Witch) 稳定 8-12 pairs
- 注意: ep23 尾部 NK2 卡死 (adventure_wait timed out after 90s, 只采 11 pairs 正常保存) — 见踩坑点 #22

**冒烟发现并修复 4 个真 bug** (commit c6f3ec06c):
1. **动作 8 INTERACT 从未实现** (设计文档标✅但 AAI.cpp 无分支, 历史遗留) → interactTarget 优先城镇+逐格逼近
2. **GARRISON 卡死 120s**: 英雄未 visiting 城镇时 swapGarrisonHero 挂起 → 先 moveHero 进城等 getVisitedTown() 再 swap
3. **MOVE_TO 直接 moveHero 远处被 BLOCK** (moveHero 单格版只允许相邻格) → moveOneStepToward 8 方向逐格
4. **smoke 用 Nullkiller2 模式 yourTurn 不执行** (NK2 自己决策, Python 动作全被忽略, 之前 PASS 是假象!) → 冒烟必须 MMAI 模式

**关键 API 知识**:
- moveHero 单格版只允许相邻格 (CGameHandler STANDARD 检查) — 高层移动必须逐格或传路径数组
- moveHero 目标是 convertFromVisitablePos (可站格), 直接传对象 visitablePos 会被 BLOCK (城镇格 terrain 不可站)
- 英雄 visiting 判断: cur->getVisitedTown() (visitedTown 是 ObjectInstanceID)
- 单英雄图上 SPLIT 类动作目标缺失 = 静默跳过 = 合法无崩溃 (v1 引擎侧"最近目标"解析的固有行为)

### NK2 卡死修复 ✅ 全闭环 (2026-08-16 晚, T8 前置 — )

> 根因链 6 环: Router battleStart 战斗模型名 (3 处) → battleEnded 补调 → dialog 自动应答 → 守卫战斗收尾卡 (IFML 宏漂移) → 战斗后死锁链 4 层。

- [x] Router SCRIPTED else: THROW → fallback (第 1 轮 reasonix)
- [x] Router neutral 打野: StupidAI early return + fallback BattleAI→StupidAI (第 2 轮)
- [x] Router config 缺失 fallback BattleAI→StupidAI (第 3 轮, router.cpp:90)
- [x] battleEnded 服务器侧补调 (第 4 轮, AIGateway::battleEnd) — 验证 battle=0 ✓
- [x] dialog 自动应答 (第 5 轮, CGameHandler::showBlockingDialog AI 自动答) — 验证 Blocking dialog=0 ✓
- [x] 守卫战斗收尾卡 (第 6 环, ✅ 2026-08-16 闭环): 根因 = IFML 宏漂移 (`onlyOnePlayerHuman || IFML(true,false)` 恒 true → CBattleDialogQuery 永不 pop → onExposure 链断) → 修复 `if(onlyOnePlayerHuman)` (BattleResultProcessor.cpp:303) → 战斗闭环验证通过
- [x] 战斗后死锁链 (4 层, ): ① NK2 持读锁决策 vs runNetwork 写锁 ② turnCounter 修旧线程残留 ③ MMAI endTurn 自锁 (wtr=false) ④ 战斗 AI wtr=false 残留 livelock (endTurn 显式 wtr=true) → 3/3 局完整跑通 (steps=10)
- [x] 训练链路打通: ep_runner 完整跑局 steps=10 ×4 局全通 (r_openO/U/V/W/X), 0 次 EndTurn 拒绝
- [x] BC 重采集恢复 (2026-08-16 晚启动): 40 局采集, ep0 满采 2000 pairs (修复前 7-42 pairs/局)

**验证证据**: 修复后 3/3 局完整跑通 (r_openU/V/W: steps=10, err=None, 0 次 EndTurn 拒绝); 清理打点后冒烟 1/1 (r_openX, 日志 2499 行)。gdb 抓死锁流程。

### 有头 GUI 验证 ✅ (2026-08-18, T9 前置)

- 标准 client-server 有头 GUI 打通 (ServerPlugin 校验放宽 + 4 个渲染崩溃修复 + 玩家视角模式)
- 结论: spectate 弃用 (官方也崩); 显示走 Windows 原生 (WSLg 虚拟显卡无解); Windows 构建 fork = 下一步 (0x618 官方 NK2 bug, fork 已修)
- 踩坑见 #33

### MOVE_TO (动作 24) 训练端落地 ✅ (2026-08-19)

- C++ 侧 (strategic_state.cpp): fill_target_list 填 obs[3251:3315] — 最近 8 个可采集目标 (未占矿/资源堆/篝火/宝箱/宝物, 同层曼哈顿距离, 守卫战力 log2); libmlclient.so 重编部署
- Python 侧 (ep_runner_one.py): 24 开放 (11-23 屏蔽); 展开 = 朝 target_list 最近目标走一格 (passable 检查) + 粘滞 (目标不漂移) + stall 检测 (卡 6 步换目标) + 无目标 → END_TURN
- 训练引导: logits[24] 偏置 2.0 → 0 (前 200 ep 线性衰减)
- 验证: Twins --move_to_test 60 步 target_list 填充正确、目标导向移动、粘滞生效
- 已知局限: 贪心不绕路 (地形挡会卡, stall 兜底) — 完整寻路由 Phase T6.2 BFS 接管

### Phase T6.1 NK2 估值 → 奖励塑形 ✅ 冻结 (2026-08-22)

**目标**: 用 NK2 专家估值替代拍脑袋奖励, 给模型密集奖励信号。

**实现**:
- nk2_state_value(): 从 StrategicState 计算全局状态价值 (资源+矿+城+军力-威胁)
- reward = nk2_state_value(t) - nk2_state_value(t-1) + step_fixed
- 行为级兜底: 横跳惩罚(-2)/循环检测(8步窗口>=5次→-3)/END_TURN冷却(连续3次屏蔽)/熵bonus(0.05)

**冻结参数**:
- KL_TARGET=0.08, KL_COEF_MIN=0.15, KL_COEF_MAX=10.0
- penalty=1.8, explore=0.5
- NK2 5x: 矿+3.5~5, 城+10~15
- BFS 15×15 (Python 端), BC 重载

**训练结果** (45 episodes):
- avg_r 稳定 -0.4~-1.6, klc 触顶 10
- 循环/END_TURN 穿插不变
- ep38/40/43/44 r=-10/-12 多样化偶尔出现

**判死**: Phase T6.1 到天花板, 不再调。转 Phase T6.2 C++ 寻路桥接。

**踩坑**:
- #78: 强制引导动作空转 — MOVE_TO 无目标时原地 27 步
- #79: 动作循环惩罚诱发 END_TURN 穿插投机 (penalty 3.0→1.8)
- NK2 shaping: nk2_state_value 去掉 gold/资源/军力(被动漂移), 只留矿/城/威胁/胜负

**教训**:
- 稀疏奖励 + 行为级修补是打地鼠, 根因是模型没有密集信号
- NK2 估值是专家先验, 能给出密集奖励信号
- KL 约束是双刃剑: 防止偏离但也阻止学习

### Phase T6.4 训练地图体系 ✅ 生成完成 (2026-08-23)

- VMAP 全面替代 H3M: 31 张课程地图 (T01×5 / T02×6 / T03×6 / T04×6 / T05×4 / T06×4), BFS 可达性 31/31 通过
- 引擎加载修复链 7 环 (players 数组→dict / core:iona / 地形 rc / 全草地 / resource amount / town mask 5x3 / swordsman) —-94; wt/rc segfault 误判纠正 (#94: 真因 = NK2 守卫战斗断言)
- 统一生成脚本 `maps/training/gen_curriculum_all.py` (坐标规则化 + 界内校验); 自动生成+验证流程: auto_generate_validate.py + verify_maps.py + test_bfs.py + test_vcmi_load.py
- 水/岩障碍启用 (T02/T03/T04 岩柱+水湖, 4 邻防封路), 带障碍版实跑 20 步全满
- **决策**: H3M 路线停用 (对象表解析已完成, h3m_tool.py 保留作逆向参考); 原"战略训练只用 H3M"原则作废

### Phase T6.2 C++ BFS 全图寻路 ✅ 代码完成 (2026-08-23, commit 3464c79)

- C++ fill_next_dir(): 全图 BFS 搜 8 个 target_list 目标; passability = isLand+isPassable+!blocked (对齐 canMoveFrom); 目标格 blocked 豁免 (矿/资源物体); hero pos 用 visitablePos; z-level 过滤
- reserved[0..7]=next_dir[8], reserved[8..15]=diagnostics; Python obs builder 读 16 槽
- ep_runner 三层回退: C++ BFS → Python 15x15 → 贪心
- .so 同步 4 副本 (rel/bin, build/bin, workspace/vcmi/rel/bin, vcmi-native-build/rel/bin)
- 冒烟 18/18 = 100% BFS 命中率
- (训练验证持续进行中 → 见当前任务清单 T6.2)

### Phase T6.2 超参调优链 ✅ 冻结 (2026-08-24)

- Before (stuck, 33ep 无改进): BATCH=128, LR=5e-5, CLIP=0.08, EPOCHS=2, KL_TARGET=0.30, entropy=-0.05, NK2 scale=1.0
- After (冻结): BATCH=1024, LR=3e-4, CLIP=0.2, EPOCHS=4, KL_TARGET=0.50, KL_COEF_MIN=0.05, entropy=-0.05, NK2 scale=0.3, explore=0.2, act_loop_penalty=1.0, move_to_force=15, return normalization
- NK2 移除实验失败: critic 爆炸 (vloss 159-164) → 恢复 NK2 0.3 + return norm → vloss 1
- entropy=-0.01 正r率 15% → -0.05 → 27% ()
- **Level 3 经济动作实验毒化回滚** (): 16-21 强制采样 → noTarget 惩罚 → END_TURN 刷步 → klc 触顶 → 模型毒化; 修复 = logits[16:24]=-inf 屏蔽, 等 reward 解决 noTarget 再试
- **教训** (): L3 回滚未备份致 L1/L2 checkpoint 丢失 → backup_on_promotion() 加入脚本

### 守卫战斗根因闭环 ✅ (2026-08-26, Level 2 正奖励率 83%)

- 三层根因链: ① 守卫格 passable=0 → 模型/MOVE_TO 绕开守卫 ② 先占矿 → 守卫消失 (守矿机制) → 战斗永不触发 ③ **终极根因: random_armies 3000-5000 → 英雄战力 >> 守卫 → takenAction 评估 JOIN/FLEE → 守卫消失无战斗**
- 修复 (全 Python 侧, 零编译): T03×6 vmap 守卫移近 3-7 格 + random_heroes 1→0 + MOVE_TO 守卫恒优先 (跳过 passable) + 守卫清除检测进格 +100 + random_armies 关闭
- 验证 (12 局, step 45376→47472): 正奖励率 83.3% (判据 65%), r mean 37.0 (判据 10) 双超

### 守卫真战斗全链打通 ✅ (2026-08-27, 用户拍板三指令)

- 08-26 "守卫清除检测" 实为守卫逃跑误判: vmap monster 字段名是 **character** 非 aggression (无效字段被忽略) → agression=0 → 恒 FLEE
- 修复链: character:"savage" (恒 FIGHT) + neverFlees:true + 守卫 swordsman×3/2堆 (总战力 1470, relStrength 1.33; >1.5 会 JOIN/FLEE) + AAI::battleEnd queryID=-1 断言跳过 + BattleResultProcessor.cpp.o 旧缓存强制重编 () + yourTurn 挂起重构 (轮询 0.3s + battleEnd 回调收尾 + 兜底 CAS) + garrison dialog 服务器侧跳过 (AI 自动合并残余)
- 运行时地图目录 = /home/administrator/vcmi-native/data/Maps; patch 须同步 3 副本 ()
- 守卫接近梯度 +0.3/格 (曼哈顿差分); 验证: 手动双守卫连续战斗全通 + 200 步长局全通
- 详见 vcmi-gym refs/guard-battle-autofight-20260827.md

### END_TURN zombie 三层防御 + v4 段交付 ✅ (2026-08-28)

- 三层防御: ① logits mask (END_TURN 3-streak) ② zombie 标记 2-step 熔断 (旁路① passable 全 0 / 旁路② MOVE_TO 无目标, 全部标 zombie=True; zombie_streak≥2 → done=True + break, GAE 不再穿过死亡段) ③ endturn_streak≥30 独立 break 全局兜底 (BND-20260828-02)
- MAPS 6→2 (仅 T03_20×20 两张, 远守卫图 obs_nz=314/397/356 100% 过滤, BND-20260828-01)
- 2min 训练健康监控 py/train_health_monitor.ps1 + monitor_alerts.log (OPS-20260828-02)
- v4 段效果: -500 级灾难局清零, 近 50ep r mean -58.3 → -1.02, 守卫接近梯度 0.3→0.5

### v5 训练运维根治 ✅ (2026-08-28)

- v4 死因 = WSL2 idle shutdown: Windows 侧会话全关 → VM auto-shutdown → SIGTERM; setsid/nohup 挡不住 VM 级死亡
- v5 = `systemd-run --user` 托管 (unit=homm3-train-v5) + Windows keepalive (`Start-Process -WindowStyle Hidden wsl.exe -ArgumentList 'sleep infinity'`); 停止用 `systemctl --user stop homm3-train-v5` (优雅保存)
- 断点续训 step=173991 (wsl2_ckpt_173991.pt); v5 段 ep1-71: 首胜率 7.0% (v4 段 ~2%), 正 r 峰值 85-101, [ZOMBIE] ≤2 步收尾

### 首胜-战死关联分析 ✅ (2026-08-29)

- 全部 7 场首胜 (v4×2 + v5×5) 100% 英雄战死收尾: 最后一击 → 守卫清除 +100 + 英雄同战死 → 引擎封锁动作 → [10,10] ZOMBIE 2 步确认 → done
- 死亡不扣 +100 (无英雄存活项), 当前奖励结构下属理性行为; zombie 熔断已无害化
- T04 阶段 (有城镇复活/征兵) 再评估是否加存活塑形

### T7 阶段② 经济前置准备 ✅ (2026-08-29, 零干扰设计)

- **s2a 经济奖励编码** (ep_runner_one.py): get_resource_points() 解析 vmap 资源点 (L136-155) + 经济跟踪变量一局生命周期 (L203-215) + 成型奖励段 (L434-475: 兵力增量差分 / 资源点访问 / 首 RECRUIT+BUILD_2 / 资源-招兵闭环)
- **s2b 动作合法性掩码** (L242-296): 非 T04 → logits[16:24]=-inf (与旧行为等价); T04 → 位域解码 (recruit_mask@640 / build_mask) + 资源阈值 (gold/wood/ore) 逐动作 -inf
- **s2c 超参注释包** (train_wsl2_ppo_v2.py L15-20): BATCH=2048 / EPOCHS=6 / EXTREME_ADV_CLIP=6.0 (Level 3 启用时取消注释)
- **s2d T04 地图池注释块** (L35-44, 6 张) + **s2e economy_force 切换点注释** (run_episode 内 explore/nk2_scale/economy_force; 经济冷启动 = 前 N 步强制采样 16-21 轮换)
- 验证: py_compile 双文件通过 (WSL venv); T03 地图经济奖励零触发 (掩码屏蔽 16-21) — 当前 Level 2 行为 100% 不变
- **晋级决策** (2026-08-29): 现在不晋 Level 3 — ① Level 2 未打穿 (真赢 1~2%) ② 学习爬坡无平台 ③ 守卫修复+首胜+100 上线仅 1 天样本不足; 晋级线 = 最近 100 局胜率 ≥30~50% 或 avg_r 持续为正; 晋级后先只切 T04 不开经济, 稳定后一次一轴开经济 (奖励塑形先行 + 掩码源头做 + NK2/explore 临时上调 + BATCH 加大)

---

## ✅ 已完成历史 (更早归档)

### Phase T4.6 训练停止 (2026-08-01, 用户关机)
- 训练重启后约 40 分钟, 用户关机前手动停止 (wsl --shutdown 全清)
- 无有效 checkpoint, BC 起点重来代价小
- **开机恢复路径** (已过时, 待 T5.8 新流程): wsl → 清 pyc → nohup train_loop.sh

### B 态势感知扩展 ✅ (2026-08-01, OBS 264→2689)
- 全英雄 8×23 / 城镇 8×14 / local 15×15 / global 32×32×2 / active_hero
- 编译排障 4 坑: CMakeCache fmt / vcmi-native 缺 agent / MLClient 旧版 / data symlink
- 审计修复: global_explored 维度错位 (z 内层 vs 外层) → C 侧改 [z][gy][gx]
- **教训**: 手写 schema 双端易错 → 部署前字段序自动化对照测试 (T5.5 必做)

### A+B 合并: 事件奖励 + KL 约束 ✅ (2026-08-01)
- 占矿+10 / 打赢+100 / 升级+10; 删 exp per-step (堵 act=8 漏洞); 出生区探索预填
- KL_COEF=0.05 冻结 kl_ref; BC 加载失败优雅降级
- 采集 4 连排障: fill segfault (isClear 无 from) / 无锁竞争 (shared_lock) / red AI 分配失败 (跨 .so 全局 g_adventure_allied_ai) / router.cpp 旧版

### T4.1 修复链 EEXIST 解锁 ✅ (2026-07-29)
- data 符号链接 EEXIST → 真实目录+软链替代 cmake POST_BUILD
- T12 connector 重建成功 (681KB); T12 VcmiEnv init segfault 归入 T8

### T4.1 修复链 全线打通 ✅ (2026-07-29)
- env.reset() + step() 完整可跑通, obs_nz 8→34, reward -0.1→+6.15
- 关键修复: installNewBattleInterface segfault / Discord null guard / 信号量通信 / passability 全1 / moveHero 卡死 (waitTillRealize=false)

### C7 Connector 适配 + 修复链 ✅ (2026-07-29)
- N2-N11 + B 全部完成 (详见总任务.md C7)


### 09-16 P8-C 收尾 2.1-4.2 QueryReply 197 组包 + green null-ENGINE 段错误根治 ✅
- **2.1/2.2 QueryReply 组包脚本 (8B/9B 回退)**: `py/p8/p8c_query_reply.py` — 双数据源 (QUERY-DIAG tail + client 88/154-160) 拿真实 qid, 8B absent 首发 + 9B present 回退 (判据=197 fishy), 离线 selftest_layouts 字节精确 PASS
- **3.1/3.2 招募闭环 + 两次 clean run**: run10 qid=2/red 8B 回送 197 zero fishy=PASS(replied); run12 qid=-1 only + 16PST广播=3≥2 + 197 zero fishy=PASS(qid=-1 only). 离线回归 167/167 PASS
- **4.1/4.2 spec delta + 踩坑归档**: `openspec/specs/vcmi-protocol/spec.md` QueryReply 8B/9B 双布局 + "双布局回退" Requirement; 踩坑 #213/#214/#215 入 `docs/WSL踩坑点-引擎-VCMI-API.md`
- **遗留① green(NK2) runNetwork 段错误根治** (vcmi-native commit 65515ef24): 根因 = headless 路径 14 处裸 `ENGINE->` (CServerHandler.cpp:697 ENGINE->discord() 首崩点 / Client.cpp:536 ENGINE->windows() 二崩点) 在 ENGINE=null 时解引用 null+0x80 = dmesg "segfault at 80"; 全加 if(ENGINE) 守卫 + 重编 vcmiclient; 验证 16PST 广播 3→4 (green 活到第3回合), 无 Connection lost, dmesg 无新 segfault
- **遗留② 8B absent vs C++ uint32 路径字节不匹配疑点**: 实机 8B 已受理 (run10), 9B 为回退候选, 待 197 fishy 实锤时验证
- **交付**: 主仓 commit 1eb664b (脚本+spec+踩坑#213/214) + 7ae9ae7 (踩坑#213 崩溃点补充) + dfdc532 (踩坑#215 + 任务清单遗留①标已修); vcmi-native commit 65515ef24 (C++ 修复, mmai-ml 分支本地, 未推送)
- **关联**: 踩坑 #213/#214/#215 / spec vcmi-protocol / `py/p8/p8c_query_reply.py` / 知识库「P8-C 收尾」章

### WIN-1 击杀激励批次A 收口 ✅ (2026-09-17)

- **判据结算（生效窗 44 局, step 739253→745008+, 三判据全绿）**:
  - 窗口语义修正: L101674 首开窗段 (22 局) ep_runner 为旧代码 (零 BHERO_GRAD 输出 = P-H1 未生效), 判为污染段不计; **生效窗 = L102375 checkpoint resume 起新代码**。教训: 开窗重启后必须验证新参数在子进程实际生效 (BHERO_GRAD 首条输出), 不能只看主进程 [WIN1_BATCH] banner
  - ① 贴近: 非duel 30 局 min_d **p50=47.0 < 64** (基线 128), min_d=2 贴脸 4 局
  - ② 回报: 滚动 avg_r 0.6→0.8 持平微升; 聚合 avg_r=-73 由 T05/duel 极端局贡献, **duel 对照组 (P-H1 关闭) avg -157~-168 实锤与本轴无关**
  - ③ 护栏: 守卫/击杀事件 7 次 / RECRUITED 877 次 (START_HOME 44/44) / 局长 mean=131, 截断 20% (前 26%) 不塌
  - KL: 生效窗 2/2 更新点 <1.0; 污染段 1.096 尖峰定性为旧代码段产物 (R9 降级 🟢)
- **A2 归因定谳 (批次B 前置)**: **P-H2 同格判据 `_d1==0` (ep_runner_one.py L1131) 结构性永不触发**——注释"走上敌英雄格必触发战斗"系误读 #143 事实3 (原文: moveHero 到敌方英雄格被 "destination tile is blocked" 拒绝, 英雄战不由走上敌格触发; 蓝英雄格 passable=0 本就 BFS 不入, min_d=2 贴脸即机械下限)。批次A contact=[] = 参数未开的预期零行为, 非"没贴到"。**批次B 开窗前必修**: 首选 `_d1<=1` 邻域兜底 (零 C++), 或 battleStarted 真事件 (查 C++ 透传)
- **批次B checklist 核查全绿**: env 映射 (train_wsl2_ppo_v2.py L116-122) / 默认 0.0 零行为 / 注入过滤 `not in (None,"","0","0.0")` / 幂等 (`_contact_paid`/`_kill_paid` 每局每敌 id) / duel 排除 / 开窗路径 (restart 脚本 L10 预留行) 唯一阻塞 = P-H2 判据
- **工具修复/新建**: `py/win1_batchA_agg.py` 修复双缺陷 (窗口定位误用 loads[-1] + enumerate(lines, start) 全文迭代致"5349 局"假象) + 窗口语义注释; 新建 `py/win1_guardrail_probe.py` (护栏计数: TOWN_CAPTURE/GUARD/RECRUITED/局长分布)
- **新观察 (待查)**: [ECON] first BUILD_2 全窗 0 条——T05/T06 非_T04 图 act20 强制轮换进 877 次 RECRUITED 但 BUILD_2 首奖零触发, BUILD 类动作无效嫌疑; min_d=2 第 4 例 (T06_72_01 r=+77.5 健康局) 再证同格不可达
- **交付**: 当前任务清单 WIN-1 段改收口态 + A1-A6 表完结 (A6 遗留 BUILD 疑点); 备料文档 `docs/备料_T05引导期卡死熔断_20260917.md` / `docs/方案_own_town重复访问衰减_20260917.md`; 下一步 = P-H2 判据修复 + 批次B Environment 同窗合并

### 批次B + A3 同窗部署 ✅ (2026-09-17, 用户拍板同窗)

- **前置修复 (P-H2 判据, A2 定谳落地)**: ep_runner_one.py 新增 `--blue_hero_contact_d` (默认 0=旧行为仅同格), 判定 `_d1==0` → `_d1<=_d_th`; 批次B 注入 **contact_d=2** = 曼哈顿≤2 同格+8邻 (与 legacy"贴脸 8 邻"口径一致; 纠偏: A2 报告 `_d1<=1` 实为 4 邻域, 8 方向移动下真 8 邻 = ≤2)。[BHERO_CONTACT] 日志加 `d=…(th=…)`。语义=接战塑形近似 (贴脸奖), 真实战斗触发链 (OBS-3 aggression 遗留) 另行专项。
- **A3 合码 (own_town 衰减+空撞拉黑)**: target_scorer.py 候选4 加 own_town_blocked 剔除 / max_visits 闸门 / visits+decay 注入; type_value 衰减 V×0.5^min(visits,3) 封底3次; ep_runner 状态机 = 窗开启记 tid+兵力快照 → 窗完成 visits+1 → army power 段延迟一帧空撞复核 (零增量 → [TOWN_EMPTY] 拉黑本局); 离线自测 `py/a3_selftest.py` 5 项全过 (89.5 零行为 / 17.5 衰减 / 拉黑 / 硬上限)。
- **部署**: 优雅停 (step=747974 保存) → `py/restart_train_v5_win1_batchB.sh` start (8 Environment: grad 0.2+cap 25+contact_r 15+**contact_d 2**+kill 40/30+**decay 0.5+max_visits 4**)。生效验证: [WIN1_BATCH] banner **8 组注入** + 子进程命令行全参数 (吸取批次A 污染段教训, 开窗即验子进程)。
- **判据窗 (~40 局) 双轨可分**: 批次B = BHERO_CONTACT 非零局/BHERO_KILL/TOWN_CAPTURE/avg_r≤+150 防通胀; A3 = own_town pick 骤降 (基线 589→<100)/TOWN_VISIT 3.0→1-2 次/局/TOWN_EMPTY 计数/T05 avg_r 回升 (基线 -170~-255)。
- **回退链**: 批次B 塌 → batchA 脚本 (仅 P-H1); A3 异常 → 删 HOMM3_OWN_TOWN_DECAY/MAX_VISITS 两行 (独立可撤); 全关 → restart_train_v5_sys.sh。
- **纪律注记**: 批次B (激励轴下半批) 与 A3 (scorer 引导面) 同窗为用户拍板, 依据 = 判据可分 (事件流不同源, T05 回升主归 A3); 聚合工具 `py/win1_batchA_agg.py` 自动取最后 WIN1_BATCH 行 = 当前窗起点。

### 批次B + A3 中期收口 ✅ (2026-09-17, 修复后窗 42 局 L105629 起)

- **部署后插曲**: 首窗 25 局暴露 A3 空撞复核时序 bug (踩坑 #253: pending 在窗开启帧设, RECRUIT 未发出必零增量 → 开局取兵窗全误拉黑, START_HOME 被废); 修复 (pending 移窗结束帧) + 重启 (step=750519) 后验证 TOWN_EMPTY=0 / RECRUITED 恢复 24 次/局。首窗 T05 r 翻正结论方向成立但幅度以修复后窗为准。
- **批次B 结算**: ① 接战率>0 ✅ 5 局触发 [BHERO_CONTACT] (全 T06_72 族, d=2(th=2) +15, 局末 contact=[3] 记账闭环; min_d=2 贴脸剧本成型 — grad_total+13.2/step94 高度一致); ④ 防通胀 ✅ 窗口 avg_r **+12.83 首次转正** (对比批次A 窗 -73), 分图峰值 KoP +113.8; ② 单杀 / ③ TOWN_CAPTURE = 0 → **归 passable 闸门项** (OBS-3 预研定谳, 非参数问题, 移交后续窗)
- **A3 结算 (达标 ✅)**: own_town pick **0** (基线 589/局霸屏) / TOWN_EMPTY 0 (修复后零误判) / TOWN_VISIT **1.64 次/局** (基线 3.0, 节律 [2,35,68] 仍在但不再霸屏回流) / **T05 三图 avg +62.0/+61.7/+90.6 全翻正** (基线 -170~-255), 长局转正 (250 步局 r=+9~+71, 8/8 正局样本中 1 局 -44.3 留观察) / 衰减中间态 (V=17.5/8.75) 无直接日志点 (own_town 从未进 pick/runner_up 打印), 达标按行为终点口径成立
- **P-H1 保持**: min_d p50 33 (批次A 47 → 基线 128), 5 局 min_d=2; KL 0.71/0.3 平稳; 截断 16%
- **duel 三图** -110.5/-126.4/-170.4 (前窗 -157~-168, 有改善未翻正) — 激励真空 (P-H1/H2/H3 全排除 duel) + 贴脸僵局, 等 passable 预案
- **下一轴候选 (材料齐, 待拍板)**: passable 闸门 C++ 预案 (击杀链②③ + duel 共同前提; 方案甲口径对齐 GHandler movingOntoObstacle / 方案乙最小特判; 改动点 strategic_state.cpp L698-700+L955-974; 验证=首个真实战斗事件+BHERO_KILL+死亡率<20%; 回退=.so 备份; 详见预研 §1.4); BUILD P1 (改图预置 fort+L1, 经济轴)
- **交付**: 任务清单 WIN-1 段中期收口态 + 预研文档 §1.4 立项材料; 训练继续攒局 (当前窗参数不变)

---

## 2026-09-26 T14.3 T14Blue 工程验收闭环归档

### T14.3 蓝方战略层 C++ 插件（R13 收割层）✅ 工程验收通过（09-26）
- **真相源勘误**：reasonix 派单产出 D: 树 `AI/T14BlueMod/`（SHARED .so + dlopen 假设，事件流 34467 行 run_done ok=false）= **设计文档 §1.2 旧假设版，已清除**。唯一真相源 = **WSL 仓 `4a1675c6a2`（13 文件，STATIC_AI 门控版，fact #64 八轮探针定谳架构）** + 主仓 `ce40f7e5`（补丁器 + checkpoint 文档）。
- **部署（服务器 172.16.2.40）**：`git format-patch` 整包 `/DATA/hero3/t14blue_4a1675.patch`（441 行，13 文件）`git apply --check` rc=0 → apply（7 引擎文件 `.bak_t14blue` 备份）→ `cmake -DENABLE_T14_BLUE=ON .` + `make -j32 vcmi mlclient vcmiserver vcmiclient` → **全绿**（T14BlueMod 6 cpp / Nullkiller2 / vcmi facade / mlclient / vcmiserver 全 Built；vcmiclient 可执行体 link 失败=既有问题 fact #37，非本次引入）。符号落位：`nm libvcmi.so` 见 `T14Blue::T14Nullkiller::onPursueTurn`（T）+ NK2 `W onPursueTurn()` 空钩子（NK2 实例零行为变化）。
- **构建坑（新，踩坑 §2.21）**：首编炸=`AI/MMAI/CMakeLists.txt:204` 硬编码 `/usr/lib/x86_64-linux-gnu/libpython3.12.so`（WSL 路径，c80796a17 引入）在 ARM 服务器 link 行找不到 → 临时桥接=符号链接 `libpython3.12.so→/usr/lib64/libpython3.11.so` + `/usr/include/python3.12→python3.11`（hack，正式修=find_package(Python)，挂 T14.3b）。
- **冒烟（KTV N=4 双臂，#298 纪律）**：`/DATA/hero3/t14smoke.py`（09-25 全池同款 env 口径）8/8 全 rc=0 0 崩。T14Blue 臂 R=-414/-104/-470/-441（均值 **-357**）vs Nullkiller2 臂 -504/-324/-451/-438（均值 **-429**），**go 全 0（单图 DRAW 未破）**。结果 `/DATA/hero3/output/t14smoke_ktv_0926.json`。
- **加载实锤（判据勘误）**：常规冒烟 `T14_ACT=0` **不代表没加载**——`[T14-ACT]` 走 `logAi->info`，runner 硬编码 `vcmi_loglevel_ai=error` 全抑制 info 级。补 `loglevelAI=info` 探针（`/DATA/hero3/t14load_probe.py`）grep 到 `[T14-ACT] T14Blue resolved archetype=P0 aggr=1 seed=4242050860 map=Maps/Key to Victory.h3m`（CDynLibHandler `stem==libT14Blue` 分支命中，gateway 实例化实锤）。**判据教训：引擎 info 级打点判据必须配 `loglevelAI=info`，否则假阴性。**
- **能力面判定（不达标，挂 T14.3b）**：T14 均值不负 NK2 ~72 方向对，但 KTV 单图仍全 DRAW——R13 是 nudge 非硬注入（targetObject 每 pass 被 NK2 `updateState()` 重置，只影响 decompose 窗+blocking-dialog 判定）+ 红方 StupidAI 不接战（"对手不配合"非 T14 失效）。**区分度要看全池矩阵（红方 PPO 模型 1003121 对抗 + NK2/T14 双基线，与 S-9④ 合批三档矩阵）= T14.4 余量，下一窗口**。
- **训练无损核验**：重编全程 NRestarts=0、step 1.54M→1.58M 爬、`[SLOT]` 全 rc=0（跑中进程不读盘上替换的 .so，下次重启才吃新 .so；NK2 空钩子默认实现=存量零行为变化，风险低但非零）。20:47 一次手工优雅 restart（journal 无 kill signal、无 timer/cron，存 step=1565581→resume，零损失）。
- **回退垫**：`bin/*.bak_t14blue`（libvcmi 538MB / libmlclient 312MB / vcmiserver）+ 源树 `.bak_t14blue` 7 份 + `py/patch_t14blue_engine.py --rollback`。
- **T14.3b 另排**：① 硬目标注入（decompose 私有面）② Settings 权重轴（字段 private 无 setter，R10-R16 本版做不了）③ MMAI CMake python 路径 find_package 化（去符号链接 hack）。
- **指针**：`docs/T14.3_T14Blue_20260926_checkpoint.md`（关机 checkpoint 全量）/ 服务器知识库 §5.19 + 踩坑 §2.21 / 任务清单 T14.3 段 / fact_store "T14.3 T14Blue"。

## 2026-09-25 收敛归档（当前任务清单瘦身：✅ 已收口条原文迁出）

> 来源：从 `docs/当前任务清单.md` 迁出的已收口（✅）条目原文，按文档约定 append-only 归档。
> 当前清单只保留 🔄/🟡/待拍板/风险/拍板记录 + 服务器基建状态板（三-B 全表）。

### WIN-1 批次B + A3 中期收口 / passable 闸门 ✅（09-18~09-19 验证 4 判据全绿）
### WIN-1 — 批次B + A3 中期收口 ✅ / passable 闸门 ✅（已收口，09-18~09-19 验证 4 判据全绿）
- **收口结论**：passable 闸门（方案甲，09-18 部署 `ML/strategic_state.cpp` 两处 `isClear`→`getTerrain()->isPassable() && !(blocked() && !visitable())`）4 判据全绿：① 新口径不炸 ② 首个真实战斗（[BHERO_ATTACK] 跨图复现）③ BHERO_KILL 首次全链路记账（[BHERO_SLAIN] slain=[3] +40 end-of-ep credit）④ 死亡局占比<20% + avg_r 无塌。批次B+A3 全绿（own_town pick 0、T05 三图 +62/+62/+91 全翻正、P-H1 min_d p50 33）。duel 三图 -110~-170 已知底噪。
- **下一轴三选一（09-19 拍板）**：首选 S2 撤梯子（已 09-19 部署，见 T7.5）；次选 WIN-3 ①难度轴；WIN-2 回池殿后。开源周首推+CI 绿（`vcmi-strategic-gym` HEAD `c0cb8ed`），剩余迭代项（英文 quickstart/真测试/模型 release）不阻塞训练主线。
- **回退线**：批次B 塌/通胀 → batchA 脚本；A3 异常 → 删 2 行 Environment。聚合工具 `py/win1_batchA_agg.py` + `py/win_bB_watch.py`。
- **指针**：预研 `docs/预研_OBS3英雄战斗触发链与BUILD链_20260917.md` §1.4 / 踩坑 #250-253 / 知识库 09-18 章。错窗纪律：与 S2/WIN-3 互斥。

### WIN-4 官方 H3M 全量批转入池 ✅（09-21 收官，137/159 入池，PASS 率 85%）
### WIN-4 — 官方 H3M 全量批转入池 ✅（09-21 收官，137/159 入池，PASS 率 85%）
- **目标**：159 张官方 H3M 逐张转换 → 每张 250 步真实训练环境验证 → PASS 才改名 `<safe>_h3m.vmap` 入 `maps/training/h3m_pool/`。
- **工具链**：h3m2vmap R2v2 + `py/strip_underground_vmap.py` + `py/h3m_batch_pipeline.py` + `py/run_h3m_batch.sh`。
- **✅ 收官（09-21 第四次重启轮全量跑完）**：PASS=137 FAIL=22 SKIP=1。H3M 转换七钉全拔（header 玩家残留 / objects 残留 / VFS 小写 / removeObject null 槽 / h3m2vmap 重链 / timeout 判据 / teams+mainTown）。
- **FAIL 22 张定性（全部关闭）**：① launch fail ×18 = 无英雄/单英雄开局图，放弃入池（图池已 137 张，修 v3 hero 生成属无限长尾）；② adventure timeout ×2（knee_deep/pirates，#296 同族，挂起）；③ 早期战死 ×2（生态性正常）。SKIP=1 = warm 地下城。
- **#290 Battle of the Sexes.h3m 已修**（main.cpp null 槽，vcmi-native 41a99cd52c），09-21 重链后 PASS，无需 gdb。
- **唯一遗留 = B5 接线**：h3m_pool 137 张进训练采样——**等 T7.8 观测窗收口后接**（pool_index blue_ai 联动已就绪；`_pool_rank.json` include 全 <0.5 = 渐进混合：小图→h3m 图→1v7）。
- **指针**：踩坑 #289/#290/#295/#296/#297 / 知识库 09-20/21 章 / 普查产物 `py/h3m_type_survey.json` / 任务清单 A13。
- **VMAP→H3M 反转引擎侧验证（09-21 收尾）**：`vmap2h3m.py` 反转 + 引擎侧 `StrategicEnv` 加载验证，地图加载层通过；`query -1` 属 mlclient C++ 侧 h3m 开局初始化问题，Python 层改不动，搁置（#296）。测试脚本 `py/_test_v2h3m_engine_load.py` 已入 `py/`。

- **09-21 批转进展 + 结构性 skip 机制（#297）**：4 张新 PASS 入池（`a_viking_we_shall_go` / `all_for_one` / `and_one_for_all` / `a_viking_we_shall_go_allied`，blue_ai=MMAI_RANDOM，h3m_pool 37→41）；`a_warm_and_familiar_place` 定性"地下城"结构性无出生点，已入 `_pool_index.json` skip 列表（`blue_ai="skip", reason="地下城"`）。批转已中断（用户指令），训练当前停止——重跑 = `wsl -u root bash py/run_h3m_batch.sh 250`（启动后需手工拉 Windows keepalive）。


### T7.7 根因C'修复观察窗 ✅（09-19 收口，判据达成 → 演进 T7.8）
### T7.7 — 根因C'修复观察窗（战死/困死重定性 + duel 净胜率复跑）✅（09-19 收口，判据达成 → 演进为 T7.8）

- **修复内容（09-19 停训窗）**：`ep_runner_one.py` ① ah=-1（红英雄消失）立即终局 `[RED_DEAD]`+death_penalty -50+done（消除 fallback=0 坐标错位连锁：战死后 heroes 段重排蓝英雄顶 slot0 → 假贴脸 d=0 → 假 BHERO_ATTACK → zombie 假判定，实锤踩中立怪 battleFinished winner=1 战败局）；② B 方案 d=0 时 `_atk_skip_nav=False` 走 BFS 绕行（L1054-1062）；③ 冒烟验证：假 HERO_DEATH 3/3→0/3、假攻击 4→0、RED_DEAD 3/3 正确触发，两局长跑 r=256/239。
- **观察判据（40-60 局后，`eval_new_baseline_0919.py` 复跑）**：① `[RED_DEAD]` vs `[ZOMBIE]` 比例 → **重写 #283"62% 全困死非战死"定性**（若战死占比高，激励重心从"突围"转向"打不过别踩/变强"）；② duel 净胜率（守卫胜-困死）对比修复前（+21%，分图 +20/0/+40）；③ 假阳性清零确认（主日志不应再出现 ah=-1 后的 BHERO_ATTACK/ZOMBIE）。
- **快速拉数**：`wsl bash -c "grep -c RED_DEAD /mnt/d/Bigdata/hero3_fresh/train_loop.log; grep -c ZOMBIE /mnt/d/Bigdata/hero3_fresh/train_loop.log"`（RED_DEAD 主日志有转储）。
- **指针**：知识库 09-19「startBattle 引擎层直调」章（D 方案储备）/ 任务清单七-远期 D 方案条 / ep_runner_one.py L1607-1614（RED_DEAD）/ L1054-1062（B 方案）。

### T7.4 死亡惩罚 ✅ + T7.6 A2 攻击步旁路冒烟 ✅（09-21 收敛关闭 / 09-18 达成）
### T7.4 — 死亡惩罚 ✅（09-21 收敛关闭：判据 1 已定性，无剩余动作）

### T7.6 — A2 攻击步旁路单局冒烟 ✅（09-18 晚 B+C 窗达成：引擎侧全链路打通，红蓝英雄真实战斗首胜）

- **最终结论（详见知识库 09-18 晚 B+C 章）**：① 攻击步三处前置缺陷已修（commit `8c5b5e1`）+ C 方案 bfs_full_dir 动态障碍豁免（`7e8491d`）+ B 方案 DEBUG_FORCE 强攻开关（`69bab3d`）；② **`[BHERO_ATTACK]` 首次触发且引擎全链路受理**：moveHero 落敌格（零 blocked）→ startBattle（objType=hero，RED+BLUE 双人参战）→ R6 结算 winner=0 → 蓝英雄被歼（notifyObjectAboutRemoval 实锤）——**判据②"真实战斗事件"达成，OBS-3 链路级闭环**；③ 残留：BHERO_KILL 双帧确认在局尾截断来不及（记账时序，非链路），**局尾补记账修复待拍板**。
- **运维**：贴脸局快照器 `py/contact_log_snapshot.py`（环形 5 槽×240s+contact 触发，重启 `py/restart_snapshotter.sh`）；keepalive 守护 `py/homm3_keepalive_guard.ps1`（计划任务每 5 分钟自愈，22:31 idle shutdown 再发后加）。
- **经验**：主日志 10-env 交织 + 事件行无地图名 → 局级归因必须用局边界或 `/tmp/hermes_ep_<pid>.log`（踩坑 #256）；pgrep/pkill -f 自匹配假阳性（踩坑 #257）
- 冒烟步骤：`wsl -u root systemctl stop homm3-train-v5` → 临时改 restart 脚本加 `Environment=HOMM3_BLUE_HERO_ATTACK_BYPASS=1` → 启动 → 等 72_01 图开局（约 10-20 局内）→ grep `[BHERO_ATTACK]` 日志 + 引擎 `startBattle` + R6 onnx `battle_result≠0` → 优雅停训恢复
- 判据：`[BHERO_ATTACK]` 至少 1 条 + `state.battle_result` 非 0（R6 onnx 结算正常）
- 风险：踩坑 #254 已修（`WIN1_ENV_ARGS` 映射补齐），但冒烟前须 grep `ps aux | grep ep_runner_one | grep blue_hero_attack_bypass` 确认参数已传到子进程
- 指针：`docs/方案_攻击步旁路_20260917.md` §6（冒烟方法）/ 踩坑 #254

- 判据 1（死亡局 r 转负）：⏸ 挂起已定性——全草地大图 ZOMBIE 结构性不可达（T03/T04 小图历史 441 局，大图 0 局）；真败北信号断链另一半已由 #228 根修（红败 go=2/r=−200 秒级传递）。
- 判据 2（接战率 ±20% 不塌）✅；判据 3（avg_r 跌 <20%，基线 2.34）✅。
- 回退线：接战率塌 >30% 或 avg_r 跌 >20% → 降档 −25 或 0。指针 #204/#210/#228。

### P0-1 冻结 Mode B 修复 ✅（trade cap 16，vcmi-native 8679dd35a1）
| P0-1 ✅ | 冻结 Mode B 修复 | **已修（根因修正）**：`ResourceTrader::trade` while 循环**无上界**（每轮重跑 `buildAnalyzer.update()` 全量评估），NK2 yourTurn TBB 全程持 `CGameState::mutex` 共享锁 → `runNetwork`（Client.cpp:381 唯一写锁点）写锁饥饿。修复 = `py/patch_mlfix_tradecap.py` **cap 16** + `[ML-fix] trade BREAK` 哨兵（vcmi-native `8679dd35a1`；libvcmi.so 实查含 `trade BREAK`×1 / `[ML-time]`×3，libmlclient.so 不含——NK2 编进 libvcmi）。**验证 elbow_room ×12 全绿**。**09-24 官方归属澄清**：Mode B 是 fork 债（`CGameState::mutex` 与 `AI/Nullkiller2/` 均 fork 引入，官方无此二物），官方只修过自身内部锁（`99c075` AI turn deadlock / `61c71e` NK2 HeroChain deadlock / `a836cd3` TOCTOU），**不可能等 upstream** |

### P1-1 采样机制迁移 + 打点实证 ✅（09-24，MIX_TRACE→POOL_SCHED）
| P1-1 ✅ | 采样机制迁移 + 打点实证（09-24，`[MIX_TRACE]`→`[POOL_SCHED]`） | **用户拍板弃用百分比随机混合**（`HOMM3_H3M_MIX`）→ **确定性节拍 `HOMM3_POOL_INTERVAL=5`**（每 5 局插 1 局池图 + 队列轮转，一张一张按顺序循序）。`train_wsl2_ppo_v2.py` L168-178 `_POOL_INTERVAL` / L557-566 `[POOL_SCHED]` 打点。**实证**：`pool=2`（batch1 只剩 `good_to_go`+`judgement_day`）；MIX=0.10 时 35 局 0 池图 = 命中率正常波动（10% × 35，0 张概率 4.8%）；MIX=0.5 重启后 3 局全 `hit=True` → **采样机制正常**。**但 3/3 局 traj 丢失 → 见 P1-4** |

### P1-2 哨兵计数 ✅（09-24 13:0x）
| P1-2 ✅ | 哨兵计数（09-24 13:0x） | 重启后（step≥951759，11 局）：**[EP298_SWALLOW]=0 / [ML-upg] BREAK=0 / err≠no=0 / [ML-fix] force game_over=0**（11 局全 err=no，零 Traceback）。**样本小，按 N≥4 纪律不下根除结论**。全程对照：[EP298_SWALLOW]=0 / [ML-upg] BREAK=0 / 472 _h3m / 3015 局（15.7% 为 09-23 混入数据，不可比 09-24 重启后） |

### P1-4 池图开局故障 → 踩坑 #308 已修 ✅（XDG 目录缺失）
| P1-4 ✅ | **池图开局故障 → 踩坑 #308 已修（非 Mode B、非并发竞态）** | 09-24 MIX=0.5 采到池图 3/3 局 traj 丢失（`[WARN] traj 读取失败`，静默吞局，零 `[EP_TIME]`，crashlog 空）。**真根因（用户身份 A/B 铁证）**：服务 `User=administrator` 而 VCMI 的 XDG 用户数据目录 `$HOME/.local/share/vcmi/` **从未存在** → `CFilesystemLoader::load` 打开 `.vmap` 失败（VCMI **误报 `Permission denied`**）→ C++ `Exception` → `std::terminate` → **SIGABRT(rc=-6/-134)**。A/B：administrator→rc=134 + `Failed to open .../good_to_go_h3m.vmap`；root→rc=0。**同进程修复前后**：`rc=-6` → `[EP_TIME] map=good_to_go steps=84 r=-189.5 err=no`。**修复**：`py/setup_vcmi_runtime.sh`（预建 XDG 链 + `data/Maps` 软链 → 运行时 Maps）+ unit `Environment=XDG_DATA_HOME=...`，已接线 `restart_train_v5_sys.sh`。**验证**：池图首次跑通 `[EP_TIME] map=judgement_day_h3m.vmap steps=250 secs=299 err=no`。⚠ 旧结论「#297 同族并发竞态 / 单进程复现不了」**已证伪并标注为误判历程**（Python 侧实为串行） |

### P2-1~P2-5 代码/文档沉淀 ✅（09-24 13:0x，全完成）
| P2-1 ✅ | C++ 改动重编实查（09-24 13:0x） | **重编无需执行**——`connectors/rel/connector_v13.so`（mtime 09-19）字符串扫描命中 `.local/share` + `XDG_DATA_HOME` → 09-23 第二批路径环境化 C++ 改动**已编入 .so**（后续重编已带进），清单旧登记"未重编"已过时。`strategic_env.py` 3456→3464 docstring 修复（M6）已 commit `4c733e6` + push（master=4c733e6，09-24） |
| P2-2 ✅ | 开源周实查（09-24 13:0x） | **第二批已 push**：本地 master = origin/master = `66d6d04`（0 ahead/0 behind），CI 真测试已加（11 engine-free 单测）。M6 docstring 已 commit+push `4c733e6`。**剩余**：① #205 PR（需 PAT/浏览器，`CVCMIServer::onPacketReceived` 加 try/catch，worktree `vcmi_pr205` 分支 21 行 diff 已备）② 模型 release（需定版本） |
| P2-3 | 文档收敛检查 | 本清单已收敛；知识库 09-23/09-24 章 + 踩坑 #298/#299 为权威细节；当前清单保持短小 |
| P2-4 ✅ | **仓库拓扑坐实 + 镜像仓收敛（09-24，零干扰，已完成）** | 详录：知识库「09-24 仓库拓扑坐实」节 / 台账 §1.1 / 踩坑 #302-#305。要点：① 真实结构 = **工作区仓 + vcmi 子模块（仓#3）+ WSL 工作克隆（仓#2）**，**训练真正用的是仓#2**（`/home/administrator/vcmi-native`，非 Windows 侧任何仓）；② 仓#2 的 **10 个未推送提交**（含 #298 恢复 + 09-24 五处修复）已快进推入仓#3（`30f62b867a..8679dd35a1`，1202 文件）；③ 中转裸镜像 `D:\Bigdata\git-mirrors\vcmi-native.git` **已删**（395.6MB，删前验三分支无唯一对象），仓#2 `origin` 改指仓#3；④ **修 `py/setup_wsl_train.sh` 灾备源**——原 `cp -r $PROJ/vcmi` fallback 拷的是仓#3 的 `fix_action_mapping` 工作树，会让 **WSL rootfs 重建后静默丢掉 #298 与 09-24 全部修复**；已改 `SRC=$PROJ/vcmi` + 源缺失 `FATAL`；⑤ 新增 `D:\Bigdata\vcmi-native-wt`（仓#3 detached worktree @8679dd35a1）供 D: 侧查看/编辑。**⭐ 首次算出训练分支真实分叉面：`mmai-ml-wsl` 落后官方 1923 / 领先 20 / 冲突 786**（vs `fix_action_mapping` 的 969/69/32）→ **窗口 B 若做全量对齐，成本必须按此重估**（786 里含 370 个 add/add，源于历次"文件级搬运式上游同步"未推进 merge-base） |
| P2-5 ✅ | **仓清理 + 仓清单坐实（09-24，零干扰，已完成）** | 判据 = "git 能恢复"，**共回收 583 MB**。删除：`tools\h3mtxt`(313.7MB，其唯一未推送提交已存 `docs/patches/`)、`vcmi-official-20260829`(54.5MB)、3 个 PR worktree(`_pr_battle`/`_pr_spectator`/`vcmi_pr205`，123MB)、`D:\Bigdata\vcmi-native-wt`(92MB)、空壳 `git-mirrors`。**分支全部保留**，可用 `git worktree add` 恢复。保留：`vcmi_gym`（主仓 74 文件跟踪它）、`hero3_vcmi\20260924\vcmi`（台账在用，用户拍板保留）。**并坐实"平时说的 git"= `D:\Bigdata\hero3_fresh`**。详见知识库「09-24 仓清单与仓清理」节 |
