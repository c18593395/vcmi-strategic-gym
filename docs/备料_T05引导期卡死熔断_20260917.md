# 备料: T05 引导期卡死熔断 (A4, 09-17 重定性: 方案证伪归档, 根治转 A3)

日期: 2026-09-17 | 状态: ⛔ 方案证伪归档 (guide_abort 不部署); T05 治理转 A3 own_town 衰减
分析工具: `py/a4_stall_analyze.py` (本窗 64 局逐局名义轨迹重建 + 分段停滞 + 目标关联)

## §0 原假设与证伪结论

- **原假设 (09-16)**: T05 卡死段在 step 38-59 (act 长串同向), 落在 act_loop 门控 (60 步) 之外 = 惩罚真空带 → 方案 guide_abort (零推进 K=6 熔断提前终止引导)。
- **证伪 (09-17, 生效窗 64 局实证)**:
  1. act 长同向段 = **MOVE_TO(24) 的合法展开** (ep_runner L906-957: a=24 执行时被改写为 BFS/贪心方向动作), 是直线奔袭不是卡死; 09-16 把"直奔目标"误判为卡死。
  2. 引导窗 (24-59) 停滞段全组恒=5 (=econ 每 40 步 4 步提醒窗), **guide_abort 模拟捕获面 0%** (T05/T06/duel 全部 0/16, 0/16, 0/22) — 引导窗没有真停滞。
  3. **真停滞在自由期 (60+)**: T05 15/16 局停滞≥6 步 (同动段塌到 1-6, 振荡窗 5-30); duel 22/22 全崩; **T06 对照 0/16** (自由期同动段仍 20-28 继续奔袭)。
  4. T05 引导窗 [SCORE] pick **100% = own_town** (176/176 次, score=89.5 贴脸恒定分); TOWN_VISIT 节律 [1,34,67] 间隔≈33 步 (=取兵冷却 30 + 窗 4), 局均 3.0 次回城 vs T06 1.0 次。

## §1 重新定性的完整病理链 (T05 负分局)

```
scorer own_town 贴脸 89.5 恒定霸屏 (A3 根因: V=35 无衰减 + F=1 + g 小)
→ 引导窗 MOVE_TO 高效直奔回城 (act 长同向段 22 步, 曾被误读为卡死)
→ TOWN_VISIT 取兵窗 (可能空撞: recruit_mask≠0 但钱不够)
→ 取完/空撞后目标重选, own_town 89.5 又夺回头名 (stick 破坏后回流)
→ 自由期 (60+) 模型自主阶段在城边碎片化移动 (走1-2步→econ提醒打断→再走→横跳),
   act_loop 同动/交替/三阶检测被 econ 插穿, 只剩横跳 -3 与步罚慢性扣分
→ 烧到 130-250 步, r -117 ~ -337
```

- **T05 vs T06 唯一差异变量 = own_town 霸屏与否** (T06 scorer 走 blue_hero/blue_town 高 V 大目标, 自由期继续奔袭, r +18~+105) → **根治 = A3 own_town 衰减 + 空撞拉黑** (`docs/方案_own_town重复访问衰减_20260917.md`), 而非引导熔断。

## §2 guide_abort 方案处置

- **不部署**, 本文档降级为证伪归档。理由: 捕获面 0% (无停滞可捕), 且引导窗健康奔袭误熔断反而伤及正常引导。
- `--guide_abort_stall` 参数不实现 (避免死参数)。

## §3 残余观察 (保留, 不立项)

1. **自由期碎片化移动绕过 act_loop 检测**: econ 每 40 步 4 步提醒打断同动/交替计数窗口 → 惩罚密度不足。但这是**收益端**问题 (城边打转的 shaping 收益仍≥罚), 等 A3 部署斩断目标回流后复查: 若 T05 自由期恢复奔袭则彻底关闭; 若仍停滞再议"自由期碎片化治理" (如 8 步窗动作多样性门控)。
2. **duel 自由期 22/22 崩**: duel 无 own_town 霸屏 (pick=blue_hero), 停滞疑 = 贴脸僵局 (min_d=2 机械下限, 敌格 blocked 无路可走) + P-H1 关闭无梯度 — 待批次B (contact_d=2 贴脸奖 + kill 阶梯) 改变激励面后自然复验, 不单独立项。
3. **名义轨迹积分工具** `py/a4_stall_analyze.py` 保留 (含 _DIRS 编码, 可复用于任意窗的逐局行为形态审计); 注意局限: 被拒移动不可见 (名义=上限), visit 时间点 [1,34,67] 的跨局一致性表明确定性事件 (econ 强制+冷却节律) 主导前 70 步。
