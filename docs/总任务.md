# HoMM3 全盘操盘 AI — 总任务（整体脉络）

> **最终目标**：AI 完整操控真实 HoMM3，与你对战（1v7）并取胜。
> **演进路线**：1v7 高级电脑 → 2-3 真人 → 人 vs 人 vs 模型联网（VCMI Windows 引擎内部接口）。
> **总体策略**：VCMI 沙盒训练战略模型 → 真实游戏内存操控 → 自对弈持续进化。

## 任务三件套导航

| 文档 | 职责 | 特性 |
|------|------|------|
| **总任务.md**（本文件） | 整体脉络：大项 T1-T13、里程碑、架构、参数快照、原则与资产索引 | 状态随闭环回写 |
| `当前任务清单.md` | 当下窗口要完成/解决的任务：窗口焦点 WIN-x、观察期任务、A/B/C/D/P/R 工作台 | 完成即移出 |
| `已完成任务.md` | 已闭环任务的详细归档（追加型，新条目置顶） | 只增不删 |

技术参考（非任务体系）：`源码分析地图.md`、`动作空间设计文档.md`；运维与踩坑：`WSL知识库.md`、`WSL踩坑点.md`。

## 编号规则（AI 必读）

- 大项 `T1`~`T13`，子任务 `T.n`（如 T6.2）；插入中间任务用小数（T6.5）。
- 任务变更（取消/合并）保留原号打 ❌ 并注明去向，**不重用编号**，避免引用断裂。
- 新增大项顺延 T14+；窗口型任务（无 T 归属的当窗工作）在当前清单用 `WIN-x` 编号。
- 状态标记：✅ 完成 ｜ 🔄 进行中/观察期 ｜ 🟡 部分完成/前置就绪 ｜ ⬜ 待办 ｜ ❌ 取消。

---

## 一、总进度

| 大项 | 目标 | 状态 |
|------|------|------|
| T1 | VCMI 冒险地图 API | ✅ 完成 |
| T2 | 战略 Gym 环境（obs 264→2689→3464） | ✅ 完成（含 09-14 终局信号修复，#228） |
| T3 | 基础 PPO 训练 | ✅ 完成 |
| T4 | Connector 适配 + BC 采集微调（事件奖励 + KL） | ✅ 完成 |
| T5 | 动作空间宪法冻结（64 码 + obs 3464） | 🔶 T5.1-T5.7 ✅ / T5.8 🔄 训练中 / T5.9 ⬜ 远期 |
| T6 | NK2 逻辑融合（1 估值→2 寻路→3 决策） | 🔶 T6.1 ✅ / T6.2 ✅ / T6.4 🔄 大图池训练中 / T6.3 ⬜ |
| T7 | 城镇经济（当前主线） | 🔶 T7.1-T7.4/T7.6 ✅ / T7.4 观察期 / T7.5 🟡 节奏已定（09-15）待 WIN-1 触发 |
| T8 | 战斗集成 | ⬜ 待办（v15 合并后评估） |
| T9 | 真实游戏对齐（Track 2） | 🟡 战斗链+战后恢复闭环（08-19）；路线 C 待 T5.8 成熟 |
| T10 | 你 vs AI（1v1） | ⬜ 待办（T13.10/T9 铺路） |
| T11 | 1v7 战胜你 | ⬜ 最终目标（需 T12 前完成 v15 迁移） |
| T12 | v15 架构迁移（GNN + py::dict） | ⬜ Phase G 前 |
| T13 | 外挂 AI 架构（VCMI 网络协议） | ✅ T13.1-T13.10 全闭环（P8-B/C/D/E 实机；P8-D 双机路径已用 WSL 双节点 9/9 + 单机双实例 13/13 实跑完成 09-14；P8-E 人机混局 2 客户端拓扑 PASS 09-15，`py/p8e_human_mix_probe.py`） |

---

## 二、大项总览（整体脉络）

### T1 — VCMI 冒险地图 API ✅
**目标**：VCMI 冒险地图层原子通信，供战略 Gym 读取。

- T1.1 adventure_process_turn ✅
- T1.2 strategic_state_update ✅
- T1.3 adventure_get / send_action ✅
- T1.4 g_strategic_state 全局指针 ✅

### T2 — 战略 Gym 环境 ✅
**目标**：gymnasium.Env 战略环境，obs 维度 264→2689→3464。

- T2.1 StrategicEnv 主环境 ✅
- T2.2 obs 构建器 ✅
- T2.3 奖励函数 ✅（09-14 修复 NK2 分支吞终局 ±200，#228）
- T2.4 ep_runner_one 单局执行器 ✅
- T2.5 passability mask ✅
- T2.6 态势感知扩展（obs 264→2689）✅
- 附：09-14 终局信号通道修复（红败秒级 game_over=2 / r=-200，不再 300s 超时丢信号，#228）✅

### T3 — 基础 PPO 训练 ✅
**目标**：PPO 自对弈基础能力。

- T3.1 500ep×20 步 ✅
- T3.2 GPU 加速 ✅
- T3.3 Checkpoint + 对手池 + ELO ✅
- T3.4 1000ep×50 步×128 batch，110 张 H3M ✅
- T3.5 多玩家 + AI 对手 ✅
- T3.6 STEPS_PER_EP 50→200 ✅

### T4 — Connector 适配 + BC 采集微调 ✅
**目标**：Connector 适配修复链 + BC 行为克隆采集微调（事件奖励 + KL）。

- T4.1 Connector 适配 + 修复链（N2-N11，EEXIST 解锁，全线打通）✅
- T4.2 事件奖励 ✅
- T4.3 KL 约束 ✅
- T4.4 BC 采集 → PPO 重启链路 ✅（后续演进归入 T5.8 / T6.2）
- T4.5 采集排障 4 连 ✅
- T4.6 训练重启 ✅

### T5 — 动作空间宪法冻结 🔶
**目标**：动作码 64 位 + obs 3464 维长期宪法，一次设计冻结。

- T5.1 源码 9 面枚举 ✅
- T5.2 动作空间 v2 冻结（64 码）✅
- T5.3 OBS schema v3 冻结（2689→3464）✅
- T5.4 敌方可见性验证 ✅
- T5.5 obs v3 实现 ✅
- T5.6 C++ 动作 11-24 ✅
- T5.7 冒烟测试 16 项 ✅
- T5.8 BC 重采集 → PPO 重启 🔄 训练验证中
- T5.9 v2 MultiDiscrete 三维参数化 ⬜ 远期（与 T6.3 合并）

### T6 — NK2 逻辑融合 🔶（主线之一）
**目标**：1v7 正道——NK2 专家骨架 + 学习组件渐进替换（1 估值→奖励，2 执行→寻路，3 决策→分层）。

- T6.1 NK2 估值 → 奖励塑形 ✅ 冻结（08-22）
- T6.2 执行层 C++ BFS 全图寻路 ✅ 代码完成 + T03 段毕业（08-29）
- T6.3 决策结构分层 ⬜ 远期（与 T5.9 合并）
- T6.4 训练地图体系（VMAP 课程 T01-T06 + H3M 官方图）🔄 当前 MAPS=10 大图池训练中（v5）
  - 09-08：T06 1v3 课程结案（尾窗四判据全达标）
  - 09-13：地图轴二次扩展（去 T04/T05 MIR，加 T06 三张 72/108 大图 + King of Pain）
  - 09-14：King + 4 张 T06 _02 故障图全部修复回池，MAPS=10 干净池

> 当前 v5 = T6.1 估值 + T6.2 寻路 + T7.3 经济，三者叠加实跑。

### T7 — 城镇经济 🔶（当前主线）
**目标**：1v7 必经之路（经济招兵）。**纪律：晋级与开经济不同时做，一次只加一个难度轴。**

- T7.1 Level 3 晋级 ✅（08-29）
- T7.2 切 T04 地图（不开经济）✅（08-29）
- T7.3 开经济（一次一轴）✅（08-29 深夜）
- T7.4 存活塑形 / 死亡惩罚 ✅ 已上线（09-11，-50 试探档）→ 🔄 观察期；判据 1（死亡局样本）因大图 ZOMBIE 结构性不可达挂起，其引擎根因（败北信号断链）已由 09-14 H3 修复闭环（#228）
- T7.5 经济奖励撤梯子策略 🟡 S2-S4 节奏已定（09-15，WIN-1 达标 + capture 稳定 ≥2 窗后触发 S2；S2/S3 激励轴与 WIN-3 难度轴错窗；部署时机 = 自然重启批次与 D3/C2 L0 合并）
- T7.6 取兵链路修复（P1→P1e + start_home）✅（09-03 四拍闭环）
- WIN-1 capture +100 激励观察窗 🔄（09-08 上线，详见当前清单）

### T8 — 战斗集成 ⬜
**目标**：英雄战斗进观测/奖励，战斗结果建模。

- T8.1 英雄踩怪 → autofight ✅
- T8.2 英雄 VS 英雄战斗 ⬜
- T8.3 战斗结果进观测/奖励 ⬜
- T8.4 战斗状态追踪（obs.in_battle）⬜

> 注：MMAI v15 未合并官方，2-4x 慢；战斗层版本锁定不追。R6 已启用现成 onnx 战斗 AI（USING_ONNX=1），当前无需自训战斗模型。

### T9 — 真实游戏对齐（Track 2）🟡
**目标**：模型落地真实 HoMM3（路线 C：原版 SoD 3.2 + H3API DLL 为最终目标）。

- T9.1 H3API 编译验证 + 注入 demo ⬜（前置 T5.8 成熟）
- T9.2 状态读取层（obs ↔ H3API）⬜
- T9.3 动作执行层（hook 原版函数）⬜
- T9.4 推理层 ONNX + DLL ⬜
- T9.5 稳定性 24 局 ⬜
- T9.6 VCMI Windows 路线验证 ⬜
- 里程碑：战斗链 + 战后恢复闭环 ✅（08-19，1.7.5 GUI 5AI 7 战 0 拒 0 崩）；mq 模型部署线 v5 全模型 1v7 实机 PASS（09-11）

### T10 — 你 vs AI 1v1 ⬜
**目标**：人机 1v1 对战底座。待 T9 / T13.10 铺路。

### T11 — 1v7 战胜你 ⬜
**目标**：最终目标。需在 T12 前完成 v15 迁移。

### T12 — v15 架构迁移 ⬜
**目标**：图状态取代平面向量 + GNN + py::dict 通信 + 训练体系重写（Phase G 前）。

- T12.1 图状态（nodes + links）
- T12.2 GNN 取代 MLP
- T12.3 py::dict 取代 struct
- T12.4 训练体系重写

### T13 — 外挂 AI 架构（VCMI 网络协议外挂）🔶
**目标**：实现外部 AI 客户端，通过 VCMI 官方 TCP 网络协议接入游戏，**不改 VCMI 一行代码**；终极目标人 vs 人 vs 模型联网。
**架构**：外挂 AI 实现 VCMI 客户端协议（`lib/network/` + `lib/networkPacks/`），与 vcmiclient 同地位接入 NetworkServer，Server 不区分人类 vs AI。

- T13.1 序列化逆向 ✅
- T13.2 核心包验证 ✅
- T13.3 客户端连接层 ✅
- T13.4 战略包发送器 ✅
- T13.5 客户端包解析器 ✅
- T13.6 Query 回复管理器 ✅
- T13.7 战斗包对接 ✅
- T13.8 模型接入层 ✅
- T13.9 端到端验证 ✅（离线 144→167 项全 PASS）
- T13.10 多人对战验证 ✅ P8-B（lobby + 完整对局）✅ / P8-C（MoveHero + Query + 城镇链决策接入）✅ / P8-D（双机部署路径已用单机双实例实跑完成：WSL 双节点 9/9 + 单机双实例 13/13，09-14）/ P8-E（人机混局验证 2 客户端拓扑：Python host + 外挂AI，人类 GUI 同机共存不 join slot，09-15 PASS，`py/p8e_human_mix_probe.py`）；备料型已补（09-14）：QueryReply 重跑确认数据源墙仍成立 + 30min 浸泡 PASS（稳定性）+ 单机双实例 13/13

> 代码入口 `py/vcmi_protocol/`；实机细节与外部资源调研（proxy-server、h3mtxt 等）见知识库「VCMI 网络协议与外挂 AI 架构」章与当前清单 T13 专区。

---

## 三、重要里程碑时间线

- **2026-07-29**：T4.1 Connector 适配 + 修复链完成，EEXIST 解锁 / 全线打通。
- **2026-08-01**：T4.6 训练停止（用户关机）；T2.6 态势感知扩展 obs 264→2689；T4 事件奖励 + KL 合并。
- **2026-08-14 ~ 08-15**：T5 动作空间宪法冻结（T5.1-T5.7），obs v3 落地 2689→3464，C++ 动作 11-24，冒烟 16 项。
- **2026-08-16 ~ 08-19**：NK2 卡死修复全闭环（T8 前置）；有头 GUI 验证；MOVE_TO 训练端落地；真实游戏战略层联调。
- **2026-08-22 ~ 08-24**：T6.1 估值塑形冻结；T6.4 地图体系 T01-T06 生成；T6.2 BFS 代码完成 + 超参链冻结。
- **2026-08-26 ~ 08-28**：守卫战斗根因闭环（Level 2 正奖励率 83%）→ 真战斗全链打通；END_TURN zombie 三层防御 + v4 段交付；v5 训练运维根治。
- **2026-08-29**：T7 经济前置准备；T03 毕业 + T7.1/T7.2 Level 3 晋级；T7.3 开经济；B 方案自主通道修复 → 课程突破。
- **2026-08-31**：TOWN 轴复活（触发率 0→36% 局）；回城取兵链路打通；Router 断言根因 R6（USING_ONNX=0）定位；garrison obs 恒 0 发现（撤梯子硬前置）。
- **2026-09-02 ~ 09-03**：T7.6 取兵链路四拍闭环；R6 战斗 AI 修复（onnxruntime 部署，V13::BAI 接管）；T05 混入。
- **2026-09-08**：T06 1v3 课程结案（四判据全达标）；capture +100 激励上线；battle-only / GUI 战斗链闭环。
- **2026-09-10**：Windows GUI 浸泡复测 PASS（五崩全修，#164/165）。
- **2026-09-11**：T13 外挂协议栈主体完成（T13.1-T13.9）+ P8-B 实机 lobby/对局 PASS；T7.4 死亡惩罚上线；C4 NK2 寻路加速 merge（#7632）；mq v5 全模型部署链 1v7 PASS；训练存活机制重构（system 级 unit + keepalive，#201）。
- **2026-09-12**：P8-C 协议栈四层隐性缺陷修复 + MoveHero/Query 双探针闭环（#207/#208）；P8-D 设计骨架 + 本机 13/13；TOWNSTALL 平台段误判修复（#209）。
- **2026-09-13**：T06 duel capture proxy 49 误报修复（#213）；VMAP `header.players=[]` 系统性缺陷批量修补（King + T06 _02 四图，#222/#223）；地图轴二次扩展 MAPS=10；P11 VMAP→H3M roundtrip 闭环。
- **2026-09-14**：**H3 standardDefeat 败北信号断链根修闭环（#228）**——server 战斗结算去守卫 + ML .so 终局通道（-2）+ Python 配套（NK2 分支补 ±200），红败秒级 go=2/r≈-226 rc=0；T06 _02 四图 + King 全部修复回池（#224-#227）；改图同步固化 `sync_maps_to_runtime.py --strict`；假局事故三道拦截；**P8-D 双机实机闭环（WSL 双节点 9/9 PASS，HSK 预交换+跨节点 TCP+HMAC 正负例+状态文件，另修 remote_connection `close()`→`disconnect()` 遗留 bug）**；**P8-D 同机 2 实例真机部署验证（13/13 PASS，2 真实 VCMI_server 端口隔离 3030/3031 并存 + 2 真实 ModelAI client 跨实例 + HSK 跨实例预交换 + HMAC 正负例 + 双节点状态文件，`py/p8d_two_instance.py`）**；**T13.10 备料型全补（09-14）：QueryReply 重跑确认数据源墙仍成立 + 30min 混人浸泡 PASS（稳定性，无崩溃/0 断线/743 包/26 回合，`py/p8/p8b_soak_30min.py`）**。训练 resume step=689938 / maps=10。
- **2026-09-15**：P10 target_scorer 灰度启用（`--target_chain scorer`，首局 King r=119.3 正收益）；T7.5 S2 回退延后（#235 BHERO_KILL=0 前置断链）；D3 熵 bonus + C2 L0 崩溃插桩合批部署（05:34，PID 10651）；四项备料脚本落地（c2_crash_gdb.sh / t05_02_data_pull.py / win3_move_town / 方案文档）；P8-E 人机混局 2 客户端拓扑 PASS。关机停止训练，checkpoint step=696906+。

### 09-15 部署后首份观察记录（D3 + C2 L0）

**部署批次**：`systemctl stop` → 清 `__pycache__` → `systemctl start`，05:34 起 PID 10651，checkpoint resume step=696906。

**D3 熵 bonus 验证**：
- 首个 entc 行出现在 ep=14（`step698978`）：`entc=0.0853 ent=1.560`
- 公式核对：`0.05 × (1 + exp(-698978/2000000)) = 0.0853` ✓ 完全一致
- entc 随 step 增大向 0.05 收敛（warmup 衰减）
- **踩坑**：L435（per-episode 行，无 entc）每局打一行；L534（per-epoch 行，含 entc）需攒满 1 个 BATCH（~10-14 局 / ~55min）才打。部署后前 30min 看不到 entc 是正常现象。

**C2 L0 崩溃插桩验证**：
- 零 SIGSEGV/SIGABRT/rc=139/rc=134 命中（本会话 0 崩溃）
- C2 L0 设计目标：零崩溃时静默，复发才触发（rc 信号名翻译 + 崩溃 ep_log 归档 crashlog/ + 近 100 局≥3 次红警）
- 441 条 `[ZOMBIE] hero dead (all-blocked x2)` 为 T06 地图设计性现象（蓝方英雄卡死），非崩溃，C2 不计入红警

**P10 `[SCORE]` 灰度（重启后延续）**：
- own_town 128 次 + blue_hero 79 次；own_town 锚点 (2,1) score=89.5 稳定（经济期回城）
- blue_hero 的 F=-0.32（经济期 V×0.2 衰减）生效；plen 跨度 56-129（英雄移动中路径长度变化正常）
- 无 hard_filter 全过滤 / 无 score=0 边缘 case

**avg_r 对照（仅本次会话）**：
| 段 | n | mean | 正率 | r≥2 |
|----|---|------|------|-----|
| 重启前 ep1-6（PID 9342） | 6 | +0.7 | 67% | 50% |
| 重启后 ep1-25（PID 10651） | 25 | -0.26 | 44% | 0% |
| 重启后 最近 5 局 | 5 | -0.02 | 60% | 0% |

小样本（25 局）+ 熵调度初期扰动，最近 5 局已回升至 0 附近，**非模型退化，无需干预**。

**训练节奏**：time 从 527s/ep（重启前）降至 ~300s/ep（重启后近期），BATCH 周期 ~55min。

**下次观察点**：ep=28 行确认 entc 继续衰减；观察 ent 是否从 1.560 向 1.0 以下收敛（探索降低）；T6.4 训练暂停后重启需 `systemctl start homm3-train-v5` + Windows keepalive。

---

## 四、架构

### 双轨并行

```
┌──────────────────────┐   ┌──────────────────────────┐
│  Track 1: 训练 🟢    │   │  Track 2: 真实游戏对齐 ⬜ │
│  持续提升模型强度      │   │  模型落地到真实 HoMM3    │
│  WSL2 GPU 后台跑      │   │  Windows 内存读取验证    │
│  不占交互注意力        │   └──────────────────────────┘
└──────────────────────┘
```

- Track 1：PPO 自对弈，WSL2 RTX3060 24/7。
- Track 2：ONNX 导出 → 真实游戏内存读取 → PostMessage 操控。
- T13 是 Track 2 的替代路径：不改游戏，走网络协议外挂。

### 战略层 vs 战斗层

```
战略层 RL (PPO) → 决定移动/探索/占矿/攻城/分兵/合兵/招兵
  ↓ 遇敌
战斗裁决 → VCMI autofight / V13::BAI(onnx) 自动裁决（当前）
         → v15 战斗模型（后续 T8，需官方合并 + 重采集）
```

### 两段式设计

| 端 | 位置 | 用途 |
|----|------|------|
| 训练端 | WSL2，RTX3060 | PPO 自对弈训练，出 ONNX |
| 推理端 | ARM64 服务器（172.16.2.40） | ONNX 推理，ctypes 读状态 |
| 部署端 | 本机 HD Mod | 内存读取 + PostMessage 操控 |

---

## 五、当前训练参数快照（2026-09-14，v5 大图池生效中）

**训练状态**：v5 system 级 unit 托管（`homm3-train-v5.service`，/etc/systemd/system，enabled）+ Windows keepalive 常驻；09-14 06:24 部署：checkpoint resume **step=689938**，**maps=10，device=cuda**。09-15 05:34 部署：D3 熵 bonus + C2 L0 崩溃插桩合批（systemctl stop/start，PID 10651，checkpoint resume **step=696906**）；09-15 关机停止（T6.4 训练暂停，重启需 `systemctl start homm3-train-v5` + Windows keepalive）。日志 `train_loop.log`（实时 `Get-Content train_loop.log -Tail 20 -Wait`）。

**地图池 MAPS=10**（权威源 `train_wsl2_ppo_v2.py` L48-77；改图后必跑 `py/sync_maps_to_runtime.py --strict`）：

| 分组 | 地图 |
|------|------|
| T05 ×3 | 36X36_01 / 52X52_01 / 52X52_02 |
| T06 ×6 | 72X72_01 / 72X72_01_duel / 72X72_02 / 72X72_02_duel / 108X108_02 / 108X108_02_duel |
| H3M 官方 ×1 | King_of_Pain_h3m |

**核心超参**：

- 训练：BATCH=2048 / EPOCHS=6 / STEPS_PER_EP=200 / N_EPISODES=1000 / GAE λ=0.90 / grad_clip=1.0 / LR=3e-4 / KL_TARGET=0.50 / CLIP=0.2 / EXTREME_ADV_CLIP=6.0
- 塑形（生产 ep 实参）：reward_explore=0.3 / nk2_shaping_scale=0.45 / economy_force=24 / objective_reward=30 / move_to_force=60 / move_to_bias=2.0 / cycle_detect=5 / act_loop_penalty=1.0 / guard_done_steps=15
- 终局/事件：胜 +200 / 败 -200（**H3 修复后 NK2 分支同样真实传递**，clip (-300, 300)）；死亡惩罚 death_penalty=-50；守卫 +100；capture proxy +100（1v3 三蓝英雄全灭 / duel 已排除误报）
- 经济：周期提醒（每 40 步 4 步经济窗）+ RECRUIT first+12/每次+2 + BUILD_2 first+15/每次+3 + 兵力增量 0.01
- 对手：red=MMAI_USER，blue=MMAI_USER / 对手池（生产局 blue_ai=MMAI_RANDOM、blue_adventure_ai=MMAI；对战胜负验证用 Nullkiller2）
- 防坍缩栈：横跳惩罚 -3.0 + 熵 -0.05 + END_TURN 冷却 + KL 自适应（0.50 / coef_min 0.05）+ NK2 scale 0.45 + return normalization + zombie 三层防御
- Checkpoint：每 50 step 保留 10 个；OBS_DIM=3464 / N_ACTIONS=25（0-24，25-63 预留）

---

## 六、关键原则

1. **训练地图用 VMAP**（ZIP+JSON，T01-T06 课程体系）；H3M 经典图已停用（`h3m_tool.py` 保留逆向参考），King_of_Pain 是唯一经 h3m2vmap 转换入池的官方图。
2. **改代码运维**：`wsl -u root systemctl stop homm3-train-v5` 优雅停 → 改完 `start`（checkpoint resume）。训练挂 system 级 enabled unit + Windows keepalive `wsl --exec sleep infinity`（防发行版容器空闲关停，#114/#201；Windows 重启后需重起 keepalive）。
3. **VCMI 铁律**：不重编 libvcmi.so；只编 vcmiserver 可执行文件 + libmlclient.so + libMMAI.so + connector。
4. **双树甄别（09-14 纠错）**：生产编译树 = `/home/administrator/vcmi-native`（构建目录 `rel/`）；`vcmi-native-build` 是 08-02 陈旧树、**不参与生产**，改码前先判定生产树（strings 内嵌路径 + CMAKE_HOME + mtime 三证）。libmlclient.so 需 rel/bin + build/bin 双副本同步；详见知识库 H3 章与 #228。
5. **改 Python 后清 `__pycache__`**。
6. **动构建树前备份** .so + 源码（backups/ + `*.bak.<日期>`）。
7. **OBS/动作空间冻结铁律**：禁增删维度 / 改语义 / 重排段序；扩展只启用预留位或走旁路（如 terrain 走 terrain_grid.bin，不占 obs）。
8. **不追官方更新**：锁战斗层版本；NK2 寻路加速（#7632，约 40% 吞吐）已 merge（09-11）。
9. **奖励原则**：被动收入不做 per-step 奖励（导致 END_TURN 塌缩），只做终局/事件奖励。
10. **晋级纪律**：晋级与开经济不同时做，一次只加一个难度轴（T7 铁律）。
11. **事实核查**：「X 是否在用 / 是否生效」以代码 + 运行日志 + 二进制产物为准，文档仅作索引。

---

## 七、关键资产索引

| 资产 | 位置 |
|------|------|
| 训练脚本（V2） | `train_wsl2_ppo_v2.py`（MAPS 权威清单 L48-77） |
| StrategicEnv | `vcmi_gym/envs/v13/strategic_env.py` |
| ep 执行器 | `ep_runner_one.py`（一子进程一局，收尾 os._exit） |
| strategic_state（C++） | `vcmi-native/ML/strategic_state.h/.cpp` |
| 生产 server | `/home/administrator/vcmi-native/rel/bin/vcmiserver` |
| 训练托管 | system 级 unit `homm3-train-v5`（/etc/systemd/system）；停/启 `wsl -u root systemctl stop\|start homm3-train-v5` |
| 训练日志 | `D:\Bigdata\hero3_fresh\train_loop.log`（ep 明细 `/tmp/hermes_ep_<pid>.log`） |
| Checkpoints | `checkpoints/`（最新 wsl2_ckpt_689498.pt，state step=689938） |
| VMAP 训练地图 | 权威源 `maps/training/`；运行时 `vcmi/data/Maps`（两棵 VCMI 树软链同一真实目录） |
| 改图同步 | `py/sync_maps_to_runtime.py --strict`（改图后必跑，rc≠0 禁训） |
| H3M 地图（已停用） | `maps/`（h3m_tool.py 保留逆向参考） |
| 动作空间宪法 | `docs/动作空间设计文档.md` |
| 源码分析地图 | `docs/源码分析地图.md` |
| 健康监控 | `py/train_health_monitor.ps1` + `monitor_alerts.log` |
| 外挂 AI 协议栈 | `py/vcmi_protocol/`（+ `py/p8/` 实机探针） |
| 任务三件套 | `总任务.md` / `当前任务清单.md` / `已完成任务.md` |
