# 服务器知识库 — HoMM3 战略 AI（WSL2 PPO 自对弈）

> **验证原则**：本文每条事实均来自源码或服务器实测，标注格式 `【来源：文件:行 / 实测】`。旧架构（SB3 256 维 / 11 动作 / v13 connector）内容已迁出，踩坑与历史纠正见 `docs/服务器踩坑点.md`。
> 最后更新：2026-09-24（全量重写逐条核实；补 :8080 推理服务契约/T13 远程决策后端/热更新；新增 §七 外部 VCMI 生态与网络协议参考：T13 参考源=VCMI 官方网络层（非第三方）、GitHub 生态可用性分档、H3M↔VMAP 翻译机是 VCMI）。

---

## 一、项目概述与当前架构

**目标**：AI 完整操控 HoMM3 真实游戏，战胜玩家（1v7）。

**策略**：VCMI 沙盒训练战略模型 → 真实游戏内存操控 → 自对弈持续进化。

**当前阶段（已核实）**：
- 训练端：WSL2（x86, RTX3060），`py/train_wsl2_ppo_v2.py` 24/7 运行，SIGTERM/SIGINT 安全落盘【来源：train_wsl2_ppo_v2.py L504-505】。
- 模型：自研 PPO `Net`，观测 3464 维状态向量 + 独立 4×21×21 地形栅格，动作 25【来源：strategic_env.py L213-216；train_wsl2_ppo_v2.py L136-151】。
- checkpoint：每 50 step 存 `checkpoints/wsl2_ckpt_<step>.pt`【来源：train_wsl2_ppo_v2.py L660-665】。

**双轨**：
- Track 1 训练（WSL2 GPU）：持续提升模型强度，后台 24/7。
- Track 2 真实游戏对齐（Windows 内存读取）：模型落地真实 HoMM3，内存偏移见 `对齐清单_CN31.md` / `对齐清单_HD.md`。

**服务器 172.16.2.40 角色**：模型仓库（`/models/load`）+ WIN RATE 结果中枢（:8088）+ 旧 SB3 推理服务（:8080）；**不跑新模型训练/评测**（无 GPU + ARM64 旧架构维度不兼容）。详见 §五。

---

## 二、训练架构（WSL2，逐条核实）

### 2.1 模型 Net
【来源：train_wsl2_ppo_v2.py L136-151】
- 状态分支：`Linear(3464,128) → ReLU → Linear(128,128) → ReLU`
- 地形分支（CNN）：输入 `(4,21,21)` → `Conv2d(4,16,3,padding=1)→ReLU→MaxPool2d(2)` → `Conv2d(16,32,3,p1)→ReLU→MaxPool2d(2)` → `Conv2d(32,64,3,p1)→ReLU` → `Flatten(1600) → Linear(1600,128)→ReLU`
- 合并：`Linear(256,128)→ReLU`（状态 128 + 地形 128）
- 头：actor `Linear(128,25)` / critic `Linear(128,1)`
- `forward(x, terrain=None)`：地形栅格是**独立第二输入**，不并入 obs。

### 2.2 观测空间（OBS_DIM = 3464）
【来源：strategic_env.py L213-216, L285-304】
- obs 是 3464 维**状态向量**；地形以独立 `(4,21,21)` 栅格作为 CNN 第二输入（不在 obs 内）【来源：strategic_env.py L235-236 TERRAIN_GRID_SIZE/CHANNELS；调用 `model(obs, terrain)`】。
- obs 布局（索引，区间总和验算 = 3464）：
  - `[0:8]` global：day/week/month/current_player/map_w/map_h/has_underground/player_count
  - `[8:128]` players（8×15）
  - `[128:336]` heroes（8×26）
  - `[336:480]` towns（8×18）
  - `[480:1155]` local window（15×15×3：可通行 / 对象类型 / 守卫战力）
  - `[1155:3203]` explored（32×32×2）
  - `[3203:3211]` active_hero
  - `[3211:3219]` passable（8 方向，动作屏蔽用）
  - `[3219:3251]` nav（32）
  - `[3251:3315]` target_list（64）
  - `[3322:3326]` enemy_threat（7）
  - `[3326:3330]` battle_pred（4）
  - `[3330:3464]` events(4) + reserved(130)
- 注：区间已逐段相加验证为 3464；`_test_suite M6` 曾误写 3456，已校正【来源：strategic_env.py L287 注释】。

### 2.3 动作空间（25）
【来源：strategic_env.py L186-213】
- 0-7：`MOVE` 八方向
- 8：`INTERACT` 拾取/对话/攻击
- 9：`NEXT_HERO` 切到下一英雄
- **10：`END_TURN` 结束回合** ← 注意：结束回合是动作 10，**不是 24**
- 11：`SPLIT_1OF3` / 12：`SPLIT_1OF2` / 13：`SPLIT_ALL` / 14：`MERGE_FROM` / 15：`SWAP_ARMY`（分/合/换兵力）
- 16-18：`RECRUIT_1/2/3`（最近己方城镇招兵）
- 19-21：`BUILD_1/2/3`（最近己方城镇建造）
- 22：`GARRISON` 驻守 / 23：`RECRUIT_HERO` 招募英雄
- **24：`MOVE_TO` 高层移动：走到最近可交互对象** ← 注意：动作 24 是 MOVE_TO，**不是结束回合**

### 2.4 动作屏蔽（训练真源 ep_runner_one.py）
【来源：ep_runner_one.py L554-575】
- 0-7：受 `obs[3211:3219]` passability 约束（非法方向 `logits=-inf`）
- 8 `INTERACT` / 9 `NEXT_HERO` / 11-15：永久屏蔽（vcmi-native-build 无对应执行分支，纯 NOOP 刷步）
- 16-21（RECRUIT/BUILD）：仅 `T04` 地图开启；非 T04 全屏蔽；开启时还需 `recruit_mask`（`obs[640:672]`，256-bit 位域）与 `build_mask`（`obs[672:704]`，256-bit 位域）位域合法才允许
- 22 `GARRISON` / 23 `RECRUIT_HERO`：永久屏蔽（需双目标/多英雄槽）
- 10 `END_TURN` / 24 `MOVE_TO`：始终可用
- **评测器 `py/winrate_eval.py` 的 `masked_argmax` 必须镜像上述逻辑**，否则评测分布失真。

### 2.5 地图（关键纠正：当前战略训练用 .vmap，不用 H3M）
【来源：train_wsl2_ppo_v2.py L30-95；`maps/training/` 目录；strategic_env.py L491-535】
- **当前战略 PPO 训练统一使用 `.vmap` 冒险地图**，位于 `maps/training/`（该目录实际 68 个 `.vmap` 文件）。
- 训练清单 `MAPS`（train_wsl2_ppo_v2.py）：`T05_adventure_36X36_01.vmap`、`T05_adventure_52X52_01.vmap`、`T05_adventure_52X52_02.vmap`、`T06_adventure_72X72_01_duel.vmap`、`T06_adventure_72X72_01.vmap`、`T06_adventure_72X72_02_duel.vmap`、`T06_adventure_72X72_02.vmap`、`King_of_Pain_h3m.vmap` 等。
- `King_of_Pain_h3m.vmap` 实为 `.vmap` 文件，仅文件名加 `_h3m` 后缀以通过 `strategic_env.py` 的 assert（要求 mapname 含 `s1`/`mini`/`adventure`/`h3m`）【来源：train_wsl2_ppo_v2.py L75 注释】。
- `strategic_env.py` 默认地图 `adventure-A1.vmap`，assert 要求 mapname 含 `adventure` 等关键字【来源：strategic_env.py L491-535】。
- **H3M 经典图（`available_maps.json` 110 张、`scan_maps.py` 扫描 `.h3m`）是另一套**——用于旧项目 ELO / 经典图评估，不是当前 PPO 训练图集。二者不可混淆。

### 2.6 Checkpoint 与对手池
【来源：train_wsl2_ppo_v2.py L656-685, L29, L107-108】
- 每 50 step：`checkpoints/wsl2_ckpt_<step>.pt`（仅权重）+ 同时存完整状态 `wsl2_model_state.pt`（含优化器+step）。
- best_vloss 时额外存 `wsl2_model.pt`（权重，供对手池/推理）。
- 保留最近 `OPPONENT_POOL_SIZE=10` 个 `wsl2_ckpt`，旧文件自动删除。

### 2.7 胜负口径
【来源：strategic_env.py L174-176, L1075-1149】
- `state.game_over == 1` → 红方（RL）胜；`== 2` → 蓝方胜。
- `winrate_eval.py` 用 `info.get("game_over",0)` 统计；`game_over=0` 记为 DRAW（不计入胜率分子）。

---

## 三、环境构建（WSL2，当前）

### 3.1 三件套同源编译
- vcmi-native 三件套（libvcmi.so + libmlclient.so + connector）必须同源编译，否则 ABI 不兼容崩溃【来源：服务器踩坑点.md 1.3】。
- 启用开关：`-DENABLE_ML=ON -DENABLE_MMAI=ON -DENABLE_NULLKILLER2_AI=ON`（历史沿用，待 WSL 侧复核）。
- 环境变量：`LD_LIBRARY_PATH=.../vcmi-native/rel/bin:~/vcmi-workspace/vcmi_gym/connectors/rel`、`STRATEGIC_STATE_LIB=.../libmlclient.so`。

### 3.2 启动训练
```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/d/Bigdata/hero3_fresh/train_loop.sh 2>&1 &
bash /mnt/d/Bigdata/hero3_fresh/check_training.sh   # 查状态
```
> 注：本机 WorkBuddy sandbox 禁用 `wsl.exe`，以上 WSL 侧命令需用户在 WSL 内执行【来源：服务器踩坑点.md 2.2】。

---

## 四、关键文件索引（路径已核实存在）

| 文件 | 用途 | 核实 |
|------|------|------|
| `py/train_wsl2_ppo_v2.py` | PPO 训练主脚本（新架构 Net） | `ls py/` ✅ |
| `py/ep_runner_one.py` | 单 episode 执行器；动作屏蔽训练真源 | `ls py/` ✅ |
| `py/winrate_eval.py` | 新模型 WIN RATE 评测器（WSL2 本地） | `ls py/` ✅ |
| `py/ckpt_push_hook.py` | checkpoint 推送链路编排 | `ls py/` ✅ |
| `py/ckpt_watcher.sh` | cron 触发器（每 15min 去重） | `ls py/` ✅ |
| `scripts/export_rl_onnx.py` | `.pt → ONNX` 导出 | `ls scripts/` ✅ |
| `scripts/results_hub.py` | 服务器 WIN RATE 结果中枢（:8088） | `ls scripts/` ✅ |
| `train_loop.sh` / `check_training.sh` | 24/7 循环 / 状态查询 | 根目录 ✅ |
| `py/scan_maps.py` | 全地图扫描（`.h3m`） | `ls py/` ✅ |
| `py/eval_elo.py` / `py/strategic_reader.py` | 旧架构评测 / ctypes 读状态（历史） | `ls py/` ✅ |
| `对齐清单_CN31.md` / `对齐清单_HD.md` | Track2 真实游戏内存偏移 | 根目录 ✅ |
| `docs/checkpoint推送链路与WINRATE自动评测.md` | 推送链路完整说明 | `ls docs/` ✅ |
| `docs/服务器踩坑点.md` | 踩坑与认知纠正合集 | 本文 §六 |

> 旧文档将 `train_wsl2_ppo_v2.py` / `ep_runner_one.py` / `scan_maps.py` 等写成根目录路径，实际均在 `py/` 下，已更正。

---

## 五、服务器 172.16.2.40（实测 2026-09-24，live）

> 登录：`ssh root@172.16.2.40`（Windows 侧免密）。所有端口局域网可达（防火墙 ACCEPT）。

### 5.0 SSH 登录凭据（2026-09-24 双通道验证）【来源：paramiko + sshpass 实测 / 服务器 /root/SSH_ACCESS_172.16.2.40.md】

**有效凭据**：`root` / `C/jw2073721`（注意中间是 20，写成 `C/jw073721` 是错版本；旧密码"度搜"已失效；`admin` 用户不存在）。

**免交互登录三种方式**（脚本/agent 用，不弹密码框）：
```bash
# ① 密钥直连（日常首选，不受 faillock 限制；本机 id_ed25519，~/.ssh/config 别名 xm-server）
ssh -o BatchMode=yes root@172.16.2.40 "command"

# ② sshpass（本机 WinGet 已装）
sshpass -p "C/jw2073721" ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@172.16.2.40 "command"

# ③ Python paramiko（纯代码，allow_agent/look_for_keys 双 False 防密钥劫持）
paramiko.SSHClient().connect("172.16.2.40", 22, "root", "C/jw2073721", allow_agent=False, look_for_keys=False)
```

**服务端 sshd 配置**（实测）：`passwordauthentication yes` / `PermitRootLogin yes` / `kbdinteractive no`。

**⚠️ 踩坑：pam_faillock 锁定（09-24 实测）**
PAM 配置 `deny=3 even_deny_root unlock_time=60`：15 分钟内密码失败 3 次 → root 密码登录锁 60 秒，**锁期内正确密码也被拒**（会误判成"密码错了"；09-24 就踩过：试错 2 次 + paramiko 错密码 1 次凑满 3 次，随后对的 `C/jw2073721` 也全拒）。
- 诊断/解锁（走密钥，不受 faillock 影响）：
  ```bash
  ssh -o BatchMode=yes root@172.16.2.40 "faillock --user root"          # 查失败计数
  ssh -o BatchMode=yes root@172.16.2.40 "faillock --user root --reset"  # 清零
  # 之后等 >60s 再试密码通道
  ```
- 纪律：批量测密码（paramiko/sshpass 循环）前先查 faillock 状态，失败 2 次即停，别凑满 3 次触发锁。

### 5.1 硬件/环境（实测）
- CPU 鲲鹏 920 ×2，64 核 2.6GHz（ARM64/aarch64）；内存 250GiB；`/DATA` 25.5T（LVM）。
- 无 GPU；torch 2.13.0+cu130 仅 CPU 运行。
- OS openEuler 24.03 LTS-SP3；Python 3.11.6（/usr/local/bin/python3）。
- 已装 torch / onnxruntime / fastapi+uvicorn / SB3 2.9.0 / docker / Xvfb :99。
- 现存 `/DATA/hero3`（旧训练项目：80+ SB3 模型 + vcmi-build/bin 全套 .so）、`/opt/vcmi`。

### 5.2 推理服务 :8080（SB3 旧架构，实测在线）

**状态**：`systemctl is-active vcmi-inference` = active；代码 `/DATA/hero3/inference_service.py`（FastAPI + uvicorn）。
【来源：curl :8080/health → `{"status":"ok","models_loaded":80,"current_model":"ppo_Key to Victory_h3m_500000steps"}`；curl :8080/openapi.json 实测路由与 schema】

**80 个历史模型可直接调**：服务启动自动扫描 `/DATA/hero3/models`（~80 SB3 zip）、`vcmi-build/bin/models`、`vcmi/` 并加载，`GET /models` 可列全部（每项含 `obs_dim:256 / n_actions:11 / type:sb3`）。无需逐个注册即可 `POST /predict`。

**最实用场景 = T13 外挂 AI 远程决策后端**：P8 阶段 Python 客户端把 obs 发到服务器、服务器回 action，把"重推理"从客户端卸载到服务器。契约（实测 openapi.json）：
- 请求 `POST /predict`：`ObservationRequest{ observations: number[][](N×256), deterministic: bool=true }`
- 响应 `PredictionResponse{ actions: int[], values?, log_probs?, inference_time_ms, model_name }`
- 另有 `POST /predict/batch` 批量推理；`GET /models` 列模型；`GET /health` 探活。

**延迟（口径必须分清，勿混用）**：
- 用户实测（P8 客户端，局域网本机）：往返 **2–12ms**，局域网完全够用【来源：用户说明，2026-09-24】。
- 本机 sandbox 实测：服务器响应 `inference_time_ms` ≈ **20–33ms**；sandbox→服务器链路 wall ≈ 73–165ms（注：sandbox 经容器 NAT 跳数，不代表 LAN 客户端真实延迟，参考价值低）。
- ⚠️ 二者量级不同步，可能源于服务器负载 / 模型轻重 / 网络跳数差异；精确数字以你 P8 客户端在局域网本机实测为准。

**checkpoint 热更新（无需重启服务）**：
- 新模型上线：`POST /models/load`（`LoadModelRequest{ name, path, model_type? }`）加载 → `POST /models/{name}/activate` 切换当前模型，**服务不重启即生效**。
- 已用于推送链路：`py/ckpt_push_hook.py` 在 WSL 训练出新 checkpoint 后 scp 到服务器并调这两个接口【来源：py/ckpt_push_hook.py L40-41,100】。
- 注意：80 基础模型为旧 256 维 SB3。**09-25 勘误**：S-7 切新 ABI 后 3464 维 ONNX 也能 `/predict`（81 模型含 `wsl2_ckpt_*`，实测正常，见 §5.12）；256/3464 按模型维度各走各的。

### 5.3 WIN RATE 结果中枢 :8088（新，实测在线）
【来源：curl http://172.16.2.40:8088/health → `{"status":"ok","count":0}`】
- 代码 `/DATA/hero3/results_hub.py`；systemd `vcmi-results`。
- 接收/查询历史 WIN RATE，落盘 `/DATA/hero3/output/winrate_history.jsonl`，进程重启自恢复。
- API：`GET /health` / `POST /results` / `GET /results` / `GET /results/latest` / `GET /results/{model}` / `GET /models`。

### 5.4 checkpoint 自动推送链路（新）
【来源：docs/checkpoint推送链路与WINRATE自动评测.md】
- 关键约束：服务器 ARM64 旧架构跑不了新模型（3464 维），WIN RATE 必须在 WSL2 算。
- 链路：WSL2 训练存档 → `ckpt_push_hook.py`（导出 ONNX → scp → `/models/load`+activate → 本地 `winrate_eval` → 结果 POST :8088 + scp 落盘）。
- 触发：`ckpt_watcher.sh`（cron 每 15min）或训练脚本尾部 hook（`Popen`，不阻塞训练）。
- 查结果：`curl http://172.16.2.40:8088/results/latest`。

### 5.5 批量评测农场并发限制（09-24 对照矩阵定谳，踩坑 #309）
- **约束**：多 worker 各开**不同地图**时，VCMI 资源缓存（进程内单例）并发读共享 `data/` 软链 → 堆越界 → `worker_crashed`（glibc abort）。同图并发无此问题。
- **崩率**：跨 12 图并发 ~42%（10/24）；同图 12 并发 0/12；串行 0/12。
- **临时对策**：并发度按"每 worker 固定同图"组织（同图可任意并发）；根治 = boot flock 串行化资源加载（S-5 待落）。
- **调试工具链**：服务器 gdb 被 `/usr/lib64/libpython3.11.so.1.0` 坏链（指向静态库）卡死 → core 分析一律 `eu-stack --core=<core>`（elfutils，不依赖 libpython）。
- **收尾纪律**：并发评测跑完必 `pgrep -f strategic_worker` 清孤儿（VCMI 线程父进程死后不退，孤儿 99% CPU 烧核 27min+）。

### 5.6 服务器定位结论
- **适合**：并行评测（64 核）、ONNX/SB3 推理、CI 构建、checkpoint 仓库（25T）。
- **不适合**：RL 训练本体（无 GPU）、新模型评测（ARM64 旧架构维度不兼容）。
- 与 WSL2 RTX3060 训练互补：训练留 WSL2，评测/推理/CI 卸载到服务器。
### 5.7 Hermes 远程网关 hermes-serve（09-24 部署）【来源：systemd hermes-serve + /api/status 实测】

**部署**：systemd 服务 `hermes-serve`，`/usr/local/bin/hermes serve --host 0.0.0.0 --port 9119`（hermes v0.19.0，HERMES_BACKEND_READY 验证通过，端口 9119 监听 0.0.0.0）。
**鉴权**（6 月加固，公网绑定强制 basic auth）：用户名 `hermes`，密码存服务器 `/root/HERMES_SERVE_PASSWORD.txt`（24 位 hex，0600；.env 同存 scrypt SECRET）。

**远程连接方式**（用户侧）：
1. 浏览器：`http://172.16.2.40:9119` → `/login` basic 表单（Username/Password）→ cookie 会话。注意 `/api/*` 无 cookie 直连 = 401 `no_cookie`（走 cookie 流不是 basic-auth 头）。
2. 桌面 App：Gateway 指向 `http://172.16.2.40:9119`，走 native PKCE（端点族 `/auth/native/status|authorize` 实测 302 正常）。

**注意**：该网关跑的是**服务器默认 profile**（deepseek provider + feishu env，另有 hero3 profile），非本机 h3_32b_homm3 那套。feishu 平台 gateway 报 retrying（lark `extra_ua_tags` 版本错），不影响 serve。

**模型 API key 同步（09-24）**：已把本机 h3_32b_homm3 profile 的 4 个 provider（agnes / agnes-pro / apihub-5 / sensenova）+ 5 个 .env key 合并进服务器 `/root/.hermes/config.yaml`，**服务器默认模型改为本机同款 `apihub-5/agnes-3.0-flash`**。
- 服务器侧实测：agnes / agnes-pro / apihub-5 / sensenova 四个 key **全通**；`XIAOMI_API_KEY`（401）与 `OPENROUTER_API_KEY`（403）对服务器 IP 失效——这两家要么 IP 白名单要么 key 过期，服务器上暂不可用。
- 备份：`/root/.hermes/{config.yaml,.env}.bak_modelsync_0924`。
### 5.8 hermes 升级 0.19.0→0.21.5 + 本地连接复测（09-24）【来源：官方安装器 + 实测】

**升级**：pip 0.19.0（已不支持继续更新）→ 官方安装器 FHS 布局（git 源码 v0.21.5, upstream aa8a33d, 装到 `/usr/local/lib/hermes-agent/venv`）。服务器直连 github 不通 → 全局 git insteadOf 走 `gh-proxy.com` 克隆。pip 旧入口备份到 `/usr/local/bin/legacy_pip_hermes/hermes_pip019`（回退=重链回去 + `pip install hermes-agent==0.19.0`）。`/usr/local/bin/hermes` 现为软链 → 新 venv。
- npm 步（浏览器工具）失败 exit1，不影响服务器用途（已 --skip-browser）。
- 升级后 `~/.hermes` 数据不动，同步的 4 个 provider key + 默认 `apihub-5/agnes-3.0-flash` 全保留（config_version 33→46 是 hermes 内部 schema，自动迁移，不影响）。

**本地连接复测（升级后，全通）**：本机 Windows → `http://172.16.2.40:9119`，`/api/status` 200（v0.21.5），`POST /auth/password-login`（basic 表单 username=hermes）200 拿 cookie，带 cookie 访问 `/api/auth/me` 200。桌面 App 走 native PKCE。
**⚠️ 09-24 23:35 修正：0.21.5 里 `hermes serve` 是 headless-only（浏览器 UI 被禁，报 "web UI disabled — use hermes dashboard"）**。已切回：
- web UI 需手动构建：`cd /usr/local/lib/hermes-agent/web && npm install --registry=https://registry.npmmirror.com && npm run build`（产物 `hermes_cli/web_dist/`，安装器 npm 步失败过所以 dist 为空）。
- systemd `hermes-serve` unit 改为 `ExecStart=/usr/local/bin/hermes dashboard --host 0.0.0.0 --port 9119 --skip-build --no-open`（服务名沿用 hermes-serve，HERMES_DASHBOARD_READY 验证）。
- 验证：未登录 /skills → 302 跳 /login?next=/skills，登录后 200。
**运维**：`systemctl {status|restart|stop} hermes-serve`；改鉴权改 `~/.hermes/.env` 的 `HERMES_DASHBOARD_BASIC_AUTH_*` 后 restart；`hermes serve --stop` 可停全部 web server。

### 5.9 服务器评测管线重建事故与修复（09-24 深夜→09-25 凌晨，09-25 凌晨修复完成）

**事故链**（三层错配叠加，全部源于 09-24 晚 WSL 源码同步）：
1. **WSL 同步误覆盖 ML/**：`git archive 8679dd35`（WSL 产线，`init_vcmi(void*)` 新 ABI + CRLF）解压时把服务器 `ML/` 也覆盖了（声称排除但 mtime 11:06 证明没排掉）。服务器 `connector_v13.so`（7/25 ARM 预编译）要旧 ABI `init_vcmi(IModel*,IModel*,const InitArgs&)` → dlopen `undefined symbol`。
2. **vcmi-build 是 WSL 拷来的毒构建树**：CMakeCache/link.txt/build.make 残留 `/usr/lib/x86_64-linux-gnu/libpython3.12.so` 假前置 + `-lpython3.12`（ARM 无 3.12 只有 3.11）→ make 报"没有规则可制作目标"。修复=删 build.make 假前置行 + link.txt 去 `-lpython3.12` + 清 .o 重编。
3. **符号冲突**：WSL 版 `server/strategic_state.cpp` 混入 `libvcmiservercommon.a`，与恢复的服务器版 ML 重复定义（`g_strategic_state`/`adventure_*` 8 组）→ `ar d bin/libvcmiservercommon.a strategic_state.cpp.o`（7/25 原版 .a 无此成员）。
4. **两个跨 .so 未定义符号**：`g_adventure_allied_ai[64]`（WSL 08-01 在 Client.cpp extern、ML 侧定义；服务器 7/25 版早于该改动没定义）→ MLClient.cpp 补 `extern "C" char g_adventure_allied_ai[64]={0}`；`headlessQuit`（clientapp/EntryPoint.cpp 的 static，共享库链路无人定义）→ MLClient.cpp 改局部 `static std::atomic<bool>`。
5. **导出缺失**：新编 libvcmi.so 缺一批 VCMI vtable/常量（WSL 新源码引用 7/25 源码没有的符号）→ libvcmi.so 链接行加 `-rdynamic`（538M，全符号导出）。

**最终产物**（23:59 版）：libvcmi.so（-rdynamic，538M）+ libmlclient.so（旧 ABI + `strategic_state_force_game_over` + `g_adventure_allied_ai` + 本地 `headlessQuit`，312M）+ libmlserverplugin.so（12:07 版，U logStats 由新 libvcmi.so 满足）。**RTLD_NOW 全链 4 库 dlopen 全绿**（libvcmi→mlserverplugin→mlclient→connector_v13，LD_BIND_NOW=1 实测）。

**验收**：单局 0-error 跑通（KTV 441s/局 DRAW）；KTV 4 局 4/4 DRAW 0 error（占城图模型打不死，已知 gap）；**147 图全池横评 09-25 03:11 启动**（147×16 局，32 worker，结果 `/DATA/hero3/output/batch_pool_*.json`，逐图 W/L/DR 矩阵）。

**纪律**：① WSL 归档同步服务器**必须先校验 ML/ 是否被覆盖**（比对 ABI 符号，不能只看"排除了"）；② 服务器评测链路的权威构建树 = 与 connector ABI 匹配的 7/25 服务器产线，WSL 同步只能覆盖**非 ML 的 989 fork 文件**；③ 每次 `timeout` 杀 batch 主进程必 `pkill -9 -f strategic_worker.py` 清孤儿（孤儿 250% CPU 抢 64 核，load 顶到 25+，新批次吞吐腰斩）。

### 5.10 全池横评运行特征与进度判读（09-25 04:30 实测）

`batch_eval_pool.py`（pid 426288，03:11 启动，147 图 × 16 局，32 worker）的进度判读要点（**避免误判卡死**）：

1. **调度结构**：`ProcessPoolExecutor(32)`，每张图的 **16 局在同一 worker 进程内串行**（`for g in range(16)`），32 个 worker 并行吃 32 张图 → 完成 32 张后才进下一批（4.6 波）。
2. **日志静默 ≠ 卡死**：`[map] W=n/16 WR=x%` 行**只在整图 16 局全部跑完才打印**（Python 输出缓冲 + 结构决定）。04:30 实查 75 分钟仅 1 行（`Adventures of Jared Haret` 373s early-quit），但 32 个 worker 各自在磨自己的 16 局（最老 worker etime 1151s+ 仍未完成该局）。**判活方法** = `ps -eo pid,pcpu,etimes,args | grep strategic_worker`（worker 在推进 CPU/etime）+ `cat /proc/loadavg`，不是 grep log。
3. **耗时双峰分布**：快图 early-quit ~373s/16 局（23s/局）；慢图打到 5000 天 cap 每局 2625s → 单图 16 局 ≈ 42000s（11.7h）。147 图耗时分布未出矩阵前未知。
4. **ETA 修正**：S-6 原估"5h（~08:00）出矩阵"**作废**（按快图均值外推的错）。真实区间 = 全快图 ~1h → 全慢图 4.6 波 × 42000s ≈ 54h，实际取决于地图耗时分布（147 张里慢图占比才是决定项）。**首波 32 张图 16 局跑完（即 log 出现 32 行 `[map]`）才是有效 ETA 锚点**——届时 `32/147 ≈ 7 波剩余`，单波实测时长 × 7 即 ETA。
5. **负载特征**：每 strategic_worker 占 175–452% CPU（多核），33 进程满载时 load ≈ 350+（64 核机 5.5×）。这是 vcmi 引擎多核 TBB 行为，非异常；**但 load 高 + 跨图并发 = S-5（#309 堆损坏）风险面**——目前 err=0，若出批崩 → 降并发 32→16 或降 games/map 16→4。
6. **结果落盘**：JSON `batch_pool_*.json` 只在**整批全部跑完**才写（中途只有 log）；中途要看逐图结果只能等。

### 5.11 新项目评测链路部署（新 ABI 3464，09-25 上午冒烟通过）

**背景**：用户拍板"不要旧项目内容" → 服务器 ABI 从 7/25 旧链路（256 维 v13 SB3 k2v 模型 + StupidAI + 5000 天 cap）**正式切换到 WSL 新 ABI（3464 维 torch PPO）**。这是 09-24"误覆盖"事故的正向故意版。

**做了什么**（全部有回滚垫）：
1. **ML/ 源切换**：`/DATA/hero3/vcmi/ML/` = WSL `8679dd35a1` 新 ABI 4 文件（`init_vcmi(void*)`=`_ZN2ML9init_vcmiEPv`，9×force_game_over + try_wait + g_adventure_allied_ai/headlessQuit 内置定义，旧 ABI 的三连符号修复不需要了）→ 重编 ARM `libmlclient.so`（312M，05:00）。
2. **Python 侧新树**：`vcmi_gym_new/`（3464 M6，含 connector 源 v13/14/15 新 ABI）+ `ep_runner_one.py`（torch 驱动）+ `wsl2_model.pt`（2.9MB，09-25 04:15 WSL 训练档）+ `strategic_reader.py`（ctypes 结构体，在 hero3_fresh/py/ 不在包里，易漏）→ 全落 `/DATA/hero3/`。
3. **重编 ARM pybind connector**：`connector_v13/14/15.so`（各 ~266KB，`/tmp/abi_smoke/conn_build`，CMake `-DVCMI_DIR=/DATA/hero3/vcmi`；**CMakeLists 找 libmlclient 走 `${VCMI_DIR}/rel/bin` → 需 symlink `vcmi/rel/bin/libmlclient.so → vcmi-build/bin/libmlclient.so`**）。v13/14/15 全编（包 `__init__` 急加载 v14/v15 战斗层，缺一个 import 就炸）。
4. **包 import 守卫**：`vcmi_gym_new` 两级 `__init__.py` 战斗层 import 改 try/except guard（备份 `.bak_battle`），见踩坑 §2.11。

**冒烟定论（09-25 05:25，决策点达成）**：KTV 单图单局 `ep_runner_one.py` → `[ML-fix] force game_over winner=1` + `[EP_TIME] steps=40 secs=15 r=-265.5 err=no` + traj 1.78MB。**新 ABI 3464 评测链路在 ARM 服务器跑通**。

**运行环境（照抄即可复现）**：
```
cd /DATA/hero3/vcmi-build/bin   # 引擎 data/Mods 软链在这, ML::start_vcmi 按 CWD 找资源
env PYTHONPATH=/DATA/hero3/vcmi_gym_new:/DATA/hero3 \
    STRATEGIC_STATE_LIB=/DATA/hero3/vcmi-build/bin/libmlclient.so \
    LD_LIBRARY_PATH=/DATA/hero3/vcmi-build/bin DISPLAY=:99 \
    python3 -P /DATA/hero3/ep_runner_one.py 250 <traj.json> "Knee Deep in the Dead.h3m" \
    --model /DATA/hero3/wsl2_model.pt --blue_ai MMAI_RANDOM --blue_adventure_ai MMAI
```
`python3 -P` 必须加：防 CWD/PATH 上旧 `vcmi_gym`（256 维）影子顶上新树。

**退役面**：老 SB3 256 维评测链（`strategic_worker.py`/`subproc_env.py`/`batch_eval_pool.py` + `ppo_k2v_*` 模型）ABI 不兼容，事实退役（备份在，可回滚）。**`vcmi-inference` :8080 不受影响**（ONNX 推理，不加载 libmlclient，09-25 实查 active + 81 模型 + 当前 `wsl2_ckpt_1003121`，S-4 checkpoint 推送链路照常）。

**回滚垫**：`/DATA/hero3/quarantine_old_256/rollback/`（**06:25 已从 /tmp 持久化到 /DATA**——/tmp 重启即丢，旧 ABI 三件套 .so 当时是唯一可用二进制）：`abi_switch_backup_0925`（旧 ABI libmlclient/libmlserverplugin/libvcmi + 修复版旧 ML 源 md5 `d21f…`）+ `abi_smoke`（`ML_oldabi_0925` 旧 ABI ML 源 + 本次新 ABI 4 源）+ `final_pre0924` + `ML_server_backup_0924`（7/25 纯原始旧 ML 源 md5 `6260…`，与修复版**不同份**，勿合并）。回滚 = cp 回 .so + ML/ 源 + 换回 quarantine 里的老 SB3 脚本。

**铺开执行（09-25 06:05 启动，pid 700518）**：`batch_eval_newabi_pool.py`（新链 harness：`/DATA/hero3/batch_eval_newabi_pool.py`，N worker 各领连续图块、块内顺序跑，子进程显式注入 6 个 env，结果逐图行 + 聚合 JSON）：`wsl2_model.pt` × 147 全池 × 每图 4 局 × 32 worker = 588 局，蓝方 `MMAI_RANDOM`，250 步 cap。开跑前 4 项工程补齐：① ep_runner 注入 `traj["game_over"]` 字段（C++ 权威终局 1=WIN/≥2=LOSS/0=DRAW，之前 traj 无胜负字段）；② S-5 flock 移植新树（`.bak_s5flock_new`）；③ ep_runner WSL 硬编码路径修（L3 `setdefault` + `VCMI_WORKSPACE_DIR=/root/vcmi-workspace`，踩坑 §2.14 坑 1）；④ 2 图 × 2 局 mini 验证（`DONE maps=2 W=0 DRAW=4 err=0`）通过才铺全池。
**吞吐实测**：flock boot 串行（S-5 根治代价，每局新进程 ~15s boot）→ 全局 ~4 局/分钟 → 588 局 ETA ≈ 2.5h（~08:30 出矩阵）。06:22 实查 48/588 局、0 真 ERROR、首批 2 图出行全 `W=0/4 DRAW=4`。**判读注意**：① 大批 `[MUTEX]/[THREAD]` + `Cannot answer the query -1`（#296 无害刷屏）占 log 90%，判 error 看 `^\[map \]` 行 err 字段不是裸 grep ERROR；② 部分局 40 步被 `ENDTURN_FUSE`（act10×30）截断记 go=0/DRAW → 出矩阵后须分「fuse 截断 vs 真走满 250 步」再判 DRAW 真伪；③ 蓝方 MMAI_RANDOM 是弱基线（NK2 OOM 已弃），WR 不代表 vs 真实玩家/NK2 水平。
结果落 `/DATA/hero3/output/batch_newabi_pool_*.json`（聚合 + 逐图逐局）+ `newabi_pool_full.log`；watcher 完成写 `/tmp/pool_done.flag`。

### 5.12 vcmi-inference :8080 crash-loop 修死（09-25，踩坑 §2.12）
【来源：journalctl + ss -tlnp + /predict 实测】
- 双根因：① 09-23/24 遗留老进程（`inference_service.py`）带外占 :8080 → systemd 新进程 bind 失败循环崩（NRestarts 1538）；② `vcmi/defender-ipnkyfqb-best3.onnx` 38 字节坏文件（拷错占位文本）→ onnxruntime 加载崩。
- 修复：stop → kill 老残留 → 坏 onnx 隔离 `/DATA/hero3/quarantine/` → start。判活 = MainPID 70s 不变 + journal 近 70s 零 FAILURE。实测 `/predict` 3464 维 obs 正常出 action。
- **勘误（09-25）**：§5.2 旧注「新 3464 维 ONNX 能 load 但 predict 维度不兼容」**已过时**——S-7 切新 ABI 后，81 模型里 `wsl2_ckpt_*` 是 3464 维，`/predict` 喂 3464 obs 正常（09-25 实测）；256 维旧 SB3 模型仍按旧产线口径走。

### 5.13 ARM64 CI 常态化（09-25，CI-ARM64 全绿）
【来源：GitHub API runs + runner `_diag` worker log 实测】
- `c18593395/vcmi-strategic-gym` 仓 `.github/workflows/arm64-ci.yml`（workflow 366453367）：`runs-on: [self-hosted, Linux, ARM64]`，kunpeng-920 runner 接单，做 lint（compileall）+ engine-free unittest（numpy/gymnasium）。
- 修 2 坑后全绿（run 36063423686）：① checkout rc=128 = git dubious ownership → runner 侧 `safe.directory=*`；② setup-python 无 3.11-arm64 预构建 → 砍掉，直接吃系统 `python3.11`（numpy 2.4.6/gymnasium 已预装）。
- 推仓路径：ghp token 只读 → **Windows 侧 SSH 推**（WSL 当日出站 22/443 全断，绕不过）。
- 价值：GitHub hosted（x86）CI 测不到 aarch64 面；ARM64 CI 守构建/运行面，64 核无排队。

---

### 5.14 ABI 守门 CI + 双 CLI 协作模式（09-25，C1 ABI 守门全绿 run 36067279374）
【来源：GitHub API runs/jobs + runner `_diag` 日志 + dry-run 实测】
- **ABI 守门 job**（`arm64-ci.yml` 第 2 job `abi-gate-arm64`，仓 commit 1694cb2）：仓内 `connectors/v13/14/15` 源码（28 文件，md5 与服务器树同源）对着**服务器活引擎 ABI**（`/DATA/hero3/vcmi` 头 + `rel/bin/libmlclient.so`）全量重编 6 个 pybind 模块 + `RTLD_NOW` dlopen 验链接。守的正是 09-24 毒化事故砸的那一层（WSL 源同步覆盖 ML/ → 桥接层 dlopen 炸），引擎本体 6000 文件重编**不入 CI**（留手动，全量几十分钟不值得每 push 跑）。
- **self-hosted 价值实证**：job 能直接读本机 `/DATA/hero3/vcmi*`（GitHub hosted x86 做不了，也读不到 ARM 引擎）；前置 = Boost 1.83/cmake 3.27/GCC 12/python3.11 dev + `rel/bin/libmlclient.so` 软链（→`vcmi-build/bin`）；`pybind11 v2.13.6` 仓内未 git 跟踪（CMakeLists L15 `add_subdirectory`）→ CI 在线取 codeload。dry-run（configure→build 6→dlopen 6/6）全绿后才固化进 workflow。
- **双 CLI 协作模式**（本机两个 hermes CLI 会话同项目）：分工 = 引擎侧（.so/评测链/全池铺开，对方维护 `/DATA/hero3/当前任务清单.md`）vs 外围侧（CI 守门/模型仓 :8080/验证验收/D 盘知识库+git，本侧维护 `docs/当前任务清单.md`）；协调介质 = 共享文件 + `pgrep`，动手前三查（谁在跑/读对方清单尾部/git status 脏文件），重活错峰（池 32 worker 在跑时本侧编译 `-j16` 不起第二批），踩坑编号两边共享（§2.14 同源）。详见 踩坑 §2.15。
## 七、外部 VCMI 生态与网络协议参考

### 7.1 T13 外挂 AI 参考源 = VCMI 官方网络协议栈（非第三方项目）
【来源：vcmi.eu/developers/Networking；DeepWiki vcmi/vcmi 2.1 Client-Server Communication / 8. Multiplayer and Networking；GitHub 实测 openapi 不含网络 client】
- 总任务 T13 架构里"外挂 AI 实现 VCMI 客户端协议（`lib/network/` + `lib/networkPacks/`），与 vcmiclient 同地位接入 NetworkServer"——这套就是 **VCMI 内建的联机架构本身**，参考实现全部在 VCMI 官方引擎本体 `vcmi/vcmi`，**不是**从某个 fork/第三方 AI 仓库抄的：
  - 网络协议/连接管理：`lib/network/`（官方文档原文 "implementation details see files located at lib/network directory"）
  - 数据包三继承线：`lib/networkPacks/` —— `CPackForClient`(server→client 状态同步) / `CPackForServer`(client→server 玩家指令，自带 player 身份 + requestID) / `CPackForLobby`(大厅)
  - NetworkServer：`server/CVCMIServer.cpp` + `server/CGameHandler.cpp`（authoritative 状态，接收各 client 连接与动作包）
  - "与 vcmiclient 同地位"参照实现：`client/CServerHandler.cpp`(连接管理) + `client/NetPacksClient.cpp`(收包处理) + `client/Client.cpp`
- **可行性根因**：VCMI 是 authoritative server 架构，Server 只认协议、不区分 client 来源（人类 vcmiclient / AI client / 外挂程序都是"一个连进来的 client"）→ T13「不改 VCMI 一行代码、外部 AI 接入」成立。
- **关键认知纠正**：VCMI 内置 AI（VCAI / Nullkiller / StupidAI）是 **进程内** 实现，通过 `cb`（CPlayerSpecificCallback / Gateway）直接跟 server 交互，是 server 侧逻辑的一部分，**不是独立网络 client**；GitHub 生态里**没有现成"纯 TCP 网络 AI client"第三方项目**可抄。T13 要自研网络 client，做法是复用 `client/` 的序列化/收包逻辑，把"渲染+输入"层换成"AI 决策"（见踩坑点 3.5）。
- 与现有训练路径区别：当前 `strategic_env` 走**进程内 ctypes 直读/写 VCMI C++ 内存（Connector）**（改了引擎+加桥接 .so）；T13 走 **TCP 网络协议，标准 client，不改 VCMI**。两路可并存，且 S-2 场景（P8 客户端发 obs 到 :8080 拿 action）正是 T13 外挂 client 的远程决策后端（详见 §5.2）。

### 7.2 VCMI GitHub 生态对本项目可用性
【来源：GitHub 检索 2026-09-24；用户账户 `c18593395`（本项目仓库 `vcmi-strategic-gym`），upstream = `smanolloff/vcmi-gym`】
| 项目 | 用途 | 对本项目 |
|---|---|---|
| `vcmi/vcmi` | VCMI 官方引擎本体（C++17/SDL2/Qt），含 H3M↔VMAP 地图系统、launcher、editor、MMAI 战斗 AI（v1.7.0+ 官方集成） | ✅ 沙盒底层引擎；关注地图加载 / adventure AI（VCAI/StupidAI）/ battle AI 更新 |
| `smanolloff/vcmi-gym` | 本项目 git upstream（你 `c18593395/vcmi-strategic-gym` 从其 fork 改造）；做 **MMAI 战斗 AI**（combat AI），含 gym/Connector/RL 编排/W&B | ⚠️ 方向不同：它优化「战斗」，本项目做「冒险地图战略自对弈」；其 gym/Connector/RL 基础设施可借鉴，训练目标代码不照搬 |
| `eamag/vcmi-gym` | smanolloff 另一 fork，同样 MMAI 战斗 AI | 同上同类参考 |
| `vcmi-mods/chinese-maps-collection` | 中文地图/战役合集（H3M） | ✅ 可经 VCMI 转 VMAP 扩充 `maps/training/` 训练池（你已有转换能力） |
| `vcmi-mods/custom-templates` | 玩家自定义 RMG 随机地图模板 | ✅ 程序化生成更多训练地图，配合 VCMI RMG |
| `vcmi-mods/horn-of-the-abyss` / `wake-of-gods` | HotA / WoG 扩展（新城镇/生物/法术） | ⚠️ 慎引：复杂度飙升，除非训练需要，否则先别加 |
| `vcmi-mods/vcmi-extras` | VCMI 运行必需额外文件（字体/音效） | ✅ headless 沙盒若缺资源可能启动异常，对照补齐 |
| `potmdehex/homm3tools` | H3M 二进制解析/版本间（RoE↔AB↔SoD↔WoG）转换 | ⚠️ 部分有用：H3M 预处理/校验，不碰 VMAP（见 7.3） |

### 7.3 H3M↔VMAP 转换：翻译机是 VCMI，homm3tools 替代不了
【来源：homm3tools README 子项目清单 + HeroesCommunity 讨论（potmdehex 逆向 RoE/AB/SoD .h3m）；DeepWiki vcmi/vcmi Map System】
- **VMAP = VCMI 原生 JSON 格式**：ZIP 内含 `header.json` + `surface_terrain.json` / `underground_terrain.json` + `objects.json`，与原版 H3M 二进制布局完全不同（两套独立编码）。
- **唯一权威转换器 = VCMI 本身**：`lib/mapping` 的 `CMapService` 按文件签名自动识别 H3M 或 VMAP，分别用 `CMapLoaderH3M` / `CMapLoaderJson` 加载；VCMI 地图编辑器可载入 H3M 另存 VMAP。
- **homm3tools 能做的**：`h3mlib` 等库解析 H3M 二进制；`h3mconvert` 做 H3M **版本间**转换（不是格式间）；地图编辑/资源提取/HD Edition 模组。其 README **零处提及 VCMI / vmap**。
- **h3msnake 链接 404**：你曾给的 `potmdehex/h3msnake` 实测 GitHub 404 不存在；potmdehex 名下与 "snake" 相关的只是 `homm3tools` 里的 `h3complete_snakeloader`（H3Complete 游戏 loader），非独立转换工具（见踩坑点 1.10）。
- 结论：H3M→VMAP / VMAP→H3M 只能用 VCMI；homm3tools 仅作转换流水线的"预处理/校验"环节（如先用 `h3mconvert` 归一 H3M 版本再交 VCMI 转 VMAP，减少导入脏数据）。

---

## 八、关联文档
- **踩坑与认知纠正**：`docs/服务器踩坑点.md`（每条含根因/修复/来源）。
- **任务体系**：`总任务.md` / `当前任务清单.md`（§三-B S-1~S-4）/ `已完成任务.md`（09-24 归档）。
- **推送链路细节**：`docs/checkpoint推送链路与WINRATE自动评测.md`。
- **Track2 真实游戏对齐**：`对齐清单_CN31.md` / `对齐清单_HD.md`。
