# 训练踩坑记录

## 2026-07-29 11:32 — 模型塌缩 (ep_steps=1 r=0.00 × 146+ 次连续)

- **检测来源**: 定时监控脚本 (20min cron job)
- **现象**: 连续 146+ 个 episode 全部 ep_steps=1, r=0.00, act=[10], avg_r=0.0
- **最后状态**: 训练进程已退出 (无 Python 训练进程运行), train_loop.sh 也未运行
- **日志位置**: `D:\Bigdata\hero3_fresh\train_loop.log` 末尾行 (约 line 9346)
- **最后保存**: step=389, 已保存 STATE_PATH 和 MODEL_PATH (见 train_loop.log 最后几行)
- **建议操作**: 
  1. 手动重置模型权重
  2. 检查 WSL2 中 train_loop.sh 为何未自动重启
  3. 考虑降低 LR (当前 5e-5) 或调整 GAE_LAMBDA (当前 0.90)
  4. 恢复: 启动 `wsl ~/vcmi-workspace/train_loop.sh` (在 WSL2 中)

## 2026-08-29 — ML 目录双机分叉对账同步（fill_terrain_grid 缺失修复）

- **发现**: Windows `vcmi/ML/strategic_state.cpp` 停在 08-16（无 fill_terrain_grid），WSL `vcmi-native/ML` 停在 08-23。若从 Windows 源码重编，CNN 地形链路静默丢失（旧代码可编译，不报错）。
- **对账结论**:
  - `strategic_state.cpp/.h` = WSL 独有演进（纯新增 Phase I.4，win 侧 4 行差异为旧写法已被 generation 防陈旧版取代）→ 直拷 WSL→win
  - `MLClient.cpp` = win 独有 `f.string()` 可移植修法（validateFile 形参 std::string，boost path 在 Linux 隐式转换、Windows 编不过）→ 回写 WSL
  - `main.cpp` = **两条不同产线**（win=fork1.8 战斗层 agent-v15/random-armies/tuple 返回；wsl=战略层 agent-v13/random-stack-chance/InitArgs 返回）→ **永不互相覆盖**
  - `fill_next_dir_patch.cpp` = 遗留补丁文件，内容已并入 WSL 版 strategic_state.cpp:252,749 → 删除（备份留存）
  - `CMakeLists.txt` / `user_agents/*` 内容一致（仅 MLClient.cpp 行尾 win=CRLF/wsl=LF，无影响）
- **已执行**: strategic_state.cpp/.h 直拷到 win；MLClient.cpp .string() 回写 WSL；双侧 cmp 验证 SAME
- **规约**: 双目录同步不是全目录 cp——逐文件定方向：strategic_state.* 单向 WSL→win；MLClient.cpp 先 diff 再合并；main.cpp 同步时永远跳过
- **备份**: `backups/ML_sync_20260829/{ML_win_pre, ML_wsl_pre}` + WSL 侧 `vcmi-native/backups_ML_sync_20260829/`
- **警告**: 按 08-26 教训，工作 .so 是 backup-t03-target-20260825_233554/ 版本，08-23 源码重编即崩（start_vcmi 未调→GAME null）；本次同步只改源码不动编译产物，**重编前先解决崩溃根因**

## 2026-08-29 (Task7) — 真实游戏战略层联调: DLL 端 obs 对齐 08-23 语义 + RL 模型部署

- **做了什么**: fork 1.8 ModelAI.dll 从"BC 模型+3464 旧 obs"升级到"RL 双分支模型+3464+terrain CNN 完整 obs"。
- **缺口根因**: DLL 端 obs_fill.cpp 是 08-18 移植版, 缺 08-23 新增的三块: fill_terrain_grid (Phase I.4)、fill_target_list (H.8)、fill_next_dir (I.2, 写 reserved[0..7]); 且 obs_build.cpp 展平时 reserved 段直接跳过 (`idx += RESERVED_SIZE`, 注释"已 memset 为 0"), 训练端却是真实拷贝 → 双端 obs 分布结构性不一致。
- **修复 (4 文件)**:
  1. `strategic_state.h`: 用 08-29 对账同步版 (含 terrain_grid[1764])
  2. `obs_fill.cpp`: 逐位复刻移植 3 个 fill 函数 (C0=terrain 枚举/C1=blocked/C2=物体类型 10 级优先级/C3=全知; target_list 按 dist 排序 8 槽; next_dir BFS 8 向首步)
  3. `model_infer.cpp`: onnx 双输入 obs[1,3464]+terrain[1,4,21,21], uint8 HWC→float CHW/255 (与训练端 strategic_env._build_terrain_grid 完全一致)
  4. `obs_build.cpp`: reserved 段改真实拷贝
  5. `model_ai.cpp`: 模型路径 env 可覆盖 (MODELAI_MODEL, 默认 rl_model_v3464t_0829.onnx) + MODELAI_DUMP_OBS dump 钩子 (stderr 输出 obs+terrain 供分布核对)
- **模型导出**: `scripts/export_rl_onnx.py` (新) — checkpoint 裸 state_dict→ONNX 双输入, strict 加载, torch/onnxruntime 对拍 logits maxdiff=1.9e-06; WSL 跑 (dynamo=False)。
- **验证 (forktest66, A Warm 2 人图, 2×ModelAI)**: 64 INFER / 244 EndTurn / 0 Disaster / 0 fishy / MASK 43 正常工作; obs 14 逻辑段 DLL vs WSL 分布对照全 OK (reserved 11.2% nz 两侧一致)。
- **新坑**:
  - 用户 settings.json 的 ai 节点会丢 (被重置/覆盖), 丢→回落 Nullkiller2→bin/AI 无该 DLL→LoadLibrary 126→启动即 Disaster; settings.json 现有 .bak_0829 备份, ai 节点 5 字段已补回
  - obs_build.cpp 是 LF 行尾 (obs_fill/model_ai 是 CRLF) — 多行锚点替换前必须逐文件查行尾
  - patch 脚本多对替换中途失败会丢前面已改内存未写盘的项 — 每对替换成功后立即写盘, 或失败时从头重放
  - train 端 raw tg 是"未归一化但 /255 后进 CNN"; DLL 端 uint8 直读→推理时 /255, 数值语义已核对一致
  - towns.recruit_mask_hi 等位掩码字段值可达 2^24 量级 (25,469,088) — 不是脏数据
- **未竟**: 战斗未触发 (A Warm 双 AI 5 分钟未接战); 1.7.5 官方版未重编 (MSVC 版需同步本次 4 文件改动); MOVE_TO(act24) DLL 端执行器未实现 (模型可输出但会 fallback)。
- **判据提醒**: 战斗链验证看 `11BattleEnded` + 战后 EndTurn 持续增长 (P44/P46 教训)。
