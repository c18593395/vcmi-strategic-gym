# T14.3 T14Blue 蓝方战略层 C++ 插件 — 09-26 关机 checkpoint

> 状态 = **引擎侧改动已全落 + 本地 commit（WSL 仓），未编译、未冒烟**。明早一键续跑。

## 已落地（WSL 仓 vcmi-native，mmai-ml-wsl，本地不推）
commit `4a1675c6a2`：
- **`AI/T14BlueMod/` 6 文件**（OBJECT 库，NK2 同款形态）：
  - `T14BlueGateway.h/.cpp` — 薄派生 `NK2AI::AIGateway`，构造换 `nullkiller` 为 `T14Nullkiller`
  - `T14Nullkiller.h/.cpp` — **R13 收割 nudge**（覆盖 `onPursueTurn`）：
    原型 FNV1a(mapname#difficulty#playerID) → P0龟20天/P1速攻7天/P2游击不推进/P3飞取最远主城/P4均衡最近；aggression=(hash>>3)%4 越早出门；
    每回合 re-assert「最强未锁英雄 → 敌城」（`setActive`+`setTargetObject`+`objectClusterizer->reset()`，与 ExecuteHeroChain 同款三调用）；`[T14-ACT]` 打点支撑 T14.4 可复现判据
  - `CMakeLists.txt` — OBJECT 库，门控 `ENABLE_T14_BLUE`
  - `StdInc.h` — PCH 根（照 NK2 同款）
- **引擎侧 7 处门控编辑**（全 `#ifdef ENABLE_T14_BLUE` / `if(ENABLE_T14_BLUE)`，option **默认 OFF = 存量构建字节零 diff**）：
  1. `CMakeLists.txt:59` option + `:367` add_definitions
  2. `AI/CMakeLists.txt:25` add_subdirectory 门控
  3. `libFacade/CMakeLists.txt:55` target_link_libraries(vcmi PRIVATE T14BlueMod) 门控
  4. `lib/callback/CDynLibHandler.cpp` — STATIC_AI createAny 特化加 `stem=="libT14Blue"` 分支（**本 fork 蓝方实际加载点**，headless onlyai 走这里）
  5. `lib/callback/AIFactory.cpp` — createAdventureAI + isAvailableAdventureAI 名字分支
  6. `AI/Nullkiller2/Engine/Nullkiller.h:131` — **`virtual void onPursueTurn() {}` 空钩子**（NK2 默认空实现 = 零行为变化）
  7. `AI/Nullkiller2/Engine/Nullkiller.cpp:549` — makeTurn defender 预留后调用钩子
- 备份全在：各引擎文件 `.bak_t14blue`（7 份）

## 架构定谳（fact #64，八轮探针坐实，修正设计文档 §1.2「dlopen 待验点」）
本 fork = **STATIC_AI 构建**（`CDynLibHandler.cpp:10 #define STATIC_AI`，NK2 符号 2371 条 T 全在 libvcmi.so，无独立 AI 插件 .so、无 dlopen 通路）→ 蓝方评测链路 = `client/Client.cpp initPlayerInterfaces → CDynLibHandler::getNewAI(blue_adventure_ai) → createAny 特化按 stem 匹配`。设计文档原假设的 MMAI dlopen 直通在此 fork 不成立，接入点改 CDynLibHandler/AIFactory 门控分支。

## 边界（诚实登记，T14.3b 另排）
- R13 当前 = **nudge 非硬目标注入**：targetObject 被 NK2 每 pass `updateState()` 重置，钩子 re-set 影响主 decompose 窗 + blocking-dialog 判定（AIGateway.cpp:597）；真·硬目标注入需 decompose 私有面
- `Settings` 字段全 private 无 setter → R10-R16 权重轴本版做不了
- R17/R6 = 继承 NK2 现成（`selectBestSkillIndex` / `combatEnemyAI` 路由），零覆盖

## 明早续跑（10 分钟）
```bash
cd /home/administrator/vcmi-native/rel
cmake -DENABLE_T14_BLUE=ON .        # 重新配置（新增 option，需 reconfig）
make -j8 vcmi vcmiserver vcmiclient mlclient   # 引擎窗口受控重编（NK2 同款路径）
# 首验（踩坑纪律）: 存量构建门控 OFF 重编一次 vcmi 确认零 diff 不炸, 再 ON 编 T14BlueMod
# 冒烟: KTV 单局 N=4 蓝方=T14Blue (strategic_env blue_adventure_ai="T14Blue"),
#       判据 = [T14-ACT] 打点出现 + 不崩 + go 字段出现自然终局 (对照 NK2 基线)
```
回退：`python3 /mnt/d/Bigdata/hero3_fresh/py/patch_t14blue_engine.py --rollback`（引擎 7 文件还原 .bak_t14blue，T14BlueMod 目录可留）

## 待办余量
- 编译 + KTV/108_02 冒烟（引擎窗口，~2h 含受控重编）
- 评测侧 `strategic_env` 加 `blue_adventure_ai="T14Blue"` 入口（T14.4 合批，服务器通了跑矩阵）
- T14.3b：Settings 权重轴 + 硬目标注入
