# 当前任务清单（当下活跃任务）

> 父文档：[总任务.md](总任务.md)（大项 T1-T14 脉络）。规范 / 编号规则 / SOP：[项目管理.md](项目管理.md)（唯一权威）。
> 更新纪律：完成即移出（归档见 [已完成任务.md](已完成任务.md)，此处只留销项登记一行）；部分完成按拆分规则自动拆子号，父任务必须收口。
> 训练背景（以实读为准）：常态 = 服务器 172.16.2.40 `homm3-train-server`（N=16，日志 `train_full.log`）；WSL `homm3-train-v5` 为回切备胎。

---

## 一、与其他文档的关系

| 文档 | 本文件与其的关系 |
|------|------|
| `总任务.md` | 大项 T 编号的定义与全局状态表在彼处；本文件只放**活跃** T 子项的执行详情 |
| `已完成任务.md` | 完成任务的详细归档在彼处（只增不删）；本文件完成后只留"编号 + 一句话"销项登记 |
| `项目管理.md` | 公共规范 / SOP / 编号规则在彼处；本文件不重复纪律正文，只指针 |
| `WSL知识库.md` / `WSL踩坑点.md` / `服务器知识库.md` / `服务器踩坑点.md` | 技术细节 / 踩坑在彼处；本文件技术内容一律指针（`#n` = 踩坑编号） |
| `docs/方案_*.md` / `docs/预研_*.md` / `docs/T14_API面清单.md` 等 | 单任务方案全文在彼处；本文件只留触发条件与判据 |

---

## 二、当前任务（P 系列，按编号顺序；完成即删条）

> 编号格式 `P-xxx [Pn]`，[P0]＞[P1]＞[P2]＞[P3] 为优先级字段，降级/升级不改编号。

**P-001 [P1] too_many_monsters 留观复核**
  - 状态：在池（batch:2）留观 1 窗，分桶改善中 -616→-585（#326）。
  - 判据：~50 局时间桶，改善 >20% 或正局 ≥5% 才留，否则摘出（batch:99）。工具：时间桶均值对比（`train_full.log`）。
  - 关联：判"留"是 P-005 批转前置之一。

**P-002 [P1] batch3 地下 13 张 OBS 同层假设重验**
  - 问题：地下层红蓝英雄可能不同层，strategic_env obs 同层假设需逐张验证。
  - 约束：OBS_DIM=3464 冻结不动（项目管理.md §三.3）；验证产物 = 逐张 obs dump 对照。
  - 已备：13 张真地下图已转出并部署两侧（`_convert_batch3_ug.py`，darwin_s_prize_allies 空地下层排除，commit `34c5eeeb`）。
  - **09-27 定谳：全密度 13/13 张 OBS 同层假设成立**——13 张全图 gate 类对象（subterraneanGate/labyrinth）=0，英雄 spawn l=0 → 英雄永远地表作战，地下层（344~635 对象）英雄到不了。

**P-003 [P1] batch3 每张满步准入 N≥4**
  - 判据：每张满 250 步整局 ≥4 局；**禁 30 步冒烟判**（#326 定谳：入池依据 30 步冒烟不可靠）。
  - 资源：`py/_298_repeat.sh` 同款 N 轮工具；服务器 CPU 错窗跑。
  - **09-27 全密度实测：0/13 张达成**（52 局全 31~40 步 HERO_DEATH）。已转「减怪重转」路线 → 待重跑 `_ug_low` 13 张。

**P-004 [P1] batch3 通用评测口径**
  - 判据：新 ckpt 4 局 meanR≥0 且正局 ≥50%（T05 回池同款）。工具：`py/eval_promo.py`。
  - 冲突/限制：与训练抢资源 → 错窗跑（评测属服务器 CPU 密集）。
  - **09-27 全密度实测：13/13 张全未达标 → 全 hold**（meanR 全负 -7.6~-64.3，正局率最高 25%，52 局全 go=0；`b3d95a43` 双侧 index `_ug` 13 张 → batch:99）。
  - **减怪重转路线（09-27 用户拍板）**：`py/_make_ug_low.py` 从全密度 `_ug` 生成 13 张新编号 `_ug_low_h3m.vmap`——只删地表层中性怪物族对象 50%（确定性交替删，保留玩家 owner 兵 + 资源/矿/巢穴 + 地下层全保留）。全密度 `_ug` 原图保留 batch:99（ckpt 提升后回评用）。删减量 3~84 张/图。index `_ug_low` 标 batch:3 + low_density 标记。commit `b7da8749`，服务器双侧已部署（pool_dir+vcmi_maps md5 13/13，备份 `.bak_0927_uglow`）。
  - **下一步 = `_ug_low` 13 张重跑 P-003/P-004（N≥4 + meanR/正局判据），达标才开 P-005**。

**P-005 [P1] batch3 批转开关**
  - 动作：`HOMM3_H3M_BATCH` 2→3（u 阈值）+ unit 双副本同步（#306 教训：/etc 与 py/ 仓内逐字核对）+ trainer 600s 重读确认。
  - 前置：P-001~P-004 全绿 + P-010 duel 窗收口（错窗纪律：难度轴与图池变更不同窗）。
  - 防负数机制：未标 batch 图一律不放行（`HOMM3_H3M_BATCH` 只放行显式标记且 ≤ 阈值）。

**P-006 [P2] batch4 数据层 5 维标签 + 岛屿 4 张分型**
  - 动作：`_pool_index.json` 补 `{layer, underground, island, gate, water}` 5 维标签（复用 `h3m_flat_pool.csv`）；island_king ×2 / thousand_islands ×2 逐张分型。
  - 判据：读 `header.players.*.mainTown` 坐标 + surface_terrain——mainTown 在岛而开局在大陆 = `open_water_isolation`（水堵 hold，BUILD_BOAT 落地前不放行，同 a_viking batch=99 待遇）。

**P-007 [P2] batch4 转换层 `--include-open-water` 开关**
  - 动作：`py/h3m_batch_pipeline.py` 加第 3 开关（`--keep-underground`/`--include-gate-island` 已有）。
  - 限制：open 水域需 P-008 造船激励先行评估，本任务只做转换能力备料。

**P-008 [P2] 动作码 25 BUILD_BOAT + C++ 执行分支**
  - 定位：**动动作空间宪法，最重**——用预留位 25（N_ACTIONS=25，25-63 预留），C++ AAI 加执行分支。
  - 前置：P-006 分型确认 open_water_isolation 规模值得动宪法；流程走项目管理.md 改动流向（本地 commit → format-patch → 服务器）。
  - 回退：env 开关控制掩码放行与否。

**P-009 [P2] GARR_REVISIT RECRUITED 信号 H3M 大图验证**
  - 背景：09-25 晚 C++ dst=cur 修复后 duel 图 slot5 RECRUITED=9/局达标，但样本全在 step<60；GARR_REVISIT（step>60 回城）的取兵信号待 H3M 大图局推进验证。
  - 判据：step>60 局出现 `[RECRUITED]` dp>0 非零；打点 `[MOVE_ARMY]/[TOWN_RETRY]/[RECRUITED]/[ECON]` 全跑通后搭下次 runner 部署降级（每 ep 1 行汇总，09-25 拍板）。

**P-010 [P1] duel 难度轴观察窗收口判定**（归属 T6.4）
  - 现状：`HOMM3_DUEL_FOCUS=1` 非池局 60% 强制 108X108_02_duel（先 80% 后拍板降 60% 防单图过拟合；回退 `HOMM3_DUEL_FOCUS=0`），服务器在跑。
  - 判据：非池局时间桶趋势达标（r_mean 维持正 + 无恶化）；收口后与 P-005 错窗排程。
  - 纪律：难度轴与激励轴/图池变更不同窗；挪城脚本 `py/win3_move_town_108_02_duel.py` 备而不用。

**P-011 [P2] 36X36_02 回池评估**（归属 T6.4，殿后）
  - 触发：错窗确认 + 02 入池攒 ~40 局（09-25 定谳 J1 无分母不可算）。
  - 判据：J1 = r_mean(02)/r_mean(01) ∈ [0.8,1.25]，不通过才走图 03。工具：`py/t05_02_data_pull.py`；方案全文 `docs/方案_T05_36X36_02重评估_20260910.md`。
  - 限制：历史 51 局全负数据采于假糖时代仅背景。

**P-012 [P2] 评测图池对齐训练域**（S-9①）
  - 问题：S-7 全池 DRAW 主因 = 训练分布（VMAP 课程图）≠ 评测分布（H3M 池岛屿/地下未训过 → act10 空转 fuse 截断），DRAW 不作能力信号（用户 09-25 定性，非 bug）。
  - 动作：vmap 课程图评测，或 H3M 池只留训练覆盖图类。

**P-013 [P2] 评测即停类治理**（S-9③）
  - 问题：52 局 steps=0/1 即停（err 空，独立样本类）。
  - 资源：`/DATA/hero3/output/batch_newabi_pool_20260925_072335.json` 逐局明细 log 定位。

**P-014 [P2] 评测模型换档 1003121**（= T14.4B 同一事项）
  - 动作：评测红方模型从 09-25 稳定档换 1003121；与 T14.4B（红方 PPO 互评 Python 通道）同一事项，完成一处即双销。
  - 依赖：T14.4A 蓝方可插拔参数先行（同一改造窗口）。

**P-015 [P2] 服务器 N=16 并行长稳验收**
  - 判据：12h+ 连续跑 0 崩（`Restart=on-failure` 兜底）+ `pgrep -c ep_runner_one.py`≈16 + `HERMES_N_EP` 语义=PPO cycle 非局数（§2.18）判读确认。
  - 限制：纯观察验收，不占停训窗。

**P-016 [P1] WSL `maps/training/` 权威源补齐**
  - 问题：回切 SOP 跑通但 WSL 侧权威源缺失，改图前必须先 sync，否则双树漂移。
  - 动作：从服务器拉回或按 `py/sync_maps_to_runtime.py --strict` 校验重建；完成后回切 SOP 该前置项关闭。

**P-017 [P2] 冻结 Mode B 架构级根修**
  - 现状：Mode A 已修（shutdown 延迟 + game_over 强制置位 + trade cap 16）；Mode B（NK2 建筑评估持 `CGameState::mutex` 读锁长规划 → `runNetwork` 写锁饥饿）被 cap 16 压制非根治。
  - 方向三档（知识库 09-23 章）：① 服务器查询栈看门狗（top=null/popIfTop FAIL 强制清理）② MapObjectVisitQuery 根因 ③ AI endTurn `waitTillRealize=false`（放松回合序）。先选型拍板再实施。
  - 观察口径：`[EP298_SWALLOW]`/`err=` 按复现率（N≥4），单局不下结论。

**P-018 [P2] #205 PR 推送**
  - 现状：worktree `vcmi_pr205` 分支 `pr205-server-pack-guard`（21 行 diff）已提取未推；卡认证（#258）。
  - 资源：需用户出 PAT 或浏览器手建 fork。

**P-019 [P3] vcmi-strategic-gym 模型 release**
  - 动作：定版本 + 模型文件 release；前置 = 路径清洗（已完成）+ 敏感扫描（已绿）。

**P-020 [P3] 开源仓历史二进制清洗**
  - 问题：历史混入 3 个 `.bak/.so` 二进制（4 commit 全历史 + 当前树扫描结论）。
  - 方案已定（filter-repo 类），触发 = 下次开源周窗口。

**P-021 [P2] ARM64 CI workflow 推 GitHub + 首跑**
  - 现状：`vcmi_gym` 仓 `10c27e8`（`.github/workflows/arm64-ci.yml`，kunpeng runner，lint+unittest 同 ci.yml）本地未推。
  - 前置：推 GitHub + 首跑需用户拍板（"git github"）。

**P-022 [P2] ep_runner 横跳 P0 修正 commit**
  - 内容：prev2→obs[-3] + 8 步窗平级重构。
  - 限制：属奖励面改动且 09-24 后无验证记录——引擎窗口验证后再提 commit（在跑窗不混入）。

**P-023 [P3] `py/reorder_wslpit_engine.py` 未跟踪定性**
  - 动作：读码定性 → 入库或删，消除工作树悬置。

**P-024 [P1] 异地备份：仓#2 十提交推 fork**
  - 风险：仓#2（训练真源）10 个提交现仅存本机两仓，无异地副本。
  - 动作：推用户自己的 GitHub fork（c18593395 账号可用，#258 已知）；PAT 泄漏已排查闭环（09-25 四路搜索 0 命中，拍板不吊销）。

**P-025 [P2] T7.5 S2 正式收口复查**（触发型，归属 T7.5）
  - 触发：`[RECRUITED]`/局 回升 ≥5（09-19 FAIL 定性 = 困死同根因，维持训练攒局等恢复）。
  - 动作：复跑 40 局判据（avg_r 跌 <20% + 招兵/建设频率不塌）→ 过则排 S3（≈0.125/+0/0.04/低+5 高+10 防+5）；S4 绑 T05 换图节点。
  - 错窗：S3 与 BUILD P1 错窗（S2 收口后 BUILD P1 入池切换，A11 备料就绪）。

**P-026 [P1] T7.8 满窗评估复跑**（触发型，归属 T7.8）
  - 触发：攒满 100 局（当前窗判据背景：A3 九改动净效果为负已四修五修压制，大负局主源 = Φ_guard shaping 已修）。
  - 判据：① 战死率 <35% ② duel 净胜率 >0% ③ 大负局（<-300）<3 ④ avg_r >0。工具：`eval_new_baseline_0919.py`。
  - 回退：monster_mask=0 / f_min 摘参 / 硬闸 git revert（ea259f0 前后两 commit）。
  - 注意：T7.8 收口是 P-028（H3M 混合接线）的触发前置。

**P-027 [P3] 评测结果回流 Windows 分析管道**
  - 动作：服务器 `output/*.json` → Windows 侧聚合分析脚本（未排期）。

**P-028 [P2] H3M 137 张渐进混合接线**（归属 T6.4）
  - 触发：T7.8 收口（P-026 达标）后接，渐进混合。
  - 依赖：137 张已入池（PASS 率 85%，#290 已修）；防负数机制走 batch 阈值渐进。

---

## 三、T 系列任务（按编号顺序：活跃 + 远期储备；大项定义见总任务.md）

**T5.8 🔄 BC 重采集 → PPO 重启（训练验证中）**
  - 持续状态项：v5 训练本身即 T5.8 验证载体；无独立动作，异常时按踩坑/知识库排查。

**T6.3 ⬜ 决策结构分层（远期；含 T5.9 v2 MultiDiscrete 三维参数化，合并执行，T5.9 定义见总任务.md）**

**T6.4 🔄 训练地图体系**
  - 历史：原 WIN-4 官方 H3M 批转 ✅ 137/159 入池收官（09-21）。
  - 活跃子项已 P 化：batch3 链（P-001~P-005）/ batch4 链（P-006~P-009）/ duel 轴（P-010）/ 02 回池（P-011）/ H3M 混合（P-028）。
  - 现池态：服务器 `HOMM3_H3M_BATCH=2` + `POOL_INTERVAL=10`，在池 3 张 = good_to_go / judgement_day / too_many_monsters(留观)；maps=6（T06 六张）。
  - 远期子项：
    - ⬜ T6.5 capture-all-mines 胜利条件地图模板（原 A2；PR #7810 已 merge，地图轴错窗待选，备料 `py/probe_victory.py`）。
    - ⬜ T6.6 h3m2vmap 转换器 C/D/E（原 P10-B；课程产出图入池设计）。
    - ⬜ T6.7 对手池 self-play/NK2 混合（原 P6；NK2 三树源码就绪，回用直接重编）。

**T7（原 WIN-1）✅ 批次B+A3+passable 闸门 4 判据全绿收口（09-19）**

**T7.4 ✅ 死亡惩罚判据 1 定性收敛关闭（09-21）**

**T7.5 🔄 经济奖励撤梯子（S2 在线）**
  - 定义：分四步撤掉经济激励中的引导性奖励（"梯子"），逼模型自主招兵/建设：
    | 步 | RECRUIT/次 | BUILD_2/次 | 兵力系数 | 建设奖 | 状态 |
    |----|-----------|-----------|---------|--------|------|
    | S1 | +0.5 | +0.75 | 0.02 | 无 | 原档 |
    | S2 | +0.25 | +0.375 | 0.03 | 低档巢 +3 | 🔄 在线（09-19，commit `ab93213`） |
    | S3 | ≈0.125 | ≈0 | 0.04 | 低+5 / 高+10 / 防+5 | 待 S2 收口 |
    | S4 | 0 | 0 | 0.05 定稿 | garrison + R6 大调同批 | 绑 T05 换图节点 |
  - S2 收口复查 = P-025（触发：`[RECRUITED]`/局 ≥5）；错窗：激励轴与难度轴不同窗。

**T7.6 ✅ A2 攻击步旁路单局冒烟，红蓝真实战斗首胜（09-18）**

**T7.7 ✅ 根因 C' 修复观察窗判据达成，演进 T7.8（09-19）**

**T7.8 🔄 A3 战力闸观察窗**
  - 满窗复跑 = P-026；判据/回退见其详情。观察口径：按复现率，单局异常不下结论（#298 纪律）。
  - 远期子项：
    - ⬜ T7.9 动作码 16-23 内政全面启用（Level 3 切入时先解决 noTarget 惩罚）。
    - ⬜ T7.10 CGameHandler garrison 自动合并（触碰铁律，断言不复发不做）。

**T8 ⬜ 战斗集成**
  - ⬜ T8.2 英雄 VS 英雄战斗 / T8.3 战斗结果进观测奖励 / T8.4 战斗状态追踪。
  - 远期子项：
    - ⬜ T8.5 MMAI v15 合并评估（PR #7654 未合并官方，2-4x 慢，合并后需重采集）。
    - ⬜ T8.6 D 方案 startBattle 引擎层直调（技术储备，09-19 拍板登记；触发 = B 方案失效或 T05 接战成瓶颈，调研见知识库 09-19 章）。
    - ⬜ T8.7 战斗模型训练（触发 = T05 对抗接战胜率成战略瓶颈；当前 R6 onnx 够用，勿课程中途升级）。

**T9 ⬜ 真实游戏对齐 Track 2（前置 T5.8 成熟）**
  - ⬜ T9.1 H3API 编译验证 + 注入 demo / T9.2 状态读取层 / T9.3 动作执行层 / T9.4 推理层 ONNX+DLL / T9.5 稳定性 24 局 / T9.6 VCMI Windows 路线验证。

**T10 ⬜ 你 vs AI 1v1**（待 T9 / T13.10 铺路，底座已就绪）

**T11 ⬜ 1v7 战胜你**（最终目标；需 T12 前完成 v15 迁移）

**T12 ⬜ v15 架构迁移（Phase G 前）**
  - ⬜ T12.1 图状态 / T12.2 GNN 取代 MLP / T12.3 py::dict 取代 struct / T12.4 训练体系重写。

**T13 ✅ 外挂 AI 架构全闭环（T13.1-13.10：离线 167/167 + P8-B/C/D/E 实机）（09-16）**

**T14.1 / T14.2 / T14.2b / T14.3 ✅ 摸底、规则设计、handicap mod、T14BlueMod 工程验收全闭环（09-25~26）**

**T14.4 ✅ KTV 冒烟 + 四图全池矩阵 32/32 全 rc=0，go 双非零首信号（09-26）；余量已拆 T14.4A/B/C（见下）**

**T14.3B1 ⬜ 硬目标注入（decompose 私有面）**
  - 背景：T14.3 工程验收 ✅（09-26，服务器 patch 部署 + KTV N=4 双臂 0 崩 + `[T14-ACT]` 加载实锤）；能力面单图 DRAW 未破，但 T14.4 全池数据（T14 四图全不负 NK2，总均值差 +143）后**优先级可降**——先跑 T14.4A 再定是否需要。
  - 真相源：WSL 仓 `4a1675c6a2`（13 文件）；checkpoint 文档 `docs/T14.3_T14Blue_20260926_checkpoint.md`。

**T14.3B2 ⬜ Settings 权重轴（字段 private 无 setter）**
  - 调参零重编的权重 JSON 化；随 T14.4A 结果决定做否。

**T14.3B3 ⬜ MMAI CMake python 路径 find_package 化**
  - 临时桥接（符号链接 libpython3.12→3.11，踩坑 §2.21）的正式修法；服务器 ARM 重编前必须落，否则每次 ARM 构建都要手工桥接。

**T14.4A ⬜ 147 图全池矩阵（NK2 / T14BLUE 双臂）**
  - 动作：`batch_eval_newabi_pool.py` 加蓝方可插拔参数；规模 top-N × 每图 4 局（#298 纪律 N≥4）。
  - 判据：DRAW 率 / go 非零占比双基线对照；预期 T14 区分度（T14.4 四图矩阵已给方向：总均值差 +143，go 双非零）。
  - 资源：通用评测口径同 P-004（`py/eval_promo.py` 全 profile 等条件触发）。

**T14.4B ⬜ 红方 PPO 模型互评通道**（= P-014）
  - 动作：`batch_eval_newabi_pool.py` 红方接 Python 通道载 1003121（gap② PPO 当蓝方档的天花板验证）。

**T14.4C ⬜ 三档矩阵全谱（NK2 / T14BLUE / PPO）**
  - 依赖：A、B 完成后一次出对手强度区分度全谱（定位排序验证：gap② PPO > T14 > NK2）。

**T14.5 ⬜ 可选 Lua 战斗辅助 MOD**（未排期）
  - AMER_HumanAI 三条战斗启发（招魂/恶魔养猪/减伤）；触发 = 全池矩阵显示战斗层为区分度瓶颈。

---

## 四、观察与风险（OBS / R —— 非任务编号，复发才行动）

**OBS 长期观察集**
- OBS-1 R7 battle query 卡顶：埋点五件套 + 300s fuse 在位，复发按 map×secs/steps 聚合定位。
- OBS-2 BHERO_KILL 空拍：双帧确认 + 空拍跳过防护生效中。
- OBS-3 英雄战斗触发：passable 闸门（方案甲）已修并随 WIN-1 验证；D 方案储备见 T8.6。
- OBS-4 TOWNSTALL 引导可达性：平台段误判已修（#209），继续观察。
- OBS-6 #296 `Cannot answer the query -1` 降噪：Popen grep 管道已部署，复发再查（勘误：报错在 `CCallback.cpp:53` 非 mlclient）。
- OBS-7 8B optional 字节级不匹配疑点（T13 遗留）：C++ 写 4B / Python 写 1B，run10 实证 server 受理；待 197 fishy 实锤再验 9B present。
- OBS-8 H3M 池图 obs 全零三层根因（#315）：L1 header.players 空（主因，已修）/ L2 引擎 reset 竞态（~4% 残余）/ L3 XDG（已修 #308）；N=16 若再现先跑 `sync_maps_to_runtime.py --strict` 验 L1，勿归因竞态。
- OBS-9 C2 L1 gdb 取证：崩溃复发才触发（工具 `py/c2_crash_gdb.sh`）。

**R 风险登记**
- R1 动作拒绝不可感知 🔄 Python action mask 在用未审计，观察中。
- R3 异步时序竞态 ✅ 约束型（新动作走 waitTillRealize）。
- R4 queryID=-1 断言雷 ✅ 已修（libMMAI.so 13c0f041）。
- R5 双源码树不同步 🔄 拓扑已坐实（工作区仓 + 仓#3 vcmi 子模块 + 仓#2 训练真源；仓#2↔#3 直连；分支同名 `fix_action_mapping` 易混）；源码改动先 WSL 侧 grep。
- R6 战斗 AI fallback ✅ onnxruntime 1.19.2 + V13::BAI，长期观察。
- R7 battle query 卡顶 🔄 → OBS-1。
- R8 训练进程存活 ✅ 服务器 system unit（`Restart=on-failure`）+ WSL 回切 SOP；WSL 侧 keepalive 纪律见项目管理.md §4.1。
- R9 KL 失控 🔄 生效窗 <1.0 无复发；持续 >1.0 → 评估回退梯度注入。
- 冻结（AI EndTurn realize 死锁 / Mode B）🔄 三套补丁压制中（A 臂 16/16），根修 = P-017。
