# 当前任务清单（当下窗口要完成/解决的任务）

> ⚡ **下次会话第一步 (0910 更新)**: 训练在跑 (resume step=599094, MAPS 8 图), **观察窗 = 新口径基线积累期** — 三项当日修复已生效: ①[GUARD]/[MINE] visitable 口径 (`_anchor_to_visitable`, 假糖消除, 见 0910 重大发现条) ②[HEROSEG_EMPTY] 空拍诊断埋点 ③[EP_TIME] 局耗时打点 (慢速画像基建)。例行复核: 跑 1-2 窗后聚合 [EP_TIME]/[GUARD]/[MINE]/capture 判据 (新 r 基线预期回落 = 挤水分非退化) → 届时拍板 36X36_02 原样回池 (docs/方案_T05_36X36_02重评估_20260910.md) 与 T7.4 死亡惩罚实装 (docs/方案_T74_死亡惩罚_20260910.md, 与图池轴错窗)。R7 battle query 埋点长期观察; 取兵链路四拍已全通无需重验。若训练未在跑，恢复命令：
> `wsl bash -c "systemctl --user reset-failed homm3-train-v5 2>/dev/null; systemd-run --user --collect --unit=homm3-train-v5 --working-directory=/mnt/d/Bigdata/hero3_fresh /bin/bash -c 'exec /home/administrator/vcmi-workspace/venv/bin/python train_wsl2_ppo_v2.py >> train_loop.log 2>&1'"`
> 另起 Windows keepalive: `Start-Process -WindowStyle Hidden wsl.exe -ArgumentList 'sleep infinity'`（防 idle shutdown, 踩坑 #114）。

> 父文档：`总任务.md`（整体脉络，大项 T1-T12）。本文件 = 当下要完成/解决的任务清单：① 当前主线大项详细任务；② 待办工作台 A/B/C/D 档；③ 训练期并行 P；④ 风险登记 R。完成即移入 `已完成任务.md` 并回写总任务状态。
> 编号：与总任务 T 体系一致；A/B/C/D/P/R 为本文件工作台视图分类标签（非大项编号），明确归属某大项的标注 T 编号。
> 脱钩：本文件只写任务，不引用知识库/踩坑点（技术细节见对应设计文档）。

---

## 当前主线：T7 城镇经济（当前主线，T7.1-T7.3 ✅）

### T7.1 Level 3 晋级 ✅ 完成 (08-29)
晋级线：近 100 局胜率 ≥30~50% 或 avg_r 持续为正。三判据全过：eval 10/10 全胜 avg_r=99 / 近 100 局首胜率 64% / 30X30_01 A/B 44 局 0% 大负率。决策：跳过 30X30_02（同构边际低），直接晋级。

### T7.2 切 T04 地图（不开经济）✅ 完成 (08-29)
MAPS=T04×6 + Level 3 超参 (BATCH=2048/EPOCHS=6/ADV_CLIP=6.0)；step=205345 resume；配套 clip±300 对称修复 + objective_reward 30 (首占矿/首进城镇 +30) + 目标优先层 (矿 > 资源堆) + move_to_force 60 常驻。

### T7.3 开经济（一次一轴）✅ 完成 (08-29 深夜)
economy_force 24 (50→24 修复空转) + explore 0.3 + nk2 0.45；RECRUIT first+12/每次+2, BUILD_2 first+15/每次+3（自主通道修复）；兵力增量 0.001→0.01；周期提醒每 40 步 4 步经济窗；城镇引导降级。[ECON] 全链验证；avg_r 段高 +0.8；80+ 分局 3/15 常态化；历史新高达 +105.9；自主 16-21 0.6→2.0/局 爬升中。

### T7.4 存活塑形评估 📋 设计稿已出 (09-10)
首胜-战死 100% 关联 (7/7)；T04 有城镇后评估是否加死亡惩罚/存活奖励。**设计稿 `docs/方案_T74_死亡惩罚_20260910.md`** — 要点: 引擎判负不可达实锤 (死亡局 game_over 恒 0, -200 轮不到) → ep_runner zombie 确认点 (L1152) 追加 `traj["rewards"][-1] += death_penalty` (-50 试探档, 上限 -100 对称守卫, argparse 可调含 0=关闭); 全局生效; 观察判据 = 死亡局 r 转负 + [GUARD] 接战率不塌 (±20%) + avg_r 跌幅 <20%; **实装时机 = capture 观察窗达标后下一窗 (激励轴不同窗纪律)**; 引擎侧 standardDefeat 判负根修独立课题。

### T7.5 经济奖励撤梯子策略 📋 已登记
渐进半价不跳崖：① 现在不动 (自主 2.0/局未固化) → ② 自主 3+/局 连续 2 窗稳定后每次奖励半价 (+2/+3→+1/+1.5), first 不动 → ③ 大调 (兵力 0.01/explore) 打包到换图节点一次付非平稳成本。每步观察 1~2 窗确认无行为退化。

**⚠ 撤梯子硬前置 — towns.garrison obs 填充 (08-31 发现)**：
- 现状：C++ strategic_state.cpp towns 全知填充段 `memset(t.garrison, 0, ...)` — H.3 schema 定义了 garrison[7] 但从未实现填充，obs 恒 0 → **模型世界观里所有城都是空城**
- 影响：撤梯子移除回城取兵引导后，模型无任何 obs 信号区分"城里 300 枪兵 vs 空城" → 内生回城取兵策略在信息上不可能学出；T5 对抗图攻城评估 (battle_pred 城防) / 守城决策 (act22) / 兵力分布感知全依赖它
- 修复：C++ towns 段补 garrison[7] 填充 (~5 行, obs 布局不变=不违冻结) + 停训窗口重编 libmlclient + .so 三副本同步
- **时机绑定：与撤梯子③同批进 T05 换图节点** (字段从恒 0 变非零 = obs 分布突变, 中途修=非平稳成本); 同批顺带: Router 断言排查 (R 项)
- **✅ 已完成 (09-02, 提前于计划)**: P3 已实现 (strategic_state.cpp getUpperArmy() 7 槽 count → obs 城槽 field 6-12, 布局不变不违冻结), obs 突变成本已随取兵链路修复停训窗口一并消化 (T7.6); libmlclient 重编 + 4 副本同步

**📋 拍板 (08-31, 数据埋点已上线)**：
- **占城率进晋级判据** — 同意但带联动条款: 占蓝城若需战斗则 R6 修复前结构性为 0, 判据在 R6 修复后启用; 修复前先跑 2-3 窗定基线。埋点: [TOWN_CAPTURE] (蓝城 owner 1→0 事件, 只观测不加奖) 已上线, 占城率 = 次数/局数
- **visited 奖励换 owner 校验版** — 同意, 排 Router (R6) 之后, 否则 owner 恒 1 → 奖励归零回修复前; 届时建议双级化: visited 探索糖降价保留 + capture 大额 (+100 复用守卫模式), 参数等 [TOWN] owner= 标注数据再定。埋点: [TOWN] 行已带 owner= 标注 (captured/enemy-held)

**📋 撤梯子③ 多步渐进方案 (08-31 定稿, 递归半价 + 效果奖渐入, 每步 1-2 窗观察)**:

| 步 | 触发 | RECRUIT每次 | BUILD_2每次 | 兵力系数 | 建设奖(新巢穴) |
|----|------|------|------|------|------|
| 当前 | — | +1 | +1.5 | 0.01 | 无 |
| S1 (已改码等下局生效) | — | +0.5 | +0.75 | 0.02 | 观测埋点 [BUILD_NEW]/[RECRUITED] |
| S2 | S1 无退化 1 窗 | +0.25 | +0.375 | 0.03 | 低档巢 +3 |
| S3 | S2 无退化 1 窗 | +0.125(≈0) | ≈0 | 0.04 | 到位: 低 +5 / 高 +10 / 防 +5 |
| S4 | T05 换图节点 | 0 | 0 | 0.05 定稿 | garrison 填充 + R6 大调同批 |

设计要点: ① 糖每窗 ×0.5 与效果信号反向渐变, 总激励平滑迁移 ② 建设奖低值试探→到位, 防模型死刷建设 ③ 埋点先于奖励 (S1 只观测) ④ 全部 ep_runner/注释层, 每步自然重启生效 ⑤ 每步判据: avg_r 跌幅 <20% + 招兵/建设频率不塌 + 取兵窗维持

**T04 当前态势 (08-31 晚更新)**：avg_r +8.3 (首次显著转正) / 正局率 ~51% (⚠ 此前文档 82-96% 系统计公式错 2 倍: n=100 时误用 p*2, 实际 40→44→48→51% 缓升, 趋势方向不变) / 80+ 局 20/100 / [TOWN]+30 触发率 36% 局 / **[TOWN_VISIT] 取兵窗已触发 12 次** (链路打通) / [TOWN] owner=enemy-held 标注生效 / [TOWN_CAPTURE]=0 (R6 未修符合预期) / 撤梯子②半价 (+1/+1.5) 已生效。OWN_TOWN_GUIDE=0 = 取兵窗靠自然路过触发, 引导分支未被走到 (模型未对己方城发 24), 非阻塞。观察点: 取兵窗后英雄兵力增量是否出现 / 占城率基线积累。

**⚠ 经济动作自发塌方 (09-03 定量实锤, S1 判据违反)**：最近 12 ep 共 515 步动作分布 = 移动交互 494 (3×243/5×115/4×82/7×35/2×23/6×9) + **自发经济动作 0** (16×2/17×4/18×2 全部来自 start_home economy_force 强制窗; 19-21 建设类 0 次)。S1 判据 "招兵/建设频率不塌" 已塌 → **S2 暂停推进**。定性候选 (待区分): ①糖减半后自发招兵期望收益 < 探索糖, 模型退回纯探索套利 ②start_home 每局强制送兵掩盖自发招兵需求 (兵白送, 自发无增量价值) ③R6 StupidAI 下兵力无战斗力兑现, 兵力系数 0.02 信号弱 ④长局 act 高度重复 (同图同 act 序列复现 = 确定性策略), 探索糖主导。用户观察 "现在好像没有经济动作了" 与数据一致。

**📋 拍板 (09-03, 用户)**：经济塌方**不动, 等 R6 战斗修复** — 兵力无战斗力兑现是结构性弱信号, 回撤糖价/降频 start_home 都只动症状; R6 (onnxruntime + USING_ONNX=1, 绑 T8 战斗集成/T5 对抗前置节点) 修复后兵力价值可兑现, 经济动作预期自然回归, 届时再评估撤梯子节奏 (S2-S4 或重新定案)。训练继续跑积累占城/探索基线; start_home 强制窗保留 (招兵链路四拍验证资产, 不拆)。

**📋 拍板 (09-03, 用户): T04 同级加图暂缓** — R6 战斗切换为大非平稳节点 (StupidAI→BAI+Python), 观察窗 (50+ ep 或 1 训练日) 内不加图, 避免归因混合。**触发条件 (任一满足即加图)**: ①观察窗后经济动作仍未回归 + 行为固化依旧 (同图同 act 序列逐字复现) → gen_level3.py 现生成 4-6 张 T04 同级图加入 MAPS 轮换 (多样性轴, 管线现成半天可完成) ②R6 带来明显收益 (avg_r 上台阶/战斗频繁) → 跳过加图直接评估 T05 前置。当前 T04 库存仅 6 张全部在用, 无备用。观察点: 短局 done 触发路径 (start_home 取兵完成后 env done, r≈48 无 ±200 胜负信号, game_over 路径与奖励构成不匹配, 既有行为待查) / TOWNSTALL 升至 4.8 次/ep (此前 2.8)。

**✅ 观察窗复查 (09-03 16:50, 96ep/5.8h) → 触发条件①满足 → 加图已执行**：Router fallback 0 / ZOMBIE 0 / FUSE 0 全程健康; avg_r 0.2~7.3 无上台阶; **自发经济仍 0** (16-18 共 122 次 = 强制窗供给); **固化依旧** (ep 结局同构组合逐字反复, 短局占比升高); TOWNSTALL 4.9/ep。**加图实施**: gen_level3.py 与实际不符 (含守卫怪, 实际 T04 0 守卫 — 解包 objects.json 实锤: 2 城+2 英雄+1 金矿+5 资源堆) → 改用 **180° 旋转变体方案** (py/gen_t04_mirror.py: terrain 行列反转+对象坐标变换, 完全保留 0 守卫/城/矿/资源/英雄/胜利条件 = 同级同特征零难度轴), 生成 6 张 _mir 图 (20X20/30X30/36X36 各 2 张) 落 Maps/training/, MAPS 扩至 12 图, 停训重启后镜像图加载正常 (ep1-4 即出场 3 张, 71 步局正常)。镜像图上策略泛化正常 (obs_nz/结局与原图同款)。运行时地图路径实锤: 训练 CWD=项目根, VCMI ResourceHandler 读 Maps/training/ (relative), 无需额外副本同步。

**✅ T05 混入轮换 (09-03 晚, 用户拍板: 战斗密度提升打破固化剧本)**：动作重复专项分析 (用户观察 "动作都重复") 定量实锤 = **6 ep act 序列逐字节相同 + 相邻重复 41% + hermes 'Cannot move hero' 56 次/局** (选→拒→盲重试), 固化三层机制 = 策略熵塌缩 (根本) + 引擎拒绝盲重试 (放大) + start_home/经济窗固定段落; 12 张同构图探索糖吃完 + 战斗密度 ≈0 → 无力量推离剧本 → **T05 混入决策** (T05 自带 4 守卫挡矿路, 战斗信号密度来源, 不动经济奖励参数): MAPS 16 图 (12 T04 + 4 T05), T05 分支 move_to_force 60 同款。**T05 首 ep 即兑现**: `[GUARD] guard (15,14) fought & won +100` 首落袋 (R6 修复后战斗奖励首次经策略模型打通) + GUARD_DONE 终局机制苏醒 + r=131 (vs T04 剧本 15-48)。

**✅ 200 步挂死局诊断修复 (09-03 晚) → 09-04 复查回滚**: T05 混入后 18ep 出现 6 个 200 步 truncation 局 (T05 r=-126.21 最重 + T04 镜像成对 -40.75/15.98/-17.35)。**r=-126 局解剖**: start_home 取兵正常 (0-8 步) → econ_force 瓜分 (0-24) → move_to_force 60 步窗被瓜分殆尽 → **60 步后引导撤走, 模型面对陌生 T05 图熵塌缩** → END_TURN 连刷 28 次 + 城边徘徊 140 步 (act_loop_penalty 持续扣) → 全局零事件 (无 MINE/GUARD/TOWN) → 200 步空转耗尽。修复尝试 move_to_force 60→200 全程引导 → **09-04 复查判定过度修正回滚 60**: 200 版 13/32 挂死局暴增 + r=-429/-280/-191 大额负分 + a=2 (END_TURN) 1089 次成第一大动作 (34%) + TOWNSTALL 10.4/ep — 全程引导把英雄拖着撞墙 (obj_best 目标在长局/新图频繁失效, 无目标 fallback endTurn 遍地); **60 版对照 18ep avg_r 1.8 有 +131 局明显更优**。GUARD 战斗 4 次仍在 (战斗信号未丢)。**当前状态: move_to 60 + 16 图轮换观察窗继续**。遗留观察: 200 步局根因 = obj_best 引导目标可达性 (10.4/ep TOWNSTALL 实锤引导失效), 引导目标选择机制优化待立项; T05 局 [GUARD] 频率 / 自发经济回归。

**✅ T05 扩图 3→7 张 (09-05, 用户需求)**: ①3 张可用图镜像变体 (gen_t05_expand.py, 180° 旋转守卫同步旋转难度不变) ②36X36_02 守卫减半回归 (swordsman 8→4 / archer 10→5 / peasant 15→8 可赢难度) — 双路径同步 (Maps/training + rel/bin/data/Maps, 踩坑 #143 事实 2); **验证: 52X52_01_mir 首次真实加载 obs_nz=215 独立特征带 (区别原版 223) + 原版 r=150.44 新高** — map= 标签真实性检验法 (obs_nz 特征带) 首次实战应用成功。T05 课程图库: 36X36_01(+mir) / 36X36_02(调守卫) / 52X52_01(+mir) / 52X52_02(+mir) = 7 张轮换。待观察: _mir 局结局分化 / 36X36_02 调守卫后可赢性 / 全新生成图 (方案③, 需布局设计, 待这批验证后扩)。

**✅ 330ep overnight 复查 + 36X36_02 二次移除 (09-06)**: **战斗剧本大爆发** — [GUARD] 252 场/330ep (战斗-终局闭环高频) + [MINE] 250 + [RECRUITED] 5962 + **_mir 镜像图成最优图集** (52X52_01_mir 多局 147-167 / 52X52_02_mir 147-168 / 36X36_01_mir 134-148, **全场新高 168.48**) + avg_r 纯 T05 口径 **1.8~3.4** (判据③补达标) + 200 步局降至 8% + ZOMBIE/RouterEx 0。**36X36_02 调守卫失败二次移除**: 减半后 51 局仍全部负分 (-50→-181 恶化趋势) — 非守卫兵量问题, 疑守卫位置/地形结构压制红方路线, 回归需重设计该图。**当前: MAPS 6 图全绿** (36X36_01±mir / 52X52_01±mir / 52X52_02±mir), step=524897 resume。T05 全面化判据 4/4 全达标 (③纯口径补达标)。

**✅ T06 前置评估 + 方案 A 执行 (09-06, 用户拍板)**: **评估结论** — 4 张 T06 (72X72_01/02 + 108X108_01/02) 结构 = **1v3** (1 red vs 3 blue 英雄+3 蓝城) + 5 金矿 + 15 资源 + 20 野怪, 一次叠加 3 个难度轴 (大图 + 多敌 + 3 城胜利条件) 违反"一次一轴"纪律; 三类非法 identifier 与 T05 同套路 (hero core:inham ×3 / resource core:resourceGold/Wood/Rare ×15 / monster footman/pikeman/griffin/cavalier/angel/blackKnight/hydra ×14 + `core: pikeman` 带空格 bug); obs 兼容 ✓ (MAX_HEROES=8, 英雄段 8 槽×26 字段, 4 英雄无盲区; 地形走 terrain_grid.bin 旁路, 3464 冻结不破)。**方案 A 执行** (只加大图轴不加多敌轴): ①fix_t06_maps.py 修 4 图全部 identifier (inham→edric/iona/alchemist 按 hero 编号 / resource→gold/wood/crystal 补 amount=8 / monster→三兽集数量不变 + 补 aggression=guard) ②生成 `T06_adventure_72X72_01_duel.vmap` (去 hero_2/3 + town_2/3 → 1v1 对角, 结构与 T05 一致) ③inspect 5 图全绿 + 双路径同步。**单集冒烟** (等价训练参数): 73 步守卫胜 r=+75.31, 零卡死零断言。**混入轮换重启** (MAPS 6→7 图, T06 分支 move_to_force 60 同款, step=528482 resume): **duel 首集 ep_steps=73 r=+84.74 obs_nz=263 (独立特征带) time=97s** — T05 战斗剧本无缝迁移 72X72 大图, 97s/ep 性能无压力。**运维固化**: transient unit 停止即消失 (--collect), 重启必须 systemd-run 重建 → py/restart_train_v5.sh (venv 绝对路径, hero3_fresh 下无 venv)。**观察窗指标**: duel 自发经济 / 200 步局占比 / avg_r vs T05 对照; **后续**: duel 站稳后上 1v3 原版 (72X72_01) 验证多敌轴。

**📋 T05→T06 晋级判据草案 (09-06, 待数据触发)**: **duel 站稳判据** (触发条件 = 连续 1 训练日全部满足 → 上 1v3 原版 72X72_01): ①duel 局数 ≥30 且 avg_r 无恶化趋势 (后 1/3 局 avg ≥ 前 1/3 局 avg × 0.8) ②自发经济 ≥80% 局 (当前基线 4/4) ③200 步局 ≤20% (当前基线 0%) ④守卫胜闭环 (GUARD_DONE 或正 r) ≥70% 局 (当前基线 4/4=100%)。**1v3 上线方案** (一次只加多敌轴, 图尺寸轴已在 duel 熟悉): 直接混入 72X72_01 原版 1 张, MAPS 7→8 图, 观察窗指标 = 3 blue roaming 堵路频率 (dyn_blocked/TOWNSTALL 对比 duel) / 蓝英雄击杀事件 (3× 战斗分剧本是否出现) / blue 3 城招兵行为 / avg_r vs duel 基线。**回退线**: 1v3 上线后 avg_r 跌幅 >30% (vs duel 基线) 或挂死局 >40% → 移除回 7 图。**纪律条款**: 1v3 与其他难度轴变更 (新图入池 / 奖励参数) 不同窗。**duel 变体矩阵备料 ✅ (09-06)**: gen_t06_duel.py 生成 72X72_02_duel + 108X108_01/02_duel (inspect 全绿 + 运行时副本同步, 未入 MAPS) — duel 判据达标后 72X72_02_duel 即插即用, 108X108 duel 留作更大尺寸轴 (⚠ 108 对角距离 ~145 格, 200 步上限可完成度先观察)。数据源 = py/check_duel_watch.py (自发经济/200 步局/r 分布/T05 对照 一键统计)。

**✅ 1v3 上线 + 双根因修复 (09-06 晚)**: duel 判据 4/4 终判达标 (32 局, 后 1/3 avg 83.0 vs 前 1/3 65.1 比值 1.27 上行 / 自发 100% / 0% 挂死 / 守卫胜 96.9%) → MAPS 7→8 图混入 `T06_adventure_72X72_01.vmap` 原版 1v3。**首两局暴露双根因** (确定性 1 步终局 + obs_nz=0, 两局完全一致): ①`core:alchemist` 是职业名 (hero **subtype** 字段) 非英雄名 — fix_t06_maps.py 初版误用作 VALID_HERO, 引擎报 `Couldn't resolve hero identifier core:alchemist` 拒启动 → 4 原图 hero_3 alchemist→**core:christian** (castle.json 真实名, edric/iona/christian 三英雄集; duel 变体因删 hero_2/3 不受影响) — 教训: **identifier 校验集必须实测验证, 不能混入 subtype/模板类字段值** (fix_t06_hero3.py, inspect_t06.py/fix_t06_maps.py VALID_HERO 同步修正) ②fork 训练栈 `server/ML/ServerPlugin.cpp` HeroPool 对称性校验: 2 owner 时双方 pool 英雄数必须相等 (1≠3 throw `Owners have differently sized pools`) — **1v3 及未来 1v7 全部被拒** → 校验放宽: pool 大小不等降级为 stdout 警告 (pool 名不同仍 throw 保留真错发现能力), **libmlserverplugin.so 独立库不触 libvcmi.so 铁律** (单文件重编 -j4, 备份 .bak_pool_0906, vtest + hero3_vcmi 副本同步)。**1v3 真实首局 (双修复后)**: `ep_steps=73 r=82.38` — 与 duel 基线 75.5 同水平, 73 步守卫胜闭环完整, blue 3 英雄未速攻破防。**观察窗**: 1v3 自发经济 / blue roaming 堵路 (dyn_blocked/TOWNSTALL) / 蓝英雄击杀 / avg_r vs duel 75.5; 回退线 avg_r -30% 或挂死 >40%。**隔夜运维 (09-07 晨)**: keepalive 丢失 → VM idle shutdown 非优雅中断 (踩坑 #135) → checkpoint 回滚 546379 (损失 ~10 局); 恢复序 = 补挂 keepalive → restart_train_v5.sh (#134 transient unit 重建) → 已 active maps=8; 四坑 #132-#135 已入踩坑点, T06 课程双根因 + a1ea3f4d2d/battle-only 两章已入知识库。

**✅ 1v3 首周判据数据面达标 + 训练全面后台化 (09-07 午后)**: **观察窗爬坡** — 首夜窗 23 有效局 avg≈68.9 (含 r=4.0 战败) → 午后窗口 15 局 **avg≈80.4** (75.1~86.11) **超 duel 基线 75.5**, 前 1/3 80.0 → 后 1/3 81.8 上行, 早期战败局消失; 自发 100% / 200 步局 0% / ZOMBIE 0 / obs_nz=301 带稳定 — 回退线从未接近, 4 项判据数据面达标 (守卫胜闭环/蓝英雄击杀随窗续跟踪)。**多敌图 reward 反超 duel 解读**: 3 蓝城+5 金矿经济事件目标更多 + 蓝方 roaming 未形成压制, 非战斗变强。**运维**: 16:09 发现 unit + keepalive 双 inactive (前台终端实例死亡风险兑现) → #135 恢复序第二次实战 (keepalive PID 389 → restart_train_v5.sh → resume 555165, 损失 ~1245 步); **定论: 训练一律后台 transient unit, 禁前台终端跑法** (无存档兜底)。**统计基建**: 多 resume 日志窗口取法固化 (踩坑 #136 awk 陷阱 + 行号法), check_duel_watch.py 尾窗口径与行号窗口互补。**下一步候选**: ①72X72_02_duel 即插即用 (已备料) ②108X108 duel 可完成度观察 ③1v3 全局累计 30 局判据正式结案。

**✅ a1ea3f4d2d 部署纠偏结案 + 开机零损失恢复 (09-07 晚)**: **纠偏** — 原登记"重编 libMMAI.so"有误: 改动 `AI/Nullkiller2/AIGateway.cpp` 属 libNullkiller2.so 域, 训练栈加载 libMMAI.so (--blue_ai MMAI_RANDOM, NK2 已弃用), MMAI/ 无同构 race 代码 (grep 0 命中) → **无需重编, race 不在训练链路**; 改动保留 NK2 源码树, 回用 NK2 时重编即生效 (踩坑 #137: 登记待重编任务前双确认 target 归属 + 运行时加载链); cmake 零编译行 = 依赖未变正确信号。**开机恢复** (#135 恢复序第三次): 关机前优雅 stop 存档 564079 → 开机 keepalive + restart_train_v5.sh → resume 564079 **零损失** — 实证"优雅停 = 零损失 vs 被动死 = 回滚", 关机前主动 stop 入标准流程。**battle-only GUI 冒烟**: 四要素静态全绿 + settings 双配在 8-29 旧日志确认, 运行时冒烟待用户启动 VCMI_client.exe 打一场后验 lead by ModelAI。

**✅ 1v3 课程正式结案 (09-08, 用户拍板)**: 全局累计 88 局超结案线 (30), 尾窗四判据全达标 — avg_r 81.1 (比值 0.97≥0.8) / 自发经济 100% / 200 步局 0% / 守卫胜闭环 100% vs duel 基线 96.9% / ZOMBIE 0; 回退线全程未接近 (avg 反超 duel 基线 +7%, 挂死 0%)。**口径修正**: ①蓝英雄击杀 0 = 真实观测 (BHERO_KILL 埋点 09-08 上线已验证, T05 图 6 起, 1v3 图 blue 3 英雄未与红方交战, 终局剧本 = 守卫胜收局) ②完整胜利路径 (消灭蓝英雄+占蓝城) 无局达成 → 遗留课题。**结案后训练继续跑积累 1v3 经验, 不停训**。**下一步候选** (一次一轴, 待拍板): ①72X72_02_duel 即插即用 (已备料) ②108X108 duel 可完成度观察 ③1v3 击杀/占城数据自然积累 ④1v3 完整胜利路径引导立项 (占城奖励方案)。已归档 `已完成任务.md` (09-08 条)。

**✅ 下一难度轴拍板 + 完整胜利路径引导执行 (09-08 晚, 用户授权 AI 拍板)**: 四候选对比后选 **④ 1v3 完整胜利路径引导 (激励轴)** — ③等数据是伪选项 (剧本锁定, r 67.7~88.0 每局同构守卫胜收局, 激励不变数据只重复, T04 固化教训); ①② 地图轴留下一窗; 08-31 拍板已预授权 capture 大额方案。**实施 (全部零停训, ep_runner 子进程每局重启生效)**: ① **TOWN_CAPTURE 接 +100 奖励** (ep_runner_one.py L943-957) — 仅 T06 双图生效 (T04/T05 剧本零扰动 = 守卫胜判据对照组), 复用守卫一次性事件模式, 每城一次天然封顶 (logged 集合), countdown 重置同 [MINE]/[GUARD] 新目标语义 (占城后引导继续下一目标, 防占城后 truncation); 检测滞后 1 step (obs=上轮 nobs, L1085 同步) ② **旁路事件文件** — 主日志白名单无 TOWN_CAPTURE 且主进程改码需停训 → BHERO_KILL 同款写 battle_quality_events.log (行带 map= 自含归属) ③ **check_duel_watch.py 升级** — 数据源③读旁路 capture + per-map/对照表加占城列 + 结案面板旁路口径。**⚠ 附带发现: BHERO_KILL 空拍误报** — 上线后 9 起事件 (T05×6 + T06×3) 全部 live_slots=0 = heroes 段整段空拍 (非真实歼灭, T06 三蓝英雄同 step 56 全"消失"实锤), 空拍防护已加 (跳过差集与 prev 更新); 结案口径修正: 埋点上线后无可信击杀观测, 空拍根因 (obs 填充链) ✅ 分析结案 + 诊断埋点已部署 (09-10 停训窗): 空拍形态实锤 = strategic_state.cpp L644 先清 8 槽 id=-1 后填充循环 0 行 (段全 -1, Python id>0 全无效); 时机特征 (T06 三起同 step 56 = 守卫战斗触发点 + T05 六起, 偶发 9/数千局) → 主候选 = **战斗瞬态竞态** (obs 填充跑在 yourTurn 线程, 恰落 server 线程战斗初始化/结算窗口, 毫秒级窗口解释偶发); 诊断埋点 [HEROSEG_EMPTY] 已部署 (ep_runner L907-916, 空拍时转储 8 槽 id/owner + ah + cur_p + go 至 battle_quality_events.log) — 复发一击定位: 全槽 -1=填充跑但循环 0 行(竞态) / 全槽 0=共享内存未填充重映射 / red 在 blue 空=owner 异常 / red 也空=整段视角异常; 防护已确认正确 (跳过差集与 prev 更新), 空拍只损失击杀观测不影响训练正确性。py_compile 通过。。**观察判据 (1-2 窗)**: T06 capture 触发率非零 / 守卫胜闭环不塌 (≥80%) / avg_r 跌幅 <20% / 200 步局 ≤20%。达标后下一窗走 ①72X72_02_duel 地图轴 (一次一轴)。

**✅ capture 激励首窗复盘 + 引导断链修复 (09-09, 用户授权 AI 分析拍板)**: **首窗数据** — capture 上线后 1v3 新增 24 局 + duel 27 局, 占城 **0 触发**; 1v3 尾窗 avg 77.6 (较结案 81.1 回落 4.3%, 比值 0.95 达标) / 自发 100% / 200 步局 0% / ZOMBIE 0, 全局 112 局; BHERO_KILL 空拍防护生效 (0 新误报), check_duel_watch.py 已过滤历史误报行 (live_slots=0 不计)。**根因链闭合 (T06 最近 30 局事件画像)**: [START_HOME]×3/局 + [TOWN_VISIT]×2-3/局 (取兵链全通) + [GUARD]×1/局 (守卫首胜 +100 全拿) + [GUARD_DONE]×1/局 (100% 守卫胜收局) + **[MINE]=0 / [TOWN]=0** → 因果链 = 72X72 大图 target_list top-8 距离排序被近目标挤占 (P10 已知瓶颈) → 矿引导 obj_best 恒 None → mine_taken 永 False → **town_best 蓝城引导激活条件 (mine_taken 前置) 永假** → 蓝城引导从未启动 → capture +100 死信 (无引导信号模型永远发现不了, 守卫奖能被发现是因为守卫挡路)。**修复 (零停训, ep_runner 层)**: ep_runner_one.py L655-656 — T06 图绕过 mine_taken 门槛 (`mine_taken or _t06_direct`), T04/T05 行为不变; town 引导分支基础设施全现成 (BFS 可达性过滤 / move_stall 6 步放弃 / town_blocked 本局禁用 / countdown 重置 L735)。**预期行为变化**: 守卫胜后引导去蓝城 (~95 格单程, countdown 被引导重置不中途收局), 局长上升 + 200 步局占比回升 = capture 学习必要成本 (非退化); 攻城战败 = 英雄死局终 (死亡无惩罚遗留 → 送死成本≈0, 攻城赢局 +100 供 PPO 学习); blue roaming 拦截可能使 1v3 接战率上升 (多敌轴对抗数据)。**观察判据 (1-2 窗)**: T06 capture 触发非零 / 1v3 avg_r 跌幅 <20% / 自发经济 ≥80% 不塌 / 守卫胜闭环不塌 (≥80%)。

**⚠ 0910 重大发现 — [GUARD]/[MINE] 位置重合判据系统性失真 (停训窗专项实锤)**: ①**假糖时序实锤** (ep_418 T06 72X72_01 逐行解剖): [GUARD] guard (5,7) fought & won step 32 打印在 **battleStarted 之前** → +100 是"hero 路过静态位"假糖; 后续 battleStarted+CGCreature::battleFinished winner=1 才是真战斗 (路径上真踩怪, 与判据无关)。②**机理链** (CGObjectInstance.cpp 源码实锤): vmap objects.json x,y = `setAnchorPos` = 引擎 pos (anchor 语义); monster/hero 的 **visitable 格 = pos - getVisitableOffset()** (mask 'A' 相对位置决定) → hero 移动到 visitable 格才 visit 触发战斗; **anchor 格 (静态判据位) 与 visitable 格存在偏移** (hero mask 3x2 实测 offset=(1,0) 历史实锤) → hero 路过 anchor 格 = 与怪贴脸但不 visit = 无战斗但判据命中假 +100; hero 真战胜后站 visitable 格 → 判据不匹配漏奖。③**双实证**: ep_1386 (T05 36X36_01) hero step42 站 (10,9) 拿 +100 全程 0 引擎战斗 (诊断局复测同图 hero=(3,3) 零偏移 = 图文件无问题); ep_418 (T06 72X72_01) [GUARD] step32 早于 battleStarted。④**影响面**: [GUARD] 1442 条全 T05 时期 = 假糖为主; [MINE]/[TOWN] 同款位置重合判据 ([MINE] ==判据 / [TOWN] dist<=1 判据) 同失真风险 ([TOWN] owner 标注因两侧同 anchor 语义巧合自洽); T04 [MINE] 占矿 92% 口径存疑; r 高分构成 (T05 130-168 / T06 93.95) 中 +100 全部为路过假糖, **T05/T06 剧本解读需修正**; capture 检测 (obs owner 变化口径) 不受影响 ✓。⑤**诊断基建**: py/diag_obj_dump.py (引擎 hero/towns 实位 dump, 单图 2s) + py/stat_t06_guard_battle.sh 入库。⑥**修复方案 (待拍板)**: A. get_guards/get_objectives 坐标换算 visitablePos 口径 (mask offset 实测校正, hero=(1,0) 已实锤, monster 3x3 待实测) B. [GUARD] 判据改守卫消失 (obs 怪消失 = 战斗已胜, 与 TOWN_CAPTURE obs 口径同款) C. [MINE] 判据改矿 owner 变化。建议 B+C (obs 口径零猜错风险) + 引导目标坐标同步换算。**✅ 修复实施 (0910, 用户拍板 B+C)**: 实现取 **visitable 口径换算** (B 的 obs 怪消失口径不可行 — obs 无全局 monster 段; 源码级实锤后 A/B 融合): ep_runner_one.py 新增 `_anchor_to_visitable(o)` — 解析 template.mask 复刻 C++ `calculateVisitableOffset` 算法 (y 外 x 内扫第一个 'A'/'T' 格, ObjectTemplate.cpp L245-249: 'A'=VISIBLE|BLOCKED|VISITABLE 本体格, 'V'=可通行), `get_guards`/`get_objectives` mines 全部换算, towns 保持 anchor 口径 ([TOWN] dist<=1 与 obs towns pos 两侧同 anchor 语义历史自洽 241 次匹配, 蓝城引导去 anchor 邻格=门格语义正确)。**下游消费点全部经 get_guards 源头自动生效**: [GUARD] 判据 (hero 站真位 = visit 已发生 = autofight 胜)、守卫目标池、接近梯度、黑名单、v2 fail-count 贴脸计数 (dist<=2 真位口径); 矿引导 obj_best 同步去门格。**验证**: py_compile OK + 独立单测 (mask 算法断言 3 组全过) + 真图对照 (T05 (10,9)→(9,8) = ep_1386 假阳性位的正确真位; T06 (5,7)→(4,6) = **0909 振荡局 hero 曾站 (4,6) 即真位实锤** — 当时战斗未触发 = aggression 遗留独立问题继续专项)。**生效判据 (重启训练后新局)**: [GUARD] +100 时 hero 站 anchor−(1,1) 位 / 引导不再拉 hero 去 anchor 空格 / r 构成中 +100 与引擎战斗对齐 (局均 r 预期下降 = 挤掉假糖水分, 非退化)。

**✅ 0910 观察窗初期快照 + 36X36_02 重评估**: ①**新口径命中验证全绿** — duel 局 [GUARD] (4,6) ×2 局 (ep_2583/ep_2908 同图同 step31 = 确定性剧本依旧) = monster_3 vis 真位; 52X52_02_mir 局 [GUARD] (38,39) = 该图 vis 位 (与 [EP_TIME] map 字段互证); [MINE] ×3 正常; [EP_TIME] 基线 1.29s/步; r=153 局 (52X52_02_mir 73 步) 提示该图 r 构成大部分非假糖。②**36X36_02 重评估** (docs/方案_T05_36X36_02重评估_20260910.md): 地形压制假设**排除** (两图 0 岩石 0 水体纯草地, 布局同构); 真实差异 = 守卫减半 + **red 半场近资源 (18,5)/(5,18) 移远 (18,30)/(30,18)** → red 短目标真空 → 长局空转大负分自洽; **51 局全负数据采集于判据失真时代归因失效** → 建议第一步 02 原样回池新口径重评估 (判据: 与 01 局均 r 差异<20%), 第二步差异化重设计备料 (图 03 = 01 骨架 + gold mine 加 swordsman 6 守门 = 矿-战斗耦合课程), 图池变更与 T7.4/capture 错窗纪律。

**✅ 0910 观察窗快照复核 (29 局, py/snapshot_observe.py 可复用)**: ①**visitable 修复核心验证 PASS** — [GUARD] 26/26 全部命中 visitable 真位 miss=0 (旧口径全 anchor 假位); [MINE] 26 与 [GUARD] 1:1 = 清守卫即占矿战斗-经济闭环真实化; ZOMBIE/ENDTURN_FUSE/异常局/HEROSEG_EMPTY 全 0; 正局率 28/29。②**r 新基线不降反稳** — avg_r 141.3 中位 154.7, 36X36_01_mir 160.2 / 52X52_01_mir 164.7 / **72X72_01 avg 171.1 (单局 188.6 新高)**: 旧口径假发≈漏奖总量相抵, 修复净效应 = 时序/位置正确化非总量变化。③**capture 路径推进中** — 72X72_01 局长 73→156 步翻倍 (move_to_force=200 全程引导生效), [TOWN_CAPTURE] 仍 0, [TOWNSTALL] 18 次引导可达性问题残留, 判据观察继续。④**慢局 7/29 (24%) 被 [EP_TIME] 抓住** — 52X52_01 10.6s/步 / 52X52_02_mir 8.9s/步 (r 正常), T06 长局 3.6-4.3s/步 (大图长局天然慢混入, 需区分); 慢速样本积累启动, 复发聚合可定位。⑤特征带全独立 (215/223-224/263/301) 无混图。

**⚠ 0909 晨二次复盘 — 两项新发现**: ① **T06 引导修复未生效 (双重死锁实锤)** — 修复部署后本进程 T06 52 局 capture 仍 0 触发, 全部 73 步守卫胜收局: [GUARD_DONE] 打点实锤 **guard_done_steps=15** (守卫胜后 15 步收局, 引导只活 15 步) + **move_to_force=60 强制窗外模型不采 24** (守卫胜 step 38-58 在窗边/窗外, 蓝城引导激活了但 MOVE_TO 动作发不出去 — T04/T05 引导有效全靠 60 步强制期覆盖, 目标事件都在窗内; T06 蓝城引导天然在窗外)。修复路径 (ep_runner 内图分支覆盖, 零停训): T06 move_to_force 60→200 全程 + guard_done 15→0 (禁收局, 200 truncation 兜底); T05 全程引导失败教训的两个前提已变 (BFS 可达过滤 0901 已加 + 卡死防护 move_stall/town_blocked 现成), 且 T06 引导目标 (蓝城) 是稳态大目标非 T05 的易失效 obj_best。② **16% 局单步卡 300s (fuse 兜底特征, R7 复发中等置信)** — 本进程 199 局 32 局超长 311-398s (平均 136s vs 正常 99s), 全程均匀分布跨图 (T05×27/T06×7), 步数正常 (29-73), r 正常 (-9~158); fuse 机制确认 (strategic_env.py L861 _adventure_wait timeout=min(vcmi_timeout,300), R7 fix 900→300s) → 单步卡满 300s 被兜底。最新超长局 (ep=200) query 链打点: qid37/38 popIfTop FAIL (top=null) + MapObjectVisitQuery notify-done ✓ + **CHeroMovementQuery qid37 onExposure ENTER 后无 visit-done = 链路中断** (与 R7 三候选兼容, battleFinished RETURN 0 命中); 但 38 回收完成与 R7 完整卡死判据不完全吻合 — 需多局样本对比定位。**处置**: R7 类 = fuse 兜底非阻塞 (局不死仅慢 300s), 登记观察积累样本, 恶化再专项; T06 引导 = 修复待拍板。**✅ 慢速画像基建 (0910)**: 历史慢局样本确认已丢失 (ep 日志轮换覆盖 + ep 行无 time 字段) → ep_runner 加局耗时打点 `[EP_TIME] map= steps= secs= r= err=` (L326 t0 / L1221 打印) + 主日志白名单补 [EP_TIME] (train_wsl2_ppo_v2.py L197, 主进程改动优雅重启生效 step=599094 零损失)。**基线首样本**: 52X52_02_mir 73 步 94s = 1.29s/步正常档。复发时聚合 [EP_TIME] 行 (secs/steps 比值 × map × 时段) 一击定位慢局规律。

**✅ T06 双死锁修复执行 (09-09, 用户拍板先停训再修)**: 优雅停 (step=596306 存档零损失) → ep_runner_one.py L126-132 args 覆盖式修复 (T06 图分支: `move_to_force=200` 全程 + `guard_done_steps=0` 禁收局) → py_compile + 清 pycache → restart_train_v5.sh 重建 transient unit → resume 零损失验证 (`Loaded train state step=596306`)。实现要点: args Namespace 属性覆盖在 parse_args 后立即执行, 所有下游引用点 (L494 强制窗/L851 countdown 启动/L1132 GUARD_DONE) 自动生效, 零散改; mapname 为位置参数有默认值非 None, startswith 安全; guard_done=0 时 countdown 恒 None, 所有重置点 `is not None` 守卫自动跳过。**生效判据 (T06 新局)**: act 序列大量 24 (全程引导) / 局长 >73 步 / 无 [GUARD_DONE] 行 / 期望 [TOWN_CAPTURE] +100 首触发。**撤梯子登记**: capture 触发率稳定后 move_to_force 200→常规窗 (200 全程 = 发现期模型自主性受限, 是 capture 发现的必要代价)。

**✅ T06 双死锁修复执行 + 首局再解剖 + 取兵震荡修复 (09-09 晨, 三连修)**: ①优雅停 (step=596306 零损失) → args 覆盖式修复 (ep_runner L126-132: T06 图 `move_to_force=200` + `guard_done_steps=0`) → restart resume 零损失 (`Loaded train state step=596306`)。②**机理修正** — 读 MOVE_TO 执行块 (L764-814) 实锤**引导是动作替换制**: 引导块设 move_target (tx 非 None) 即接管, 把 a 替换为方向动作 (nd/BFS/贪心), 与 move_to_force 窗无关; act 里从无 24 (全历史 0 命中) 是因为 24 只是中间语义被翻译成方向 — 双死锁中 move_to_force 窗是伪死锁, **guard_done=15 才是真死锁** (守卫胜后 15 步收局, town_best 即使激活 15 步内走不到 ~95 格外的蓝城)。③**修复后首局 T06 duel 200 步 truncation r=28.68** — guard_done=0 生效实锤 (不再 73 步收局); 但解剖出**取兵震荡**: [TOWN_VISIT]×4 (step 67/105/145/187, garrison 每周期回满 → own_town 引导复活) 与蓝城引导反复打架, 200 步耗在往返没到蓝城 (r=28 vs 旧守卫胜剧本 78); 亮点 = 守卫胜 +100 ✓ + act 大量 3 (朝蓝城 (69,69) 方向) ✓。④**取兵震荡修复 (零停训)**: ep_runner L684-685 T06 取兵引导限次 2 (`own_town_guide_count >= 2` 禁用, 复用现有引导段计数器 L367), T04/T05 不限。**观察判据不变**: T06 [TOWN_CAPTURE] 非零 / 蓝城单程 ~95 格 vs 200 步预算 (95 移动+守卫绕路 20-40+取兵 2×15 ≈ 140-160 步 可达)。

**✅ 守卫振荡陷阱定位 + 黑名单修复 (09-09, 用户拍板执行)**: **定位 (traj_ep.json hero 位置序列解析)** — T06 duel 局 hero (1,2)→(5,7) 守卫格后 **(4,6)↔(5,7) 回跳振荡 26/60 步** (唯一位置仅 9 个, 两局 act 逐字相同 = 确定性剧本) — 战斗未触发/未胜 (无 [GUARD]/守卫未消失/英雄未死) 且排除逻辑只在 hero 恰在格上生效, 离格后 guard_best 又选中 → 往复; capture 0 的当前真因 = 守卫格振荡堵路 (取兵限次已生效, 本局无取兵震荡)。已有防护为何没接住: cycle_detect=5 八步窗抓不到 2 周期振荡 (回位 4 次<5), 位置级罚分只扣分不强改。**修复 (ep_runner 双点, 零停训)**: ① guard_best 计算循环 (L631-649) — hero 站上守卫格 (位置重合法 = 战斗已触发, 同 [GUARD] 奖口径) → `guard_blacklist.add((gx,gy))` 本局黑名单 + 计算时跳过黑名单守卫 (L331 初始化) ② 守卫接近梯度块 (L912-914) 同步排除黑名单守卫 — 否则梯度把 hero 拉回已放弃守卫, 与蓝城引导对抗成新振荡源。**效果预期**: hero 到达守卫格一次 → 黑名单 → 引导自动切蓝城 (打不赢就绕); T05 一次即胜的局不受影响 (位置重合 = 奖已拿); 第二只守卫照常引导 (per-位置黑名单)。**引擎侧遗留**: T06 守卫对象战斗未触发疑与 fix_t06_maps.py 补丁 (aggression=guard 重打) 相关, 待 capture 打通后专项 (不影响引导层绕行)。**⚠ v1 验证失败 + v2 迭代 (09-09)**: 站上格条件没接住 (hero 邻格被引擎拒从未站上, 验证局 act 逐字复现振荡) → 改 **fail-count 贴脸计数** (L654-658: guard_best 选中且 dist<=2 每步 +1, 3 步未胜黑名单) — 不依赖站上, 邻格停滞即计数。**v2 验证实锤**: T06 局 r 序列 28.68 → 30.03 → 41.80 (v1) → **93.95 (v2, 46 步守卫胜快速闭环)** — 振荡解除。知识库新章 + 踩坑 #144-#147 已沉淀 (MOVE_TO 翻译层 / 振荡陷阱 / BHERO_KILL 空拍 / 间歇性慢速)。**观察判据不变**: T06 [TOWN_CAPTURE] 非零。

### 训练期并行执行 (09-06, 用户拍板摘取上游 a1ea3f4d2d + battle-only 验证)

**✅ 上游 a1ea3f4d2d "Fix concurrency race in NKAI's" 摘取 (源码级, 重编部署留停训窗)**: 官方 patch 实锤 = 锁从 `AIStatus::removeQuery` 删除、挪到 `AIStatus::receivedAnswerConfirmation` 函数头 (纯 2 行, AI/Nullkiller 与 Nullkiller2 双文件; fork 无 NK1 目录只改 NK2)。**安全性验证**: removeQuery 全树唯一调用点 = receivedAnswerConfirmation 内 (持锁后调用, 无死锁; std::mutex 非递归), 其余容器访问点 (addQuery/waitTillFree/attemptedAnsweringQuery 等) 各自持锁 — 锁移动后无裸跑窗口。**改动已落 WSL vcmi-native/AI/Nullkiller2/AIGateway.cpp + Windows vcmi/ 树 cp 同步 (R5 防不同步, 双树 grep 'a1ea3f4d2d' 标记)**。**意义**: 本函数正是 NK2 死锁链/battle-query-hang (R7) 同区 — receivedAnswerConfirmation 此前裸跑访问 requestToQueryID/remainingQueries, 与其他线程持锁访问并发 = 真实 race 缺口 (非已含, 08-29 落后 662 提交范围内当时漏摘)。**✅ a1ea3f4d2d 重编待办结案 (09-10, 停训窗口核实)**: **libMMAI.so 无需重编 (09-07 纠偏二次实锤)** — 训练栈 `--blue_ai MMAI_RANDOM` (train_wsl2_ppo_v2.py L130), MMAI/ 目录 race 代码 grep 0 命中, 运行时 libMMAI.so 无 receivedAnswerConfirmation 符号 (NK2 域专属) → race 不在训练链路, 原待办"重编 libMMAI"属误登记 (踩坑 #137 教训再现)。**同窗发现并处置: NK2 源码树↔构建树双向分叉** — 构建树独有 TBB 防 OOM fix (C8.5 `max_allowed_parallelism=4`), 源码树独有 a1ea3f4d2d + [ML-*] 打点 + battleEnded/ring6 fix; 直接任一侧重编都会丢对方修复 → **已合并**: TBB fix 摘回 WSL 源码树 (构造函数开头 3 行) + cp 构建树 (diff IDENTICAL), Windows vcmi/ 树精确插入 TBB 3 行 (⚠ Windows 树另有引擎 API 级分叉 getCalendar/showGarrisonDialog MetaString 新签名 = 09-09 NK2 编译修复产物, 与 WSL 旧 API 各自匹配各自引擎, **不可镜像同步**只做 fix 级摘取)。三树双标记验证全绿 (a1ea=2 / tbb=1), 备份 /tmp/AIGateway.cpp.bak_{src,build}_0910。**NK2 运行时 so 未重编** (libNullkiller2.so 仍 09-02 产物缺 patch, 但 NK2 全链路无消费者: 训练 MMAI_RANDOM / eval StupidAI / GUI ModelAI) → 回用 NK2 时 (P6 对手池) 直接重编即生效, 三树源码已就绪。⚠ removeQuery 语义变化需记住 — 未来新调用点必须自带外层锁。

**✅ battle-only 场景 ModelAI 战斗 AI 选择 — 静态+运行时双验证 (09-08)**: **链路实锤** (fork 1.8 源码): battle-only 对局玩家全 AI → `CClient` L253-257 玩家 AI = red 走 `g_adventure_allied_ai` 注入或 `settings["ai"]["adventureAlliedAI"]`, 非红走 `aiNameForPlayer()` → `adventureEnemyAI` → `CDynLibHandler::getNewAI(name)` → `AI/<name>.dll` (GetNewAI 导出) — **battle-only 走 adventure AI 链而非 getNewBattleAI**; `aiNameForPlayer` 有 DLL 存在性检查 (名与 AI/ 下文件名一致才用)。**四要素全绿**: ①`ModelAI.dll` 导出 GetNewAI (CGlobalAI 工厂, model_ai.cpp L569; 战斗回调由 CGlobalAI→CBattleGameInterface 继承实现承接, model_ai.cpp L65 battle interface pure virtuals) ②schema adventureAlliedAI/adventureEnemyAI enum allow "ModelAI" (660ea59c28, settings.json L609/L614) ③运行时 settings (My Games/vcmi/config) adventureAlliedAI/adventureEnemyAI 已双配 "ModelAI" (Task7 联调遗留) ④DLL 部署在位 D:\vcmi-fork-build\bin\AI\ModelAI.dll + onnxruntime.dll + bc_model_v3464b.onnx。**运行时冒烟 (09-08)**: DLL 污染修复 (踩坑 #138: 24 种 GCC→统一 msys64 GCC 16.x) → fork VCMI GUI 启动 → main menu → lobby → 战斗模式 tab → 选图 → PlayerStartsTurn, **ModelAI 加载成功** (blue+green 均 `lead by ModelAI`, `Opening ModelAI` → `Loaded ModelAI` ×2)。崩在 AI 首轮行动 (Server gives turn to red 后 3 秒, NULL+8 空指针, StupidAI 也崩 → 非 ModelAI 逻辑); headless+testmap debugStartTest 初始化路径也崩。**关键发现**: ①官方 VCMI 1.7.5 (MSVC) + ModelAI.dll (GCC) = ABI 不兼容 (`?LIBRARY@@3PEAVGameLibrary@@EA` vs `LIBRARY`, 踩坑 #139) → 官方永远不能加载 fork AI DLL ②fork GUI 崩溃修复 = msys64 DLL 全量覆盖 + 还原 fork 特有文件 (固化四步流程) ③数据目录隔离 (踩坑 #140) 排除 Maps/Mods 为根因。**冒烟四要素静态全绿 + ModelAI 运行时加载链路实证通过 (任务原始目标)**, 游戏启动后 AI 首轮崩需单独排查 (debugStartTest / fork 自定义逻辑路径)。详见 WSL知识库 §Windows VCMI GUI 冒烟完整记录 (09-08)。

**✅ v13 战斗 onnx 数据缺口补齐 + 推理验证 PASS (09-08)**: **缺口定性** — bc_model_v3464b.onnx 实为战略模型 (metadata 无 version 键, MMAI v13 factory readVersion 直接拒绝), defender-ipnkyfqb-best3.onnx 是 38B 断链占位文本 → 全项目不存在有效 v13 战斗 onnx (数据缺口非代码缺口); 本地重导出不可行 (战斗 checkpoint 缺失) → **官方 vcmi-gym releases 成品** `defender-fqcbvmti-best7.onnx` (v1.4, 4 输入静态版, 22.4MB) 部署 ~/mmai-battle-test (models/ 目录 + mmai-settings.json 四键 attacker/attacker.siege/defender/defender.siege 全切)。**GUI 全链路两次受挫** (run1 red Elbow Room 避战 3 轮不触发战斗模型加载 — 加载时机 = red 自己开战 battleStart; run2 客户端 NK2/GUI 层 ~5min 随机 core dump, 与模型无关) → 改 **onnxruntime 直推验证** (`py/verify_v13_inference.py` 按 C++ V13::NNModel 4 输入接口喂 dummy) → **VERIFY PASS** (6 输出 shape 全精确匹配 + act0 softmax 和=1.0) — 战斗模型数据缺口正式闭环。遗留: GUI 全链路验证待客户端稳定性修复后复验; side=1 defender 跑 attacker 键已知妥协 (报错则补 v2.0 attacker-pdpyqkrb)。详见知识库 §v13 战斗 onnx 部署与推理验证。

**✅ Windows battle-only AI 首轮崩根因闭环 (09-08 下午)**: **根因 = AI DLL 缺失, 非堆损坏非模型** — `client/Client.cpp:253-257` 全 AI 玩家 (battle-only/testmap) 无条件走 `CDynLibHandler::getNewAI(settings ai.adventureEnemyAI)` 动态加载, 默认值 "Nullkiller2" 而 `bin/AI/` 仅 ModelAI.dll + BattleAI.dll (2016 官方旧文件, 非 fork 产物); build.ninja 只有 NK2/StupidAI 的 .obj 编译规则无链接目标 (fork Windows 构建从未产出任何 adventure AI DLL) → `LoadLibraryW error 126` → throw → "Disaster happened" → 崩。"Server gives turn to red 后 3 秒崩" = red(人) 首回合结束轮到 blue AI 触发; "StupidAI 也崩" = 同样无 DLL 产物, 排除 ModelAI 的结论依然成立但崩因是配置名对应 DLL 均缺失。**排查方法**: Windows 事件日志 (Application Error 三类签名: fork 0x40000015@VCMI_lib.dll / 0xc0000374 堆损坏×2, 官方 1.7.5 0xc0000409@ucrtbase fail-fast×3) → `py/analyze_vcmi_dump.py` (minidump 解析, 手动 CONTEXT x64, 实锤异常线程在 ntdll 堆报告路径但 dump 无栈内存) → **IFEO PageHeap (GlobalFlag=0x02000000+PageHeapFlags=3, 无需 Debugging Tools) + headless testmap 复现** 一击命中崩点日志。**修复 = settings.json (My Games/vcmi/config) 恢复 ai.adventureAlliedAI/adventureEnemyAI = "ModelAI"** (此前 StupidAI 复测后被清空, 文档 85 行"已双配"状态失效)。**验证 PASS**: headless testmap (Maps/Twins.h3m) PageHeap 下跑至 day=31, red/blue 回合轮转正常, ModelAI 战斗真实发生且 query 链闭合 (`[BTL-AI] battleEnded` + `[ML-q] CGCreature::battleFinished winner=1`), 修复前 3 秒必崩场景消失。**附带发现**: ①Windows ModelAI.dll = fork 自研版 (model_ai.cpp/model_infer.cpp, `MODELAI_MODEL` 环境变量指定模型, 默认 rl_model_v3464t_0829.onnx) — 与 WSL MMAI v13 factory (mmai-settings.json 四键) 是两套体系, fqcbvmti v13 onnx 不直接适用于此 DLL ②"堆损坏 0xc0000374" 判定为 throw/abort 路径次生表象 (PageHeap 全程无真崩) ③server 打点 "Attack cannot be performed!" = ModelAI 出招偶发被拒后重试成功, 非致命 ④两树 diff 实锤 R5: server/ML/ServerPlugin.cpp (HeroPool 放宽 09-06 未同步 Windows) / server/CGameHandler.cpp / client/CServerHandler.cpp (Windows 树已有 if(ENGINE) 保护) / client/Client.cpp 四文件分叉, 其中 CServerHandler ENGINE 保护 Windows 树已含 — 记忆中 "fork headless 必崩 ENGINE null" 在 Windows 树不成立, WSL 树需单独核对。PageHeap 注册表已清理。**遗留**: GUI 模式人工复测一场 (用户操作); v13 战斗模型 Windows 侧适配 (MODELAI_MODEL 指向 v13 需先核实 model_infer.cpp 接口); battle-only 链路 = 未来 vcmi-gym 战斗自对弈共享基础设施, 本次打通后 T8 战斗集成前置条件就绪。

**✅ router bug 修复版 .so 停机窗口同步 (09-08)**: MMAI router.cpp 资源路径 bug 修复版 libMMAI.so (`77840da2...`) 从构建树同步到 `vcmi-native/rel/bin/AI/` + `vtest/bin/AI/` 两处旧版 (`3a28f494...`, md5 确认 vtest 同属待同步副本), 现四副本一致 (构建树/训练目录/vtest/mmai-battle-test); 旧版备份 `~/backup-so-sync-0908/`。铁律流执行: 优雅停 (step=580111 存档, journal 确认 Stopped) → cp → systemd-run 重启 (踩坑 #134 二次复现: stop 后 systemctl start 报 not found, 且 is-active 对已消失 unit 输出 inactive 有迷惑性) → resume 无缝 (`Loaded train state (step=580111)`)。训练走 baggage 路径不触发此 bug, 行为零影响。

**✅ Windows GUI 死锁根因闭环 + ModelAI 回调修复 (09-09)**: **根因 (minidump 锁 owner 实锤)** = ModelAI heroMoved 回调 (网络线程) 启动 detached 延迟线程调 endTurn → 线程生命周期失控, 持锁路径随退出失效 → ENGINE->interfaceMutex 永久失锁 → 主线程+runNetwork 双死等。**修复** (D:\vcmi_model_ai\model_ai.cpp, 旧版备份 .bak_0908_deadlock): 移除 detached 线程 → heroMoved (无战斗) / battleEnded (有战斗) 回调内同步 endTurn (waitTillRealize=false 非阻塞) + battle_active 原子变量分流 + in_my_turn 重置防误触发; NK2 编译错误三连修 (turnCounter 声明 / getCalendar / showGarrisonDialog 参数)。**验证**: gui3 复测 `endTurn after heroMoved` 正常流转。**minidump 工具链固化** (py/ 五件套: take_dump / walk_stuck_dump / identify_all_threads / stuck4_cfbb / stuck4_heap, 见知识库)。**线程插桩** ([THREAD] 打点 ×5 处: runNetwork/runServer/progressTracking/startPlayerBattleAction/autofightingAI) 已落 vcmi/ 源码树 — ⚠ VCMI_client.exe 仍 08-18 产物未生效, 重编即带上 (复发雷达, 非阻塞)。

**⚠ GUI 复测弹窗崩溃定性 = 系统资源耗尽, VCMI 无责 (09-09)**: 事件日志实锤 — 07:14:46 Resource-Exhaustion 2004 (**3×python.exe 共 36GB commit**, 各 11.5-12.5GB) → 07:18-19 pwsh/GDEPService/agent-tool-host/dwm.exe 四进程连锁崩 + LiveKernelEvent 141; VCMI 无 WER APPCRASH、无新 rpt/dmp → 代码层无新崩溃, 复测撞上资源耗尽窗口 (dwm 崩 = 画面冻结伪装游戏卡死)。**排查方法论**: 多进程齐崩 = 资源耗尽指纹, 先查 WER (Application 1000/1001 + System 2004) 再怀疑代码 (踩坑 #149)。**待办**: ①用户确认 3×python 来源 ②干净环境再复测一场 (checklist 见知识库: 英文界面+ENG 输入法+settings AI 双配+无大内存任务并行+stderr 重定向) ③弹窗内容若再现抄录 (VCMI assert vs 系统弹窗)。

**✅ T05 全面化执行 (09-04, 方案 B 全切, 用户拍板)** — **197 ep/1 训练日复查判据终判**: ①T05 三图高分各 13/13、8/9、11/12 局 (近乎全胜 ✓✓✓) ②自发经济 **0→54 次/40ep, 20 局有自发** 含 T05 局 (✓ 兵力→战斗+100→招兵价值闭环生效, "等 R6 兑现"拍板兑现) ③avg_r 0.2~1.9 未站稳 2.0 (⚠ 口径失真: 混合轮换被 T04 短剧本 13-28 分稀释, T05 局均 130 远超标准) ④200 步局 24.4%<25% + 大负局 0 (✓) — **实质 3.5/4 达标 → 执行方案 B**: MAPS 全切 T05 三图 (36X36_01/52X52_01/52X52_02), T04 12 图退役注释存档可回退。**同批发现并修复: map= 标签造假 (踩坑 #143 事实 5)** — _mir 图 6 张未 cp 运行时路径, VCMI 加载失败不报错而 fallback 复用缓存图 → 197ep 中 "_mir 局" 全部是缓存图假数据; 补齐副本后 _mir 首次真实加载即高分 (101.75/90.35) — 此前 "镜像图泛化" 结论作废后重立为真。**当前: T05 三图全切训练中**, 观察点 = 三图剧本稳定/自发经济持续/T05_36X36_02 调守卫回归评估/T05 全面化后 avg_r 口径恢复纯 T05 (预期站上 2.0+)。

**T04 态势 (09-02 更新, P1b 部署后)**：avg_r ≈ 0 (43 集窗口, S1 撤梯子后与基线持平无退化) / [MINE] 占矿 92% (73/79) / [TOWN] 敌城路过正常 / [TOWNSTALL] 2.8 次/集良性收敛 (被拒 100% 对角步 = 回合末剩余机动点 <141 对角价但 ≥100 直行价, 黑名单改道正交兜底, 见知识库) / [TOWN_BLOCKED] 均为真围堵 (blue 英雄 camping 城门, 引导正确弃目标) / ZOMBIE ~32% (自 75% 高点回落) / ⚠ 主日志是选择性转储 — [ECON]/[RECRUITED] 必须查 /tmp/hermes_ep_*.log (踩坑 #132)。

### T7.6 取兵链路修复 (P1+P1b+P1c+P2+P3+start_home+P1e) ✅ 完成+闭环验证通过 (09-03)

- **根因 (三层, 79 集 13 取兵窗 0 效果)**: ① 招兵 dst=getUpperArmy() 落 garrison 黑洞 (城格 pas=0 visit 永不发生 + VCMI 1.8 getUpperArmy 不含 visiting hero, 踩坑 #133); ② garrison obs 恒 0 双盲; ③ 兵力检测器布局错位 (11-19 vs 真实 15-21 纯 count)
- **修复**: P1 = case16-18 招兵前 visit 块 + 邻接守卫 (distSq>2 快速放弃, 远距离不烧 2s); P1b = dst 显式 getVisitingHero(); **P1c (09-02 下午) = 邻接守卫改 visitablePos() Chebyshev≤1 口径 + 双路径 moveHero (锚点不同走 standPos / 已在锚点走对象格触发 visit) — 修 P1 锚点坐标系误杀 71/71 全弃 (踩坑 #137)**; P2 = 检测器改 15-21 纯 count 权重 [10,40,120,350,900,1600,2500]; P3 = garrison obs 填充 (T7.5 硬前置提前完成)
- **start_home 出发前招兵 (09-02 下午, 用户设计)**: ep_runner_one.py 每局开局 start_home=True — a=24 回城引导 move_town_bfs 到首个己方城邻格, 已邻接退出交引擎 P1 visit+招兵接管; 无己方城/超 80 步放弃; 冷却保持防 spam (初版误清冷却 bug 已修)。确定性前置取兵, 不依赖模型自发回城
- **09-02 深夜真因链闭环 (owner 嫌疑已消解, 21 次全弃 ≠ 选城错误)**: obs 城段 owner 语义证无罪 (owner=0=red 语义正确, 与地图数据吻合); 真因四层: ① bfs_full_dir 城格目标结构性不可达 → 贪心裸奔徘徊 fuse (已修: 城格目标降级 8 邻可站代理目标, 拍 1 验证通过); ② P1c visitableOffset 口径死锁 (P1d 逼近走修复); ③ P1d-v1 两缺陷实测: x 向候选格恒被挡 → 单轴退化只走 y (tv.x=0 vs hero x=27 永不收敛) + 回合末移动力<100 moveHero 静默失败 (void 无反馈) → hero 原地打转 8 连弃; ④ **P1d-v2 (09-02 深夜已部署): 抛弃手工单步逼近, `cb->moveHero(cur, tp, false)` 引擎寻路直走 门格** — 多格推进/移动力尽停半路/下拍续走, 踏上门格即 visit (tp=town->visitablePos()=真门格)
- **P1e→r2→r3 (09-03, approach 分支重写, 闭环验证通过)**: P1e = approach 分支 PathfinderCache 门格 8 邻选中间格 + CGPath 逐点推进 + 尾补 standGoal 兜底; r2 部署后实锤招兵链路端到端全通 (两局 avail 28→27, dstIsHero=1 兵上英雄), 但同时实锤 CGPathNode 格子坐标直喂 moveHero = **幽灵 x-1 偏移** (plen=4 只推进 2 格, 第 3 步出图中断; 根因 CGameHandler.cpp L874 `convertToVisitablePos(dst)` — moveHero dst 是锚点坐标, CGPathNode 是格子坐标, offset=(1,0)); r3 = path 节点入队前 `convertFromVisitablePos` (格子→锚点) + 起点跳过改 `convertToVisitablePos(cur->pos)` 格子口径 — **plen 4→3, 单拍直达门格** heroPos=(1,3) visited=1 同拍招兵, 取兵四拍链压缩为 approach→招兵两拍; .so md5 5872b453 (备份链 .bak_p1e_r2), 镜像 tools/patches_20260902/ 同步; 判别法: approach2P plen=3 + 下一拍 DIAG6 heroPos=门格即 r3 生效
- **坐标三实锤 (09-02 P1d 日志, 详见知识库)**: hero appearance offset=(1,0) **非 (0,0)** (双数据点交叉确认: heroPos=(3,4)/hv=(2,4) 与 (27,29)/(26,29)); town offset=(2,0) (城锚点 (2,3) → 真门格 (0,3)); P1c 主路径 standPos 语义在真 offset 下自洽, 缺陷仅限 approach 分支
- **runner 三态诊断增强 (09-02 晚)**: ep_runner_one.py start_home 块 — `begin step=0` 首拍 8 城段全转储 (id/owner/x,y, 坐实 owner 语义) + `abort(no_own_town|timeout>80)` 带原因与城段转储 + `adjacent`/`guide` 保留; train_wsl2_ppo_v2.py 主日志白名单加 `[START_HOME]`/`[RECRUITED]` (防 hermes_ep 逐局覆盖丢失, 踩坑 #132 关联)
- **部署**: libMMAI + libmlclient 重编, 4 副本同步; 备份链 .bak_taketroops_0902_0433 → AAI.cpp .bak_p1d_0902(P1c) / .bak_p1dv1_0902(P1d-v1) → .so .bak_p1dv1_0902(P1d-v1); C++ 补丁镜像 `tools/patches_20260902/` (含 P1c+DIAG7+P1d-v2); 机制详解见知识库 P1/P1b/P1c/P1d 条
- **待闭环验证 (四拍判据链)**: ① `[START_HOME] adjacent` (hermes) → ② DIAG7 `approach2 from/to/goal` 续走至 `afterMove visited=1` (/tmp/rl_recruit_diag.log; ⚠ 重启须显式清空 — 09-02 实测未清混入旧 .so 残留 88 行, 判别法: 有 enter 无 guard=P1c, 有 approach to=P1d-v1, 有 approach2=P1d-v2) → ③ `dstIsHero=1` → ④ `[RECRUITED]` (hermes) — 四拍全中即全链贯通; 一键 `py/monitor_recruit.sh`; 确认后删 AAI.cpp 临时诊断 (标注"验证后可删")
- **遗留观察**: 死亡局正收益问题 (ZOMBIE 局 r=+50~+93 vs 超时局 r=-18~-107, 死亡无惩罚 → "拿完 +30 送死止损"梯度) — 待决策死亡惩罚方案 (T7.4 关联)

**前置准备（08-29 完成，零干扰）**：s2a 经济奖励编码 / s2b 动作合法性掩码 / s2c 超参注释包 / s2d T04 地图池注释块 / s2e economy_force 切换点——全部条件开关设计（详见 `已完成任务.md`）。

---

## 并行主线：T6 NK2 逻辑融合（训练验证中）

- T6.1 NK2 估值→奖励塑形 ✅ 冻结 (08-22)，v5 实跑中。
- T6.2 C++ BFS 全图寻路 ✅ 代码完成 + T03 段毕业 (08-29)，v5 实跑 (step≈212k, MAPS=T04×6)。
- T6.4 训练地图体系 (VMAP T01-T06) 🔄 Level 3 训练中。
- T6.3 决策结构分层 ⬜ 远期（与 T5.9 合并）。
- 注：v5 = T6.1 + T6.2 + T7.3 三者叠加训练中。

---

## 待办任务总表（A/B/C/D 档，标注是否影响训练）

> 影响训练判定：是否触碰 WSL 侧 rel/ 运行时 .so、训练进程/日志、GPU/显存、vcmi-native 工作源码。

### A. 立即可做（零影响：Windows 侧 / 纯只读）
> A1-A4（S5 健康复核 / A Viking 战斗验证 / 1.7.5 MSVC 重编 / MOVE_TO DLL 执行器）✅ 全部完成 (08-29 晚) — 详见 `已完成任务.md`。当前无待办，新的零影响任务追加于此。

### B. 设计文档类（零影响：只写方案）
> B1-B4（解封方案 / 经济上线参数 / 熵 bonus / 晋级判据校准）✅ 方案全部出稿 (08-29) → `docs/方案_B1-B4_20260829.md`；实装在 C1 / D1 / D2 / D3 触发。当前无待办。

### C. 谨慎（需隔离设计，有资源/文件重叠）
| # | 任务 | 归属 | 影响训练? |
|---|------|------|-----------|
| C1 | 晋级判据自动化 eval | T7.1 | ⚠ 会抢 GPU/CPU；CPU-only + 低频 (≥2h) + 限 batch |
| C2 | 重编崩溃根因插桩 | T6.2 | ⚠ 分析可，部署禁止 |
| C3 | CGameHandler garrison 自动合并 | T8.1 | ⚠ 触碰铁律；除非断言复发否则搁置 |
| C4 | NK2 寻路加速 merge (+40% 吞吐) | T6.2 | ⚠ 动 libvcmi.so，训练期禁 |

### D. 等待条件触发（不在当前窗口做）
| # | 任务 | 归属 | 影响训练? |
|---|------|------|-----------|
| D1 | 经济动作实际上线 | T7.3 | 🔴 改 ep_runner，须停训窗口 |
| D2 | 30x30/36x36 实际解封 | T7 | 🔴 需重启训练 |
| D3 | 开局熵 bonus 实装 | T3 | 🔴 改训练脚本 |
| D4 | 2v2/多人对局验证 | T10 | ❌/⚠ fork headless 不影响，GUI 验证需用户在场 |

**建议顺序**：A/B 档已清空；活跃窗口 = C1（eval 自动化，视训练稳定度）→ D 档等触发（D1 经济深化 / D2 解封 / D3 熵 bonus）。

---

## 训练期并行任务（P，不碰运行中 v5 进程）

| # | 任务 | 类型 | 状态 / 依赖 |
|---|------|------|------------|
| P1 | 训练空转根因 (断言雷) 已修复 | 解锁型 | ✅ 完成 (08-29) — libMMAI.so 换 13c0f041, libmlclient 保持 5d2b9e9d；冒烟 2 战斗 0 断言 |
| P2 | 晋级判据自动化 eval 脚本 (py/eval_promo.py) | 备料型 | ✅ 完成 (08-29) — CPU-only，不抢训练资源 |
| P3 | 30x30/36x36 守卫距离解封（代码就绪待激活）| 解锁型 | 🔧 待激活 — `--guard_grad_scale_by_map` |
| P4 | ZOMBIE/GUARD all-blocked 事件聚合分析 (py/analyze_deadlock.py) | 备料型 | ✅ 完成 (08-29) |
| P5 | 开局确定性对策 (entropy 调度) | 备料型 | ⏸ 等自然重启窗 |
| P6 | 对手池 self-play/NK2 混合 | 备料型 | ⏸ 等 20x20 首胜稳定 |
| P7 | 真实游戏战略层联调 (fork 1.8 DLL) | 铺路型 | ⬜ 低强度并行 |
| P8 | 人机对战底座 (大厅 1V7 配置) | 铺路型 | ⬜ 远期 |
| P9 | done 信号接线 — 核实已存在无需开发 | 备料型 | ✅ 误报排除 |
| P10 | H3M 官方图 → VMAP 转换 (h3m2vmap) | 备料型 | 🔧 A 步实测 (08-29) |

### P10 H3M 官方图 → VMAP 转换（A 步实测结论）
需求：用官方 H3M 图真实复杂度做地形骨架，降低守卫强度，按官方复杂度渐进。定位：备料型，不碰在跑的 v5 训练。
- A 步实测：① 72~144 尺寸可塞进 21×21 obs（fill_terrain_grid 以英雄为中心 10 格局部窗）；② 官方图物体密度不爆槽（白名单 45 vs 自研 48 同量级）；③ H3M 属性需自建转换入口（无命令行）；④ 水域/船已规避。
- ⚠ 真瓶颈：fill_target_list 排序 = 纯曼哈顿距离 top-8，无价值/威胁加权 → 最近 8 目标大概率全是被强守卫堵住的资源 → 模型看不到可行路径。"减少守卫"真实作用 = 释放 top-8 槽位让可行目标可见（守卫系数 = 天然课程轴）。
- 选图：`Knee Deep in the Dead.h3m`（36×36 无地下, ROE, 2 玩家, 城需删/封）。
- 实施序：A ✅ / B 转换器 🔧 B1 ✅ (09-01)（设计稿见 `docs/P10-B-h3m2vmap转换器设计稿.md`, 08-31 接口梳理+改写规则 R1-R7+待验证 V1-V6；工具源码 `vcmi-native/tools/h3m2vmap/`, Windows 副本 `tools/h3m2vmap/`）/ C 改写规则 ⬜ / D 验证链 ⬜ / E 入池 ⬜。禁覆盖 rel/ 下 .so。
- 旁路 ✅ vmap2h3m 反向转换 (09-01)：`py/vmap2h3m.py` (SOD, 5 类对象特征集)，h3m_tool strict 对账 skipped=0 + 引擎 `h3m2vmap --check-h3m` 实测 ENGINE LOAD OK；V1 (cb=nullptr) 实测定论可用，modName 须传 "map"。详见设计稿 §8。

---

## 风险登记（R，引擎侧）

| 风险 | 机制 | 状态 / 对策 |
|------|------|------------|
| R1 动作拒绝不可感知 | server 拒绝只 complain，AI 侧无回调 | 🟡 已缓解未审计 — Python action mask 在用 |
| R2 done 信号 — 核实轮询链已通 | 终止靠轮询链非事件回调 | ✅ 误报排除 |
| R3 异步时序竞态 | 动作靠 waitTillRealize 同步化 | 🟢 约束型 — 新增动作走 waitTillRealize |
| R4 queryID=-1 断言雷 | 战斗结束攻方 queryID=-1 触发 ASSERT | ✅ 已修 — libMMAI.so 13c0f041 部署 |
| R5 双源码树不同步 | Windows vcmi/ 与 WSL vcmi-native 不同步 | 🟡 长期隐患 — 源码级改动前 WSL grep 确认 |
| R6 战斗 AI 恒 fallback StupidAI (08-31 根因定位) → **✅ 已修复 (09-03)** | MMAI CMakeLists `add_definitions(-DUSING_ONNX=0)` 硬编码 + 无 onnxruntime 依赖 → CreateBAI 返回空指针 → router 断言 → 红方战斗恒 StupidAI。**修复前全部训练成果 (T03/T04) 均在此条件下取得** | ✅ 修复完毕 (09-03): ①onnxruntime 1.19.2 linux-x64 (与 Windows 树同版) 部署 /opt/onnxruntime + ldconfig (WSL 断网 → Windows curl 下载 tgz 经 /mnt/d 安装, 踩坑 #141) ②CMakeLists USING_ONNX=1 + target_link /opt/onnxruntime (复用 Windows 树查找段布局), 双树同步 ③停训窗口重编 libMMAI 一次通过 (21.5MB), ldd 解析 OK, vtest 副本同步 ④resume 验证: **战斗实测发生 (ep_11638: 三层 query 栈 movement93→visit94→battle95 与 R7 机理链一致) 且 Router fallback 零出现 (修复前每战必有), V13::BAI 接管; R7 埋点实战首通 (notifyObjectAboutRemoval ENTER hasResult=1 objType=town → battleFinished RETURN winner=0 攻方胜, 回收链闭合)**。⚠ 战斗强度变化 = 非平稳 (课程节点一次付, 战略层 act 序列不变已验证) — 后续观察 avg_r/胜率波动; 经济塌方预期随兵力可兑现回归 (09-03 拍板)。PATH 模式 (C++ 内 onnx 推理) 模型文件 bc_model_v3464b.onnx 未部署 (MMAI_USER 训练走 Python 回调不需要), 届时部署到 VCMI AI_MODEL 资源路径即可。备份: backups/so_bak_20260831/。**✅ R6 定向冒烟完成 (09-03 晚, 决策回路闭环)**: T03 守卫图定向冒烟 (T03smoke_adventure_20X20_bai, 守卫 3→8 均势) — 战斗触发 (objType=monster) + 守卫被消灭 (removeObject) + winner 确定性结算 + query 链闭合 = **VCMI 战斗循环 (双方 AI 出招) 完整执行** (战斗在 server 内部同步毫秒级完成, 日志只有 battleStart/Finished 打点, 13 行间隔≠跳过)。附带修复: **AAI.cpp:396-401 if 作用域缺陷** (P1b 引入: fprintf 单语句抢走 if 作用域, recruitCreatures 无条件对所有 tier 执行, 空 tier second.front() = UB → 冒烟图内存布局不同即 segfault libMMAI.so+0x49948 runNetwork; 训练 96ep UB 潜伏未爆) — 加花括号+break 对齐 16/17, 重编部署。新发现: ①core:footman 在当前 fork 缺失 (T05 全部 4 图加载失败实锤, gen_level3 含 footman 守卫同样不可用 — T04 实际 0 守卫的历史成因) ②VCMI moveHero 到敌英雄格被 "destination blocked" 拒 (英雄战不由此触发, T04 win"消灭对方英雄"的达成机制待查) ③冒烟图运行时路径 = rel/bin/data/Maps/ (vmap 需复制到此, Maps/training 不是运行时路径) |
| R7 battle query 卡顶 — 09-03 立项排查完毕 (根因定性, 断点精确定位) | **机理链闭合**: 移动踩守卫/野怪 → 栈叠三层 movement111→visit112→battle113 (ep_4040 实锤); moveHero/visitObject 末尾收尾 popIfTop FAIL×2 = 原版正常 (静默靠 onExposure 链回收); 战斗结束 endBattleConfirm → removeQuery(battle113) → visit112.onExposure (ENTER 打点实锤到达) → **notifyObjectAboutRemoval 内部中断** (BattleQueries.cpp:38-41, `[ML-q] CGCreature::battleFinished` 打点缺失 = 未走到或非 CGCreature 对象) → visit112/movement111 残留 → RED 全部动作被拒 (含 endTurn, "has to answer queries") → 回合系统死等 s_turn_player → adventure_wait_for_turn 永旋 → 冻结。**已排除**: restartBattle/L86 result 清空 (RED isHuman()==false → dialog 不创建不可达); heroGotLevel queryID=-1 (升级 fix AI 自动选技能, 无害)。**三候选待埋点区分**: result 空跳过 (assert NDEBUG no-op) / visitedObject 非 CGCreature / battleFinished DEFENDER 分支 merge stacks 死循环或 hero 死亡悬空指针 (CHeroMovementQuery 持 hero 裸指针)。**Router dynamic cast 失败 (router.cpp:442) = 并存独立异常** (战斗质量 issue, 高频复现最新 ep 仍有), 非 query 链卡死直接成因 | 🟡 埋点已部署待复发捕获 (09-03 五件套执行完毕) — ①埋点增强已部署: notifyObjectAboutRemoval 打 hasResult+objType getTypeName+battleFinished RETURN winner / MapObjectVisitQuery.onExposure 补 qid+notify-done / CHeroMovementQuery.onExposure 补 qid+visitDestAfterVictory+visit-done / popIfTop OK 成功打点 (验证 ep: OK×77+onExposure 带状态, 链路正常); ③fuse 900→300s 已实施 (strategic_env.py:861, ep_runner 看门狗表述更正为缩短 fuse 上限); ⑤AAI.cpp 六处 [AAI DBG] fprintf 已清 (保留 selectionMade); 双树已同步+mlclient/MMAI 重编部署+训练 resume (step=442907) 验证通过。**下次 battle query 冻结复发时新打点一击定位三候选**。②removeQuery 空 result 防御未做 (埋点 SKIP 行已覆盖诊断)。④Router cast 失败专项已随 R6 修复解决 (09-03, 见 R6 条目)。venv 注意: /mnt/d/.../venv 不存在, 实际 /home/administrator/vcmi-workspace/venv (systemd-run 需绝对路径, 踩坑 #139)。**部署后验证 (09-03): popIfTop OK×86 零 FAIL 新增; f7 四拍全通 (TOWN_VISIT→引擎 visit=1/dstIsHero=1→[RECRUITED] 兵力 +10/+40 进英雄→act 18/16/17 轮换, monitor_recruit.sh 复核); f10 首轮 10 ep × 6 图零复发; 09-03 阶段性结案: 埋点部署完成 + 无复发, 转长期观察 (复发时按知识库区分表定位), 精力转回 T7.5/经济主线。知识库已沉淀 (R7 章节 + 踩坑 #139/#140) |

---

## 远期 / 阻塞集

- T5.9 v2 MultiDiscrete 三维参数化（与 T6.3 合并）⬜ 远期
- NK2 寻路加速 merge（官方 #7632, +40% 吞吐）— 训练稳定后评估（→ C4）
- MMAI v15（PR #7654）— 未合并官方，2-4x 慢；合并后需重采集（→ T8）
- **战斗模型训练（条件触发, 08-31 登记）** — **当前不需要**: R6 启用现成 bc_model_v3464b.onnx (v13) 零训练成本; T04 战斗频率低非瓶颈。**触发条件 = T05 对抗图中接战胜率成为战略层瓶颈** (战略决策合格但战斗打不赢)。路径 = v13 战斗自对弈 (vcmi-gym battle 环境 + BC 权重热启动 → PPO 强化, 基础设施现成); v15 GNN 战斗模型 (rl/v15 框架已备, 无权重) 绑 T12。⚠ 战斗模型升级 = 战略环境非平稳, 勿在课程中途做
- 动作码 16-23 内政启用 — Level 3 切入时先解决 noTarget 惩罚
- fogOfWarMap 语义确认（visible vs explored）— T5.4 顺带
