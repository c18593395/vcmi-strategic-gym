# 当前任务清单（当下窗口要完成/解决的任务）

> 📖 **09-11 T13 实施增量**: VCMI 网络协议调研 → T13 外挂 AI 架构立项并实施主体完成 (T13.1-T13.9 ✅, T13.10 🟡 Lobby/P8-A 前置完成)。新增 `py/vcmi_protocol/` 外挂协议栈, 离线 `test_e2e.py` 144 项全 PASS; 剩余 P8-B/C/D 实机多人局验证。踩坑 #175-#185 已沉淀。
> 📖 **09-11 mq 模型部署线闭环增量**: v5 全模型部署链全通 — checkpoint 体检 → `rl_model_v5_0911.onnx` 导出+探针对拍 (maxdiff 1.4e-6) → ModelAI C++ v5 适配全量落盘 (ObsBuilder 3464 双输入 / ModelInference 按名定位+shape 防御 / PpoModelAI AAI 模式移植 25 动作+延迟 endTurn) → 重编部署 → 实机 1v7 验证 PASS (7 AI 模型加载 / action 采样多样化 5-7 / moveHero 真实执行且 N-start CW 方向语义严格吻合 / day1→4 无卡死)。**关键发现: policy 平坦 (top1-top2 差 0.1~0.3), 部署须采样非 argmax** (argmax 恒 5/6, 踩坑 #196), 训练采样行为多样但 argmax 单峰 — 根因已沉淀 (#196 平坦/采样 / #197 日志通道 / #198 N-start CW 方向表, 知识库新增「mq 模型部署线闭环」章), 训练提质待观察。
> 📖 **09-11 C4 #7632 归档增量**: NK2 寻路加速 merge 完成 (停训窗大改, native `1c580aee3`+`0a44db3b9` / workspace `825a40b8e7`), 训练 resume step=621725 重启, 首窗观察中。踩坑 #178-#184 新增 (ASSERT 宏 namespace 陷阱 / <ranges> 显式 include / ResetInfo API 映射 / 副本三目录不在链路 / T05 小图 +15% 变慢预警)。知识库新增「C4 #7632 执行记录」章。
> 📖 **09-11 C 方案上线增量**: T06 duel 蓝英雄死亡 = TOWN_CAPTURE proxy (+100, 仅 duel 图, 每局一次, ep_runner L410/L940-958) — 停训窗大改 (transient unit 三次复现 → `py/restart_train_v5.sh` 重建, resume step=629167), 首局验证 PASS (duel step 95 蓝英雄 [1] 死亡 → +100 hero-kill proxy, 无双记)。踩坑 #195 新增, 知识库新增「T06 duel 蓝英雄死亡 = capture proxy」章。
> ⚡ **下次会话第一步 (0911 C4 闭环后更新)**: C4 首窗观察已闭环 (活跃任务 8, 20 局零崩溃, 判定本负载无 +40% 收益, 用户拍板保留新栈)。观察窗回到 **capture +100 激励验证期** (活跃任务 1, 0910 三重修复: [GUARD]/[MINE] visitable 修复 / BFS blocked 统一 / T06 双死锁修复; **09-11 C 方案上线: T06 duel 蓝英雄死亡 = capture proxy, 首局 PASS**) — 跑 1-2 窗/攒 ~40 局后聚合 [EP_TIME]/[GUARD]/[MINE]/capture 判据 (T06 [TOWN_CAPTURE] 非零 / avg_r 跌幅<20% / 守卫胜≥80% / 自发≥80%, r 基线 141.3) → 届时拍板 36X36_02 原样回池 (docs/方案_T05_36X36_02重评估_20260910.md) 与 T7.4 死亡惩罚实装 (docs/方案_T74_死亡惩罚_20260910.md, 与激励轴错窗)。R7 battle query / [HEROSEG_EMPTY] 空拍埋点长期观察。训练恢复命令：
> `wsl bash -c "systemctl --user reset-failed homm3-train-v5 2>/dev/null; systemd-run --user --collect --unit=homm3-train-v5 --working-directory=/mnt/d/Bigdata/hero3_fresh /bin/bash -c 'exec /home/administrator/vcmi-workspace/venv/bin/python train_wsl2_ppo_v2.py >> train_loop.log 2>&1'"`
> 另起 Windows keepalive: `Start-Process -WindowStyle Hidden wsl.exe -ArgumentList 'sleep infinity'`（防 idle shutdown, 踩坑 #114；0911 已另起）。重启用 `py/restart_train_v5.sh` (transient unit 重建)。训练已跑 (0911 C4 重启), 以上命令仅在 unit 失效时再用：

> 父文档：`总任务.md`（整体脉络，大项 T1-T13）。本文件 = 当下要完成/解决的任务清单：① 当前主线大项详细任务；② 待办工作台 A/B/C/D 档；③ 训练期并行 P；④ 风险登记 R。完成即移入 `已完成任务.md` 并回写总任务状态。
> 编号：与总任务 T 体系一致；A/B/C/D/P/R 为本文件工作台视图分类标签（非大项编号），明确归属某大项的标注 T 编号。
> 脱钩：本文件只写任务，不引用知识库/踩坑点（技术细节见对应设计文档）。
> 📌 0910 梳理: 09-01~09-10 已完成明细已归档 `已完成任务.md`（置顶 8 组日期条目），本文件只留活跃任务/观察判据/待拍板事项。

---

## 当前主线：T7 城镇经济

### 已完成子项（详情见 `已完成任务.md`）
- T7.1 Level 3 晋级 ✅ (08-29) / T7.2 切 T04 地图 ✅ (08-29) / T7.3 开经济 ✅ (08-29)
- T7.6 取兵链路修复 ✅ (09-03, 四拍闭环验证通过: [START_HOME]→visit=1→dstIsHero=1→[RECRUITED]; start_home 出发前取兵常态化)
- 撤梯子 S1 + garrison obs 填充 ✅ (09-02, towns.garrison[7] 填充提前完成, 不违 3464 冻结)

### 🎯 活跃任务 1: capture +100 激励观察窗（当前窗口核心, 09-08 上线）
**背景**: 1v3 结案后完整胜利路径 (消灭蓝英雄+占蓝城) 无局达成 → capture +100 激励 (仅 T06 双图, T04/T05 零扰动) + 蓝城引导修复链 (mine_taken 门槛绕过 / 取兵限次 2 / fail-count 贴脸黑名单 v2 / 双死锁修复 / BFS blocked 统一)。
**当前状态**: 0910 快照 29 局 — visitable 修复验证 PASS ([GUARD] 26/26 真位命中, [MINE] 与 [GUARD] 1:1 闭环真实化), 72X72_01 局长 73→156 步 (move_to_force=200 生效), [TOWN_CAPTURE] 截至 0910 为 0 → **09-11 C 方案上线后首条 duel proxy 记录 PASS** (T06_adventure_72X72_01_duel step 95 蓝英雄 [1] 死亡 → +100 hero-kill proxy, 见下方 C 方案事实块); TOWNSTALL 18 次引导可达性残留, 慢局 7/29。
**观察判据 (1-2 窗)**:
- T06 [TOWN_CAPTURE] 触发非零（核心判据）
- 守卫胜闭环不塌 (≥80%)
- avg_r 跌幅 <20% (当前基线 141.3)
- 自发经济 ≥80% 局不塌
- 200 步局 ≤20%
- C 方案上线后 (09-11): 攒 ~40 局 (duel + 1v3 混跑) 再聚合上述五判据
**撤梯子登记**: capture 触发率稳定后 move_to_force 200→常规窗 (200 全程 = 发现期模型自主性受限, 是 capture 发现的必要代价)。
**达标后**: 下一窗走 72X72_02_duel 地图轴（一次一轴）。

**g3b 事实块 (09-11 只读取证, [GUARD_DONE] 早停 + TOWNSTALL + target_list 排查链, 全部代码+日志实锤)**:
- **日志级别矩阵 (核心发现)**: ep_runner L330 传 `vcmienv_loglevel="ERROR"` → strategic_env L883 WARNING "timed out after 300s" 与 L766 INFO "Episode done" 均被抑制; 唯一能进 ep 日志的是 L725 ERROR "adventure_wait timed out: … — forcing episode end", 历史 ep 日志无此条 → **9/10 历史 T06 局判真实 game_over, 非 300s 超时 forcing** (纠正"旁证指向超时 forcing"推断)。但 L1182 终局静默 + train 日志高亮词表无超时词 → 超时 forcing 仍是**双重失明隐藏终止源** (触发即无人可见)。
- **NK2 无末步 ±200**: `_calc_reward` L1057 提前 return (NK2 势差分) → r=135.4 在 timeout/go=1/go=2 三场景均可凑出 → **末步 r 无终局方向判别力**。
- **干净只读判别器**: obs 无 game_over 通道; players 段 base=8、每玩家 15 字段、alive 为第 12 个 → **红 p0 alive = obs[19], 蓝 p1 alive = obs[34]**; C++ 规则 alive_count≤1 → game_over=last_alive+1: 双 0 = timeout forcing (全零 obs); 19=1 & 34=0 = go=1 红胜; 19=0 & 34=1 = go=2 蓝胜; 双 1 + 无 terminal = 200 步截断。
- **battle_quality_events.log 死路已封**: 40 条 T06 HEROSEG_EMPTY 全 `go=0 slots=0/0 ah=0 cur_p=0` = 观测瞬态空拍 (非 terminal 记录), 不可用于胜负方向推断。
- **T06 override 实锤 (ep_runner L130-132, 0909 双死锁修复拍板)**: 实际 T06 局 = `move_to_force=200` (全程 forced 24) + `guard_done_steps=0` (GUARD_DONE 禁用) — **非**交接 spec 写的 15/60; 探针保真口径按 override 后参数。
- **只读路线已穷尽**: ep_runner traj 无 terminal 字段 (仅 obs/act/rew/nobs/done) + 9/11 T06 traj 已被 52X52 (pid 45100) 覆盖丢失 → 方向定判靠探针运行实证。
- **探针 (已写不跑, `py/probe_t06_gameover.py`)**: 镜像 ep_runner L328-342 构造 (T06 训练参数 + `vcmienv_loglevel="INFO"` 让被抑制行可见 + `vcmi_timeout=900` fuse 300s); `--move24` (镜像 override 全程引导, env 引擎侧解析最近目标, 不复制 Python 粘滞链) + 自由 greedy 双轨; 终局 dump (terminated/truncated/game_over/timeout/nobs[19]/nobs[34]) + 判别 + players obs[8:43] sanity; traj 持久落 `py/probe_t06_traj.json` (D: 盘)。执行 = 错窗: 停 v5 → duel + 1v3 各 1-2 局 (T06_adventure_72X72_01_duel / T06_adventure_72X72_01) → 重启 v5。
- **g4 待办 (错窗拍板后)**: `docs/方案_T06终局方向定判_*.md` — GUARD_DONE 推断修正 / 300s 超时隐藏终止源 + 日志矩阵 / NK2 无 ±200 / alive 判别器 / battle_quality 死路 / T06 override 实锤 / target_list P10 合并立项。

**C 方案上线事实块 (09-11 停训窗, 用户拍板 A/B/C/D 四方向选 C)**:
- **根因**: duel 型 T06 图蓝英雄唯一, 一死即 game_over 终局 → 蓝城 (69,69) owner 翻转结构性死信; 原始 TOWN_CAPTURE 仅 1v3 图生效 (3 蓝英雄 / 3 蓝城, c2 确认)。
- **修复 (ep_runner_one.py 两处)**: L410 `_t06_hero_kill_capture` 标志 (每局一次) + L940-958 proxy 块 (插入空拍与 BHERO_KILL 分支之间, 蓝英雄 id 差集在 prev 更新前计算, duel 限定 +100, 无双记); 1v3 保留原口径, 监控 check_duel_watch.py 零改动。
- **部署**: 优雅停 v5 (transient unit 坑 #195/#168 三次复现, 重启走 `py/restart_train_v5.sh`) → 清 __pycache__ → resume step=629167 → `grep -c '_t06_hero_kill_capture'` = 3 在位确认。
- **首局验证 PASS**: `battle_quality_events.log` 首条 `[TOWN_CAPTURE] map=T06_adventure_72X72_01_duel.vmap blue_hero_killed=[1] at step 95 +100 (C: hero-kill proxy)`; 已跑 step 629638 avg_r=2.1, T06 1v3 r=156.53/steps=114, 零崩溃零告警。
- **待办**: 攒 ~40 局聚合观察五判据 (见上); 达标后按错窗纪律走 72X72_02_duel 地图轴。

### 活跃任务 2: 36X36_02 原样回池评估（待触发, 与图池/激励轴错窗）
- 第一步: 02 原样回池, 新口径 (visitable 修复后) 重评估, 判据 = **与 01 局均 r 差异 <20%**; 方案文档 `docs/方案_T05_36X36_02重评估_20260910.md`
- 第二步备料: 差异化重设计 (图 03 = 01 骨架 + gold mine 加 swordsman 6 守门 = 矿-战斗耦合课程)
- 历史归因失效说明: 51 局全负数据采集于判据失真时代 (假糖), 地形压制假设已排除 (纯草地同构布局), 真实差异 = 守卫减半 + red 半场近资源移远

### 活跃任务 3: T7.4 死亡惩罚实装（设计稿已出, 待窗口）
- **设计稿 `docs/方案_T74_死亡惩罚_20260910.md`**: 引擎判负不可达实锤 (死亡局 game_over 恒 0, -200 轮不到) → ep_runner zombie 确认点 (L1152) 追加 `traj["rewards"][-1] += death_penalty` (-50 试探档, 上限 -100 对称守卫, argparse 可调含 0=关闭); 全局生效
- **观察判据**: 死亡局 r 转负 + [GUARD] 接战率不塌 (±20%) + avg_r 跌幅 <20%
- **实装时机 = capture 观察窗达标后下一窗**（激励轴不同窗纪律）
- 关联遗留: 死亡局正收益问题 (ZOMBIE 局 r=+50~+93 vs 超时局 r=-18~-107, "拿完 +30 送死止损"梯度)
- 引擎侧 standardDefeat 判负根修 = 独立课题（非本任务前置）

### 活跃任务 4: T7.5 经济奖励撤梯子 S2-S4（节奏需重估）
- 背景: S2 曾因自发经济塌方暂停 (09-03 拍板"等 R6 战斗修复") → R6 已修复 (09-03) + 自发经济已回归 (09-04, 0→54 次/40ep) → **S2-S4 推进节奏待重新拍板**
- 每步判据: avg_r 跌幅 <20% + 招兵/建设频率不塌 + 取兵窗维持

**📋 撤梯子③ 多步渐进方案 (08-31 定稿, 递归半价 + 效果奖渐入, 每步 1-2 窗观察)**:

| 步 | 触发 | RECRUIT每次 | BUILD_2每次 | 兵力系数 | 建设奖(新巢穴) |
|----|------|------|------|------|------|
| 当前 | — | +1 | +1.5 | 0.01 | 无 |
| S2 | 节奏重估后 | +0.25 | +0.375 | 0.03 | 低档巢 +3 |
| S3 | S2 无退化 1 窗 | +0.125(≈0) | ≈0 | 0.04 | 到位: 低 +5 / 高 +10 / 防 +5 |
| S4 | T05 换图节点 | 0 | 0 | 0.05 定稿 | garrison 填充 ✅已提前完成 + R6 大调同批 |

设计要点: ① 糖每窗 ×0.5 与效果信号反向渐变, 总激励平滑迁移 ② 建设奖低值试探→到位, 防模型死刷建设 ③ 埋点 [BUILD_NEW]/[RECRUITED] 先于奖励 ④ 全部 ep_runner/注释层, 每步自然重启生效 ⑤ 与 capture 观察窗 / T7.4 / 地图轴错窗。

### 活跃任务 5: 下一难度轴候选（一次一轴, capture 达标后择一）
- ① 72X72_02_duel 即插即用（已备料 gen_t06_duel.py, inspect 全绿, 未入 MAPS）
- ② 108X108 duel 可完成度观察（对角距离 ~145 格 vs 200 步上限, 先观察）
- ③ 1v3 击杀/占城数据自然积累（BHERO_KILL + [TOWN_CAPTURE] 埋点在位）
- 纪律条款: 难度轴与激励轴/图池变更不同窗。

### 活跃任务 6: 长期观察集（不占窗口, 复发才行动）
- **R7 battle query 卡顶**: 埋点五件套已部署, fuse 300s 兜底非阻塞 (局不死仅慢); 0909 晨 16% 局单步卡 300s 样本积累中, **[EP_TIME] 打点已上线** (map/steps/secs/r, 主日志白名单), 复发时聚合 secs/steps 比值 × map × 时段一击定位
- **BHERO_KILL 空拍**: 防护已生效 (0 新误报), [HEROSEG_EMPTY] 诊断埋点待复发捕获 (全槽 -1=竞态 / 全槽 0=共享内存未填充 / red 在 blue 空=owner 异常 / red 也空=整段视角异常)
- **T06 守卫战斗未触发** (aggression 遗留): 与 fix_t06_maps.py 补丁相关, 待 capture 打通后专项
- **TOWNSTALL 引导可达性**: 0910 快照 18 次, BFS blocked 统一修复后观察是否收敛

### 活跃任务 7: Windows GUI 浸泡复测 ✅ 闭环 (09-10)
- 五崩全部修复 (第三崩①②死锁+判空 / 第四崩 showOverview spectator 守卫 / 第五崩 runServer+runNetwork 线程 catch-all), 重编生效
- **soak_gui15 复测 PASS**: 用户全流程 (战斗结算/层切换/系统菜单/KingdomOverview 直接点击) 确认无问题; stderr 93617 行零 THREW/零 Disaster
- 技术细节归档: 踩坑 #164/#165 + 知识库 "GUI 观众模式崩溃系列闭环" 章; 工具 py/parse_crash_dmp.py 留用
- 遗留观察项 (非阻塞): 第五崩原始 throw 点 (已埋点, 复发即报 e.what()) / [MUTEX] 差值 / 良性刷屏两源

### 活跃任务 8: C4 #7632 新栈首窗观察 ✅ 闭环 (09-11)
**结论**: 20 局全 err=no 零崩溃, ZOMBIE 均为正常 hero 死亡事件; 吞吐判定 = **本负载无 +40% 收益** — T06 duel 中性 (4.96/7.35s/步, 旧区间 3.67-6.23), T05_52_mir 持续 +11% 慢 (104-112s vs 旧 97s×6, 踩坑 #184 假设坐实), T05_36X36 中性; reward 全程不劣化。
**拍板 (09-11 用户)**: 保留新栈不回滚 — 本负载中性无害, 课程体系向 72X72_02/108X108 更大图推进时 NK2 可能兑现收益, 且回滚需再开停训窗风险>收益。回滚安全网保留 (~/so_backup_0910_7632 + git revert 三笔), 未来晋级大图轴时若吞吐仍无收益再议。

---

## 关键拍板记录（影响后续节奏）

- **(08-31)** 占城率进晋级判据, 联动条款 = R6 修复后启用 → R6 已修复 (09-03), [TOWN_CAPTURE] 埋点在位, capture 观察窗后可启用
- **(09-08, 用户授权 AI)** 下一难度轴四候选选 **④ 1v3 完整胜利路径引导** (激励轴) — ③等数据是伪选项 (剧本锁定, 激励不变数据只重复), 地图轴留下一窗 → 已实施 (capture +100)
- **错窗纪律**: 激励轴 (capture/T7.4) / 地图轴 (36X36_02 回池/新图) / 难度轴 (72X72_02_duel) 不同窗, 一次只加一个难度轴
- **(09-07)** 训练一律后台 transient unit, 禁前台终端跑法; 优雅停 = 零损失 vs 被动死 = 回滚 (踩坑 #169), 关机前主动 stop 入标准流程
- **(09-11, 用户)** T06 duel capture 死信对策拍板: A/B/C/D 四方向选 **C 方案 (蓝英雄死亡 = capture proxy +100, 仅 duel 图, 每局一次)** — 最小改动 (ep_runner 两处), duel 语义近似等价; 停训窗上线, 首局 PASS (见活跃任务 1 C 方案事实块; 踩坑 #195)

---

## 并行主线：T6 NK2 逻辑融合（训练验证中）

- T6.1 NK2 估值→奖励塑形 ✅ 冻结 (08-22)，v5 实跑中。
- T6.2 C++ BFS 全图寻路 ✅ 代码完成 + T03 段毕业 (08-29)。
- T6.4 训练地图体系 (VMAP T01-T06) 🔄 **T06 课程 1v3 已结案 (09-08, 尾窗四判据全达标)**, 当前 MAPS 8 图 (T05 6 图 + T06 duel + 72X72_01 1v3) 训练中, capture 观察窗。
- T6.3 决策结构分层 ⬜ 远期（与 T5.9 合并）。
- 注：v5 = T6.1 + T6.2 + T7.3 三者叠加训练中。

---

## 待办任务总表（A/B/C/D 档，标注是否影响训练）

> 影响训练判定：是否触碰 WSL 侧 rel/ 运行时 .so、训练进程/日志、GPU/显存、vcmi-native 工作源码。

### A. 立即可做（零影响：Windows 侧 / 纯只读）
> A1-A4 ✅ 全部完成 (08-29)。当前无待办，新的零影响任务追加于此。

### B. 设计文档类（零影响：只写方案）
> B1-B4 ✅ 方案全部出稿 (08-29)。当前无待办。

### C. 谨慎（需隔离设计，有资源/文件重叠）
| # | 任务 | 归属 | 影响训练? |
|---|------|------|-----------|
| C1 | 晋级判据自动化 eval | T7.1 | ⚠ 会抢 GPU/CPU；CPU-only + 低频 (≥2h) + 限 batch |
| C2 | 重编崩溃根因插桩 | T6.2 | ⚠ 分析可，部署禁止 |
| C3 | CGameHandler garrison 自动合并 | T8.1 | ⚠ 触碰铁律；除非断言复发否则搁置 |
| C4 | NK2 寻路加速 merge (+40% 吞吐) | T6.2 | ✅ 完成 (09-11, 停训窗) — 详情见知识库「C4 #7632 执行记录」章 + 活跃任务 8 首窗观察 |

### D. 等待条件触发（不在当前窗口做）
| # | 任务 | 归属 | 状态 |
|---|------|------|------|
| D1 | 经济动作实际上线 | T7.3 | ✅ 已发生 (capture +100 于 09-08 上线, 零停训 ep_runner 层) |
| D2 | 30x30/36x36 实际解封 | T7 | ✅ 已被 T05 全面化覆盖 (09-04 全切 T05, P3 解封参数未用 = 过时) |
| D3 | 开局熵 bonus 实装 | T3 | 🔴 改训练脚本, 等自然重启窗 (= P5) |
| D4 | 2v2/多人对局验证 | T10 | ❌/⚠ fork headless 不影响，GUI 验证需用户在场 (GUI 浸泡复测见活跃任务 7) |

**建议顺序**：capture 观察窗复核（活跃任务 1, 1-2 窗后聚合判据; C4 首窗已闭环, 活跃任务 8）→ 拍板 36X36_02 回池 / T7.4 实装 → 72X72_02_duel 地图轴。C1 视训练稳定度择机。

---

## 训练期并行任务（P，不碰运行中 v5 进程）

| # | 任务 | 类型 | 状态 / 依赖 |
|---|------|------|------------|
| P1 | 训练空转根因 (断言雷) 已修复 | 解锁型 | ✅ 完成 (08-29) |
| P2 | 晋级判据自动化 eval 脚本 (py/eval_promo.py) | 备料型 | ✅ 完成 (08-29) — CPU-only，不抢训练资源 |
| P3 | 30x30/36x36 守卫距离解封 (`--guard_grad_scale_by_map`) | 解锁型 | ⚠️ 过时 (0910 梳理) — 课程图体系已替代守卫系数轴; 守卫坐标口径已换 visitable, 若复用需先适配; 可废弃 |
| P4 | ZOMBIE/GUARD all-blocked 事件聚合分析 (py/analyze_deadlock.py) | 备料型 | ✅ 完成 (08-29) |
| P5 | 开局确定性对策 (entropy 调度) | 备料型 | ⏸ 等自然重启窗 (= D3) |
| P6 | 对手池 self-play/NK2 混合 | 备料型 | ⏸ 远期; NK2 三树源码已就绪 (a1ea3f4d2d + TBB fix 双标记), 回用时直接重编即生效 |
| P7 | 真实游戏战略层联调 (fork 1.8 DLL) | 铺路型 | 🔄 大幅推进 (09-08~09-10): ModelAI 加载链实证 + battle-only 战斗链闭合 (7 battleStarted/1 battleFinished day=31) + **GUI 稳定性闭环 (09-10, 活跃任务 7 soak_gui15 PASS)** — 人机完整对局底座由 P8/T13.10 接续; **09-11 ppomodelai C++ 批落盘** (teal 卡死修复: ONNX 单例 + 双坐标三重门 + VCMI 新 API 迁移, 6 文件 +259/-82, 源码 commit `0aa2047` + DLL 04:39 重编并部署 `bin/AI/ModelAI.dll` 已确证 [strings 4/4 命中], 详见知识库 "PpoModelAI teal 卡死修复" 章 + 踩坑 #189-192); **09-11 mq 线 v5 全模型部署链闭环 ✅** (3464 obs 双输入 / 25 动作采样推理 / 实机 1v7 验证 PASS, action 多样化 + moveHero 真实执行 + 方向语义吻合, 详见头部 mq 增量) |
| P8 | 人机对战底座 (大厅 1V7 配置) | 铺路型 | 🟡 前置完成 (09-11, T13.10): `py/vcmi_protocol/` + Lobby 包 + P8-A 实机入口已落地; 离线 144 PASS; 剩 P8-B/C/D 实机多人局验证 — **CLI 方案已备** (vcmiclient `--loadserver/--loadplayer` 双客户端直开多人局, 免 lobby UI, 见知识库 T13/P8 章; 需核 fork 1.8 参数支持); **09-11 单机侧 AI 1v7 实机对局 PASS** (ModelAI dll 采样推理, mq 线闭环, 见头部增量) — 多人外挂协议线不受影响 |
| P9 | done 信号接线 — 核实已存在无需开发 | 备料型 | ✅ 误报排除 |
| P10 | H3M 官方图 → VMAP 转换 (h3m2vmap) | 备料型 | 🔧 A 步实测 (08-29) |

### P10 H3M 官方图 → VMAP 转换（A 步实测结论）
需求：用官方 H3M 图真实复杂度做地形骨架，降低守卫强度，按官方复杂度渐进。定位：备料型，不碰在跑的 v5 训练。
- A 步实测：① 72~144 尺寸可塞进 21×21 obs（fill_terrain_grid 以英雄为中心 10 格局部窗）；② 官方图物体密度不爆槽（白名单 45 vs 自研 48 同量级）；③ H3M 属性需自建转换入口（无命令行）；④ 水域/船已规避。
- ⚠ 真瓶颈：fill_target_list 排序 = 纯曼哈顿距离 top-8，无价值/威胁加权 → 最近 8 目标大概率全是被强守卫堵住的资源 → 模型看不到可行路径。"减少守卫"真实作用 = 释放 top-8 槽位让可行目标可见（守卫系数 = 天然课程轴）。72X72 大图 top-8 挤占问题已在 T06 引导链中暴露（09-09 mine_taken 死信根因），target_list 排序优化可与此合并立项。
- 选图：`Knee Deep in the Dead.h3m`（36×36 无地下, ROE, 2 玩家, 城需删/封）。
- 实施序：A ✅ / B 转换器 🔧 B1 ✅ (09-01)（设计稿见 `docs/P10-B-h3m2vmap转换器设计稿.md`；工具源码 `vcmi-native/tools/h3m2vmap/`, Windows 副本 `tools/h3m2vmap/`）/ C 改写规则 ⬜ / D 验证链 ⬜ / E 入池 ⬜。禁覆盖 rel/ 下 .so。
- **C 步工具链 ✅ roundtrip 验证 PASS (09-11 停训窗)**: [h3mtxt](https://github.com/alexanderbelous/h3mtxt) 已构建 (`tools/h3mtxt/build/src/h3mtxt/h3mtxt.exe`, mingw 三坑源码补丁见踩坑 #196), 4 图 roundtrip 全 RAW-IDENTICAL (含 A Viking 真实图 144x144); **管线定型**: h3m→JSON→Python 改写→h3m→h3m2vmap --check-h3m。⚠ 边界: 仅 AB/SoD (ROE 拒读, P10 原选图 Knee Deep 是 ROE 需换图或升格式); JSON 带 `//` 注释需 json5/剥注释; 对账口径 = gzip 解压后 raw 一致 (gzip 头 mtime 差异忽略)。备选: [homm3tools](https://github.com/potmdehex/homm3tools)。
- 旁路 ✅ vmap2h3m 反向转换 (09-01)：`py/vmap2h3m.py` (SOD, 5 类对象特征值)，h3m_tool strict 对账 skipped=0 + 引擎 `h3m2vmap --check-h3m` 实测 ENGINE LOAD OK。详见设计稿 §8。

---

## 风险登记（R，引擎侧）

| 风险 | 状态 / 对策 |
|------|------------|
| R1 动作拒绝不可感知 | 🟡 已缓解未审计 — Python action mask 在用 |
| R2 done 信号轮询链 | ✅ 误报排除 |
| R3 异步时序竞态 | 🟢 约束型 — 新增动作走 waitTillRealize |
| R4 queryID=-1 断言雷 | ✅ 已修 — libMMAI.so 13c0f041 部署 |
| R5 双源码树不同步 | 🟡 长期隐患 — 源码级改动前 WSL grep 确认; Windows 树另有引擎 API 级分叉 (getCalendar/showGarrisonDialog 新签名), **不可镜像同步**只做 fix 级摘取 (09-10 实锤) |
| R6 战斗 AI 恒 fallback StupidAI | ✅ 已修复 (09-03): 根因 = MMAI CMakeLists USING_ONNX=0 硬编码 → onnxruntime 1.19.2 部署 /opt/onnxruntime + 重编 libMMAI, 战斗实测 V13::BAI 接管 (Router fallback 0), R7 埋点实战首通。⚠ 战斗强度变化 = 非平稳已消化 (T05 时期自发经济回归兑现)。附带发现: core:footman 在当前 fork 缺失 (T04 0 守卫历史成因) / moveHero 到敌英雄格被拒 (win"消灭对方英雄"达成机制待查)。长期观察 avg_r/胜率 |
| R7 battle query 卡顶 | 🟡 长期观察 — 机理链闭合 (三层 query 栈 + notifyObjectAboutRemoval 中断), 埋点五件套已部署, fuse 900→300s 兜底 (局不死仅慢); 0909 晨 16% 局单步卡 300s 样本积累中 ([EP_TIME] 聚合定位); 复发时按知识库区分表定位三候选。Router dynamic cast 失败已随 R6 修复解决 |
| R8 训练进程存活 | 🟡 流程约束 — transient unit + keepalive 常驻 (踩坑 #114/#168/#169); 一律后台跑禁前台; 关机前主动优雅 stop (零损失) |

---

## 远期 / 阻塞集

- T5.9 v2 MultiDiscrete 三维参数化（与 T6.3 合并）⬜ 远期
- NK2 寻路加速 merge（官方 #7632, +40% 吞吐）— 训练稳定后评估（→ C4）
- MMAI v15（PR #7654）— 未合并官方，2-4x 慢；合并后需重采集（→ T8）
- **战斗模型训练（条件触发, 08-31 登记）** — **当前不需要**: R6 启用现成 onnx 零训练成本。**触发条件 = T05 对抗图中接战胜率成为战略层瓶颈**。路径 = v13 战斗自对弈 (vcmi-gym battle 环境 + BC 权重热启动 → PPO 强化); v15 GNN 战斗模型 (rl/v15 框架已备, 无权重) 绑 T12。⚠ 战斗模型升级 = 战略环境非平稳, 勿在课程中途做。v13 onnx 数据缺口已闭环 (09-08, fqcbvmti-best7 VERIFY PASS, 部署 ~/mmai-battle-test)
- 动作码 16-23 内政启用 — Level 3 切入时先解决 noTarget 惩罚
- fogOfWarMap 语义确认（visible vs explored）— T5.4 顺带
- 引擎侧 standardDefeat 判负根修 — 独立课题 (T7.4 关联, 死亡局 game_over 恒 0 的引擎层根因)
- **T13 外挂 AI 架构** — T13.1-T13.9 ✅ (09-11), T13.10 🟡 Lobby/P8-A 前置完成; 待实机 VCMI server/client 多人局验证 P8-B/C/D。详见下方 T13 专区
- target_list 排序优化 (曼哈顿 top-8 → 价值/威胁加权) — T06 引导链已暴露瓶颈 (09-09 mine_taken 死信), 可与 P10 合并立项

---

## T13 外挂 AI 架构任务分解（主体完成，T13.10 待实机多人局）

> 定位：网络协议外挂 AI 主线。T13.1-T13.9 已离线实现/验证 (09-11); T13.10 只做实机多人局闭环。依赖真实 VCMI server/client 环境; 不碰运行中 v5 训练。
> 目标：实现外部 AI 客户端，通过 VCMI 官方 TCP 协议接入，不改 VCMI 代码。最终实现人 vs 人 vs 模型联网。

### T13.1 序列化逆向 ✅ 完成 (09-11)
- **输出**：`docs/序列化协议规格.md` — 完整二进制序列化格式规格
- **关键发现**:
  - TCP 帧 = [uint32 header][payload]，little-endian，心跳 10s 空包
  - 整数编码 = 变长整数 (LVarInt)，1 byte < 0x3f, 2+ bytes 用 7-bit 分段 + 0x40 负号位
  - 容器: vector/set/map = uint32 长度前缀 + 元素；array = 无长度前缀
  - 字符串: 带全局限去重，首次保存写 length+raw，重复出现写负数 ID
  - 指针: isNull(bool) → pointerID(uint32) → typeID(uint16) → 数据；同一对象只序列化一次
  - optional: present(uint8) + 值；variant: index(int32) + 值；bitset: 按 N≤16/32/64 选 uint16/32/64
  - int3 = 3×si32 变长整数
  - StaticIdentifier (ObjectInstanceID/QueryID/PlayerColor 等) = 直接 int32 变长
  - EntityIdentifier (CreatureID/HeroTypeID/BuildingID/SpellID 等) = encode 为字符串再序列化
  - 类型 ID: 完整注册表 82-215 (RegisterTypes.h)，服务器包 179-212，客户端包 82-177
  - CPackForServer 序列化 = isNull+pid+tid+player+requestID+子类字段

### T13.2 核心包类型验证 ✅ 完成 (09-11)
- **输出**：`py/test_serialization_protocol.py` — 9 项测试全 PASS
- **验证结果**:
  - LVarInt: 24 值往返一致 (0, 63, 64, -1, -64, 16383, INT32_MAX/MIN)
  - 基本类型: bool/int/float/string/int3/optional/vector 全通过
  - EndTurn: 6 bytes `00 00 b4 00 01 01`
  - MoveHero (4 路径点): 22 bytes
  - QueryReply: 8-9 bytes
  - RecruitCreatures: 23 bytes (含 "core:footman" 字符串)
  - 包大小典型 <50 bytes, 远小于 64MB 限制
- **验证方法**: Python 参考实现序列化→反序列化往返对比
- **剩余**: BattleAction/TradeItem/ArtifactLocation/Lobby 包需实机抓包验证

### T13.3 客户端连接层 ✅ 完成 (09-11)
- **输出**：`py/vcmi_protocol/connection.py` — TCP 帧收发、心跳、重连
- **验证**：TCP 帧格式 (header+payload)、心跳空包、连接状态管理 — test_e2e PASS

### T13.4 战略包发送器 ✅ 完成 (09-11)
- **输出**：`py/vcmi_protocol/packs.py` — 12 个服务器包序列化
- **包**: EndTurn(180)/MoveHero(182)/QueryReply(197)/RecruitCreatures(187)/
  BuildStructure(185)/DismissHero(181)/UpgradeCreature(188)/SetFormation(194)/
  CastAdvSpell(200)/GamePause(212)/HireHero(195)/MakeAction(198)
- **验证**：序列化→反序列化往返一致 — test_e2e PASS

### T13.5 客户端包解析器 ✅ 完成 (09-11)
- **输出**：`py/vcmi_protocol/packs.py` + `protocol.py` — 18 个客户端包解析
- **包**: NewTurn/PlayerStartsTurn/PackageApplied/TryMoveHero/SystemMessage/
  ChangeObjPos/SetAvailableHero/SetAvailableCreatures/BattleStart/BattleResult/
  HeroLevelUp/BlockingDialog/GarrisonDialog 等
- **验证**：parse_client_pack() 正确解析所有包 — test_e2e PASS

### T13.6 Query 回复管理器 ✅ 完成 (09-11)
- **输出**：`py/vcmi_protocol/protocol.py` — QueryManager 类
- **功能**：自动检测 Query 包 (88/154/156/157 等), 自动回复 QueryReply, 支持自定义处理器
- **验证**：自动回复 + 自定义 handler + 统计 — test_e2e PASS

### T13.7 战斗包对接 ✅ 完成 (09-11)
- **输出**：`py/vcmi_protocol/packs.py` — BattleAction + MakeAction(198)
- **字段**：side/stackNumber/actionType/spell/target(vector<DestinationInfo>)/battleID
- **验证**：完整序列化→反序列化往返一致 — test_e2e PASS

### T13.8 模型接入层 ✅ 完成 (09-11)
- **输出**：`py/vcmi_protocol/model_bridge.py` — ModelBridge 类
- **功能**：obs(3464维)→模型推理→action(0-24)→CPackForServer 包, 回合状态管理
- **动作映射**：25 个动作 (移动8/交互/招兵/建城/升级/结束回合/等待等)
- **验证**：默认决策 + step() 接口 + 状态统计 — test_e2e PASS

### T13.9 端到端验证 ✅ 完成 (09-11)
- **输出**：`py/vcmi_protocol/tests/test_e2e.py` — 144 项测试全 PASS
- **覆盖**：LVarInt/基本类型/int3/12种服务器包/8种客户端包/战斗包/Query管理/TCP帧/模型接入/注册表/包大小

### T13.10 多人对战验证（终极目标，P8 前置完成）
- **状态**：🟡 部分完成 (09-11) — Lobby 协议 + P8-A 实机入口已落地；真实多人局仍需 VCMI server/client 实测。
- **任务**：2+ 人类客户端 + 1 外挂 AI 客户端，同一 VCMI server
- **场景**：1v7 大厅配置，AI 接入任意玩家槽位
- **已落地**：
  - `py/vcmi_protocol/` 完整外挂协议栈 (TCP / CPack / Query / ModelBridge)
  - Lobby 包: `LobbyClientConnected(216)` / `LobbyClientDisconnected(217)` / `LobbyChatMessage(218)` / `LobbyUpdateState(226)` / `LobbyQueryState(265)` / `LobbyModsCheck(266)` / `LobbySetMap(229)`
  - P8-A 实机入口: `python py/vcmi_protocol/tests/test_e2e.py --p8-1v7 --map Maps/Twins.h3m`
  - 离线验证: `144 passed, 0 failed`
- **剩余验证**：
  - P8-B: 实机启动 VCMI server/client, AI 正常加入/退出对局
  - P8-C: AI 与人类混合游戏无异常, 回合交替正常
  - P8-D: 跨机器部署（AI 客户端在远程机器）
- **输出**：实机验证报告 + 部署文档
- **前置**：T13.9 ✅ / 实机 VCMI server/client 可用
- **预估**：1 天（仅实机验证）

### T13 总览

| 阶段 | 子任务 | 预估 | 依赖 |
|------|--------|------|------|
| 协议基础 | T13.1 序列化逆向 | ✅ 2-3d → 0.5d 完成 | — |
| 协议基础 | T13.2 核心包验证 | ✅ 0.5d 完成 | T13.1 |
| 连接层 | T13.3 客户端连接 | ✅ 完成 | T13.1 |
| 包处理 | T13.4 战略包发送 | ✅ 完成 | T13.1+T13.3 |
| 包处理 | T13.5 客户端包解析 | ✅ 完成 | T13.1+T13.3 |
| 包处理 | T13.6 Query 回复 | ✅ 完成 | T13.4+T13.5 |
| 包处理 | T13.7 战斗包对接 | ✅ 完成 | T13.4+T13.5+T13.6 |
| 模型接入 | T13.8 模型接入层 | ✅ 完成 | T13.4+T13.5+T13.6+T5.8 |
| 验证 | T13.9 端到端验证 | ✅ 完成 | T13.3-T13.8 |
| 验证 | T13.10 多人对战 | 🟡 前置完成 | T13.9 + 实机 server/client |
| **总计** | | **20-37 天** | **T13.1-T13.9 已完成, T13.10 前置完成, 待实机多人局验证** |

### T13 与现有轨道的关系

| 轨道 | 关系 |
|------|------|
| Track 1 (训练) | T13 不碰训练，但依赖 T5.8 模型成熟 |
| Track 2 (真实游戏对齐) | T13 是 Track 2 的替代路径 — 不修改游戏，通过网络协议外挂 |
| T8 战斗集成 | T13.7 可复用 T8 的战斗模型，也可用内置 BattleAI |
| T9 真实游戏对齐 | T13 与 T9 是两条独立落地路径（T9=修改游戏内存 / T13=网络协议外挂）|
| T10/T11 人机对战 | T13.10 直接实现终极目标 |

