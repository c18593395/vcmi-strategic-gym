# 当前任务清单

> 父文档: `docs/总任务.md` | 踩坑: `docs/踩坑记录.md` | 知识库: `docs/知识库.md`

---

## 双轨并行架构

```
┌──────────────────────┐   ┌──────────────────────────┐
│  Track 1: 训练 🟢    │   │  Track 2: 真实游戏对齐 ⬜ │
│  ✅ C7 全部完成       │   │  模型落地到真实 HoMM3    │
│  ✅ moveHero 验证通过  │   │  Windows 内存读取验证    │
│  ✅ passability mask  │   └──────────────────────────┘
│  ✅ Connector 适配(N2)│
│  🏃 训练运行中        │
└──────────────────────┘
```

---

## Track 1: 训练 🏃（GPU 24/7 后台跑）

### 当前训练状态（2026-07-27）

```
训练脚本: train_wsl2_ppo_v2.py（GAE λ=0.90 + grad_clip=1.0）
AI 配置: red=MMAI_USER（RL 控制）, blue=MMAI_USER/对手池
管道状态: ✅ Connector 适配完成（新 1-param init_vcmi(void*) API）
           ✅ 4 张开放 H3M 图，新 connector 已部署到 rel/
           ✅ 训练运行中（PID 12948, step=18909 续训）
           ✅ 对手池传参修复（ML_USER→MMAI_MODEL + blue_model_path 透传）
```

### 当前改动

| 改动 | 文件 | 说明 |
|------|------|------|
| passability C++ 端 | `strategic_state.h/cpp` | 8方向可通行性检测，用 `TerrainTile::isClear()` |
| passability mask | `ep_runner_one.py` | 模型采样前过滤非法方向，全堵→END_TURN |
| obs 维度扩展 | `strategic_env.py` | OBS_DIM 256→264，末尾加 passable[8] |
| ctypes 绑定 | `strategic_reader.py` | 加 passable 字段 |
| 网络输入层 | `Net` 三处 | 256→264 |
| 模型重置 | `wsl2_model.pt` | 随机权重，step=0 |
| GAE λ | `train_wsl2_ppo_v2.py` | 0.95→0.90（降低 NaN） |
| Connector 适配 | v13/v14/v15 connector | `unique_ptr<InitArgs>` + `ML::init_vcmi(void*)` |
| 打印修正 | `train_wsl2_ppo_v2.py` | `red=MMAI`→`red=MMAI_USER` |
| 对手池 AI 名 | `ep_runner_one.py` | `ML_USER`→`MMAI_MODEL`（新版 API） |
| blue_model 传参 | `strategic_env.py` + `ep_runner_one.py` | 新增 `blue_model_path` 参数透传到 connector |

### 训练参数

| 参数 | 值 |
|---|---|
| N_EPISODES | 1000 |
| STEPS_PER_EP | 200 |
| BATCH | 128 |
| device | cuda (RTX3060) |
| 地图 | 4 张开放 H3M（For Sale, Elbow Room, Elbow Room(Allies), Deluge） |
| 对手 | MMAI_USER（自对弈 + 对手池） |
| 对手池 | 70%当前 / 20%早期 / 10%随机 |
| Checkpoint | 每 50 step, 保留 10 个 |

---

## ✅ 已完成

| # | 任务 | 说明 |
|---|------|------|
| A1 | passability mask | C++ strategic_state + Python mask |
| A2 | obs 扩展 | OBS_DIM 256→264 |
| N2 | Connector 适配 | 新版 libmlclient 1-param init_vcmi(void*)，已适配+编译+部署 |
| N6 | 训练传参修复 | 打印误导 + ML_USER→MMAI_MODEL + blue_model_path 透传 |

---

## 下一步

| # | 任务 | 说明 | 优先级 |
|---|------|------|--------|
| B | ⏸ VCMI server crash 修复 | 修 applyMoveHero 失败后 segfault | 🟡 |
| N1 | 🏃 训练运行中 | 4 张开放图, step=18909 续训 | 🟢 |
| N3 | ⬜ vmap 适配修复 | gen_strategic_vmap.py 的 JSON/object 兼容 | 🟡 |
| N4 | ⬜ 战斗集成（Phase D） | moveHero + autofight 深度整合 | ⬜ |
| N5 | ⬜ 切换 v15 架构 | 图状态 + GNN + py::dict，Phase G 前重写 | ⬜ |

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `train_wsl2_ppo_v2.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器（含 passability mask + 对手池） |
| `strategic_env.py` | 战略 Gym 环境（v13） |
| `vcmi/ML/strategic_state.h` | 结构体定义（含 passable[8]） |
| `vcmi/ML/strategic_state.cpp` | passability 填充逻辑 |
| `strategic_reader.py` | ctypes 绑定 |
| `tools/gen_strategic_vmap.py` | vmap 生成器（需修复 JSON/object） |
| `docs/总任务.md` | 全局架构、Phase A~G 进度 |
| `docs/当前任务清单.md` | **本文件—双轨可执行任务表** |
| `docs/踩坑记录.md` | 踩坑详细记录 |
| `docs/知识库.md` | 单页知识总汇 |
