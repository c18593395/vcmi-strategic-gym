# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `踩坑记录.md` | 知识库: `知识库.md`
> 每条任务标注对应 `总任务.md` 的 Phase 编号

---

> ⚠️ **#45 新发现（2026-07-28）**：MMAI 的 `fill_state_from_cb` 没有输出不是因为代码没被执行，而是因为：
> 1. `train_wsl2_ppo_v2.py:59` 的 `stderr=subprocess.DEVNULL` 吞掉所有子进程诊断输出（已修复为写文件）
> 2. VCMI 内部重定向 stdout/stderr，`fprintf` 不可见（已改为 fopen 写 `/tmp/mmai_diag.txt` 绕过）
> 3. **真正阻塞**：connector_v13.so 与 libmlclient.so 的 `init_vcmi` ABI 不匹配（3-arg vs 1-arg），导致 ep_runner core dump
> 4. **深层根因**：项目源码(v13 schema: BATTLE_SIDE) 与 vcmi-native-build(v13 schema: BATTLE_ROUND) 版本分歧，connector/libmlclient/MMAI 三件套必须统一编译

---

## 📌 开机任务（重新开机后执行）

上一步进展到 #45 发现核心阻塞：connector/libmlclient ABI 不匹配 + schema 版本分歧。
修复策略：用 vcmi-native（旧版 schema，BATTLE_ROUND）作为统一编译目标，修复 connector 代码后重建。

### Step 1: 修复 connector 代码 → 改用 1-arg init_vcmi

connector_v13 的 threadconnector.cpp 还在调老版 3-arg `init_vcmi`，但 libmlclient 只有 1-arg 版本。

```bash
# 检查问题行
grep -n "init_vcmi" D:/Bigdata/hero3_fresh/vcmi_gym/connectors/v13/threadconnector.cpp
# 期望输出: Line 573 调用 ML::init_vcmi((void*)initargs.get())
# 如果还是 3-arg 调用 → 手动改成 1-arg
```

修复内容——在 `D:\Bigdata\hero3_fresh\vcmi_gym\connectors\v13\threadconnector.cpp` 第 573 行附近：

```
// 旧（3-arg）:
ML::init_vcmi(leftModel, rightModel, *initargs);

// 新（1-arg）:
ML::init_vcmi((void*)initargs.get());
```

### Step 2: 重建 connector_v13（用旧版 schema）

```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash -c '
cd /home/administrator/vcmi-workspace/vcmi_gym/connectors/rel
rm -rf CMakeCache.txt CMakeFiles/

# 关键: 用 vcmi-native（BATTLE_ROUND schema）编译，确保与 libmlclient 一致
cmake .. -DVCMI_DIR=/home/administrator/vcmi-native 2>&1 | tail -3
make -j4 connector_v13 2>&1 | tail -5
echo "=== exit=$? ==="

# 验证符号: 必须链接 1-arg init_vcmi(void*) 不是 3-arg
nm -D connector_v13.so 2>/dev/null | grep init_vcmi
# 期望: U _ZN2ML9init_vcmiEPv  (1-arg void*)
'
```

### Step 3: 部署 connector + MLSchema 保持不变

```bash
# connector 已在同一位置（/mnt/d/Bigdata/hero3_fresh/vcmi_gym/connectors/rel/  = workspace 挂载）
# libmlclient/MMAI 上次部署后不变

# 确认 libmlclient ABI
nm -D /home/administrator/vcmi-native/rel/bin/libmlclient.so 2>/dev/null | grep init_vcmi
# 期望: T _ZN2ML9init_vcmiEPv  (1-arg void*)

# 确认 MMAI 含文件诊断
strings /home/administrator/vcmi-native/rel/bin/AI/libMMAI.so | grep mmai_diag.txt
# 期望: /tmp/mmai_diag.txt
```

### Step 4: 直接测试 ep_runner（不走训练，快速验证）

```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash -c '
rm -f /tmp/mmai_diag.txt /tmp/test_traj.json
cd /home/administrator/vcmi-workspace
export LD_LIBRARY_PATH=/home/administrator/vcmi-native/rel/bin:/home/administrator/vcmi-native/rel/bin/AI:/home/administrator/vcmi-workspace/vcmi_gym/connectors/rel
export STRATEGIC_STATE_LIB=/home/administrator/vcmi-native/rel/bin/libmlclient.so
timeout 30 /home/administrator/vcmi-workspace/venv/bin/python /mnt/d/Bigdata/hero3_fresh/ep_runner_one.py 5 /tmp/test_traj.json "Key to Victory.h3m" 2>&1
echo "EXIT=$?"
cat /tmp/mmai_diag.txt 2>/dev/null || echo "mmai_diag.txt NOT FOUND"
'
```

**解读结果：**

| 结果 | 含义 | 下一步 |
|------|------|--------|
| `mmai_diag.txt` 有 `YT: entry` + `FILL: enter` | ✅ MMAI 工作 | 进 Step 5 |
| `mmai_diag.txt` 不存在 | ❌ MMAI 未执行（connector ABI 或 schema 仍不匹配） | 回 Step 2 检查 nm |
| `mmai_diag.txt` 有 YT 无 FILL | ❌ yourTurn 进了但 fill_state_from_cb 前崩了 | 查 g_adventure_cb 路径 |
| core dump + no file | ❌ VCMI init 阶段段错误 | 检查 schema 版本一致 |

### Step 5: 清旧状态 + 启动训练

```bash
cd D:/Bigdata/hero3_fresh
rm -f /tmp/traj_ep.json /tmp/hermes_ep_model_*.pt /tmp/hermes_ep_*.log
find . -path "*/__pycache__" -type d -exec rm -rf {} + 2>/dev/null

MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash -c '
rm -f /mnt/d/Bigdata/hero3_fresh/wsl2_model*.pt /tmp/mmai_diag.txt
cd /home/administrator/vcmi-workspace
export LD_LIBRARY_PATH=/home/administrator/vcmi-native/rel/bin:/home/administrator/vcmi-native/rel/bin/AI:/home/administrator/vcmi-workspace/vcmi_gym/connectors/rel
export STRATEGIC_STATE_LIB=/home/administrator/vcmi-native/rel/bin/libmlclient.so
nohup /home/administrator/vcmi-workspace/venv/bin/python -u /mnt/d/Bigdata/hero3_fresh/train_wsl2_ppo_v2.py >> /mnt/d/Bigdata/hero3_fresh/train_loop.log 2>&1 &
echo "PID=$!"
'
```

### Step 6: 等 3 分钟后检查诊断

```bash
# 检查 MMAI 诊断
cat /tmp/mmai_diag.txt

# 期望:
#   YT: entry cb=0x...
#   FILL: enter
#   FILL: gameState OK, map objects=N

# 检查 obs 是否非零
grep "obs_nz" /mnt/d/Bigdata/hero3_fresh/train_loop.log | tail -5
# 期望 obs_nz > 0
```

### Step 7: 根据结果判断下一轮

```bash
# 如果 obs_nz > 0 稳定出现 → #45 解决！训练开始收敛
# 如果 obs_nz = 0 但有 FILL: enter → fill_state_from_cb 内 CGameState 仍为空（try/catch 吞异常）
# 如果 mmai_diag.txt 有规律更新但 obs_nz=0 → 需查 _read_strategic_state 是否读到新数据
```

---

## 双轨状态

```
┌──────────────────────┐   ┌──────────────────────────┐
│  Track 1: 训练 🟡    │   │  Track 2: 真实游戏对齐 ⬜ │
│  Phase C7 全部完成    │   │  Phase E (待启动)        │
│  🛑 三件套 ABI 阻塞   │   └──────────────────────────┘
│  #45 connector 适配 🔴 │
└──────────────────────┘
```

---

## Track 1: 训练 🛑

### 当前状态

| 项目 | 值 |
|------|-----|
| 脚本 | `train_wsl2_ppo_v2.py` |
| Phase | C7 完成, #45 未解决 |
| avg_r | 0.0 (模型塌缩 — obs 全零) |
| obs_nz | 0 |
| maps | 4 张开放 H3M |
| 对手 | MMAI_USER 自对弈 + 对手池 |
| **阻塞** | connector/libmlclient ABI 不匹配 → ep_runner core dump |

---

## 🔴 #45: connector/libmlclient/MMAI 三件套统一（当前阻塞点）

### 已知事实

| 发现 | 详情 |
|------|------|
| ep_runner stdout/stderr = DEVNULL | `train_wsl2_ppo_v2.py:59` 子进程所有输出进黑洞 |
| VCMI 重定向 fd 1/2 | CBasicLogConfigurator 可能在启动时接管 stdout/stderr |
| init_vcmi ABI 不匹配 | 旧 connector(3-arg) vs 新 libmlclient(1-arg void*) |
| schema 版本分歧 | 项目源码(BATTLE_SIDE+LINK_SIZES) vs vcmi-native-build(BATTLE_ROUND) |
| CMakeLists.txt 硬编码 | 第 11 行 `set(VCMI_DIR ...)` 覆盖 -D 参数 |
| `__declspec(dllexport)` GCC 不兼容 | MSVC 扩展在 WSL GCC 下报错，用 DLL_EXPORT 宏修复 |
| ep_runner core dump | 因为 schema/ABI 不匹配，VCMI init 阶段段错误 |

### 所需修复

1. **threadconnector.cpp:573** — 调用 `ML::init_vcmi((void*)initargs.get())` 1-arg 版本
2. **CMakeLists.txt** — 改为 `if(NOT VCMI_DIR)` 条件设置
3. **统一 schema** — 确定用 BATTLE_ROUND（旧版）还是 BATTLE_SIDE（新版），三份源码同步

---

## 已完成 (C7)

| # | 总任务 Phase | 任务 |
|---|-------------|------|
| N2 | C7 | Connector 适配 |
| N6 | C7 | 训练传参修复 |
| N7 | C7 | ABI 修复 |
| N8 | C7 | 调用链修复 |
| N9 | C7 | MMAI fallback action |
| N10 | C7 | CMake /DEF: 修复 |
| B | C7 | VCMI server crash |
| N3 | C7 | vmap 适配 |
| N11 | C7 | MMAI 重编 + action 验证 |

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `docs/总任务.md` | 全局架构、Phase A~G 进度列表 |
| `docs/当前任务清单.md` | **本文件 — 双轨可执行任务表** |
| `docs/踩坑记录.md` | 踩坑详细记录（#49 看 #45 调试全记录） |
| `docs/知识库.md` | 单页知识总汇 |
| `train_wsl2_ppo_v2.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器 |
| `vcmi_gym/envs/v13/strategic_env.py` | 战略 Gym 环境 (v13) |
| `vcmi/AI/MMAI/AAI/AAI.cpp` | #45 主要修改文件（fill_state_from_cb + passability） |
