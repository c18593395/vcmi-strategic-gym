# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `WSL踩坑点.md` | 知识库: `WSL知识库.md`

---

## #46 全线打通 ✅ (2026-07-29)

**env.reset() → obs_nz=8/264 → step() 5 步无崩溃**

### 修复链
| 问题 | 修复 | 文件 |
|------|------|------|
| installNewBattleInterface segfault | 重建三件套自然修复 | `client/Client.cpp` |
| Discord null dereference segfault | `if(ENGINE->hasDiscord())` guard | `client/GameEngine.h`, `client/CServerHandler.cpp` |
| 信号量通信 (adventure_wait 永远不返回) | `AAI::yourTurn` 调 `adventure_process_turn()` | `AI/MMAI/AAI/AAI.cpp` |
| MMAI constructor 未导出 | `__attribute__((used))` | `AI/MMAI/AAI/AAI.cpp` |
| Hero pool even 检查抛异常 | `pop_back()` 替代 throw | `server/ML/ServerPlugin.cpp` |

### 当前工作组合（全部最新重建）
| 组件 | 位置 | 说明 |
|------|------|------|
| libmlclient.so | `vcmi-native-build/rel/bin/libmlclient.so` | 含 Discord fix + 信号量 + state fill |
| libMMAI.so | `vcmi-native-build/rel/bin/AI/libMMAI.so` | 含 `adventure_process_turn` 调用 |
| vcmiserver | `vcmi-native-build/rel/bin/vcmiserver` | 含 Discord fix |
| libmlserverplugin.so | `vcmi-native-build/rel/bin/libmlserverplugin.so` | 含 hero pool pop_back |
| connector_v13.so | `vcmi-workspace/vcmi_gym/connectors/rel/connector_v13.so` | 最新重建 |

### 剩余问题
- ⚠️ `obs_nz=8/264` — 仅有 passable。`getHeroesInfo()` 返回空（地图无初始英雄）。`fill_state_from_cb` 通过 `userData` 传 `cb` 指针但未返回数据
- ⚠️ hero pool pop_back fix 被 git checkout 冲掉，需重 `make mlserverplugin && cp`
- ⚠️ `step()` 内 `_adventure_wait` 在 ep_runner 中超时（可能 hero pool 异常导致 game 未正确启动）

### 开机任务
1. `cd /home/administrator/vcmi-native-build/rel && make -j4 mlserverplugin && cp bin/libmlserverplugin.so ~/../vcmi-native/rel/bin/`
2. `cd /home/administrator/vcmi-workspace && export LD_LIBRARY_PATH=... && timeout 120 python ep_runner_one.py 20 /tmp/ep.json "For Sale.h3m"`
3. 根据 ep_runner 输出决定是否启动训练
