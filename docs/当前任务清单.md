# 当前任务清单（当下窗口要做什么）

> ## ⚠️ 启动训练必带 Windows keepalive（09-18 实证，置顶纪律）
> **每次启动训练后，必须立即拉起 Windows 侧 keepalive，否则 ~60 秒后 WSL idle shutdown 把训练整个带走**：
>
> ```powershell
> Start-Process wsl.exe -ArgumentList '-d','Ubuntu','sleep','infinity' -WindowStyle Hidden
> ```
>
> - WSL 空闲判定只看 **Windows 侧客户端**（终端/keepalive），不看 VM 内训练进程——训练在跑也会被关机（09-18 实证：journalctl 大量 "Stopping homm3-train-v5" 全是 **poweroff 关机流程**，极易误判为外部 stop；`last` 显示 WSL 当天 reboot 9+ 次）。
> - **keepalive 是隐藏进程，每次手工拉起（09-19 拍板：不做任何自动启动）**。
> - **09-25 勘误**：原「360 安全卫士会杀进程+删计划任务（#267）」的根因描述有误——**本机未安装 360 安全卫士**。#267 两次 idle shutdown（22:31/02:24）根因 = keepalive 未手工拉起 / 重启后丢失（非杀软误杀）。判活仍按踩坑 #201 三件套。
> - 兜底：enabled unit 在 WSL 冷启动时自动恢复训练（02:25 实证零损失），keepalive 只是防冷启动中断。
> - 判活仍按踩坑 #201 三件套（PID etime + 日志 mtime + cgroup/MainPID）；异常重启先查 `wsl bash -c "last | head"` 看 WSL 是否 reboot，再查 journalctl。
> - 指针：踩坑 #114/#201 + 09-18 WSL idle shutdown 循环定性（已固化知识库 09-18 章）+ 09-25 keepalive 勘误。

> ## ⚠️ 竞态类验证纪律（09-23 血泪，置顶硬规则）
> **任何"概率性/竞态类问题已修复"的结论，必须 N≥4 重复 + 基线对照，单样本通过不算数。** 反例：09-23 07:28「4/4 全绿 = 冻结消失」被 A/B 证伪——`elbow_room` **基线（无任何新修复）就 4/6 异常**（2×rc=139 + 2×rc=124），单样本 4/4 毫无判别力。
> 执行口径：① 同图重复 **N≥4 轮**，记**逐图复现率**而非"过/不过"（工具 `py/_298_repeat.sh`）；② 必须有**基线对照**（回退修复 → 重编 → 同 N 局同图）；③ 崩溃栈用 `py/_298_crash_gdb.sh`，但注意崩溃可能**只在非 gdb 下复现**（gdb 下 6 局全过），gdb 单跑不能反证没问题；④ 改源码后重编，必须确认构建日志出现 `Building CXX object <目标文件>`（`shutil.copy2` 会保留旧 mtime 让重编变空操作）。
> 指针：踩坑 #298「09-23 竞态类修复的验证纪律」条。

> 父文档：[总任务.md](总任务.md)（大项 T1-T13 整体脉络）。本文件只写**当前有效**的任务：窗口焦点 `WIN-x`、观察期 T 任务、训练期可做工作、风险与远期。
> **脱钩原则**：技术细节不在本文件展开，一律指针到 `WSL知识库.md` / `WSL踩坑点.md` / `已完成任务.md`。完成即归档移出，保持短小。
> 最近整理：2026-09-26（**池图/课程图三档治理 ✅**：① elbow_room_allies batch 2→99（双窗证伪，热生效）② King_of_Pain 移出课程 MAPS（duel 挤压回归）③ **T05 三张移出课程 MAPS（A/B 双臂 N=4/臂 定谳能力真空，踩坑 #327）**；too_many_monsters 留观 1 窗（~50 局复核）。方法论=30 步冒烟禁判池准入 + 判留/摘看时间桶趋势。commit `c855653d`+`d0b9a33b`，重启后 **maps=6（T06 六张）+ 池 3 张（good_to_go/judgement_day/too_many）**，resume step=1600910。另：delegation agnes 401 已切 apihub-5（fact #66）。**下一次换地图验收标准 7 条件已写入 WIN-5 段（duel 轴收口为前置第 1 条，错窗纪律）**）
> 最近整理：2026-09-26（**T14.3 工程验收闭环 ✅**：真相源=WSL 仓 `4a1675c6a2`（reasonix D: 残件 dlopen 旧假设版已清除）→ 服务器 patch 部署 + `cmake -DENABLE_T14_BLUE=ON` 全绿 + 符号落位 + **KTV N=4 双臂冒烟 0 崩**（T14 均值 -357 vs NK2 -429，加载实锤 `[T14-ACT] P0`）；**能力面单图 DRAW 未破**（nudge 软推进，挂 T14.3b 硬注入+权重轴）；构建踩坑=MMAI CMake 硬编码 x86/python3.12 路径靠符号链接桥接（踩坑 §2.21/知识库 §5.19）。指针：`docs/T14.3_T14Blue_20260926_checkpoint.md` + 任务清单 T14.3 段 + fact_store "T14.3 T14Blue"）。
> 最近整理：2026-09-26（**P1 引擎窗口三项收口 ✅**：P0-2 三套 298 A/B 对照定谳（A 臂 16/16 全绿 vs B 臂 12×rc=124 冻结，**三套全留**，压制非根治）/ P1-3 #215 headless 守卫重写 6 处 + vcmiclient 链接修复（vcmi-native `11be8ba34d`）/ P1-4 connector C++ 重编生效（vcmi_gym `87df8cc`）。证据 `/home/administrator/_298_p1_20260926/`。详见 知识库「09-26 P1 引擎窗口三项收口」章 + 踩坑 #320-#325 + 已完成任务 09-26 条。遗留=冻结 Mode B 根修另排。）
> 最近整理：2026-09-25（**T14.1 摸底完成 ✅**：纯 Lua 前提勘误（VCMI Lua 层无战略动作 API，NK2 为 C++）→ 路线改 C++ 新 AI 模块；六-B 按 T14_API面清单.md 修正；`docs/T14_API面清单.md` 产出）。
> 最近整理：2026-09-25（**09-25 停训窗收口**：WIN-5 节拍判据实测定谳 / batch2 维持挂起 / WIN-2 J1 无法计算（02 未回池）/ WIN-3 择 108X108_02_duel；**WIN-6 PPO-DNA 并行化 N_SUBPROC=4 灰度上线**（.wslconfig 12GB 前置 + slot 池化 + cmd 对齐 + 全崩兜底 + SIGTERM 清理，4 ep_runner 同时跑））。
> 最近整理：2026-09-24（**收敛**：09-23/09-24 密集条目已并入知识库 09-23/09-24 章与 09-23 09-24 收口条；WIN-1/2/3 收口为短指针；T7.8 精简；新增「可立即执行」小节。WIN-1 passable 闸门 ✅ / WIN-4 批转收官 ✅ / 09-24 停训窗收口 ✅）。

> ## ✅ 09-25 停训窗收口（WIN-5 节拍判定 + WIN-2/3 定谳，训练优雅停训 step=1000719 后重启）
> **事实链（09-25 停训窗，本段 = step 979814 起 125 局）**：
> ① **WIN-5 节拍判据实测定谳**——`[POOL_SCHED] interval=5 / pool=2`（good_to_go + judgement_day 轮转）命中节奏**精确正常**：25/126 行 = 每 10 局 1 局池图，good_to_go 13 局 / judgement_day 12 局。但**池图质量未达标**：good_to_go r_mean=-328.8（p75=-141）、judgement_day r_mean=-897.4（几乎全满步 250/250），池图大负(<-300)=17/25，远负于课程图基线 r_mean=-6.8（大负 0/100）。
> ② **batch2 维持挂起**——原触发条件"首批 5 张全绿 50 局"已被 a_viking 摘除 + elbow_room 移池打破（batch1 现存仅 2 张）；judgement_day 持续满步大负 → **`too_many_monsters` + `elbow_room_allies` 不批转进池**，`_pool_index.json` 两图 batch:2 标记保留但不放行（防负数核心机制 `HOMM3_H3M_BATCH=1` 只放行 batch≤1）。
> ③ **T7.8 duel 净胜率定谳未达**——本段 duel 图 29 局：`fought & won`(守卫胜)=0（守卫接战全在课程图，全日志 [GUARD] 2789 行但 duel 局内 0 行）+ duel 局内战死率 58.6%（RED_DEAD 17/29）→ **净胜率 0% - 58.6% = -58.6%**（目标 >0 未达）。duel r_mean=+45.1 为"不踩守卫不接战 + 不战死就满步截断"的结构性底噪，非接战胜率。
> ④ **WIN-2 J1 无法计算（NA）**——本段 36X36_02 n=0（未回池），36X36_01 n=7 r_mean=-30.4 → J1 需 02 回池攒 ~40 局后才能算，本窗仅登记不展开。
> ⑤ **WIN-3 择 108X108_02_duel**——duel 候选 r 分布（本段）：72X72_02_duel n=11 r_mean=+9.5 / 108X108_02_duel n=13 r_mean=+78.7 / 72X72_01_duel n=5 r_mean=+36.1。108_02_duel 表现最优且 250 步方案已部署、#238"不可达"已作废 → **首选 WIN-3 ② 108X108_02_duel 作为下一独立难度轴**（与 WIN-5 错窗：batch1 维持 + 不开 batch2 后择窗上难度轴）。
> ⑥ **哨兵/吞局健康**——本段 `[EP298_SWALLOW]`=2、`[ML-upg]BREAK`=0、err≠no=0/125；全日志战死局率 54.4%（RED_DEAD 68/125，duel 占 17）。
> **重启**：训练优雅停训存档 step=1000719 → 改 WIN-3=108_02_duel 难度轴 + WIN-5 batch1 维持 → `systemctl start` + 手工拉 keepalive。**09-25 续窗已上线**：WIN-3 难度轴 `HOMM3_DUEL_FOCUS=1` 非池局 **60%** 强制 108_02_duel（代码 `py/train_wsl2_ppo_v2.py` L84-90/L573-579 + unit 双副本同步；先上 80% 自检 OK 后**拍板降 60%** 防单图过拟合，停训 step=1000952 重启 MainPID 97737，自检 DUEL-FOCUS 打点"60%"OK），keepalive 已拉。回退=`HOMM3_DUEL_FOCUS=0`。
> **指针**：知识库 09-25 节（本窗新增）+ 踩坑 #298（竞态验证纪律 N≥4）。
> **判据提醒**：T7.8 duel 判据需 S2 撤梯子后 `[RECRUITED]/局` 回升 ≥5 才做正式收口；本窗 duel 净胜率 -58.6% 是能力轴（不接战）未达，归 T7.8 观察窗，不单独触发。

> ## ✅ 09-24 停训窗收口 + 续窗 game_over 强制置位（已归档）
> 事实链/修复/验证全量原文见 `已完成任务.md`「2026-09-25 收敛归档」+ 知识库「09-24 停训窗」节。要点：Mode A 修复（shutdown 延迟）✅ / 崩溃 rc=139 两处判空修复 ✅ / Mode B 定谳已修（trade cap 16，`8679dd35a1`）/ game_over 强制置位 `patch_mlfix_gameover.py`（elbow_room ×12 验证，哨兵 `[ML-fix] force game_over`）✅ / 训练重启 resume step=951759。

> ## ✅ 09-23 冻结修复与三处修复恢复收口（已归档知识库 09-23 章）
> **要点**：冻结源定谳 = NK2 两条 AI 线程卡 `CClient::sendRequest` 死锁（300s 超时）；upgrade 死循环 + 网络线程自锁双故障；三套补丁 `patch_298_stacktrace/netthread_fix/upgrade_probe`（各带 rollback）4/4 全绿；boot hang 看门狗（400s 无 kick → `os._exit(43)`）；三处 08-17 修复恢复（`patch_298_restore_0817.py`，含 A/B 对照定谳"崩溃/超时是既有竞态非本次引入"）；vcmi-native 4 提交入库。
> **⚠ 重大勘误（保留置顶，影响所有判读）**：07:28「4/4 全绿 = 冻结消失」是**单样本侥幸**——A/B 基线 elbow_room 即 4/6 异常 → **冻结（AI EndTurn realize 死锁）从未真正消失，仍需真修**（方向三档见知识库 09-23 章）；偶发图 elbow_room/a_viking ~33% rc=124/139 → 判据按**复现率**；竞态类验证**强制 N≥4 + 基线对照**。
> **指针**：知识库 09-23 章 + 踩坑 #298/#299 + 已完成任务 09-23 条（全量细节）。

> ## ✅ 紧急收口（09-19）：WSL 重建 + MMAI 修复全链路完成（已归档）
> 事实链：Lxss/ext4.vhdx 被删（#269）→ rootfs 重装 → .wslconfig 网络打通 → STATIC_AI 8 处源码修复（#280）→ 冒烟 3 步全绿 → 训练恢复 step=770300。全量原文见知识库 09-19「重建收尾全链路」+ 踩坑 #270-#283。
> **⚠ #282/#283 环境变更登记（仍影响判读）**：STATIC_AI 激活蓝方对抗 → red 死亡率 0%→62%（**全部 all-blocked 困死非战死**，用户拍板不降级）；**S2 撤梯子窗基线作废**，从恢复点重开 40 局复查；观察项 `[RECRUITED]/局` 回升 ≥5 后再做 S2 正式收口（→ T7.5）。

---

## 一、当前窗口焦点 WIN-x

### WIN-1 — 批次B + A3 中期收口 ✅ / passable 闸门 ✅（已收口 09-18~09-19，4 判据全绿）
- **收口**：passable 闸门（方案甲）4 判据全绿；own_town pick 0、T05 三图全翻正。**下一轴三选一已定**（首选 S2 撤梯子→见 T7.5；次选 WIN-3 难度轴；WIN-2 回池殿后）。全量原文 + 回退线 + 聚合工具见 `已完成任务.md`「2026-09-25 收敛归档 WIN-1」。

### WIN-2 — 36X36_02 原样回池评估 🟡（归属 T6.4，**09-25 定谳：J1 无法计算，02 未回池 n=0，本窗不展开**）
- **09-25 现状**：本段 36X36_02 n=0（未入池），36X36_01 n=7 r_mean=-30.4 → **J1=r_mean(02)/r_mean(01) 无分母，无法计算**。回池评估须先入池攒 ~40 局再跑 `py/t05_02_data_pull.py`。方案 §4（触发条件表 / 回池步骤 / J1-J5 判据 / 拉数命令）见 `docs/方案_T05_36X36_02重评估_20260910.md`。
- 触发 = WIN-1 达标（✅）+ 错窗确认；J1 = r_mean(02)/r_mean(01) ∈ [0.8,1.25]；不通过才走图 03。历史 51 局全负数据采于假糖时代，仅背景。**殿后等 WIN-5 batch1 收口 + WIN-3 难度轴上 108_02_duel 后再触发**。

### WIN-3 — 下一难度轴候选 🟡（归属 T6.4，一次一轴，WIN-1 达标后择一）
| 候选 | 红 hero→蓝镇距离 | /250 步 | 结论 |
|------|----------------|---------|------|
| ① 72X72_02_duel | 128 (51%) | 250 内可完成 capture | 本段 n=11 r_mean=+9.5（转正但底噪） |
| ② 108X108_02_duel | 200 (80%) | plen=201<250 已可达 | ✅ **09-25 定谳首选**：本段 n=13 r_mean=+78.7（正收益最强） |
- **09-25 定谳（停训窗）**：duel 三图 r 分布（本段 125 局）→ **108X108_02_duel 首选**（r_mean +78.7 明显为正，250 步方案已部署、#238"不可达"作废）；72_02_duel 仅 +9.5（转正但近零）。难度轴与激励轴/图池变更**错窗**——**WIN-5 batch1 维持 + 不开 batch2 后，择独立窗上 108_02_duel**。
- 250 步已全局生效（`STEPS_PER_EP=250`）；duel 副作用由 P10-target-2b 修复（duel move_to_force=60，r −1665→−123~−183）。挪城脚本 `py/win3_move_town_108_02_duel.py` 备而不用。③ 1v3 击杀/占城数据持续自然积累，不占窗口。
- **纪律**：难度轴与激励轴/图池变更不同窗。原"挪城修堵点"方案已撤销（72_02 全草地 BFS 可达，地形无墙）。

### WIN-4 — 官方 H3M 全量批转入池 ✅（09-21 收官，137/159 入池，PASS 率 85%）
- **收官**：H3M 转换七钉全拔，PASS=137 / FAIL=22（全定性关闭）/ SKIP=1；#290 已修。**唯一遗留 = B5 接线**（137 张进训练采样，等 T7.8 收口后接，渐进混合）。全量原文（工具链/FAIL 定性/vmap2h3m 反转/批转进展）见 `已完成任务.md`「2026-09-25 收敛归档 WIN-4」。

### WIN-5 — 多类型地图入训练体系（用户指令登记 09-19，后续做）
- **目标**：让"单层/地下/有门/有岛/strict/open"等地图类型都能作为可配置难度轴加入训练。
- **三层方案**：① 数据层 = `_pool_index.json` 5 维标签（`{layer, underground, island, gate, water}`）复用 `h3m_flat_pool.csv`；② 训练层 = `train_wsl2_ppo_v2.py` 的 `MAPS` 改读 `_pool_index.json` + `map_filter`；③ 转换层 = `py/h3m_batch_pipeline.py` 加 3 开关（`--keep-underground` / `--include-open-water` / `--include-gate-island`）。
- **关键约束**：① OBS_DIM=3464 冻结（地下/门/岛/水都不占 obs，只改地图文件+标签）② 错窗纪律（一次只加一个难度轴）③ open 水域需先评估 SHIP 造船激励 ④ 地下层保留 = 红蓝英雄可能不同层，strategic_env obs 同层假设需重验。
- **状态（09-25 停训窗定谳）**：① **训练层节拍机制实测定谳正常**——`HOMM3_POOL_INTERVAL=5` 确定性节拍（good_to_go + judgement_day 轮转）命中节奏精确（25/126 行 = 每 10 局 1 局池图），打点 `[POOL_SCHED]` 无异常。② **batch1 现况 = 仅 2 张**（`a_viking`×2 → 99、`elbow_room` → 99 已摘除），且**质量未达标**：judgement_day r_mean=-897.4 几乎全满步 250/250、大负 17/25 → **维持 batch1 这 2 张在池但暂不扩**。③ **batch2（`too_many_monsters` + `elbow_room_allies`，`_pool_index.json` 已标 batch:2）维持挂起不放行**——原"首批 5 张全绿 50 局"触发条件已被 a_viking 摘除打破，judgement_day 大负未翻正前不开 BATCH=2（防负数核心机制 `HOMM3_H3M_BATCH=1` 只放行 batch≤1）。④ **数据层 5 维标签 + 转换层 3 开关** → 启用窗按需补。⑤ 节拍判据取代 MIX 比例判据：每 5 局应见 1 局池图（已实采满足）。
- **09-26 池态现状（三档治理后，覆盖上方②③）**：服务器 `HOMM3_H3M_BATCH=2` + `POOL_INTERVAL=10`，实际在池 3 张 = good_to_go(+8) / judgement_day(**+392 翻正**) / too_many_monsters(**留观 1 窗**，分桶改善中 -616→-585，~50 局复核)；**elbow_room_allies 已 batch 2→99**（双窗证伪，#326）。教训固化：入池依据 30 步冒烟不可靠（09-21 verify 全是 steps=30），准入必须满 250 步整局 N≥4。
- **✅ 验收标准：下一次换地图（开 batch_ug 地下 14 张，09-26 定谳顺序先于 batch3）全部满足才放行**：
  | # | 条件 | 判据/证据 | 工具 |
  |---|------|-----------|------|
  | 1 | **duel 轴窗口收口** | 非池局 `HOMM3_DUEL_FOCUS=1` 的 108_02_duel 难度轴观察窗判据达标，且与本次换池**错窗**（WIN-3 纪律：难度轴与图池变更不同窗） | train_full.log 时间桶趋势 |
  | 2 | too_many_monsters 复核收口 | ~50 局分桶：改善 >20% 或正局 ≥5% 才留，否则摘出（#326） | 时间桶均值对比 |
  | 3 | 转换层落地 | `py/h3m_batch_pipeline.py --keep-underground` 转出 14 张地下图 + `_pool_index.json` 标 `batch:ug`（双副本：服务器热生效 + 主仓 #306 防漂移） | md5 双核对 |
  | 4 | OBS 同层假设重验 | 地下层红蓝英雄可能不同层，strategic_env obs 逐张验（OBS_DIM=3464 不动） | obs dump 对照 |
  | 5 | 满步准入 N≥4 | 每张满 250 步整局 ≥4 局，禁 30 步冒烟判（#326 定谳） | `py/_298_repeat.sh` 同款 |
  | 6 | 通用评测口径 | 新 ckpt 4 局 meanR≥0 且正局 ≥50%（T05 回池同款） | `py/eval_promo.py` |
  | 7 | 批转开关 | 全绿后 `HOMM3_H3M_BATCH` 加 u（ug 阈值）开地下批；未标 batch 图一律不放行（防负数核心机制） | unit `Environment=` + trainer 600s 重读 |
  - **后续批（batch3 水/岛 26 张）回池前置 4 件，一件不齐不放**：① 5 维标签+岛屿 4 张分型（`open_water_isolation`=mainTown 在岛而开局在大陆 = 水堵 hold）② 转换层 `--include-open-water` ③ 动作码 25 `BUILD_BOAT` + C++ 执行分支（动动作空间宪法，最重）④ 能力门 `[RECRUITED]`/局≥5（09-25 晚 duel 图 slot5=9 已开，GARR_REVISIT step>60 信号待 H3M 大图验证）
- **入池节奏四批定稿（09-21，用户纠正）**：`_pool_batches.json`（`py/_gen_pool_batches2.py` 生成）：
  | 批 | 触发条件 | 张数 | 说明 |
  |----|---------|------|------|
  | batch1 首批 | 无水+无岛+无地下 | 原 5 → 现 2 | `MIX=0.10 + BATCH=1` 启用 |
  | batch2 二批 | batch1 安全图余 + `ally_chaos` | **实 2 张**（09-21 旧述 ~7 未全标） | 首批 2 张全绿后 `BATCH=2` |
  | batch3 水/岛 | `water_touched` OR `has_island` OR `open_water_isolation` | 26 | WIN-5 造船激励轴落地后才入 |
  | batch_ug 地下 | `has_underground=1`（无水无岛） | 14 | `--keep-underground` 转换轴落地后才入 |
  - **首批 5 张定稿**（`_pool_index.json` 已标 `batch:1`）：`good_to_go_h3m.vmap`（include=0.47）/ `judgement_day_h3m.vmap`（0.34）/ `elbow_room_h3m.vmap`（0.33）/ `a_viking_we_shall_go_allied_h3m.vmap`（0.27）/ `a_viking_we_shall_go_h3m.vmap`（0.26）。两张 Viking 图 survey 未覆盖（`convert_fail` 旧记录），include 低+risk=none，先入首批但后续人工核对。
  - **09-25 逐张摘除去向定谳（原 5 → 现 2，摘 3 张各因何）**：
    | 图 | 现状 | 摘除/保留原因 |
    |----|------|--------------|
    | good_to_go_h3m | ✅ 留 batch:1 | include 最高 + 验证全绿 |
    | judgement_day_h3m | ✅ 留 batch:1 | 在池但**质量未达标**（r_mean -897 满步大负 17/25），"留但暂不扩" = 09-25 batch2 挂起核心依据 |
    | elbow_room_h3m | ❌ 摘 → batch:99 | **竞态偶发**：A/B 实测 ~33% rc=124/139（基线 4/6 异常，与 09-23 修复无关），400s 看门狗全进程重启风险 → 摘出池降级**冻结复现专用探针图**（09-24） |
    | a_viking_we_shall_go_h3m | ❌ 摘 → batch:99 | **满步能力真空**：两局 -934.7/-891.4，250 步满步 act3 刷墙循环，大图 250 步内够不到蓝方 → 能力没到位前入池只污染 PPO 样本（09-22 50 局判定） |
    | a_viking_we_shall_go_allied_h3m | ❌ 摘 → batch:99 | 同上（a_viking 双图同因一并摘除） |
    **共性**：3 张摘除 = 1 竞态（elbow_room）+ 2 满步能力真空（a_viking ×2），皆"现入池弊大于利"；等竞态根治 + 长距离推进能力提升后再议回池。
  - **二批**：`too_many_monsters_h3m.vmap`（include=0.26）+ `elbow_room_allies_h3m.vmap`（include=0.45，`ally_chaos`）。
  - **启用时点**：T7.8 观测窗收口后 → `HOMM3_H3M_MIX=0.10 HOMM3_H3M_BATCH=1` 启动训练（systemd unit `Environment=` 加两行）→ 50 局后判绿 → `MIX=0.15 BATCH=2` → batch3/batch_ug 等各自轴落地后再扩。
  - **防负数核心机制**：`HOMM3_H3M_BATCH>0` 时只采 `_pool_index.json` 里显式标了 `batch` 字段且 `<= 阈值`的图，没标 batch 的一律不放行（水/岛/地下图未标记 = 不入采样）。踩坑 #298 已记录。
  - **09-25（午后）岛屿类 4 张待分型 + 水堵 hold 判据（新增，WIN-5/SHIPPING 行动项）**：`island_king_h3m` / `island_king_allied_h3m` / `thousand_islands_h3m` / `thousand_islands_allies_h3m` 冒烟 40 步全绿（0 崩，r≈-209/-224/-266 正常区间）后曾临时标 `batch:3` 入池。09-25 用户问"模型还没学会造船"→ 实查三层门：① 25 动作码**无 BUILD_BOAT**（BUILD_1/2/3 只走 3 条建筑 tier 链）② 训练日志 `[RECRUITED]` 总行数=0（招兵断流）③ 转换层无 `--include-open-water`。实测 `island_king_allied` 在池采出 `R=-464.7 nsteps=250`（水堵 signature：满步全负无终局），动作空间里**不可逃** → **回退决定**：岛屿 4 张撤 `batch:3` 标签（回 None=不放行），`HOMM3_H3M_BATCH=3→2` 重启（MainPID 1055471），训练池回到 batch1+2 共 4 张。
    - **分型判据（数据层，低成本逐张判）**：读 `header.players.*.mainTown` 坐标 + `surface_terrain` 该格——mainTown 在岛而开局在大陆（中间开放水域隔离）= **开放水域隔离类**，BUILD_BOAT 落地前必须 hold 出池（同 a_viking 的 batch=99 hold 待遇）；mainTown 自洽在岛上 = 普通岛屿类，可正常训。
    - **回池前置 4 件（一件不落地都不放）**：① 数据层 5 维标签（`water_touched`/`has_island`/`open_water_isolation`）② 转换层 `--include-open-water` ③ 动作层预留位 25 加 `BUILD_BOAT` + C++ 执行分支（动动作空间宪法，最重）④ 能力门 `[RECRUITED]` 回升（**09-25 晚已开**：C++ dst=cur 修复后 duel 图 slot5 RECRUITED=9/局≥5 达标，0.03×dp 奖励轴复活；见 WIN-5 末 C++ 根因修复段 + 知识库§5.20）。**09-25 量化现状（追加，修复前快照存档）**：全量 ep 日志扫 `[ECON] recruit`（发出招兵指令 act16/17/18）= **1212 次**（集中 step20-26 强制采样窗口，脚本引导非模型自主）vs `[RECRUITED]`（英雄军队 power 增量确认 dp>0）= **0 次** —— 判据"能力门 RECRUITED≥5"修复前 0 未开；且 `[RECRUITED]` 恒 0 ⇒ 兵力增量奖励 0.03×dp 一路恒 0，PPO"攒兵"激励轴死信号。⚠ 注意 1212 次里绝大部分是 Level-3 强制采样窗口喂的引导动作，不能当"模型会招兵"的证据。**09-25 晚更新：本段"RECRUITED 恒 0"定案被 C++ dst=cur 修复推翻（第四层根因=英雄邻接城未 visiting→getUpperArmy 落城 garrison）——修复后 duel 图 RECRUITED 已出（slot5=9），本段为修复前快照存档。**
    - **取兵链路恒 0 根因定案（09-25 深挖三层，取代上方"三假说待分叉"）**：执行链 = `adventure_send_action` → C++ `AAI.cpp` case 16-18 → `cb->recruitCreatures(town, dst=town->getUpperArmy())` → `RecruitCreatures` pack 发服务器（`CCallback.cpp` L62，dst 实参与）→ 英雄 visiting 时 `getUpperArmy`=英雄部队 ⇒ **招兵本可直上英雄部队**。恒 0 = 三层叠加，全结构性非模型：
      L1 空招源：兵只能招自已建巢穴，周招兵额按 `newWeek` 刷新，开局周额=0 时 `recruitCreatures` 引擎 clamp 招 0（扣 0 钱）；
      L2 永久拉黑：runner A3 空撞判定——英雄出生在城上 → 开局取兵窗（4 步 RECRUIT）→ 窗结束复核 dp=0（招兵未生效/额=0）→ `TOWN_EMPTY`（累计 108 次，集中在 step 3-5）→ `own_town_blocked` 本局整城剔除；
      L3 通路全灭：此后英雄再回访该城取兵窗不再触发（blocked 命中）+ act22 GARRISON（C++ L318 `swapGarrisonHero`）被 runner `logits[22:24]=-inf` 永久屏蔽 ⇒ 招出的兵永远堆 garrison，英雄军队 power 恒 0 ⇒ `[RECRUITED]`（dp>0）结构性恒 0，0.03×dp 奖励轴死信号。
      **结论：能力门判据 RECRUITED≥5 在修 runner 前永远是假阴性（1212 次招兵指令均出自强制窗引导，非模型行为）**。
    - **彻底修复（09-25，本会话执行）**：最小改动修 runner 取兵窗——`TOWN_EMPTY` 首撞改"重试"：`own_town_blocked` 永久拉黑降级为 `own_town_empty_count` 计数，首撞只记 1 + 打点 `[TOWN_RETRY]` 放行后续取兵窗（周额刷新后重撞），二撞（≥2）才真拉黑；不改 C++、不动动作空间、不动 act22 屏蔽。同步修正 runner L699 过时注释（"garrison 字段恒 0 未实现"——C++ P3 已在 `fill_v3_fields` L736-744 填驻军，obs 城槽 field 6-12 可用）。部署=`SIGTERM 停服 → 覆盖 runner → 重启续跑`；验=`[TOWN_RETRY]` 出现 + 后续局 `[RECRUITED]` 首次非零。
      **09-25 错窗部署定局（用户拍板"错窗部署"）**：取兵修复 + 方案B（13/14/15 开放，对方 `1533a1da`）合到 git 真相源 `4288b1db` 后**同窗全上**——双路 `/mnt/d` 与 `/DATA/hero3` 同 inode（bind 挂载，cp 报"同一文件"），覆盖 1 份 = 双路生效，md5 `85374ba`，重启 MainPID 1107513，`crashlog=0 / NRestarts=0 / ep_runner=12`，`TOWN_RETRY` 已出 16 次。⚠ 判读注意：方案B 与取兵修复同窗，13/14/15 行为变化与取兵效果会混叠；若崩率/满步局异常，先查 nsteps=250 局是否集中在开放 13/14/15 的局。回退垫=原始 09:03 版 `3f8d681`（`/DATA/hero3/ep_runner_one.py.bak_recruitfix_0925`）；`.bak_a3only_0925` 内容=85374ba 与真相源同（部署时误标，无回退价值，已改名 `_same_as_source`）。
      **09-25 方案B 扩表（13/14/15 进强制窗）**：查证 13/14/15 在 148 份 traj 0 选中（掩码开了但无样本无引导，靠 PPO 自己探索永远探不到 logits 极负的码——08-19 "bias 对从未见过的码无效"同款教训）→ `econ_acts` 由 `[16,17,18,19,20,21]` 扩成 `[13,14,15,16,17,18,19,20,21]`（9 项轮换，前 24 步 + 每 40 步 4 步窗），并加 `[MOVE_ARMY]` 纯观测打点（每码每局上限 3，不发奖励）。副作用（已知悉）：1v1 单英雄下 13/14/15 C++ 侧 `noTarget` → 退化为 END_TURN，即前 24 步窗内每 9 步有 3 步提前结束回合（周额刷新反而更快，对取兵链路是正偏）。 **打点保留决策（09-25 用户拍板）**：`[MOVE_ARMY]/[TOWN_RETRY]/[RECRUITED]/[ECON]` 全保留不撤，全部跑通（RECRUITED 首非零 + 能力门开）后搭车下次 runner 部署再降级/撤；降级口径=每 ep 1 行汇总，撤 print 但保留 `[TOWN_EMPTY]` 二撞故障打点 + 拉黑逻辑。
    - **09-25（晚）RECRUITED C++ 根因修复 + ABI 事故恢复（RECRUITED 首非零验收达成）**：回城（runner 粘滞，ecd78a8c）已生效但兵仍堆 garrison（94 次 RECRUIT 全发出、hero_army 0/16 局涨幅、342 帧邻接城 is_garrisoned=0）→ 真根因在 C++ 执行侧 `AAI.cpp` case16-18 `dst=getUpperArmy()`（邻接未 visiting 时落城 garrison）。修复（submodule 892343da）：RECRUIT 前复用 case22 moveHero-进城范式使英雄真 visiting + `dst=cur`（引擎校验 CGameHandler L2463 允许 visitingHero）；补 `onNewSystemMessageReceived` 打桩 + 5 参 `showGarrisonDialog`（本地 .cpp 陈旧缺口）。部署中踩 ABI 事故：whole-file scp 陈旧 .cpp → 16 worker 全 rc=1 崩 boot → 止血 → 09-24 回退垫缺 init_vcmi 仍崩 → 补全源码后同 build 树连贯重编 + 双符号 nm 验证 + 受控 boot 全绿 → 起服（踩坑§2.22，知识库§5.20）。**验收**：1860744 `RECRUITED=9`（step3-22 开局取兵窗），slot5 hero_army 160→16830 活英雄帧（非死亡帧假信号），rc1=0 NRestarts=0。duel 能力门 RECRUITED/局≥5 已开。**GARR_REVISIT（step>60 回城）的 RECRUITED 信号待 H3M 大图局推进到 step>60 验证**（这批全在 duel/课程图 step<60）。
    - **顺序定谳（09-25 讨论）**：batch2 之后走 **batch_ug（地下 14 张，转换层 `--keep-underground` + OBS 同层重验，不动动作空间）** 先于 **batch3（水/岛 26 张，4 前置全齐才入）**；batch_ug 性价比更高。

### WIN-6 — PPO-DNA 并行化（N_SUBPROC=4 灰度上线，09-25）
- **目标**：直击串行 1 局 5-600s + GPU 利用率近 0 的吞吐瓶颈，4 个 ep_runner 并行 Popen，GPU 利用率提升 ~9x（PPO 周期从 ~2700s 降到 ~300s）。
- **架构**：`train_wsl2_ppo_v2.py` 主循环分叉——`N_SUBPROC=1` 走原串行路径（零行为变化），`N_SUBPROC>1` 走 slot 池化路径（Popen 非阻塞 + 2s poll 收割 + buffer≥BATCH 阻塞跳出进 PPO 更新）。traj/ep_ckpt/ep_log 按 `_{slot}` 后缀隔离（踩坑 #302 同款 EPERM 防护）。
- **内存前置**：`.wslconfig` memory=12GB + swap=4GB（09-25 新建，WSL 默认 50% 上限实测 7.6Gi → 扩后 11Gi，N=4 占 38% 留 6GB 安全垫）。
- **cmd 对齐**：`_spawn_slots` 的 cmd 构造与 `run_episode` L254-318 逐条对齐（HERMES env 化 + move_to 分支 + T06 act_loop_from_step + WIN1_ENV_ARGS + #296 grep 降噪），确保 N=4 时策略输入 = N=1 完全一致。
- **全崩兜底**：4 slot 全崩时 while 死循环风险 → 连续 3 轮无新 traj 强制跳出进 PPO 更新（不卡死）。
- **SIGTERM 清理**：`save_shutdown` 清理 N 个 ep_runner + grep 管道，`if "slots" in globals()` 防御（slots 定义前 SIGTERM 不 NameError）。
- **灰度状态（09-25 已上线）**：`HOMM3_N_SUBPROC=4` 生效中，4 ep_runner 同时跑（PID 979/982/985/988），checkpoint resume step=1012552。
- **回退**：`HOMM3_N_SUBPROC=1` + `systemctl daemon-reload` + 重启训练 = 完全恢复串行（零行为变化）。
- **观察项（1 周）**：① 晋级判据 5 指标 vs 串行基线（avg_r / vloss / 正局率 / 大负率 / GUARD 首胜率）；② 全崩兜底触发频率（`[SLOT] 全崩兜底` 行）；③ 内存 available 维持 > 6GB；④ 对手池 70/20/10 并行多样性爆炸（可固定 N slot 同对手）。
- **⚠️ 09-25 09:0x 迁移终止注（本侧登记）**：WSL 侧 `HOMM3_N_SUBPROC=4` 灰度随 `homm3-train-v5` 停止（训练迁移服务器 172.16.2.40，见 S-10）**事实终止**——slot 池代码已由对方 `bc451b68` 入史、并经服务器 N=4 POC 验证（4 局全 rc=0 吞吐 ×2.2），后续并行训练全在服务器 N=16 全量跑（`homm3-train-server`），WSL 不再跑 PPO。上方 ①-④ 观察项改挂服务器全量跑（看 `train_full.log` + `systemctl is-active`），本条仅作历史指针。
- **踩坑**：#310（Popen 池化结构 bug，for ep 循环 N 轮后退出 buffer 永远攒不满）/ #311（_spawn_slots cmd 漏 move_to_force 按 map 前缀分支）/ #312（WSL 默认内存 50% 上限需前置扩 .wslconfig）/ #313（save_shutdown 清理 slots 时 NameError）/ **#314（`[SLOT] 全崩兜底` 日志措辞误导，09-25 已改为「batch 满跳出」，rc=0=健康退出/rc≠0=崩溃）** / **#315（H3M 池图 obs 全零三层叠加根因，非单一竞态，09-25 排查结论）**。
- **指针**：知识库「09-25 PPO-DNA 并行化」节 + 踩坑 #310-#315 + `py/train_wsl2_ppo_v2.py` L31-36 / L226-419 / L582-728 / L777-815 / L546-557。
- **日志口径（09-25 补，防误判）**：并行 `[SLOT]` 行打印 `avg_r=`（= `np.mean(traj["rew"])` 逐帧均值），串行 `r=`（= `total_rew` 整局累积）——口径不同但都是奖励指标；`rc=0`=子进程正常退出（健康）/ `rc≠0`(-6 SIGABRT / -11 SIGSEGV / -15 SIGTERM)=崩溃被 `[FILTER]` 丢弃。4 slot 全 rc=0 = 全健康。
- **H3M 池图 obs 全零排查（09-25 收口）**：N=4 灰度里 King/good_to_go/judgement_day 偶发 `[FILTER] obs_nz=0 首拍全零丢弃`，**非单一「引擎 reset 竞态」，实为三层叠加**：L1 地图 `header.players=[]` 空数组（确定性，#222/#223 已修，主因——`h3m2vmap` 导出漏注入 players → 引擎不建玩家槽位 → obs 城段 owner 全 0 → `no_own_town` abort）；L2 引擎 reset 冷启动竞态（偶发 ~4%，#214 残余，H3M 大图 reset ~600s 竞争窗口大）；L3 XDG 目录缺失（已修 #308，独立故障线，走 `[WARN] traj 读取失败` 非 obs_nz=0 分支）。#228「obs 3464 修复」是败北信号断链根修（局末），与此（局首）不同源。**下一步**：N=4 若 H3M 仍见 obs_nz=0，优先跑 `py/sync_maps_to_runtime.py --strict` 验在池 3 张 H3M 的 `header.players` 是否修净（确定性根因 L1），勿归因「竞态」。

---

## 二、观察期任务

### T7.8 — A3 战力闸观察窗（踩怪战死治理 + B 攒 100 局）🔄（09-19 部署）
- **由来（一行）**：T7.7 观察窗实锤 #283 推翻——困死 0/120、**战死 72/120=60%**。duel 净胜率干净基线 **-23%**（旧 +21% 为污染口径不可比）。
- **A3 战力闸（09-19，四改动 + 二修三修五处追加）**：① `get_guards` 真实战力；② SCORE BFS 避怪；③ `candidate_power_c` 重写；④ F 硬闸；⑤ power_self 军队战力公式；⑥ f_min -1.0→-0.2→0.0；⑦ 蓝英雄硬闸 -0.3→-0.1；⑧ 采样硬 mask `--monster_mask` 默认 1；⑨ mask 豁免2（贴脸目标不 mask）。
- **满窗评估结果（09-20，108 局）**：① 战死 38%（41/108，接近未达）；② duel 净 -43%（恶化 vs 基线 -23%）；③ 大负局 12+；④ avg_r 全段负（-77~-144）。**结论：A3 九改动净效果为负**——战死治了一半但 capture/守卫两条大奖路径被约束堵死 + 振荡大负局比战死更亏。
- **大负局主源（09-20 源码解剖）**：`Φ_guard = -0.5×dist` 绕怪暂增被系统性重罚 + shaping 无 γΦ 终结修正 → **A3 四修（09-20）**：① potential 终结修正（done 时补 -Φ(s')）；② 守卫梯度降权 -0.5→-0.15。
- **两 bug 修复（09-20，主仓 94b1e2c）**：① RECRUITED 死亡帧假信号（ah<0 帧跳过）；② 贪心回退穿怪（优先非怪格方向）。
- **#292 横跳熔断（09-21 三次修复，终版 c61e4db）**：`ts_pick_count` 同目标累计 pick ≥8 → `[SCORE_BLACK]` 拉黑 + [GUIDE_OSC_DROP] 横跳熔断 + zigzag 计数每拍更新（奖励面零变更）。冒烟：-803 → step28 熔断 → 190 步正常收局 r=-393。
- **回滚对照定谳（09-21 深夜）**：EARLY vs CURRENT × 3 关键图各 1 局——均值 **-400.3 vs -290.9（当前好 27%，训练非负优化）**。结论：训练方向有效，继续攒窗，无需回滚。绝对水平仍负 = 1v3 能力问题，等图池混合轴。
- **WIN-5 batch1 上线（09-22 00:5x，T7.8 85 局收口评估后）**：85 局评估（判据③✅ 0 大负 / ①65% ②-68% ④-68 均未达，两窗同分布 = 护栏收益吃满，能力轴是主矛盾）→ 用户拍板批次制。unit 已上线 `HOMM3_H3M_MIX=0.10 + HOMM3_H3M_BATCH=1`。**batch1 50 局判定（09-22）**：课程图基线 ✅绿 / batch1 曲线 ⚠️黄（a_viking 两局 -934.7→-891.4）/ 止损 ❌红（两局均 250 步满步 act3 刷墙）→ **a_viking×2 batch 1→99**（hold_reason 入 index），batch1 剩 3 张继续观测。
- **#297 开局随机失败 + batch1 混合轴挂起（09-22 深夜）**：batch1 剩余 3 张上线后 100+ 局零出现 → **开局概率性失败静默吞局**（query -1 卡 300s 或 segfault；训练循环 traj=None continue 无痕跳过）。处置：MIX=0 挂起混合轴（BATCH=1 开关保留），纯课程图训练继续。
- **A3 五修（09-23 02:56 部署生效）**：72_01_duel 9/10 战死局解剖定谳根因 = 英雄踩**中立守卫**（monster_* aggression=guard）战死，原 F 硬闸只检蓝英雄。**改动**：① `target_scorer.py` F 闸扩展 is_guard=True；② `power_feasibility` 改比例公式 d=ln((ps+1)/(pc+1))-log_margin(0.5, e^0.5≈1.65 倍)，temp=0.5；③ `ep_runner_one.py` L1139/L1268 两处内联 F 公式同步。自测 `py/test_f_gate_neutral_guard.py` 全 6 case PASS。部署 = 停训窗 `systemctl stop` → 双副本 cp 同步 → `systemctl start`。
- **09-23 进展（详见顶部 09-23 收口段）**：① 冻结源定谳 = NK2 两条 AI 线程卡 `CClient::sendRequest` 死锁；② 三套补丁（栈打点/网络线程方案1/upgrade 熔断 cap 8）4/4 全绿；③ boot hang 看门狗（400s 无 kick → `os._exit(43)`）；④ 三处 08-17 修复恢复（`py/patch_298_restore_0817.py`）；⑤ 重大勘误：07:28「4/4 全绿 = 冻结消失」是单样本侥幸，**冻结从未真正消失**。
- **09-24 进展（详见顶部 09-24 收口段）**：① `elbow_room` 移池（batch:1→99）；② 冻结 Mode A 定谳并修复（占城胜利在 moveHero 包处理中途 `setState(SHUTDOWN)` 立即杀网络循环→包流截断→NK2 永久卡死；修复=`py/patch_mlfix_shutdown.py` 延迟到 `onPacketReceived` 完成当前包再停机）；③ 崩溃 rc=139 两处修复（`GameStatePackVisitor` 英雄战死后 mana 恢复判空 ×2 + `AAI::battleStart` 无英雄防守方判空；`libvcmi.so`(842a954c)+`libmlclient.so`(83b1cfd2)）；④ Mode B 定谳未修（NK2 建筑评估持 `CGameState::mutex` 读锁长规划→`runNetwork` 写锁饥饿，架构级另排）；⑤ 训练 10:16 重启 resume step=951759；⑥ 续窗 game_over 强制置位（`py/patch_mlfix_gameover.py`，elbow_room ×12 验证 3/12 冻结 = Mode B 维持另排，崩溃 0/12，自检 15/15，`libmlclient.so`=c1c43450，训练二次重启拾取 PID 13830）。
- **判据（100 局后复跑 `eval_new_baseline_0919.py`）**：① 战死率 <35%；② duel 净胜率 >0%；③ 大负局（<-300）清零或 <3 个；④ avg_r >0。
- **回退**：全部 Python 旁路零重编；monster_mask=0 关 mask / f_min 摘参 / 硬闸+军队公式 git revert（ea259f0 前后两 commit）。
- **指针**：T7.7（前置，✅ 收口）/ 踩坑 #285/#286/#298/#299 / 知识库 09-19/09-23/09-24 章 / ep_runner_one.py L218-237（get_guards）/ L591-606（采样 mask）/ L1098-1102（BFS 避怪）/ L766-777（power_self）/ L1053-1072（BHERO_ATTACK 军队公式）/ target_scorer.py L62-80（比例公式 power_feasibility）/ L313-323（F 闸三分支）/ ep_runner_one.py L1139/L1268（内联 F 同步）/ py/anatomy_72_01_duel.py / py/test_f_gate_neutral_guard.py / train_wsl2_ppo_v2.py L179-184（f_min=-0.2）。

### T7.7 — 根因C'修复观察窗 ✅（09-19 收口，判据达成 → 演进 T7.8）
- **收口**：RED_DEAD 即时终局 + B 方案 BFS 绕行，判据达成后演进为 T7.8。全量原文（修复内容/观察判据/拉数命令）见 `已完成任务.md`「2026-09-25 收敛归档 T7.7」。
- **指针**：知识库 09-19「startBattle 引擎层直调」章（D 方案储备）/ 任务清单七-远期 D 方案条 / ep_runner_one.py L1607-1614（RED_DEAD）/ L1054-1062（B 方案）。

### T7.4 — 死亡惩罚 ✅（09-21 收敛关闭：判据 1 已定性，无剩余动作）
### T7.6 — A2 攻击步旁路单局冒烟 ✅（09-18 达成：引擎侧全链路打通，红蓝英雄真实战斗首胜）
- 两条 ✅ 原文（冒烟步骤/判据/回退线/判据 1-3 收敛）见 `已完成任务.md`「2026-09-25 收敛归档 T7.4/T7.6」。
- 指针：`docs/方案_攻击步旁路_20260917.md` §6 / 踩坑 #254 / 知识库 09-18 晚 B+C 章。

### T7.5 — 经济奖励撤梯子 S2-S4 🔄（**S2 已部署生效 09-19 凌晨**；S3 等 S2 无退化 1 窗）

| 步 | 触发 | RECRUIT/次 | BUILD_2/次 | 兵力系数 | 建设奖 |
|----|------|-----------|-----------|---------|--------|
| S1 | ~~当前生效~~ | +0.5 | +0.75 | 0.02 | 无 |
| **S2 🔄** | **09-19 部署在线** | **+0.25** | **+0.375** | **0.03** | **低档巢 +3** |
| S3 | S2 无退化 1 窗 | ≈0.125 | ≈0 | 0.04 | 低+5/高+10/防+5 |
| S4 | T05 换图节点 | 0 | 0 | 0.05 定稿 | garrison + R6 大调同批 |

- S2 改动（commit `ab93213`，ep_runner 四处 + DEBUG_FORCE 调试件退出）：RECRUIT L1496 / BUILD_2 L1515 / 兵力系数 L1469 / 低档巢 +3 新增（BUILD_NEW 点，mask 低两位=T1/T2 巢穴）。**生效验证**：[ECON] recruit +0.25 / build2 +0.375 已见主日志。
- 每步判据：avg_r 跌 <20% + 招兵/建设频率不塌 + 取兵窗维持（攒 40 局后看板复查）。顺序：~~WIN-1 达标~~（✅）→ S2（✅ 在线）→ S3 → T05 换图节点 → S4。
- 与 BUILD P1 错窗：S2 窗收口后 BUILD P1 入池切换（A11 备料就绪）。
- D3 熵 bonus（entc=0.05×(1+exp(−step/200万))，09-15 部署）+ C2 L0 崩溃插桩均在线，细节见已完成任务 09-15/09-16 条。
- **🛑 S2 收口 FAIL（09-19，#283 困死定性后）**：新基线 40 局（实跑 52 局）判据 `[RECRUITED]/局` 13.2→0.6 塌 95%，与 62% all-blocked 困死同根因（英雄被困→招兵断流）；建设轴健康（build2 持平/首建每局触发）。**判 FAIL 不触发 S3**（错窗纪律：S2 未通过不得叠 S3 难度轴），不触发 blue 降级（#283 拍板）。**正确动作**：维持训练攒局让模型适应新环境（学会突围、恢复招兵能力），`[RECRUITED]/局` 回升到 ≥5 后再做 S2 正式收口复查。

### OBS — 长期观察集（不占窗口，复发才行动）

- **OBS-1 R7 battle query 卡顶**：埋点五件套+300s fuse 在位，复发按 map×secs/steps 聚合定位。
- **OBS-2 BHERO_KILL 空拍**：双帧确认+空拍跳过防护生效中（WIN-1 批次A同源护栏）。
- **OBS-3 英雄战斗未触发（旧登记"aggression 遗留"方向已证伪，09-17 改述；09-18 闸门已部署待验证）**：✅ 09-17 预研定谳（`docs/预研_OBS3英雄战斗触发链与BUILD链_20260917.md`）——引擎 `moveHero → blockingVisit → onHeroVisit → startBattle` 链**无条件触发战斗，无 aggression 开关**；真闸门 = **观测 passable 通道**：旧 `isClear` 口径把敌英雄格/城格/怪格全标 passable=0 → 模型动作掩码 -inf / MOVE_TO 排除 / 蓝 AAI 跳过 HERO 三方结构性不可达（**09-18 方案甲已修**：strategic_state.cpp 两处口径改 `!(blocked() && !visitable())` 对齐 GHandler，重编+重启训练，验证 4 判据见 WIN-1 段；回退 = .so 备份恢复）。A2 攻击步旁路代码已实现（默认关，`--blue_hero_attack_bypass=1` + `--attack_f_min`，#254 已修映射；旁路冒烟 T7.6 与闸门窗可合并执行）。OBS-3 附带产出：BUILD 链断点 = dungeon fort 前置 + 每日一建 + 观测盲区（P0-P3 备料落点见预研 §2.2）。
- **OBS-4 TOWNSTALL 引导可达性**：平台段误判已修（#209），继续观察。
- ~~OBS-5~~ 已定性关闭（09-15，开局全堵 zombie 自然终局），归档见已完成任务 09-17 条。
- **OBS-6 #296 `Cannot answer the query -1` 刷屏降噪**：✅ 09-23 Popen 层 grep -v 管道已部署（`train_wsl2_ppo_v2.py` L275-298），hermes 日志 0 行（重启前 498 行/局），训练链路正常推进。#296 勘误：报错实际位置 = `lib/callback/CCallback.cpp:53`（libvcmi.so 内），非 mlclient。详见知识库 09-23 章 + 踩坑 #299。

---

## 三、并行主线：T6 NK2 逻辑融合

| 子任务 | 状态 |
|--------|------|
| T6.1 NK2 估值→奖励塑形 | ✅ 冻结，v5 实跑中 |
| T6.2 C++ BFS 全图寻路 | ✅ 完成；NK2 寻路加速 #7632 已 merge（09-11） |
| T6.3 决策结构分层 | ⬜ 远期（与 T5.9 合并） |
| T6.4 训练地图体系 | 🔄 MAPS=10 大图池（总任务五）；WIN-1 批次A 同窗混跑 |

---

## 三-B、并行基建：172.16.2.40 服务器三件套 ✅（09-24 部署完成）

> 定位：推理端 + 评测农场 + CI 构建节点（训练仍留 WSL2 RTX3060）。明细见知识库「十五、172.16.2.40 服务器三件套部署」+ 踩坑 #55-60。

| # | 项 | 状态 | 要点 |
|---|---|---|---|
| S-1 | 推理服务 | ✅ online | `vcmi-inference` systemd，`:8080` 局域网可达，80 SB3 模型已加载，批 1.6ms/obs；`POST /models/load` 可热推 checkpoint |
| S-2 | 批量评测 | ✅ 管线重建完成（09-25 凌晨）→ **旧 ABI 链已退役（09-25 上午切新 ABI）** | 09-24 晚 WSL 源码同步误覆盖 ML/ + 毒构建树 → 全 4 层 .so 修复（知识库 §5.9 / 服务器踩坑点 §2.6-2.8），RTLD_NOW 全链 dlopen 全绿；KTV 4 局 4/4 DRAW 0-error。**09-25 上午按用户拍板切新 ABI（见 S-7），老 SB3 256 维链（strategic_worker/subproc_env/batch_eval_pool + ppo_k2v_* 模型）事实退役**（06:3x B2 已隔离 `/DATA/hero3/quarantine_old_256/`，回滚垫 06:25 B3 持久化到 `quarantine_old_256/rollback/`——原 `/tmp/abi_switch_backup_0925` 已迁走，/tmp 重启即丢）。推理服务 :8080 不受影响（ONNX 不加载 libmlclient，active + 81 模型） |
| S-3 | Actions runner | ✅ CI-ARM64 全绿 + ABI 守门 job（09-25） | `kunpeng-920-arm64`（user=runner, systemd enabled）。`arm64-ci.yml`（workflow 366453367）两 job：① lint-arm64（修 2 坑：`safe.directory=*` + 砍 setup-python 吃系统 python3.11，run 36063423686 全绿）② **abi-gate-arm64**（仓内 connectors/v13/14/15 对服务器活引擎 ABI 全量重编 6 pybind 模块 + RTLD_NOW dlopen 验链接，commit 1694cb2，run 36067279374 全绿）——防 09-24 毒化再犯。引擎本体重编不入 CI（留手动）。详见 知识库 §5.13/§5.14 / 踩坑 §2.13 |
| S-4 | checkpoint 自动推送链路 | ✅ 完成 | WSL 训练存档→导出 ONNX→scp→`:8080/models/load`+activate→WIN RATE 评测→结果 POST `:8088` 结果中枢 + scp 落盘；触发器 cron（`ckpt_watcher.sh`）或训练尾部 hook。**09-25 勘误**：原"服务器跑不了 3464 维新模型"已过时——新 ABI 已上服务器（见 S-7，`vcmi-inference` :8080 现载 81 模型含 `wsl2_ckpt_*`，WIN RATE 评测可直接在服务器 ARM 上跑） | **09-25 实推**：最新 ckpt 1003121 导出 ONNX（parity allclose，3464 维）→ scp → `:8080` load（80→81）+activate，`/predict` 3464 obs 出有效 action ✅（推理面证明）；WR 评测面归 S-7 铺开（待拍板）。导出依赖 onnx 包走 Windows 侧 cp312 wheel 离线装（WSL 当日 PyPI 断）。 |
| S-5 | 跨图并发评测 worker_crashed 治理 | ✅ 根治闭环销项（09-25，全池 07:23 验收确认） | 根因=跨图并发 boot 共享资源堆损坏（#309）。flock 根治：老链 `vcmi_gym/envs/v13`（`.bak_s5flock`）+ **新树 `vcmi_gym_new/envs/v13` 已移植**（`.bak_s5flock_new`，pool driver 头注「每 worker 只 boot 一次 VCMI, flock 仅串行 boot」）。**最终验收（09-25 07:23 全池收工）**：147 图 × 4 局 × 32 worker（pid 700518）全程 **0 worker_crashed**、`DONE maps=147 err=0`，对比修复前跨图崩率 42%（#309）= flock 根治成立，销项。09-25 05:14 我 B3 12图全 err=撞退役老链误诊已撤。详见 踩坑 §2.14(铺坑)/§2.15(验收闭环) |
| S-6 | 全地图横评（147 图胜率矩阵） | ⏸ 已停（09-25 03:11 启动 → 04:4x 用户下令停） | 跑了 ~1.5h 仅 1/147 图打印（`Adventures of Jared Haret` W=0/16，早期退出图 373s），**未出 JSON 即停**（JSON 只在整批跑完才落盘）→ 本次跑图结果=无矩阵产出。停因：ETA 不现实（慢图单图 ~11.7h，全池 4.6 波）+ 期间用户停 llama-server 让核。主进程 426288 + 32 pool worker + 33 strategic_worker 全清（`pkill -9 -f "batch_eval_poo[l]"/"strategic_worke[r]"` 不自匹配写法），watcher/flag 已清。重跑选项：① 降 `games_per_map` 16→4（耗时÷4）② 先跑 top-N 高价值图子集 ③ 保留 16 局但接受多日窗口。**判读要点沉淀在知识库 §5.10 / 踩坑 §2.9**。**注（09-25 上午）**：S-6 用的是退役的旧 SB3 链（StupidAI + 5000 天 cap），基线不对齐新项目；重跑改走 S-7 新 ABI 链（MMAI_RANDOM + 250 步 cap + torch 模型），规模 top-N × 每图 4 局。 |
| S-7 | 新 ABI（3464）评测链部署 | ✅ 完成销项（09-25）：冒烟 05:25 → 全池 07:23 收工 | 用户拍板"弃旧保新"：服务器 ABI 从 7/25 旧 256 链切 WSL 新 3464（知识库 §5.11 / §5.15 / 踩坑 §2.10-2.11）。已落：`vcmi/ML`=新 ABI 源 → ARM `libmlclient.so`；`vcmi_gym_new`（3464 M6）+ ARM `connector_v13/14/15.so`；`ep_runner_one.py` + `wsl2_model.pt`（torch）+ `strategic_reader.py` 全在 `/DATA/hero3/`；包 `__init__` 战斗层 guard（`.bak_battle`）。冒烟 KTV 单局 `err=no` r=-265.5 steps=40。**全池完成（pid 700518，05:45 起，07:23 收工）**：`batch_eval_newabi_pool.py` 147 图 × 4 局 × 32 worker = 588 局，蓝方 `MMAI_RANDOM`，`DONE maps=147 W=0 DRAW=588 err=0 secs=5565`，**0 worker_crashed**；步数分布 ≤45 步 453 局（ENDTURN_FUSE act10 截断为主）/46-82 步 135 局，无超 250 cap。**判读口径（用户 09-25 纠正，非 bug）**：训练分布=VMAP 图（怪少资源多），H3M 池大量岛屿/地下层地图未训过 → 走不出迷宫 act10 空转 fuse 截断是**分布不匹配预期行为**，DRAW 不作能力信号。结果 `/DATA/hero3/output/batch_newabi_pool_20260925_072335.json`（55KB，watcher `NEWABI_POOL_DONE`）+ 逐局明细 log。记档：C++ 连接器 terrain 写硬编码 `/home/administrator/vcmi-workspace/` 触发 21274 次（非致命，下次重编 C++ 时修）。**数据层注（另一层，非部署层）**：矩阵 go 字段全 0 含 10 条 winner=1 没收进 JSON = winner→go 收割 gap（§2.17 坑1），W=0 暂非模型 verdict，挂 S-9 修管线（协作会话建）。**部署补丁回写**：`ep_runner_one.py` 的 2 处部署补丁（L3 `setdefault` STRATEGIC_STATE_LIB + 尾块 `traj["game_over"]` 注入）已固化成独立 patch 件 `py/ep_runner_deploy_fix.patch`（git `2d616c76`，基于 HEAD 生成，`git apply --check` 对含协作会话方案 B/P0 的脏工作树 rc=0 正交可叠加）；**本体暂不 apply**（`py/ep_runner_one.py` 是协作会话未提交脏文件，守 S-8 约定④只 add 自己文件）→ **本体闭环（09-25 10:1x，用户授权代办，S-8④ 例外，commit `1533a1da`）**：协作会话方案B（open 13/14/15 + `if True` 全图内政）/P0（obs 振荡修正）原本体未 commit，经用户拍板由本侧收进 git：`ep_runner_one.py` 单文件 commit（含 方案B/P0 + S-7 L3 setdefault + S-9 fresh-read 尾段，即 `py/ep_runner_s7_s9.patch` 两 hunk 全部落 HEAD），协作另一活文件 `train_wsl2_ppo_v2.py`（2 行日志文案）未带、保持 M 态。`git apply --check` 现 FAIL（hunk 已落地=patch 使命完成，旧 `ep_runner_deploy_fix.patch` 与 `ep_runner_s7_s9.patch` 均留档为历史件）。终验：HEAD 版 L3 setdefault=1 / S-9 `_s9_state`=4 / 方案B=1，py_compile OK |
| S-9 | 新 ABI 评测「无终局」治理（分布对齐 + winner→go 收割 gap） | 🔄 go 收割 gap 已修+确定性验证（09-25 09:0x）→ 复合项余分布对齐/即停类/换档 | S-7 全池矩阵 4 陷阱定谳（踩坑 §2.17 / 知识库 §5.15）：① **分布不匹配（用户 09-25 纠正, 权威）**：训练=VMAP 图, 评测 H3M 池岛屿/地下层未训过 → act10 空转 fuse 截断是预期行为, DRAW 不作能力信号 → 首修=图池对齐训练域（vmap 课程图评测）或 H3M 池只留训练覆盖过的图类（**未做, 挂起**）② winner→go 收割 gap：log 10 条 winner=1 痕迹（5×force_game_over+5×battleFinished）没进 go 字段（588 全 0）——**已修**：根因=`strategic_state_force_game_over` 只设 `s_force_game_over` atomic, 结构体 `g_strategic_state->game_over` 只在**下次 yourTurn** 的 `strategic_state_update` 才刷新, 而终局后 yourTurn 永不再来 → 结构体恒 0；修=force 时直接写结构体（`strategic_state.cpp` +3 行, 备份 `.bak_s9go`）+ `ep_runner_one.py` 尾段 fresh-read（`_s9_state=env._read_state()`, 不信 `env._game_over` 缓存）, `libmlclient.so` 重编 md5 `589cf0b3`。**确定性 oracle 验证（不靠随机）**：旧 .so force(1) 后结构体 go=0（复现 588 局全 0 bug）/ 新 .so force(1) 后 go=2（blue win）→ PASS；尾段链路 fresh-read=2 缓存=0 → 打通（踩坑 §2.19）③ 52 局 steps=0/1 即停（err 空, 独立样本类）（**未做**）④ 评测模型换 1003121 档（**未做**）。force10 自然重跑（10 受影响图×4 局=40）0 条 force 触发=随机事件这批没打到, 非反证。**归属引擎侧, 本侧 go 收割已修+oracle 验证, 余 ①③④ 待排期**。详见 知识库 §5.17 / 踩坑 §2.19 |
| S-10 | 训练迁移 WSL→服务器 172.16.2.40 | ✅ 全量已起（09-25 09:0x，N=16）· **09-26 三档治理后新分布=maps 6（T06 六张）+ 池 3（good_to_go/judgement_day/too_many），resume step=1600910, MainPID 2834236** | POC 双绿：串行 4 局有效（avg_r -0.2~+0.7 非极端）+ 并行 N=4 slot 池首跑 4 局全 rc=0 （吞吐 ~×2.2 vs 串行，对方 08:01 slot 池代码验证通过；该 slot 池代码已由对方 `bc451b68` 08:31 commit 入史，**S-8④ 混合文件顾虑解除，trainer 工作树现仅余 2 行日志文案**）。**全量**=`systemd homm3-train-server` (enabled, N_SUBPROC=16, HOMM3_DUEL_FOCUS=1, resume step=1016277, log `train_full.log`)；WSL `homm3-train-v5` stop+wants删+keepalive `sleep infinity`撤（腾 Track2）。**监控**：`tail -f train_full.log`（`[SLOT] stepN avg_r=`=有效局 / `Traceback`/`core dumped`=崩）+`pgrep -c ep_runner_one.py`(≈16)+`systemctl is-active`。checkpoint 每 50 step 落 `train_server/checkpoints/`。**遗留**：① N=16 长稳未验（评测池验过 32 worker×1.5h，训练 12h+ 长稳待观察，崩了 `Restart=on-failure` 自动重起）② 并行 `HERMES_N_EP` 语义=PPO cycle 数非局数（§2.18）③ 后续 ckpt 走 S-4 推 :8080 ONNX + WR 评测（评测模型同步换档）。详见 知识库 §5.16 / 踩坑 §2.18 | 评估结论（用户 09-25 拍板迁）：模型 2.9MB PPO，GPU 非瓶颈，**引擎局生成才是瓶颈** → 服务器 64 核 N env 并行吞吐 ×8-16；附带消除 WSL↔服务器 ABI 双产线漂移（09-24 毒化根因）+ 无 WSL idle-shutdown 坑 + 与 S-9 修复同机。代价：服务器无 GPU（模型 <50MB 前无忧）、并发长稳只验过 1.5h（先 4 env POC 再全量）。**Phase 1 已落**：① 资产同步 `/DATA/hero3/train_server/`（10 课程图 + 全量 state 1011214 + pool_index + bc + target_scorer）② trainer `py/train_wsl2_ppo_v2.py` 8 处路径 env 化（`HERMES_*` 默认=WSL 原值零行为变化；**08:31 对方 `bc451b68` 已把 slot 池+env 化一并 commit 入史**，POC 部署走 scp 单文件）③ `/mnt/d` 符号链接层解 runner 硬编码 5+2 处 ④ ARM 掩码分叉实锤（服务器 runner 11:16 全掩，**训练必用服务器 runner，不可复用 WSL 仓 runner**）。**Phase 2 全量已起**：串行 N=1 smoke 4 局全有效（`avg_r -0.2~+0.7`，0 崩；首跑因漏 `VCMI_NATIVE_DIR` + 课程图未进 `vcmi/data/Maps` 全 `Bad value`，修=3 env + 10 图 symlink，§2.18 坑5/坑6）→ 并行 N=4 slot 池首跑 4 局全 rc=0（吞吐 ×2.2）→ 全量 N=16 走 systemd `homm3-train-server`（enabled，resume step=1016277=WSL 08:50 最终档）+ 停 WSL unit + 撤 keepalive。N=16 活数据 09:2x 核验：active/NRestarts=0/崩率0/step→1017630/load 41.7→24.4。WSL 侧腾出给 Track2 真实游戏（1V7）；**WSL 侧 WIN-6 N=4 灰度（对方 `bc451b68` 08:31）随 WSL unit 停止事实终止，观察项全改挂服务器全量**。详见 知识库 §5.16 / 踩坑 §2.18 |

**后续可做（未排期）**：① 评测结果回流 Windows 分析管道；③ 服务器 CI workflow 编写（ARM64 Linux build + 交叉编译验证）。

| S-11 | 项目硬编码路径 env 化改造（三层） | ✅ L1/L2/L3 全层完成（09-25 09:1x） | 用户"对项目有硬编码的都进行改造"。改造模式统一=绝对路径→`os.environ.get("VAR", 原值)`（默认值不变=零回归，部署侧注入 env 覆盖）。**L1 服务器部署面 8 py 完成**：`eval_orch/eval_worker/strategic_single/train_v13_ppo`（`/home/administrator`→`VCMI_WORKSPACE_DIR`）+ `inference_service/results_hub/newabi_matrix_post/batch_eval_newabi_pool`（`/DATA/hero3`→`HERO3_ROOT`），全 `py_compile` OK，带 `.bak_hardcode0925` 备份（原地改，未回 git）。**L2 C++ terrain 完成**：`strategic_state.cpp` `fopen("/home/administrator/...terrain_grid.bin")`（21274 次触发、写全失败、Python 读侧 terrain 全空）→ `getenv("VCMI_WORKSPACE_DIR")` 兜底，与 Python 读侧 `strategic_env.py:25` 同变量对齐，**真正修活 terrain 功能**（全池时写 21274 次全失败→重编后 KTV 冒烟 `[TERRAIN] wrote + Python read OK: 1250 non-zero`）。**L3.5 shell 层批量改造完成（09-25，93 sh 文件 158 顶层赋值行，`bash -n` 93/93 全绿，commit `7b67ea83`）**：顶层赋值 `VAR=/abs` → `VAR="${VAR:-/abs}"`（无 env 时默认=原值零回归，部署侧注入同名 env 覆盖；含 `$( )` 命令替换嵌套 4 行人工核过）；158 命中文件里纯内联散点 75 文件不批量（逐文件低优先）。**L3 本地仓 py/ 批量改造完成（09-25 09:1x，100 文件 py_compile 全绿）**：实查 210 py/ 文件含项目硬编码路径（`/mnt/d/Bigdata/hero3*`/`/home/administrator/`/`D:/Bigdata/hero3`），其中**顶层常量可安全批量=100 文件**（纯顶层 65 + 顶层+内联 35），已 env 化（`VAR = os.environ.get("VAR", 原值)` + 补 `import os`），全 `py_compile` 0 失败；**实态更正（09-25 复核实）：100 文件中 93 tracked（本地分析/scratch 脚本已入仓）+ 7 untracked，93 全部纯 L3 diff 无协作会话混杂 → 已单独 commit 入仓（`l3_hardcode`）；`git ls-files py/` 输出已含 `py/` 前缀，勿再拼一次**；剩 104 文件仅内联散点（不批量，需逐文件，低优先）。**L3.6 C++ 本地树对齐（09-25 末）**：本地 `vcmi/ML/strategic_state.cpp`（09-10 未跟踪旧快照，git 无提交史）与服务器权威树 diff 66 行全为服务器单向演进（L2 getenv + S-9 force 刷新 + 09-17 passable 闸门）→ 本地对齐服务器版（备份 `.bak_pre_l2align_0910`），本地 C++ 活树硬编码归零（只剩 getenv 兜底）。**活文件 ep_runner_one.py / train_wsl2_ppo_v2.py（协作会话 M 态）严格排除未碰（S-8④）**。详见 踩坑 §2.20 / 知识库 §5.18 |
**说明**：② 已落地——详见知识库「十五、④ WIN RATE 结果中枢 + checkpoint 自动推送链路」+ `docs/checkpoint推送链路与WINRATE自动评测.md`；闭环归档见 `已完成任务.md`（09-24）；架构总览见 `总任务.md` 两段式设计。

## 四、可立即执行（09-24 收敛，按优先级）

> 影响判定红线：WSL rel/ 运行时 .so、训练进程/日志写、GPU、vcmi-native 工作源码、eval 抢 CPU。
> **训练当前在跑**（09-24 验收实读：`systemctl is-active` = **active**，PID **43557**；unit 实读 env = `XDG_DATA_HOME=/home/administrator/.local/share` + `HOMM3_POOL_INTERVAL=5`（每 5 局插 1 局池图）+ `HOMM3_H3M_BATCH=1` + `LimitCORE=infinity`；keepalive 已拉起）。前一段停机存档 **step=962235**，重启后 resume 累加。P0-1 Mode B 已修（trade cap 16）；**P1-4 池图 SIGABRT 已定谳并修复（踩坑 #308，XDG 目录缺失）**。**09-26 引擎窗口收口：P0-2 三套 298 A/B 对照已定谳（三套全留，压制非根治，证据 `/home/administrator/_298_p1_20260926/`）+ vcmi-native #215 headless 守卫重写 6 处 commit（`11be8ba34d`，连带 vcmiclient 链接修复）+ 开源周 C++ 路径环境化重编进 connector .so（vcmi_gym `87df8cc`）**。遗留 = 冻结 Mode B 架构级根修另排。

**P0 — 引擎窗口必做（需停训窗）**

| # | 任务 | 说明 |
|---|------|------|
| P0-1 ✅ | 冻结 Mode B 修复 | **已修（trade cap 16）**：`py/patch_mlfix_tradecap.py`（`8679dd35a1`），elbow_room ×12 全绿。全量原文（根因/验证/官方归属澄清）见 `已完成任务.md`「2026-09-25 收敛归档 P0-1」 |
| P0-2 ✅ | 三套补丁对照实验（09-26 引擎窗口定谳） | **A/B 对照完成（N≥4 + 基线，#298 纪律）**：同 4 图 × 4 轮 30 步。A 臂 with-patches（09-24 .so 原态）16/16 全 rc=0 零冻结 vs B 臂 baseline（`py/revert_298.py` 手术式逆向三套 298 → 重编）4/16 rc=0 + **12×rc=124 冻结**（judgement_day 4/4、a_viking 4/4、elbow 3/4、g2g 2/4）。**定谳 = 三套全留**（栈打点哨兵 / 方案1 / upgrade 熔断 cap 8）；边界 = **压制非根治**（Mode B 根修另排，方向三档见知识库 09-23 章）。基线工具 `py/revert_298.py`（旧 .so.bak_*298 是 09-23 快照会误伤 09-24 提交，不可 rollback，踩坑 #321）。证据 `/home/administrator/_298_p1_20260926/arm_a.txt+arm_b.txt+32 逐局 log`（rootfs）。全量见 已完成任务「09-26 P1 引擎窗口三项收口」条 + 踩坑 #322 |

**P1 — 训练层 / 旁路（可热部署或下次启动）**

| # | 任务 | 说明 |
|---|------|------|
| P1-1 ✅ | 采样机制迁移 + 打点实证（09-24，`[MIX_TRACE]`→`[POOL_SCHED]`） | **已迁 `HOMM3_POOL_INTERVAL=5` 确定性节拍**（`[POOL_SCHED]` 打点，池图命中正常）。全量原文 + 实证见 `已完成任务.md`「2026-09-25 收敛归档 P1-1」 |
| P1-2 ✅ | 哨兵计数（09-24 13:0x） | 11 局哨兵全 0、零 Traceback（样本小按 N≥4 不下根除结论）。全量原文见 `已完成任务.md`「2026-09-25 收敛归档 P1-2」 |
| P1-3 | 三处 08-17 修复恢复核验 | `CGameHandler` levelUpHero / `BattleResultProcessor` removeQuery / `QueriesProcessor` 去守卫。脚本 `py/patch_298_restore_0817.py` 幂等+校验+rollback。A/B 对照已定谳：崩溃/超时是既有竞态，非本次引入 |
| P1-4 ✅ | **池图开局故障 → 踩坑 #308 已修（非 Mode B、非并发竞态）** | **已修（踩坑 #308）**：XDG 用户数据目录缺失 → SIGABRT（A/B 铁证）；修复 `py/setup_vcmi_runtime.sh` + unit `XDG_DATA_HOME`，池图首次跑通。全量原文（根因/修复/验证/证伪历程）见 `已完成任务.md`「2026-09-25 收敛归档 P1-4」 |

**P2 — 代码 / 文档沉淀（零干扰）**

| # | 任务 | 说明 |
|---|------|------|
| P2-1 ✅ | C++ 改动重编实查（09-24 13:0x） | **结论：无需重编**——路径环境化改动已编入 connector .so；M6 docstring 已 push `4c733e6`。全量原文见 `已完成任务.md`「2026-09-25 收敛归档 P2-1」 |
| P2-2 ✅ | 开源周实查（09-24 13:0x） | **第二批已 push**（`66d6d04`，CI 真测试 11 单测）。**剩余**：① #205 PR（需 PAT）② 模型 release。全量原文见 `已完成任务.md`「2026-09-25 收敛归档 P2-2」 |
| P2-3 | 文档收敛检查 | 本清单已收敛；知识库 09-23/09-24 章 + 踩坑 #298/#299 为权威细节；当前清单保持短小 |
| P2-4 ✅ | **仓库拓扑坐实 + 镜像仓收敛（09-24，零干扰，已完成）** | **已坐实 + 收敛**：仓#2（WSL 工作克隆）为训练真源；10 提交推入仓#3；中转镜像已删；灾备源已修；⭐ 分叉面 mmai-ml-wsl 落后 1923/冲突 786 → 窗口 B 全量对齐成本按此重估。全量原文见 `已完成任务.md`「2026-09-25 收敛归档 P2-4」 |
| P2-5 ✅ | **仓清理 + 仓清单坐实（09-24，零干扰，已完成）** | **已清理 583 MB**（git 可恢复为准；分支全保留）。坐实「平时说的 git」= `D:\Bigdata\hero3_fresh`。全量原文见 `已完成任务.md`「2026-09-25 收敛归档 P2-5」 |

**历史 A 表（A1-A14 全 ✅，详见 git 版本库）**

A1 批次A 聚合 / A2 贴近未接战归因 / A3 own_town 衰减方案 / A4 act_loop 错配备料 / A5 批次B checklist / A6 文档沉淀 / A7 上游侦察+capture-all-mines 备料 / A8 BHERO_KILL 局尾补账 / A9 BUILD_2 定谳+白名单 / A10 观测基建三件套 / A11 BUILD P1 备料 / A12 S2 自动收口监控 / A13 JEV 决策模型运维工具链 / A14 独立代码测试套件+8 条问题全量修复收口。

**B. 等条件触发**：WIN-2 回池 / WIN-3 难度轴（错窗三选一）/ `py/eval_promo.py` 全 profile（窗内不跑）/ C2 L1 gdb 取证（崩溃复发触发）。

**C. 需停训/自然重启窗**：P0-1/P0-2（引擎窗口）/ 撤梯子 move_to_force 覆盖块改动 / C3 CGameHandler garrison 自动合并（触碰铁律，断言不复发不做）。

**D-0 ✅ 选图机制改动已入库（09-25，`fe141127`）**：WIN-3 难度轴 `HOMM3_DUEL_FOCUS=1` 双文件 commit（`py/train_wsl2_ppo_v2.py` + `py/homm3-train-v5.service`），#306 双副本一致性核验通过（/etc vs py/ 逐字一致）。**仍挂账**：`py/ep_runner_one.py` 横跳 P0 修正（prev2→obs[-3] + 8步窗平级重构）属奖励面改动且 09-24 后无验证记录，训练在跑**不混入 commit**（引擎窗口验证后再提）；`.gitignore` 纯 CRLF 行尾噪音不入库；`py/reorder_wslpit_engine.py` 未跟踪（登记待定性）。**ARM64 CI 备料已落（09-25，vcmi_gym `10c27e8`，本地未推）**：`.github/workflows/arm64-ci.yml`（kunpeng runner, 同 ci.yml lint+unittest，守 aarch64 面）；推 GitHub + 首跑需用户拍板（"git github"）。（原此段挂账的 vcmi-native #215 headless `if(ENGINE)` 守卫待 commit → **已销（09-26）**：原 65515ef24 随 09-19 rootfs 事故丢失，重做最小 6 处 commit `11be8ba34d`，含 vcmiclient 链接修复；踩坑 #320。）：D4 2v2/多人 GUI 验证 / 8B optional 序列化 1B vs 4B 字节不匹配疑点保留 / ~~MIX 三方漂移待拍板~~ → ✅ **已定谳并处置**（根因＝改 `/etc` 漏改 `py/` 仓内副本，**踩坑 #306**；且机制已由 `HOMM3_H3M_MIX` 迁移为 `HOMM3_POOL_INTERVAL=5`，见 WIN-5） / ~~PAT 泄漏~~ → ✅ **已闭环**（本地：摘除 `url.insteadOf` 改写 + remote 全改 ssh + 读写均验证；**GitHub 侧：代码/提交/issue/gist 四路搜索全部 0 命中，确认无暴露**，token 仍有效亦为旁证 → **用户拍板不吊销**，`GH_TOKEN` 保留） / **异地备份缺口**：仓#2 那 10 个提交现仅存于仓#3 与仓#2（**都在本机**），推自己的 fork 才能真正异地 / **选图机制改动待入库**：`py/train_wsl2_ppo_v2.py` 与 `py/homm3-train-v5.service` 的 POOL_INTERVAL 改动**尚未 commit**（入库前注意 #306 的双副本一致性）。

---

## 五、风险登记 R（引擎侧）

| 编号 | 风险 | 状态 / 对策 |
|------|------|------------|
| R1 | 动作拒绝不可感知 | 🟡 Python action mask 在用，未审计 |
| R3 | 异步时序竞态 | 🟢 约束型（新动作走 waitTillRealize） |
| R4 | queryID=-1 断言雷 | ✅ 已修（libMMAI.so 13c0f041） |
| R5 | 双源码树不同步 | 🟡 `vcmi-native-build` 是 08-02 陈旧树（#228 实锤）；源码改动先在 vcmi-native grep；Windows 树只摘 fix。**09-24 拓扑坐实（更新）**：实体实为「工作区仓 `hero3_fresh` + 其 vcmi **子模块**（仓#3）+ WSL 工作克隆（仓#2，**训练真正在用 `mmai-ml-wsl`**）」；仓#2↔仓#3 已直连（中转镜像已删）。**注意仓#3 与工作区仓分支同名 `fix_action_mapping`，极易混**。详见图 + 分叉面数字见知识库 09-24 节 / 台账 §1.1 / 踩坑 #303 |
| R6 | 战斗 AI fallback StupidAI | ✅ onnxruntime 1.19.2 + V13::BAI（09-03），长期观察 |
| R7 | battle query 卡顶 | 🟡 见 OBS-1 |
| R8 | 训练进程存活 | 🟢 system 级 enabled unit + Windows keepalive（#114/#201）；判活看 PID etime+日志 mtime；改码前 `wsl -u root systemctl stop homm3-train-v5`（禁 --user） |
| R9 | KL 失控 | 🟢 生效窗（L102375 起）2/2 更新点 <1.0，无复发；1.096 尖峰属旧代码污染段（09-17 收口定性）。再现/持续 >1.0 → 评估回退梯度注入 |

---

## 六、T13 外挂 AI — T13.10 多人对战验证 ✅（全闭环）

- T13.1-T13.9 协议栈主体完成（离线 167/167 PASS）；T13.10：P8-B 握手轮转 / P8-C 决策接入+真实 qid=2 样本 / P8-D 单机双实例 13/13 / P8-E 人机混局 / 30min 浸泡，全部 PASS。代码 `py/vcmi_pro/`、`py/p8/`，明细见已完成任务 09-11~09-16 条。
- **遗留两点**：① ~~vcmi-native 仓 #215 headless 守卫修复待 commit（见四-D）~~ → **✅ 已销（09-26）**：重写最小 6 处守卫 + vcmiclient 链接修复，vcmi-native `11be8ba34d`（本地仓不推，踩坑 #320）；② 8B absent optional 字节级不匹配疑点（C++ 写 4B/Python 写 1B，run10 实证 server 受理），待 197 fishy 实锤再验 9B present。
- 真机 `--remote` 与长对局浸泡均已闭环/废弃，无剩余待办。目标场景（2 人类+1 外挂 AI，1v7 大厅）底座就绪。
- **参考源（09-24 核实）**：架构**非第三方项目**，参考 VCMI 官方网络层（`lib/network` + `lib/networkPacks` + `CVCMIServer`）；VCMI 内置 AI 是进程内 `cb` 接口、非网络 client，T13 需自研网络 client 参照 `client/`。详知识库 §7.1 / 踩坑点 3.5。

---

## 六-B、T14 蓝方对手 AI MOD（09-25 登记，执行拆解）

> 定位与强度排序见 `总任务.md` T14（gap② PPO > T14 > 丢弃 Lua+Jev；参考源 AMER_HumanAI 两类设计输入）。本节只写执行面。
> **⚠️ T14.1 勘误（09-25 摸底定谳）**：原「纯 Lua MOD」前提**不成立**——VCMI Lua 运行时（`vcmi/luascript/`）只暴露「冒险层只读 + 战斗层读写」API，**战略动作（移动/招募/城建/施法/结束回合）全不暴露**，且 NK2 本身就是 C++（`ai/Nullkiller2/`，非 Lua）。**路线改为 C++ 新 AI 模块（NK2 同构 `CAdventureAI` 子类）**，见 `docs/T14_API面清单.md`。
> 总原则：新 AI 模块只编独立 .so（**不重编 libvcmi.so 引擎本体，铁律 3 边界内**）；蓝方选择参数化、NK2 一键回退；验收按 #298 纪律 N≥4 + 基线对照。

### T14.1 NK2 源码 + Lua API 面摸底（API 面定谳）✅【完成 09-25】
1. 定位源勘误：NK2 = `vcmi/ai/Nullkiller2/`（纯 C++，`AIGateway : CAdventureAI` 为回合入口 `yourTurn(QueryID)`）；Lua 层 = `vcmi/luascript/api/Registry.cpp` 全量 API 登记（只读 library/adventure + 战斗 ServerCallback 15 个 mutation）。
2. 缺口定谳：移动/寻路/endTurn/招募/建筑/购宝物/战略施法 **Lua 全 ❌**（「缺 1 项」的决策门不成立，是整个冒险动作面缺失）。
3. 产出 `docs/T14_API面清单.md`：两大前提勘误（NK2 非 Lua / luascript 非 AI 运行时）+ API 全表（带源文件锚）+ 可执行路线（纯 Lua ❌ / **C++ 新 AI 模块 ✅ 推荐** / Lua 战斗辅助 MOD △降级）+ 给 T14.2-4 的输入。
- **判定**：✅ 完成。核心结论 = 纯 Lua 否决，路线改 C++；handicap 参数（日金/回城术 L20/飞行 L28）改走 C++ 侧 Settings（`Nullkiller2/Engine/Settings.cpp` 模式）。

### T14.2 规则设计（C++ 骨架 + 状态机 + 5 原型 seed 参数化）✅【完成 09-25】
产出 `docs/方案_T14规则设计_20260925.md`（判据全过：规则 18 条≥15 ✓ / 每条 触发+动作+优先级+落点类 ✓ / 5 原型 seed 映射公式 ✓ / NK2 骨架类标注 ✓）。
关键定谳：① **铁律 3 合规实现路径坐实**——NK2 是 OBJECT 库进 libvcmiMain（CMake L155/159），T14BlueMod 走 **MMAI plugin 范式**（`main.cpp` 4 函数入口抄自 `ai/MMAI/main.cpp`，独立 .so，零引擎文件改动）+ `NK2AI::AIGateway` 继承复用骨架 ② 架构三段 = 薄派生层 T14BlueGateway（半径 32000 / handicap 首回合段 / 魔力回满 / heroGotLevel 技能择优）+ `config/ai/t14blue/*.json`（5 原型 + handicap 数值 JSON 化，调参零重编）+ **VictoryPursuer（R13，~300 行，最高优先）**= 解析胜利条件生成总攻任务链，治 DRAW 根因 ③ 确定性 = 全 RNG 走引擎通道（`cb->getRNG()` 族，NK2 先例），hash(gameSeed^mapHash) → P0-P4 + 激进度 + handicap 开关。
- **遗留待 T14.3 首验（不阻塞）**：① 引擎对 lobby AI 名 `T14Blue` 的 dlopen 放行（AIFactory `isAvailableAdventureAI` 写死 false，引擎本体不改）→ 不通则备选方案在方案 §1.2 ② `cc->gainResources` 对 AI 方合法性（层 1 handicap mod 生效后此项仅 R7 魔力回满仍需验证）③ NK2 继承 visibility 异常退路（composition 替代）。

### T14.2b handicap 纯 JSON mod ✅（09-25 完成实现 + KTV N=4 冒烟闭环）【已落仓 `vcmi/Mods/t14-handicap/`，零 C++】
- **冒烟闭环（09-25）**：KTV N=4 双组对照（A=无 mod / B=启用 mod，同 ckpt 1014734 / 同参 / seed 对齐）+ ridiculous 值铁证：① `difficulty.json` 通道 A **确认流入进程内引擎**（obs gold log1p 反算：A 组开局双方=10000=core king 档；B 组=15000=mod king 档 ×1.5；ridiculous 500000 原样进 obs）② 两文件引擎 `Loading mod: OK (t14-handicap)` 无 schema 报错 ③ **边界结论**：B 组开局金 15000>10000、末帧金 B>A（周收益通道 B 在工作），但 250 步 KTV 两组 go 仍全 0（全截断）——**对称经济加速未单独治好 KTV 单图 DRAW**，需全池铺开或 T14.3 VictoryPursuer 收割层（治 DRAW 根因=无胜利条件解析，对称加金只是加速双方攒钱）。部署态：WSL 双树 `vcmi-native/Mods/` + `rel/bin/Mods/` 同步仓内修正版（settings key=`weeklyBonusesAI`，description typo 已清），preset 默认关（零干扰），启用=modSettings.json default.mods 加 "t14-handicap" 一行。
- **踩坑（写进记忆）**：① obs 资源槽是 `np.log1p` 压缩值（strategic_env L454-462），读 raw 需 `exp(obs)-1`，直接 int() 取整=造出"曲线重合"假象 ② schema 合法 JSON key=`weeklyBonusesAI`（带 es；C++ 枚举名 `RESOURCES_WEEKLY_BONUPS_AI` 是无 es 的官方 typo，两侧不一致，错一个字母=`additionalProperties:false` 下通道 B 静默 0 效果）③ 部署双树：引擎 chdir `rel/bin`（`MLClient.cpp` `VCMI_BIN_DIR`），但 `rel/bin/data` 软链到 `vcmi-native/data`（modSettings 一份），`Mods/` 双份独立需双写。
- **实现（09-25）**：自写 `t14-handicap` mod（不直接用 vcmi-mods 三个现成 mod——均未在 1.8 fork 验证过，走"自写同构"分支）：① `Content/config/difficulty.json` `ai` 段 = 官方 core ×1.5（king 开局 gold 10000→15000，`assembleFromFiles` 自动并入引擎 config，`initDifficulty` 按 `p.human?human:ai` 分发）② `mod.json` `settings.resources.weeklyBonusesAI` = 官方默认 ×1.5（`CModHandler::initializeConfig`→`GameSettings::loadBase`→`NewTurnProcessor:267-288` 消费，`!isHuman` 才加，周分布日收入百分比）③ `keepDisabled=true` 默认不启用=零干扰，启用开关 = preset 加 `"t14-handicap"` 一行。
- **根因修正（09-25 挖到底，替代旧"只强化蓝 NK2"表述）**：headless 评测池 `onlyai=true` 时红（模型）蓝（NK2）两侧引擎眼里**都是 AI**（`human=connectedPlayerIDs 非空`，无 lobby 连接）→ handicap **对称**加给双方。但对称经济加速恰对症 **S-7 全池 DRAW 根因**（双方攒不够经济→5000 天截断）：双方都更快推进=更早自然终局，且 baseline 同享→**WR 公平性不变**。Track2 真实游戏用户=human/模型=AI，同一 mod 自动变不对称蓝方强化。一个 mod 两用。
- 动作（余）：③ T14.3 收窄为层 2 三件不变（VictoryPursuer 治根因 + 原型化权重 + 技能择优；不对称 per-player handicap 走 lobby `LobbySetPlayerSettings.handicap` 通道 = C++ 侧，归 T14.3）。①WSL 部署 ②冒烟 已于 09-25 闭环（见上"冒烟闭环"段）
- **收档引用（09-26）**：fact_store #61（log1p 坑 + key 拼写 + 边界结论）；commit 主仓 f21c192c + vcmi 仓 27f2c9714；知识库「待整理」区 T14.2b 段 + 踩坑主索引「待归档新增」区 #316-#319（log1p 假象 / WSL 嵌套 shell 变量 / CRLF 行尾 / key 拼写）已落档，待用户归档。
- 方案 §十 全量（mod 对比表/两通道消费代码坐实/根因修正/数值对照表）见 `docs/方案_T14规则设计_20260925.md`。
- **判据（原）已全过**：规则 18 条含 R1-R18（11 条★治 DRAW）；AMER_HumanAI 三条战斗启发（招魂/恶魔养猪/减伤）定位 = 可选 T14.5 Lua 战斗层辅助 MOD（未排期）。

### T14.3 实现 + KTV 冒烟 ✅ 工程验收通过（09-26 闭环）/ 🟡 能力面未达标挂 T14.3b
> **方向锁定（09-26 用户拍板）**：T14.2b 冒烟已证对称加金不治单图 DRAW（末帧战力 B>A 却 go=0，NK2 攒金不收割）→ **直接进 T14.3 VictoryPursuer C++ 收割层，治 DRAW 根因（无胜利条件解析）**；全池对称加速验证不再单排，T14.4 评测池接入时顺带看。
> **09-26 真相源勘误**：实现以 **WSL 仓 `4a1675c6a2`（13 文件）为唯一真相源**——reasonix 派单产出的 D: 树 `AI/T14BlueMod/`（SHARED .so + dlopen 假设）**已作废清除**（架构定谳 fact #64：本 fork 是 STATIC_AI 构建无 dlopen 通路，NK2 同款 OBJECT 库聚进 libvcmi.so + 引擎 7 处门控，见 checkpoint `docs/T14.3_T14Blue_20260926_checkpoint.md`）。
- **部署（服务器 09-26）**：patch `/DATA/hero3/t14blue_4a1675.patch`（WSL 4a1675c6a2 整包）`git apply` 上服务器 `/DATA/hero3/vcmi` → `cmake -DENABLE_T14_BLUE=ON .` + `make vcmi mlclient vcmiserver vcmiclient` 全绿（T14BlueMod 6 cpp + Nullkiller2 + vcmi facade 全 Built）。符号落位实锤：`nm libvcmi.so` 见 `T14Blue::T14Nullkiller::onPursueTurn` + NK2 `W onPursueTurn()` 空钩子。回退垫：`bin/*.bak_t14blue`（libvcmi 538MB/libmlclient 312MB/vcmiserver）+ 源树 `.bak_t14blue` 7 份 + `py/patch_t14blue_engine.py --rollback`。
- **冒烟（T14.4 KTV N=4 双臂，09-26，全 rc=0 0 崩，result JSON `/DATA/hero3/output/t14smoke_ktv_0926.json`）**：
  | 臂 | R 四局 | 均值 | go |
  |----|--------|------|----|
  | T14Blue | -414/-104/-470/-441 | **-357** | 全0(DRAW) |
  | Nullkiller2 | -504/-324/-451/-438 | **-429** | 全0(DRAW) |
  - **加载实锤**：`loglevelAI=info` 探针 grep 到 `[T14-ACT] T14Blue resolved archetype=P0 aggr=1 seed=4242050860 map=Maps/Key to Victory.h3m`（CDynLibHandler stem=libT14Blue 分支命中）；常规冒烟 `T14_ACT=0` 是判据失效（runner 硬编码 `vcmi_loglevel_ai=error` 抑制 info 级打点），**非没加载**。
  - **能力面判定**：T14 臂均值比 NK2 好 ~72（方向对）但 **KTV 单图仍全 DRAW**——nudge 是软推进（targetObject 每 pass 被 updateState 重置）+ 红方 StupidAI 不接战，单图破不了 DRAW 根，与 T14.2b 边界结论一致。**真区分度要看 T14.4 全池矩阵（红方换 PPO 模型 1003121 有力对抗 + NK2/T14 双基线，#298 纪律 N≥4）= 与 S-9④ 合批三档矩阵，下一窗口**。
- **构建踩坑（已登记踩坑 §2.21/知识库 §5.19）**：`AI/MMAI/CMakeLists.txt:204` 硬编码 WSL 路径 `/usr/lib/x86_64-linux-gnu/libpython3.12.so`（c80796a17 引入）→ ARM 服务器 link 炸；临时桥接=符号链接 `libpython3.12.so→libpython3.11.so` + `python3.12→python3.11` include 目录（hack，正式修=find_package(Python)，源树改动挂 T14.3b）。vcmiclient 可执行体 link 仍失败=既有问题（fact #37，管线走 threadconnector 不用它）。
- **训练无损核验**：重编期间 NRestarts=0 全程健康；20:47 一次手工优雅 restart（存 step=1565581 → resume，非崩溃）。
- **边界（T14.3b 另排）**：① 硬目标注入（decompose 私有面）② Settings 权重轴（字段 private 无 setter）③ MMAI CMake python 路径 find_package 化。

### T14.4 评测池接入验收（S-7 新 ABI 链）🟡【KTV N=4 冒烟✅已随 T14.3 闭环（09-26）；余=全池矩阵，依赖 T14.3b 能力面】
- **已做（09-26 随 T14.3 冒烟）**：KTV N=4 双臂（T14 vs NK2，`t14smoke.py`，0 崩）——**判读=单图 DRAW 未破，区分度需全池+红方换 PPO 模型**（详见 T14.3 段）。
- **余量（下一窗口，与 S-9④ 合批）**：1. `batch_eval_newabi_pool.py` 加蓝方可插拔参数（NK2 / T14BLUE），与训练 N=16 **错 CPU 窗**；2. 矩阵：KTV + 3 图 × 4 局 ×（T14 vs NK2 双基线），红方换 PPO 模型 1003121，N≥4 + 基线对照（#298 纪律）；3. 指标：DRAW 率 / go 非零占比 / 步数分布 / 0 崩。
- **判据**：DRAW 率显著 < NK2 基线（S-7 参考 588/588=100% DRAW）；go 字段出 winner 信号则顺带交叉验证 S-9 收割路径。
- **与 S-9④ 合批**：T14 完成时正好接「三档对手矩阵」（NK2 / T14BLUE / PPO 模型 1003121 互评），一次出区分度全谱。

### T14 排期与成本（决策参考，T14.1 勘误后重估）
| 项 | 成本 | 干扰 | 前置 |
|----|------|------|------|
| T14.1 | ✅ 完成（09-25） | 零（只读） | 无 |
| T14.2 | ✅ 完成（09-25） | 零 | 方案_T14规则设计_20260925.md |
| T14.3 | ✅ 工程验收（09-26：服务器编+KTV N=4 冒烟 0 崩+加载实锤；能力面单图 DRAW 未破） | 引擎窗口（编新 .so，服务器 ARM 已编完） | T14.2 + 真相源=WSL `4a1675c6a2` |
| T14.3b | 另排（硬目标注入 + Settings 权重轴 + MMAI CMake python 路径 find_package 化） | 引擎窗口 | T14.3 |
| T14.4 | 🟡 KTV N=4 冒烟已随 T14.3 闭环（09-26）；余=全池矩阵（红方 PPO 1003121 对抗） | 服务器 CPU 错窗 | T14.3b 能力面 + S-9④ 合批 |
- **总 ~2.5-3.5d 剩余（T14.3 2-3d 编译实现+冒烟 / T14.4 0.5d 接入）**。启动拍板建议：T14.3 择自然停训窗（plugin .so 不碰引擎本体，可错开训练窗）；T14.4 与 S-9④ 模型换档同窗合批。

---

## 七、远期 / 阻塞集

- ⬜ T5.9 v2 MultiDiscrete 三维参数化（与 T6.3 合并）。
- ⬜ MMAI v15（PR #7654）未合并官方、2-4x 慢，合并后需重采集（绑 T8）。
- ⬜ **A2 capture-all-mines 胜利条件（PR #7810 merged 09-12）**：`.vmap` 文本可直接写 `["controlCurrent", {"objectType": "mine"}]` 替换当前 `specialVictory.condition`（攒 100 金），`victoryIconIndex` 2→9，`victoryMessage` 改 `core.vcdesc.10`；属**地图轴**，与 WIN-1 激励轴错窗，WIN-1 达标后择窗实施；Phase II 经济课程新地图模板候选。备料 `py/probe_victory.py`（探查 T05_36X36_01 / T06_72X72_02 / T05_52X52_01_mir 三图 `victoryConditions` 字段结构）+ 知识库 09-17 章。
- ⬜ 战斗模型训练：当前不需要（R6 现成 onnx）；触发 = T05 对抗接战胜率成战略瓶颈；勿在课程中途升级。
- ⬜ **D 方案：引擎层 startBattle 直调（技术储备，09-19 用户拍板登记"以后要用"）**：Python 不走 moveHero 直接发起英雄战，根治 d=0 同格/敌格 moveHero 结构性拒绝（areNeighbours）。触发条件 = B 方案（BFS 绕行）实测失效或 T05 对抗接战成瓶颈。调研结论（onHeroVisit→startBattle 全链符号+行号 / connector .so 重编非 libvcmi.so / OBS 冻结约束下走旁路通道）见知识库 09-19「startBattle 引擎层直调」章。
- ⬜ 动作码 16-23 内政全面启用：Level 3 切入时先解决 noTarget 惩罚。
- ⬜ P6 对手池 self-play/NK2 混合：NK2 三树源码就绪，回用时直接重编即生效。
- ⬜ P10-B h3m2vmap C/D/E：转换器 B1-B4 已收官，产出图入训练池的课程设计（WIN 窗外）。
- ✅ 引擎 standardDefeat 判负/败北信号（#228）；fogOfWarMap = explored 累积语义（09-15 源码核查，见 `docs/源码分析地图.md`）。

---

## 八、关键拍板记录（影响后续节奏）

- **错窗纪律**：激励轴（capture/T7.4/击杀梯度）/ 地图轴（36X36_02 回池/新图）/ 难度轴（大图 duel）不同窗，一次一轴。
- **关机标准流程**：后台 unit；优雅停零损失；改码前 `wsl -u root systemctl stop homm3-train-v5`（system 级，无 --user）。
- **(09-15)** WIN-1 ⑤：撤销改图（72_02 全草地无墙）；⑤口径只数 C 类退化，B 类截断不阻塞聚合；策略空转方向转 P10 target 加权（已上线）。
- **(09-16 晚)** 击杀链归因定谳（真实击杀=0，proxy 无 bug）→ 击杀激励重设计批次A 开窗；与 S2/WIN-3 互斥。
- **(09-16)** C1 B4 晋级判据人工拍板实质达成（3 次 eval avg 133-153 + 四指标全过）；属 T7.1 历史判据复核，**不触发** S2/WIN-2/WIN-3。
- **(09-18)** 开源立项拍板（用户发起）：战略层 RL 官方/fork 全空白（MMAI PR #4788 原话 "adventure AI... nothing particular is planned"）→ **独立项目为主，官方仓只提干净小 PR**。3 仓结构（strategic-gym MIT / engine-plugin GPL-2.0+ / models-release+mod）。**官方小 PR 清单 09-18 复核后收窄 3→1**：~~NKAI race `a1ea3f4d2d`~~ 官方已含（Ivan Savenko）/ ~~JSON seek `01f741713c`~~ 官方已含（kdmcser）/ **#205 服务端 pack try/catch（唯一可提，我方原创，upstream `CVCMIServer::onPacketReceived` 仍裸调 `retrievePack`）**。触发条件（任一→开"开源周"）：① passable 闸门 4 判据达标 ② 首个可展示 milestone（真实击杀实锤/1v7 录屏）③ 下次大改图自然停训窗；当前只登记不执行（WIN-1 错窗纪律）。备料已动：`vcmi_gym` 仓构建产物清出（.gitignore/README/LICENSE 已立；认证踩坑 #258，账号 c18593395）。**✅ 09-18 已首推 + CI 绿 + 已改名**：GitHub 仓 `c18593395/vcmi-strategic-gym`（建时名 `cmi-strategic-gym` 少个 v，用户 Settings 已改名，本地 `git remote set-url origin` 已同步），SSH push 完成（74 文件含 LICENSE/CI/CONTRIBUTING，HEAD `c0cb8ed`）。CI 首跑（run #35362844432）**Success**：compileall 语法闸 + 英文 CONTRIBUTING（构建命令已核对实际 preset `vcmigym-rel`）。**✅ 敏感内容扫描全绿**（4 commit 全历史 + 当前树，API key/权重/map 零命中；唯二打磨项=5 文件硬编码 `/home/administrator` WSL 路径 + 历史混入 3 个 `.bak/.so` 二进制，清洗方案已定留开源周）。**#205 PR 已提取未推**：基于 `upstream/develop` 在 worktree `vcmi_pr205` 建分支 `pr205-server-pack-guard`（21 行 diff，剔本地 commit 里的诊断脚手架，符号实锤完成），推 PR 需浏览器手建 fork 或用户出 PAT（gh API 认证断 #258）。开源周触发时在此基础上迭代（路径清洗/英文 quickstart/模型 release/CI 加真测试）。详见知识库 09-18「上游侦察+开源立项」章 + 踩坑 #258/#259。
  - **09-23 开源周进展（用户拍板 "push + 英文 quickstart"）**：① `ecaa641`（nk2 shaping 两处）已 push（`c0cb8ed..ecaa641`）；② **README 英文 quickstart 完成并 push**（`4e6dfdc`）——补前置依赖表（CMake≥3.16/Conan≥2.13/Python≥3.11/**libmlclient 非上游 VCMI**/pybind11 需 clone 到 `connectors/pybind11`）+ 可跑构建路径（`conan install . -of conan-generated` → `cmake --preset vcmigym-rel` → `cmake --build rel`）+ 运行环境变量表（`STRATEGIC_STATE_LIB`/`LD_LIBRARY_PATH`/`XDG_DATA_HOME`）+ 两条 caveat（`envs/v13/` 测试脚本硬编码 WSL2 路径需改；map 不入库需自备）；③ CONTRIBUTING conan 输出目录 `build`→`conan-generated` 修正（与 `CMakeUserPresets-vcmigym.json` 里 `conan-generated/conan_toolchain.cmake` 对齐）；④ **复扫敏感内容**：API key/权重零命中，硬编码 `/home/administrator` 实测 **5 文件 16 处**（`connectors/CMakeLists.txt:11`、`connectors/v13/threadconnector.cpp:522`、`envs/v13/strategic_env.py`×7、`test_adventure.py`、`test_strategic_env.py`）——**未修**（涉及 C++ 重编 + 脚本路径语义，待拍板是否 env 化）。**剩余**：#205 PR（需 PAT/浏览器）、CI 加真测试、模型 release、路径清洗。
  - **09-23 第二批（用户拍板"三项全做"，提交 `66d6d04`，CI #4 Success 24s）**：① **路径环境化完成（硬编码 16 处 → 0）**——`strategic_env.py` 新增 `VCMI_NATIVE_DIR`/`VCMI_WORKSPACE_DIR` 派生常量（`DEFAULT_MLCLIENT`/`DEFAULT_TERRAIN_GRID`）替换 6 处字面量；两个 test 脚本路径同源派生；`CMakeLists.txt` VCMI_DIR 改 **CACHE PATH**（`-DVCMI_DIR` 可覆盖，默认 `$ENV{HOME}/vcmi-native`）；`threadconnector.cpp:522` XDG_DATA_HOME 改取 `$HOME/.local/share`（未设置时才写）。**行为等价已断言**：administrator 下 expanduser 解析结果与原字面量逐字一致（两路径均 OK 且文件存在）。⚠ **C++ 改动未重编** → connector .so 需重编才生效（等引擎窗口空闲）；`py/run_h3m_batch.sh` 补 `export VCMI_NATIVE_DIR/VCMI_WORKSPACE_DIR`（该脚本以 root 运行，HOME=/root，不补会静默拿 /root 下地形栅格=全零）。WSL 侧副本 `~/vcmi-workspace/vcmi_gym/` 已同步 5 文件。② **CI 加真测试**——新增 `tests/test_util_wrappers.py`（11 个 **engine-free** 单测，stub env 覆盖 LegacyActionSpace/LegacyObservationSpace/BlankObservationSpace 三 wrapper；本地 11/11 PASS）+ ci.yml 加 `pip install numpy gymnasium` + `python -m unittest discover -s tests -v`。**剩余**：#205 PR（需 PAT）、模型 release（需定版本）、C++ 重编。
  - **09-23 #298 修复① 打点（已入库 `57622dc`）**：`[EP298_SWALLOW]` 打点在 `ep_runner_one.py` L1890-1900（条件 `secs>250 且 steps<max_turns`，与自然终局/满步截断不重叠）+ 父进程白名单 `train_wsl2_ppo_v2.py` L329 含 `[EP298_SWALLOW]`/`adventure_wait timed out`。**勘误**：此前记"已在 `b6a0a3d` 落地"有误——实为工作区改动，已由 `57622dc` 入库。**依赖核验**：`args.max_turns`（L85）/`_ep_t0`（L409）均存在 → 打点不会被 `except` 静默吞掉。**判据复核（04:06 剖面后修正）**：既然步进正常（~1.4s/步），`secs>250 且 steps<max_turns` 基本等价"存在冻结"（30 步预算下正常不可能超 250s）→ **判据成立**；此前记的"慢速假阳性面"作废。窄例外：250 步满步预算下若提前战死（如 200 步/280s）会误报，可加"该局存在 `adventure_wait timed out`"加固。
  - **09-23 #298 修复②（S 精准修复：heroExchange bothAI 自动关闭）+ 拆轴定谳**：`CGameHandler::heroExchange`（`CGarrisonDialogQuery` 第二创建点 :1582）bothAI 短路；03:08 补丁 → 03:09 构建（libvcmi.so 系依赖链连带重链，RelWithDebInfo/-O2 非 Debug）→ 03:11 起验证。**s300×4 = 4/4 rc=124；s600×2 = good_to_go rc=0/28 步/340s、judgement rc=143（boot hang）**。**拆轴定谳（04:06 `py/_298_stepprofile.sh` 字节增长剖面）**：总 332s / **最长零增长窗口 305s（占比 92%）** / 活跃期仅 27s 跑 19 步 = **1.4s/步（正常）** → ① **不存在"慢速轴"**（"~12s/步"是总耗时÷步数的假象）；② 唯一问题 = **单次 ~300s 完全冻结**，被内部 300s 超时打断后收局；③ s300 组 rc=124 = 冻结 300s + 工作 30s > 300s 被判杀；④ 修复② 对冻结**无消除作用**（前后均 1 次超时）；⑤ judgement **boot hang 2/2**（卡 TERRAIN 段，与 patch 无关，疑 #212 同族）。**冻结源定谳（04:22 `py/_298_freezedump.sh` + gdb 全线程栈）**：**根因不是 dialog**——红方 MMAI(AAI) 与蓝方 NK2 **两条 AI 线程同形卡在 `CClient::sendRequest(item=1414/1415)` 的 `ThreadSafeVector::waitWhileContains`**（← `CCallback::endTurn` ← `yourTurn`），而 `runServer` 空转 `do_epoll_wait` → **死锁：两条 AI 的 EndTurn 永不被 realize、服务器不推进** → 主线程卡 ctypes `adventure_wait` → 300s 超时。**前兆**：`[ML-q] popIfTop FAIL ... 19MapObjectVisitQuery qid=2662 player BLUE, top=null`（服务器查询栈不一致）。**这解释了修复②为何对冻结无效**。**修复方向**：① 服务器查询栈看门狗（top=null/popIfTop FAIL 强制清理推进）② 根因查 MapObjectVisitQuery 为何在 top=null 被 pop（同族 08-17 `removeQuery` PvP 计数欠减）③ AI `endTurn` 走 `waitTillRealize=false`（解死锁但放松回合序）。**下游仍阻塞**：h3m 池 137 张 / WIN-5 batch1-3（`MIX=0`，恢复条件 = 冻结源消除 + boot hang 看门狗验证）。
  - **09-23 收官：4/4 全绿（两个故障全定性 + 三套补丁）**：① **upgrade 死循环**（被冻结掩盖的第二故障）：`AIGateway::makePossibleUpgrades` 的 `do{...upgradeCreature()}while(hasUpgrades())` × 服务器 `CGameHandler::upgradeCreature:2578` 拒绝 → 客户端/服务器状态分歧 → 永久重试（10 万+/20MB、2.4GB 日志、仅 1 步）；前提 = 共享标志 `cc->waitTillRealize=false`。② **网络线程自锁**：`visitPlayerStartsTurn` 在网络线程同步调 AI `yourTurn` → `endTurn` → `sendRequest(waitTillRealize=true)` 阻塞网络线程，而解锁路径 `visitPackageApplied` 只能由该线程处理（gdb 铁证）。**三套补丁**（各带 `.bak` + rollback）：`patch_298_stacktrace.py`（栈打点）/ `patch_298_netthread_fix.py`（**方案1**：网络线程内跳过等待）/ `patch_298_upgrade_probe.py`（upgrade 打点 + **熔断 cap 8**）。**验证（`py/_298_verify_sfix.sh`）4/4 全绿**：good_to_go 30 步/31s、judgement_day 30/24、elbow_room 30/24、a_viking 30/39，rc=0/swallow=0/timeout=0 全满足；健康局仅 2 次 upgrade、`waitTillRealize=1`、熔断未触发。**⚠ 漂移疑点**：方案1 跳过路径健康局 0 次触发 → 冻结消失精确归因待对照实验（回退三套补丁重编再跑）。**待决**：① 是否重启训练并试开 `MIX=0.10 + BATCH=1`（batch1 四图已具可用性）② 开 MIX 后**盯 `[ML-upg] BREAK`**（出现 = flag=false 路径又被踩）③ 三套补丁的对照实验与最终去留。
  - **09-23 #298 boot hang 看门狗上线（`ed8067f`，并行会话，即时生效无需停训）**：根因 = `bootTimeout=120` 在 threadconnector 只包 cond2（client 等 server 启动完成），**cond1.wait 无超时在管辖外** → boot hang 时子进程不死（600s+），主进程 `proc.wait` 可停摆数小时。**修复** = `ep_runner_one.py` daemon 滚动看门狗：400s 无 kick → `os._exit(43)`（绕 GIL/C++ 栈，SIGALRM handler 在 C++ 栈中不可靠）；kick 链 = 启动 + reset 后 + 每步后；rc=43 → 主进程按崩溃局归档（吞局有痕）。**冒烟**：judgement 单局 **30 步/26s rc=0**（boot hang 未复现 = 概率性竞态确认）+ 零误杀。工具 `py/_298_wd_smoke.sh`。
  - **09-24 停训窗执行完毕（冻结定谳 + 崩溃两处 + 移池 + 训练已重启）**: ① **偶发图移池**：`elbow_room` batch:1→99（A/B 实测 ~33% 异常），MIX 池 = good_to_go + judgement_day。② **冻结 Mode A 定谳并修复**——占城胜利在 moveHero 包处理中途 `setState(SHUTDOWN)` 立即杀网络循环 → 包流截断 → NK2 等 realize 永久卡死；gdb 断点 `setState if value==2` 首局命中实锤；修复 = 延迟到 `onPacketReceived` 完成当前包再停机（`py/patch_mlfix_shutdown.py`）。③ **崩溃 rc=139 两处修复**（core.2888/core.5836 尸检实锤）：`GameStatePackVisitor` 英雄战死后 mana 恢复判空 ×2 + `AAI::battleStart` 无英雄防守方判空；修复落 `libvcmi.so`(842a954c)+`libmlclient.so`(83b1cfd2)。④ **Mode B 定谳未修**：NK2 建筑评估持 `CGameState::mutex` 读锁长规划 → `runNetwork` 写锁饥饿（r8 冻结栈），架构级另排。⑤ 训练 10:16 重启 resume step=951759；自检 14/14；今晨 WSL 空闲自杀+保活丢失事故已记踩坑点。详录：知识库「09-24 停训窗」节。
  - **09-24 冻结最后一环关闭（game_over 强制置位，续停训窗）**: **修复**——`visitPlayerEndsGame` 开头调 `strategic_state_force_game_over(winner)`（1v1 口径：victory?player:1-player），`adventure_wait_for_turn/try_wait` 检测后返回 -2（connector 既有 done 路径），`strategic_state_update` 双段（引用/指针版）强制终局优先于 alive 推导；补丁 `py/patch_mlfix_gameover.py`，vcmi-native 已提交。**验证（elbow_room ×12）**: r10/r11 = `EXIT=1 + rc=0 + EP_TIME 正常收局 + [ML-fix] force game_over: winner=1 哨兵`（修复前 EXIT=1 必挂死）→ **Mode A 闭环实证**；剩余 3/12 冻结 = **Mode B**（r9 栈：NK2 卡 `BuildAnalyzer::update` 建筑评估段，与 r8 同型）维持架构级另排；崩溃 **0/12**（判空修复后未再复现，按纪律不下根除结论）。自检 **15/15**；`libmlclient.so`=c1c43450；训练已二次重启拾取（PID 13830）。**判据提醒**：训练态 `[EP298_SWALLOW]`/`err=` 按**复现率**观察，单局异常不下结论。
  - **09-24 Mode B 缓解落地（第二停训窗，trade 循环熔断 + 规划链计时）**: **根因修正定谳**——Mode B 非传统死锁，是 `ResourceTrader::trade` 的 while 循环**无上界**：每成功交易一轮就重跑 `buildAnalyzer.update()` 全量建筑评估（全城×全建筑×依赖递归），NK2 yourTurn TBB 任务全程持 `CGameState::mutex`（static，"AI mutex"）**共享锁** → 读锁内狂转 → `runNetwork` 包 apply 写锁饥饿（实锤：r9 冻结局日志 **89704 行** vs 健康局 ~4000，logAi 每轮刷 Free/Missing）。**修复**（`py/patch_mlfix_tradecap.py`，vcmi-native `8679dd35a1`，`libvcmi.so`=bbce355a）：循环轮数 **cap 16**（超限 `[ML-fix] trade BREAK`，对齐 upgrade 熔断模式）+ NK2 规划链 `[ML-time]` RAII 耗时打点（makeTurn 总/update 每轮/trade passes+耗时）。**验证（elbow_room ×12）**: **12/12 rc=0 全绿零冻结零崩溃**（历史异常率 25-50%，P≈3% 统计显著）；健康耗时基线：trade 1 轮 0ms / update 0-1ms / makeTurn 159-552ms；3 局 20s 慢段为局末长规划非死锁（抓栈误杀，时间线可见）。**训练已三次重启拾取**（PID 16674）。**遗留降级**：Mode B 从"架构级死锁"降级为"已熔断的循环上界问题"，残余风险 = cap 触发时交易次优（可接受）。
  - **09-23 当前状态与待决**：训练**处于停止状态**（04:0x 为取证人工停，`step≈948k` 已存档）——**待拍板**：① 重启训练恢复 T7.8 观测窗 ② 或继续在窗口内做冻结源修复（方向见上条三档）。#298 冻结源已定谳（**AI EndTurn realize 死锁**，非 dialog），修复②（heroExchange）对冻结无效、去留待配对样本。
  - **09-23 三件事执行完毕（收官窗口，实况核验）**：① **落文档 + 提交** —— `5ec8d27`（知识库 09-23「冻结源修复 + 4/4 全绿」节 + 踩坑点 L1548 收官条含 `popIfTop FAIL` 勘误 + 本条收官条）+ `0c22bca`（三套补丁脚本入库），工作区干净。② **重启训练 + 试开 MIX** —— unit 双副本改 `HOMM3_H3M_MIX=0.10 + HOMM3_H3M_BATCH=1` + `daemon-reload` + start；checkpoint resume `step=948241`，进程环境实测生效（MainPID 247909，keepalive 8 进程），课程图基线未塌（T05/T06 r=19.5~71.3、King_of_Pain -109）。③ **盯 `[ML-upg] BREAK`** —— 重启后**实采到池图 `a_viking_we_shall_go_h3m.vmap` ×2**（`MIX` 生效实证），`BREAK` = **0** / `[EP298_SWALLOW]` = **0**。**观察**：`a_viking` 满步局奖励 `r=-934.7 / -891.4`（池图负奖励量级远大于课程图，待随缘归因）。**仍待决**：三套补丁对照实验与最终去留（漂移疑点未钉死）。
  - **09-23 漂移取证 + 对照实验设计（收官窗口续）**: 实况核查确认**漂移条件成立**——WSL `~/vcmi-native` 工作区相对 HEAD **3028 文件 / 79959+ / 73473- / 5074 hunk** 未提交（完整上游重同步 + ML fork 代码：`ML/MLClient.cpp` 610、`MLBot.cpp` 639、`AINodeStorage.cpp` 641 等）→ **`.so` 无法从已提交源码复现**（系统性风险）。**⚠ 勘误**：先前记的"6 文件 / 508+/214-"是误报（`git diff --stat` 带了 6 文件 pathspec，只统计子集）。**入库策略**：只提已分类 6 文件 + 显式标注两处 08-17 回退；~3000 文件另起专项盘点，勿 `git add -A`。`BattleResultProcessor.cpp` 的 08-17 `removeQuery` 修复在工作区被回退成 `popIfTop`，但 `.bak_stk298`（09-19 04:18）已是 `popIfTop` → 回退**早于本次重编**，**不能**当修复源。**对照实验设计**：只回退三套补丁、不动其余改动 → 重编 → 同 4 图 **N≥4 轮**（冻结是竞态，单轮会误判）。**去留倾向**：熔断 cap 8 **留**（防 upgrade 死循环，独立价值）/ 方案1 **留**（正确性有逻辑论证，0 触发恰说明只在真死锁时才动手）/ 两处打点留到观测窗结束再撤（当哨兵）。**排期**：下个自然停训窗，**不打断当前 MIX 观测窗**。
  - **09-23 两处 08-17 修复被上游同步冲掉（已核验）+ 恢复判断**: ① `CGameHandler.cpp` `levelUpHero`「AI 玩家升级自动选第一个技能」——HEAD 里有、工作区与 09-19 备份里都没有 → **09-19 上游同步时静默丢失**；② `BattleResultProcessor.cpp` `removeQuery` 强制移除，同因丢失。**共性 = ML 修复以"工作区改动"形式存在，上游同步即被覆盖**。缓解：`ENABLE_ML` throw 熔断**确已编入** `rel/bin/libmlclient.so`（`grep -a 'unanswered query'` = 1，`strings` 是假阴性）；训练日志 `has to answer queries` = **0 次** → 暂无征兆。**判断：要恢复，优先级中** —— 按现行上游结构重新实现（在 `!hero->getOwner().isValidPlayer()` 之外补 AI 分支直接取 `hlu.skills.front()`），**不是 revert**；排到下个自然停训窗（一次一轴）。
  - **09-23 全树丢失排查 + 3022 文件盘点（收官窗口续②）**: **排查结论 = 只有那两处丢失，无第三处**（⚠ 后修正：**实为三处**——标记判据漏了「纯代码改动、注释不变」型，见下条）。两道判据交叉：① 标记计数差（`git grep -c` 于 `41a99cd52c` vs 工作区，15 文件对齐，净 −1 仅两处）② 全树 diff 删行 CJK 扫描（仅 11 行，逐行核为重构/搬家）。**反例勿误判**：`CQuery.cpp` 08-17 去重修复**注释丢但逻辑在**（上游自加 `// prevent duplicates`）。**3022 文件盘点**：3022 文件 / 79379+ / 73187−，**1541 文件仅 chmod 模式变更**（噪声）；主体是完整上游重同步（上游核心 1934 + 翻译 369 + 测试 165 = 76%），ML fork 仅 73 文件。**入库前先压 chmod 噪声**（`core.fileMode false` 或单独 mode commit）。
  - **09-23 入库 + 三处修复恢复（收官，已改-编-验）**: ① **入库**（vcmi-native，4 提交）—— `778e7bcc67`/`e66da367ab`（已分类 6 文件，上游同步/ML 集成 各 1 提）+ `375fba919e` 上游重同步 1160 文件 + `25f14db927` ML fork 31 文件；`core.fileMode=false` 压掉 1541 chmod 噪声；`.so` 现已可从 git 复现。② **三处修复恢复**——发现**第三处丢失**（09-14「删 `removeQuery` 的 `removalDone` 守卫」，标记判据盲区），三处一并恢复：`CGameHandler` levelUpHero（按新上游结构）、`BattleResultProcessor` 调用点还原 `removeQuery`、`QueriesProcessor` 去守卫；脚本 `py/patch_298_restore_0817.py`（幂等+校验+rollback，**rollback 已修 `copy2` mtime 坑**），提交 `e439b22759`。③ **A/B 对照（决定性）**——`elbow_room ×6`：基线 4/6 异常（2×139+2×124）vs 恢复后 3/6（2×139+1×124）→ **崩溃/超时是既有竞态，非本次引入**；恢复后 4 图冒烟 3/4 rc=0（`a_viking` rc=124）。④ **训练已重启**——`MIX=0.10 + BATCH=1`，resume `step=951388`，PID 286097，keepalive 8，`BREAK`=0 / `SWALLOW`=0。
  - **⚠ 09-23 重大勘误（动摇过 ② 决策前提）**: **07:28「4/4 全绿 = 冻结消失」是单样本侥幸**——A/B 基线在 `elbow_room` 就有 **2/6 的 300s 超时** → **冻结从未真正消失**，"方案1/熔断修好冻结"的结论**已证伪**。**遗留**：① 冻结（AI EndTurn realize 死锁）仍需修，方向见上文三档；② `elbow_room`/`a_viking` 为偶发图（超时 ~33%，与本次改动无关），**MIX 池含偶发图 → 训练会持续见到偶发 rc=124/139，判据需按复现率而非单局**；③ 竞态类验证从此**强制 N≥4 + 基线对照**。
  - **09-23 夜间三条（用户拍板，本窗口收官口径）**:
    - **① 冻结仍需真修**：07:28「4/4 全绿 = 冻结消失」**已 A/B 证伪**（`elbow_room` 基线即 4/6 异常）→ **AI EndTurn realize 死锁仍在**（红 MMAI/AAI + 蓝 NK2 两条 AI 线程卡 `CClient::sendRequest` 的 `waitWhileContains`，`runServer` 空转）。**方向三档**（知识库 09-23 章）：① 服务器侧查询栈看门狗（`top=null`/`popIfTop FAIL` 强制清理推进，治标直接解死锁）② 根因查 `MapObjectVisitQuery` 为何 `top=null` 被 pop（08-17 `removeQuery` 计数欠减同族）③ AI `endTurn` 走 `waitTillRealize=false`（解死锁但放松回合序）。**三套补丁（stacktrace / netthread 方案1 / upgrade 熔断）的对照实验排下个自然停训窗**，不打断观测窗。**结论：冻结未修 → h3m 池混合轴的价值打折，MIX 处置与冻结修复联动决策。**
    - **② 偶发图登记（待拍板）**：`elbow_room` / `a_viking` **~33% 概率 rc=124（300s 超时）/rc=139（SIGSEGV）**，与本次改动无关（A/B 基线同量级）。`a_viking`×2 已于 09-22 摘除（`batch:1→99`）；`elbow_room` 仍在 `batch:1`。**移出与否的量化依据**：MIX=0.10 且 batch1 三张均匀 → 全局约 3.3% 局落在 `elbow_room` × 33% 失败 ≈ **1.1% 全局吞局率**（每局浪费 ~300s），量级不大但污染样本且 `[EP298_SWALLOW]` 打点至今 **0 次命中**（观测盲区未验证）。
    - **③ 竞态验证纪律**：强制 N≥4 + 基线对照，单样本通过不算数 —— 已提升为**置顶硬规则**（见本文件顶部），踩坑 #298 同步。
    - **⚠ 数据核查勘误（`a_viking` ×2 归属）**：09-23 收官条记「重启后实采到池图 `a_viking` ×2（MIX 生效实证）」**归属错误** —— 该两局在 **step=889517/905401**，而 09-23 resume 起点 **step=948241**，属 **09-22 窗**。**09-23 重启后实跑 17 局、池图 0 局**（MIX=0.10 期望 ~1.7 局，`0.9^17≈0.17`，样本不足，既不能证"MIX 生效"也不能证"零出现复发"）。→ **下窗开 MIX 后第一件事 = 先验「池图出现率 ≈ MIX 值」**，再谈偶发图影响。
  - **09-23 关机前状态（用户关机，训练优雅停止）**: 训练 **inactive**，存档 `Saved STATE_PATH (step=951439) + MODEL_PATH`，无残留引擎进程；主仓 `d88af0e` 干净。**发现并修正一处配置漂移**：unit `homm3-train-v5` 当时是 `UnitFileState=disabled`（与规则「system 级 enabled」不符，会导致重启后不自启）→ 已 `systemctl enable` 还原（符号链接 `multi-user.target.wants/` 已建，**未启动**，保持 inactive）。**下次开机 WSL 起来后训练会自动续跑**（checkpoint resume）；若要先做冻结修复/对照实验，需先 `systemctl stop` 避免占引擎窗口。另注：MIX=0.10 池含偶发图（`elbow_room`/`a_viking` ~33% 出现 rc=124/139），判据按复现率看。
