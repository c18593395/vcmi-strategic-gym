# 当前任务清单（当下窗口要做什么）

> ## ⚠️ 启动训练必带 Windows keepalive（09-18 实证，置顶纪律）
> **每次启动训练后，必须立即拉起 Windows 侧 keepalive，否则 ~60 秒后 WSL idle shutdown 把训练整个带走**：
>
> ```powershell
> Start-Process wsl.exe -ArgumentList '-d','Ubuntu','sleep','infinity' -WindowStyle Hidden
> ```
>
> - WSL 空闲判定只看 **Windows 侧客户端**（终端/keepalive），不看 VM 内训练进程——训练在跑也会被关机（09-18 实证：journalctl 大量 "Stopping homm3-train-v5" 全是 **poweroff 关机流程**，极易误判为外部 stop；`last` 显示 WSL 当天 reboot 9+ 次）。
> - keepalive 是隐藏进程，**360 安全卫士会杀进程+删计划任务（#267：22:31/02:24 两次 idle shutdown 根因）**。**09-19 拍板：不做任何自动启动，每次手工拉起**：
>
> ```powershell
> Start-Process wsl.exe -ArgumentList '-d','Ubuntu','sleep','infinity' -WindowStyle Hidden
> ```
> （或双击 `py/homm3_wsl_keepalive.vbs`；拉起后验证进程数 ≥1：`Get-CimInstance Win32_Process -Filter "Name='wsl.exe'" | ? { $_.CommandLine -like '*sleep*infinity*' }`）
> - 兜底：enabled unit 在 WSL 冷启动时自动恢复训练（02:25 实证零损失），keepalive 只是防冷启动中断。
> - 判活仍按踩坑 #201 三件套（PID etime + 日志 mtime + cgroup/MainPID）；异常重启先查 `wsl bash -c "last | head"` 看 WSL 是否 reboot，再查 journalctl。
> - 指针：踩坑 #114/#201 + 09-18 WSL idle shutdown 循环定性（已固化知识库 09-18 章）。

> 父文档：[总任务.md](总任务.md)（大项 T1-T13 整体脉络）。本文件只写**当前有效**的任务：窗口焦点 `WIN-x`、观察期 T 任务、训练期可做工作、风险与远期。
> **脱钩原则**：技术细节不在本文件展开，一律指针到 `WSL知识库.md` / `WSL踩坑点.md` / `已完成任务.md`。完成即归档移出，保持短小。
> 最近整理：2026-09-19（**WSL 灾难重建完成，主训练已 resume step=770300 device=cuda**；待用户手工拉 keepalive 后进入稳定攒局。S2 窗续攒 + `s2_auto_review.py` 重拉 + WIN-4 批转续跑（8/159）为恢复后三件事。**09-19 白天补记**：网络恢复窗口收口防再犯三件套①（vcmi-native git 重建 + 513 未入库增量 commit d62071da8a + D 盘镜像仓同步 + SSH 密钥恢复，踩坑 #278）；② export / ③ 重建脚本仍待办）。

> ## ✅ 紧急收口（09-19 10:xx 二次更新）：WSL 重建+MMAI 修复全部完成，训练稳定运行（step 770467+，首局 r=151.8）
> **事实链回顾**：02:25-02:48 Lxss 注册表 Ubuntu 项被删（疑似 360）+ ext4.vhdx 删除（踩坑 #269）→ rootfs 重装 + systemd/用户重配 → 用户配置 .wslconfig（firewall/dnsTunneling/autoProxy=false）后网络打通（新子网 172.26.192.0/20）→ 重建全链路完成 → **MMAI 静态 AI 机制修复（STATIC_AI + 8 处源码修复，踩坑 #280）→ 冒烟 3 步全绿 → 训练恢复 step=770300 起正常累加（首局 r=151.8 err=no，[ECON]/[RECRUITED] 正常）**。
> **恢复后待办勾选**：① keepalive 手工拉起 ✅ ② 首局 obs 终验 ✅（BATTLE_ROUND 运行期通过，OBS_DIM=3464 契约成立）③ ~~s2_auto_review.py 重拉~~（待办）④ WIN-4 批转续跑（8/159，断点在；注意 py/ 迁移后管线路径 py/h3m_batch_pipeline.py）⑤ 防再犯三件套：#281 实证镜像仓已建+513 增量已入 git（①完成✅），补丁集 8 件已 commit d6f8f373a8 推镜像仓（09-19 晚✅），`py/setup_wsl_train.sh` 一键总装脚本已固化（③完成✅），剩 wsl --export 定期备份（②，停训窗执行）。
> **新识别坑**：#278（export 锁定发行版→0x8000000d）/ #279（根目录 .py"消失"= 并行会话 git mv 到 py/ 落实代码入 py 规则，RUNNER/ExecStart 已同步更新）/ #280（STATIC_AI 大坑）/ #281（.ssh+git 元数据修复）。
> **⚠️ 环境变更登记（#282，用户拍板 B）+ ✅ 困死定性（#283，用户拍板不降级）**：STATIC_AI 修复激活蓝方对抗行为（blue 会移动/战斗/发展），red 死亡率 0%→62%、duel 僵局消失——**S2 撤梯子窗基线作废，从恢复点重开计数 40 局复查**；恢复段 19 局作废不纳入评估。**#283 已定性：62% 死亡率全部为 all-blocked x2 困死（非战死，BHERO_* 战斗事件全 0），属蓝方围堵的合理环境副作用，不触发 blue 降级（弱化 MMAI_RANDOM 无法对症"围堵"问题且削弱训练信号）**。观察项改为：监控 `[RECRUITED]/局` 是否从 0.6 回升（当前因英雄困死招兵断流），回升到 ≥5/局后再做 S2 正式收口复查；all-blocked 熔断 -50 语义攒数据再议。
> **指针**：知识库 09-19「重建收尾全链路」+「MMAI 库编译修复」章 / 踩坑 #270-#283。

---

## 一、当前窗口焦点 WIN-x

### WIN-1 — 批次B + A3 中期收口 ✅ / passable 闸门方案甲已部署 🔄（09-18 重启验证窗）

- **由来（一行）**：09-15 capture proxy 空拍误报修复后，三层证据链证明**真实击杀基线=0** → 击杀激励重设计。批次A（P-H1）✅ 收口；批次B+A3 同窗（用户拍板，判据可分）✅ 09-17 中期收口；passable 闸门方案甲 09-18 立项部署（编译+重启，验证窗进行中）。
- **当前窗配置（09-17 部署，`py/restart_train_v5_win1_batchB.sh`，8 Environment）**：grad 0.2 + cap 25 + **contact_r 15 + contact_d 2** + kill 40/30 + **own_town_decay 0.5 + max_visits 4**；中途修复 A3 空撞复核时序 bug（踩坑 #253，step=750519 重启）。
- **中期结算（修复后窗 42 局，L105629 起）**：
  - 批次B：① 接战率>0 ✅ **5 局触发 [BHERO_CONTACT]**（全 T06_72 族 d=2 贴脸 +15，contact=[3] 记账闭环）；④ 防通胀 ✅（窗口 avg_r **+12.83 首次转正**，分图峰值 KoP +113.8）；② 单杀 / ③ TOWN_CAPTURE = 0 → **归 passable 闸门项**（预研定谳：观测 passable 封锁敌格三方结构性不可达，非参数问题）
  - A3 全绿 ✅：own_town pick **0**（基线 589）、TOWN_EMPTY 0（修复后零误判）、TOWN_VISIT **1.64 次/局**（基线 3.0）、T05 三图 **+62/+62/+91 全翻正**（基线 -170~-255）、长局转正（250 步局 r=+9~+71）；衰减中间态（V=17.5/8.75）无直接日志点（own_town 从未进 pick/runner_up），达标按行为终点口径成立
  - P-H1 保持：min_d p50 **33**（批次A 47 → 基线 128），5 局 min_d=2 贴脸；KL 0.71/0.3 平稳；截断 16%
  - duel 三图 -110~-170（前窗 -157~-168 有改善未翻正）——激励真空+贴脸僵局，同等 passable 预案
- **passable 闸门（方案甲，09-18 部署 🔄）**：`ML/strategic_state.cpp` 两处 passable 口径由 `isClear` 改 `getTerrain()->isPassable() && !(blocked() && !visitable())`（逐字对齐 GHandler `movingOntoObstacle`，CGameHandler.cpp:916）→ 敌英雄格/敌城格/怪格 passable=1，模型方向动作、MOVE_TO BFS、蓝 AAI 三方结构性封锁解除（WIN-1"真实击杀=0"机械根因）。双备份 `.bak_passable_0917`（源码+.so）；补丁 `py/patch_passable_0917.py`（精确替换+count 断言，失败即退回不改文件）；重编 `cmake --build ~/vcmi-native/rel --target mlclient -j8`（RC=0，libmlclient.so 294945296B @ 09-17 16:48，`STRATEGIC_STATE_LIB` 唯一运行时副本，rel/bin 原位即部署）；重启走 `restart_train_v5_win1_batchB.sh`（8 Environment 不变 + checkpoint resume）。**验证 4 判据**：① passable 新口径不炸（无 moveHero 全拒/#143 复发）② 首个真实战斗事件 ③ BHERO_KILL 非零 ④ 死亡局占比 <20% + avg_r 无塌。回退 = .so 备份恢复 + 重启窗。
- **验证窗快照（09-18 16 局 → 晚间 31 局 + B+C 窗 → 深夜补记账窗，看板 `py/passable_gate_watch.py`）**：**4 判据全绿 ✅**：① ✅ 持续零错误局；② ✅ 攻击步下发 → 引擎 startBattle objType=hero 双人 → winner=0（[BHERO_ATTACK] 已进主日志，跨图复现 72_02+108_02）；③ ✅ **达成**（09-19 00:17 72_02 局：攻击步 → [BHERO_SLAIN] +40 end-of-ep credit → BHERO_GRAD **slain=[3]** 非空——BHERO_KILL 首次全链路记账）；④ ✅ 大负局全为 duel 图已知底噪，非 duel 0 死亡局；avg_r 无塌。正面信号：贴脸率 33%（批次B 中期 2.7 倍）、min_d p50=31。**WIN-1 passable 闸门收口条件达成，待用户拍板进入下一轴**（BUILD P1 经济轴 / 开源周 / S2，见八-09-18 开源立项触发条件①已满足）。
- **下一轴候选（passable 闸门验证达标后）**：BUILD P1（T05/T06 生成器给红城预置 fort+L1，改图走 sync_maps 流程）经济轴候选；与门禁见预研 §1.4/§2.2。**09-19 凌晨备料完成（A11）**：5 张非 duel 图 `_p1.vmap` 副本就绪，入池流程见 A11 行。
- **下一轴三选一建议（09-19 凌晨，待拍板）**：**首选 S2 撤梯子**（T7.5 顺序表 WIN-1 达标后第一顺位；激励微调 RECRUIT +0.5→+0.25 / BUILD_2 +0.75→+0.375，判据明确 avg_r 跌<20%）；次选 WIN-3 ①难度轴；WIN-2 回池殿后（历史 51 局全负数据太老）。**开源周**：首推+CI 绿已完成（`vcmi-strategic-gym` HEAD `c0cb8ed`），剩余迭代项（英文 quickstart/真测试/模型 release）不阻塞训练主线，挂起随缘。**B+C 调试开关去留**：下一窗（S2）开窗时建议关 `HOMM3_ATK_DEBUG_FORCE` 留 BYPASS+BFS 豁免（激励纯净，C 方案自然引导已可落格）。
- **回退线**：批次B 塌/通胀 → batchA 脚本；A3 异常 → 删 2 行 Environment 独立可撤。聚合工具 `py/win1_batchA_agg.py`（自动取最后 WIN1_BATCH 行）+ `py/win_bB_watch.py`（双轨信号）。
- **指针**：预研 `docs/预研_OBS3英雄战斗触发链与BUILD链_20260917.md`（§1.4 立项材料）；踩坑 #250-253；错窗纪律：与 S2/WIN-3 互斥。

### WIN-2 — 36X36_02 原样回池评估 🟡（归属 T6.4，待触发）

- 方案已完善：`docs/方案_T05_36X36_02重评估_20260910.md` §4（触发条件表 / 回池步骤 / J1-J5 五判据 / 拉数命令）。
- 触发 = **WIN-1 达标 + 错窗确认**；第一步 02 图原样回池重评估，判据 J1 = r_mean(02)/r_mean(01) ∈ [0.8, 1.25]；不通过才走图 03（01 骨架+gold mine 加 swordsman×6 守门）差异化设计。
- 历史 51 局全负数据采集于假糖时代，仅作背景。

### WIN-3 — 下一难度轴候选 🟡（归属 T6.4，一次一轴，WIN-1 达标后择一）

| 候选 | 红 hero→蓝镇距离 | /250 步 | 结论 |
|------|----------------|---------|------|
| ① 72X72_02_duel | 128 (51%) | 250 内可完成 capture | ✅ 即插即用，已在 MAPS |
| ② 108X108_02_duel | 200 (80%) | plen=201<250 已可达 | ✅ 250 步方案已部署，#238"不可达"作废 |

- 250 步已全局生效（`STEPS_PER_EP=250`，banner `×250steps` 验证）；duel 副作用由 P10-target-2b 修复（duel move_to_force=60，r −1665→−123~−183）。挪城脚本 `py/win3_move_town_108_02_duel.py` 备而不用。
- ③ 1v3 击杀/占城数据持续自然积累，不占窗口。
- **纪律**：难度轴与激励轴/图池变更不同窗。原"挪城修堵点"方案已撤销（72_02 全草地 BFS 可达，地形无墙）。

### WIN-4 — 官方 H3M 全量批转入池（图池扩容主线）🔄（09-19 启动，用户指令主线）

- **目标**：159 张官方 H3M 逐张转换 → **每张 250 步真实训练环境验证**（与主训练同构）→ PASS 才改名 `<safe>_h3m.vmap` 入 `maps/training/h3m_pool/`；双方英雄同层（strip 去地下，无地下最优）。
- **工具链（已定稿）**：h3m2vmap R2v2 智能重配（aiSlot 恒 blue / 他色中立化删英雄 / blue 城保底 / factions 补全）+ 官方图一律 `--no-r1` + `py/strip_underground_vmap.py` + `py/h3m_batch_pipeline.py`（断点续跑；报告 `maps/h3m_to_vmap/_pipeline_report.json`）。
- **09-19 类型普查（h3m_type_survey.py，150/159 成功 + 9 张 SOD 自定义英雄头待解析）**：层结构 单层42/有门32/纯地下76；水域 strict28/open122/sealed0；有岛44（陆地分量≥20 的重岛图16张）；玩家 4p47/3p23/6p22/5p20/2p14/8p12/7p11/1p1；同盟图56张（teams≥2）；尺寸 72/108/36/144=68/36/30/16。H3 转换风险标签 OPEN_WATER=122、UNDERGROUND=98、TEAMS=47 → 免风险仅 Judgement Day / King of Pain 2 张；其余图 PASS 全靠在转换期 strip 去地下 + 验收期模型能力兜底。
- **状态（09-19 23:xx）**：存量 5 PASS（Ready or Not / Faeries / A Viking We Shall Go ×2 / All for One）+ 2 早期 FAIL（A Warm and Familiar Place steps=30 / Adventures of Jared Haret steps=1）；引擎已恢复（STATIC_AI 路线，libmlclient/libvcmi 09:46 重建，训练 22:40 已重启至 step 813k 正常跑）；**全量批跑启动方式已定 = `py/run_h3m_batch.sh`（停训 → nohup 批跑断点 → 自动重启训练，纯串行禁并行，踩坑 #131）**，管线 bug 已修（`steps_done` 作用域 NameError → `_extract_steps(detail)`；旧 100 步 PASS 条目默认信任不重验，`--resume-fail` 只重验 fail 条目）。
- **判据**：steps≥125（50%）+ traj 无 error + rc∈{0,124}；老 100 步 PASS 自动按 250 步重验。
- **后续**：批跑完出 PASS/FAIL 统计 + FAIL 归因迭代；入池后从 h3m_pool 挑图进主训练 MAPS——**晋级纪律：一次一轴，与晋级/开经济错窗**。
- **风险与护栏**：批跑必须停训窗内做（双进程同吃 CPU，踩坑 #131）；任何 pkill 先查 ppid（#264 险情）；存量 #266（traj["rew"]）已修待下一停训窗验证。
- **指针**：踩坑 #260-#266 / 知识库 09-19 章 / 普查产物 py/h3m_type_survey.json。

### WIN-5 — 多类型地图入训练体系（用户指令登记，09-19，后续做）

- **目标**：让"单层/地下/有门/有岛/strict/open"等地图类型都能作为可配置难度轴加入训练。当前 `h3m_batch_pipeline.py` 只收 strict 单层图，地下图被 `strip_underground` 去层（地下轴被消除），open/有门/有岛未入池。
- **三层方案**：
  - **数据层**：新建 `maps/training/h3m_pool/_pool_index.json`，每张入池图标 5 维标签 `{layer: single|underground, underground: bool, island: bool, gate: bool, water: strict|open}`。标签直接复用 `h3m_flat_pool.csv` 的 `category/underground/border_gates/land_components` 列，不重算。
  - **训练层**：`py/train_wsl2_ppo_v2.py` 的 `MAPS` 列表从写死改为读 `_pool_index.json` + `map_filter` 配置项（`{"layer":"*","water":"*","underground":bool,"gate":bool,"island":bool}`），按标签过滤采样。加新类型=改标签不改代码。
  - **转换层**：`py/h3m_batch_pipeline.py` 加 3 开关：`--keep-underground`（地下图不去层直接入池，地下变独立难度轴）/ `--include-open-water`（open 水域入池，需补造船激励）/ `--include-gate-island`（门/岛入池）。每图入池仍跑 250 步验证，FAIL 自动剔除。
- **关键约束**：① OBS_DIM=3464 冻结——地下/门/岛/水都不占 obs，只改地图文件+标签 ② 错窗纪律——一次只加一个难度轴，加地下轴时不能同时加 open 轴 ③ open 水域需先评估 reward 是否加 SHIP 造船激励（当前 RECRUIT/BUILD/WIN 没算造船，模型可能学不会造船全卡水边） ④ 地下层保留=红蓝英雄可能不同层，strategic_env obs 同层假设需重验。
- **落地顺序（错窗，每次停训窗 1 轴）**：1 建 `_pool_index.json`+训练层采样（无新图跑通）→ 2 `--keep-underground` 入 1-2 张地下图验证 → 3 `--include-gate-island` 验证 → 4 open 水域（先评估造船激励再入池）。
- **状态**：🟡 登记，WIN-4 批转完（159 张 strict 入池）后启动。

---

## 二、观察期任务

### T7.8 — A3 战力闸观察窗（踩怪战死治理 + B 攒 100 局）🔄（09-19 部署）

- **由来（一行）**：T7.7 观察窗实锤 #283 推翻——困死 0/120、**战死 72/120=60%**（half-self 近似 pc=self*0.5 → F 恒正 → 打不过也踩 + BFS 穿怪格路过误踩）。duel 净胜率干净基线 **-23%**（旧 +21% 为污染口径不可比）。
- **A3 战力闸（09-19 停训窗，四改动）**：① `get_guards` 5 元组带真实战力（AIValue×amount，peasant=20/archer=67/swordsman=119，未知回退 150）；② SCORE BFS 避怪：非守卫目标时 vmap 怪格入 blocked（导航绕行，目标格豁免由③兜底）；③ `candidate_power_c` 重写：守卫直读真实战力 + 资源堆 t[6] log2 反解（2^gp-1），half-self 废弃；④ F 硬闸：F<-0.3 的怪/带守卫资源不入池（蓝英雄/蓝城豁免）。自测：守卫400 vs 英雄100 → F=-0.95 剔除 ✓。
- **A3 二修（同日，日志分析发现）**：⑤ **power_self 军队战力公式**——obs[base+10] 是英雄**法力属性**（恒 1，非军队战力，旧注释 "total_power" 误导），F 闸拿法力比军队 → guard_pick=0/守卫胜 0；改 army_count field 15-21 × AIValue 阶梯（复用 L1492 公式），F 变真实动态值（蓝英雄 -0.20~-0.23，验证 traj army=5250）；⑥ **f_min -1.0→-0.2**（train_wsl2_ppo_v2.py L179）——-1.0 是 F 假值时代的无护栏档，F 真实后微负不送（实锤 3/4 局攻 F=-0.2 蓝英雄战败）。修正后 25 局：战死 72%（仍高，集中在非 duel 72 族）、duel 5 局全活、370+ 新高分三连（72_01:372.9 / 72_02:373.9,375.3 历史未见）。
- **A3 三修（方案C，同日）**：⑦ **采样硬 mask**（`--monster_mask` 默认 1）——8 邻怪格方向 logits=-inf（local_tiles ch2 = obs[930:1155] 守卫战力层判定），治模型直发 act0-7 裸奔踩怪（BFS 避怪只管 MOVE_TO 链的最后一块裸奔路径）；豁免 move_guard_target + 8 向全怪不 mask（防 softmax NaN）；冒烟 3 局全活验证 ✓。已知残留：贪心回退（全图 BFS 不可达时 pas[d] 直选）未避怪，量级待满窗数据。
- **B 攒局**：mask 上线后攒满 100 局（step 819788 起，预估 ~step 860k）。
- **判据（100 局后复跑 `eval_new_baseline_0919.py`）**：① 战死率 60% → **<35%**；② duel 净胜率 -23% → **>0%**；③ 守卫胜率口径复核（现 0 完全避怪——若满窗验证"不打怪占矿照拿"，判据③改写为"370+ 高分局占比"）；④ avg_r 抬升。
- **回退**：全部 Python 旁路零重编；monster_mask=0 关 mask / f_min 摘参 / get_guards+BFS+score 层 git revert。
- **指针**：T7.7（前置，✅ 收口）/ 踩坑 #285/#286 / 知识库 09-19 深夜窗 / ep_runner_one.py L218-237（get_guards）/ L591-606（采样 mask）/ L1098-1102（BFS 避怪）/ L766-777（power_self）/ L1053-1072（BHERO_ATTACK 军队公式）/ target_scorer.py L106-125/L294-297 / train_wsl2_ppo_v2.py L179-184（f_min=-0.2）。

### T7.7 — 根因C'修复观察窗（战死/困死重定性 + duel 净胜率复跑）✅（09-19 收口，判据达成 → 演进为 T7.8）

- **修复内容（09-19 停训窗）**：`ep_runner_one.py` ① ah=-1（红英雄消失）立即终局 `[RED_DEAD]`+death_penalty -50+done（消除 fallback=0 坐标错位连锁：战死后 heroes 段重排蓝英雄顶 slot0 → 假贴脸 d=0 → 假 BHERO_ATTACK → zombie 假判定，实锤踩中立怪 battleFinished winner=1 战败局）；② B 方案 d=0 时 `_atk_skip_nav=False` 走 BFS 绕行（L1054-1062）；③ 冒烟验证：假 HERO_DEATH 3/3→0/3、假攻击 4→0、RED_DEAD 3/3 正确触发，两局长跑 r=256/239。
- **观察判据（40-60 局后，`eval_new_baseline_0919.py` 复跑）**：① `[RED_DEAD]` vs `[ZOMBIE]` 比例 → **重写 #283"62% 全困死非战死"定性**（若战死占比高，激励重心从"突围"转向"打不过别踩/变强"）；② duel 净胜率（守卫胜-困死）对比修复前（+21%，分图 +20/0/+40）；③ 假阳性清零确认（主日志不应再出现 ah=-1 后的 BHERO_ATTACK/ZOMBIE）。
- **快速拉数**：`wsl bash -c "grep -c RED_DEAD /mnt/d/Bigdata/hero3_fresh/train_loop.log; grep -c ZOMBIE /mnt/d/Bigdata/hero3_fresh/train_loop.log"`（RED_DEAD 主日志有转储）。
- **指针**：知识库 09-19「startBattle 引擎层直调」章（D 方案储备）/ 任务清单七-远期 D 方案条 / ep_runner_one.py L1607-1614（RED_DEAD）/ L1054-1062（B 方案）。

### T7.4 — 死亡惩罚 🔄（-50 试探档 09-11 上线）

### T7.6 — A2 攻击步旁路单局冒烟 ✅（09-18 晚 B+C 窗达成：引擎侧全链路打通，红蓝英雄真实战斗首胜）

- **最终结论（详见知识库 09-18 晚 B+C 章）**：① 攻击步三处前置缺陷已修（commit `8c5b5e1`）+ C 方案 bfs_full_dir 动态障碍豁免（`7e8491d`）+ B 方案 DEBUG_FORCE 强攻开关（`69bab3d`）；② **`[BHERO_ATTACK]` 首次触发且引擎全链路受理**：moveHero 落敌格（零 blocked）→ startBattle（objType=hero，RED+BLUE 双人参战）→ R6 结算 winner=0 → 蓝英雄被歼（notifyObjectAboutRemoval 实锤）——**判据②"真实战斗事件"达成，OBS-3 链路级闭环**；③ 残留：BHERO_KILL 双帧确认在局尾截断来不及（记账时序，非链路），**局尾补记账修复待拍板**。
- **运维**：贴脸局快照器 `py/contact_log_snapshot.py`（环形 5 槽×240s+contact 触发，重启 `py/restart_snapshotter.sh`）；keepalive 守护 `py/homm3_keepalive_guard.ps1`（计划任务每 5 分钟自愈，22:31 idle shutdown 再发后加）。
- **经验**：主日志 10-env 交织 + 事件行无地图名 → 局级归因必须用局边界或 `/tmp/hermes_ep_<pid>.log`（踩坑 #256）；pgrep/pkill -f 自匹配假阳性（踩坑 #257）
- 冒烟步骤：`wsl -u root systemctl stop homm3-train-v5` → 临时改 restart 脚本加 `Environment=HOMM3_BLUE_HERO_ATTACK_BYPASS=1` → 启动 → 等 72_01 图开局（约 10-20 局内）→ grep `[BHERO_ATTACK]` 日志 + 引擎 `startBattle` + R6 onnx `battle_result≠0` → 优雅停训恢复
- 判据：`[BHERO_ATTACK]` 至少 1 条 + `state.battle_result` 非 0（R6 onnx 结算正常）
- 风险：踩坑 #254 已修（`WIN1_ENV_ARGS` 映射补齐），但冒烟前须 grep `ps aux | grep ep_runner_one | grep blue_hero_attack_bypass` 确认参数已传到子进程
- 指针：`docs/方案_攻击步旁路_20260917.md` §6（冒烟方法）/ 踩坑 #254

- 判据 1（死亡局 r 转负）：⏸ 挂起已定性——全草地大图 ZOMBIE 结构性不可达（T03/T04 小图历史 441 局，大图 0 局）；真败北信号断链另一半已由 #228 根修（红败 go=2/r=−200 秒级传递）。
- 判据 2（接战率 ±20% 不塌）✅；判据 3（avg_r 跌 <20%，基线 2.34）✅。
- 回退线：接战率塌 >30% 或 avg_r 跌 >20% → 降档 −25 或 0。指针 #204/#210/#228。

### T7.5 — 经济奖励撤梯子 S2-S4 🔄（**S2 已部署生效 09-19 凌晨**；S3 等 S2 无退化 1 窗）

| 步 | 触发 | RECRUIT/次 | BUILD_2/次 | 兵力系数 | 建设奖 |
|----|------|-----------|-----------|---------|--------|
| S1 | ~~当前生效~~ | +0.5 | +0.75 | 0.02 | 无 |
| **S2 🔄** | **09-19 部署在线** | **+0.25** | **+0.375** | **0.03** | **低档巢 +3** |
| S3 | S2 无退化 1 窗 | ≈0.125 | ≈0 | 0.04 | 低+5/高+10/防+5 |
| S4 | T05 换图节点 | 0 | 0 | 0.05 定稿 | garrison + R6 大调同批 |

- S2 改动（commit `ab93213`，ep_runner 四处 + DEBUG_FORCE 调试件退出）：RECRUIT L1496 / BUILD_2 L1515 / 兵力系数 L1469 / 低档巢 +3 新增（BUILD_NEW 点，mask 低两位=T1/T2 巢穴）。**生效验证**：[ECON] recruit +0.25 / build2 +0.375 已见主日志。
- 每步判据：avg_r 跌 <20% + 招兵/建设频率不塌 + 取兵窗维持（攒 40 局后看板复查）。顺序：~~WIN-1 达标~~（✅）→ S2（✅ 在线）→ S3 → T05 换图节点 → S4。
- 与 BUILD P1 错窗：S2 窗收口后 BUILD P1 入池切换（A11 备料就绪）。
- D3 熵 bonus（entc=0.05×(1+exp(−step/200万))，09-15 部署）+ C2 L0 崩溃插桩均在线，细节见已完成任务 09-15/09-16 条。
- **🛑 S2 收口 FAIL（09-19，#283 困死定性后）**：新基线 40 局（实跑 52 局）判据 `[RECRUITED]/局` 13.2→0.6 塌 95%，与 62% all-blocked 困死同根因（英雄被困→招兵断流）；建设轴健康（build2 持平/首建每局触发）。**判 FAIL 不触发 S3**（错窗纪律：S2 未通过不得叠 S3 难度轴），不触发 blue 降级（#283 拍板）。**正确动作**：维持训练攒局让模型适应新环境（学会突围、恢复招兵能力），`[RECRUITED]/局` 回升到 ≥5 后再做 S2 正式收口复查。

### OBS — 长期观察集（不占窗口，复发才行动）

- **OBS-1 R7 battle query 卡顶**：埋点五件套+300s fuse 在位，复发按 map×secs/steps 聚合定位。
- **OBS-2 BHERO_KILL 空拍**：双帧确认+空拍跳过防护生效中（WIN-1 批次A同源护栏）。
- **OBS-3 英雄战斗未触发（旧登记"aggression 遗留"方向已证伪，09-17 改述；09-18 闸门已部署待验证）**：✅ 09-17 预研定谳（`docs/预研_OBS3英雄战斗触发链与BUILD链_20260917.md`）——引擎 `moveHero → blockingVisit → onHeroVisit → startBattle` 链**无条件触发战斗，无 aggression 开关**；真闸门 = **观测 passable 通道**：旧 `isClear` 口径把敌英雄格/城格/怪格全标 passable=0 → 模型动作掩码 -inf / MOVE_TO 排除 / 蓝 AAI 跳过 HERO 三方结构性不可达（**09-18 方案甲已修**：strategic_state.cpp 两处口径改 `!(blocked() && !visitable())` 对齐 GHandler，重编+重启训练，验证 4 判据见 WIN-1 段；回退 = .so 备份恢复）。A2 攻击步旁路代码已实现（默认关，`--blue_hero_attack_bypass=1` + `--attack_f_min`，#254 已修映射；旁路冒烟 T7.6 与闸门窗可合并执行）。OBS-3 附带产出：BUILD 链断点 = dungeon fort 前置 + 每日一建 + 观测盲区（P0-P3 备料落点见预研 §2.2）。
- **OBS-4 TOWNSTALL 引导可达性**：平台段误判已修（#209），继续观察。
- ~~OBS-5~~ 已定性关闭（09-15，开局全堵 zombie 自然终局），归档见已完成任务 09-17 条。

---

## 三、并行主线：T6 NK2 逻辑融合

| 子任务 | 状态 |
|--------|------|
| T6.1 NK2 估值→奖励塑形 | ✅ 冻结，v5 实跑中 |
| T6.2 C++ BFS 全图寻路 | ✅ 完成；NK2 寻路加速 #7632 已 merge（09-11） |
| T6.3 决策结构分层 | ⬜ 远期（与 T5.9 合并） |
| T6.4 训练地图体系 | 🔄 MAPS=10 大图池（总任务五）；WIN-1 批次A 同窗混跑 |

---

## 四、训练期可做工作（不碰运行中 v5 进程）

> 影响判定红线：WSL rel/ 运行时 .so、训练进程/日志写、GPU、vcmi-native 工作源码、eval 抢 CPU。
> 当前处于 WIN-1 批次A 激励窗，**任何 ep_runner/训练脚本改动只备料不部署**（等自然停启窗，一次一轴）。

**A. 零干扰立即可做（Windows / WSL 只读 / 只写方案）**：

| # | 任务 | 状态与产出 |
|---|------|-----------|
| A1 ✅ | **批次A 窗口跟踪到 40 局聚合** | 完成（09-17 收口，44 局三判据全绿）；`py/win1_batchA_agg.py` 修复窗口定位缺陷（enumerate 全文迭代 + 污染段语义），`py/win1_guardrail_probe.py` 护栏计数 |
| A2 ✅ | **"贴近未接战"只读归因（最高价值）** | **定谳：P-H2 同格判据 `_d1==0` 结构性不可达**（#143 事实3 实锤：敌英雄格 moveHero 被拒，非"走上即触发"）；批次A contact=[] 是参数未开的预期；批次B 前必修判据（`_d1<=1` 兜底或 battleStarted 真事件）|
| A3 ✅ | **own_town 评分霸屏衰减方案（只写文档）** | `docs/方案_own_town重复访问衰减_20260917.md`：89.5=35+55−0.5×1 硬推导；方案=0.5^n 访问衰减+空撞拉黑，`--own_town_decay` 默认 0 关闭 |
| A4 ✅ | **T05 act_loop 惩罚窗口错配备料** | **09-17 深查证伪归档**（`docs/备料_T05引导期卡死熔断_20260917.md` + `py/a4_stall_analyze.py`）：act 长同向段=MOVE_TO 合法展开（L906-957 a=24 被改写为方向），引导窗停滞 0/54 局、guide_abort 捕获面 0%；真凶=自由期 own_town 回城循环（38-59 段 pick 100% own_town 176 次，visit 节律 ~33 步/次）→ **根治转 A3，A4 不部署** |
| A5 ✅ | **批次B 开窗 checklist 预置核查** | 全绿：env 映射/默认 0/注入过滤/幂等/duel 排除/开窗路径（restart 脚本 L10 预留行）全就绪；唯一阻塞=P-H2 判据待修（A2） |
| A6 ✅ | **文档沉淀** | 完成：知识库 09-17 章（批次A 结算/双定谳/批次B+A3 配置）+ 踩坑 #250（P-H2 同格不可达）/#251（MOVE_TO 展开误判卡死）/#252（开窗子进程验证）已固化；~~遗留观察：[ECON] first BUILD_2 零条~~ → **09-18 深夜 A9 定谳关闭**（纯观测假象，见 A9 行） |
| A7 ✅ | **VCMI 上游侦察 + A2 capture-all-mines 备料** | 09-17 完成：本地基线 vcmi-native HEAD=`65515ef24`（09-16）/ libvcmi.so=VCMI 1.8.0 develop；上游 09-11~09-16 新增 ~10 条提交分档；**A1（#7837 manaOverflowFactor）已剔除**（zip-of-JSON 扫 62 张训练地图，Pandora/事件蓝=0，manaReward 8 次全是 hero_0.options.mana=10 初始属性）；**A2（#7810 capture all mines）备料完成**：`py/probe_victory.py` 探查 T05/T06 三图 `victoryConditions` 字段结构，确认 .vmap 文本可直接写 `["controlCurrent", {"objectType": "mine"}]` 替换 `specialVictory.condition`（攒 100 金），`victoryIconIndex` 2→9，`victoryMessage` 改 `core.vcdesc.10`；属地图轴，与 WIN-1 激励轴错窗，WIN-1 达标后择窗实施；知识库 09-17 章 + 踩坑 #255 已固化 |
| A8 ✅ | **BHERO_KILL 局尾补记账（判据③收口件）** | 09-19 凌晨已部署并**实战命中**（修正版口径：攻击步已下发 + id 从末帧 obs 消失 → 补记账+补发阶梯奖，修最后一帧 traj["rew"]；初版依赖挂账被 HEROSEG_EMPTY 瞬态绕过已废弃）；[BHERO_SLAIN] (end-of-ep attack-step credit) 实测 slain=[3] +40 |
| A9 ✅ | **A6 BUILD_2 零触发定谳 + highlights 白名单修复** | 09-18 深夜：`py/build2_probe.py` 统计全历史 6166 局，act20 出现 4864 局（79%）22919 次 → **BUILD_2 +15 一直在发，"零触发"纯观测假象**（[ECON] 不在 highlights 白名单）；已补 [ECON]/[BHERO_ATTACK]/[BHERO_KILL]/[ATK_DBG] 四标签进白名单（主进程改动，重启窗部署） |
| A10 ✅ | **观测基建三件套** | 09-18 深夜：贴脸局快照器 `py/contact_log_snapshot.py`（环形 5 槽×240s + contact 触发，重启 `py/restart_snapshotter.sh`）+ keepalive（~~守护计划任务~~ → #267 拍板改**每次手工拉起**，脚本文件保留备用）+ passable 4 判据看板 `py/passable_gate_watch.py` |
| A11 ✅ | **BUILD P1 备料（经济轴）** | 09-19 凌晨：探针定谳 T04/T05/T06 城 JSON 结构同源（均无 buildings 字段，fort 缺失在运行时层）；源码依据 `CGTownInstance::serializeJsonOptions`（L1107 buildingsLIC：`options.buildings.all` 数组 → addBuilding，dungeon jsonKey `core:fort`/`core:dwellingLvl1`，L216 dwellingLvl1 requires fort）；`py/build_p1_fort_town.py` 已生成 5 张非 duel 图 `_p1.vmap` 副本（红城 x1 预置 fort+dwellingLvl1）；**入池流程**：窗内改名覆盖原文件 → `sync_maps_to_runtime.py --strict` → 首局验 [ECON] first BUILD_2（JSON 错则图加载失败即回退删 _p1）；运行时加载验证留入池窗首局 |
| A12 ✅ | **S2 自动收口监控** | 09-19 凌晨：`py/s2_auto_review.py`（Windows 后台）每 5 分钟查局数，**≥40 局自动复查**（4 判据 + S2 三判据，剔 duel 底噪口径），报告写 `s2_review_report.txt`；FAIL 则 80 局二次复查。窗对比 `py/s2_gate_check.py` |

**B. 等条件触发（现在不做）**：
- WIN-1 批次A/B 达标 → T7.5 S2 → WIN-3 ① 或 WIN-2 回池（错窗三选一）。
- `py/eval_promo.py` 全 profile：**窗内不跑**（grad 扰动 avg_r 基线 + CPU 竞争），激励窗结束后训练低峰、独立进程组跑。
- C2 L1 gdb 取证（`py/c2_crash_gdb.sh`）：仅崩溃复发触发，当前 crashlog/ 空、零 SIGSEGV。

**C. 需停训/自然重启窗（排队等窗）**：
- ~~批次B 开窗包 + A3~~ **已部署**（09-17 同窗，见 WIN-1 段）；撤梯子 move_to_force 覆盖块改动——统一进自然停启批次，遵守一次一轴。
- C3 CGameHandler garrison 自动合并：触碰铁律，断言不复发不做。

**D. 需用户在场 / 跨仓确认**：
- D4 2v2/多人 GUI 验证（fork headless 部分不影响，GUI 需在场）。
- vcmi-native 仓 working tree：#215 green client headless 13 处 `if(ENGINE)` 守卫已改已验，**待正式 commit**（跨仓操作，待确认）；8B optional 序列化 1B vs 4B 字节不匹配疑点保留，待 197 fishy 实锤。

---

## 五、风险登记 R（引擎侧）

| 编号 | 风险 | 状态 / 对策 |
|------|------|------------|
| R1 | 动作拒绝不可感知 | 🟡 Python action mask 在用，未审计 |
| R3 | 异步时序竞态 | 🟢 约束型（新动作走 waitTillRealize） |
| R4 | queryID=-1 断言雷 | ✅ 已修（libMMAI.so 13c0f041） |
| R5 | 双源码树不同步 | 🟡 `vcmi-native-build` 是 08-02 陈旧树（#228 实锤）；源码改动先在 vcmi-native grep；Windows 树只摘 fix |
| R6 | 战斗 AI fallback StupidAI | ✅ onnxruntime 1.19.2 + V13::BAI（09-03），长期观察 |
| R7 | battle query 卡顶 | 🟡 见 OBS-1 |
| R8 | 训练进程存活 | 🟢 system 级 enabled unit + Windows keepalive（#114/#201）；判活看 PID etime+日志 mtime；改码前 `wsl -u root systemctl stop homm3-train-v5`（禁 --user） |
| R9 | KL 失控 | 🟢 生效窗（L102375 起）2/2 更新点 <1.0，无复发；1.096 尖峰属旧代码污染段（09-17 收口定性）。再现/持续 >1.0 → 评估回退梯度注入 |

---

## 六、T13 外挂 AI — T13.10 多人对战验证 ✅（全闭环）

- T13.1-T13.9 协议栈主体完成（离线 167/167 PASS）；T13.10：P8-B 握手轮转 / P8-C 决策接入+真实 qid=2 样本 / P8-D 单机双实例 13/13 / P8-E 人机混局 / 30min 浸泡，全部 PASS。代码 `py/vcmi_pro/`、`py/p8/`，明细见已完成任务 09-11~09-16 条。
- **遗留两点**：① vcmi-native 仓 #215 headless 守卫修复待 commit（见四-D）；② 8B absent optional 字节级不匹配疑点（C++ 写 4B/Python 写 1B，run10 实证 server 受理），待 197 fishy 实锤再验 9B present。
- 真机 `--remote` 与长对局浸泡均已闭环/废弃，无剩余待办。目标场景（2 人类+1 外挂 AI，1v7 大厅）底座就绪。

---

## 七、远期 / 阻塞集

- ⬜ T5.9 v2 MultiDiscrete 三维参数化（与 T6.3 合并）。
- ⬜ MMAI v15（PR #7654）未合并官方、2-4x 慢，合并后需重采集（绑 T8）。
- ⬜ **A2 capture-all-mines 胜利条件（PR #7810 merged 09-12）**：`.vmap` 文本可直接写 `["controlCurrent", {"objectType": "mine"}]` 替换当前 `specialVictory.condition`（攒 100 金），`victoryIconIndex` 2→9，`victoryMessage` 改 `core.vcdesc.10`；属**地图轴**，与 WIN-1 激励轴错窗，WIN-1 达标后择窗实施；Phase II 经济课程新地图模板候选。备料 `py/probe_victory.py`（探查 T05_36X36_01 / T06_72X72_02 / T05_52X52_01_mir 三图 `victoryConditions` 字段结构）+ 知识库 09-17 章。
- ⬜ 战斗模型训练：当前不需要（R6 现成 onnx）；触发 = T05 对抗接战胜率成战略瓶颈；勿在课程中途升级。
- ⬜ **D 方案：引擎层 startBattle 直调（技术储备，09-19 用户拍板登记"以后要用"）**：Python 不走 moveHero 直接发起英雄战，根治 d=0 同格/敌格 moveHero 结构性拒绝（areNeighbours）。触发条件 = B 方案（BFS 绕行）实测失效或 T05 对抗接战成瓶颈。调研结论（onHeroVisit→startBattle 全链符号+行号 / connector .so 重编非 libvcmi.so / OBS 冻结约束下走旁路通道）见知识库 09-19「startBattle 引擎层直调」章。
- ⬜ 动作码 16-23 内政全面启用：Level 3 切入时先解决 noTarget 惩罚。
- ⬜ P6 对手池 self-play/NK2 混合：NK2 三树源码就绪，回用时直接重编即生效。
- ⬜ P10-B h3m2vmap C/D/E：转换器 B1-B4 已收官，产出图入训练池的课程设计（WIN 窗外）。
- ✅ 引擎 standardDefeat 判负/败北信号（#228）；fogOfWarMap = explored 累积语义（09-15 源码核查，见 `docs/源码分析地图.md`）。

---

## 八、关键拍板记录（影响后续节奏）

- **错窗纪律**：激励轴（capture/T7.4/击杀梯度）/ 地图轴（36X36_02 回池/新图）/ 难度轴（大图 duel）不同窗，一次一轴。
- **关机标准流程**：后台 unit；优雅停零损失；改码前 `wsl -u root systemctl stop homm3-train-v5`（system 级，无 --user）。
- **(09-15)** WIN-1 ⑤：撤销改图（72_02 全草地无墙）；⑤口径只数 C 类退化，B 类截断不阻塞聚合；策略空转方向转 P10 target 加权（已上线）。
- **(09-16 晚)** 击杀链归因定谳（真实击杀=0，proxy 无 bug）→ 击杀激励重设计批次A 开窗；与 S2/WIN-3 互斥。
- **(09-16)** C1 B4 晋级判据人工拍板实质达成（3 次 eval avg 133-153 + 四指标全过）；属 T7.1 历史判据复核，**不触发** S2/WIN-2/WIN-3。
- **(09-18)** 开源立项拍板（用户发起）：战略层 RL 官方/fork 全空白（MMAI PR #4788 原话 "adventure AI... nothing particular is planned"）→ **独立项目为主，官方仓只提干净小 PR**。3 仓结构（strategic-gym MIT / engine-plugin GPL-2.0+ / models-release+mod）。**官方小 PR 清单 09-18 复核后收窄 3→1**：~~NKAI race `a1ea3f4d2d`~~ 官方已含（Ivan Savenko）/ ~~JSON seek `01f741713c`~~ 官方已含（kdmcser）/ **#205 服务端 pack try/catch（唯一可提，我方原创，upstream `CVCMIServer::onPacketReceived` 仍裸调 `retrievePack`）**。触发条件（任一→开"开源周"）：① passable 闸门 4 判据达标 ② 首个可展示 milestone（真实击杀实锤/1v7 录屏）③ 下次大改图自然停训窗；当前只登记不执行（WIN-1 错窗纪律）。备料已动：`vcmi_gym` 仓构建产物清出（.gitignore/README/LICENSE 已立；认证踩坑 #258，账号 c18593395）。**✅ 09-18 已首推 + CI 绿 + 已改名**：GitHub 仓 `c18593395/vcmi-strategic-gym`（建时名 `cmi-strategic-gym` 少个 v，用户 Settings 已改名，本地 `git remote set-url origin` 已同步），SSH push 完成（74 文件含 LICENSE/CI/CONTRIBUTING，HEAD `c0cb8ed`）。CI 首跑（run #35362844432）**Success**：compileall 语法闸 + 英文 CONTRIBUTING（构建命令已核对实际 preset `vcmigym-rel`）。**✅ 敏感内容扫描全绿**（4 commit 全历史 + 当前树，API key/权重/map 零命中；唯二打磨项=5 文件硬编码 `/home/administrator` WSL 路径 + 历史混入 3 个 `.bak/.so` 二进制，清洗方案已定留开源周）。**#205 PR 已提取未推**：基于 `upstream/develop` 在 worktree `vcmi_pr205` 建分支 `pr205-server-pack-guard`（21 行 diff，剔本地 commit 里的诊断脚手架，符号实锤完成），推 PR 需浏览器手建 fork 或用户出 PAT（gh API 认证断 #258）。开源周触发时在此基础上迭代（路径清洗/英文 quickstart/模型 release/CI 加真测试）。详见知识库 09-18「上游侦察+开源立项」章 + 踩坑 #258/#259。
