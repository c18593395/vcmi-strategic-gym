# 当前任务清单（当下窗口要做什么）

> 父文档：[总任务.md](总任务.md)（大项 T1-T13 整体脉络）。本文件只写**当前有效**的任务：窗口焦点 `WIN-x`、观察期 T 任务、待触发/备料工作台（A/B/C/D/P/R）、风险与远期。
> **脱钩原则**：技术细节不在本文件展开，一律指针到 `WSL知识库.md` / `WSL踩坑点.md` / `已完成任务.md`。完成即归档移出，保持短小。
> 最近整理：2026-09-14（H3 #228 闭环后全量重组，历史日期增量统一归档至 `已完成任务.md`）。

---

## 一、当前窗口焦点 WIN-x

### WIN-1 — capture +100 激励观察窗 🔄（归属 T7，09-08 上线）

- **目标**：1v3 完整胜利路径（消灭蓝英雄 + 占蓝城）此前无局达成 → capture +100 激励（仅大图，T04/T05 零扰动）+ 蓝城引导修复链。
- **现状（09-14，干净 10 图池 36 有效局）**：零 err=yes / 零 SIGSEGV；`[TOWN_CAPTURE]=4` 全在 1v3 图且均为三蓝英雄全灭 `blue_hero_killed=[1,2,3]`（108_02 step56、72_02 step39/128、72_01 step92）；duel 系 0 次符合预期（#213 误报已排除）。新 1v3 大图 capture proxy 链路验证生效，继续攒局聚合。
- **判据（攒到 ~40 局再聚合）**：① T06 TOWN_CAPTURE 触发非零（核心，已达成维持）；② 守卫胜闭环 ≥80%；③ avg_r 跌幅 <20%；④ 自发经济 ≥80% 局不塌；⑤ 200 步局 ≤20%。
- **⑤ 专项结论（09-15，py/analyze_trunc200.py，全历史 1124 有效局/1v3 133 局）**：截断局 **100% 为 B 类（守卫胜+占矿后未 capture）**，无 A 胜后空转、无 C 退化空转（零 ZOMBIE/ENDTURN_FUSE、END_TURN=0%）→ ⑤不是策略退化信号。结构卡点 = **72_02 1v3：7/12=58%**，成功/失败局前 51 步事件完全同构（同矿 step43/同守卫 (23,20) step51/同 TOWNSTALL `hero=(39,6) tgt=(69,2) plen=30 block=(40,5) pas=全1`），分叉在 stall 后：成功绕行遭遇三蓝英雄（127 步 r≈275），失败一路向东撞墙 90 步到 200（r≈92，跨 6k 步训练 7 局指纹零改善）。72_01 截断 21%（r截 188 温和版）、108_02 1v3 2/4 样本不足。
- **⑤ 拍板建议**：**提步数（250）对 72_02 无效**（step108 起已撞墙，多给 50 步=多撞 50 步）；有效干预按序：① 改图（蓝城 (69,2) 挪离东缘/清 BFS 误判堵点 (40,5)/蓝英雄入 MOVE_TO 目标池，与 WIN-3②"挪蓝镇"同方向，**72_02 优先于 108**）；② 判据口径修订（B 类高分截断 r≥150 不算病态；或⑤只数 C 类，当前为 0）。在改图/修订前，⑤结构性不可达 ≤20%，不应阻塞 WIN-1 聚合（①②③④已达标）。
- **撤梯子登记**：capture 触发率稳定后 `move_to_force` 由全程引导回常规窗。⚠ 09-15 事实修正：ep_runner_one.py L134-136 对 **T06 无条件覆盖 move_to_force=200/guard_done_steps=0**（命令行传 60/15 无效，T04/T05 才用传入值）；撤梯子需改 ep_runner 该覆盖块（改代码→重启窗）。
- **下一步**：五判据聚合达标后，按错窗纪律走 72X72_02_duel 地图轴（见 WIN-3）。
- **指针**：C 方案/duel proxy 细节 #195、#213；09-14 36 局明细知识库「生产回池首批验证」；g3b 终局调研结论已被 H3 #228 吸收。

### WIN-2 — 36X36_02 原样回池评估 ⬜（归属 T6.4，待触发，与图池/激励轴错窗）

- **第一步**：02 图原样回池，用新口径（visitable 修复后）重评估，判据 = 与 01 局均 r 差异 <20%。
- **第二步备料**：差异化重设计（图 03 = 01 骨架 + gold mine 加 swordsman×6 守门 = 矿-战斗耦合课程）。
- **历史归因**：51 局全负数据采集于判据失真时代（假糖）；地形压制假设已排除（纯草地同构），真实差异 = 守卫减半 + red 半场近资源移远。
- **指针**：方案文档 `docs/方案_T05_36X36_02重评估_20260910.md`。

### WIN-3 — 下一难度轴候选 ⬜（归属 T6.4，一次一轴，WIN-1 达标后择一）

- **① 72X72_02_duel**：即插即用，已在 MAPS（02 系缺 terrain_0.json 系误报、蓝方贴镇 6 格实为双方对称，#212 已证伪；reset 冷启动脏样本已由 obs_nz==0 过滤拦截，#214）。
- **② 108X108 duel 可完成度**：对角 ~145 格 vs 200 步上限偏临界，先观察；建议提 250 步或挪蓝镇到 (98,98)。**09-15 专项更新**：更急的是 **72_02 1v3（58% 截断，见 WIN-1 ⑤专项）**；250 步方案只适用于 108 临界（差几步），对 72_02 撞墙型无效，72_02 走挪蓝城/修堵点改图路线。
- **③ 1v3 击杀/占城数据自然积累**：BHERO_KILL + TOWN_CAPTURE 埋点在位。
- **纪律**：难度轴与激励轴 / 图池变更不同窗。

---

## 二、观察期任务

### T7.4 — 死亡惩罚 🔄（已上线观察，09-11）

- **实装**：`death_penalty=-50` 试探档（上限 -100，传 0 可关），红英雄 8 方向全堵 ×2（zombie）→ -50；蓝英雄死走 capture proxy +100，不同帧无双罚；全局生效不按图分支。
- **三判据现状**：
  - 判据 1（死亡局 r 转负）：⏸️ 挂起并已定性——全草地大图英雄不会 8 向全堵，ZOMBIE **结构性不可达**（T03/T04 小图历史 441 局有、T05/T06 大图 0 局；T04 回池验证同日否决）。
  - 判据 2（[GUARD] 接战率 ±20% 不塌）：✅ 通过。
  - 判据 3（avg_r 跌幅 <20%，基线 2.34）：✅ 通过（无恶化，零 err=yes）。
- **回退线**：接战率塌 >30% 或 avg_r 跌 >20% → 降档 -25 或 0 关闭。
- **关联根因已闭环**：判据 1 的另一半——真败北（无将无城）后败北信号断链——已由 **09-14 H3 修复闭环（#228）**：红败秒级 game_over=2 / r=-200，不再 300s 超时丢信号；生产首批自然 PvP 红败/红胜局继续异步观察。
- **指针**：#204（0 命中三义性）、#210（HERO_DEATH 0 根因）、#228（败北信号根修）、知识库「T7.4 HERO_DEATH 0 死亡根因」「H3 双编译树判定」章。

### T7.5 — 经济奖励撤梯子 S2-S4 ⬜（节奏需重估）

- **背景**：S2 曾因自发经济塌方暂停（等 R6 战斗修复）→ R6 已修（09-03）+ 自发经济回归（09-04，0→54 次/40ep）→ S2-S4 推进节奏待重新拍板。硬前置 towns.garrison obs 已填充（09-02，不违 3464 冻结）。
- **每步判据**：avg_r 跌幅 <20% + 招兵/建设频率不塌 + 取兵窗维持。
- **多步渐进方案（08-31 定稿，递归半价 + 效果奖渐入，每步 1-2 窗）**：

| 步 | 触发 | RECRUIT 每次 | BUILD_2 每次 | 兵力系数 | 建设奖（新巢穴） |
|----|------|------|------|------|------|
| 当前 | — | +1 | +1.5 | 0.01 | 无 |
| S2 | 节奏重估后 | +0.25 | +0.375 | 0.03 | 低档巢 +3 |
| S3 | S2 无退化 1 窗 | +0.125(≈0) | ≈0 | 0.04 | 低 +5 / 高 +10 / 防 +5 |
| S4 | T05 换图节点 | 0 | 0 | 0.05 定稿 | garrison ✅ + R6 大调同批 |

> 要点：糖每窗 ×0.5 与效果信号反向渐变；建设奖低值试探防死刷；埋点 [BUILD_NEW]/[RECRUITED] 先于奖励；与 WIN-1/T7.4/地图轴错窗。

### OBS — 长期观察集（不占窗口，复发才行动）

- **OBS-1 R7 battle query 卡顶**：埋点五件套在位，fuse 300s 兜底（局不死仅慢）；`[EP_TIME]` 打点已上线，复发按 map×secs/steps 聚合一击定位。
- **OBS-2 BHERO_KILL 空拍**：防护生效（0 新误报）；`[HEROSEG_EMPTY]` 埋点待复发捕获（全槽 -1=竞态 / 全槽 0=共享内存未填充）。
- **OBS-3 T06 守卫战斗未触发**（aggression 遗留）：与 fix_t06_maps.py 相关，待 capture 打通后专项。
- **OBS-4 TOWNSTALL 引导可达性**：平台段误判已修（#209，`>=` 改 `>`，重启窗 66 局 TOWN_BLOCKED=0）；观察是否继续收敛。
- **OBS-5 108_02_duel 无标记短终局**：09-14 36 局中 2 局（34/41 步，r16/20，8-9.4s/步偏慢），无任何终局标记、err=no，攒样本定性（疑长战斗/卡顿后自然终局，非脏局，与死亡惩罚无关）。

---

## 三、并行主线：T6 NK2 逻辑融合（训练验证中）

| 子任务 | 状态 |
|--------|------|
| T6.1 NK2 估值 → 奖励塑形 | ✅ 冻结（08-22），v5 实跑中 |
| T6.2 C++ BFS 全图寻路 | ✅ 代码完成 + T03 段毕业（08-29）；NK2 寻路加速 #7632 已 merge（09-11） |
| T6.3 决策结构分层 | ⬜ 远期（与 T5.9 合并） |
| T6.4 训练地图体系 | 🔄 T06 1v3 已结案（09-08）；当前 MAPS=10 大图池（清单见总任务五）；capture 窗 + T7.4 观察同窗混跑 |

---

## 四、待办工作台 A/B/C/D（标注是否影响训练）

> 影响判定：是否触碰 WSL rel/ 运行时 .so、训练进程/日志、GPU/显存、vcmi-native 工作源码。

**A. 立即可做（零影响：Windows / 纯只读）**：A1-A4 已全部完成（08-29），当前无待办，新增零影响任务追加于此。

**B. 设计文档类（零影响：只写方案）**：B1-B4 方案全部出稿（08-29），当前无待办。

**C. 谨慎（需隔离设计，有资源/文件重叠）**：

| 编号 | 任务 | 归属 | 状态 / 约束 |
|------|------|------|------------|
| C1 | 晋级判据自动化 eval | T7.1 | 🟡 工具就绪（09-14）：`py/eval_promo.py` 重写为 MAPS=10 profile + 生产参数对齐 + 1v3 课程判据面板 + 2h 低频护栏 + 独立进程组；CPU-only 但 vcmiserver 仍吃 CPU，全 profile 待训练低峰跑 |
| C2 | 重编崩溃根因插桩 | T6.2 | 🟡 方案就绪（09-15，只分析零部署）：`docs/方案_C2_崩溃插桩_20260915.md`（历史台账+四级缺口+L0/L1/L2 插桩+复发 SOP）；当前零崩溃不触发，L0 建议随 D3 同窗口 |
| C3 | CGameHandler garrison 自动合并 | T8.1 | ⬜ 触碰铁律；除非断言复发否则搁置 |
| C4 | NK2 寻路加速 merge（+40% 吞吐） | T6.2 | ✅ 完成（09-11，首窗观察本负载中性无害，保留新栈；见已完成任务 C4 条） |

**D. 等待条件触发**：

| 编号 | 任务 | 归属 | 状态 |
|------|------|------|------|
| D1 | 经济动作实际上线 | T7.3 | ✅ 已发生（capture +100 于 09-08，ep_runner 层零停训） |
| D2 | 30x30/36x36 实际解封 | T7 | ❌ 已被 T05 全面化覆盖（09-04），P3 解封参数过时 |
| D3 | 开局熵 bonus 实装 | T3 | 🟡 备料完成（09-15）：补丁 `py/d3_entropy_bonus.patch`（dry-run 5 hunk 全过+py_compile 过），等自然重启窗应用（= P5） |
| D4 | 2v2/多人对局验证 | T10 | 🟡 fork headless 不影响；GUI 验证需用户在场 |

**建议顺序**：WIN-1 攒局聚合 → 拍板 WIN-2（36X36_02 回池）→ WIN-3 地图轴；C1 视训练稳定度择机。

---

## 五、训练期并行任务 P（不碰运行中 v5 进程）

| 编号 | 任务 | 类型 | 状态 / 依赖 |
|------|------|------|------------|
| P1 | 训练空转根因（断言雷）修复 | 解锁型 | ✅ 08-29 |
| P2 | 晋级判据自动化 eval（py/eval_promo.py，CPU-only） | 备料型 | ✅ 08-29 |
| P3 | 30x30/36x36 守卫距离解封 | 解锁型 | ❌ 过时可废弃（课程图体系已替代，复用需先适配 visitable 口径） |
| P4 | ZOMBIE/GUARD all-blocked 聚合分析（py/analyze_deadlock.py） | 备料型 | ✅ 08-29 |
| P5 | 开局确定性对策（entropy 调度） | 备料型 | 🟡 备料完成（09-15）= D3：ent_coef×(1+exp(-step/200万))，补丁就绪等窗口 |
| P6 | 对手池 self-play/NK2 混合 | 备料型 | ⏸ 远期；NK2 三树源码就绪，回用时直接重编即生效 |
| P7 | 真实游戏战略层联调（fork 1.8 DLL） | 铺路型 | ✅ 大幅推进至闭环：ModelAI 加载链 + battle-only 战斗链 + GUI 稳定性 + mq v5 全模型 1v7 PASS（见已完成任务 09-10/09-11 条与知识库 mq 章） |
| P8 | 人机对战底座（大厅 1v7 配置） | 铺路型 | 🟡 = T13.10，见第七节 |
| P9 | done 信号接线 | 备料型 | ✅ 误报排除（已存在无需开发） |
| P10 | H3M 官方图 → VMAP 转换（h3m2vmap） | 备料型 | ✅ A-E 全通，King 已入池（见已完成任务 09-01/09-13 条） |
| P11 | VMAP → H3M 反向转换 + roundtrip | 备料型 | ✅ roundtrip PASS（09-13，见已完成任务 P11 条） |

> P10 遗留课题：target_list 排序 = 纯曼哈顿 top-8 无价值/威胁加权，大图最近 8 目标易被强守卫堵死（与 09-09 mine_taken 死信同源），排序优化与远期「target_list 加权」合并立项。**09-15 方案已出**：`docs/方案_P10_target加权排序_20260915.md`（Python 旁路打分器，零重编）。

---

## 六、风险登记 R（引擎侧）

| 编号 | 风险 | 状态 / 对策 |
|------|------|------------|
| R1 | 动作拒绝不可感知 | 🟡 已缓解未审计（Python action mask 在用） |
| R2 | done 信号轮询链 | ✅ 误报排除 |
| R3 | 异步时序竞态 | 🟢 约束型（新增动作走 waitTillRealize） |
| R4 | queryID=-1 断言雷 | ✅ 已修（libMMAI.so 13c0f041） |
| R5 | 双源码树不同步 | 🟡 长期隐患——09-14 已实锤 `vcmi-native-build` 是 08-02 陈旧树（#228）；源码改动前先在生产树 vcmi-native grep；Windows 树另有引擎 API 分叉不可镜像同步，只摘取 fix |
| R6 | 战斗 AI 恒 fallback StupidAI | ✅ 已修复（09-03，USING_ONNX=0 硬编码 → onnxruntime 1.19.2 部署重编，V13::BAI 接管）；长期观察 avg_r/胜率 |
| R7 | battle query 卡顶 | 🟡 长期观察（见 OBS-1） |
| R8 | 训练进程存活 | 🟢 流程已固化——system 级 enabled unit + Windows keepalive（#114/#201）；一律后台跑，关机前优雅 stop（零损失），判活看 PID etime + 日志 mtime 勿只信 is-active |

---

## 七、T13 外挂 AI 专区 — T13.10 多人对战验证 ✅（实机闭环 + 备料已补 09-14）

> T13.1-T13.9 协议栈主体已全部完成（离线 167/167 PASS），详细归档见 `已完成任务.md` 09-11/09-12 T13 条目与知识库「VCMI 网络协议与外挂 AI 架构」章。代码 `py/vcmi_protocol/`。本节仅跟踪 T13.10 实机闭环。

| 阶段 | 内容 | 状态 |
|------|------|------|
| P8-B-1 | lobby join 握手（Python 直连 VCMI_server，LobbyClientConnected 与真 client BYTE-IDENTICAL） | ✅ 09-11 |
| P8-B-2 | 完整对局 EndTurn 轮转（host 让位 ChangeHost → SetMap → StartGame → Turn2 轮转 zero fishy） | ✅ 09-11 |
| P8-C | 决策接入：MoveHero 真实执行 + QueryReply + Build→TOWNAVAIL→Recruit 城镇链 + 协议栈四层缺陷修复 + 双探针 | ✅ 09-11~09-12（#205/#206/#207/#208） |
| P8-D | 跨机器部署（HMAC 认证 + 远程连接 + 拓扑管理） | ✅ 双机部署路径已用单机双实例实跑完成：WSL 双实例 9/9 PASS（`py/p8d_dual_node_sim.py`）+ 单机双实例 13/13 PASS（`py/p8d_two_instance.py`：2 真实 VCMI_server 端口隔离 3030/3031 并存 + 2 真实 ModelAI client 跨实例 + HSK 跨实例预交换 + HMAC 正负例 + 双节点状态文件） |

- **剩余工作**：① ~~真机 `--remote` 部署~~ **已废弃**——双机部署路径已用单机双实例实跑完成（09-14 `py/p8d_two_instance.py` 13/13 PASS：2 真实 VCMI_server 端口隔离 3030/3031 并存 + 2 真实 ModelAI client 跨实例 + HSK 跨实例预交换 + HMAC 正负例闭环 + 双节点状态文件，共享 fork build 无需副本），无需第二台真实机器，`--remote <host>` 路径保留但不再作为待办；② QueryReply 真实 qid≠-1 样本——09-14 重跑 `p8c_query_probe_real.py` 确认数据源墙仍成立（PASS qid=-1 only：22 条 PlayerStartsTurn 全 qid=-1，0 条真实 QueryReply，server 健康、回合正常），需有计时器场景才能拿到 qid≠-1 样本；③ 混人长对局浸泡——09-14 新建 `py/p8/p8b_soak_30min.py`（30min 浸泡探针，fork server + 真实 ModelAI client + Python host，每 5min checkpoint，崩溃/断线/回合监控），30min 跑完 PASS（稳定性）：无崩溃 / 0 断线 / 743 包 / 26 回合，⚠ 回合前 ~1min 冻结（战斗终态，同源 P8-C 数据源墙，无 timer 场景 server 不打断当前玩家回合）。
- **目标场景**：2+ 人类客户端 + 1 外挂 AI 客户端同一 VCMI server，1v7 大厅配置，AI 接入任意玩家槽位。

---

## 八、远期 / 阻塞集

- ✅ **引擎侧 standardDefeat 判负/败北信号**：09-14 H3 已闭环（server 战斗结算 + ML .so 终局通道 + Python 配套，#228；红败 go=2/r=-200 秒级传递）。原 T7.4「同帧双罚」复核已确认无需去重（done break 先于 -50）。
- ⬜ **T5.9 v2 MultiDiscrete 三维参数化**（与 T6.3 合并）。
- ⬜ **MMAI v15（PR #7654）**：未合并官方、2-4x 慢，合并后需重采集（绑 T8）。
- ⬜ **战斗模型训练**：当前不需要（R6 启用现成 onnx，零训练成本）。触发条件 = T05 对抗图接战胜率成为战略层瓶颈；路径 v13 战斗自对弈（BC 热启动 → PPO），v15 GNN 绑 T12。勿在课程中途升级（战略环境非平稳）。
- ⬜ **动作码 16-23 内政启用**：Level 3 切入时先解决 noTarget 惩罚。
- ✅ **fogOfWarMap 语义确认**（visible vs explored，09-15 源码核查）：**= explored 累积语义，非当前可见**。英雄移动只增量置 1（`TryMoveHero.fowRevealed` 仅收新视野中 fow==0 格，apply 置 1），正常对局无"离开→清 0"路径；清 0 仅 ViewWorld/ViewAir 法术失效与网络回滚。故 obs[1155:3203] `global_explored` 命名与语义正确；local_tiles 三通道以 explored 为黑雾门控、门内对象/守卫为实时直读（非探索时刻快照）。引擎头注释 "true-visible" 与 ML 注释 "visible/explored" 系误导用词。若将来要"当前帧可见"需另按 sightRadius 实时计算，引擎无现成 per-tick 图。详见 `docs/源码分析地图.md` 后续待确认第 4 条。
- 🟡 **target_list 排序优化**（曼哈顿 top-8 → 价值/威胁加权）：**09-15 纯设计完成零部署**，`docs/方案_P10_target加权排序_20260915.md`。核心=Python 旁路统一打分器（候选全量化+vmap 缓存+蓝英雄动态入池+BFS plen+战力 logistic 可打性+阶段调制），零 obs 变更零重编，`--target_chain {legacy,scorer}` 开关灰度；与 ⑤/72_02 卡点联动（蓝城 stall 后蓝英雄承接，探针局验证）。

---

## 九、关键拍板记录（影响后续节奏）

- **错窗纪律**：激励轴（capture/T7.4）/ 地图轴（36X36_02 回池/新图）/ 难度轴（大图 duel）不同窗，一次只加一个难度轴。
- **(09-08，用户授权)** 下一难度轴四候选选「1v3 完整胜利路径引导」（激励轴）→ capture +100 已实施；地图轴留下一窗。
- **(09-11，用户)** T06 duel capture 死信选 C 方案（蓝英雄死亡 = capture proxy +100，仅当时 duel 图，每局一次）；09-13 因 49 误报改为 duel 排除（#213）。
- **(09-13，用户)** T7.4 判据 1 不走 T04 死路密集轴（实证非 ZOMBIE），判据 1 改靠引擎 standardDefeat 根修 → 09-14 H3 闭环。
- **(09-14，用户/H3)** 败北信号走 H3 方案（改 ML .so + server，不碰 libvcmi），弃用重库 H1 与纯 Python 旁路。
- **关机标准流程**：一律后台 unit；优雅停 = 零损失 vs 被动死 = 回滚（#169），关机/改码前主动 `wsl -u root systemctl stop homm3-train-v5`（system 级 unit，0911 起；无 --user）。
- **(09-15，待用户拍板)** WIN-1 ⑤判据：专项实锤 200 截断 100% 为 B 类、卡点是 72_02 撞墙型（详见 WIN-1 区）。二选一或组合：**改图**（72_02 蓝城 (69,2) 挪位/堵点，另开窗口，优先于 108）或**修订判据**（B 类 r≥150 非病态 / ⑤只数 C 类）；在拍板前⑤结构性不可达 ≤20%，但不阻塞①②③④达标后的 WIN-1 聚合。同窗备料：C2 崩溃插桩方案、P10 target 加权排序方案（均🟡零部署，见 docs/方案_*_20260915.md）；D3 熵 bonus 补丁+C2-L0 可合并到同一自然停启批次。
