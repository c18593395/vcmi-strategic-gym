# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `踩坑记录.md` | 知识库: `知识库.md`
> 每条任务标注对应 `总任务.md` 的 Phase 编号

---

## 双轨状态

```
┌──────────────────────┐   ┌──────────────────────────┐
│  Track 1: 训练 🟢    │   │  Track 2: 真实游戏对齐 ⬜ │
│  Phase C7 全部完成    │   │  Phase E (待启动)        │
│  🏃 训练运行中        │   └──────────────────────────┘
└──────────────────────┘
```

---

## Track 1: 训练 🏃

### 当前状态

| 项目 | 值 |
|------|-----|
| 脚本 | `train_wsl2_ppo_v2.py` |
| Phase | C7 完成, 训练续跑中 |
| step | ~19039 (续训) |
| avg_r | 0.0 (模型塌缩中, #45 待修) |
| maps | 4 张开放 H3M |
| 对手 | MMAI_USER 自对弈 + 对手池 |

---

## 下一阶段

| # | Phase | 任务 | 优先级 | 说明 |
|---|-------|------|--------|------|
| D | **Phase D** | 战斗集成 (moveHero + autofight 深度整合) | ⬜ | 英雄VS英雄、战斗奖励、状态追踪 |
| v15 | **v15** | 架构迁移 (GNN + py::dict) | ⬜ | Phase G 前必须完成 |
| E | **Phase E** | 真实游戏对齐 (Track 2) | ⬜ | ONNX → 内存读取 → PostMessage |
| F | **Phase F** | 你 vs AI (1v1) | ⬜ |
| G | **Phase G** | 1v7 战胜你 | ⬜ |

### 当前阻塞: 模型塌缩 (#45 CGameState yourTurn 时空)

训练目前 avg_r=0.0, 100% act=[10] (END_TURN)。根因是 `strategic_state_update` 读到的 CGameState 在 yourTurn 回调时为空，obs 全零，模型无有效信号。这是 Phase C7 留下的根技术债。

**解法**: 绕过 `igic->gameState()` 直读，改用 `cb->getHeroesInfo()` + `cb->getTile()` 填充观测。

---

## ✅ 已完成 (C7)

| # | 总任务 Phase | 任务 | 说明 |
|---|-------------|------|------|
| N2 | C7 | Connector 适配 | 新 1-param `init_vcmi(void*)`, 三版本适配 |
| N6 | C7 | 训练传参修复 | ML_USER→MMAI_MODEL, blue_model_path 透传 |
| N7 | C7 | ABI 修复 | struct 补 action 字段 |
| N8 | C7 | 调用链修复 | strategic_state_update 死代码, dlsym 调通 |
| N9 | C7 | MMAI fallback action | fallback 路径执行 moveHero |
| N10 | C7 | CMake /DEF: 修复 | MinGW 链接标志修正 |
| B | C7 | VCMI server crash | moveHero→selectionMade 顺序调换 + passability 用 cb |
| N3 | C7 | vmap 适配 | gen_strategic_vmap.py JSON/object 兼容 |
| N11 | C7 | MMAI 重编 + action 验证 | libMMAI.so 部署验证, 源码执行路径确认 |

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `docs/总任务.md` | 全局架构、Phase A~G 进度列表 |
| `docs/当前任务清单.md` | **本文件 — 双轨可执行任务表** |
| `docs/踩坑记录.md` | 踩坑详细记录 |
| `docs/知识库.md` | 单页知识总汇 |
| `train_wsl2_ppo_v2.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器 |
| `vcmi_gym/envs/v13/strategic_env.py` | 战略 Gym 环境 (v13) |
| `vcmi/ML/strategic_state.h/cpp` | C++ 结构体 + passability 逻辑 |
| `vcmi/server/ML/ServerPlugin.cpp` | 战斗钩子 |
| `tools/gen_strategic_vmap.py` | vmap 生成器 |
| `train_loop.sh` | 24/7 训练循环守护 |
