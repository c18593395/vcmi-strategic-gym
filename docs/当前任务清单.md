# 当前任务清单（当下窗口要做什么）

> 父文档：[总任务.md](总任务.md)（大项 T1-T13 整体脉络）。本文件只写**当前有效**的任务：窗口焦点 `WIN-x`、观察期 T 任务、待触发/备料工作台（A/B/C/D/P/R）、风险与远期。
> **脱钩原则**：技术细节不在本文件展开，一律指针到 `WSL知识库.md` / `WSL踩坑点.md` / `已完成任务.md`。完成即归档移出，保持短小。
> 最近整理：2026-09-14（H3 #228 闭环后全量重组，历史日期增量统一归档至 `已完成任务.md`）。

---

## 一、当前窗口焦点 WIN-x

### WIN-1 — capture +100 激励观察窗 🔄（归属 T7，09-08 上线；09-15 关机暂停，重启后继续攒局）

- **目标**：1v3 完整胜利路径（消灭蓝英雄 + 占蓝城）此前无局达成 → capture +100 激励（仅大图，T04/T05 零扰动）+ 蓝城引导修复链。
- **现状（09-15 翻案，已修复重开窗）**：零 err=yes / 零 SIGSEGV 维持；但此前所有 `[TOWN_CAPTURE]`（09-14 36 局 4 次、全历史 218 次）经 battle_quality_events.log 严格配对 **100% 为空拍误报** —— 218/218 均紧跟同图同步 `[HEROSEG_EMPTY]`（8 槽全 id=0 = 战斗/visit 瞬态共享内存未填充帧），现地图池真实 BHERO_KILL=0。根因 = ep_runner_one.py proxy 块在 `_bnow=空集` 时照算 `prev-空=全灭`（#209 只排了 duel 的 49 次，非 duel 169 次裸奔），假 +100 长期污染价值学习。
- **09-15 修复（已部署，695099 停启窗）**：ep_runner_one.py proxy 判定 ① 空拍帧整体跳过（条件加 `_bnow`）；② 双拍确认 —— 差集先挂账 `_kill_pending`，下一**非空**拍仍缺席才发 +100（日志带 `confirmed 2 frames`），空拍冻结挂账、英雄回来撤账。真击杀晚 1 拍发奖、不漏（prev 空拍不更新机制已有）。判据①即日起重新攒窗，预期短期 capture=0（真实全灭从未发生），①重新非零才算真信号。
- **判据（攒到 ~40 局再聚合）**：① T06 TOWN_CAPTURE 触发非零（核心，**修复后重新计**；BHERO_KILL 主日志白名单漏网 + 全历史 9 次全空拍误报 #146，改用 TOWN_CAPTURE 作判据①，主日志 grep 即可）；② 守卫胜闭环 ≥80%（09-15 窗 88-92% 维持）；③ avg_r 跌幅 <20%；④ 自发经济 ≥80% 局不塌（100% 维持）；⑤ 200 步局 ≤20%。
- **⑤ 专项结论（09-15 订正，py/analyze_trunc200.py + vmap 解包实证）**：截断局 100% 为 B 类（守卫胜+占矿后未 capture）的分类仍成立；**但"72_02 地形结构卡点"归因证伪** —— T06_72X72_02.vmap 地形 = gr24_ ×5184 全草地（零河/零山/零路），(40,5) 是草地、蓝城 (69,2) BFS=64 完全可达；4 个截断局 200 动作逐位相同（东向 86/东南 34/西北 25，窗口位移完全一致）= **同一确定性策略局**：前 51 步打同矿 (42)/同守卫 (23,20)(51)，之后一路东进、东缘空输出 ~90 步。"成功局"（r≈275）动作序列前 75 步与失败局完全一致、触发假 capture 时意图位置仅 (34,18)（蓝英雄/城均在 x≥66，零接触）——不存在"绕行遭遇三蓝英雄"的策略分叉，r 差 182 = 100 假奖 + 后续引导差。72_01 截断 21%（r截 188 温和版）、108_02 1v3 2/4 样本不足。
- **⑤ 拍板结论（09-15 拍板并执行）**：~~改图挪蓝城/清堵点~~ **撤销**（地图无墙，改图无收益且引入课程变量）；提步数 250 同样无效。⑤本质 = 策略东缘空转（疑似与长期假 +100 噪声相关，修复 proxy 后先观察），干预方向转到 P10 target 加权排序（蓝英雄/蓝城动态入目标池，`docs/方案_P10_target加权排序_20260915.md`，零部署待灰度）；口径上⑤只数 C 类退化（当前 0），B 类截断不阻塞 WIN-1 聚合。
- **撤梯子登记**：capture 触发率稳定后 `move_to_force` 由全程引导回常规窗。⚠ 09-15 事实修正：ep_runner_one.py L134-136 对 **T06 无条件覆盖 move_to_force=200/guard_done_steps=0**（命令行传 60/15 无效，T04/T05 才用传入值）；撤梯子需改 ep_runner 该覆盖块（改代码→重启窗）。
- **下一步**：五判据聚合达标后，按错窗纪律走 72X72_02_duel 地图轴（见 WIN-3）。
- **指针**：C 方案/duel proxy 细节 #195、#213；09-14 36 局明细知识库「生产回池首批验证」；g3b 终局调研结论已被 H3 #228 吸收。

### WIN-2 — 36X36_02 原样回池评估 🟡（归属 T6.4，待触发，与图池/激励轴错窗）

- **评估方案已完善（09-15）**：`docs/方案_T05_36X36_02重评估_20260910.md` §4 补充操作细则——触发条件表（WIN-1 达标 + 错窗确认 + 基线数据就绪）/ 回池操作步骤 / 五判据表（J1-J5）/ 数据拉取命令 / 图 03 触发条件。
- **第一步**：02 图原样回池，用新口径（visitable 修复后）重评估，判据 = J1 r_mean(02)/r_mean(01) ∈ [0.8, 1.25]。
- **第二步备料**：差异化重设计（图 03 = 01 骨架 + gold mine 加 swordsman×6 守门 = 矿-战斗耦合课程），触发条件 = J1 不通过。
- **历史归因**：51 局全负数据采集于判据失真时代（假糖）；地形压制假设已排除（纯草地同构），真实差异 = 守卫减半 + red 半场近资源移远。
- **指针**：方案文档 `docs/方案_T05_36X36_02重评估_20260910.md` §4。

### WIN-3 — 下一难度轴候选 🟡（归属 T6.4，一次一轴，WIN-1 达标后择一；09-15 纯评估完成）

**评估结论**（`py/win3_eval_difficulty_axis.py`，不改图不重启）：

| 候选 | 红 hero → 蓝镇曼哈顿距离 | /200 步上限 | 结论 |
|------|------------------------|-------------|------|
| ① 72X72_02_duel | 128 | **64%** | ✅ 即插即用，已在 MAPS，200 步内可完成 capture |
| ② 108X108_02_duel | 200 | **100%** | ⚠ 临界不可达——200 步走满才到蓝镇门口，必须 **提 250 步** 或 **挪蓝镇到 (98,98)**（距离降为 174，比 69%） |

**操作细则**：
- ① 无需改动，已在 10 图池。WIN-1 达标后下一难度轴窗口可直接启用。
- ② 触发条件 = WIN-1 达标 + 错窗确认；**二选一**（不混做）：
  - **250 步方案**：改 `STEPS_PER_EP=200`→`250`，全局影响所有图（T05 小图节奏变慢 ~25%，需观察），错窗单独开
  - **挪城方案**：`py/win3_move_town_108_02_duel.py`（09-15 备料）把 108_02_duel 蓝镇从 (105,105) 移到 (98,98)，距离 200→174（比 69%），单图改动不影响其他图；`--dry-run` 可预览；执行后必须跑 `sync_maps_to_runtime.py --strict`
- ③ 1v3 击杀/占城数据：BHERO_KILL + TOWN_CAPTURE 埋点已在位，持续自然积累，不占窗口。
- **纪律**：难度轴与激励轴 / 图池变更不同窗。

**撤销记录**：原「挪城修堵点」方案（09-10）已撤销——72_02 1v3 截断已查明为 proxy 空拍误报 + 策略东缘空转（非地形卡点，见 WIN-1），地形层无需改动。

---

## 二、观察期任务

### T7.4 — 死亡惩罚 🔄（已上线观察，09-11）

- **实装**：`death_penalty=-50` 试探档（上限 -100，传 0 可关），红英雄 8 方向全堵 ×2（zombie）→ -50；蓝英雄死走 capture proxy +100，不同帧无双罚；全局生效不按图分支。
- **三判据现状**：
  - 判据 1（死亡局 r 转负）：⏸️ 挂起并已定性——全草地大图英雄不会 8 向全堵，ZOMBIE **结构性不可达**（T03/T04 小图历史 441 局有、T05/T06 大图 0 局；T04 回池验证同日否决）。
  - 判据 2（[GUARD] 接战率 ±20% 不塌）：✅ 通过。
  - 判据 3（avg_r 跌幅 <20%，基线 2.34）：✅ 通过（无恶化，零 err=yes）。
- **回退线**：接战率塌 >30% 或 avg_r 跌 >20% → 降档 -25 或 0 关闭。
- **关联根因已闭环**：判据 1 的另一半——真败北（无将无城）后败北信号断链——已由 **09-14 H3 修复闭环（#228）**：红败秒级 game_over=2 / r=-200，不再 300s 超时丢信号；生产首批自然 PvP 红败/红胜局继续异步观察。
- **指针**：#204（0 命中三义性）、#210（HERO_DEATH 0 根因）、#228（败北信号根修）、知识库「T7.4 HERO_DEATH 0 死亡根因」「H3 双编译树判定」章。

### T7.5 — 经济奖励撤梯子 S2-S4 🟡（09-15 S2 已实施后回退，用户拍板延后；D3+C2 L0 合批已部署 05:34，09-15 关机暂停）

- **背景**：S2 曾因自发经济塌方暂停（等 R6 战斗修复）→ R6 已修（09-03）+ 自发经济回归（09-04，0→54 次/40ep）→ S2-S4 推进节奏 09-15 重新拍板。硬前置 towns.garrison obs 已填充（09-02，不违 3464 冻结）。
- **每步判据**：avg_r 跌幅 <20% + 招兵/建设频率不塌 + 取兵窗维持。
- **多步渐进方案（08-31 定稿，递归半价 + 效果奖渐入，每步 1-2 窗）**：

| 步 | 触发 | RECRUIT 每次 | BUILD_2 每次 | 兵力系数 | 建设奖（新巢穴） |
|----|------|------|------|------|------|
| S1 | — | +0.5 | +0.75 | 0.02 | 无 |
| **S2 ⏸ 已回退 (09-15)** | WIN-1 达标 + capture proxy 稳定 ≥2 窗 | +0.25 | +0.375 | 0.03 | 低档巢 +3 |
| S3 | S2 无退化 1 窗 | +0.125(≈0) | ≈0 | 0.04 | 低 +5 / 高 +10 / 防 +5 |
| S4 | T05 换图节点 | 0 | 0 | 0.05 定稿 | garrison ✅ + R6 大调同批 |

**S2 实施后回退（09-15，用户拍板）**：
- 因数据核查发现 S2 触发前置（WIN-1 判据① BHERO_KILL 非零）当前不满足，用户拍板"D3+C2 L0 先部署，S2 延后"
- 数值代码已 5 处 SearchReplace 回退：RECRUIT +0.25→+0.5 / BUILD_2 +0.375→+0.75 / 兵力系数 0.03→0.02（与 HEAD 一致）
- 保留 3 处注释改动：L1151 陈旧 `× 0.01`→`× 0.02` 修正（HEAD 注释与实际代码不符）+ L1185-1186/L1204 两处 S2 回退说明注释
- **部署状态**：ep_runner_one.py 数值与 HEAD 完全一致（S1 值生效中），无需重启即生效；S2 待 WIN-1 判据①真正非零后再实施

**节奏重估结论（09-15）**：
- S2 触发前置 = **WIN-1 达标**（capture proxy 修复后五判据聚合达标）——在 capture 判据未稳定前撤梯子会混入变量。
- 顺序：WIN-1 达标 → S2（1-2 窗）→ 无退化确认 → S3（1-2 窗）→ T05 换图节点 → S4。
- **S2 前置数据核查（09-15 根因深挖后更新）**：原"判据① BHERO_KILL 非零"是错误定义（主日志白名单漏网 + 全空拍误报）；修正为"判据① TOWN_CAPTURE 非零"后主日志 215 条已满足。本次窗（04:57:32 起）需重新攒 40 局严格聚合，达标后 S2 才部署。
- **与 WIN-3 难度轴错窗**：S2/S3 属激励轴，WIN-3 属难度轴，不得同窗。
**D3 实施内容（09-15，train_wsl2_ppo_v2.py 四处，py_compile 过）**：
- L3 `import` 增 `math`
- L14-18 常量块 `ENT_COEF_BASE=0.05` / `ENT_WARMUP_STEPS=2_000_000`
- L474 循环外初值 `ent_coef_t, ent_mean = ENT_COEF_BASE, 0.0`
- L478-481 熵系数调度：`ent_coef_t = ENT_COEF_BASE * (1.0 + math.exp(-total_steps / ENT_WARMUP_STEPS))`，loss 改用 `ent_coef_t * ent_mean`
- L525 日志增 `entc={ent_coef_t:.4f} ent={ent_mean.item():.3f}`
- **注**：`py/d3_entropy_bonus.patch` 因 L3 已含 `shutil` 无法 `git apply`，已手动逐处应用（内容等价）
- **部署状态**：改动已落盘，训练进程仍在跑（04:57:32 起，旧 ent_coef=0.05 恒定生效中）；下次自然重启窗口生效

**D3 合批部署清单（D3 + C2 L0，一次 stop/start；S2 已回退延后）**：
| 批次项 | 文件 | 状态 |
|--------|------|------|
| D3 熵 bonus | `train_wsl2_ppo_v2.py` | ✅ 已实施，py_compile 过 |
| C2 L0 崩溃插桩 | `train_wsl2_ppo_v2.py` + `py/train_health_monitor.ps1` | ✅ 已实施，crashlog/ 目录就绪 |
| T7.5 S2 奖励系数 | `ep_runner_one.py` | ❌ 已回退（S2 延后，等 WIN-1 五判据聚合达标 — 判据① 已修正为 TOWN_CAPTURE 非零，主日志 215 条实质满足，仍需本次窗 40 局严格聚合） |
| `__pycache__` 清理 | — | ✅ 已清 |

**⚠ S2 触发前置数据核查（09-15 05:18，`py/win1_five_criteria_snapshot.sh`）**：
- 本次窗启动 04:57:32，仅 4 个 training step ≈ 40 局样本，**判据①样本量严重不足**
- **09-15 判据①根因深挖结论（`py/bhero_kill_rootcause_probe.sh`）**：BHERO_KILL 不在主日志白名单（仅走旁路 battle_quality_events.log），且全历史 9 次**全部** live_slots=0 空拍误报（#146 防护已生效），非空拍差集恒为空 = 真无行为。三义性判定 = ② 观测无效 与 ③ 真无行为 的混合。**判据① 定义错误**：应改为「TOWN_CAPTURE 非零」（TOWN_CAPTURE 完整标签进主日志，白名单 `"[TOWN"` 前缀通配覆盖，主日志 215 条已实质满足）。判据①前置断链解除，但严格五判据仍需本次窗 40 局重新攒窗。
- 判据① TOWN_CAPTURE 非零：**✅ 主日志 215 条 / 旁路 218 条已满足**（BHERO_KILL=0 是观测口径错误非真断链）；HEROSEG_EMPTY=0 主日志（旁路 427 次空拍诊断）
- 判据② GUARD 接战：✅ 2482 次维持
- 判据③ avg_r：近 40 局 **1.56** vs 基线 2.34（跌 33%）⚠ 含本次重启 + P10 scorer 灰度初期波动
- 判据④ 自发经济：✅ RECRUITED=48454
- 判据⑤ 200 步截断率：✅ 10.0%（4/40）
- **结论**：严格口径下 S2 触发前置（WIN-1 五判据聚合）未满足（本次窗仅 40 局样本，判据① TOWN_CAPTURE 主日志 215 条但全历史累积，本次窗需重新攒窗）；D3/C2 L0 无前置依赖，可独立部署。**已拍板**：D3+C2 L0 先部署，S2 等 WIN-1 五判据聚合达标。

### OBS — 长期观察集（不占窗口，复发才行动）

- **OBS-1 R7 battle query 卡顶**：埋点五件套在位，fuse 300s 兜底（局不死仅慢）；`[EP_TIME]` 打点已上线，复发按 map×secs/steps 聚合一击定位。
- **OBS-2 BHERO_KILL 空拍**：防护生效（0 新误报）；`[HEROSEG_EMPTY]` 埋点待复发捕获（全槽 -1=竞态 / 全槽 0=共享内存未填充）。
- **OBS-3 T06 守卫战斗未触发**（aggression 遗留）：与 fix_t06_maps.py 相关，待 capture 打通后专项。
- **OBS-4 TOWNSTALL 引导可达性**：平台段误判已修（#209，`>=` 改 `>`，重启窗 66 局 TOWN_BLOCKED=0）；观察是否继续收敛。
- **OBS-5 108_02_duel 无标记短终局**：✅ **已定性关闭 (09-15)** — 09-14 36 局中 2 局（34/41 步，r15.9/19.57，9.4/8.2s/步偏慢，err=no，无 BattleStart 事件）。**定性 = 英雄开局全堵 zombie 段自然终局，非脏局**：动作序列从 `3,7,3,7`（走动+经济交替）退化为 `3,3,3,3`（纯 END_TURN 死循环）= 8 方向 passable 全 0（英雄 4/8 邻格全堵），与 ep_runner L1227-1228 zombie 段逻辑一致；r≈16-20 远低于正常 108_02 局（r≥96）= 英雄前期被堵死未到经济/战斗阶段；8-9s/步偏慢 = 全堵时引擎层每步轮询 passable + 等待 AI 回合开销大；err=no + 无战斗事件 = 自然终局非崩溃。修正原始假设"疑长战斗"→**实为开局全堵 zombie 段（无战斗事件实锤）**。2/36 局 5.5% 频率低，短期无需修；若后续 zombie 短终局频率 >20% 再查 108_02 英雄起始格 passable 是否被改图误堵（与 #209 TOWN_BLOCKED 同类风险）。`py/obs5_short_term_analysis.sh` 可复用。

---

## 三、并行主线：T6 NK2 逻辑融合（训练验证中）

| 子任务 | 状态 |
|--------|------|
| T6.1 NK2 估值 → 奖励塑形 | ✅ 冻结（08-22），v5 实跑中 |
| T6.2 C++ BFS 全图寻路 | ✅ 代码完成 + T03 段毕业（08-29）；NK2 寻路加速 #7632 已 merge（09-11） |
| T6.3 决策结构分层 | ⬜ 远期（与 T5.9 合并） |
| T6.4 训练地图体系 | 🔄 T06 1v3 已结案（09-08）；当前 MAPS=10 大图池（清单见总任务五）；capture 窗 + T7.4 观察同窗混跑 |

---

## 四、待办工作台 A/B/C/D（标注是否影响训练）

> 影响判定：是否触碰 WSL rel/ 运行时 .so、训练进程/日志、GPU/显存、vcmi-native 工作源码。

**A. 立即可做（零影响：Windows / 纯只读）**：A1-A4 已全部完成（08-29），当前无待办，新增零影响任务追加于此。

**B. 设计文档类（零影响：只写方案）**：B1-B4 方案全部出稿（08-29）。现状：B1 被 T05 全面化覆盖（09-04）/ B2 经济实装（09-03 R6 后 0→54 次）/ B3=D3 熵补丁（09-15 就绪）→ 均已消化；**B4 规格已落地 C1（09-15）：`py/eval_promo.py` 补 B4 五指标 grep（GUARD 首胜率 proxy=guard>0 占比 / 正局率 / avg_r / 大负率 / vloss N/A）+ 双条件 PROMO_HINT（GUARD≥30% AND avg_r≥+15 连续 3 窗，查 eval_history.jsonl 同 ckpt 趋势）；首次 eval 历史不足 3 窗不触发需累积，py_compile + 干跑四场景验证过**。

**C. 谨慎（需隔离设计，有资源/文件重叠）**：

| 编号 | 任务 | 归属 | 状态 / 约束 |
|------|------|------|------------|
| C1 | 晋级判据自动化 eval | T7.1 | ✅ 工具就绪（09-14）+ B4 规格已落地（09-15）：`py/eval_promo.py` MAPS=10 profile + 生产参数对齐 + 1v3 课程判据面板 + 2h 低频护栏 + 独立进程组；**B4 五指标 grep + 双条件 PROMO_HINT（GUARD≥30% AND avg_r≥+15×3窗，查 eval_history.jsonl 同 ckpt 趋势）已补**；CPU-only 但 vcmiserver 仍吃 CPU，全 profile 待训练低峰跑 |
| C2 | 重编崩溃根因插桩 | T6.2 | ✅ L0 已部署（09-15 05:34 随 D3 合批，systemctl stop/start homm3-train-v5，PID 10651）：rc 信号名翻译 / 崩溃局 ep_log 归档 `crashlog/` / 词表补纯崩溃关键词 / 近 100 局 ≥3 次 SIGSEGV/SIGABRT 红警；L1 备料完成（`py/c2_crash_gdb.sh` + `crashlog/` + 方案 §6 SOP），L2 C++ 插桩方案就绪（`docs/方案_C2_崩溃插桩_20260915.md`），当前零崩溃不触发 |
| C3 | CGameHandler garrison 自动合并 | T8.1 | ⬜ 触碰铁律；除非断言复发否则搁置 |
| C4 | NK2 寻路加速 merge（+40% 吞吐） | T6.2 | ✅ 完成（09-11，首窗观察本负载中性无害，保留新栈；见已完成任务 C4 条） |

**D. 等待条件触发**：

| 编号 | 任务 | 归属 | 状态 |
|------|------|------|------|
| D1 | 经济动作实际上线 | T7.3 | ✅ 已发生（capture +100 于 09-08，ep_runner 层零停训） |
| D2 | 30x30/36x36 实际解封 | T7 | ❌ 已被 T05 全面化覆盖（09-04），P3 解封参数过时 |
| D3 | 开局熵 bonus 实装 | T3 | ✅ 已部署（09-15 05:34 随 C2 L0 合批，systemctl stop/start homm3-train-v5，PID 10651）：`train_wsl2_ppo_v2.py` 四处：import math / ENT_COEF_BASE+ENT_WARMUP_STEPS 常量 / 熵调度 `×(1+exp(-step/2M))` / 日志 entc+ent；`py/d3_entropy_bonus.patch` 因 L3 含 shutil 无法 git apply，手动逐处应用内容等价；T7.5 S2 已回退延后（#235），本批仅 D3+C2 L0 两项 |
| D4 | 2v2/多人对局验证 | T10 | 🟡 fork headless 不影响；GUI 验证需用户在场 |

**建议顺序**：WIN-1 攒局聚合 → 拍板 WIN-2（36X36_02 回池）→ WIN-3 地图轴；C1 视训练稳定度择机。

---

## 五、训练期并行任务 P（不碰运行中 v5 进程）

| 编号 | 任务 | 类型 | 状态 / 依赖 |
|------|------|------|------------|
| P1 | 训练空转根因（断言雷）修复 | 解锁型 | ✅ 08-29 |
| P2 | 晋级判据自动化 eval（py/eval_promo.py，CPU-only） | 备料型 | ✅ 08-29 |
| P3 | 30x30/36x36 守卫距离解封 | 解锁型 | ❌ 过时可废弃（课程图体系已替代，复用需先适配 visitable 口径） |
| P4 | ZOMBIE/GUARD all-blocked 聚合分析（py/analyze_deadlock.py） | 备料型 | ✅ 08-29 |
| P5 | 开局确定性对策（entropy 调度） | 备料型 | ✅ 已部署（09-15 05:34）= D3：`train_wsl2_ppo_v2.py` 全 4 hunk 已应用（L3 math import / L17-18 ENT_COEF_BASE=0.05+ENT_WARMUP_STEPS=2M / L476 ent_coef_t init / L487-489 熵系数×(1+exp(-step/2M)) / L534 print entc+ent）；随 C2 L0 合批 systemctl stop/start 生效（PID 10651），首个 step 日志确认 entc 字段即完成 |
| P6 | 对手池 self-play/NK2 混合 | 备料型 | ⏸ 远期；NK2 三树源码就绪，回用时直接重编即生效 |
| P7 | 真实游戏战略层联调（fork 1.8 DLL） | 铺路型 | ✅ 大幅推进至闭环：ModelAI 加载链 + battle-only 战斗链 + GUI 稳定性 + mq v5 全模型 1v7 PASS（见已完成任务 09-10/09-11 条与知识库 mq 章） |
| P8 | 人机对战底座（大厅 1v7 配置） | 铺路型 | 🟡 = T13.10，见第七节 |
| P9 | done 信号接线 | 备料型 | ✅ 误报排除（已存在无需开发） |
| P10 | H3M 官方图 → VMAP 转换（h3m2vmap） | 备料型 | ✅ A-E 全通，King 已入池（见已完成任务 09-01/09-13 条） |
| P10-target | target_list 加权排序 Python 旁路打分器（零重编） | 备料型 | ✅ 实施完成 + 灰度启用（09-15）：`py/target_scorer.py`（候选池 5 类 + 统一打分公式 + 硬约束打分侧过滤 + pick 选头名 + 经济期远目标 V×0.2 衰减 #232）+ `ep_runner_one.py` `--target_chain {legacy,scorer}` 7 参数 + `[SCORE]` 诊断日志；`train_wsl2_ppo_v2.py` L196 透传 scorer + L222 `[SCORE]` 进主日志（#234）；离线 7 项测试全过，py_compile 通过；灰度首日 King r=119.3 正收益 |
| P10-target-2 | T06 duel act_loop 门控解耦 + r=-0.5 赋值 bug 修复 | 修复型 | ✅ 已实施（09-15，#237）：`ep_runner_one.py` 3 处改动（`--act_loop_from_step` 新参数 L110 / act_loop 门控 L1228 解耦 / 横跳 8 步窗 L1269 门控同步解耦 / L1257 `r = -0.5` → `r += -0.5` 累加修复）+ `train_wsl2_ppo_v2.py` L184 T06 专属 `--act_loop_from_step 60`；py_compile 双文件过；`systemctl stop/start` 重启生效；预期 T06 duel r 从 -440 回升至 -200~-300 |
| P11 | VMAP → H3M 反向转换 + roundtrip | 备料型 | ✅ roundtrip PASS（09-13，见已完成任务 P11 条） |

> P10 遗留课题（target_list 加权排序）已实施 + 灰度启用（09-15）：`docs/方案_P10_target加权排序_20260915.md`，Python 旁路打分器 `py/target_scorer.py` + ep_runner `--target_chain scorer` 灰度开关，零 obs 变更零重编；经济期远目标 V×0.2 衰减（#232）+ 训练脚本透传 + [SCORE] 进主日志（#234）已上线。

---

## 六、风险登记 R（引擎侧）

| 编号 | 风险 | 状态 / 对策 |
|------|------|------------|
| R1 | 动作拒绝不可感知 | 🟡 已缓解未审计（Python action mask 在用） |
| R2 | done 信号轮询链 | ✅ 误报排除 |
| R3 | 异步时序竞态 | 🟢 约束型（新增动作走 waitTillRealize） |
| R4 | queryID=-1 断言雷 | ✅ 已修（libMMAI.so 13c0f041） |
| R5 | 双源码树不同步 | 🟡 长期隐患——09-14 已实锤 `vcmi-native-build` 是 08-02 陈旧树（#228）；源码改动前先在生产树 vcmi-native grep；Windows 树另有引擎 API 分叉不可镜像同步，只摘取 fix |
| R6 | 战斗 AI 恒 fallback StupidAI | ✅ 已修复（09-03，USING_ONNX=0 硬编码 → onnxruntime 1.19.2 部署重编，V13::BAI 接管）；长期观察 avg_r/胜率 |
| R7 | battle query 卡顶 | 🟡 长期观察（见 OBS-1） |
| R8 | 训练进程存活 | 🟢 流程已固化——system 级 enabled unit + Windows keepalive（#114/#201）；一律后台跑，关机前优雅 stop（零损失），判活看 PID etime + 日志 mtime 勿只信 is-active |

---

## 七、T13 外挂 AI 专区 — T13.10 多人对战验证 ✅（实机闭环 + 备料已补 09-14）

> T13.1-T13.9 协议栈主体已全部完成（离线 167/167 PASS），详细归档见 `已完成任务.md` 09-11/09-12 T13 条目与知识库「VCMI 网络协议与外挂 AI 架构」章。代码 `py/vcmi_protocol/`。本节仅跟踪 T13.10 实机闭环。

| 阶段 | 内容 | 状态 |
|------|------|------|
| P8-B-1 | lobby join 握手（Python 直连 VCMI_server，LobbyClientConnected 与真 client BYTE-IDENTICAL） | ✅ 09-11 |
| P8-B-2 | 完整对局 EndTurn 轮转（host 让位 ChangeHost → SetMap → StartGame → Turn2 轮转 zero fishy） | ✅ 09-11 |
| P8-C | 决策接入：MoveHero 真实执行 + QueryReply + Build→TOWNAVAIL→Recruit 城镇链 + 协议栈四层缺陷修复 + 双探针 | ✅ 09-11~09-12（#205/#206/#207/#208） |
| P8-D | 跨机器部署（HMAC 认证 + 远程连接 + 拓扑管理） | ✅ 双机部署路径已用单机双实例实跑完成：WSL 双实例 9/9 PASS（`py/p8d_dual_node_sim.py`）+ 单机双实例 13/13 PASS（`py/p8d_two_instance.py`：2 真实 VCMI_server 端口隔离 3030/3031 并存 + 2 真实 ModelAI client 跨实例 + HSK 跨实例预交换 + HMAC 正负例 + 双节点状态文件） |
| P8-E | 人机混局验证（人类 GUI + 1 外挂 AI 同局） | ✅ 09-15 PASS：`py/p8e_human_mix_probe.py` 2 客户端拓扑（Python host + 外挂AI，P8-B 范式），人类 GUI 同机共存不 join slot；验证"人类 GUI 进程与 AI 同局共存"（Twins 仅 2 玩家 slot，第 3 客户端 join lobby 触发 NEW_GAME "Picking random factions" 崩溃）；含 BattleStart(132) 多回合闭环 |

- **剩余工作**：① ~~真机 `--remote` 部署~~ **已废弃**——双机部署路径已用单机双实例实跑完成（09-14 `py/p8d_two_instance.py` 13/13 PASS：2 真实 VCMI_server 端口隔离 3030/3031 并存 + 2 真实 ModelAI client 跨实例 + HSK 跨实例预交换 + HMAC 正负例闭环 + 双节点状态文件，共享 fork build 无需副本），无需第二台真实机器，`--remote <host>` 路径保留但不再作为待办；② QueryReply 真实 qid≠-1 样本——09-14 重跑 `p8c_query_probe_real.py` 确认数据源墙仍成立（PASS qid=-1 only：22 条 PlayerStartsTurn 全 qid=-1，0 条真实 QueryReply，server 健康、回合正常），需有计时器场景才能拿到 qid≠-1 样本；③ 混人长对局浸泡——09-14 新建 `py/p8/p8b_soak_30min.py`（30min 浸泡探针，fork server + 真实 ModelAI client + Python host，每 5min checkpoint，崩溃/断线/回合监控），30min 跑完 PASS（稳定性）：无崩溃 / 0 断线 / 743 包 / 26 回合，⚠ 回合前 ~1min 冻结（战斗终态，同源 P8-C 数据源墙，无 timer 场景 server 不打断当前玩家回合）。
- **目标场景**：2+ 人类客户端 + 1 外挂 AI 客户端同一 VCMI server，1v7 大厅配置，AI 接入任意玩家槽位。

---

## 八、远期 / 阻塞集

- ✅ **引擎侧 standardDefeat 判负/败北信号**：09-14 H3 已闭环（server 战斗结算 + ML .so 终局通道 + Python 配套，#228；红败 go=2/r=-200 秒级传递）。原 T7.4「同帧双罚」复核已确认无需去重（done break 先于 -50）。
- ⬜ **T5.9 v2 MultiDiscrete 三维参数化**（与 T6.3 合并）。
- ⬜ **MMAI v15（PR #7654）**：未合并官方、2-4x 慢，合并后需重采集（绑 T8）。
- ⬜ **战斗模型训练**：当前不需要（R6 启用现成 onnx，零训练成本）。触发条件 = T05 对抗图接战胜率成为战略层瓶颈；路径 v13 战斗自对弈（BC 热启动 → PPO），v15 GNN 绑 T12。勿在课程中途升级（战略环境非平稳）。
- ⬜ **动作码 16-23 内政启用**：Level 3 切入时先解决 noTarget 惩罚。
- ✅ **fogOfWarMap 语义确认**（visible vs explored，09-15 源码核查）：**= explored 累积语义，非当前可见**。英雄移动只增量置 1（`TryMoveHero.fowRevealed` 仅收新视野中 fow==0 格，apply 置 1），正常对局无"离开→清 0"路径；清 0 仅 ViewWorld/ViewAir 法术失效与网络回滚。故 obs[1155:3203] `global_explored` 命名与语义正确；local_tiles 三通道以 explored 为黑雾门控、门内对象/守卫为实时直读（非探索时刻快照）。引擎头注释 "true-visible" 与 ML 注释 "visible/explored" 系误导用词。若将来要"当前帧可见"需另按 sightRadius 实时计算，引擎无现成 per-tick 图。详见 `docs/源码分析地图.md` 后续待确认第 4 条。
- 🟢 **target_list 加权排序 Python 旁路打分器**（P10-target，09-15 实施完成）：`py/target_scorer.py`（候选池 5 类 = target_list/蓝英雄/蓝城/己方取兵城/守卫 + 统一打分公式 + 硬约束打分侧过滤 + pick 选头名）+ `ep_runner_one.py` `--target_chain {legacy,scorer}` 7 参数 + `[SCORE]` 诊断日志；零 obs 变更零重编；离线 7 项测试全过，py_compile 通过，默认 legacy 零行为变化，待灰度启用。方案见 `docs/方案_P10_target加权排序_20260915.md`。

---

## 九、关键拍板记录（影响后续节奏）

- **错窗纪律**：激励轴（capture/T7.4）/ 地图轴（36X36_02 回池/新图）/ 难度轴（大图 duel）不同窗，一次只加一个难度轴。
- **(09-08，用户授权)** 下一难度轴四候选选「1v3 完整胜利路径引导」（激励轴）→ capture +100 已实施；地图轴留下一窗。
- **(09-11，用户)** T06 duel capture 死信选 C 方案（蓝英雄死亡 = capture proxy +100，仅当时 duel 图，每局一次）；09-13 因 49 误报改为 duel 排除（#213）。
- **(09-13，用户)** T7.4 判据 1 不走 T04 死路密集轴（实证非 ZOMBIE），判据 1 改靠引擎 standardDefeat 根修 → 09-14 H3 闭环。
- **(09-14，用户/H3)** 败北信号走 H3 方案（改 ML .so + server，不碰 libvcmi），弃用重库 H1 与纯 Python 旁路。
- **关机标准流程**：一律后台 unit；优雅停 = 零损失 vs 被动死 = 回滚（#169），关机/改码前主动 `wsl -u root systemctl stop homm3-train-v5`（system 级 unit，0911 起；无 --user）。
- **(09-15，用户拍板并执行)** WIN-1 ⑤：原拟改图方案经 vmap 解包证伪（72_02 全草地、蓝城 BFS 可达），撤销改图；改拍板**修复 capture proxy 空拍误报**（已在 695099 停启窗部署，详见 WIN-1 区）。⑤口径 = 只数 C 类退化（当前 0），B 类截断不阻塞聚合；策略东缘空转转 P10 target 加权排序方向。同窗备料：C2 崩溃插桩方案、P10 target 加权排序方案（均🟡零部署，见 docs/方案_*_20260915.md）；D3 熵 bonus 补丁+C2-L0 可合并到同一自然停启批次。
