# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `WSL踩坑点.md` | 知识库: `WSL知识库.md`

---

## #46 全线打通 ✅ (2026-07-29)

**env.reset() + step() 循环完整可跑通，obs 数据已填充**

### 修复链

| 问题 | 修复 | 状态 |
|------|------|------|
| installNewBattleInterface segfault | 重建三件套自然修复 | ✅ |
| Discord null dereference segfault | `if(ENGINE->hasDiscord())` guard | ✅ |
| 信号量通信 (adventure_wait 不返回) | `AAI::yourTurn` 调 `adventure_process_turn()` | ✅ |
| MMAI constructor 未导出 | `__attribute__((used))` | ✅ |
| Hero pool even 检查抛异常 | `pop_back()` 替代 throw | ✅ |
| battle 卡死 (MMAI_USER 等待回调) | `red="StupidAI"` 战斗自动解析 | ✅ |
| passability 全1 | dx/dy 统一 N-start + `getTile().isClear()` | ✅ |
| yourTurn 卡死 (blue/tan 不处理) | non-red auto selectionMade+endTurn | ✅ |
| moveHero/endTurn 卡死 | `waitTillRealize = false` | ✅ |
| obs_nz=8 (状态数据为空) | `CGameInfoCallback` public 方法替代 `getHeroesInfo()` | ✅ |
| g_ml_player_cb 全局指针 | `AAI.cpp` 设置、`adventure_process_turn` 读取 | ✅ |

### 工作组合（全部最新重建）
| 组件 | 位置 | 说明 |
|------|------|------|
| libmlclient.so | `vcmi-native-build/rel/bin/libmlclient.so` | 含 CGameInfoCallback fill + atomic |
| libMMAI.so | `vcmi-native-build/rel/bin/AI/libMMAI.so` | 含 `AAI::yourTurn` + g_ml_player_cb |
| vcmiserver | `vcmi-native-build/rel/bin/vcmiserver` | 含 Discord fix |
| libmlserverplugin.so | `vcmi-native-build/rel/bin/libmlserverplugin.so` | 含 hero pool pop_back |
| connector_v13.so | `vcmi-workspace/vcmi_gym/connectors/rel/connector_v13.so` | 最新重建 |

### 验证结果（2026-07-29 16:00）
```
OBS: nz=32        ← 之前 nz=8，只有 passability
STEP0: r=+6.150   ← 之前 r=-0.100，现在有英雄/资源/城镇奖励
STEP1: r=+6.150
STEP2: r=+6.150
```

---

## 开机任务

**Step 1: 重部署 hero pool fix** ✅
```bash
cd /home/administrator/vcmi-native-build/rel && make -j4 mlserverplugin
cp rel/bin/libmlserverplugin.so /home/administrator/vcmi-native/rel/bin/
```

**Step 2: 跑 ep_runner 验证完整 episode** ✅
```bash
ep_runner_one.py 20 /tmp/ep.json "For Sale.h3m"
```
（已改用 StupidAI 修复战斗卡死）

**Step 3: 修复 obs_nz — 填英雄/资源数据** ✅
- `CGameInfoCallback::getPlayerState().getHeroes()` 替代 `getHeroesInfo()`
- 全局 `g_ml_player_cb` 指针传参
- obs_nz: 8 → 34, reward: -0.1 → +6.15

**Step 4: 启动 PPO 训练** ✅（2026-07-29 19:50）
- passability 修复验证通过: `[0,0,1,0,0,1,0,0]` 非全1, hero (13,6)→(14,6), r=+6.2
- 模型首次拿到正奖励: r=+1480~2000/ep, 英雄在移动
- 使用 V2 训练器: LR=5e-5, GAE λ=0.9, 梯度裁剪=1.0
- 三件套: make mlclient (passability) + MMAI (yourTurn fix) 均已重建部署

### 文档已更新
- 知识库: passability 系统 (方向映射/计算/waitTillRealize/non-red) ✅
- 踩坑点: 4条新陷阱 (方向映射/canMoveBetween/waitTillRealize/non-red) ✅

### 后续改进 ✅ (2026-07-29)
- **`map_size`/`day` 未填充** — 已修复：在 `adventure_process_turn()` 加 `gameState().day` + `getMapSize()` 填充 ✅
- `passability` 在 Python 侧计算（`obs[-8:]`，不受 C++ 影响）— 已知，不处理

### 下一步（2026-08-01 讨论结论: C8.5 收尾 + Phase D 前置）

**当前训练状态**: 健康运行（200 步/局，内存稳定）但模型塌缩 act=[8,10×199]（交互拿一次 +2.9 → END_TURN 刷到底）
**塌缩根因**: PPO 无 KL 约束偏离 BC + 无事件奖励（占矿/杀敌未实现）+ 无结束机制 + obs 缺态势感知（1v7 最小阻塞集 #1-4/#8/#13）

**方向选项**（讨论，建议 B+C 优先）:
- **A. KL 约束 BC**（训练脚本，快）— PPO 加 KL 惩罚防偏离初始策略；交互奖励限制（重复 act=8 不给 exp 奖励）；探索奖励重设计（出生区基线问题）
- **B. 态势感知扩展**（strategic_state C++，中量）— #1 全图探索格点、#4 己方所有英雄（现仅 1 槽）、#8 NEXT_HERO action、#2/#3 敌方英雄/城镇位置 —— **1v7 最小阻塞集前提**
- **C. 事件奖励**（strategic_state 扩展+奖励函数，中量）— 矿归属追踪（占矿+10）、战斗结果（杀敌+100）、占城已有 town_mult=30 —— 需 B 的态势感知才有效（不知道敌人在哪，杀敌奖励拿不到）
- **D. NK2 内存根因深挖**（可选）— 3.7-7.5GB/局内存爆炸（EmptyAI 430MB/MMAI_RANDOM 493MB 正常），修好恢复真对手

**建议顺序**: B+C 优先（态势感知是 1v7 前提，事件奖励在无感知时无效）→ A 作保护后加 → D 可选

---

## B 态势感知扩展 ✅ (2026-08-01, OBS 264→2689)

**决策**: 训练已确认塌缩（act=[8,10×199] END_TURN 刷底），用户下令关闭训练专注 B。训练进程全退（train_loop.sh/train/ep_runner），GPU 释放。

**OBS schema v2 (2689 维)**:
```
[0:8]      global 8 (day/week/month/current_player/map_w/map_h/underground/player_count)
[8:104]    players 8×12=96
[104:288]  heroes 8×23=184 (全 8 英雄，不再截断)
[288:400]  towns 8×14=112 (id,owner,pos×3,buildings,garrison[7],gold_income) ← 新增
[400:625]  local window 15×15=225  ← 新增: active hero 为中心, 0=未知/1=可通行/2=障碍
[625:2673] global explored 32×32×2=2048 ← 新增: 全图下采样(块内任一探索=1), z 层序
[2673:2681] active_hero + 7 pad ← 新增: obs[2673]=当前活动英雄索引
[2681:2689] passable 8 (位置不变)
```

**C++ 改动（已编译部署 vcmi-native/rel/bin, 2026-08-01 10:53）**:
| 文件 | 改动 |
|------|------|
| strategic_state.h | StrategicState 末尾追加 active_hero(int32) + local_tiles[15][15](int8) + global_explored[32][32][2](int8)，现有字段一字未动（ABI 安全）；_version→2 |
| strategic_state.cpp | `extern "C" g_active_hero` 全局；新增 fill_exploration()（team->fogOfWarMap 读探索，per-player 视角；local 窗口 active hero 为中心；global 下采样 ceil 块）；fill_strategic_state + strategic_state_update 双处调用；passability 改用 active hero |
| AAI.cpp | act==9 NEXT_HERO：g_active_hero=(g_active_hero+1)%n（越界回退 0）；移动 0-7 用 heroes[g_active_hero]（越界回退 h[0]）；INTERACT(8) 保持原样 |

**Python 改动（已 py_compile + 25/25 mock 验证）**:
| 文件 | 改动 |
|------|------|
| strategic_reader.py | StrategicState._fields_ 追加 3 字段 + LOCAL_WIN/GLOBAL_GRID 常量 + read() 加 active_hero |
| strategic_env.py | OBS_DIM 264→2689；_strategic_state_to_obs 重写（towns/local/global/active_hero 段）|
| train_wsl2_ppo_v2.py | Net Linear(2689,128)；BC 加载失败需优雅降级 |
| bc_train.py | Net Linear(OBS_DIM=2689,128) |

**编译排障记录**（3 个阻塞）:
1. CMakeCache 损坏（fmt 找不到）→ 备份后删 CMakeCache.txt 重配（-DENABLE_ML=ON -DENABLE_DISCORD=OFF -DENABLE_LAUNCHER=OFF RelWithDebInfo）。研究确认：fmt 只在 ENABLE_DISCORD 块内需要，-DENABLE_DISCORD=OFF 可跳过
2. vcmi-native 缺 user_agents/agent-v14/v15.cpp → 从项目源同步
3. vcmi-native MLClient.cpp 旧版（{"server",combatAlliedAI} 无效路径）→ 同步项目源 601 行新版
4. rel/bin/data 是真目录（00:08 建）导致 POST_BUILD create_symlink 失败 → 移走为 data_gamedata_bak，重链后手动 ln -s /mnt/d/GAMES/Heroes3

**产物**: libmlclient.so 294MB(10:53) + libMMAI.so 18.6MB(10:48)，nm -D 确认 `B g_active_hero` 导出。备份: backups/B_strategic_20260801/（Windows 源）+ *.bak_B（WSL .so）。

**待验证点**: fogOfWarMap 语义（VCMI 注释 visible vs 累计 explored）——影响"走过又离开视野"的格子；运行时观察 local_tiles 数据确认。

**审计修复 (2026-08-01 11:05)**: 审计 agent 发现 global_explored 维度错位 — C `[32][32][2]`(z内层) vs ctypes `c_int8*32*32*2` = `[2][32][32]`(z外层)，2048 字节索引语义错乱。修复：C 侧声明改 `[MAX_LEVELS][GLOBAL_GRID][GLOBAL_GRID]` + 写序改 `[z][gy][gx]`（z 外层，与 Python 现有访问/展平/测试天然一致），Python 零改动。local_tiles/active_hero 审计确认一致。教训：手写 schema 双端易错，下次部署前做字段序自动化对照测试。

**连带影响**: obs 维度变化 → 旧 bc_model.pt(264 维) 作废，BC 数据(264 维) 作废，需按 2689 重新采集+训练 BC 权重。

---

## A+B 合并: 事件奖励 + 奖励逻辑修正 + KL 约束 ✅ (2026-08-01 11:30 开发完成)

**目标**: 一次到位堵住塌缩根因（无事件奖励 + exp 交互漏洞 + 无 KL 约束），obs 保持 2689 只清一次权重。

**C++ 侧（已编译部署 11:31-11:34）**:
| 文件 | 改动 |
|------|------|
| strategic_state.h | StrategicMine 结构（id/type/owner/pos×3）+ StrategicState 末尾追加 mine_count + mines[64] + battle_result；StrategicMine=24B, StrategicState 4660→6204B |
| strategic_state.cpp | fill_mines() 遍历 gs.getMap().objects 筛 Obj::MINE 填归属表（getOwner()，非 0-7 写 -1 中立）；fill_strategic_state + strategic_state_update 双处调用 |
| ServerPlugin.cpp | extern "C" g_battle_result；endBattleHook 写战斗结果（br->winner + heroAttacker 归属判断 red 胜负：0/1/2/3=无/赢/输/平；打野也记录——原代码 heroDefender 空 return 不记；ESCAPE/SURRENDER 写 0） |

**Python 侧（28/28 mock 验证）**:
| 文件 | 改动 |
|------|------|
| strategic_reader.py | StrategicMine ctypes + mine_count/mines[64]/battle_result 字段（与 C 字节一致，审计 12/12 确认） |
| strategic_env.py | 奖励: 占矿+10（owner 变化→0）/打赢+100（battle_result→1）/升级+10（level 变化）；**删 exp per-step（堵 act=8 +2.9 漏洞）**；探索预填出生区 3×3（reset 时，必须走出去才有探索奖励）；clip 上限 -10,10→-10,300（防 +100 被吞） |
| train_wsl2_ppo_v2.py | KL_COEF=0.05；BC 加载成功后建冻结 kl_ref 参考网络（requires_grad_(False)），loss += KL_COEF·KL(pi‖pi_ref)；无 BC 时 USE_KL=False 从零训 |

**编译排障**: 编译树 ServerPlugin.h 等 5 文件版本漂移（旧版无 HeroPools/randomArmies）→ 全量同步 server/ML 后重编成功。

**验证**: libmlclient.so 11:34 + libmlserverplugin.so 11:31；nm -D g_battle_result B 符号 ✓；data symlink ✓；StrategicMine=24B/StrategicState=6204B（C g++ vs Python ctypes 一致）；备份 backups/AB_events_20260801/ + *.bak_AB。

**进行中**: BC 采集（2689 维, NK2 24 局, → bc_data/bc_raw_v2689.npz）→ bc_train → 训练重启（KL + 事件奖励）。

### 采集排障（2026-08-01 14:00, 0 pairs → 771 pairs + 训练重启）

**采集全挂根因链（4 个，逐层定位）**:
| # | 根因 | 修复 | 验证 |
|---|------|------|------|
| 1 | fill_exploration segfault: `tile.isClear()` 无 from 参数 → VCMI 内部 from->getTerrain() nullptr 解引用 | 传 `&from_tile`（英雄所在格，对齐 passability 写法） | gdb backtrace 定位 |
| 2 | fill 无锁直读 CGameState → 与 NK2 规划线程数据竞争 → NK2 决策损坏 → moveHero 全被服务器拒（死循环） | fill_exploration/fill_mines 加 `std::shared_lock gsLock(CGameState::mutex)` | H2 隔离实验（禁用 fill → NK2 恢复 100 pairs） |
| 3 | red 冒险 AI 分配失败: MLClient 写 `settings` session 层, Client.cpp 读配置层 → 不一致 → red 落默认 MMAI（NK2 不加载, act=-1 全是 MMAI 的 send_action(-1)） | 跨 .so 全局 `extern "C" char g_adventure_allied_ai[64]`（MLClient strncpy 写 / Client.cpp 读, g_ml_player_cb 同模式） | Opening Nullkiller2 ✓ |
| 4 | 战斗崩溃: 编译树 router.cpp 旧版（无 C8.5 neutral guard `ASSERT(cb->getPlayerID()->hasValue())` → neutral 打野崩） | 同步项目源 router.cpp（try + pid->hasValue 防御） | 单局 45 pairs 采集成功 |

**其他发现**:
- 编译树（vcmi-native）AI/MMAI 只有 v13 结构, 项目源是 fork tip（含 v14/v15 Graphmind）——**不能全量同步 MMAI**（v15 draft 编译风险），只同步关键修复文件
- 编译树 AIGateway.cpp 无采集 hook（C8.3 的 hook 在 vcmi-native-build）→ 同步项目源 AIGateway 重编 libNullkiller2.so（含 hook）
- `extern "C" extern char x[64]` 非法（块作用域/链接规范语法）→ 必须 `extern "C" { extern char x[64]; }`
- settings 写读层不一致对训练无影响（red 默认 MMAI 恰好正确）——只有采集（NK2）暴露

**结果**: 24 局 771 pairs（C8.3 是 435）→ bc_train best_val=1.584（预测 8 方向全覆盖不塌缩）→ **训练 14:11 重启**（BC 2689 加载 ✓ + KL_COEF=0.05 ✓ + 事件奖励 + 出生区预填）。

**训练停止（2026-08-01 14:5x, 用户关机）**: 训练重启后约 40 分钟, 用户关机前手动停止（wsl --shutdown 全清）。训练产出: ROUND 1 起步（step 7600 之前的旧记录; 新训练刚开始, 无有效 checkpoint 损失——BC 起点重来代价小）。

**下次开机恢复路径**: `wsl` 启动 → 清 pyc → `nohup bash train_loop.sh`（BC_PATH 已指向 bc_model_v2689.pt, KL ON 自动生效）→ 验证 GPU + train_loop.log 出现 "Loaded BC weights + KL constraint ON"。

---

## #45 EEXIST 解锁 ✅ (2026-07-29)

**v15 connector 前置阻塞已解**

| 问题 | 修复 | 状态 |
|------|------|------|
| data 符号链接 EEXIST | 真实目录+文件软链替代 cmake POST_BUILD 符号链接 | ✅ |
| config 文件空 | 所有目录所有大小写变体写入有效 JSON | ✅ |
| 递归 config 符号链接 | `rm -f config/config` | ✅ |

**v15 connector 重建** — `cmake --build rel --target connector_v15` 成功（681KB）

**已知阻塞：** v15 VcmiEnv init 后 segfault（中立玩家 `installNewBattleInterface` 后崩溃）— 归入 Phase D 战斗集成

---

## 训练重启 ✅ (2026-07-29 21:07)

**清旧模型 + 启动新训练**

| 操作 | 说明 |
|------|------|
| 清旧权重 | `wsl2_model.pt` + `wsl2_model_state.pt` + `checkpoints/*.pt` |
| 部署新 .so | `libmlclient.so`（含 day/map_size） → `vcmi-native/rel/bin/` |
| 启动 | `train_loop.sh` via `wsl bash` background |
| V2 参数 | LR=5e-5, GAE λ=0.9, 1000eps×200steps, 4 maps |
| obs 质量 | `obs_nz=61`（旧模型仅 29，因缺 day/map_size/passability） |

**教训：** obs 结构变化后旧模型权重编码了错误模式，必须清权重从头训练。

---

## C8 行为克隆预训练 🔶 (2026-08-01: C8.1-C8.4 ✅, C8.5 微调进行中)

**当前训练=双方 MMAI_USER 自对弈→模型塌缩（act=10 全覆盖）。**

| 发现 | 结论 |
|------|------|
| act=10 = END_TURN | 模型学到的"最优策略" |
| 根因不是奖励，是对手死了 | 两边都不会动，没互动 |
| 人类/Nullkiller2 会正常玩 | BC 从 Nullkiller2 学是正确路径 |

**C8 执行计划：**

| Step | 内容 | 状态 |
|------|------|----------|
| C8.1 | Nullkiller2 白名单 + 重建 .so | ✅ 完成 |
| C8.2 | 验证 Nullkiller2 在 env 中可移动 | ✅ 完成 (2026-07-31) |
| C8.3 | BC 数据采集管道 + action 推断 | ✅ 完成 (24 局 435 pairs, 2026-08-01) |
| C8.4 | BC 训练脚本 → bc_model.pt | ✅ 完成 (2026-08-01, best_val=5.4) |
| C8.5 | PPO 微调（blue=Nullkiller2, 探索奖励） | 🟢 进行中 (2026-08-01 03:09 启动, 1000eps) |

### C8.2 验证结果 ✅ (2026-07-31)

**Nullkiller2 可移动验证 PASS** — reset 48s / 3 完整回合 / 无超时无崩溃:
```
reset OK in 49.1s, obs_nz=33, day=1.0
  step 0 r=+10.00 day=2    ← red(MMAI) → blue(NK2) → tan 全推进
  step 1 r=+10.00 day=3
  step 2 r=+10.00 day=4    ← max_turns=3 到期
```
NK2 真 AI 活动证据（trace）: heroMoved x432、tileRevealed x432、requestRealized x298、Build/Buy/Recruit x52

### C8.3 采集管道打通 ✅ (2026-07-31 夜)

**架构：** red=Nullkiller2（学习对象）自主玩，blue=MMAI（自动 endTurn），
NK2 每个决策点（moveHeroToTile/endTurn）阻塞采集 (obs, action) 对。

**C++ 改动（全部已重建部署）:**
| 文件 | 改动 |
|------|------|
| strategic_state.cpp | fill 逻辑抽成 fill_strategic_state()；新增 adventure_capture_turn()（填 obs + 记录 NK2 action + 阻塞等 Python） |
| strategic_state.h | 声明 adventure_capture_turn/adventure_capture_noblock/adventure_try_wait |
| MLClient.h | InitArgs 末尾加 red/blue 字符串字段（冒险 AI 名，带默认值保持 28 位置参数兼容） |
| MLClient.cpp | adventureAlliedAI=a.red, adventureEnemyAI=a.blue（原硬编码 MMAI/Nullkiller2） |
| threadconnector.h/cpp + connector.cpp | 新增 redAdventureAI/blueAdventureAI pybind 参数（战斗 AI 与冒险 AI 解耦） |
| AIGateway.cpp (build 侧) | moveHeroToTile 决策点 + endTurn 采集 hook；makeTurn catch-all 补 endTurn()（NK2 异常时防卡死） |

**验证数据（ep0 样例）:** 43-46 pairs/局, 8 方向全覆盖 + END_TURN,
day 1→4, obs_nz=48-60。action 分布例: {1:20, 6:6, 3:4, 4:4, 5:4, 7:2, 2:1, 0:1, 8:1, 10:3}

**关键教训:**
1. **方案 B（heroMoved 非阻塞采集）死锁** — fill 在主线程回调执行，与 NK2 后台规划线程竞争 CGameState 锁。回退方案 A（阻塞式，fill 在 NK2 后台线程）。
2. **NK2 makeTurn 异常不 endTurn → 游戏卡死**（current_player=0 无新决策）— catch-all 补 endTurn()。
3. **NK2 卡死（chain 重试死循环）概率 ~50%** — 采集脚本每局子进程隔离 + 卡死自动跳局 + 增量保存。
4. **WSL 每 20-30 分钟重启**（内存压力）— /tmp 会清空，采集输出必须写 /mnt/d 持久路径。
5. **2 人图**（slots=[0,1]）: Dungeon Keeper/Key to Victory/Good Witch Bad Witch/Fort Noxis 等 10 张（scan_players_v2.py 158 张全解析，8/8 对照通过）。
6. **InitArgs 加字段必须放末尾带默认值**，三副本同步（vcmi-native-build/vcmi-native/project source）。

**C8.4/C8.5 脚本就绪:** bc_train.py（CrossEntropy 11 分类，网络与 PPO Net 一致）；
train_wsl2_ppo_v2.py 已加 BC 权重加载（fc+actor 初始化，critic 随机）+ blue_adventure_ai=Nullkiller2；
ep_runner_one.py 加 blue_adventure_ai 透传；strategic_env.py 加 reward_explore 探索奖励。

**过程中修复 5 个阻塞**（训练 07-29 死后遗留）:
1. MLClient.h 被重构为 33 字段 string 版，但 libmlclient.so 实际是 28 字段 IModel* 版（gdb ptype 证实）→ connector 编译失败。恢复 28 字段 + init_vcmi(void*) 声明（三副本同步）
2. connector_v13.so 本体丢失（rel/ 只剩 .current/.bak）→ 重建（712928 bytes）
3. adventureAlliedAI/EnemyAI 被 C8.1 全改 Nullkiller2 → red 也被 NK2 接管，模型注入断 → AlliedAI 恢复 MMAI
4. build 目录 ServerPlugin::startBattleHook 要求双英雄，NK2 打野 abort → 野怪保护（项目源已有，build 旧版补齐）
5. Client.cpp 强制 player 0 = MMAI（onlyai 下无 human → 全走 EnemyAI）

**关键机制（见知识库）**: VCMI AI 分配 = aiNameForPlayer(ps.name > adventureAlliedAI/EnemyAI by alliedToHuman)；onlyai 移除 host human；Client.cpp 编进 vcmiclientcommon → **libmlclient.so**（非 vcmiserver）

**遗留**: For Sale.h3m 是 3 人图（tan 也 NK2，拖慢回合）；obs 不含敌方英雄（callback 只读自己，已知限制）

### 1v7 需求讨论（架构评估）

**讨论结论：** 当前架构到 1v7 需扩展 25 项需求（分五大类），完整清单见 `总任务.md` 附录。

**根本限制（三个必须解决）：**
1. 态势感知—缺地图探索、敌方感知、全英雄状态
2. 记忆—MLP 无记忆，需 LSTM/RNN
3. 动作空间—需切英雄、进城镇

**当前阶段（C8 BC）：** 先让模型学会走路和方向选择，其他后续。

---

## C8.3-C8.5 执行推进 ✅ (2026-08-01 夜)

**采集卡死修复 → 数据齐 → BC 出权重 → PPO 微调启动**

| 时间 | 事件 |
|------|------|
| 00:19 | ep1 子进程卡死 1h27m（NK2 chain 死循环，reset 阶段无超时兜底） |
| 01:49 | 修复: collect_bc.py 加 `--watchdog 900`（subprocess.run timeout, kill 跳局）+ 日志落盘，重启采集 |
| 02:58 | 采集完成: 24 局 435 pairs，动作覆盖 9 类（0-8+10），day 1~6，每局 5~62 pairs |
| 03:08 | bc_train.py 出 bc_model.pt（best_val=5.4，预测覆盖多方向不塌缩） |
| 03:09 | C8.5 启动: 删旧 wsl2_model_state.pt（抢 BC 加载路径）→ MAPS 换 2 人图 + blue=NK2 + reward_explore=1.0 → train_loop.sh |

**关键配置（C8.5）:**
- BC_PATH 存在 → fc+actor 用 BC 权重，critic 随机（strict=False）
- MAPS: Dungeon Keeper / Key to Victory / Good Witch, Bad Witch / Fort Noxis（2 人图无 tan 拖慢）
- blue_adventure_ai=Nullkiller2, reward_explore=1.0（新格子 +1）
- 1000eps×200steps×batch128, GAE λ=0.9, LR=5e-5, grad_clip=1.0

---

## C8.5 训练卡死根因修复链 🔶 (2026-08-01 夜~晨, 战斗集成前置阻塞)

**现象**: 训练 ep_steps=1 (模型第一步后 VCMI 卡死/崩溃, 对局无法展开)

**根因链** (逐层定位):
1. vcmi_timeout=15 对 NK2 回合太短 → 120
2. MMAI_MODEL moveHero 异步未实现就 endTurn → 加 2s 位置轮询等待
3. combatEnemyAI/NeutralAI 设置在 "server" 无效路径 → 改 "ai" + 全 MMAI 体系
4. libStupidAI.so ABI 不匹配 (无 GetNewAI) → 弃用内置战斗 AI
5. Router::battleStart neutral 无 playerID → ASSERT 抛异常崩溃 → neutral 处理 + try-catch
6. 战斗结果 CBattleDialogQuery AI 不回答 → IFML(true,false) 禁用
7. WSL 内存崩溃 → .wslconfig memory=6GB

**当前状态** (08:41 重启训练):
- 战斗能打赢 (BattleAI 自动裁决, "I won" 日志)
- ep_steps 2~4 (仍偶发卡死: 战斗后/Server 拒绝 action)
- 训练运行中, watchdog 20min 监控

**遗留**: 战斗结束偶发卡死 (server "Player is not allowed" / 战斗后清理) — Phase D 战斗集成前需解决

## C8.5 训练恢复 🔶→✅ (2026-08-01 09:22, NK2 内存炸弹弃用)

**根因**: NK2 (Nullkiller2) 单局内存 3.7-7.5GB (1 分钟内 430MB→3.7GB 膨胀) → WSL 8GB 限制 OOM 崩溃 → 训练无法运行
- 验证: blue=EmptyAI 430MB 正常 / blue=MMAI_RANDOM 493MB 正常 → NK2 确凿元凶
- NK2 settings (nk2ai-settings.json) 无解 (openMap 已全关, bucket 小) — Phase D 需深挖 NK2 分析内存

**方案**: blue 对手 = MMAI_RANDOM (自动随机行动, 内存 493MB, 每步 2s)
- ep_runner_one.py: blue=args.blue_ai or "StupidAI" (新 --blue_ai 参数)
- train_wsl2_ppo_v2.py: --blue_ai MMAI_RANDOM --blue_adventure_ai MMAI

**训练状态** (09:22): ✅ 健康运行 — ep_steps=200 (满步) r=1482, 每局 ~50s, 内存 6GB available
- ⚠️ 模型塌缩迹象: 前 20 步 act=8 (交互) 后 180 步 act=10 (END_TURN) — BC 后 PPO 微调监控中
- 遗留: NK2 内存问题 (Phase D 前置), 模型 END_TURN 倾向 (奖励/探索耗尽)

## C8.5 结论: 奖励修复 + 模型塌缩分析 (2026-08-01 10:00)

**奖励 bug 修复**: reward_gold_mult 0.01→0 (被动 gold 每步+5~10 → END_TURN 坚守)
- 效果: 模型开始行动 (act=5 移动×6, act=8 交互×10 后再 END_TURN) — 不再全程 +10 白拿

**塌缩残留分析** (act=[8,10×199]):
- 交互 (act=8) +2.9 (英雄升级 exp×0.001) — 免费正奖励 → 模型拿一次就 END_TURN
- 移动探索 +1/新格 但基线/MP 限制 → 移动奖励≈0
- 对局 200 步固定, END_TURN 不结束 → -0.1/步 是最低损耗 → 模型刷到底
- 根因: PPO 无 KL 约束偏离 BC + 无事件奖励 (占矿/杀敌) + 无结束机制

**Phase D 依赖** (1v7 前置):
- strategic_state 扩展: 矿归属追踪 (占矿+10), 战斗结果 (杀敌+100), 敌方位置 (#2)
- PPO KL 约束 BC 或行为克隆 warm-start 保护
- 结束机制/终局奖励强化 (reward_win=200 已存在但模型无法达成)
