# HoMM3 RL 项目知识库

## 项目目标
AI 通过强化学习控制 HoMM3 真实游戏，最终实现 1v7 电脑对战。
部署路线：VCMI Windows AI DLL（非 HD Mod），走引擎内部接口。

## 双轨架构

### Track 1: 训练 🟢
- WSL2 Ubuntu + RTX3060 GPU
- `train_wsl2_ppo_v2.py` — PPO 主脚本（GAE λ=0.95 + grad_clip=1.0）
- `train_loop.sh` — 24/7 循环守护，crash 自动重启
- `ep_runner_one.py` — 每局执行器（子进程方式）
- 参数：STEPS_PER_EP=200, BATCH=128, N_EPISODES=1000, 110 张 H3M 地图
- 对手池：70% 当前 / 20% 早期 / 10% 随机

### Track 2: 真实游戏部署 🟡（已延期）
- VCMI Windows AI DLL（PpoModelAI.dll）加载 ONNX 模型推理
- ONNX 导出已验证（wsl2_model.onnx, 205KB, 推理 <5ms）

## 关键架构发现

### 动作映射 BUG（2026-07-26 发现，修复中）
**根因：** 动作 int 从 Python 发送到 C++ 后从未被读取。
- Python 写 `s_turn_action` → C++ `AAI::yourTurn()` 从不读它，始终 `endTurn()`
- 方向 0-7 全部等效于结束回合，英雄从不移动

**尝试过的修复方案：**
1. **`adventure_read_action()` 函数** — 跨 .so 符号解析，C++ 读 libmlclient.so 的函数 ✅ 理论正确
2. **`dlsym(RTLD_DEFAULT)`** — 替代函数查找方式 ✅ 理论正确
3. **`g_strategic_state->action` struct 字段** — Python 写入共享内存，C++ 读取 ✅ 理论正确
4. **`cb->moveHero()`** — 在 MMAI 回调内调用 → ❌ segfault（可能因为回调链中调用 game action 不合法）

**结论：** 通过修改 MMAI `yourTurn()` 回调来调用 `moveHero` 的方案不可行。需要换技术路线：
- 在 connector/Python 层直接控制英雄移动（不走 AI 回调）
- 或修改 `adventure_process_turn()` 层（同一 .so，无跨库问题）


### 通信链路
```
Python step(action)
  → _adventure_wait()
    → ctypes adventure_wait_for_turn()
      → while (s_turn_player < 0) sleep()    # 等 C++ 回调
  → C++ yourTurn 触发
    → adventure_process_turn()
      → s_turn_player = playerColor          # Python 的 spin-loop 退出
      → while (!s_turn_action_ready) sleep() # 等 Python 发动作
  → Python _send_action(action)
    → ctypes adventure_send_action(action)
      → s_turn_action = action, s_turn_action_ready = true
  → C++ spin-loop 退出
  → yourTurn() 继续（当前问题：不读 s_turn_action）
```

## 训练状态（当前轮次）
- ROUND 2（2026-07-25 20:37 启动）
- 从 step=8623 续训，N_EPISODES=1000
- 当前约 ep=660/1000, ~4.5h 完成
- 后续每轮 ~6h（1000ep × 200steps × ~22s/ep）

## 奖励函数
- 步罚: -0.1/步
- 金币变化: ×0.01
- 木材/矿石: ×0.005
- 稀有资源: ×0.02
- 占城: +30/座
- 招英雄: +10/个
- 英雄经验: ×0.001
- 胜利: +200, 失败: -200
- 全部 clip 到 [-10, 10]

## 评估机制
- 训练每 20min 显示最后 20 条日志（后台 terminal notify）
- round 完成时自动分析 avg_r, vloss, 负奖励比例
- TODO: 建立评估子 agent 自动评估机制

## 开发方向
1. ✅ 修复动作映射（s_turn_action 未被读取）
2. ⬜ 验证修复后训练稳定性
3. ⬜ C6.3: Reward 工程（探路奖、兵力成长奖）
4. ⬜ C6.4: 500 步 / 1v7 评估
5. ⬜ C5.3: 多玩家 StrategicEnv（3+ 玩家）
6. ⬜ C5.4: 多英雄管理
7. ⬜ E6: PPO Model AI DLL（VCMI Windows）

## 关键文件
- `D:\Bigdata\hero3_fresh\` — 项目根
- `train_wsl2_ppo_v2.py` — PPO 训练主脚本
- `ep_runner_one.py` — 单局执行器
- `strategic_env.py` — Gym 环境
- `train_loop.sh` — 24/7 循环
- `vcmi\AI\MMAI\AAI\AAI.cpp` — MMAI AI 实现（含 yourTurn bug）
- `vcmi\ML\strategic_state.cpp` — 原子通信层
- `vcmi\ML\strategic_state.h` — 状态结构体定义
- `vcmi_gym\connectors\v13\threadconnector.cpp` — C++ connector
- `scripts\training_report.py` — 训练报告脚本
