# HoMM3 RL 项目知识库

## 项目目标
AI 通过强化学习控制 HoMM3 真实游戏，最终实现 1v7 电脑对战。
部署路线：VCMI Windows AI DLL（非 HD Mod），走引擎内部接口。

## 双轨架构

### Track 1: 训练 🟢
- WSL2 Ubuntu + RTX3060 GPU
- `train_wsl2_ppo_v2.py` — PPO 主脚本（GAE λ=0.95 + grad_clip=1.0）
- `train_loop.sh` — 24/7 循环守护，crash 自动重启
- `ep_runner_one.py` — 每局执行器（子进程方式）
- 参数：STEPS_PER_EP=200, BATCH=128, N_EPISODES=1000, 110 张 H3M 地图
- AI 配置：red=MMAI（真实 MMAI，含 g_adventure_cb）, blue=MMAI_USER/对手池

### Track 2: 真实游戏部署 🟡（已延期）
- VCMI Windows AI DLL（PpoModelAI.dll）加载 ONNX 模型推理
- ONNX 导出已验证（wsl2_model.onnx, 205KB, 推理 <5ms）

---

## 动作映射修复（C7）完整记录

### 根因
`s_turn_action` 从 Python 写入后从未被 C++ 读取。AI 的 `yourTurn()` 始终调用 `cb->endTurn()`，
方向动作 0-7 全部等效于结束回合，英雄从不移动。

### 尝试过的方案

| # | 方案 | 结果 | 根因 |
|---|------|------|------|
| 1 | AIFactory 内调 `cb->moveHero()` | ❌ segfault | 回调链中调 game action 不合法 |
| 2 | asyncTasks->run() + moveHero | ✅ 编译通过，运行中 | MMAI 已有 AsyncRunner（与 Nullkiller2 相同） |
| 3 | struct 字段 `g_strategic_state->action` | ❌ ABI 错位 | 改 struct 尺寸需全量重编，vcmiservercommon 有旧 .o |
| 4 | 独立全局变量 `g_rl_action` | ✅ 已编译运行 | 不改变 struct 尺寸，Python ctypes 直写 |
| 5 | `dlsym(RTLD_DEFAULT)` 查 `strategic_state_update` | ❌ 跨库不可靠 | RTLD_DEFAULT 在不同 .so 中搜索范围不同 |
| 6 | `extern "C"` 直调 `strategic_state_update` | ✅ 已编译 | 链接器直接解析符号 |

### 当前阻塞：g_strategic_state NULL
- `strategic_state.cpp` 被编译进 **libmlclient.so**（mlclient.dir）和 **libvcmiservercommon.a**（vcmiservercommon.dir）
- 两者都被链接进 libmlclient.so，但 `g_strategic_state` 变量有两个独立副本
- `init_vcmi()`（MLClient.cpp）分配了副本 A
- `strategic_state_update()` 检查副本 B → 为 NULL → 直接 return
- **解法：** 在 `strategic_state_update` 里如果 NULL 就 self-allocate（不依赖 init_vcmi）

### 通信链路（当前）
```
Python step(action)
  → _send_action(action)
    → ctypes 写 g_rl_action = action
    → ctypes 调 adventure_send_action(action)
  → C++ yourTurn 触发
    → extern "C" strategic_state_update()    # 直调，不 dlsym
    → if (g_adventure_cb) g_adventure_cb()   # spin-wait
    → 读 g_rl_action
    → asyncTasks->run([...]()) { selectionMade + moveHero + endTurn }
```

---

## 关键踩坑点

### 1. struct 尺寸不可改
`StrategicState` 的 sizeof 是跨 .so ABI 契约。加字段会导致全量编译失败（vcmiservercommon 的旧 .o 与新尺寸不匹配）。
**解法：** 新数据用独立全局变量（`g_rl_action`），不放在 struct 内。

### 2. extern "C" vs extern
`extern "C"` 使用 C 符号名（`strategic_state_update` 不变）
`extern`（无 "C"）使用 C++ mangled 名（`_Z22strategic_state_updatePv`）
头文件声明 `extern "C"`，但如果在函数体内重新声明为 `extern`（无 "C"），链接器找 mangled 名会失败。
**解法：** 始终 `extern "C"`。

### 3. dlsym 跨库不可靠
`dlsym(RTLD_DEFAULT, ...)` 搜索范围因调用者而异：
- 从 Python（主程序）调 → 搜全局表 + 所有 RTLD_GLOBAL 库 → ✅ 找到
- 从 libvcmi.so 调 → 搜 libvcmi 视角 + 依赖库 → ❌ 可能没加载到
**解法：** 用 `extern "C"` 直调替代 dlsym。

### 4. cb->moveHero() 在回调链中 segfault
`yourTurn()` 内调 `cb->moveHero()` 导致 SIGSEGV，因为 VCMI 在 query 回调上下文中禁止 game action。
Nullkiller2 通过在回答 query（`selectionMade`）后用 `asyncTasks->run()` 在 TBB 线程上执行来规避。
**MMAI 已有相同的 AsyncRunner 框架**（AAI.h:39 → std::make_unique<AsyncRunner>()）。

### 5. AIS 列表 vs AIFactory
- `MLClient.h` 的 `AIS` 列表用于 Scripted（战斗 AI）校验，不控制冒险 AI
- 冒险 AI 在 `AIFactory::createAdventureAI()` 中创建
- `MMAI_USER` 不匹配 `"MMAI"` → fallthrough 到 CEmptyAI（不调 g_adventure_cb）
- **修复：** AIFactory 加 `|| name == "MMAI_USER"` 来创建真实 MMAI

### 6. 三件套 .so 必须同源编译
`libmlclient.so` + `libvcmi.so` + `connector_v13.so` 必须在同一 cmake 配置下编译。
单独重编任意一个可能导致符号解析失败或 ABI 不匹配。

### 7. VCMI 数据加载链
Mods/core → ~/.local/share/vcmi/Data/ → /mnt/d/GAMES/Heroes3/Data/（LOD 文件）
核心文件：H3bitmap.lod（含 LCDESC.TXT 等文本资源）
Mods 目录通过 `rel/bin/Mods` 符号链指向 `vcmi-native/Mods`

### 8. 训练进程与手动测试环境不同
- 训练子进程由 `subprocess.Popen(cmd, stdout=DEVNULL, stderr=DEVNULL)` 启动
- 手动测试时加载相同的 .so 和环境变量 → 仍崩溃
- **根因不明** — 可能与 WSL shell 初始化顺序或权限有关

---

## 通信链路（原始/未修复）

```
Python step(action)
  → _adventure_wait()
    → ctypes adventure_wait_for_turn()
      → while (s_turn_player < 0) sleep()    # 等 C++ 回调
  → C++ yourTurn 触发
    → adventure_process_turn()
      → s_turn_player = playerColor          # Python 的 spin-loop 退出
      → while (!s_turn_action_ready) sleep() # 等 Python 发动作
  → Python _send_action(action)
    → ctypes adventure_send_action(action)
      → s_turn_action = action, s_turn_action_ready = true
  → C++ spin-loop 退出
  → yourTurn() 继续（BUG：不读 s_turn_action，直接 endTurn）
```

---

## 训练状态
- 当前：ROUND N（2026-07-26），red=MMAI，新 .so 带 asyncTasks + extern C 直调
- g_rl_action 写入正常，g_strategic_state 仍 NULL → 观测全零
- avg_r=0.0（null 观测导致模型输出相同动作 act=[8]）
- 每局 ~8-10s（vs 旧版 ~3-4s，说明 VCMI 在真实运行）

---

## 奖励函数
- 步罚: -0.1/步
- 金币变化: ×0.01
- 木材/矿石: ×0.005
- 稀有资源: ×0.02
- 占城: +30/座
- 招英雄: +10/个
- 英雄经验: ×0.001
- 胜利: +200, 失败: -200
- 全部 clip 到 [-10, 10]

---

## 开发方向
1. 🔴 **C7.6: 修 g_strategic_state NULL** — strategic_state_update self-allocate
2. ⬜ C7.7: 验证英雄移动（ep 步数 > 1）
3. ⬜ C7.8: 恢复 20min 报告循环
4. ⬜ 正常训练（修复后 avg_r 应有正趋势）
5. ⬜ C6.3: Reward 工程
6. ⬜ C6.4: 500 步 / 1v7 评估
7. ⬜ E6: PPO Model AI DLL

---

## 关键文件
| 文件 | 用途 |
|------|------|
| D:`\Bigdata\hero3_fresh\` | 项目根 |
| `train_wsl2_ppo_v2.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单局执行器 |
| `strategic_env.py` | Gym 环境（含 g_rl_action 写入） |
| `train_loop.sh` | 24/7 循环 |
| `vcmi\AI\MMAI\AAI\AAI.cpp` | MMAI yourTurn（asyncTasks + extern C 直调） |
| `vcmi\ML\strategic_state.h/cpp` | 共享状态 + g_rl_action 全局 |
| `vcmi\ML\MLClient.h` | AIS 列表（含 MMAI + Nullkiller2） |
| `vcmi\lib\callback\AIFactory.cpp` | MMAI_USER→真实 MMAI 路由 |
| `vcmi_gym\connectors\v13\threadconnector.cpp` | C++ connector（设 g_adventure_cb） |
| `strategic_reader.py` | ctypes StrategicState 绑定 |
| `评估方案.md` | 三种技术路线对比 |
| `fix_action_mapping.md` | 调试全记录 |
| `当前任务清单.md` | 追踪 C7 进度 |
| `总计划.md` | 三阶段路线图 |
