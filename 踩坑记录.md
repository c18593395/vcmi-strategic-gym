# HoMM3 RL 踩坑记录

## 1. struct 尺寸不可改

**现象：** 在 `StrategicState` 中加入 `int32_t action;` 字段后全量 `make` 编译失败。
**根因：** `strategic_state.cpp` 被编译进 `libmlclient.so`（mlclient.dir）和 `libvcmiservercommon.a`（vcmiservercommon.dir）。
旧版 `vcmiservercommon.a` 使用旧 `sizeof`，链接到新 `libmlclient.so` 后 ABI 错位 → 内存访问越界 → VCMI 初始化崩溃。
**解决：** 新数据用独立全局变量，不修改 struct。

## 2. extern "C" 区分 C/C++ mangling

**现象：** 在函数体内 `extern void strategic_state_update(void*);` 链接失败。
**根因：** `extern`（无 "C"）用 C++ mangling（`_Z22strategic_state_updatePv`），
但目标函数在头文件中用 `extern "C"` 声明（无 mangling）。符号名不匹配。
**解决：** 用 `extern "C" void strategic_state_update(void*);`

## 3. dlsym 跨库搜索范围

**现象：** `dlsym(RTLD_DEFAULT, "strategic_state_update")` 从 Python 调能找到，
但从 libvcmi.so 调返回 NULL。
**根因：** RTLD_DEFAULT 搜索从调用模块的视角进行。Python 主程序看到所有
RTLD_GLOBAL 库，但 libvcmi.so（作为依赖库被加载）的搜索范围可能不包括
后加载的 libmlclient.so。
**解决：** 用 `extern "C"` 直调替代 dlsym。

## 4. cb->moveHero() 在回调链中 segfault

**现象：** 在 `AAI::yourTurn()` 内调 `cb->moveHero()` 产生 SIGSEGV。
**根因：** VCMI 禁止在 query 回调上下文中调用 game action。回答 query
（`selectionMade`）前调用 `moveHero` 触发了非法状态。
**解决：** 先 `cb->selectionMade(0, queryID)` 回答 query，然后通过
`asyncTasks->run()` 在 TBB 线程上异步执行 `moveHero`（与 Nullkiller2 相同模式）。

## 5. MMAI_USER 路由到 CEmptyAI

**现象：** `red="MMAI_USER"` 时英雄不移动，不调 `g_adventure_cb`。
**根因：** AIFactory 中 `if(name == "MMAI")` 不匹配 `"MMAI_USER"`，fallthrough 到 CEmptyAI。
CEmptyAI 不调用 `g_adventure_cb()`，导致 Python 侧的 `_adventure_wait()` 一直超时。
**解决：** `if(name == "MMAI" || name == "MMAI_USER")`。

## 6. 训练子进程与手动测试结果不一致

**现象：** 同样的 .so 和环境变量，训练子进程工作但手动测试 segfault。
**分析：** 训练子进程用 `subprocess.Popen` 启动，stdout/stderr 全通到 /dev/null。
手动测试从 WSL shell 启动，可能有不同的 PATH/环境变量或工作目录。
**教训：** 始终用训练脚本（ep_runner_one.py）来验证，不要直接调 StrategicEnv。

## 7. VCMI 数据加载顺序

**现象：** `GameLibrary init failed (no game data)` 后 VCMI 继续初始化仍崩溃。
**根因：** `modSettings.json` 中 `"core"` 作为 mod 但不存在对应目录。VCMI
通过 `~/.local/share/vcmi/Data/` → `/mnt/d/GAMES/Heroes3/Data/` 链式路径加载。
如果 `.local` 目录损坏或 LOD 文件被替换，初始化失败。
**注意：** `.local/share/vcmi/Data/` 下的文件是 Windows D: 盘游戏文件的 symlink。
在 WSL 中修改这些 symlink 会影响 Windows 侧的访问。

## 8. cmake 重建覆盖 Mods 符号链接

**现象：** 运行 `cmake .. && make` 后 VCMI 的 Mods 数据目录不可用。
**根因：** cmake 重新创建了 `rel/bin/Mods` 符号链接，使其指向
`vcmi-native/Mods`（缺少 core mod）而非原来的 `hero3_vcmi/build/bin/Mods`。
**解决：** 手动修正符号链接。只在 `make mlclient vcmi` 时不必跑 `cmake ..`。

## 9. ext4 大小写敏感 + Data/DATA 路径

**现象：** 7/19 备份的 .so 能跑，fresh build 的 libvcmi.so 在 `debugStartTest` 内 abort（DATA/LCDESC not found）。
**根因：** filesystem.json 使用大写首字母路径 `Data/H3bitmap.lod`，但实际目录是 `DATA/`（全大写）。
ext4 下 `Data` ≠ `DATA`。fresh build 的 libvcmiclientcommon.a 严格检查资源存在，7/19 备份版跳过。
**解决：** `ln -sf /mnt/d/GAMES/Heroes3/Data ~/vcmi-native/data/Data`（直接 symlink，不要 DATA→Data 套娃导致循环引用）。

## 10. LCDesc.txt ≠ LCDESC.TXT

**现象：** VCMI 初始化时报 `Resource DATA/LCDESC and type TEXT not found` 然后 std::terminate。
**根因：** 中文版 Heroes3 的 `LCDesc.txt`（小写c）不在 .lod 内。VCMI 通过资源系统查找 `LCDESC.TXT`（大写），ext4 大小写敏感 → 找不到。
7/19 备份版在旧 GCC 版本下 noexcept 行为不同，silent skip；fresh build 在 GCC 13.3 下变成 std::terminate。
**解决：** `cp LCDesc.txt LCDESC.TXT`。

## 11. C++ ABI 新旧混用导致 undefined symbol

**现象：** 新编 libmlclient.so 链接 7/19 libvcmi.so 时报 `undefined symbol: JsonNode::String()` 或 `g_adventure_cb_userdata`。
**根因：** 7/19 备份用旧 C++11 ABI（`Ss` = `std::string`），fresh build 用新 ABI（`__cxx11::basic_string`）。新旧 string 内存布局不同 → 符号不匹配。
**解决：** 要么全用旧 ABI (`-D_GLIBCXX_USE_CXX11_ABI=0`)，要么全用新 ABI。不要混用。7/19 备份用新 ABI（`__cxx11`），所以 fresh build 也用默认新 ABI。

## 12. strategic_state.cpp 缩水版缺少英雄/城镇填充

**现象：** 观测 256 维中 184 维全零（英雄/城镇数据）。
**根因：** 本地 `strategic_state.cpp` 的英雄/城镇填充循环被删了，只剩 `heroes[0].id = -1` sentinel。
**解决：** 从服务器版 COPY 回 hero/town 遍历循环 + getHeroes()/getTowns() + pos_x/y/z 填充。

## 13. adventure_process_turn 不区分玩家 → 蓝方死锁

**现象：** 红/蓝方都走 adventure_process_turn spin-loop 等 Python action。Python 只给红方发 action，蓝方永远等不到 → VCMI 不推进。
**根因：** 缺少 `if (playerColor != 0) return;` — 只应拦截红方 (player 0)。
**解决：** `adventure_process_turn` 入口加 `if (playerColor != 0) return;`。

## 14. endTurn/selectionMade 顺序与 waitTillRealize

**诊断经历：**
- 怀疑 selectionMade 必须在 endTurn 之后（7/19 顺序） → 实测无影响
- 怀疑 waitTillRealize=false 导致网络包不发 → 实测无影响
- 最终确认：蓝方 adventure_process_turn 死锁才是根因（#13）

## 15. dlsym vs extern "C" 直调

**现象：** `dlsym(RTLD_DEFAULT, "strategic_state_update")` 在 libvcmi.so 被加载时找到的是旧版符号（如果存在两处定义）。
**推荐：** 用 `extern "C"` 直调替代 dlsym。链接器保证正确性。libvcmi.so 未设 `--no-undefined`，允许未解析符号通过。
