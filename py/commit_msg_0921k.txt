fix: scorer 同目标 pick 计数拉黑 — 修复实战不彻底问题 (-909 局复盘)

首版修复 (8303ca2) 实战复盘: -909 局死循环依旧 (同 pick (10,13) 100+ 拍),
[GUIDE_OSC_DROP] 零触发。根因: 横跳实际形态 = 3 连发 + 多格晃 (非严格 [3,7] 对拍),
zigzag prev2 判定与 move_stall 距离启发式都抓不全。

终版锚点: 同目标累计 pick 计数 (ts_pick_count) >= 8 → ts_blocked 拉黑 —
不依赖任何位置启发式, 直接锚定 "同目标长时间完不成"。plen=1 完不成 = 引擎拒入该格。

冒烟 (同图 72_02): [GUIDE_OSC_DROP] step28 + [SCORE_BLACK] ×3 (step52/81/139 逐死格拉黑)
→ 190 步正常收局 r=-393 (修复前 -803/-909 满步死循环)。训练已重启加载终版。
