# HoMM3 全盘操盘 AI — 总任务

> ⚠️ **遇到问题先查知识和踩坑点：** `memory` 存了关键的踩坑记录（headless 不进回合、ctypes/CDLL 实例、DummyVecEnv 短路等），`skill vcmi-arm64-training` 有完整修复方案和验证命令。先看这些再分析，避免反复踩坑。**不要一上来就改代码。**

> 最终目标：AI 完整操控真实游戏，与你对战（1v7），并战胜你。
> 策略：VCMI 训练战略模型 → 真实游戏内存操控 → 自对弈持续进化。

---

## 一、架构方案

### 两段式设计（2026-07-19 重定）

```
训练端 (WSL2):  /home/administrator/vcmi-workspace/, MMAI ON, RTX 3060
推理端 (ARM64): root@172.16.2.40, 不需要 callback, ctypes 直读状态 + ONNX 推理
部署端 (真实游戏): 本机 HD Mod, 内存读取 + PostMessage, 模型推理
```

- **训练端负责：** PPO 自对弈训练战略模型，出 ONNX
- **推理端负责（其他人）：** ARM64 加载 ONNX 模型，ctypes 读 `g_strategic_state` → 推理 → `adventure_act` 执行
- **部署端：** 后续 phase，不依赖训练端的 callback 机制

### 双轨并行策略（2026-07-20 定）

```
┌──────────────────────┐   ┌──────────────────────────┐
│  Track 1: 训练 🟢    │   │  Track 2: 真实游戏对齐 ⬜ │
│  持续提升模型强度      │   │  模型落地到真实 HoMM3    │
│  WSL2 GPU 后台跑      │   │  Windows 内存读取验证    │
│  不占交互注意力        │   │  需主动执行              │
└──────────────────────┘   └──────────────────────────┘
```

说 **"按当前任务清单去训练"** → Track 1
说 **"按当前任务清单去测试实际游戏"** → Track 2

两者完全独立，训练后台永不中断。

### 决策分层

```
战略层 RL (PPO) → 决定移动/探索/占矿/攻城
  ↓ 遇敌
战斗裁决 → 自动战斗(AI 代打) 或 v13 战斗模型（后续 phase）
```

**关键原则：战略层训练只使用 H3M 经典图，不用 vmap。** vmap（如 A1.vmap、adventure-A1.vmap 等）是 v13 战斗模型的训练图，不是战略图。两款训练数据和环境不互通。

### 自对弈架构

```
Round 1:   v1(红) vs v1(蓝) → 探索规则
Round 50:  v50(红) vs v48(蓝) → 持续进步
Round 500: v500(红) vs v495(蓝) → 高手对决
```

- 对手池保留最近 10 个版本，70%最新 + 20%早期 + 10%随机
- ELO 追踪，每 50 轮评估
- 红蓝对称学习

### 观测与动作

- 观测 ~256 维：英雄位置/兵力/移动力、城镇/资源、敌方可见信息、日期
- 动作：移动(8方向) + 交互 + 城镇操作 + 全局(结束回合)
- AI 对手使用 Nullkiller2（冒险 AI），不耗 ML 推理

---

## 二、总进度

```
████████████████████░░ 75%  当前: C6 大地图适配（STEPS_PER_EP 50→200） + C4 训练运行中
```

|| | Phase | 目标 | 状态 | 负责 |
|| |-------|------|------|------|
|| | A | VCMI 冒险地图 API | ✅ 完成 | 我 |
|| | B | 战略 Gym 环境 | ✅ 完成 | 我 |
|| | C | 自对弈 PPO 训练 | ✅ WSL2 跑通（V2 稳定运行） | 我 |
|| | - | C4 长程训练 | 🟢 运行中 (V2, ~5%) | 后台 |
|| | - | C5 升级 (多玩家/强对手) | ✅ 基本完成 | — |
|| | - | **C6 大地图适配** | **🟡 进行中（STEPS_PER_EP 50→200）** | — |
|| | E | 真实游戏对齐 | ⬜ Track 2 | — |
|| | D | 战斗集成 | ⬜ | — |
|| | F | 你 vs AI (1v1) | ⬜ | — |
|| | G | **1v7 战胜你** | ⬜ | — |

---

## 三、Track 1: 训练（Phase C）🟢

### ✅ Phase A: VCMI 冒险地图 API ✅ 95%

[原有内容保留]

### ✅ Phase B: 战略 Gym 环境 ✅ 完成

[原有内容保留]

### ✅ C3/C4/C5 已完成任务

| # | 任务 | 说明 |
|---|------|------|
| C1 | 扩规模训练 | 500ep×20步, batch=64 → 验证通过 |
| C2 | GPU 加速 | device=cuda, RTX3060 |
| C3.1 | Checkpoint 保存 | 每 50 step, 保留 10 个 |
| C3.2 | 对手池 | 70/20/10 选择策略, --blue_model 参数 |
| C3.3 | ELO 评估脚本 | eval_elo.py, 红蓝轮换 |
| C4.1 | 全地图扫描 | 110/158 张可用 H3M |
| C4.2 | 参数升级 | 1000ep×50步×128batch, 110 张图 |
| C4.3 | 长程训练启动 | train_loop.sh, 后台进程运行中 |
| C5.1 | 多玩家地图扫描 | 158 张图, 2-8p 分布确认 |
| C5.2 | AI 对手升级 | Nullkiller2 king 难度 |
| C5.5 | ELO 基线评估 | 脚本完成, 已跑 3 次基线 |
| C5.6 | 基线评估 | R2模型50% / 当前最佳50% / vs King NK=0% |
| **C6.1** | **STEPS_PER_EP 50→200** | **2026-07-25, 适配大地图 1v7 目标** |

### 🟡 C4/C6 训练运行参数

```
1000ep × 200steps × batch=128  |  maps=110  |  device=cuda  |  脚本: V2 (train_wsl2_ppo_v2.py)
GAE λ=0.95  |  grad_clip=1.0  |  LR=5e-5
对手池: 70%当前 / 20%早期 / 10%随机
对手: MMAI_USER（红蓝自对弈）
Checkpoint: 每 50 step -> checkpoints/wsl2_ckpt_{step}.pt
进程: WSL 后台  |  监控: cron training-report 每20min
```

### 🟡 C6: 大地图适配（进行中）

| # | 任务 | 说明 | 难度 | 状态 |
|---|------|------|------|------|
| C6.1 | **STEPS_PER_EP 50→200** | 增加每局步数，让模型见到中期游戏 | 🟢 低 | ✅ 2026-07-25 已改 |
| C6.2 | 200 步首轮验证 | 观察稳定性（无 NaN、不崩） | 🟢 低 | ⬜ 等下一轮重启 |
| C6.3 | Reward 工程 | 探路奖、兵力成长奖 | 🟡 中 | ⬜ 等待 |
| C6.4 | 500 步 / 1v7 评估 | 根据 200 步效果决定 | 🟡 中 | ⬜ 等待 |

### ⬜ C5: 训练升级待办

| # | 任务 | 说明 |
|---|------|------|
| C5.1 | 扫描多玩家地图 | 筛 3p/4p/6p/8p 可用图 |
| C5.2 | 升级 AI 对手 | Nullkiller2 → 更强 AI |
| C5.3 | 多玩家 StrategicEnv | 环境支持 1v2, 1v3 |
| C5.4 | 多英雄管理 | 环境支持多英雄 |
| C5.5 | ELO 自动评估 | checkpoint 后自动跑 |

---

## 四、Track 2: 真实游戏对齐（Phase E）⬜

> **目标：** 验证 VCMI 训练的模型能否在真实 HoMM3 HD Mod 中跑通，state mapping 是否对齐

### 整体流程

```
VCMI 训练模型 (.pt)
  ↓
① ONNX 导出 ─── 模型脱离 PyTorch，轻量推理
  ↓
② 读真实游戏内存 ─── HD Mod 进程内存 dump
  ↓
③ 字段对齐 ─── VCMI 观测向量 ↔ 真实游戏内存地址
  ↓
④ 模型推理 + 动作注入 ─── PostMessage 操控窗口
  ↓
⑤ 端到端验证 ─── 10 回合连续运行
```

### ⬜ E1: ONNX 模型导出

| 步骤 | 内容 |
|---|---|
| 1 | 选一个 best checkpoint (.pt) |
| 2 | `torch.onnx.export()` 导出为 .onnx |
| 3 | ONNX Runtime 验证推理结果一致 |
| 4 | 记录推理速度（CPU latency） |

**风险：** 低。当前 Net 是简单 MLP，ONNX 导出无问题。

### ⬜ E2: 真实游戏内存读取

需要确认 HoMM3 HD Mod 的内存结构：

| 字段 | VCMI 观测索引 | 真实游戏内存地址 |
|---|---|---|
| hero_x, hero_y | 0-1 | ? |
| hero_army_count | 2-? | ? |
| gold | ? | ? |
| wood/ore | ? | ? |

**方法：** ReadProcessMemory + 已知 HoMM3 偏移表 → 跟 VCMI 同条件比对

### ⬜ E3: 状态注入验证

| 步骤 | 内容 |
|---|---|
| 1 | VCMI 开 Key to Victory，reset 后 dump 观测向量 |
| 2 | 真实游戏开同图同条件，读对应内存字段 |
| 3 | 比对数值（允许 ±10% 误差） |
| 4 | 记录对不齐的字段，分析根因 |

**验收标准：** 80% 字段对齐，关键字段（资源、兵力、英雄位置）全部对齐

### ⬜ E4: 模型推理 + 动作注入

| 步骤 | 内容 |
|---|---|
| 1 | 加载 ONNX 模型，读真实游戏状态 → 推理 |
| 2 | 模型输出动作（8方向/交互/结束回合等） |
| 3 | 用 PostMessage 向 HD Mod 窗口发送按键/鼠标 |
| 4 | 验证动作在真实游戏中生效 |

### ⬜ E5: 端到端验证

| 步骤 | 内容 |
|---|---|
| 1 | 0 操作跑 10 回合：读状态→推理→发动作→循环 |
| 2 | 记录回放：每步截图 + 模型输出 |
| 3 | 手动回放检查 |

**验收标准：** 连续 10 回合无 crash，模型动作在真实游戏中生效

---

## 五、历史修复记录

### ✅ 已解决的阻塞问题 (2026-07-19)

详见 `step2分析.md`。

| # | 修复 | 结果 |
|---|------|------|
| 1 | ctypes→connector 迁移 | ✅ |
| 2 | DummyVecEnv 短路移除 | ✅ |
| 3 | MMAI_USER → Nullkiller2 切换 | ✅ |
| 4 | step() WAIT→SEND 顺序修复 | ✅ |
| 5 | MLClient 清理 broken g_cached_game_state | ✅ |
| 6 | CCallback::getGameStatePtr() 公开访问器 | ✅ |
| 7 | AIGateway.cpp 加 adventure callback 钩子 | ✅ |

### ✅ 服务器部署 (2026-07-18)
- vcmi 源码传输 + ARM64 编译
- MMAI=OFF, Nullkiller2 回调钩子
- E2E 验证：reset(NNZ=45)→step(NNZ=60, r=15.9)→step(NNZ=79, r=1.0)

### ❌ 错误：用 vmap 训练战略层（2026-07-19 修复）
- 之前 train_loop.sh 循环 10 张 vmap（adventure-A1, A1-A7, ml-mini, ml-train）
- **都是战斗模型训练图，不是战略图**
- 战略层训练地图只用 **H3M 经典图**

### ✅ strategic_env.py ctypes→connector 迁移 (2026-07-19)
- `_adventure_wait()`, `_send_action()` 改为用 `self.connector.adventure_wait/act`
- 去掉 `_ensure_libml_loaded()` 的 ctypes 模式
- 去掉 DummyVecEnv 短路（`_vcmi_just_started`）
- 避免两个 libmlclient 实例的静态变量隔离

---

## 六、关键踩坑点

1. **step() 必须是 WAIT→SEND 顺序**
2. **AI 引擎必须是 Nullkiller2**（不 MMAI）
3. **改 Python 源码后必须清 pyc 缓存**
4. **ep_runner 必须用 os._exit(0)**
5. **MLClient headless 用 cond_shutdown**
6. **三件套 .so 必须同源编译**
7. **子 agent 已修复**（不再 401），可派发编码任务，但 WSL 编译/执行需主 agent 跑

详见 `step2分析.md`。

---

## 七、关键资产

| | 资产 | 位置 |
| |------|------|
| | ARM64 服务器 | root@172.16.2.40, 64核, /DATA/hero3/ |
|| | 训练脚本（V2） | D:\\Bigdata\\hero3_fresh\\train_wsl2_ppo_v2.py |
|| | 训练脚本（V1） | D:\\Bigdata\\hero3_fresh\\train_wsl2_ppo.py |
|| | StrategicEnv | D:\\Bigdata\\hero3_fresh\\vcmi_gym\\envs\\v13\\strategic_env.py |
|| | 训练循环 | D:\\Bigdata\\hero3_fresh\\train_loop.sh |
|| | 任务清单 | D:\\Bigdata\\hero3_fresh\\当前任务清单.md |
|| | 训练日志 | train_loop.log (WSL) |
|| | H3M 地图 | D:\\Bigdata\\hero3_fresh\\maps\\ (158张) |
|| | 可用地图 | D:\\Bigdata\\hero3_fresh\\available_maps.json (110张) |
|| | Checkpoint | D:\\Bigdata\\hero3_fresh\\checkpoints\\ |
|| | ELO 评估 | D:\\Bigdata\\hero3_fresh\\eval_elo.py |
|| | 模型状态 | D:\\Bigdata\\hero3_fresh\\wsl2_model_state.pt（含优化器） |
|| | 模型权重 | D:\\Bigdata\\hero3_fresh\\wsl2_model.pt |
|| | ONNX 导出 | D:\\Bigdata\\hero3_fresh\\wsl2_model.onnx |
