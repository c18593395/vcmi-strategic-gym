# HoMM3 全盘操盘 AI — 总任务

> ⚠️ **遇到问题先查知识和踩坑点**：`memory` 存了关键的踩坑记录（headless 不进回合、ctypes/CDLL 实例、DummyVecEnv 短路等），`skill vcmi-arm64-training` 有完整修复方案和验证命令。先看这些再分析，避免反复踩坑。**不要一上来就改代码。**

> 最终目标：AI 完整操控真实游戏，与你对战（1v7），并战胜你。
> 策略：VCMI 训练战略模型 → 真实游戏内存操控 → 自对弈持续进化。

---

## 一、架构方案

### 两段式设计（2026-07-19 重定）

```
训练端 (WSL2):  /home/administrator/vcmi-workspace/, MMAI ON, RTX 3060
推理端 (ARM64): root@172.16.2.40, 不需要 callback, ctypes 直读状态 + ONNX 推理
部署端 (真实游戏): 本机 HD Mod, 内存读取 + PostMessage, 模型推理
```

- **训练端负责：** PPO 自对弈训练战略模型，出 ONNX
- **推理端负责（其他人）：** ARM64 加载 ONNX 模型，ctypes 读 `g_strategic_state` → 推理 → `adventure_act` 执行
- **部署端：** 后续 phase，不依赖训练端的 callback 机制

### 决策分层

```
战略层 RL (PPO) → 决定移动/探索/占矿/攻城
  ↓ 遇敌
战斗裁决 → 自动战斗(AI 代打) 或 v13 战斗模型（后续 phase）
```

**关键原则：战略层训练只使用 H3M 经典图，不用 vmap。** vmap（如 A1.vmap、adventure-A1.vmap 等）是 v13 战斗模型的训练图，不是战略图。两款训练数据和环境不互通。

### 自对弈架构

```
Round 1:   v1(红) vs v1(蓝) → 探索规则
Round 50:  v50(红) vs v48(蓝) → 持续进步
Round 500: v500(红) vs v495(蓝) → 高手对决
```

- 对手池保留最近 20 个版本，70%最新 + 20%早期 + 10%随机
- ELO 追踪，每 10 轮评估
- 红蓝对称学习

### 观测与动作

- 观测 ~200 维：英雄位置/兵力/移动力、城镇/资源、敌方可见信息、日期
- 动作：移动(8方向) + 交互 + 城镇操作 + 全局(结束回合)
- AI 对手使用 Nullkiller2（冒险 AI），不耗 ML 推理

---

## 二、总进度

```
████████████████░░░░░░ 55%  当前: Phase C WSL2 多步训练跑通
```

| | Phase | 目标 | 状态 | 负责 |
| |-------|------|------|------|
| | A | VCMI 冒险地图 API | ✅ 完成 | 我 |
| | B | 战略 Gym 环境 | ✅ 完成 | 我 |
| | C | 自对弈 PPO 训练 | 🟡 WSL2 跑通 | 我 |
| | - | 服务器 24/7 训练 | 🟡 交付他人 | 其他人 |
| | D | 战斗集成 | ⬜ | — |
| | E | 真实游戏部署 | ⬜ | — |
| | F | 你 vs AI (1v1) | ⬜ | — |
| | G | 1v7 战胜你 | ⬜ | — |

---

## Phase C: 自对弈 PPO 训练 🟡 WSL2 跑通 (2026-07-19)

### ✅ 已完成关键修复

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 观测全零 | DummyVecEnv 短路 | 删除 `_vcmi_just_started`，reset 始终等 yourTurn |
| 2 | step2 挂死 | SEND→WAIT 顺序导致 s_turn_player 被提前消费 | WAIT→SEND + 切 Nullkiller2（回调后 fall through，不手动 endTurn） |
| 3 | MMAI core dump | 旧 MLClient 含 broken g_cached_game_state 代码 | 清理，用 cond_shutdown 版本 |
| 4 | ABI 不兼容 | vcmi-native/workspace 两套源码混搭 .so | 从 vcmi-native 全量重编三件套 |
| 5 | .pyc 缓存 | NTFS 文件系统 pyc 过期检测不可靠 | 部署后 `find ... -name __pycache__ -exec rm -rf {} +` |
| 6 | subprocess 僵尸 | VCMI 线程阻止 Python 退出 | `os._exit(0)` 硬退出 |

### 训练方案

| 项目 | 内容 |
|------|------|
| AI 引擎 | **Nullkiller2**（AIGateway.cpp 回调 + AIGateway fall through） |
| 对手 | red=MMAI_USER, blue=StupidAI |
| 协议 | WAIT→SEND（step 先等 yourTurn 再回应，和 reset 对齐） |
| 地图 | 8 张 H3M 轮换 |
| 训练脚本 | `train_wsl2_ppo.py` |
| 模式 | subprocess-per-episode（多步，os._exit 清理） |

### 验证结果 (2026-07-19)

```
50 episodes x 5 steps = 250 步
vloss: 769 → 45 (下降 94%)
501s (~8 min)
模型保存: wsl2_model.pt (202KB)
```

### 核心通信机制

```
Nullkiller2::yourTurn()
  → g_adventure_cb(pn, userdata)           // AIGateway.cpp:537
  → adventure_cb_trampoline(pn, userdata)  // strategic_state.cpp
  → adventure_process_turn(pn, userdata)
      s_turn_player = pn                   // 通知 Python
      while !s_turn_action_ready: sleep    // 等 Python 发 action

Python connector.adventure_wait()
  → adventure_wait_for_turn()
      while s_turn_player < 0: sleep       // 等 C++ 设 player
      p = s_turn_player; s_turn_player = -1
      return p

Python connector.adventure_act(action)
  → adventure_send_action(action)
      s_turn_action_ready = true           // 解阻塞 C++ 的 spin
```

所有原子变量定义在 `libmlclient.so`（`strategic_state.cpp`），connector 通过链接器加载同一份 libmlclient，变量共享。

### 🔴 已解决的阻塞问题 (2026-07-19)

详见 `step2分析.md`。

| # | 修复 | 结果 |
|---|------|------|
| 1 | ctypes→connector 迁移 | ✅ |
| 2 | DummyVecEnv 短路移除 | ✅ |
| 3 | MMAI_USER → Nullkiller2 切换 | ✅ |
| 4 | step() WAIT→SEND 顺序修复 | ✅ |
| 5 | MLClient 清理 broken g_cached_game_state | ✅ |
| 6 | CCallback::getGameStatePtr() 公开访问器 | ✅ |
| 7 | AIGateway.cpp 加 adventure callback 钩子 | ✅ |

### 根因深度分析

`ENGINE->mainLoop()` 循环体是：
```cpp
for (;;) {
    input().fetchEvents();      // 等输入事件
    updateFrame();              // engineUser->onUpdate() + handleEvents()
    presentScreenTexture();     // 渲染
    framerate().framerateDelay(); // 锁帧
}
```

headless=true 初始化时，`engineUser` 未设置（null），`mainLoop()` 到 `engineUser->onUpdate()` 时虚函数调用 null 指针 → 未定义行为（ARM64 上可能挂在 `fetchEvents` 或 `presentScreenTexture`）。
非 headless 模式在 MLClient.cpp 中会 `createAndAddWindow<IMainMenu>()` + `setEngineUser()`，但 headless 跳过。

涉及源码：`lib/client/GameEngine.cpp`、`lib/server/CVCMIServer.cpp`、`lib/network/`。

### 结论：放弃 ARM64 训练

| 方案 | 说明 |
|------|------|
| ❌ 继续修 ARM64 | 涉及 VCMI 网络层 `INetworkHandler::createTimer` 源码改造，编译耗时不确定，且不确定修完有无新坑 |
| ✅ **切回 WSL2 训练** | MMAI enabled，C1/C2 已验证跑通，RTX 3060 GPU 加速。WSL2 VCMI 编译产物在 `~/vcmi-native/` |

**决定：ARM64 留作推理端部署，训练全部移到 WSL2。**

### 后续待办

1. WSL2 启动训练（恢复 `~/vcmi-workspace/` 版本）
2. 出模型后 ONNX 导出
3. ARM64 推理部署（不需要 callback，直接 ctypes 读状态 + 模型推理）

---

## 历史记录

### Phase A: VCMI 冒险地图 API ✅ 95%
[原有内容保留]

### Phase B: 战略 Gym 环境 ✅ 完成
[原有内容保留]

### Phase C 历史修复

#### ✅ 服务器部署 (2026-07-18)
- vcmi 源码传输 + ARM64 编译
- MMAI=OFF, Nullkiller2 回调钩子
- E2E 验证：reset(NNZ=45)→step(NNZ=60, r=15.9)→step(NNZ=79, r=1.0)
- 提交: vcmi `f9bc3af`, hero3_fresh `de21046`

#### ❌ 错误：用 vmap 训练战略层（2026-07-19 修复）
- 之前 train_loop.sh 循环 10 张 vmap（adventure-A1, A1-A7, ml-mini, ml-train）
- **都是战斗模型训练图，不是战略图**
- 只有 adventure-A1.vmap 能过名字检查（assert 含 "adventure"）
- 其余 9 张要么断言失败要么崩溃
- 战略层训练地图只用 **H3M 经典图**

#### ✅ strategic_env.py ctypes→connector 迁移 (2026-07-19)
- `_adventure_wait()`, `_send_action()` 改为用 `self.connector.adventure_wait/act`
- 去掉 `_ensure_libml_loaded()` 的 ctypes 模式
- 去掉 DummyVecEnv 短路（`_vcmi_just_started`）
- 避免两个 libmlclient 实例的静态变量隔离

---

## Phase D: 战斗集成 ⬜
[原有内容]

## Phase E: 真实游戏部署 ⬜
[原有内容]

## Phase F: 你 vs AI ⬜
[原有内容]

## Phase G: 1v7 终极 ⬜
[原有内容]

---

## 关键资产

| | 资产 | 位置 |
| |------|------|
| | ARM64 服务器 | root@172.16.2.40, 64核, /DATA/hero3/ |
| | 编译产物 | /DATA/hero3/vcmi-build/bin/ (libmlclient.so) |
| | 训练脚本 | /DATA/hero3/test_strategic_ppo.py |
| | StrategicEnv | `/DATA/hero3/vcmi_gym/envs/v13/strategic_env.py` |
| | 训练循环 | `/DATA/hero3/train_loop.sh` |
| | 训练日志 | `/DATA/hero3/output/train_run.log` |
| | H3M 地图 | `/DATA/hero3/vcmi/data/Maps/*.h3m` (158张) |
