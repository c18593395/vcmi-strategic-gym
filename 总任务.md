# HoMM3 全盘操盘 AI — 总任务

> 最终目标：AI 完整操控真实游戏，与你对战（1v7），并战胜你。
> 策略：VCMI 训练战略模型 → 真实游戏内存操控 → 自对弈持续进化。

---

## 一、架构方案

### 两段式设计

```
训练端 (VCMI): WSL2/xm-server, 头less, 200fps, 自对弈 → 训练战略模型
部署端 (真实游戏): 本机 HD Mod, 内存读取 + PostMessage, 模型推理
```

训练端用 VCMI 全速自对弈进化，部署端模型直接加载到真实游戏对战。

### 自对弈架构

```
Round 1:   v1(红) vs v1(蓝) → 探索规则
Round 50:  v50(红) vs v48(蓝) → 持续进步
Round 500: v500(红) vs v495(蓝) → 高手对决
```

- 对手池保留最近 20 个版本，70%最新 + 20%早期 + 10%随机
- ELO 追踪，每 10 轮评估
- 红蓝对称学习

### 三层决策

```
战略层 RL (PPO) → 决定移动/探索/占矿/攻城
  ↓ 遇敌
战斗裁决 → 自动战斗(A) 或 v13 战斗模型
  ↓ 战果
战略层继续
```

### 观测与动作

观测 ~200 维：英雄位置/兵力/移动力、城镇/资源、敌方可见信息、日期。
动作：移动(8方向) + 交互 + 城镇操作 + 全局(结束回合)。

---

## 二、总进度

```bash
███████████████░░░░░░░░ 52%  当前: Phase C 进行中，服务器就绪
```

| | Phase | 目标 | 状态 | 累计时间 |
| |-------|------|------|---------|
| | A | VCMI 冒险地图 API | ✅ 完成 | 1.5天 |
| | B | 战略 Gym 环境 | ✅ 完成 | 待定 |
| | C | 自对弈 PPO 训练 | 🟡 进行中 | 待定 |
| | - | 服务器部署 | ✅ 完成 | — |
| | D | 战斗集成 | ⬜ | 2.5周 |
| | E | 真实游戏部署 | ⬜ | 3周 |
| | F | 你 vs AI (1v1) | ⬜ | 6周 |
| | G | 1v7 战胜你 | ⬜ | 3月 |

## Phase C 详细

### ✅ C1 空图训练（完成）
- 50ep, adventure-A1.vmap, nonzero=28, loss 1686→0.25

### ✅ C2 StupidAI 训练 v1/v2（完成）
- v1: 100ep vmap, r=+6.4 恒定
- v2: 100ep Key to Victory.h3m, 发现 r=+21.6 偶然事件

### ✅ 服务器部署（完成 2026-07-18）

| 步骤 | 状态 |
|------|------|
| vcmi 源码传输 (7.5G) | ✅ |
| ARM64 编译 libvcmi+libmlclient+vcmiserver+connector_v13 | ✅ |
| MMAI 依赖去除 (ENABLE_MMAI=OFF) | ✅ |
| Nullkiller2 回调钩子 (g_adventure_cb) | ✅ |
| 跨库符号可见 (libvcmi.so 定义 + RTLD_GLOBAL) | ✅ |
| 观测读取 (CCallback::getGameStatePtr → NNZ=45) | ✅ |
| PvE 战斗崩溃修复 (ServerPlugin) | ✅ |
| 完整循环 reset→step→step (NNZ=60-79, r=15.9) | ✅ |
| LD_LIBRARY_PATH 尾冒号 bug | ✅ 已修复 |

### 🔄 C2v3 训练启动（阻塞中）
- PPO `DummyVecEnv.__init__` 额外调 reset 导致冲突
- 需在 strategic_env.py reset() 加 DummyVecEnv 短路判断
- 方案：检查 `self._turn` 是否有值，有则直接返回零 obs
- 改完后重启训练：`cd /DATA/hero3 && nohup python3 -u train_v13_ppo.py > output/train_final.log 2>&1 < /dev/null &`

### ⏳ 下几步
1. 修 strategic_env.py → 启动训练（~4h, 5000 steps）
2. 监控日志是否稳定产出 FPS
3. 跑一轮锚点循环验证连续训练
4. C3 自对弈（对手池 + ELO）

---

## Phase A: VCMI 冒险地图 API ✅ 95%

### A1: 冒险地图 ✅
- A1.1 分析 ml-mini.vmap 结构 ✅
- A1.2 创建 s1.vmap (36×36, 红蓝各1城1英雄) ✅

### A2: 状态结构体 ✅
- A2.1 strategic_state.h C 平面结构体 (<4KB) ✅
- A2.2 字段：day/week/month, players[8], heroes[8], towns[8] ✅
- A2.3 Python ctypes 读取器 strategic_reader.py ✅ (布局验证通过)

### A3: 状态填充 ✅
- A3.1 strategic_state_update(CGameState*) ✅
- A3.2 填充玩家资源/状态 ✅
- A3.3 ext4 编译 libmlclient.so ✅
- A3.4 部署 + 修复 RUNPATH ✅

### A4: 冒险 AI 回调 ✅
- A4.1 修改 AAI.cpp yourTurn() 注入 g_adventure_cb ✅
- A4.2 无回调时降级为 endTurn() ✅

### A5: Connector 扩展 ✅
- A5.1 adventureWait/Act 方法 + pybind11 ✅
- A5.2 编译 connector_v13_adventure.so ✅

### A6: 回调注册 ✅
- A6.1 回调从 connector.start() 挪到 init_vcmi() ✅
- A6.2 ctypes 直读 g_strategic_state 验证 ✅

### A7: 测试验证 ✅
- A7.1 g_strategic_state 指针非空 ✅
- A7.2 ServerPlugin 4处修复（tempOwner/pool/randomHeroes）✅
- A7.3 CProxyIOApi/ROIOApi 空指针保护 ✅
- A7.4 合并数据目录 (VCMI源+游戏数据) ✅
- A7.5 **端到端 ✅** adventure_wait/act 联通验证通过（adventure-A1.vmap）
- A7.6 libmlclient.so 重编译（h->movement→getter, extern "C" 导出）
- A7.7 connector lock 顺序修复 + 重编译部署
- A7.8 pyconnector/vcmi_env kwargs 映射修复

### 代码提交
- hero3_fresh main: `460b467` — 测试脚本+工具 (11 files)
- vcmi 子模块: `86b8c2c7a` — 核心代码 (12 files, +432/-34)

---

## Phase B: 战略 Gym 环境 ✅ 完成! (2026-07-18)

> 基于 Phase A 冒险 API（adventure_wait/act），StrategicEnv 代码已完成（590行）
> **端到端打通**: reset nonzero=23/256, step nonzero=24/256, 第二轮 yourTurn 正常触发

| # | 子任务 | 状态 |
|---|--------|------|
| B1.1 | StrategicEnv 继承 gymnasium.Env | ✅ |
| B1.2 | reset() → adventure_wait → send_action | ✅ |
| B1.3 | step(END_TURN) → yourTurn 回调 → 下一轮观测 | ✅ |
| B1.4 | gym.register("VCMI-strategic-v1") | ✅ |
| B2.1 | 256-dim 观测（展平 StrategicState）| ✅ nonzero=23~24 |
| B2.2 | ctypes 直读 g_strategic_state | ✅ (同 inode 修复) |
| B3.1 | Discrete(11) 动作 | ✅ |
| B4.1 | 奖励函数 | ✅ (step reward=-0.1 正常) |
| B4.2 | max_turns=28 截断 | ✅ |
| B5.1 | render() 文本状态 | ✅ |

### 🔧 端到端打通：4 项关键修复 (2026-07-18)

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | s_turn_player 隔离 | Python ctypes 和 VCMI 加载了不同路径/不同 inode 的 libmlclient.so | strategic_env.py lib_path → vcmi-native/rel/bin/ |
| 2 | cb->endTurn() 死锁 | VCMI 服务器单线程内调用 endTurn 等待确认 | waitTillRealize=false 异步返回 |
| 3 | 第二轮 yourTurn 不触发 | selectionMade 需在 endTurn 后（而非前） | 调整顺序 |
| 4 | rebuild 不生效 | MMAI 是 OBJECT 库，只 build MMAI 不重链 libvcmi.so | build target: vcmi（含 MMAI objects） |

> 完整知识库: skill `vcmi-adventure-atomic-sync`

| # | 子任务 | 状态 |

> ~~战斗环境有已知死锁~~ 冒险模式端到端已验证通过

---

## Phase C: 自对弈 PPO 训练 ⬜

> 用 vcmi-gym rl/algos/mppo/

| 阶段 | 轮次 | 内容 |
|------|------|------|
| C1 | ~100 | 空图：学移动+捡资源+结束回合 |
| C2 | ~200 | vs StupidAI：学占矿+打野 |
| C3 | ~500 | 自对弈：对手池+ELO |
| C4 | ~1000 | 强化高难度 |

评估：每 N 轮 vs BattleAI 基准 + 自对弈新旧对比。

### ✅ ARM64 服务器部署 (2026-07-18)

**关键改动 (已提交 vcmi `f9bc3af`, hero3_fresh `de21046`):**

| # | 改动 | 文件 |
|---|------|------|
| 1 | AI 类型 MMAI→Nullkiller2 (MMAI 关后 EmptyAI 不调 yourTurn) | MLClient.cpp |
| 2 | g_adventure_cb 定义移入 libvcmi.so (跨库符号可见) | AIFactory.cpp |
| 3 | CCallback::getGameStatePtr() 公开访问器 | CCallback.h |
| 4 | Nullkiller2 yourTurn 回调钩子 (pause→Python→continue) | AIGateway.cpp |
| 5 | 原子变量同步 + getter 函数 | strategic_state.cpp |
| 6 | PvE 战斗不抛异常 (hero2=null) | ServerPlugin.cpp |
| 7 | RTLD_GLOBAL + adventure_wait 超时 30→120s | strategic_env.py |

**E2E 验证：**
- `reset(NNZ=45) → step(NNZ=60, r=15.9) → step(NNZ=79, r=1.0)` ✅
- `test_direct.py` 直接调 env 完美运行 ✅
- `train_v13_ppo.py` 经 PPO DummyVecEnv 包装后有冲突 ⏳

### 🔧 待修复：DummyVecEnv 兼容

**问题：** PPO 构造时 `DummyVecEnv.__init__` 调 `self.reset()` → 调 env.reset() → VCMI 启动后回调不触发

**方案：** `strategic_env.py reset()` 开头加：
```python
if hasattr(self, '_turn'):
    return np.zeros(OBS_DIM, dtype=np.float32), {...}
```

### 代码提交

| 仓库 | Commit | 文件 | 改动 |
|------|--------|------|------|
| vcmi | `f9bc3af` | 8文件 | +46/-6 |
| hero3_fresh | `de21046` | 4文件 | +36/-28 |

---

## Phase D: 战斗集成 ⬜

- D1 遇敌检测 → 切换战斗 Agent
- D2 战果回传战略层
- D3 分层 RL：战斗梯度不传播

---

## Phase E: 真实游戏部署 ⬜

- E1 验证 HD Mod 5.5 内存偏移
- E2 观测映射 VCMI→真实游戏
- E3 PostMessage 按键执行
- E4 ONNX Runtime 推理
- E5 你随时接管操作权

---

## Phase F: 你 vs AI ⬜

- F1 1v1 对战（信息公平，只看可见范围）
- F2 难度 4 档：新手/中等/专家/噩梦
- F3 保存 replay，分析弱点

---

## Phase G: 1v7 终极 ⬜

- G1 7 AI 共享视野 + 任务分工
- G2 xm-server 24×7 自对弈
- G3 里程碑：1v1赢你 → 你+1 AI vs 2 AI → ... → 1v7赢你

---

## 关键资产

| | 资产 | 位置 |
| |------|------|
| | VCMI 编译链 | ext4: ~/vcmi-native/ (✅ ENABLE_ML=ON 重建成功) |
| | ARM64 服务器 | root@172.16.2.40, 64核, /DATA/hero3/ |
| | 编译产物 | /DATA/hero3/vcmi-build/bin/ (libvcmi.so 409M) |
| | 训练脚本 | /DATA/hero3/train_v13_ppo.py |
| | StrategicEnv | `/DATA/hero3/vcmi_gym/envs/v13/strategic_env.py` |
| | 冒险 API | Phase A 产出 (adventure_wait/act) ✅ 已验证 |
