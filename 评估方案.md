# 动作映射修复方案

## 问题陈述

训练中模型输出方向动作（0-7），但 C++ 侧的 `AAI::yourTurn()` 从不读取 `s_turn_action`，
始终执行 `cb->endTurn()`。英雄不移动，蓝方 AI 在第一回合秒杀红方，90%+ 的局只有 1 步。

## 已探明的路径

### ❌ 方案 A：在 MMAI 回调内调 moveHero（已尝试，segfault）

```
Python → ctypes adventure_send_action(action)
  → C++ adventure_process_turn（spin-wait）
  → AAI::yourTurn() 读 g_strategic_state->action
  → cb->moveHero(hero, dst, false)  ←  segfault
```

崩溃原因：`moveHero` 在 yourTurn 回调链中调用，VCMI 的 server 端状态可能不允许在 query 回调期间发送 move 命令。try/catch 抓不住 SIGSEGV。

### ✅ 方案 B：Nullkiller2 异步路径（推荐，可工作）

Nullkiller2 的 `AIGateway::yourTurn()` 在回调结束后通过 `asyncTasks->run()` 异步执行 `makeTurn()`，
`makeTurn()` 内部调 `cc->moveHero()` **不会崩溃**。

**实现：**

1. 在 `strategic_state.h` 中保留 `action` 字段（已完成）
2. 在 `AAI::yourTurn()` 中读取 action，**不直接调 moveHero**，改为**设置一个全局目标坐标 flag**
3. 修改 `AAI` 增加一个 `postTurnAction()` 方法，在回调链结束后被调用
4. 该方法读取 flag 并调 `cb->moveHero()`

但 MMAI 没有类似 Nullkiller2 的 async task 框架，直接加比较复杂。

### ✅ 方案 C：Python/Connector 层直接控制（最干净，但改动大）

在 `libmlclient.so` 中暴露一个新函数：

```c
void move_hero(int hero_id, int target_x, int target_y, int target_z);
```

Python 的 `step()` 流程改为：

```python
def step(self, action):
    self._adventure_wait()
    # 根据 action 计算目标坐标
    hero = self._read_state().heroes[0]
    target = (hero.pos_x + dx[action], hero.pos_y + dy[action], hero.pos_z)
    # 直接调用 C 函数移动英雄
    self._libml.move_hero(hero.id, *target)
    # 发送 end_turn 信号
    self._send_action(10)
    # 读取状态
    state = self._read_state()
```

`move_hero` 在 libmlclient.so 内部实现，需要获取 CCallback。由于 libmlclient.so 在 VCMI 进程中运行，
可以通过 VCMI 的 `IGameHandler` 全局对象发送 move 请求。

## 推荐路线

**立即执行方案 B 的变体：** 不改 async task，而是在 `AAI::yourTurn()` 中：
1. 回答 query（`selectionMade`）
2. 直接调 `cb->moveHero()`（已验证 segfault，但可能是顺序问题）
3. **如果还是 segfault，fallback 到方案 C**

**优先尝试：确认 selectionMade 在 moveHero 之前是否能防止 segfault。**
