# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `step2分析.md` | 技能: `skill vcmi-gym`, `skill homm3`

---

## 项目状态（2026-07-20）

WSL2 战略层 PPO 训练全线跑通，当前 GPU 自对弈训练进行中。

**运行参数：**
- 500ep × 20步, batch=64, device=cuda
- 自对弈: red=MMAI_USER, blue=MMAI_USER
- 8 张 H3M 地图轮换
- 预计总耗时 ~40min
- 模型自动保存（best vloss）

**上次验证（50ep×5step）：** vloss 769→45, 250步, 501s

---

## 任务清单（全部完成 ✅）

### ✅ 1. 扩规模训练

500ep×20步, batch=64。`train_wsl2_ppo.py` 已更新。

### ✅ 2. GPU 加速

device=cuda, RTX3060。模型+tensor 均在 GPU 上。

### ✅ 3. 自对弈

blue=MMAI_USER。双方都用同一模型。`ep_runner_one.py` 已更新。

### ✅ 4. reward 函数重构

全资源追踪: gold/wood/ore/mercury/sulfur/crystal/gems + hero_exp。
gold_mult: 0.001 → 0.01。`strategic_env.py` 已更新。

### ✅ 5. 24/7 训练循环

`train_loop.sh` 已创建。轮换地图 + 僵尸清理 + 断点续训。

---

## 下步方向（待定）

| 方向 | 说明 |
|------|------|
| 对手池 | 保留最近 N 个模型版本，随机选对手 |
| ELO 追踪 | 评估每轮模型强度 |
| 战斗集成 | 遇敌切 v13 战斗模型 |
| reward 细分 | 击败英雄/攻城/占矿 单独奖励 |
| 长程训练 | 50K+ 步，真正收敛 |

---

## 快速启动

```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash -c '
find /mnt/d/Bigdata/hero3_fresh -path "*/__pycache__" -type d -exec rm -rf {} + 2>/dev/null
cd /home/administrator/vcmi-workspace
export LD_LIBRARY_PATH=/home/administrator/vcmi-native/rel/bin:/home/administrator/vcmi-workspace/vcmi_gym/connectors/rel
export STRATEGIC_STATE_LIB=/home/administrator/vcmi-native/rel/bin/libmlclient.so
/home/administrator/vcmi-workspace/venv/bin/python -u /mnt/d/Bigdata/hero3_fresh/train_wsl2_ppo.py 2>&1
'
```

或 24/7 循环：
```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/d/Bigdata/hero3_fresh/train_loop.sh 2>&1
```

---

## 关键踩坑点

1. **step() 必须是 WAIT→SEND 顺序**
2. **AI 引擎必须是 Nullkiller2**（不 MMAI）
3. **改 Python 源码后必须清 pyc 缓存**
4. **ep_runner 必须用 os._exit(0)**
5. **MLClient headless 用 cond_shutdown**
6. **三件套 .so 必须同源编译**
7. **子 agent 不可用**（401），编译/测试自己跑

详见 `step2分析.md`。

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `train_wsl2_ppo.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器 |
| `strategic_env.py` | 战略 Gym 环境 |
| `train_loop.sh` | 24/7 循环脚本 |
| `总任务.md` | 项目全貌 |
| `step2分析.md` | step2 问题分析全记录 |
| `route-backups/base/` | 基线备份 |
