# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `step2分析.md` | 技能: `skill vcmi-gym`, `skill homm3`

---

## 项目状态（2026-07-19）

WSL2 战略层 PPO 训练管道已全通：50ep×5step, vloss 769→45, 模型保存。

**当前可用：**
- 8 张 H3M 地图轮换
- Nullkiller2 AI 引擎（回调后 fall through）
- WAIT→SEND 协议（step2 已修复）
- subprocess-per-episode + os._exit(0) 清理
- 每步 ~0.1s, 每 episode ~5s（含 VCMI 启动）

---

## 任务清单

### 1. 扩规模训练 [参数调整]

**说明：** 当前 50ep×5step 太小，模型未充分收敛。扩大到合理规模。

**改动：** `train_wsl2_ppo.py`
- N_EPISODES: 50 → 500
- STEPS_PER_EP: 5 → 20
- BATCH: 16 → 64
- LR: 3e-4（保持不变）
- MAPS: 扩展到全部可用 2 人 H3M 图

**验收：**
- 500ep 完成，vloss 持续下降
- 模型保存，可加载推理
- 无 step 挂死、无 core dump

---

### 2. 加 GPU 加速 [参数调整]

**说明：** RTX 3060 可用，加速 PPO 训练。

**改动：** `train_wsl2_ppo.py`
- 模型加 `.to("cuda")`
- obs_tensor 加 `.to("cuda")`
- WSL2 CUDA 环境已配置

**验收：**
- `nvidia-smi` 显示 GPU 利用率 > 50%
- 训练速度明显提升

---

### 3. 切自对弈 [参数调整]

**说明：** 当前 blue=StupidAI（固定对手），改为 blue=MMAI_USER（双方都用模型），实现真正的自对弈。

**改动：** `train_wsl2_ppo.py` + `ep_runner_one.py`
- blue="StupidAI" → blue="MMAI_USER"
- 双方用同一模型 or 对手池（先用同一模型，后续做对手池）

**验收：**
- 红蓝双方 yourTurn 都触发 callback
- step 正常交替（red→blue→red→...）
- reward 有正有负（不再是恒负）

---

### 4. reward 函数重构 [Python ~30行]

**说明：** 当前 reward = step_penalty + 资源差值。只惩罚回合消耗，不奖励积极行为。

**设计：**
```
reward = -0.1（每步惩罚）
       + Δgold × 0.01（金币变化）
       + Δtowns × 10（占新城）
       + Δhero_level × 5（英雄升级）
       + 击败敌方英雄 × 50
       + 胜利 × 200
       - 战败 × -200
```

**改动：** `strategic_env.py` 的 `_calc_reward()`

**验收：**
- reward 不再是恒定负值
- 不同 action 产生不同 reward
- 模型 action 分布不再均匀

---

### 5. 24/7 训练循环 [新脚本]

**说明：** `train_loop.sh` 轮换所有 H3M 地图，断点续训，进程守护。

**改动：** 新建 `train_loop.sh`
- 循环地图列表
- 每轮 N 步，自动切下一张图
- 模型保存 + 断点续训
- pkill vcmiserver 僵尸清理

**验收：**
- 连续跑 10+ 轮不挂
- 日志有 fps/time 可监控
- Ctrl+C 后可从断点续训

---

## 快速启动

```bash
# WSL2 环境
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash -c '
cd /home/administrator/vcmi-workspace
export LD_LIBRARY_PATH=/home/administrator/vcmi-native/rel/bin:/home/administrator/vcmi-workspace/vcmi_gym/connectors/rel
export STRATEGIC_STATE_LIB=/home/administrator/vcmi-native/rel/bin/libmlclient.so
/home/administrator/vcmi-workspace/venv/bin/python -u /mnt/d/Bigdata/hero3_fresh/train_wsl2_ppo.py 2>&1
'
```

**部署前必做：** 清 pyc 缓存
```bash
find /mnt/d/Bigdata/hero3_fresh -path "*/__pycache__" -type d -exec rm -rf {} +
```

---

## 关键踩坑点（详见 `step2分析.md` 和 `skill vcmi-gym`）

1. **step() 必须是 WAIT→SEND 顺序**，否则 s_turn_player 被提前消费导致 step2 永久挂死
2. **AI 引擎必须是 Nullkiller2**（不是 MMAI），MMAI 回调手动 endTurn 会导致 query 超时
3. **每次改 Python 源码后必须清 pyc 缓存**，NTFS 文件系统过期检测不可靠
4. **ep_runner 必须用 os._exit(0)**（不是 sys.exit），VCMI 线程阻止正常退出
5. **MLClient headless 用 cond_shutdown**，ENGINE->mainLoop() 在 WSL2 会导致 core dump
6. **三件套 .so 必须同源编译**（libvcmi + libmlclient + libmlserverplugin），不能混搭
7. **子 agent 不可用**（deepseek-v4-flash key 401），编译/测试自己跑 terminal

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `train_wsl2_ppo.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器（subprocess） |
| `strategic_env.py` | 战略 Gym 环境 |
| `总任务.md` | 项目全貌 |
| `step2分析.md` | step2 问题分析全记录 |
| `route-backups/base/` | 基线备份（.so + 源码） |
