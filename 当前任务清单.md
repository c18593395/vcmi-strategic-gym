# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `fix_action_mapping.md` | 评估: `评估方案.md` | 知识库: `知识库.md`

---

## 双轨并行架构

```
┌──────────────────────┐   ┌──────────────────────────┐
│  Track 1: 训练 🟢    │   │  Track 2: 真实游戏对齐 ⬜ │
│  ✅ C7 全部完成       │   │  模型落地到真实 HoMM3    │
│  ✅ moveHero 验证通过  │   │  Windows 内存读取验证    │
│  🔄 待启动训练        │   └──────────────────────────┘
└──────────────────────┘
```

---

## Track 1: 训练 🟢（GPU 24/7 后台跑）

### 当前训练状态（2026-07-27）

```
训练脚本: train_wsl2_ppo_v2.py（GAE λ=0.95 + grad_clip=1.0）
AI 配置: red=MMAI（RL 控制）, blue=MMAI（非红方 guard 直接 endTurn）
管道状态: ✅ yourTurn → state_update → process_turn → moveHero → endTurn
           ✅ END_TURN 5/5 全通，nz=45
           ✅ 战斗崩溃已修复（ServerPlugin 两处 guard）
           ⚠️ moveHero 在 headless 下受限（服务器拒绝部分移动，fire-and-forget 不阻塞管道）
编译: libmlclient.so + libvcmi.so + libmlserverplugin.so 含所有改动
```

### C7 动作映射修复状态

| # | 任务 | 说明 | 难度 | 状态 |
|---|------|------|------|------|
| C7.0 | 根因确认 | s_turn_action 写入不读取，方向动作=结束回合 | 🟢 | ✅ |
| C7.1 | 直接 moveHero（去 async） | 不再 asyncTasks，yourTurn 内直接 cb->moveHero()，同步成功 | 🟡 | ✅ |
| C7.2 | g_rl_action 全局变量 | 不改变 struct 尺寸，Python ctypes 直写 | 🟢 | ✅ |
| C7.3 | extern C direct call | strategic_state_update 改用 extern "C" 避免 dlsym 跨库问题 | 🟢 | ✅ |
| C7.4 | AIS 列表扩展 | MLClient.h: 加入 EmptyAI / Nullkiller2 / MMAI | 🟢 | ✅ |
| C7.5 | AIFactory 路由 | MMAI_USER→真实 MMAI::AAI::AAI（原 CEmptyAI） | 🟢 | ✅ |
| C7.6 | g_strategic_state NULL | strategic_state_update 自分配 + 英雄/城镇填充恢复 | 🔴 | ✅ |
| C7.7 | 战斗崩溃修复 | ServerPlugin.cpp: startBattleHook + endBattleHook guard | 🔴 | ✅ |
| C7.8 | 单次 action 架构 | yourTurn 去 while 循环，Python step() SEND→WAIT→READ | 🟢 | ✅ |
| C7.9 | 训练就绪 | 管道全通，观测稳定 nz=45，战斗不崩 | 🟢 | ✅ 随时可以启动 |

### 训练参数

| 参数 | 值 |
|---|---|
| N_EPISODES | 1000 |
| STEPS_PER_EP | 200 |
| BATCH | 128 |
| device | cuda (RTX3060) |
| 地图 | 110 张 H3M 轮换 |
| 对手 | MMAI_USER（对手池） |
| 对手池 | 70%当前 / 20%早期 / 10%随机 |
| Checkpoint | 每 50 step, 保留 10 个 |

---

## 下一步

| # | 任务 | 说明 | 优先级 |
|---|------|------|--------|
| N1 | 启动训练 | train_loop.sh → 1000ep × 200步 × 110图 | 🔴 |
| N2 | scp 自生成地图工具 | 从服务器 cp gen_strategic_vmap.py + 适配 | 🟡 |
| N3 | 课程学习 6 阶段 | 纯探索→资源→矿产→城建→对战→1v7（依赖 N2） | 🟡 |
| N4 | 战斗集成（Phase D） | moveHero + autofight 深度整合，观察兵力变化 | ⬜ |
| N5 | 真实游戏部署（Phase E） | VCMI Windows PpoModelAI.dll | ⬜ |

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `train_wsl2_ppo_v2.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器 |
| `strategic_env.py` | 战略 Gym 环境（SEND→WAIT→READ） |
| `train_loop.sh` | 24/7 循环脚本 |
| `vcmi/AI/MMAI/AAI/AAI.cpp` | MMAI yourTurn（单次 action + direct moveHero + 非红方 guard） |
| `vcmi/ML/strategic_state.h/cpp` | 共享状态 + g_rl_action + adventure 同步函数 |
| `vcmi/ML/MLClient.h` | AIS 列表（含 EmptyAI / Nullkiller2 / MMAI） |
| `vcmi/server/ML/ServerPlugin.cpp` | battle hook guard（战斗不崩） |
| `strategic_reader.py` | ctypes StrategicState 绑定 |
| `docs/知识库.md` | 架构/踩坑/决策 |
| `docs/踩坑记录.md` | 全部踩坑点（含 C7 11 条） |
| `checkpoints/` | 模型 checkpoint（10 个） |

---

## 关键踩坑点

1. **dlsym 不可靠（已修）** — 跨 .so 搜索范围不同。改为 extern "C" 直调，链接器保证正确版本。
2. **struct 尺寸不能改** — 加字段会破坏 ABI。数据放独立全局变量（g_rl_action）。
3. **try/catch 吞异常** — strategic_state_update 内的 catch 吞了填充异常导致观测全零。去掉 catch 让错误暴露。
4. **战斗崩溃（已修）** — ServerPlugin.cpp: startBattleHook(hero2=null) + endBattleHook(heroDefender=null) 加 return guard。
5. **非红方 yourTurn 死循环** — AAi.cpp 顶部加 pc!=0 guard，蓝方直接 endTurn 不进入主循环。
6. **WAIT→SEND 顺序的 race** — step() 改为 SEND→WAIT→READ。state_update 在 process_turn 阻塞前执行，保证状态最新。
7. **waitTillRealize=false 对 endTurn 必要** — 否则 endTurn 等待服务器确认不返回。
8. **三件套 .so 必须同源编译** — libmlclient.so + libvcmi.so + libmlserverplugin.so 必须在同一 cmake 配置下编译。
9. **VCMI 数据目录** — Mods/core 通过 ~/.local/share/vcmi/Data/→/mnt/d/GAMES/Heroes3/Data/ 链式路径加载。
