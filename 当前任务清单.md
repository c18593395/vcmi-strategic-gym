# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `step2分析.md` | 技能: `skill vcmi-gym`, `skill homm3`

---

## 项目状态（2026-07-20）

WSL2 战略层 PPO 训练全线跑通，当前 GPU 自对弈训练进行中。

**运行参数：**
- 500ep × 20步, batch=64, device=cuda
- 自对弈: red=MMAI_USER, blue=MMAI_USER
- 8 张 H3M 地图轮换
- 预计总耗时 ~40min
- 模型自动保存（best vloss）

**上次验证（50ep×5step）：** vloss 769→45, 250步, 501s

---

## 任务清单（全部完成 ✅）

### ✅ 1. 扩规模训练

500ep×20步, batch=64。`train_wsl2_ppo.py` 已更新。

### ✅ 2. GPU 加速

device=cuda, RTX3060。模型+tensor 均在 GPU 上。

### ✅ 3. 自对弈

blue=MMAI_USER。双方都用同一模型。`ep_runner_one.py` 已更新。

### ✅ 4. reward 函数重构

全资源追踪: gold/wood/ore/mercury/sulfur/crystal/gems + hero_exp。
gold_mult: 0.001 → 0.01。`strategic_env.py` 已更新。

### ✅ 5. 24/7 训练循环

`train_loop.sh` 已创建。轮换地图 + 僵尸清理 + 断点续训。

---

## Phase C3: 自对弈 + 对手池 + ELO ⬜

> **目标：** 防止过拟合单一对手，评估模型真实强度

### C3.1 模型 checkpoint 保存 [改 train_wsl2_ppo.py]

**说明：** 当前只保存 best_vloss 单个模型。改为每 N 个 batch 保存一个 checkpoint。

**改动：**
- 每 200 step 保存 `wsl2_ckpt_{step}.pt`
- 保留最近 10 个 checkpoint，旧的自动删除
- 目录: `D:\Bigdata\hero3_fresh\checkpoints\`

**验收：**
- 训练中产生多个 checkpoint 文件
- 旧 checkpoint 自动滚动删除

### C3.2 对手池 [改 ep_runner_one.py + train_wsl2_ppo.py]

**说明：** 每局从对手池中随机选对手，而非固定当前模型。

**改动：**
- `train_wsl2_ppo.py`: 维护对手池（最近 10 个 checkpoint 路径列表）
- `ep_runner_one.py`: 接受 `--blue_model` 参数，加载指定模型做蓝方
- 选集概率: 70% 当前模型 + 20% 早期版本 + 10% 随机旧版
- 前 10 局无对手池时，用自身做对手（当前逻辑）

**验收：**
- blue 方模型版本在变化
- 训练日志显示对手版本号

### C3.3 ELO 评估 [新脚本 eval_elo.py]

**说明：** 每 50 episode 跑一次评估：当前模型 vs 基准模型（最早 checkpoint），各 10 局，统计胜率。

**改动：**
- 新建 `eval_elo.py`
- 红蓝各 10 局轮换（消除先手优势）
- 输出: `elo_log.json`（记录时间、step、胜率、平均奖励）

**验收：**
- 评估脚本可独立运行
- elo_log.json 持续记录
- 胜率随时间上升

---

## Phase C4: 强化高难度 ⬜

> **目标：** 大地图、长步数、强对手，逼近实战强度

### C4.1 全地图扫描 [工具脚本]

**说明：** 当前只用 8 张确认可用的图。需要从 158 张 H3M 中筛选所有可用地图。

**改动：**
- 新建 `scan_maps.py`
- 每张图快速 reset 测试：VCMI 启动 → yourTurn → 读状态 → 记录 nz
- 过滤：nz > 0 且无 crash → 加入可用列表
- 输出: `available_maps.json`

**验收：**
- 至少 20+ 张图可用
- 脚本 1 分钟内完成扫描（subprocess 批量）

### C4.2 训练参数升级 [改 train_wsl2_ppo.py]

**说明：** 地图多了，步数也要跟上。

**改动：**
- STEPS_PER_EP: 20 → 50
- N_EPISODES: 500 → 2000
- BATCH: 64 → 128
- MAPS: 从 `available_maps.json` 加载

**验收：**
- 训练 2000ep 不挂
- 地图覆盖 >= 80% 可用图

### C4.3 长程训练启动 [执行]

**说明：** 用 C4.1 扫描结果 + C4.2 参数 + C3 对手池，启动 C4 正式训练。

**启动命令：**
```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/d/Bigdata/hero3_fresh/train_loop.sh 2>&1 &
```

**验收：**
- 连续训练 2+ 小时不挂
- vloss 持续下降，最终 < 10
- 模型 vs 初版模型 胜率 > 60%

---

## 快速启动

```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash -c '
find /mnt/d/Bigdata/hero3_fresh -path "*/__pycache__" -type d -exec rm -rf {} + 2>/dev/null
cd /home/administrator/vcmi-workspace
export LD_LIBRARY_PATH=/home/administrator/vcmi-native/rel/bin:/home/administrator/vcmi-workspace/vcmi_gym/connectors/rel
export STRATEGIC_STATE_LIB=/home/administrator/vcmi-native/rel/bin/libmlclient.so
/home/administrator/vcmi-workspace/venv/bin/python -u /mnt/d/Bigdata/hero3_fresh/train_wsl2_ppo.py 2>&1
'
```

或 24/7 循环：
```bash
MSYS_NO_PATHCONV=1 wsl -d Ubuntu -- bash /mnt/d/Bigdata/hero3_fresh/train_loop.sh 2>&1
```

---

## 关键踩坑点

1. **step() 必须是 WAIT→SEND 顺序**
2. **AI 引擎必须是 Nullkiller2**（不 MMAI）
3. **改 Python 源码后必须清 pyc 缓存**
4. **ep_runner 必须用 os._exit(0)**
5. **MLClient headless 用 cond_shutdown**
6. **三件套 .so 必须同源编译**
7. **子 agent 不可用**（401），编译/测试自己跑

详见 `step2分析.md`。

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `train_wsl2_ppo.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器 |
| `strategic_env.py` | 战略 Gym 环境 |
| `train_loop.sh` | 24/7 循环脚本 |
| `总任务.md` | 项目全貌 |
| `step2分析.md` | step2 问题分析全记录 |
| `route-backups/base/` | 基线备份 |
