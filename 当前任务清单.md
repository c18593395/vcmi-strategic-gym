# 当前任务清单

> 父文档: `总任务.md` | 踩坑: `fix_action_mapping.md` | 评估: `评估方案.md` | 知识库: `知识库.md`

---

## 双轨并行架构

```
┌──────────────────────┐   ┌──────────────────────────┐
│  Track 1: 训练 🟢    │   │  Track 2: 真实游戏对齐 ⬜ │
│  持续提升模型强度      │   │  模型落地到真实 HoMM3    │
│  WSL2 GPU 后台跑      │   │  Windows 内存读取验证    │
│  不占交互注意力        │   └──────────────────────────┘
└──────────────────────┘
```

---

## Track 1: 训练 🟢（GPU 24/7 后台跑）

### 当前训练状态（2026-07-26）

```
训练脚本: train_wsl2_ppo_v2.py（GAE λ=0.95 + grad_clip=1.0）
AI 配置: red=MMAI（真实 MMAI，含 g_adventure_cb）, blue=MMAI_USER/对手池
当前问题: g_strategic_state 仍 NULL（跨 .so 符号双份）→ 观测全零
修复进度: 已改 asyncTasks->run() + extern C 直调 strategic_state_update + g_rl_action
编译: libmlclient.so + libvcmi.so 含所有改动
```

### 动作映射修复状态（C7）

| # | 任务 | 说明 | 难度 | 状态 |
|---|------|------|------|------|
| C7.0 | 根因确认 | s_turn_action 写入不读取，方向动作=结束回合 | 🟢 | ✅ |
| C7.1 | asyncTasks 异步 moveHero | AAI.cpp: asyncTasks->run() 在回调链外调 moveHero | 🟡 | ✅ 已编译 |
| C7.2 | g_rl_action 全局变量 | 不改变 struct 尺寸，Python ctypes 直写 | 🟢 | ✅ 已编译 |
| C7.3 | extern C direct call | strategic_state_update 改用 extern "C" 避免 dlsym 跨库问题 | 🟢 | ✅ 已编译 |
| C7.4 | AIS 列表扩展 | MLClient.h: 加入 MMAI / Nullkiller2 | 🟢 | ✅ 已编译 |
| C7.5 | AIFactory 路由 | MMAI_USER→真实 MMAI::AAI::AAI（原 CEmptyAI） | 🟢 | ✅ 已编译 |
| **C7.6** | **g_strategic_state NULL** | 跨 .so 符号双份，strategic_state_update 无法分配 | 🔴 | 🔄 进行中 |
| C7.7 | 验证英雄移动 | ep 步数 > 1 + hero pos 变化 | 🔴 | ⬜ 等 C7.6 完成 |
| C7.8 | 恢复 20min 报告 | 删除旧 20min 循环后重建 | 🟢 | ⬜ |

### 训练参数

| 参数 | 值 |
|---|---|
| N_EPISODES | 1000 |
| STEPS_PER_EP | 200 |
| BATCH | 128 |
| device | cuda (RTX3060) |
| 地图 | 110 张 H3M 轮换 |
| 对手 | MMAI_USER（对手池） |
| 对手池 | 70%当前 / 20%早期 / 10%随机 |
| Checkpoint | 每 50 step, 保留 10 个 |

---

## 关键文件

| 文件 | 用途 |
|------|------|
| `train_wsl2_ppo_v2.py` | PPO 训练主脚本 |
| `ep_runner_one.py` | 单 episode 执行器 |
| `strategic_env.py` | 战略 Gym 环境（含 g_rl_action 写入） |
| `train_loop.sh` | 24/7 循环脚本 |
| `vcmi/AI/MMAI/AAI/AAI.cpp` | MMAI yourTurn（asyncTasks + extern C 直调） |
| `vcmi/ML/strategic_state.h/cpp` | 共享状态 + g_rl_action 全局变量 |
| `vcmi/ML/MLClient.h` | AIS 列表（含 MMAI / Nullkiller2） |
| `vcmi/lib/callback/AIFactory.cpp` | MMAI_USER→真实 MMAI 路由 |
| `strategic_reader.py` | ctypes StrategicState 绑定 |
| `评估方案.md` | 三种技术路线对比 |
| `fix_action_mapping.md` | 调试全记录 |
| `知识库.md` | 架构/踩坑/决策 |
| `总计划.md` | 三阶段路线图 |
| `checkpoints/` | 模型 checkpoint（10 个） |

---

## 关键踩坑点

1. **g_strategic_state 跨 .so 双份** — strategic_state.cpp 被编译进 mlclient 和 vcmiservercommon，导致同一变量有两个副本。`strategic_state_update` 检查 `if (!g_strategic_state) return;` 但 `init_vcmi` 分配的是另一个副本的。需要 self-allocation 或单独 export 文件。
2. **struct 尺寸不能改** — 之前加了 action 字段导致 ABI 错位全量编译失败。现用独立全局变量 g_rl_action 避开。
3. **extern "C" vs extern** — C++ mangling 导致符号不匹配。必须 extern "C" 才能正确链接。
4. **dlsym 跨库不可靠** — RTLD_DEFAULT 在不同 .so 中搜索范围不同。用 extern 直调替代。
5. **三件套 .so 必须同源编译** — libmlclient.so + libvcmi.so + connector_v13.so 必须在同一 cmake 配置下编译。
6. **connector 重编后 g_adventure_cb 符号解析** — 需 LD_LIBRARY_PATH 正确设置。
7. **VCMI 数据目录** — Mods/core 通过 ~/.local/share/vcmi/Data/→/mnt/d/GAMES/Heroes3/Data/ 链式路径加载。
